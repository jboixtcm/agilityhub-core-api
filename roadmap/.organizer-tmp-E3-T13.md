Result: verified · 2026-09-25 · organizer (Claude)

- **Both steps are done.**
  - The cut-off now reads the latest `PUBLIC` block among `Member.signup` and the blocks of the submissions the member owes, so a legacy `APP_ADD_DOG` block is no boundary.
  - Each payment line is described in its own submission's locale; the internal `Line.submissionId` makes that possible.
  - The three new tests failed on the HEAD code. The readmission test also fails under the naive «`PUBLIC` only» rule (log `04`), and that is why the cut reads the owed blocks.
- **Evidence:**
  - `clean verify`: 533 unit and 915 integration tests;
  - `bin/e3-smoke`: passes twice, on fresh stacks;
  - no contract change: the generated document is byte-identical.
- **The Codex review** (`roadmap/reviews/E3-T13-20260925-1051-codex.md`) has one major: a readmission that created no payment rows, followed by a **legacy** add-dog that overwrote `Member.signup`, loses its boundary. **The organizer rules it out of scope.**
  - The scenario needs `Member.signup` overwritten by an add-dog, which only the code before E3-T10 did.
  - That code never ran outside development databases: there is no production data yet, and the Playoff migration (S18) writes members through its own path.
  - With the current code, `Member.signup` is always the latest public signup or readmission, and the cut is exact.
  - Development databases filled before 25-09 should simply be re-seeded.
