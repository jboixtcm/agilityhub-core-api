---
id: E5-T24
title: Real-core gaps found by the web's E4-W13, and the E5-T22 review (api) — D2 adds a file with the admin's upload, signed local file URLs that authorise themselves, a LEFT member with an INACTIVE dog in the demo seed, the instructor's audit origin, `fields` on exports and filter-values, a member whose plan is gone, the family member's IT, `id` filterable where it is accepted
stage: E5
repo: agilityhub-core-api
thread: A
order: 240
status: verified
depends_on: [E5-T22]
branch:
pr:
updated: 2026-09-26
---

# E5-T24 · Real-core gaps from E4-W13, and the E5-T22 review

## Objective
The web's E4-W13 ran D2's edit of a readmission's reused dog against the real core (web repo, `roadmap/tasks/E4-W13.md`, report questions 1–3). It found two gaps that block ordinary use:
- D2 cannot add a file to a pending dog with the admin's upload.
- The local stack's file URLs want the bearer that a signed URL must not need. Until the release, the local stack is what runs (A31).

The independent review of E5-T22 (`roadmap/reviews/E5-T22-20260926-1457-claude.md`) found four minors. The organizer confirmed and decided them all (see the organizer verification of E5-T22).

Steps 1 and 2 come first: D2 and every upload need them.

The nits of this task's own review go to `docs/INCIDENCIES_OBERTES.md`. Its minors go there too, unless they block the web. Majors come back as a follow-up.

**Each fix comes with a test that fails before the fix.**

## Spec references
- S04:
  - R-04-19 (D2 adds and removes a pending dog's files; amended 26-09: a new file comes from the admin's `POST /attachments/upload-url` with `purpose = DOG_DOCUMENT`);
  - R-04-06 (the reused dog), R-04-08 (the file limit);
  - R-04-09 (a member without a plan; amended 26-09).
- `CONVENCIONS_API.md`:
  - §4 (`fields` on exports and on `filter-values`; amended 26-09);
  - §5 (signed file URLs authorise themselves, on the local profile too; amended 26-09).
- `CATALEG_ESDEVENIMENTS.md`: the envelope's `origin` gains `INSTRUCTOR` (amended 26-09). S14 §3: the audit's `origin` (amended 26-09). S08 R-08-19.
- A31 (the local stack until the release).

## Context to load
- The web's E4-W13 report, questions 1–3. Step 1 and step 2 quote what matters. The evidence is in the web repo (`roadmap/evidence/E4-W13/`: `d2-readmission-add-file-core.json`, `d2-readmission-add-file-probe-core.json`, `13-e2e-core-e3-targeted.log`).
- The E5-T22 review and report (questions 1 and 2).
- `SignupService.claimDocuments`, the `PATCH /dogs/{id}` path in `DogService`, `AttachmentService` (purposes, `putLocal`, `openLocal`, `putSignupLocal`).
- `AttachmentsController` (near `:112`–`:124`: `PUT /api/v1/attachments/uploads/{id}` and `GET /api/v1/attachments/files/{id}`), `SignupFilesController`, `SecurityConfiguration` (near `:60`).
- `CurrentUserFilter` (near `:26`), `DemoSeedActor` (near `:38`), the audit writer, `AuditListProjection` (near `:37`).
- `OpenApiConfiguration` (near `:135`–`:189`), `ListContract.acceptsFields`, `ListExportService` (`selectColumns`), `ListFieldsContractIT.universalList()`.
- `SignupService` (near `:597`), `SignupPlansFollowUpIT` (near `:92`–`:146`).
- The demo seed (`seed:demo`), `seeds/README.md`, `bin/e3-smoke`'s counts.

## Steps
1. **D2 adds a file (E4-W13 question 1, R-04-19).**
   - **What the web found on `d2952d5`:**
     - the drawer sends `PATCH /dogs/{id}` with `{documents: [{type: VACCINATION_CARD, files: [<kept signup key>, <new key>]}]}`. The new key comes from the admin's `POST /attachments/upload-url` (`purpose = DOG_DOCUMENT`). The PATCH answers `400 FILE_NOT_FOUND`;
     - the same PATCH with the kept key only answers 200;
     - the admin gets `403` on `POST /signup/upload-urls`.

     So no ADMIN route yields a key that the PATCH takes as a new file.
   - **The fix:**
     - on a `PENDING` dog, `PATCH /dogs/{id}` takes a new file key from the admin's `POST /attachments/upload-url` with `purpose = DOG_DOCUMENT`, when that key was uploaded in the same club and never claimed;
     - the reused dog of a readmission takes it too. Its other document routes stay frozen;
     - a key of another club, of another purpose, already claimed or never uploaded answers `400 FILE_NOT_FOUND`, as now. A removed key stays refused (E5-T21);
     - the limit of 10 new files per request counts these keys too (R-04-08);
     - update `DogPatch.documents`'s description. The `PATCH /dogs/{id}` description also says that the card requirement applies only when the card's keys change (E5-T23 question 1).
   - **ITs:**
     - an ordinary pending public-signup dog, and a reused dog, each get a new file through the admin's upload;
     - D2's view (`GET /members/{id}/signup`) then lists that file with its stored name;
     - the validation of the reused dog writes the file to the dog (E38).
2. **Signed local file URLs authorise themselves (E4-W13 question 2, CONVENCIONS_API §5, A31).**
   - **What the web found:** the local upload URL, `PUT /api/v1/attachments/uploads/{id}?expires=…&signature=…`, answers `401` to a `PUT` that carries only `Upload.headers`. It requires an ADMIN, INSTRUCTOR or MEMBER bearer on top of its signature. `GET /api/v1/attachments/files/{id}` and `GET /api/v1/signup/files` do the same. A signed URL must authorise itself, as S3's presigned URLs do: the web never sends a bearer to the storage, and an `<img>` cannot.
   - **The fix:**
     - on the local profile, these three endpoints are authorised by their `expires` and `signature` alone. They need no bearer, and a bearer does no harm;
     - the club comes from the signed file, never from a bearer;
     - a wrong signature or an expired URL answers `403`;
     - `PUT /api/v1/signup/uploads` already works without a bearer: keep it.
   - **ITs:**
     - a `PUT` with the URL and `Upload.headers` only answers 2xx, and the file can then be claimed;
     - a tampered signature answers 403, and so does an expired URL;
     - a download URL read without a bearer answers 200, and one of another file's signature answers 403.
3. **A LEFT member with an INACTIVE dog in the demo seed (E4-W13 question 3).**
   - `seed:demo` adds a fictional `LEFT` member with an `INACTIVE` chipped dog that has a vaccination card. Then the web's real-core e2e can reuse a seed dog in a readmission (R-04-06), as E4-W13 step 6 asks.
   - The seed stays idempotent: its second run reports 0 changes.
   - Update `bin/e3-smoke`'s counts if they change, and `seeds/README.md`.
4. **The instructor's audit origin (E5-T22 review #1, S14 §3, amended 26-09).**
   - An instructor's audited action is written with `origin = BACKOFFICE`. `actorRole = INSTRUCTOR` tells it apart.
   - The event envelope keeps `INSTRUCTOR` (`CATALEG_ESDEVENIMENTS.md`, amended 26-09): N-04 and N-05 route on it (S08 R-08-19).
   - Keep E5-T22's read-side mapping for the entries stored earlier.
   - IT: an instructor's audited action stores `BACKOFFICE`.
5. **`fields` on exports and `filter-values` (E5-T22 review #2 and nit #7, CONVENCIONS_API §4, amended 26-09).**
   - Exports honour `fields`: it picks the columns, through `selectColumns`. So each export publishes the same `x-fields` as its list, and a key outside it answers `400 INVALID_FILTER`.
   - `filter-values` does not publish `fields`.
   - The parameter's description no longer points to an `x-fields` that the operation lacks.
   - `@ListContract(acceptsFields = false)` together with `fields` fails at startup.
   - A contract test: no operation publishes `fields` without `x-fields`. Remove the `/export` and `/filter-values` skip from `ListFieldsContractIT` where it hid this.
6. **A member whose own plan is gone (E5-T22 review #3, R-04-09).**
   - A member whose plan is no longer assignable (inactive, or its module off) counts as a member without a plan.
   - `GET /signup` marks no plan `current` for that member.
   - Without `planIdRequested`, `POST /me/dogs/signup` answers `400 VALIDATION_ERROR` with `fieldErrors [{planIdRequested, REQUIRED}]`.
   - IT.
7. **The family member's IT (E5-T22 review #4 and nit #5).**
   - The add-dog IT builds its family member through the family-group signup and its validation (R-04-13), not by rewriting `planId`.
   - It asserts `current: true` on `ABONAT_FAMILIAR` and a quote with the family price.
   - Rename `R_05_19_T_04_09_…` to `R_05_19_T_05_07_…`, and `R_04_09_T_04_21_…` to `R_04_09_…` (AGENTS rule 5).
8. **Filters and nulls (E5-T22 questions 1 and 2).**
   - `/weeks`, `/class-sessions`, `/ring-blocks`, `/training-bookings` and `/bookings` already accept `id`: put it in their `x-filterable`. Contract test.
   - If `TemplateClass.placementId`'s projection sends `null` under COURSES, declare it `["string", "null"]`, as `ClassSession.placementId` is. Test with COURSES on.

## Fixed conventions
- Contract changes as listed (steps 1, 5 and 8). Regenerate `docs/openapi/openapi.json`, and add one `docs/openapi/CHANGELOG.md` entry that tells the web what changes.
- No physical deletion of business data.
- Heavy runs go through the host lock.
- Fictional data only.

## Verification (run all; evidence = command + exit code + last 40 lines; long outputs → `roadmap/evidence/E5-T24/*.log`)
- `./mvnw -q clean verify`.
- `bin/e3-smoke` twice, on a fresh local stack.
- `bin/openapi-snapshot`, then `git diff --stat docs/openapi/`.

## Definition of Done
- Steps 1–8 are done.
- Steps 1, 2, 4, 5, 6 and 8 each have a test that fails before the fix.

## Evidence to return
- The command outputs.
- A step → test table.

---

## Executor report

### Summary
Steps 1–8 are done.
- **Failing before the fix.** Steps 1, 2, 4, 5, 6 and 8 each have a test that fails on the unfixed tree, at its intended assertion (log `05`: 13 tests, 11 failures). Step 6's `400 REQUIRED` guard is also shown failing on its own (log `07`).
- **Verification.**
  - `./mvnw -q clean verify` (through the host lock) exits 0: 578 unit and 1027 integration tests, 0 failures (`11-test-summary.log`).
  - `bin/e3-smoke` exits 0 twice. Each seed command prints `0 changes` on its second run.
  - `bin/openapi-snapshot` exits 0. One `docs/openapi/CHANGELOG.md` entry tells the web what changes.
- **Beyond the list.** Step 6's IT found that `GET /signup` answered `422 PLAN_NOT_AVAILABLE` to a member whose plan is gone, because it quoted that plan's add-dog options. That is fixed.
- **Real stack.** `bin/e3-smoke` now reproduces the web's E4-W13 flow:
  - it readmits the demo's LEFT member, reusing its INACTIVE dog;
  - D2 adds a file with the ADMIN's upload;
  - the PUT and the download carry no bearer.

### What was done, per step
- **Step 1 (R-04-19, R-04-08, R-04-06).**
  - `SignupService.claimDocuments` (`:544`): in a D2 edit, a new key that is not a signup key goes to the new `AttachmentService.claimDogDocument` (`:85`).
  - The key must be a `DOG_DOCUMENT` grant of the club (tenant-scoped lookup), uploaded by the caller, not bound to another dog or type, with its bytes in storage.
  - Anything else answers `400 FILE_NOT_FOUND`. A size or MIME mismatch answers `FILE_TOO_LARGE` or `FILE_TYPE_NOT_ALLOWED`, as for signup keys.
  - The stored row keeps the name the PATCH sends, and `uploadedByAccountId`. Its `id` is the key, as in S03.
  - Unchanged:
    - E5-T21's removed-key refusal runs first;
    - R-04-08's limit already counts every key that is not kept;
    - a submission still takes signup keys only.
  - The descriptions of `DogPatch.documents` and `PATCH /dogs/{id}` are updated. The latter now says the card requirement applies only when the card's keys change (E5-T23 question 1).
- **Step 2 (CONVENCIONS_API §5, A31).**
  - New `SignedFileRequests` matches `PUT /api/v1/attachments/uploads/{id}`, `GET /api/v1/attachments/files/{id}` and `GET /api/v1/signup/files`.
  - `SecurityConfiguration`:
    - `permitAll` on these routes;
    - a bearer-token resolver that never reads the bearer there, so an expired or invalid one does no harm.
  - `TenantFilter`: the three routes are global, so neither the host nor a bearer gives the tenant.
  - `AttachmentService.putLocal` and `openLocal`:
    - check the signature first (`403`);
    - then load the grant by its signed id (`UploadGrantRepository.signed`);
    - no longer check the caller's account.
  - The `@PreAuthorize` on the three controller methods is removed. `PUT /api/v1/signup/uploads` is unchanged.
- **Step 3.** `DemoDataset.Spec` gains `leftDogs`, applied to the LEFT members in order. `DemoSeedService` gives each one:
  - the fictional identity document (normalised by the country profile);
  - an INACTIVE dog (`MEMBER_LEFT` at the member's `leftAt`, with a level and a chip);
  - the required documents as PENDING, then its vaccination card received through the real upload and claim.

  `seeds/demo-canic.yaml` sets `99000001C` / «Demo Boira» / `941000000000805` on member 192. The counts gain `inactiveDogs`. `seeds/README.md` documents it. `bin/e3-smoke`'s counts do not change (184 ACTIVE members, 242 ACTIVE dogs); the new readmission section restores them after its rejection.
- **Step 4.** `AuditWriter.auditOrigin` (`:60`) writes an instructor's origin as `BACKOFFICE`. `CurrentUserFilter` and `DemoSeedActor` keep `INSTRUCTOR` for the event envelope. `AuditListProjection`'s read mapping stays, for older entries.
- **Step 5.**
  - The 6 real exports declare their list's `fields`. `GET /invoices/export`, `GET /notifications/export` (contract only) and `GET /activities/filter-values` set `acceptsFields = false`.
  - `ListExportService.fieldColumns` (`:47`): without `columns`, `fields`' keys that are columns are the columns, in their order. None → `400 INVALID_FILTER`; a key outside `x-fields` is already refused by `ListQuery.parse`.
  - The customizer's `fields` description now names this operation's `x-fields`.
  - New `ListContractValidation`, run at startup, refuses a `@ListContract` that would publish:
    - `acceptsFields = false` with `fields`;
    - `fields` without `x-fields`;
    - `x-fields` without `fields`.
  - `ListFieldsContractIT.universalList` no longer skips `/export` and `/filter-values` by path: it needs a JSON page response.
- **Step 6.** `SignupService.ownPlan` (`:604`): the member's plan while it is assignable, else null. `addDog` defaults to it, so a gone plan leads to `400 REQUIRED`. `GET /signup` uses it for `additionalDogOptions` and `planQuotes`, which fixes the 422. Two descriptions updated.
- **Step 7.** `SignupPlansFollowUpIT.familyMember()`: an ACTIVE holder, then a public signup claiming its family group, then the D2 validation with D2's proposal. The test asserts `ABONAT_FAMILIAR`, its price and the shared group. The family test asserts `current: true` and the quotes (below). Two renames.
- **Step 8.** `id` added to the `x-filterable` of `/weeks`, `/class-sessions`, `/ring-blocks`, `/training-bookings`, `/training-bookings/export` and `/bookings`. `TemplateClass.placementId` is `nullable`; the view sends `null` without a placement, and always without COURSES.

### Evidence

**1. `/Users/jordib/Dropbox/Documents/SOFTWARE_CANIC/05-desenvolupament/roadmap-kit/mac/heavy.sh ./mvnw -q clean verify > roadmap/evidence/E5-T24/10-clean-verify.log 2>&1; echo "exit $?"`: exit 0.**
- No host-lock wait was reported.
- Earlier failing attempt: `08-clean-verify.log`, exit 1, 3 of 1027 ITs failed on expectations this task changes on purpose:
  - `SignupIT:193` expected `401` on a signed download without a bearer;
  - `E4ContractIT:300` and `E5ContractIT:283` compare exact `x-filterable` lists without `id`.

  I updated them (and `SignupIT:199`, another club's host and bearer now get the file), then re-ran the three (`09-after-verify-fixes-targeted.log`, exit 0).
- Summary (`python3 roadmap/evidence/E5-T24/11-test-summary.py`, exit 0, `11-test-summary.log`):
  - surefire: tests=578 failures=0 errors=0 skipped=0 (127 classes);
  - failsafe: tests=1027 failures=0 errors=0 skipped=0 (87 classes);
  - every class and each of the 25 methods added or changed: PASS (per-class lines in the log).

Last 40 lines of `10-clean-verify.log` (with `-q` a green build prints no summary):
```
2026-09-26T16:19:31.016+02:00  INFO 68222 --- [agilityhub-core-api] [           main] org.mongodb.driver.client                : MongoClient with metadata {"driver": {"name": "mongo-java-driver|sync|spring-boot", "version": "5.5.2"}, "os": {"type": "Darwin", "name": "Mac OS X", "architecture": "aarch64", "version": "26.4.1"}, "platform": "Java/Homebrew/21.0.12.1"} created with settings MongoClientSettings{readPreference=primary, writeConcern=WriteConcern{w=null, wTimeout=null ms, journal=null}, retryWrites=true, retryReads=true, readConcern=ReadConcern{level=null}, credential=null, transportSettings=null, commandListeners=[io.micrometer.core.instrument.binder.mongodb.MongoMetricsCommandListener@64bca416], codecRegistry=ProvidersCodecRegistry{codecProviders=[ValueCodecProvider{}, BsonValueCodecProvider{}, DBRefCodecProvider{}, DBObjectCodecProvider{}, DocumentCodecProvider{}, CollectionCodecProvider{}, IterableCodecProvider{}, MapCodecProvider{}, GeoJsonCodecProvider{}, GridFSFileCodecProvider{}, Jsr310CodecProvider{}, JsonObjectCodecProvider{}, BsonCodecProvider{}, EnumCodecProvider{}, com.mongodb.client.model.mql.ExpressionCodecProvider@74b3e7f2, com.mongodb.Jep395RecordCodecProvider@152e15ea, com.mongodb.KotlinCodecProvider@761b5700]}, loggerSettings=LoggerSettings{maxDocumentLength=1000}, clusterSettings={hosts=[localhost:52022], srvServiceName=mongodb, mode=SINGLE, requiredClusterType=UNKNOWN, requiredReplicaSetName='null', serverSelector='null', clusterListeners='[]', serverSelectionTimeout='30000 ms', localThreshold='15 ms'}, socketSettings=SocketSettings{connectTimeoutMS=10000, readTimeoutMS=0, receiveBufferSize=0, proxySettings=ProxySettings{host=null, port=null, username=null, password=null}}, heartbeatSocketSettings=SocketSettings{connectTimeoutMS=10000, readTimeoutMS=10000, receiveBufferSize=0, proxySettings=ProxySettings{host=null, port=null, username=null, password=null}}, connectionPoolSettings=ConnectionPoolSettings{maxSize=100, minSize=0, maxWaitTimeMS=120000, maxConnectionLifeTimeMS=0, maxConnectionIdleTimeMS=0, maintenanceInitialDelayMS=0, maintenanceFrequencyMS=60000, connectionPoolListeners=[io.micrometer.core.instrument.binder.mongodb.MongoMetricsConnectionPoolListener@35bdcc1e], maxConnecting=2}, serverSettings=ServerSettings{heartbeatFrequencyMS=10000, minHeartbeatFrequencyMS=500, serverMonitoringMode=AUTO, serverListeners='[]', serverMonitorListeners='[]'}, sslSettings=SslSettings{enabled=false, invalidHostNameAllowed=false, context=null}, applicationName='null', compressorList=[], uuidRepresentation=JAVA_LEGACY, serverApi=null, autoEncryptionSettings=null, dnsClient=null, inetAddressResolver=null, contextProvider=null, timeoutMS=null}
2026-09-26T16:19:31.018+02:00  INFO 68222 --- [agilityhub-core-api] [localhost:52022] org.mongodb.driver.cluster               : Monitor thread successfully connected to server with description ServerDescription{address=localhost:52022, type=REPLICA_SET_PRIMARY, cryptd=false, state=CONNECTED, ok=true, minWireVersion=0, maxWireVersion=21, maxDocumentSize=16777216, logicalSessionTimeoutMinutes=30, roundTripTimeNanos=1336083, minRoundTripTimeNanos=0, setName='docker-rs', canonicalAddress=d23e69cb9ddf:27017, hosts=[d23e69cb9ddf:27017], passives=[], arbiters=[], primary='d23e69cb9ddf:27017', tagSet=TagSet{[]}, electionId=7fffffff0000000000000001, setVersion=1, topologyVersion=TopologyVersion{processId=6ab7d35d22776c5fea6adb8f, counter=6}, lastWriteDate=Sat Sep 26 16:19:30 CEST 2026, lastUpdateTimeNanos=236176274893708}
2026-09-26T16:19:31.468+02:00  INFO 68222 --- [agilityhub-core-api] [           main] c.a.core.migration.PlayoffMigrationIT    : Started PlayoffMigrationIT in 0.689 seconds (process running for 279.899)
E5-T07 outbox backlog discarded before PlayoffMigrationIT: 158 PENDING records (more than one dispatch() batch)
E5-T07 outbox backlog discarded before PlayoffMigrationIT: 158 PENDING records (more than one dispatch() batch)
E5-T07 outbox backlog discarded before PlayoffMigrationIT: 158 PENDING records (more than one dispatch() batch)
E5-T07 outbox backlog discarded before PlayoffMigrationIT: 162 PENDING records (more than one dispatch() batch)
2026-09-26T16:19:38.249+02:00  INFO 68222 --- [agilityhub-core-api] [localhost:52417] org.mongodb.driver.cluster               : Exception in monitor thread while connecting to server localhost:52417

com.mongodb.MongoSocketOpenException: Exception opening socket
	at com.mongodb.internal.connection.SocketStream.lambda$open$0(SocketStream.java:85) ~[mongodb-driver-core-5.5.2.jar:na]
	at java.base/java.util.Optional.orElseThrow(Optional.java:403) ~[na:na]
	at com.mongodb.internal.connection.SocketStream.open(SocketStream.java:85) ~[mongodb-driver-core-5.5.2.jar:na]
	at com.mongodb.internal.connection.InternalStreamConnection.open(InternalStreamConnection.java:233) ~[mongodb-driver-core-5.5.2.jar:na]
	at com.mongodb.internal.connection.DefaultServerMonitor$ServerMonitor.lookupServerDescription(DefaultServerMonitor.java:219) ~[mongodb-driver-core-5.5.2.jar:na]
	at com.mongodb.internal.connection.DefaultServerMonitor$ServerMonitor.run(DefaultServerMonitor.java:176) ~[mongodb-driver-core-5.5.2.jar:na]
Caused by: java.net.ConnectException: Connection refused
	at java.base/sun.nio.ch.Net.pollConnect(Native Method) ~[na:na]
	at java.base/sun.nio.ch.Net.pollConnectNow(Net.java:694) ~[na:na]
	at java.base/sun.nio.ch.NioSocketImpl.timedFinishConnect(NioSocketImpl.java:542) ~[na:na]
	at java.base/sun.nio.ch.NioSocketImpl.connect(NioSocketImpl.java:592) ~[na:na]
	at java.base/java.net.SocksSocketImpl.connect(SocksSocketImpl.java:327) ~[na:na]
	at java.base/java.net.Socket.connect(Socket.java:751) ~[na:na]
	at com.mongodb.internal.connection.SocketStreamHelper.initialize(SocketStreamHelper.java:76) ~[mongodb-driver-core-5.5.2.jar:na]
	at com.mongodb.internal.connection.SocketStream.initializeSocket(SocketStream.java:104) ~[mongodb-driver-core-5.5.2.jar:na]
	at com.mongodb.internal.connection.SocketStream.open(SocketStream.java:79) ~[mongodb-driver-core-5.5.2.jar:na]
	... 3 common frames omitted

E5-T07 outbox backlog discarded before PlayoffMigrationIT: 158 PENDING records (more than one dispatch() batch)
E5-T07 outbox backlog discarded before PlayoffMigrationIT: 158 PENDING records (more than one dispatch() batch)
E5-T07 outbox backlog discarded before PlayoffMigrationIT: 158 PENDING records (more than one dispatch() batch)
E5-T07 outbox backlog discarded before PlayoffMigrationIT: 158 PENDING records (more than one dispatch() batch)
E5-T07 outbox backlog discarded before PlayoffMigrationIT: 158 PENDING records (more than one dispatch() batch)
E5-T07 outbox backlog discarded before PlayoffMigrationIT: 158 PENDING records (more than one dispatch() batch)
E5-T07 outbox backlog discarded before PlayoffMigrationIT: 159 PENDING records (more than one dispatch() batch)
E5-T07 outbox backlog discarded before PlayoffMigrationIT: 158 PENDING records (more than one dispatch() batch)
E5-T07 outbox backlog discarded before PlayoffMigrationIT: 158 PENDING records (more than one dispatch() batch)
E5-T07 outbox backlog discarded before PlayoffMigrationIT: 158 PENDING records (more than one dispatch() batch)
E5-T07 outbox backlog discarded before PlayoffMigrationIT: 158 PENDING records (more than one dispatch() batch)
E5-T07 outbox backlog discarded before PlayoffMigrationIT: 159 PENDING records (more than one dispatch() batch)
```

**2. `/Users/jordib/Dropbox/Documents/SOFTWARE_CANIC/05-desenvolupament/roadmap-kit/mac/heavy.sh bin/e3-smoke > roadmap/evidence/E5-T24/12-e3-smoke-run1.log 2>&1; echo "exit $?"`: exit 0.** Each run builds the working tree image and starts its own disposable Compose stack with an empty database (`Smoke database must be empty`). Lines 4–7: `PASS club:apply seeds/club-canic.yaml run 2: 0 changes` and `PASS seed:demo --club=canic --seed=42 run 2: 0 changes`. Last 40 lines:
```
curl POST /api/v1/members/a722974d-054b-461c-b64f-14fd414d634b/validation -> 200
PASS D1 dashboard + counters: pending=3, olderThanWarn=1, activeMembers=185
PASS mailbox N-02 correlated by notificationId; locale=ca; token=[truncated]
curl POST /oauth2/token -> 200
curl GET /api/v1/me -> 200
PASS public family signup -> dry run -> validation/number increment -> N-02 -> welcome session
curl POST /api/v1/me/dogs/signup -> 201
PASS N-01 in the member APP inbox, scoped to account and entity
PASS D1 dashboard + counters: pending=4, olderThanWarn=1, activeMembers=185
curl GET /api/v1/members/a722974d-054b-461c-b64f-14fd414d634b/signup -> 200
PASS add-dog D2: only the member's current payment method, not assignable
curl POST /api/v1/members/a722974d-054b-461c-b64f-14fd414d634b/validation -> 200
PASS N-37 in the member APP inbox, scoped to account and entity
PASS D1 dashboard + counters: pending=3, olderThanWarn=1, activeMembers=185
curl POST /api/v1/signup -> 201
PASS D1 dashboard + counters: pending=4, olderThanWarn=1, activeMembers=185
curl GET /api/v1/members/32380175-c00f-4e2e-99fb-ef7712e08a60/signup -> 200
curl POST /api/v1/members/32380175-c00f-4e2e-99fb-ef7712e08a60/rejection -> 200
PASS mailbox N-03 correlated by notificationId; locale=ca
PASS D1 dashboard + counters: pending=3, olderThanWarn=1, activeMembers=185
PASS signup without family -> rejection -> N-03; D1 counters restored
curl POST /api/v1/signup -> 201
PASS D1 dashboard + counters: pending=4, olderThanWarn=1, activeMembers=185
curl GET /api/v1/members/c26e9b5e-3ea0-3b1f-9b0b-6d7daf4fe576/signup -> 200
PASS readmission of the demo's LEFT member reuses its INACTIVE seed dog and its received card
curl POST /api/v1/attachments/upload-url -> 201
curl PUT /api/v1/attachments/uploads/6261e958-c2e2-45ad-9261-bc67886ab129 -> 204
curl PATCH /api/v1/dogs/f374594a-9f64-3ae2-b1f5-c9089fe73759 -> 200
curl GET /api/v1/members/c26e9b5e-3ea0-3b1f-9b0b-6d7daf4fe576/signup -> 200
curl GET /api/v1/attachments/files/6261e958-c2e2-45ad-9261-bc67886ab129 -> 200
PASS D2 adds a file with the ADMIN's DOG_DOCUMENT upload; the signed upload and download URLs need no bearer
curl POST /api/v1/members/c26e9b5e-3ea0-3b1f-9b0b-6d7daf4fe576/rejection -> 200
PASS D1 dashboard + counters: pending=3, olderThanWarn=1, activeMembers=185
PASS rejected readmission restores the LEFT member and its INACTIVE dog; D1 counters restored
curl GET /api/v1/parameters/signup.enabled -> 200
curl PUT /api/v1/parameters/signup.enabled -> 200
curl POST /api/v1/signup -> 422 SIGNUP_CLOSED
PASS signup.enabled=false closes the form; canonical catalog status is 422 SIGNUP_CLOSED
PASS T-04-34 / T-14-11: E3 backend gate completed; credentials and capabilities withheld
Removed disposable Compose services, volumes and private mailbox; existing stacks preserved
```

**3. The same command, `> roadmap/evidence/E5-T24/13-e3-smoke-run2.log`: exit 0.** A second disposable stack, lines 4–7 again `0 changes` on each seed's second run. Last 40 lines:
```
curl POST /api/v1/members/7c7cd225-9ce2-4743-9431-802b49252953/validation -> 200
PASS D1 dashboard + counters: pending=3, olderThanWarn=1, activeMembers=185
PASS mailbox N-02 correlated by notificationId; locale=ca; token=[truncated]
curl POST /oauth2/token -> 200
curl GET /api/v1/me -> 200
PASS public family signup -> dry run -> validation/number increment -> N-02 -> welcome session
curl POST /api/v1/me/dogs/signup -> 201
PASS N-01 in the member APP inbox, scoped to account and entity
PASS D1 dashboard + counters: pending=4, olderThanWarn=1, activeMembers=185
curl GET /api/v1/members/7c7cd225-9ce2-4743-9431-802b49252953/signup -> 200
PASS add-dog D2: only the member's current payment method, not assignable
curl POST /api/v1/members/7c7cd225-9ce2-4743-9431-802b49252953/validation -> 200
PASS N-37 in the member APP inbox, scoped to account and entity
PASS D1 dashboard + counters: pending=3, olderThanWarn=1, activeMembers=185
curl POST /api/v1/signup -> 201
PASS D1 dashboard + counters: pending=4, olderThanWarn=1, activeMembers=185
curl GET /api/v1/members/c3f7707e-57ac-43ed-ba87-4f1cec4fcb2b/signup -> 200
curl POST /api/v1/members/c3f7707e-57ac-43ed-ba87-4f1cec4fcb2b/rejection -> 200
PASS mailbox N-03 correlated by notificationId; locale=ca
PASS D1 dashboard + counters: pending=3, olderThanWarn=1, activeMembers=185
PASS signup without family -> rejection -> N-03; D1 counters restored
curl POST /api/v1/signup -> 201
PASS D1 dashboard + counters: pending=4, olderThanWarn=1, activeMembers=185
curl GET /api/v1/members/a4165453-601c-34df-9311-0f0f93e4fe3c/signup -> 200
PASS readmission of the demo's LEFT member reuses its INACTIVE seed dog and its received card
curl POST /api/v1/attachments/upload-url -> 201
curl PUT /api/v1/attachments/uploads/a90637a7-f0b0-4bd1-854f-ccf04c1fc927 -> 204
curl PATCH /api/v1/dogs/ccd09084-ec9d-3b88-990a-43cee320d7a7 -> 200
curl GET /api/v1/members/a4165453-601c-34df-9311-0f0f93e4fe3c/signup -> 200
curl GET /api/v1/attachments/files/a90637a7-f0b0-4bd1-854f-ccf04c1fc927 -> 200
PASS D2 adds a file with the ADMIN's DOG_DOCUMENT upload; the signed upload and download URLs need no bearer
curl POST /api/v1/members/a4165453-601c-34df-9311-0f0f93e4fe3c/rejection -> 200
PASS D1 dashboard + counters: pending=3, olderThanWarn=1, activeMembers=185
PASS rejected readmission restores the LEFT member and its INACTIVE dog; D1 counters restored
curl GET /api/v1/parameters/signup.enabled -> 200
curl PUT /api/v1/parameters/signup.enabled -> 200
curl POST /api/v1/signup -> 422 SIGNUP_CLOSED
PASS signup.enabled=false closes the form; canonical catalog status is 422 SIGNUP_CLOSED
PASS T-04-34 / T-14-11: E3 backend gate completed; credentials and capabilities withheld
Removed disposable Compose services, volumes and private mailbox; existing stacks preserved
```

**4. `bin/openapi-snapshot > roadmap/evidence/E5-T24/14-openapi-snapshot.log 2>&1; echo "exit $?"`: exit 0.** The full output (72 lines, Spring start-up) is in the log. Its last line is `Updated docs/openapi/openapi.json`. The regeneration is stable: the same 100-line diff as the development regeneration (`01-openapi-snapshot-dev.log`). Last 40 lines:
```
 =========|_|==============|___/=/_/_/_/

 :: Spring Boot ::               (v3.5.16)

2026-09-26T16:23:28.669+02:00  INFO 70022 --- [agilityhub-core-api] [           main] c.a.c.configuration.OpenApiSnapshotTest  : Starting OpenApiSnapshotTest using Java 21.0.12.1 with PID 70022 (started by jordib in /Users/jordib/dev/agilityhub/agilityhub-core-api)
2026-09-26T16:23:28.670+02:00  INFO 70022 --- [agilityhub-core-api] [           main] c.a.c.configuration.OpenApiSnapshotTest  : The following 1 profile is active: "test"
2026-09-26T16:23:29.660+02:00  INFO 70022 --- [agilityhub-core-api] [           main] .s.d.r.c.RepositoryConfigurationDelegate : Bootstrapping Spring Data MongoDB repositories in DEFAULT mode.
2026-09-26T16:23:29.694+02:00  INFO 70022 --- [agilityhub-core-api] [           main] .s.d.r.c.RepositoryConfigurationDelegate : Finished Spring Data repository scanning in 30 ms. Found 0 MongoDB repository interfaces.
2026-09-26T16:23:30.067+02:00  INFO 70022 --- [agilityhub-core-api] [           main] o.s.b.w.embedded.tomcat.TomcatWebServer  : Tomcat initialized with port 0 (http)
2026-09-26T16:23:30.075+02:00  INFO 70022 --- [agilityhub-core-api] [           main] o.apache.catalina.core.StandardService   : Starting service [Tomcat]
2026-09-26T16:23:30.075+02:00  INFO 70022 --- [agilityhub-core-api] [           main] o.apache.catalina.core.StandardEngine    : Starting Servlet engine: [Apache Tomcat/10.1.55]
2026-09-26T16:23:30.099+02:00  INFO 70022 --- [agilityhub-core-api] [           main] o.a.c.c.C.[Tomcat].[localhost].[/]       : Initializing Spring embedded WebApplicationContext
2026-09-26T16:23:30.100+02:00  INFO 70022 --- [agilityhub-core-api] [           main] w.s.c.ServletWebServerApplicationContext : Root WebApplicationContext: initialization completed in 1424 ms
2026-09-26T16:23:30.204+02:00  INFO 70022 --- [agilityhub-core-api] [           main] org.mongodb.driver.client                : MongoClient with metadata {"driver": {"name": "mongo-java-driver|sync|spring-boot", "version": "5.5.2"}, "os": {"type": "Darwin", "name": "Mac OS X", "architecture": "aarch64", "version": "26.4.1"}, "platform": "Java/Homebrew/21.0.12.1"} created with settings MongoClientSettings{readPreference=primary, writeConcern=WriteConcern{w=null, wTimeout=null ms, journal=null}, retryWrites=true, retryReads=true, readConcern=ReadConcern{level=null}, credential=null, transportSettings=null, commandListeners=[io.micrometer.core.instrument.binder.mongodb.MongoMetricsCommandListener@71644fdf], codecRegistry=ProvidersCodecRegistry{codecProviders=[ValueCodecProvider{}, BsonValueCodecProvider{}, DBRefCodecProvider{}, DBObjectCodecProvider{}, DocumentCodecProvider{}, CollectionCodecProvider{}, IterableCodecProvider{}, MapCodecProvider{}, GeoJsonCodecProvider{}, GridFSFileCodecProvider{}, Jsr310CodecProvider{}, JsonObjectCodecProvider{}, BsonCodecProvider{}, EnumCodecProvider{}, com.mongodb.client.model.mql.ExpressionCodecProvider@58072e51, com.mongodb.Jep395RecordCodecProvider@6d275f8e, com.mongodb.KotlinCodecProvider@4be5469e]}, loggerSettings=LoggerSettings{maxDocumentLength=1000}, clusterSettings={hosts=[localhost:52705], srvServiceName=mongodb, mode=SINGLE, requiredClusterType=UNKNOWN, requiredReplicaSetName='null', serverSelector='null', clusterListeners='[]', serverSelectionTimeout='30000 ms', localThreshold='15 ms'}, socketSettings=SocketSettings{connectTimeoutMS=10000, readTimeoutMS=0, receiveBufferSize=0, proxySettings=ProxySettings{host=null, port=null, username=null, password=null}}, heartbeatSocketSettings=SocketSettings{connectTimeoutMS=10000, readTimeoutMS=10000, receiveBufferSize=0, proxySettings=ProxySettings{host=null, port=null, username=null, password=null}}, connectionPoolSettings=ConnectionPoolSettings{maxSize=100, minSize=0, maxWaitTimeMS=120000, maxConnectionLifeTimeMS=0, maxConnectionIdleTimeMS=0, maintenanceInitialDelayMS=0, maintenanceFrequencyMS=60000, connectionPoolListeners=[io.micrometer.core.instrument.binder.mongodb.MongoMetricsConnectionPoolListener@182ce28a], maxConnecting=2}, serverSettings=ServerSettings{heartbeatFrequencyMS=10000, minHeartbeatFrequencyMS=500, serverMonitoringMode=AUTO, serverListeners='[]', serverMonitorListeners='[]'}, sslSettings=SslSettings{enabled=false, invalidHostNameAllowed=false, context=null}, applicationName='null', compressorList=[], uuidRepresentation=JAVA_LEGACY, serverApi=null, autoEncryptionSettings=null, dnsClient=null, inetAddressResolver=null, contextProvider=null, timeoutMS=null}
2026-09-26T16:23:30.211+02:00  INFO 70022 --- [agilityhub-core-api] [localhost:52705] org.mongodb.driver.cluster               : Monitor thread successfully connected to server with description ServerDescription{address=localhost:52705, type=REPLICA_SET_PRIMARY, cryptd=false, state=CONNECTED, ok=true, minWireVersion=0, maxWireVersion=21, maxDocumentSize=16777216, logicalSessionTimeoutMinutes=30, roundTripTimeNanos=11119583, minRoundTripTimeNanos=0, setName='docker-rs', canonicalAddress=503312aeda17:27017, hosts=[503312aeda17:27017], passives=[], arbiters=[], primary='503312aeda17:27017', tagSet=TagSet{[]}, electionId=7fffffff0000000000000001, setVersion=1, topologyVersion=TopologyVersion{processId=6ab7d55f20445caeb53b14c9, counter=6}, lastWriteDate=Sat Sep 26 16:23:28 CEST 2026, lastUpdateTimeNanos=236415468019250}
2026-09-26T16:23:30.638+02:00  INFO 70022 --- [agilityhub-core-api] [           main] o.s.boot.web.servlet.RegistrationBean    : Filter rateLimitFilter was not registered (disabled)
2026-09-26T16:23:32.916+02:00  INFO 70022 --- [agilityhub-core-api] [           main] o.a.c.c.C.[Tomcat].[localhost].[/]       : Initializing Spring TestDispatcherServlet ''
2026-09-26T16:23:32.916+02:00  INFO 70022 --- [agilityhub-core-api] [           main] o.s.t.web.servlet.TestDispatcherServlet  : Initializing Servlet ''
2026-09-26T16:23:32.917+02:00  INFO 70022 --- [agilityhub-core-api] [           main] o.s.t.web.servlet.TestDispatcherServlet  : Completed initialization in 1 ms
2026-09-26T16:23:32.978+02:00  INFO 70022 --- [agilityhub-core-api] [           main] o.s.b.w.embedded.tomcat.TomcatWebServer  : Tomcat started on port 52711 (http) with context path '/'
2026-09-26T16:23:33.006+02:00  INFO 70022 --- [agilityhub-core-api] [           main] o.s.b.w.embedded.tomcat.TomcatWebServer  : Tomcat initialized with port 0 (http)
2026-09-26T16:23:33.007+02:00  INFO 70022 --- [agilityhub-core-api] [           main] o.apache.catalina.core.StandardService   : Starting service [Tomcat]
2026-09-26T16:23:33.007+02:00  INFO 70022 --- [agilityhub-core-api] [           main] o.apache.catalina.core.StandardEngine    : Starting Servlet engine: [Apache Tomcat/10.1.55]
2026-09-26T16:23:33.015+02:00  INFO 70022 --- [agilityhub-core-api] [           main] o.a.c.c.C.[Tomcat-1].[localhost].[/]     : Initializing Spring embedded WebApplicationContext
2026-09-26T16:23:33.016+02:00  INFO 70022 --- [agilityhub-core-api] [           main] w.s.c.ServletWebServerApplicationContext : Root WebApplicationContext: initialization completed in 36 ms
2026-09-26T16:23:33.021+02:00  INFO 70022 --- [agilityhub-core-api] [           main] o.s.b.a.e.web.EndpointLinksResolver      : Exposing 2 endpoints beneath base path '/actuator'
2026-09-26T16:23:33.037+02:00  INFO 70022 --- [agilityhub-core-api] [           main] o.s.b.w.embedded.tomcat.TomcatWebServer  : Tomcat started on port 52712 (http) with context path '/'
2026-09-26T16:23:33.044+02:00  INFO 70022 --- [agilityhub-core-api] [           main] c.a.c.configuration.OpenApiSnapshotTest  : Started OpenApiSnapshotTest in 4.566 seconds (process running for 7.037)
Mockito is currently self-attaching to enable the inline-mock-maker. This will no longer work in future releases of the JDK. Please add Mockito as an agent to your build as described in Mockito's documentation: https://javadoc.io/doc/org.mockito/mockito-core/latest/org.mockito/org/mockito/Mockito.html#0.3
WARNING: A Java agent has been loaded dynamically (/Users/jordib/.m2/repository/net/bytebuddy/byte-buddy-agent/1.17.8/byte-buddy-agent-1.17.8.jar)
WARNING: If a serviceability tool is in use, please run with -XX:+EnableDynamicAgentLoading to hide this warning
WARNING: If a serviceability tool is not in use, please run with -Djdk.instrument.traceUsage for more information
WARNING: Dynamic loading of agents will be disallowed by default in a future release
OpenJDK 64-Bit Server VM warning: Sharing is only supported for boot loader classes because bootstrap classpath has been appended
2026-09-26T16:23:35.181+02:00  INFO 70022 --- [agilityhub-core-api] [           main] o.springdoc.api.AbstractOpenApiResource  : Init duration for springdoc-openapi is: 1437 ms
2026-09-26T16:23:35.672+02:00  INFO 70022 --- [agilityhub-core-api] [omcat-handler-0] o.a.c.c.C.[Tomcat].[localhost].[/]       : Initializing Spring DispatcherServlet 'dispatcherServlet'
2026-09-26T16:23:35.672+02:00  INFO 70022 --- [agilityhub-core-api] [omcat-handler-0] o.s.web.servlet.DispatcherServlet        : Initializing Servlet 'dispatcherServlet'
2026-09-26T16:23:35.672+02:00  INFO 70022 --- [agilityhub-core-api] [omcat-handler-0] o.s.web.servlet.DispatcherServlet        : Completed initialization in 0 ms
2026-09-26T16:23:36.660+02:00  INFO 70022 --- [agilityhub-core-api] [           main] o.springdoc.api.AbstractOpenApiResource  : Init duration for springdoc-openapi is: 932 ms
Updated docs/openapi/openapi.json
```

**5. `git diff --stat docs/openapi/ > roadmap/evidence/E5-T24/15-openapi-diff-stat.log 2>&1; echo "exit $?"`: exit 0.** Full output:
```
 docs/openapi/CHANGELOG.md |  35 ++++++++++++++++
 docs/openapi/openapi.json | 100 ++++++++++++++++++++--------------------------
 2 files changed, 79 insertions(+), 56 deletions(-)
```

**6. Before-fix runs (Definition of Done).** The unfixed trees were built under `target/` by a script; the working tree was never reverted.
- `python3 roadmap/evidence/E5-T24/02-before-fix-tree.py`, exit 0 (`04-before-fix-tree.log`). Tree A:
  - the 28 changed files under `src/main`, `seeds` and `docs/openapi` get their `HEAD` (`e9a58c1`) content back (25 main files, the snapshot and 2 seed files);
  - the 2 new main files are removed.
  - It keeps the current tests except 3 classes that call new APIs and cannot compile there: `AttachmentServiceTest` and `DemoSeedsIT` get their `HEAD` content back, and `ListContractValidationTest` is removed.
- `./mvnw -f target/before-fix/pom.xml '-Dtest=<the 13 new/changed methods, full list in the log>' -Dsurefire.failIfNoSpecifiedTests=false test`: exit 1 (`05-before-fix-targeted.log`). 13 tests, 11 failures, each at its intended assertion:
  - step 1: `SignupSecurityFixesIT.R_04_19_R_04_08_…:826` and `R_04_19_R_04_06_…:874`: the D2 PATCH with the ADMIN's key answers `400 FILE_NOT_FOUND` instead of 200;
  - step 2:
    - `CONVENCIONS_API_5_R_04_19_…:924`: a tampered signature gets 401, not 403 (the route wants a bearer);
    - `CONVENCIONS_API_5_signedDownloadUrls…:954`: the signup file's URL without a bearer, on an unknown host, gets 404, not 200;
    - `CensusIT.T_03_23_…:542`: another club's bearer gets 404, not 200;
  - step 4: `CalendarIT.R_14_09_…:296`: the instructor's audit origin is `INSTRUCTOR`, not `BACKOFFICE`;
  - step 5:
    - `UniversalListsIT.CONVENCIONS_API_4_T_03_11_…:203`: `fields=id,memberNumber,version,fullName` exports the 4 default columns instead of the 2 requested;
    - `ListFieldsContractIT.CONVENCIONS_API_4_noOperation…:269`: the 12 operations with `fields` but no `x-fields`, and the old description;
  - step 6: `SignupPlansFollowUpIT.R_04_09_aMemberWhoseOwnPlanIsGoneMustChooseOne:228`: `GET /signup` answers 422 for that member;
  - step 8:
    - `ListFieldsContractIT.CONVENCIONS_API_4_everyEngineList…:290`: `id` is missing from the 5 lists;
    - `PlanningIT.T_06_01_…:139`: `$.classes[0].placementId: null found, string expected`.
  - Passing there, as expected: step 7's two tests (the family test and the rename). They test behaviour that already existed.
- **Step 6's guard on its own.** `python3 roadmap/evidence/E5-T24/02-before-fix-tree.py --without-own-plan-in-add-dog`, exit 0 (`06-before-fix-tree-b.log`), builds tree B: the fixed tree with only `addDog`'s fallback reverted to `member.planId`. In it, `SignupPlansFollowUpIT#R_04_09_aMemberWhoseOwnPlanIsGoneMustChooseOne` exits 1 at `mustChoose:204`: `expected: 400 but was: 422` (`07-before-fix-b-targeted.log`).
- A first tree-A run (`02-before-fix-tree.log`, `03-before-fix-targeted.log`: 13 tests, 11 failures) used earlier versions of two tests:
  - its download test failed first at step 1's PATCH; it now attaches its files through S03;
  - its contract test stopped at the description before listing the exports; the check now goes into the problem list.
- Development runs, for the record (`00-dev-targeted-1…7.log`):
  - `1`: a new test's club id prefix was too long;
  - `3`: `CalendarIT` used a lambda authority, which `CurrentUserFilter` does not read as `ROLE_INSTRUCTOR` (real tokens carry `SimpleGrantedAuthority`); and the `GET /signup` 422 of step 6 was found here, then fixed;
  - `5`: `ListFieldsContractIT` needed the registrations list's refused `activityId` and to skip `/instructor/week/export` (a document, not a list);
  - the others passed.

### Step → test table
| Step | Test (fails before the fix: log) |
|---|---|
| 1 | `SignupSecurityFixesIT.R_04_19_R_04_08_d2AddsAFileToAPendingSignupDogWithTheAdminsUpload`, `R_04_19_R_04_06_d2AddsTheAdminsUploadToTheReusedDogAndTheValidationWritesIt` (`05`); `AttachmentServiceTest.R_04_19_d2ClaimsOnlyTheCallersOwnDogDocumentUpload` (new API: does not compile unfixed) |
| 2 | `SignupSecurityFixesIT.CONVENCIONS_API_5_R_04_19_aSignedUploadUrlTakesAPutWithItsHeadersOnlyAndItsFileIsClaimed`, `CONVENCIONS_API_5_signedDownloadUrlsAnswerWithoutABearerAndRefuseAnotherFilesSignature`, `CensusIT.T_03_23_…` (`05`); `SignupIT.T_04_13_…` (changed expectation); `AttachmentServiceTest.CONVENCIONS_API_5_localSignedUrlsAuthoriseThemselves` |
| 3 | `DemoSeedsIT.R_04_06_R_04_07_theDemosLeftMemberHasAnInactiveChippedDogThatAReadmissionReuses` (a real `POST /signup` readmission reuses the seed dog), `T_03_42_…` counts; `bin/e3-smoke` (real stack) |
| 4 | `CalendarIT.R_14_09_anInstructorsAuditedActionIsStoredBackofficeAndItsEventKeepsInstructor` (`05`) |
| 5 | `ListFieldsContractIT.CONVENCIONS_API_4_noOperationPublishesFieldsWithoutXFieldsAndEachExportPublishesItsLists`, `UniversalListsIT.CONVENCIONS_API_4_T_03_11_anExportsFieldsPickItsColumns` (`05`); `ListContractValidationTest` (2 tests, new class) |
| 6 | `SignupPlansFollowUpIT.R_04_09_aMemberWhoseOwnPlanIsGoneMustChooseOne` (`05` at `GET /signup`, `07` at the `REQUIRED` guard) |
| 7 | `SignupPlansFollowUpIT.R_04_09_theFamilyMembersOwnHiddenPlanIsListedAsCurrentInAddDogMode`, `R_05_19_T_05_07_eachPlanCarriesItsPriceLabelInTheReadersLocale` (not required to fail) |
| 8 | `ListFieldsContractIT.CONVENCIONS_API_4_everyEngineListPublishesTheFiltersItAccepts`, `PlanningIT.T_06_01_aTemplateClassWithoutAPlacementSendsANullPlacementIdUnderCourses` (`05`); `E4ContractIT`/`E5ContractIT` expectations updated |

### Files changed
- Main:
  - `clubs/followup`: `AttachmentService`, `AttachmentsController`, `SignupFilesController`, `persistence/LocalAttachmentStorage` (Javadoc), `persistence/UploadGrantRepository`;
  - `shared/api`: `SignedFileRequests` (new), `TenantFilter`;
  - `configuration`: `SecurityConfiguration`, `OpenApiConfiguration`, `ListContractValidation` (new);
  - `shared/application/contract/ListContract` (Javadoc);
  - `clubs/census`: `SignupService`, `DemoSeedService`, `domain/DemoDataset`, `api/CensusRequests`, `api/DogsController`, `api/SignupRequests`, `api/SignupResponses`;
  - `platform`: `application/audit/AuditWriter`, `persistence/audit/AuditListProjection` (comment);
  - `ListContract` annotations and exports: `clubs/common/api/ExportsController`, `clubs/common/application/ListExportService`, `clubs/activities/api/ActivitiesController`, `clubs/training/api/TrainingController`, `clubs/scheduling/api/SchedulingController`, `clubs/bookings/api/BookingsController`;
  - `clubs/scheduling/api/SchedulingContracts`.
- Tests:
  - `SignupSecurityFixesIT`, `SignupPlansFollowUpIT`, `SignupIT`, `CensusIT`, `DemoSeedsIT`, `UniversalListsIT`, `CalendarIT`, `PlanningIT`;
  - `ListFieldsContractIT`, `E4ContractIT`, `E5ContractIT`;
  - `AttachmentServiceTest`, `ListContractValidationTest` (new).
- Seeds and docs:
  - `seeds/demo-canic.yaml`, `seeds/README.md`;
  - `docs/openapi/openapi.json` (regenerated), `docs/openapi/CHANGELOG.md`, `CHANGELOG.md`.
- Scripts: `bin/e3-smoke`.
- Evidence: `roadmap/evidence/E5-T24/*` (logs `00`–`15`, and the scripts `02-before-fix-tree.py` and `11-test-summary.py`).
- Not mine: `docs/INCIDENCIES_OBERTES.md`, `roadmap/MESSAGES.md` and `roadmap/tasks/E5-T23.md` changed in the working tree during this session. That is the organizer's E5-T23 verification (INC-13). I did not edit them.

### Rules and tests implemented
- **Rules:**
  - S04: R-04-19 (amended 26-09), R-04-08 (the limit counts the ADMIN's keys), R-04-06 and R-04-07 (the reused dog takes the upload; the seed dog is reusable), R-04-09 (amended 26-09), R-04-13 (the IT's family member);
  - CONVENCIONS_API §4 and §5 (amended 26-09);
  - S14 §3 and R-14-09 (the audit origin);
  - CATALEG_ESDEVENIMENTS (the envelope keeps `INSTRUCTOR`).
- **Tests:** test ids T-03-11, T-03-23, T-03-42, T-04-13, T-05-07 (the renamed test), T-06-01, T-06-20/T-07-17 and T-08-47/T-09-24/T-15-29 (the updated contract tests). The new tests are named after their rules and conventions (`R_…`, `CONVENCIONS_API_…`).

### Assumptions
1. **Whose upload D2 may add (step 1).** It must be the caller's own `DOG_DOCUMENT` upload, as S03's `claim` requires. R-04-19 says «que l'ADMIN ha pujat». Another ADMIN's key answers `400 FILE_NOT_FOUND`.
2. **Expiry (step 1).** The claim does not check the upload URL's five minutes, because D2 may save the drawer later. The PUT itself stays bounded by its URL.
   - A key bound to the same dog and type can be claimed again, as a signup key can.
   - A signup key is still taken as a new D2 file.
3. **P9 (step 1).** P9 cleans only `SIGNUP_DOCUMENT` grants (S15 R-15-19). So the ADMIN's file of a rejected readmission is not deleted.
4. **Signed-route errors (step 2).**
   - A valid signature for an id without a grant answers `404 NOT_FOUND`.
   - An upload URL of a claimed file answers `409 INVALID_STATE`, as before.
   - The signature is checked before any lookup, so a wrong one is always 403.
5. **The seed's LEFT member (step 3).** The seed gives the DNI and dog to the first existing LEFT member (number 192), rather than adding a fourth one. The member counts (194, D5's totals) do not change.
   - The values live in `seeds/demo-canic.yaml` (AGENTS rule 3).
   - A database seeded before this change gets `CLUB_NOT_EMPTY` on `seed:demo`, as for any changed specification (`seeds/README.md`). The web's stacks are fresh.
6. **`fields` on exports (step 5).** `columns`, when sent, keeps precedence. `fields` picks the columns only without it. `fields` that names no column (for example `fields=id`) answers `400 INVALID_FILTER`, not the default columns.
   - The startup check also refuses `x-fields` without the parameter, by the same principle.
   - `filter-values` loses `fields` in the contract only.
7. **The registrations list's `activityId` (step 8).** The runtime allowlist of `/activities/{id}/registrations` is shared with its export, which filters on `activityId`. The list refuses that filter (E5-T20, `ActivityRealCoreFollowUpsIT:137`). The new contract test names this exception explicitly.
8. **The family fare's quote (step 7).** A third dog on `ABONAT_FAMILIAR` is quoted 4500: 50 % of 90 €, since no plan includes 3 dogs. The test asserts it as the code computes it today (`billing.familyDiscountPercentFromSecondDog`); ABONAT's quote is 3000.
9. **Step 2 on the real stack.** `bin/e3-smoke` checks the PUT and the download without a bearer. The web's `<img>` case is the same GET.

### Questions / catalog proposals
1. **`GET /api/v1/exports/{id}/download`** is also a local signed URL. It still requires a bearer and the caller's own job (S14 R-14-12, `ExportsController:63`). The task named three routes, so I left it. Should it authorise itself too (CONVENCIONS_API §5)? It serves personal data for 7 days, so I did not decide it. Meanwhile: unchanged.
2. **`PUT /api/v1/signup/uploads` for a MEMBER adding a dog while signup is closed.** The web sends no bearer to the storage, so `requireSignupOpen` answers `SIGNUP_CLOSED` at the PUT, although the member got the URL. Should the signup grant record the member's account, so that its PUT passes? The task said «keep it», so it is unchanged.
3. No new parameter, event, notification or error code.

### Web (E4-W13 and later)
- Regenerate the client.
- **D2:**
  - upload a new file with `POST /attachments/upload-url` (`DOG_DOCUMENT`);
  - PUT it with `Upload.headers` only;
  - send its `fileKey` in `PATCH /dogs/{id}` with the type's kept keys.
- Local download URLs work in an `<img>` and without a bearer.
- The demo's readmission: DNI `99000001C`, chip `941000000000805` («Demo Boira», member 192).
- Exports publish `x-fields`. A member whose plan is gone has no current plan and must choose one.

## Organizer verification
Result: verified (26-09-2026, organizer).

- **Review:** `roadmap/reviews/E5-T24-20260926-1636-claude.md` (commit `dd3c246`): minor issues, no major. It found two minors and five nits.
- **CI:** `dd3c246` is green.
- **Evidence:**
  - `./mvnw -q clean verify` exit 0: 578 unit and 1027 integration tests (`11-test-summary.log`);
  - `bin/e3-smoke` twice, exit 0, with «0 changes» on each seed's second run. The smoke now replays the web's E4-W13 flow without a bearer: readmission of the demo's LEFT member, D2's new file with the ADMIN's upload, the PUT and the download;
  - `bin/openapi-snapshot` and one changelog entry.
- **Definition of Done:** steps 1–8 are done. Steps 1, 2, 4, 5, 6 and 8 fail on the unfixed tree (log `05`: 13 tests, 11 failures), and step 6's `REQUIRED` guard also fails on its own (log `07`).
- **Beyond the steps:** `GET /signup` no longer answers `422 PLAN_NOT_AVAILABLE` to a member whose plan is gone.
- **Assumptions confirmed by the organizer:**
  - 1: D2 adds only the caller's own upload. That is stricter than R-04-19 and safe.
  - 2: no five-minute limit on the claim, because D2 may save the drawer later.
  - 4 and 5 are confirmed.
  - 6 is confirmed: `columns` keeps precedence over `fields` on exports.
  - 3 (P9 does not clean the ADMIN's orphan `DOG_DOCUMENT` files) goes to INC-14.
- **Routing (organizer decisions, 26-09):**
  - **Review #1 and question 2 go to the new api task E5-T26.** With signup closed, a MEMBER adding a dog cannot upload, because the web sends no bearer to the signed URL (R-04-27).
  - **Review #2 and question 1, decision:** export links are the exception to `CONVENCIONS_API.md` §5. They carry personal data for seven days and belong to the account that asked for them (S14 R-14-12). So they keep requiring the caller's session, and the client downloads them with its bearer. §5 is amended, and there is no code change.
  - **Review #3** (open the grant's club as the tenant on the signed routes) goes to **E5-T26**, which touches the same code.
  - **The review's «Not checked» items** go to **E5-T26** too: the stored MIME type on local downloads, as S3 answers, and the CORS preflight of the signed routes.
  - **Nits #4, #5 (P9's cleanup) and #6** go to `docs/INCIDENCIES_OBERTES.md` INC-14.
  - **Nit #7:** no action.
- **Web:** E4-W13's round 2 may drop its two workarounds once the core image's revision is `dd3c246` or later: the bearer on the upload path, and the `400` fallback of the add. The demo's LEFT member with an INACTIVE dog is there too («Demo Boira», member 192).
