---
id: E0-T13
title: Staging deployment (droplet): compose, Caddy, backup/restore scripts, DEPLOY.md
stage: E0
repo: agilityhub-core-api
thread: A
order: 130
status: blocked
depends_on: [E0-T03, E0-T10, external:jordi-ssh-dns]
branch: 
pr: 
updated: 2026-09-05
---

# E0-T13 · Staging deployment (droplet): compose, Caddy, backup/restore scripts, DEPLOY.md

## Objective
A staging environment with fictional data that Jordi and Josep can open, plus the operational scripts. **Blocked until Jordi provides SSH access to the droplet and the staging DNS names** (`core.staging.agilitydoghub.com`, `id.staging…`, `clubs.staging…`, `clubsadmin.staging…`, optionally `app.staging.agilitycanic.cat`).

## Spec references
`docs/decisions/ADR-003` · S17 WP-17-D (only the skeleton; on-demand TLS is stage E10) · `docs/DECISIONS_PENDENTS.md` A13 (backups: weekly now, daily when billing goes live).

## Context to load
ADR-003 · S17 §7 (infra) · this file.

## Steps
1. `deploy/compose.staging.yaml` (api, mongo replica set **with authentication**, Caddy) + `deploy/Caddyfile` (reverse proxy per host, automatic HTTPS for the fixed staging hosts).
2. `bin/deploy-staging` (build image, `docker compose --env-file … up -d --wait`, then `club:apply` of `seeds/club-canic.yaml` and `seeds/club-minim.yaml`), `bin/backup-mongo` (`mongodump` → encrypted archive → S3 bucket from env, 30-day retention, cron weekly for now), `bin/restore-mongo --verify` (restores into a temporary container and counts documents).
3. `DEPLOY.md`: environment variables, deploy, rollback (previous image tag), restore, JWT key rotation, where logs are.

## Verification (run all; evidence = command + exit code + last 40 lines; long outputs → `roadmap/evidence/<ID>/*.log`)
- `curl -fsS https://core.staging.agilitydoghub.com/api/v1/health` → `UP`.
- `curl -fsS -H 'Host: app.staging.agilitycanic.cat' https://core.staging.agilitydoghub.com/api/v1/branding | jq .club.slug` → `canic` (or the equivalent host mapping documented in DEPLOY.md).
- `bin/backup-mongo && bin/restore-mongo --verify` → document counts match.

## Definition of Done
Staging reachable · backup restored once · DEPLOY.md complete · no secrets in the repo (`.env` on the server only).

## Evidence to return
The three outputs (hide hostnames/IPs of internal services if any).

---

## Executor report
_(not started)_

### Branch / commits
### Evidence (full command outputs)
### Files changed
### Rules and tests implemented (R-xx-nn / T-xx-nn)
### Assumptions
### Questions / catalog proposals

## Organizer verification
_(pending)_
