---
id: E5-T23
title: The Cànic's cash payment instructions (Jordi, 26-09) and the E5-T21 review's points (api) — MANUAL.instructions in the Cànic definition, the card requirement only when the card changes, a removed key refused at submission (changelog and IT), the writers' backoff Javadoc, a reused dog's merge of changed types only
stage: E5
repo: agilityhub-core-api
thread: A
order: 230
status: verified
depends_on: [E5-T21]
branch:
pr:
updated: 2026-09-26
---

# E5-T23 · The Cànic's cash payment instructions, and the E5-T21 review

## Objective
Screen 19 tells an applicant how to pay the initial amount with the club's `MANUAL.instructions` (S04 §2 row 19). The Cànic's definition has none since E3-T14, so the real core shows no such paragraph. Jordi confirmed the text of the approved mockup 19 on 26-09, account and Bizum included. This task also takes the independent review of E5-T21 (`roadmap/reviews/E5-T21-20260926-0930-claude.md`): one minor and three nits, which the organizer confirmed.

**Each fix comes with a test that fails before the fix.**

## Spec references
- S04: §2 row 19 (`MANUAL.instructions` under the total), R-04-10 (payment methods), R-04-08 (signup uploads), R-04-19 (D2 edits), R-04-06 (the reused dog).
- S17 §3 (the club definition: `paymentProviders.MANUAL.instructions` is a LocalizedText); S12 (`MANUAL {instructions: LocalizedText}`).
- The approved mockup `docs/pantalles/mobil/19-alta-pagament-consentiments-i-enviament.png`.

## Context to load
- The E5-T21 review and report (question 1).
- `seeds/club-canic.yaml` (`paymentProviders.MANUAL`), `seeds/club-definition.schema.json`, `CensusClubSettings.manualInstructions`.
- `SignupService.java`: `claimDocuments` (near `:538`–`:543`), `saveDocuments` (near `:496`–`:500`).
- `TransactionRetriesTest.java` (near `:11`–`:17`), `docs/openapi/CHANGELOG.md` (the E5-T21 entry).

## Steps
1. **The Cànic's cash payment instructions (Jordi, 26-09).** `seeds/club-canic.yaml` gets `paymentProviders.MANUAL.instructions`, word for word as Jordi confirmed:
   - ca «El pagament de l'entrada i el mes en curs (o el proper) és necessari per a la validació de l'alta. Pot fer-se per transferència al compte ES08 2100 0416 5702 0016 4955 (Club d'Agility Cànic) o per Bizum al 607 475 945.»;
   - es «El pago de la entrada y del mes en curso (o del siguiente) es necesario para la validación del alta. Puede hacerse por transferencia a la cuenta ES08 2100 0416 5702 0016 4955 (Club d'Agility Cànic) o por Bizum al 607 475 945.».
   
   Tests:
   - `club:apply` stores them, and a second run reports 0 changes;
   - `GET /signup` sends them in `paymentMethods[MANUAL].instructions`, in ca and in es;
   - `bin/e3-smoke` stays green twice.
   
   These are the club's public payment details, which the approved mockup shows. They are not a member's personal data.
2. **The card requirement only when the card changes (E5-T21 review #1).** With `signup.requireDogDocumentAtSignup=true`, the check applies only when the card's file keys differ from what D2's view shows. An edit that sends back the view (or changes another field while echoing the documents) never gets `422 DOG_DOCUMENT_REQUIRED`. IT with the parameter on: a pending public-signup dog whose card is `PENDING`.
3. **A removed key at submission (review #2).** The E5-T21 entry of `docs/openapi/CHANGELOG.md` says that a removed key is refused at submission too (`POST /signup` for a readmission, `POST /me/dogs/signup`). Add a submission IT with a removed key of the reused dog's own card.
4. **The backoff Javadoc (review #3, question 1).** `TransactionRetriesTest`'s class Javadoc says «every S05–S09 writer». The S01 and S04 writers keep their own growing backoff by design: they retry many more times.
5. **Optional (review #4).** A reused dog's merge takes only the types whose keys differ from D2's view, as the public path does.

## Fixed conventions
- No contract change beyond the changelog line.
- Heavy runs go through the host lock.
- Fictional data only, apart from the club's own public payment details of step 1.

## Verification (run all; evidence = command + exit code + last 40 lines; long outputs → `roadmap/evidence/E5-T23/*.log`)
- `./mvnw -q clean verify`.
- `bin/e3-smoke` twice, on a fresh local stack.
- `bin/core club:apply seeds/club-canic.yaml` twice (the second run reports 0 changes), as E3-T11 did.

## Definition of Done
- Steps 1–4 are done (step 5 is optional). Steps 1–3 each have a test that fails before the fix.

## Evidence to return
- The command outputs.
- A step → test table.

---

## Executor report

### Summary
Steps 1–5 are done, including the optional step 5.
- **The failing-before tests.** Steps 1, 2 and 5 each have a test that fails on the unfixed tree, at its intended assertion (log `06`).
- **Step 3.** Its new submission IT covers E5-T21's behaviour, so it passes on `HEAD`. It fails once E5-T21's removed-key check is disabled: `201` instead of `400` (log `07`).
- **Verification.**
  - `./mvnw -q clean verify` (through the host lock) exits 0: 574 unit and 1016 integration tests, 0 failures.
  - `bin/e3-smoke` exits 0 twice.
  - `bin/core club:apply seeds/club-canic.yaml` exits 0 twice on a fresh Mongo; the second run prints `0 changes (applied)`.
- **Contract.** No `openapi.json` change (`OpenApiSnapshotTest` passes in the verify). `docs/openapi/CHANGELOG.md` gets the requested line in the E5-T21 entry, plus a behaviour-only E5-T23 entry (assumption 5).

### What was done, per step
- **Step 1 (S04 §2 row 19, R-04-10; S12 `MANUAL {instructions}`; S17 §3; Jordi, 26-09).**
  - The seed: `seeds/club-canic.yaml` sets `paymentProviders.MANUAL.instructions`, `ca` and `es`, word for word as in the task. `seeds/club-canic-consumer.yaml` gets the same text: `DemoSeedsIT:106` requires it to equal the canonical seed apart from its domains.
  - The schema (`seeds/club-definition.schema.json:532`): a new `manualPaymentProvider` takes `enabled` and an optional `instructions` (`localizedText`). `SEPA_XML` and `STRIPE` still take `enabled` only.
  - `ClubDefinitionMapper`:
    - `providers` (`:102`) stores the declared `instructions` as a plain `{locale: text}` map. A definition without them keeps the stored ones, like every other stored provider setting.
    - `export` (`:41`) now includes `MANUAL.instructions`, the club's public text. Without this, the diff would compare two exports without them, and `club:apply` would report «unchanged» and never store them.
    - `instructions(Object)` (`:112`) reads the stored form: plain, or wrapped in `{values: …}` as `CensusClubSettings.manualInstructions` accepts.
  - `ClubDefinitionWriter:128` (S17 R-17-05, «`MANUAL`: instruccions amb `defaultLocale` obligatori»): instructions without the club's default locale answer `400 VALIDATION_ERROR` with `fieldErrors [{field: paymentProviders.MANUAL.instructions, code: REQUIRED}]`.
  - `bin/e3-smoke`: `MANUAL` now reads `configured: true` in `GET /club`. A new check requires `GET /signup`'s `MANUAL.instructions` to equal the stored `ca` text, and the stored locales to be `ca` and `es`.
  - `seeds/README.md` documents the exception to «provider names only».
- **Step 2 (review E5-T21 #1; R-04-19, R-04-08).**
  - `claimDocuments` now receives D2's view (`shown`); it is `null` at submission. `SignupService:548`: a card without files triggers `DOG_DOCUMENT_REQUIRED` in a D2 edit only when the view shows files for the card, that is, when the edit changes its keys.
  - At submission the check is unchanged.
- **Step 3 (review E5-T21 #2).**
  - The E5-T21 entry of `docs/openapi/CHANGELOG.md` gains the line: `POST /signup` of a readmission refuses a removed key (`400 FILE_NOT_FOUND`) and stores nothing; `POST /me/dogs/signup` answers the same.
  - `POST /me/dogs/signup` always creates a new dog (`prepareDog(…, false)`, `SignupService:612`). There, the refusal comes from `AttachmentService.claimSignup`: the grant is bound to the other dog.
  - New IT `R_04_19_R_04_06_aSubmissionRefusesAKeyRemovedFromTheReusedDogsOwnCard`.
- **Step 4 (review E5-T21 #3, question 1).** The `TransactionRetriesTest` class Javadoc now says «every S05–S09 writer». It adds that the S01 and S04 writers (`IdentityTransactions`, `SignupTransactions`) keep their own growing backoff by design.
- **Step 5 (review E5-T21 #4, optional).** `saveDocuments` (`SignupService:490`) computes the changed types once, the ones whose keys differ from D2's view, for both paths. The reused dog's merge takes only those, so a type sent back as shown no longer enters `readmissionRequest.submitted.documents`, and the validation no longer rewrites it.

### Evidence

**1. `/Users/jordib/Dropbox/Documents/SOFTWARE_CANIC/05-desenvolupament/roadmap-kit/mac/heavy.sh ./mvnw -q clean verify > roadmap/evidence/E5-T23/09-clean-verify.log 2>&1; echo "exit $?"`: exit 0.**
- There was no host-lock wait: the log starts with the build's own output.
- **Earlier failing attempt (targeted, not the verify):** `01-after-fix-targeted.log` exited 1 with one failure: `ClubDefinitionsIT.T_17_01_cliExportsYamlAndExitsWithoutOpeningTheConfiguredHttpPort:511`.
  - Cause: that test runs `bin/core`, and `bin/core` runs `target/agilityhub-core-api-*.jar`. That jar was built at 14:30, before this change, so its export lacked `instructions`.
  - Fix: I rebuilt the jar (`./mvnw -q -DskipTests package`, `02-package.log`, exit 0) and re-ran `ClubDefinitionsIT`: 18 tests, 0 failures (`03-after-fix-club-definitions.log`).
  - The code did not change. `clean verify` packages the jar before the ITs.
- After that, I added the unit test `T_17_01_storedCashInstructionsAreExportedPlainOrWrapped`: 7 tests, 0 failures (`08-after-fix-codec-test.log`).
- **Test summary** (`10-test-summary.log`, from `10-test-summary.py` over `target/surefire-reports` and `target/failsafe-reports`):
```
surefire totals: tests=574 failures=0 errors=0 skipped=0 (126 classes)
failsafe totals: tests=1016 failures=0 errors=0 skipped=0 (87 classes)
Classes this task adds or changes (DemoSeedsIT through the consumer seed and TransactionRetriesTest through its Javadoc only):
  [failsafe] com.agilityhub.core.platform.application.definition.ClubDefinitionsIT: tests=18 failures=0 errors=0 skipped=0
  [surefire] com.agilityhub.core.platform.application.definition.ClubDefinitionCodecTest: tests=7 failures=0 errors=0 skipped=0
  [failsafe] com.agilityhub.core.clubs.census.api.SignupSecurityFixesIT: tests=41 failures=0 errors=0 skipped=0
  [surefire] com.agilityhub.core.shared.application.TransactionRetriesTest: tests=2 failures=0 errors=0 skipped=0
  [failsafe] com.agilityhub.core.clubs.census.application.DemoSeedsIT: tests=9 failures=0 errors=0 skipped=0
E5-T23 test methods (added or changed; the last two unchanged, they read the new seed and export):
  PASS ClubDefinitionsIT#T_04_14_T_02_12_canicSeedCarriesItsCashPaymentInstructions
  PASS ClubDefinitionsIT#T_02_12_canicSeedEnablesDirectDebitAndCashWithoutConfiguringThem
  PASS ClubDefinitionsIT#T_17_01_enablingSeededProvidersIsOneChangeAndKeepsTheirStoredConfiguration
  PASS ClubDefinitionCodecTest#T_17_01_paymentProvidersAreNamesOrEnabledFlagsAndNeverCredentials
  PASS ClubDefinitionCodecTest#T_17_01_storedCashInstructionsAreExportedPlainOrWrapped
  PASS SignupSecurityFixesIT#R_04_19_R_04_08_aD2EditMeetsTheCardRequirementUnlessItChangesTheCard
  PASS SignupSecurityFixesIT#R_04_19_R_04_06_aSubmissionRefusesAKeyRemovedFromTheReusedDogsOwnCard
  PASS SignupSecurityFixesIT#R_04_19_R_04_06_aReusedDogsMixedEditMergesOnlyTheTypesItChanges
  PASS ClubDefinitionsIT#T_17_01_cliExportsYamlAndExitsWithoutOpeningTheConfiguredHttpPort
  PASS ClubDefinitionsIT#T_02_12_canicMatchesBrandingAndParametersFixturesAndSecondApplyDoesNotWrite
```
- **Totals:** E5-T22 had 573 unit and 1012 integration tests. This task adds 1 unit test (`ClubDefinitionCodecTest`) and 4 ITs: 1 in `ClubDefinitionsIT` and 3 in `SignupSecurityFixesIT`.
- **Log check:** `grep -c "\[ERROR\]\|<<< FAIL\|BUILD FAILURE" 09-clean-verify.log` = 0.
- Last 40 lines of `09-clean-verify.log` (5957 lines; `-q` prints no summary, so these are the last ITs' logs):
```
2026-09-26T15:17:51.881+02:00  INFO 51361 --- [agilityhub-core-api] [           main] org.mongodb.driver.client                : MongoClient with metadata {"driver": {"name": "mongo-java-driver|sync|spring-boot", "version": "5.5.2"}, "os": {"type": "Darwin", "name": "Mac OS X", "architecture": "aarch64", "version": "26.4.1"}, "platform": "Java/Homebrew/21.0.12.1"} created with settings MongoClientSettings{readPreference=primary, writeConcern=WriteConcern{w=null, wTimeout=null ms, journal=null}, retryWrites=true, retryReads=true, readConcern=ReadConcern{level=null}, credential=null, transportSettings=null, commandListeners=[io.micrometer.core.instrument.binder.mongodb.MongoMetricsCommandListener@59b6ed3], codecRegistry=ProvidersCodecRegistry{codecProviders=[ValueCodecProvider{}, BsonValueCodecProvider{}, DBRefCodecProvider{}, DBObjectCodecProvider{}, DocumentCodecProvider{}, CollectionCodecProvider{}, IterableCodecProvider{}, MapCodecProvider{}, GeoJsonCodecProvider{}, GridFSFileCodecProvider{}, Jsr310CodecProvider{}, JsonObjectCodecProvider{}, BsonCodecProvider{}, EnumCodecProvider{}, com.mongodb.client.model.mql.ExpressionCodecProvider@316a1241, com.mongodb.Jep395RecordCodecProvider@527afebf, com.mongodb.KotlinCodecProvider@63c9df77]}, loggerSettings=LoggerSettings{maxDocumentLength=1000}, clusterSettings={hosts=[localhost:49724], srvServiceName=mongodb, mode=SINGLE, requiredClusterType=UNKNOWN, requiredReplicaSetName='null', serverSelector='null', clusterListeners='[]', serverSelectionTimeout='30000 ms', localThreshold='15 ms'}, socketSettings=SocketSettings{connectTimeoutMS=10000, readTimeoutMS=0, receiveBufferSize=0, proxySettings=ProxySettings{host=null, port=null, username=null, password=null}}, heartbeatSocketSettings=SocketSettings{connectTimeoutMS=10000, readTimeoutMS=10000, receiveBufferSize=0, proxySettings=ProxySettings{host=null, port=null, username=null, password=null}}, connectionPoolSettings=ConnectionPoolSettings{maxSize=100, minSize=0, maxWaitTimeMS=120000, maxConnectionLifeTimeMS=0, maxConnectionIdleTimeMS=0, maintenanceInitialDelayMS=0, maintenanceFrequencyMS=60000, connectionPoolListeners=[io.micrometer.core.instrument.binder.mongodb.MongoMetricsConnectionPoolListener@49d6ca6d], maxConnecting=2}, serverSettings=ServerSettings{heartbeatFrequencyMS=10000, minHeartbeatFrequencyMS=500, serverMonitoringMode=AUTO, serverListeners='[]', serverMonitorListeners='[]'}, sslSettings=SslSettings{enabled=false, invalidHostNameAllowed=false, context=null}, applicationName='null', compressorList=[], uuidRepresentation=JAVA_LEGACY, serverApi=null, autoEncryptionSettings=null, dnsClient=null, inetAddressResolver=null, contextProvider=null, timeoutMS=null}
2026-09-26T15:17:51.883+02:00  INFO 51361 --- [agilityhub-core-api] [localhost:49724] org.mongodb.driver.cluster               : Monitor thread successfully connected to server with description ServerDescription{address=localhost:49724, type=REPLICA_SET_PRIMARY, cryptd=false, state=CONNECTED, ok=true, minWireVersion=0, maxWireVersion=21, maxDocumentSize=16777216, logicalSessionTimeoutMinutes=30, roundTripTimeNanos=1302000, minRoundTripTimeNanos=0, setName='docker-rs', canonicalAddress=684d1fa918e6:27017, hosts=[684d1fa918e6:27017], passives=[], arbiters=[], primary='684d1fa918e6:27017', tagSet=TagSet{[]}, electionId=7fffffff0000000000000001, setVersion=1, topologyVersion=TopologyVersion{processId=6ab7c4cc0e7e176dd7c48ec7, counter=6}, lastWriteDate=Sat Sep 26 15:17:51 CEST 2026, lastUpdateTimeNanos=232477127257291}
2026-09-26T15:17:52.314+02:00  INFO 51361 --- [agilityhub-core-api] [           main] c.a.core.migration.PlayoffMigrationIT    : Started PlayoffMigrationIT in 0.671 seconds (process running for 309.979)
E5-T07 outbox backlog discarded before PlayoffMigrationIT: 158 PENDING records (more than one dispatch() batch)
E5-T07 outbox backlog discarded before PlayoffMigrationIT: 158 PENDING records (more than one dispatch() batch)
E5-T07 outbox backlog discarded before PlayoffMigrationIT: 158 PENDING records (more than one dispatch() batch)
E5-T07 outbox backlog discarded before PlayoffMigrationIT: 162 PENDING records (more than one dispatch() batch)
2026-09-26T15:17:59.155+02:00  INFO 51361 --- [agilityhub-core-api] [localhost:50103] org.mongodb.driver.cluster               : Exception in monitor thread while connecting to server localhost:50103

com.mongodb.MongoSocketOpenException: Exception opening socket
	at com.mongodb.internal.connection.SocketStream.lambda$open$0(SocketStream.java:85) ~[mongodb-driver-core-5.5.2.jar:na]
	at java.base/java.util.Optional.orElseThrow(Optional.java:403) ~[na:na]
	at com.mongodb.internal.connection.SocketStream.open(SocketStream.java:85) ~[mongodb-driver-core-5.5.2.jar:na]
	at com.mongodb.internal.connection.InternalStreamConnection.open(InternalStreamConnection.java:233) ~[mongodb-driver-core-5.5.2.jar:na]
	at com.mongodb.internal.connection.DefaultServerMonitor$ServerMonitor.lookupServerDescription(DefaultServerMonitor.java:219) ~[mongodb-driver-core-5.5.2.jar:na]
	at com.mongodb.internal.connection.DefaultServerMonitor$ServerMonitor.run(DefaultServerMonitor.java:176) ~[mongodb-driver-core-5.5.2.jar:na]
Caused by: java.net.ConnectException: Connection refused
	at java.base/sun.nio.ch.Net.pollConnect(Native Method) ~[na:na]
	at java.base/sun.nio.ch.Net.pollConnectNow(Net.java:694) ~[na:na]
	at java.base/sun.nio.ch.NioSocketImpl.timedFinishConnect(NioSocketImpl.java:542) ~[na:na]
	at java.base/sun.nio.ch.NioSocketImpl.connect(NioSocketImpl.java:592) ~[na:na]
	at java.base/java.net.SocksSocketImpl.connect(SocksSocketImpl.java:327) ~[na:na]
	at java.base/java.net.Socket.connect(Socket.java:751) ~[na:na]
	at com.mongodb.internal.connection.SocketStreamHelper.initialize(SocketStreamHelper.java:76) ~[mongodb-driver-core-5.5.2.jar:na]
	at com.mongodb.internal.connection.SocketStream.initializeSocket(SocketStream.java:104) ~[mongodb-driver-core-5.5.2.jar:na]
	at com.mongodb.internal.connection.SocketStream.open(SocketStream.java:79) ~[mongodb-driver-core-5.5.2.jar:na]
	... 3 common frames omitted

E5-T07 outbox backlog discarded before PlayoffMigrationIT: 158 PENDING records (more than one dispatch() batch)
E5-T07 outbox backlog discarded before PlayoffMigrationIT: 158 PENDING records (more than one dispatch() batch)
E5-T07 outbox backlog discarded before PlayoffMigrationIT: 158 PENDING records (more than one dispatch() batch)
E5-T07 outbox backlog discarded before PlayoffMigrationIT: 158 PENDING records (more than one dispatch() batch)
E5-T07 outbox backlog discarded before PlayoffMigrationIT: 158 PENDING records (more than one dispatch() batch)
E5-T07 outbox backlog discarded before PlayoffMigrationIT: 158 PENDING records (more than one dispatch() batch)
E5-T07 outbox backlog discarded before PlayoffMigrationIT: 159 PENDING records (more than one dispatch() batch)
E5-T07 outbox backlog discarded before PlayoffMigrationIT: 158 PENDING records (more than one dispatch() batch)
E5-T07 outbox backlog discarded before PlayoffMigrationIT: 158 PENDING records (more than one dispatch() batch)
E5-T07 outbox backlog discarded before PlayoffMigrationIT: 158 PENDING records (more than one dispatch() batch)
E5-T07 outbox backlog discarded before PlayoffMigrationIT: 158 PENDING records (more than one dispatch() batch)
E5-T07 outbox backlog discarded before PlayoffMigrationIT: 159 PENDING records (more than one dispatch() batch)
```
(The `MongoSocketOpenException` is the INFO-level monitor of a Testcontainers Mongo that an earlier test context had already stopped, as in E5-T22's log. It is not a failure: the exit code and the summary above are the result.)

**2a. `/Users/jordib/Dropbox/Documents/SOFTWARE_CANIC/05-desenvolupament/roadmap-kit/mac/heavy.sh bin/e3-smoke > roadmap/evidence/E5-T23/11-e3-smoke-run1.log 2>&1; echo "exit $?"`: exit 0.**
- Full output: `11-e3-smoke-run1.log` (59 lines). There was no host-lock wait.
- `bin/e3-smoke` starts its own disposable Compose stack from the working tree and removes it at the end, so each run is on a fresh stack.
- Its lines 4–5 are `PASS club:apply seeds/club-canic.yaml run 1` and `PASS club:apply seeds/club-canic.yaml run 2: 0 changes`.

Last 40 lines:
```
curl POST /api/v1/signup/family-group-lookups -> 200
curl POST /api/v1/signup/identity-checks -> 200
curl POST /api/v1/signup/upload-urls -> 200
curl PUT /api/v1/signup/uploads -> 204
curl POST /api/v1/signup -> 201
curl POST /api/v1/signup -> 201
PASS D1 dashboard + counters: pending=4, olderThanWarn=1, activeMembers=184
curl GET /api/v1/members/2fa71dff-e8d1-4c18-89db-08be3bd8e005/signup -> 200
curl GET /api/v1/club -> 200
PASS cash payment instructions: the seed's, in ca, under MANUAL
PASS payment methods: GET /club, GET /signup and D2 offer SEPA_DD and MANUAL
PASS mailbox N-01 correlated by notificationId; locale=ca
curl POST /api/v1/members/2fa71dff-e8d1-4c18-89db-08be3bd8e005/validation -> 200
curl POST /api/v1/members/2fa71dff-e8d1-4c18-89db-08be3bd8e005/validation -> 200
PASS D1 dashboard + counters: pending=3, olderThanWarn=1, activeMembers=185
PASS mailbox N-02 correlated by notificationId; locale=ca; token=[truncated]
curl POST /oauth2/token -> 200
curl GET /api/v1/me -> 200
PASS public family signup -> dry run -> validation/number increment -> N-02 -> welcome session
curl POST /api/v1/me/dogs/signup -> 201
PASS N-01 in the member APP inbox, scoped to account and entity
PASS D1 dashboard + counters: pending=4, olderThanWarn=1, activeMembers=185
curl GET /api/v1/members/2fa71dff-e8d1-4c18-89db-08be3bd8e005/signup -> 200
PASS add-dog D2: only the member's current payment method, not assignable
curl POST /api/v1/members/2fa71dff-e8d1-4c18-89db-08be3bd8e005/validation -> 200
PASS N-37 in the member APP inbox, scoped to account and entity
PASS D1 dashboard + counters: pending=3, olderThanWarn=1, activeMembers=185
curl POST /api/v1/signup -> 201
PASS D1 dashboard + counters: pending=4, olderThanWarn=1, activeMembers=185
curl GET /api/v1/members/20fb1560-9f7e-47bf-871e-0b2f6c765538/signup -> 200
curl POST /api/v1/members/20fb1560-9f7e-47bf-871e-0b2f6c765538/rejection -> 200
PASS mailbox N-03 correlated by notificationId; locale=ca
PASS D1 dashboard + counters: pending=3, olderThanWarn=1, activeMembers=185
PASS signup without family -> rejection -> N-03; D1 counters restored
curl GET /api/v1/parameters/signup.enabled -> 200
curl PUT /api/v1/parameters/signup.enabled -> 200
curl POST /api/v1/signup -> 422 SIGNUP_CLOSED
PASS signup.enabled=false closes the form; canonical catalog status is 422 SIGNUP_CLOSED
PASS T-04-34 / T-14-11: E3 backend gate completed; credentials and capabilities withheld
Removed disposable Compose services, volumes and private mailbox; existing stacks preserved
```

**2b. `/Users/jordib/Dropbox/Documents/SOFTWARE_CANIC/05-desenvolupament/roadmap-kit/mac/heavy.sh bin/e3-smoke > roadmap/evidence/E5-T23/12-e3-smoke-run2.log 2>&1; echo "exit $?"`: exit 0.**
- Full output: `12-e3-smoke-run2.log` (59 lines). Same lines 4–5 as run 1.

Last 40 lines:
```
curl POST /api/v1/signup/family-group-lookups -> 200
curl POST /api/v1/signup/identity-checks -> 200
curl POST /api/v1/signup/upload-urls -> 200
curl PUT /api/v1/signup/uploads -> 204
curl POST /api/v1/signup -> 201
curl POST /api/v1/signup -> 201
PASS D1 dashboard + counters: pending=4, olderThanWarn=1, activeMembers=184
curl GET /api/v1/members/ef8c18aa-686e-4a62-a12d-a3a9ba6871d1/signup -> 200
curl GET /api/v1/club -> 200
PASS cash payment instructions: the seed's, in ca, under MANUAL
PASS payment methods: GET /club, GET /signup and D2 offer SEPA_DD and MANUAL
PASS mailbox N-01 correlated by notificationId; locale=ca
curl POST /api/v1/members/ef8c18aa-686e-4a62-a12d-a3a9ba6871d1/validation -> 200
curl POST /api/v1/members/ef8c18aa-686e-4a62-a12d-a3a9ba6871d1/validation -> 200
PASS D1 dashboard + counters: pending=3, olderThanWarn=1, activeMembers=185
PASS mailbox N-02 correlated by notificationId; locale=ca; token=[truncated]
curl POST /oauth2/token -> 200
curl GET /api/v1/me -> 200
PASS public family signup -> dry run -> validation/number increment -> N-02 -> welcome session
curl POST /api/v1/me/dogs/signup -> 201
PASS N-01 in the member APP inbox, scoped to account and entity
PASS D1 dashboard + counters: pending=4, olderThanWarn=1, activeMembers=185
curl GET /api/v1/members/ef8c18aa-686e-4a62-a12d-a3a9ba6871d1/signup -> 200
PASS add-dog D2: only the member's current payment method, not assignable
curl POST /api/v1/members/ef8c18aa-686e-4a62-a12d-a3a9ba6871d1/validation -> 200
PASS N-37 in the member APP inbox, scoped to account and entity
PASS D1 dashboard + counters: pending=3, olderThanWarn=1, activeMembers=185
curl POST /api/v1/signup -> 201
PASS D1 dashboard + counters: pending=4, olderThanWarn=1, activeMembers=185
curl GET /api/v1/members/1c150460-996e-435d-89ab-9b1de59d474d/signup -> 200
curl POST /api/v1/members/1c150460-996e-435d-89ab-9b1de59d474d/rejection -> 200
PASS mailbox N-03 correlated by notificationId; locale=ca
PASS D1 dashboard + counters: pending=3, olderThanWarn=1, activeMembers=185
PASS signup without family -> rejection -> N-03; D1 counters restored
curl GET /api/v1/parameters/signup.enabled -> 200
curl PUT /api/v1/parameters/signup.enabled -> 200
curl POST /api/v1/signup -> 422 SIGNUP_CLOSED
PASS signup.enabled=false closes the form; canonical catalog status is 422 SIGNUP_CLOSED
PASS T-04-34 / T-14-11: E3 backend gate completed; credentials and capabilities withheld
Removed disposable Compose services, volumes and private mailbox; existing stacks preserved
```

**3. `/Users/jordib/Dropbox/Documents/SOFTWARE_CANIC/05-desenvolupament/roadmap-kit/mac/heavy.sh python3 roadmap/evidence/E5-T23/13-club-apply-twice.py > roadmap/evidence/E5-T23/13-club-apply-twice.log 2>&1; echo "exit $?"`: exit 0.**
- `13-club-apply-twice.py` is E3-T11's `seeds_twice.py`, as E3-T11 did it. Only the names and the command list differ: `bin/core club:apply seeds/club-canic.yaml` twice.
- It runs on a fresh disposable Mongo (random Compose project and port), with the jar of the `clean verify` above (15:12). It removes the stack at the end.
- Full output: `13-club-apply-twice.log` (90 lines).
- In run 1, line 13 is the `paymentProviders` value with `MANUAL.instructions` in `ca` and `es`, and run 1 ends with `15 changes (applied)`.

Last 40 lines:
```
+ catalogs.prices.ABONAT:MONTHLY_FEE:2026-01-01
+ catalogs.prices.ABONAT_FAMILIAR:MONTHLY_FEE:2026-01-01
+ catalogs.prices.PACK6:PACK:2026-01-01
+ catalogs.prices.PACK10:PACK:2026-01-01
+ catalogs.prices.TERAPIA:MAINTENANCE_FEE:2026-01-01
+ catalogs.prices.COMPETICIO_1:MONTHLY_FEE:2026-01-01
+ catalogs.faq.FAQ_1
+ catalogs.faq.FAQ_2
+ catalogs.faq.FAQ_3
+ catalogs.faq.FAQ_4
+ catalogs.faq.FAQ_5
+ catalogs.faq.FAQ_6
+ catalogs.faq.FAQ_7
+ catalogs: 34 catalog changes
+ messageTemplates: schema accepted; application deferred to E7
+ pages: 3 page changes
15 changes (applied)

=== run 2: $ bin/core club:apply seeds/club-canic.yaml -> exit 0
= club unchanged
= domains unchanged
= theme unchanged
= modules unchanged
= paymentProviders unchanged
= legal unchanged
= pwa unchanged
= parameters.signup.enabled unchanged
= parameters.signup.pendingExpiryDays unchanged
= parameters.signup.requireDogDocumentAtSignup unchanged
= parameters.signup.firstMonthSplitDay unchanged
= parameters.signup.allowFamilyGroupPending unchanged
= admins: 0 to provision
= accounts: 0 to provision
= catalogs: 0 catalog changes
= messageTemplates: schema accepted; application deferred to E7
= pages: 0 page changes
0 changes (applied)

$ docker compose (project e5-t23-seeds-7a7cb110) down --volumes -> exit 0
club_apply_twice exit code 0
```
An existing club, applied from the previous seed, gets the instructions as exactly one `paymentProviders` change, then none. This is the production case (the Cànic already exists), and `T_04_14_T_02_12_canicSeedCarriesItsCashPaymentInstructions` asserts it.

**Before-fix runs (no working-tree revert).**
- `python3 roadmap/evidence/E5-T23/04-before-fix-tree.py`: exit 0 (`04-before-fix-tree.log`). It copies the repository to `target/before-fix` and gives the 7 changed files under `src/main` and `seeds` their `HEAD` (`9d34d35`) content. The tests are the current ones.
- The same script with `--without-removed-key-check`: exit 0 (`05-before-fix-tree-b.log`). It builds `target/before-fix-b`: the same tree, with E5-T21's `if(document.stored().removed().contains(key)) throw …FILE_NOT_FOUND` disabled.
- `./mvnw -f target/before-fix/pom.xml '-Dtest=ClubDefinitionsIT#T_04_14_…+T_02_12_canicSeedEnables…+T_17_01_enablingSeeded…,ClubDefinitionCodecTest#T_17_01_paymentProviders…,SignupSecurityFixesIT#R_04_19_R_04_08_aD2EditMeets…+R_04_19_R_04_06_aSubmissionRefuses…+R_04_19_R_04_06_aReusedDogsMixedEdit…' -Dsurefire.failIfNoSpecifiedTests=false test`: exit 1 (`06-before-fix-targeted.log`; full method names there). 7 tests, 6 failures, each at its intended assertion:
```
[ERROR]   SignupSecurityFixesIT.R_04_19_R_04_06_aReusedDogsMixedEditMergesOnlyTheTypesItChanges:798 [only the changed type]
[ERROR]   SignupSecurityFixesIT.R_04_19_R_04_08_aD2EditMeetsTheCardRequirementUnlessItChangesTheCard:748->result:79 [HTTP code=DOG_DOCUMENT_REQUIRED details={}]
[ERROR]   ClubDefinitionCodecTest.T_17_01_paymentProvidersAreNamesOrEnabledFlagsAndNeverCredentials:50
[ERROR]   ClubDefinitionsIT.T_02_12_canicSeedEnablesDirectDebitAndCashWithoutConfiguringThem:112
[ERROR]   ClubDefinitionsIT.T_04_14_T_02_12_canicSeedCarriesItsCashPaymentInstructions:151
[ERROR]   ClubDefinitionsIT.T_17_01_enablingSeededProvidersIsOneChangeAndKeepsTheirStoredConfiguration:348
[ERROR] Tests run: 7, Failures: 6, Errors: 0, Skipped: 0
```
  - `:798` is the mixed edit: the reused dog's own insurance, sent back as shown, entered the request.
  - `:748` is sending back the view of a `PENDING` card: `422 DOG_DOCUMENT_REQUIRED`.
  - `:50` expects the seed's `MANUAL` to carry `instructions`, `:112` the stored instructions, and `:151` `club:apply` to report them as one change (it reported 0). `:348` is the diff line of the new instructions.
  - The 7th test, step 3's IT, passes on `HEAD`: E5-T21 already refuses the key.
- `./mvnw -f target/before-fix-b/pom.xml '-Dtest=SignupSecurityFixesIT#R_04_19_R_04_06_aSubmissionRefusesAKeyRemovedFromTheReusedDogsOwnCard' -Dsurefire.failIfNoSpecifiedTests=false test`: exit 1 (`07-before-fix-b-targeted.log`):
```
[ERROR]   SignupSecurityFixesIT.R_04_19_R_04_06_aSubmissionRefusesAKeyRemovedFromTheReusedDogsOwnCard:768->submit:83->result:79 [HTTP code= details=]
expected: 400
 but was: 201
```
  Without the check, `AttachmentService.claimSignup` claims the removed key again: its grant is bound to the same `dogId:VACCINATION_CARD`. The readmission is then accepted with the removed file.

### Step → test table
| Step | Test | Before-fix (log) | After |
|---|---|---|---|
| 1 | `ClubDefinitionsIT#T_04_14_T_02_12_canicSeedCarriesItsCashPaymentInstructions` (new): one change then none, `GET /signup` in `ca` and `es` word for word, a definition without them keeps them, export round-trip, default locale required | FAIL `:151` (`06`) | PASS |
| 1 | `ClubDefinitionsIT#T_02_12_canicSeedEnables…`, `#T_17_01_enablingSeeded…` (changed: stored instructions, `MANUAL.configured: true`, diff line) | FAIL `:112`, `:348` (`06`) | PASS |
| 1 | `ClubDefinitionCodecTest#T_17_01_paymentProvidersAreNamesOrEnabledFlagsAndNeverCredentials` (changed: seed shape; `instructions` refused on `SEPA_XML`, as a string, empty, in `fr`, blank) | FAIL `:50` (`06`) | PASS |
| 1 | `ClubDefinitionCodecTest#T_17_01_storedCashInstructionsAreExportedPlainOrWrapped` (new, unit, helper branches) | not run (the helper is new) | PASS |
| 1 | `bin/e3-smoke` (`PASS cash payment instructions…`, `MANUAL.configured: true`) | not run | PASS ×2 |
| 2 | `SignupSecurityFixesIT#R_04_19_R_04_08_aD2EditMeetsTheCardRequirementUnlessItChangesTheCard` (new) | FAIL `:748` 422 (`06`) | PASS |
| 3 | `SignupSecurityFixesIT#R_04_19_R_04_06_aSubmissionRefusesAKeyRemovedFromTheReusedDogsOwnCard` (new) | PASS on `HEAD` (`06`); FAIL `:768` 201 without E5-T21's check (`07`) | PASS |
| 4 | `TransactionRetriesTest` (Javadoc only) | — | PASS |
| 5 | `SignupSecurityFixesIT#R_04_19_R_04_06_aReusedDogsMixedEditMergesOnlyTheTypesItChanges` (new) | FAIL `:798` (`06`) | PASS |

### Files changed
- `seeds/club-canic.yaml`, `seeds/club-canic-consumer.yaml`: `paymentProviders.MANUAL.instructions` (`ca`, `es`).
- `seeds/club-definition.schema.json`: `$defs.manualPaymentProvider`. `seeds/README.md`: one paragraph.
- `src/main/java/com/agilityhub/core/platform/application/definition/ClubDefinitionMapper.java`: merge, export and the `instructions(Object)` helper.
- `src/main/java/com/agilityhub/core/platform/application/definition/ClubDefinitionWriter.java`: the default-locale check.
- `src/main/java/com/agilityhub/core/clubs/census/application/SignupService.java`: `saveDocuments`, `claimDocuments(…, shown)`, the card requirement, and `storeDog` (passes `null`).
- Tests:
  - `ClubDefinitionsIT`: 1 new test and 2 changed, plus the `CASH_INSTRUCTIONS` constant;
  - `ClubDefinitionCodecTest`: 1 new test and 1 changed;
  - `SignupSecurityFixesIT`: 3 new tests and 2 helpers;
  - `TransactionRetriesTest`: the Javadoc.
- `bin/e3-smoke`: the payment-methods check.
- `docs/openapi/CHANGELOG.md`: the E5-T23 entry, and one line in the E5-T21 entry. `CHANGELOG.md`: the Unreleased entry.
- `roadmap/evidence/E5-T23/*`: logs `00`–`13`, and the scripts `04-before-fix-tree.py`, `10-test-summary.py` and `13-club-apply-twice.py`.

### Rules and tests implemented
- **Rules:**
  - S04 §2 row 19 and R-04-10 (the cash instructions under the total);
  - S12 `MANUAL {instructions: LocalizedText}`, S17 §3 and R-17-05 (the definition's `instructions`, with the default locale), R-17-01 (reapplying is an empty diff);
  - R-04-19 and R-04-08 (the card requirement only on a changed card), R-04-06 (a reused dog's merge; a removed key refused at submission).
- **Tests:**
  - named after spec ids: T-04-14 and T-02-12 (the Cànic's seed), T-17-01 (the definition);
  - named after rules: `R_04_19_R_04_08_…` and the two `R_04_19_R_04_06_…` tests.

### Assumptions
1. **What the schema takes.** `instructions` is accepted on `MANUAL` only, as S12 defines it. It is a `localizedText` (`ca`/`es`/`en`, non-empty).
   - The club's default locale is required, as R-17-05 asks of the console, because «MANUAL: instruccions amb `defaultLocale` obligatori».
   - A definition without `instructions` keeps the stored ones, like every other stored provider setting. There is no way to clear them from a definition yet.
2. **`MANUAL.configured` is now `true` for the Cànic.** The model says «`configured` es deriva de la resta de camps» (`MODEL_DADES_PLATAFORMA.md`, payments row, E3-T14). `PaymentProviderFlags.configured` does exactly that, and its Javadoc lists «creditor, keys, instructions».
   - `bin/e3-smoke` and `T_02_12_canicSeedEnablesDirectDebitAndCashWithoutConfiguringThem` assert this now.
   - I kept that test's name, for traceability with E3-T14. Its Javadoc now says that only `SEPA_XML` stays unconfigured.
3. **The consumer seed carries the same text.** It is the Cànic for the consumer image (`docs/DEPLOY.md:43`), and `DemoSeedsIT` requires both seeds to match.
4. **Step 2's comparison.** The requirement only concerns a card sent without files. For such a card, «its keys differ from D2's view» means that the view shows files for the card. For a reused dog, that view is the one after the merge: the submitted card, or its own.
5. **The contract.** Following the fixed conventions, `openapi.json` is unchanged.
   - `PATCH /dogs/{id}`'s description still says «With signup.requireDogDocumentAtSignup, a VACCINATION_CARD without files answers 422 DOG_DOCUMENT_REQUIRED, unless …». Read with `DogPatch.documents`' «Sending back the view's keys is no change», it now describes the behaviour, but it does not name the no-change case.
   - I added a behaviour-only E5-T23 entry to `docs/openapi/CHANGELOG.md` (0 operations, schemas or descriptions changed), so the web knows the 422 no longer comes for an echoed `PENDING` card. See question 1.
6. **Step 3 tests existing behaviour.** Its failing-before evidence is the tree without E5-T21's check (log `07`), not `HEAD` (log `06`, where it passes).

### Questions / catalog proposals
No new parameter, event, notification or error code. The definition check uses `VALIDATION_ERROR` with the existing field code `REQUIRED`.
1. **`PATCH /dogs/{id}` description.** Should a later contract task add «… when the edit changes the card (D2's view shows files for it)» to its `DOG_DOCUMENT_REQUIRED` sentence? It is a description-only snapshot change, which this task's conventions excluded. Meanwhile: unchanged, and the changelog says it. Blocking: no.
2. **Web.** The Cànic's `GET /signup` now sends `paymentMethods[MANUAL].instructions`, so screen 19 shows the paragraph under the total, and `GET /club` reads `MANUAL.configured: true`.
   - Web fixtures that mirror the Cànic's real responses may need the text.
   - D2 sending back a `PENDING` card no longer gets `422`.

   Blocking: no.
3. **Denied command.** `find src/main -name "LocalizedText*.java" | xargs -I{} sh -c 'echo {}; head -40 {}'; grep …`: I used `find` plus the Read tool instead. Nothing was skipped.

## Organizer verification
Result: verified (26-09-2026, organizer).

- **Review:** `roadmap/reviews/E5-T23-20260926-1530-claude.md` (commit `bdc28c6`): minor issues. It found one minor and four nits.
- **CI:** `bdc28c6`'s run was cancelled by the next push. `e9a58c1`, which contains it, is green.
- **Evidence:**
  - `./mvnw -q clean verify` exit 0: 574 unit and 1016 integration tests (`10-test-summary.log`);
  - `bin/e3-smoke` twice, exit 0;
  - `bin/core club:apply seeds/club-canic.yaml` twice on a fresh Mongo: «15 changes», then «0 changes (applied)».
- **Definition of Done:** steps 1–5 are done, the optional step 5 included. Steps 1, 2 and 5 fail on the unfixed tree (log `06`).
  - Step 3 covers E5-T21's behaviour. It fails once E5-T21's check is disabled (log `07`). That is accepted, because the step asked for the IT and the changelog line.
- **Jordi's text:** the seed carries it word for word, in `ca` and `es` (26-09).
- **Routing:**
  - The review's minor #1 and nits #2–#4 go to `docs/INCIDENCIES_OBERTES.md` INC-13, for the correction pass. They are the blank `MANUAL` instructions, R-17-05 checked only on declared instructions, and two tests' names and scope. None blocks the web, and the Cànic's seed is right.
  - Nit #5 needs no change: per S04 §8, N-01 (the applicant's email) now also carries the cash text as `payment_instructions`. Jordi is told.
  - Question 1 (the `PATCH /dogs/{id}` description) is already api E5-T24 step 1.
  - Question 2 (the web) goes to web E4-W15 step 9: the real-core e2e asserts 19's cash paragraph.
