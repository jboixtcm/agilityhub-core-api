---
id: E1-T09
title: identity: fictional accounts seed for the Cànic (admins, instructors, members) via club:apply
stage: E1
repo: agilityhub-core-api
thread: C
order: 90
status: blocked
depends_on: [E1-T02, E0-T10]
branch: 
pr: 
updated: 2026-09-09
---

# E1-T09 · identity: fictional accounts seed for the Cànic (admins, instructors, members) via club:apply

## Objective
The seeds the front and the E2 stage need: accounts with memberships and roles at the Cànic seed club, fictional only.

## Spec references
S17 WP-17-A (seed sections `admins[]`; extend with `accounts[]`), S01 §3, `docs/specs/S02-*.md` §7 (seeds).

## Context to load
`seeds/club-canic.yaml`, `seeds/club-definition.schema.json` (E0-T10) · S01 §3 · this file.

## Steps
1. Schema: section `accounts[] {email, name, locale, roles[], password? (only for local/test), onboardingPending?}` applied by `club:apply` through `AccountService.getOrCreate` + `MembershipService.setRoles` (idempotent).
2. `seeds/club-canic.yaml`: 2 admins, 3 instructors, 10 members (`@example.test`, Catalan first names, no real people); `seeds/club-minim.yaml`: 1 admin, 2 members; passwords from `SEED_PASSWORD` env, refused in `staging`/`prod` profiles unless `--allow-seed-passwords`.
3. `identity:seed-test-accounts` (E0-T09) becomes an alias of applying the `accounts` section.

## Verification (run all; paste full output in the report)
- `bin/core club:apply seeds/club-canic.yaml` twice → second run 0 changes; `curl` password grant for `admin@example.test` at the Cànic host → `/me` with `roles [ADMIN]`; the same account at the minimal host → `403 NO_MEMBERSHIP`.
- `./mvnw -q verify` green (schema test updated).

## Definition of Done
Seeds applied idempotently; no real data.

## Evidence to return
Outputs above.

---

## Executor report
Resumed on 2026-09-09. The previous session published the implementation and tests but left no report. Reviewed that work and updated its setup documentation. Verification is blocked by local Docker access; no green build or CLI smoke result is claimed.

### Branch / commits
Working tree only. No git write commands. Both dependencies are verified. No Organizer verification section or other task file changed.

### Evidence (full command outputs)
`python3 roadmap/tools/check.py --next` (exit 0):
```text
E1-T09
```

`python3 roadmap/tools/check.py --set E1-T09 in_progress` (exit 0):
```text
E1-T09: status -> in_progress
STATUS.md rendered (37 tasks)
```

`java -version` (exit 0):
```text
openjdk version "21.0.12.1" 2026-08-18
OpenJDK Runtime Environment Homebrew (build 21.0.12.1)
OpenJDK 64-Bit Server VM Homebrew (build 21.0.12.1, mixed mode, sharing)
```

`./mvnw -q verify > /tmp/e1-t09-verify.log 2>&1` (exit 1; full captured output):
```text
mkdir: /Users/jordib/.m2/wrapper/dists/apache-maven-3.9.16: Operation not permitted
```

`MAVEN_USER_HOME=/tmp/e1-t09-maven ./mvnw -q -Dmaven.repo.local=/tmp/e1-t09-maven/repository verify > /tmp/e1-t09-verify.log 2>&1` (interrupted, exit 130, after the Docker blocker was confirmed): no output. This temporary-cache retry did not produce a test result.

`docker ps --format '{{.Names}} {{.Status}} {{.Ports}}'` (failed):
```text
failed to connect to the docker API at unix:///Users/jordib/.docker/run/docker.sock; check if the path is correct and if the daemon is running: dial unix /Users/jordib/.docker/run/docker.sock: connect: no such file or directory
```

`open -a Docker` (exit 1):
```text
Unable to find application named 'Docker'
```

`open '/Applications/Docker.app/Contents/MacOS/Docker Desktop.app'` (failed):
```text
The application cannot be opened for an unexpected reason, error=Error Domain=NSOSStatusErrorDomain Code=-10827 "kLSNoExecutableErr: The executable is missing" UserInfo={_LSLine=4277, _LSFunction=_LSOpenStuffCallLocal, _LSFile=LSOpenCore.mm, _LSErrorMessage=kLSNoExecutableErr}
```

`docker desktop start` (exit 1 after cancellation; full output):
```text
Failed to fire hook: opening log file: unable to create a log file for docker-desktop in directory /Users/jordib/Library/Containers/com.docker.docker/Data/log/host: open /Users/jordib/Library/Containers/com.docker.docker/Data/log/host/docker-desktop.log: operation not permitted
Failed to fire hook: opening log file: unable to create a log file for docker-desktop in directory /Users/jordib/Library/Containers/com.docker.docker/Data/log/host: open /Users/jordib/Library/Containers/com.docker.docker/Data/log/host/docker-desktop.log: operation not permitted
✗ Failed to start Docker Desktop
Docker Desktop is still starting: context canceled
```

`curl -fsS --max-time 5 http://127.0.0.1:8080/api/v1/health` (exit 22):
```text
curl: (22) The requested URL returned error: 500
```

`git diff --check` (exit 0): no output.

Outstanding Verification commands: rebuild and run `./mvnw -q verify`; apply the Cànic definition twice with a process-only `SEED_PASSWORD`; apply the minimal definition; run the password-grant and `/me` curl checks for ADMIN and cross-tenant `403 NO_MEMBERSHIP`. These were not run against the stale packaged jar or the unhealthy existing API. Retain full output and truncate tokens/hashes when adding their evidence. The current tests and coverage have not been validated in this session.

### Files changed
This resumed session:
- `README.md`, `seeds/README.md`, `.env.example`: current seed counts, exact roles, alias syntax, password guard and insert-only account semantics.
- `CHANGELOG.md`: Unreleased entry, explicitly awaiting verification.
- `roadmap/tasks/E1-T09.md`, generated `roadmap/STATUS.md`, append-only `roadmap/MESSAGES.md`: report and access blocker.

Previous session's implementation, reviewed as part of this same task:
- `seeds/club-canic.yaml`, `seeds/club-minim.yaml`, `seeds/club-definition.schema.json`.
- `src/main/java/com/agilityhub/core/identity/application/{AccountService,ClubAccountService,SeedPasswordPolicy,SeedTestAccountsCommand}.java`.
- `src/main/java/com/agilityhub/core/identity/persistence/AccountRepository.java`.
- `src/main/java/com/agilityhub/core/platform/application/ClubAccountProvisioner.java`.
- `src/main/java/com/agilityhub/core/platform/application/definition/{ClubApplyCommand,ClubDefinitionCodec,ClubDefinitionWriter,ClubDefinitions}.java`.
- `src/test/java/com/agilityhub/core/cli/CoreCliTest.java`.
- `src/test/java/com/agilityhub/core/configuration/SeedProfileTest.java`.
- `src/test/java/com/agilityhub/core/identity/api/SeedTestAccountsIT.java`.
- `src/test/java/com/agilityhub/core/platform/application/definition/{ClubDefinitionCodecTest,ClubDefinitionsIT}.java`.

### Rules and tests implemented (R-xx-nn / T-xx-nn)
Existing implementation, pending execution:
- S01 §3 / task steps 1–2: global account creation through `AccountService.getOrCreate`; tenant-scoped exact roles through `MembershipService.setRoles`; fictional Cànic 2/3/10 and minimal 1/0/2 role populations; no platform admins.
- `T_01_02_seedRolesAndCredentialsAreIdempotentAndTenantBound`: dry-run isolation, unchanged second apply, role counts, argon2 credentials, ADMIN `/me`, cross-tenant rejection and password-free export.
- `T_01_02_existingGlobalAccountAndMemberLinksSurviveExactRoleUpdates` and `T_01_02_passwordlessSeedInitializesOnboardingWithoutConsentOrCredentialReset`: global identity preservation, links/status/profile handling and passwordless initialization.
- `T_01_02_seedPasswordsRequireAllowedProfileAndExplicitDeploymentOverride`, `T_01_02_seedPasswordHasNoFallbackAndDefaultLocalProfileWorks`: profile combinations, explicit override and no fallback secret.
- S17 WP-17-A / `T_17_01_accountsRequireClosedRolesAndUniqueNormalizedEmails`: schema validation, closed roles, duplicate email rejection across admins/accounts, optional fields and no platform-role injection.
- `T_17_01_aliasAppliesOnlyAccountsAndSupportsDryRun`, `T_17_01_seedAliasAndApplyForwardOnlyExplicitPasswordOverrides`: accounts-only alias, required existing club, dry-run and strict CLI flags.
- Existing `T_02_12` and `T_17_01` club-apply tests updated for account counts, export and transaction/audit rollback.

### Assumptions
- New account name, locale, password and onboarding flag are insert-only. Reapplying or applying another club preserves global identity and credentials. Declared membership roles are reconciled exactly; existing status and member/instructor links remain intact.
- `onboardingPending: false` does not fabricate consent or email verification. Census/team records and their links belong to later tasks.
- Legacy `admins[]` retains additive ADMIN behavior and passwordless creation. Export now includes all membership roles through `accounts[]` and omits credentials.
- Literal passwords are local/test only. Staging/prod allow only `${SEED_PASSWORD}` with the explicit task-authorized flag, including when combined with local/test profiles. Unrecognized profiles do not accept passwords.
- The alias resolves `--club=<slug>` to `seeds/club-<slug>.yaml`, or accepts a definition path; its club must already exist.
- No HTTP contract changed; snapshot regeneration is not needed for this CLI task. No parameter/event/notification/error catalog additions.

### Questions / catalog proposals
@jordi: Please start Docker Desktop outside the executor sandbox and make its Docker socket accessible to the next session. `docker desktop start` cannot write its required host log here, and the socket is absent. Assumption on resume: use a writable temporary Maven cache, run Testcontainers and a local Mongo replica set, rebuild the jar, then execute every outstanding Verification command. No catalog proposals.

## Organizer verification
_(pending)_
