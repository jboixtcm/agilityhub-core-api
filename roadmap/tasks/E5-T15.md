---
id: E5-T15
title: Follow-ups of the E5-T06 and E5-T07 round-2 reviews — S05 ring-change retry, ring_slot_locks setup, S06 duplicate-key retry, R-09-13 on the week paths, test names, ArchUnit seed rule, perf notes, D1 ring label, staff class projection, and the risk-row statuses of ruling E37
stage: E5
repo: agilityhub-core-api
thread: A
order: 150
status: ready
depends_on: [E5-T14]
branch:
pr:
updated: 2026-09-24
---

# E5-T15 · Follow-ups of the E5-T06 and E5-T07 round-2 reviews

## Objective
Close the minor findings that the organizer accepted from the reviews `roadmap/reviews/E5-T06-20260924-1157-claude.md` and `roadmap/reviews/E5-T07-20260924-1229-claude.md`. Read each finding in its review first. Nothing here changes the contract.

## Spec references
- S09 R-09-13 (`ring_slot_locks` in `MODEL_DADES_PLATAFORMA.md` since 24-09) and S05 R-05-08.
- S06 R-06-05 (the `RING_TRAINING_CONFLICT` inconsistency) and R-06-08 (whole-week validation).
- `docs/DECISIONS_PENDENTS.md` E26 (retry budgets, one instance in R1).
- S15 T-15-30 and S08 T-08-41 (the k6 runs).

## Context to load
This file, the two reviews, `CatalogService`, `SchedulingTransactions`, `RingSlotLockRepository`, `TrainingLanesOffIT`, `CacheInvalidationRaceIT`, `ArchitectureRules`, `bin/e5-perf`, `perf/README.md`.

## Steps
1. **S05 ring change retries (E5-T07 review #1).**
   - Wrap the ring branch of `CatalogService.update` (deactivate, or `allowsFreeTraining` off) in the same jittered 50–150 ms retry the other contexts use, with the E26 budget.
   - Add a lanes-off test of the other order through the real `CatalogService.update`: the ring change commits first, and the retried booking gets `422 RING_NOT_RESERVABLE`.
   - Report the new `201 / 409` split of the 4-round burst.
2. **`ring_slot_locks` setup (E5-T07 #3).** Create the collection in a `schedulingCollections` `ApplicationRunner`, like the other `*Collections` runners, not in the repository constructor. Keep the pre-creation: it avoids the implicit-creation abort you found.
3. **S06 duplicate key (E5-T07 #4).** `SchedulingTransactions` also retries on `DuplicateKey` (11000), as `TrainingTransactions.retryable` does. Unit test.
4. **R-09-13 on the week paths (E5-T07 «Not checked»).**
   - Trace the paths that place classes on rings outside `ClassSessionService.create/patch`: week generation, validation, and P1 `week-opening`.
   - Confirm that a live training booking on the ring makes the week inconsistent (`RING_TRAINING_CONFLICT`) and blocks the validation.
   - Add the test if it is missing, and report what you found.
5. **Test names (AGENTS rule 5).**
   - E5-T06 #2: `CacheInvalidationRaceIT` `T_02_01_moduleToggle…` → `T_02_09_…`, and the two `T_02_11_domain…` → `T_02_06_…`.
   - E5-T07 #5: `SchedulingTransactionsTest.T_06_23_…` → `E5_T07_schedulingRetriesWaitAJitteredBackoff…`. Optional: inject the jitter source, so the test does not sleep.
   - E5-T14 review #1: `WaitlistIT` `R_08_13_anOfferWithNoN15RecipientIsNotMarkedAndGetsNoN46` → `T_08_20_…` (ALL_AT_ONCE / N-15), and `R_08_13_R_08_14_anExpiredFifoOfferNeverGetsN46` → `T_08_21_…` (FIFO expiry). Keep the rule ids in their comments.
6. **ArchUnit seed rule (E5-T06 #4).** `BOOKING_TIME_OVERRIDE` allows only the classes whose names are `Demo*Seeder`, `DemoMembers` or `DemoSeed*`, and never in `..api..` packages. Extend `SeedAndWriterRulesTest` for both limits.
7. **Perf notes (E5-T06 #1 and #5).**
   - `bin/e5-perf` (its comment) and `perf/README.md` say «P1 before the peak»: the gated peak starts after `week-opening` has finished. T-15-30 «while P1 fans out» stays open for the release performance pass.
   - `perf/README.md` also says the seat gates rely on an even spread (VUS a multiple of the class count), so a change of pool size is not read as a regression.
8. **Optional (E5-T06 #3).** Drive one host test of `CacheInvalidationRaceIT` through the real path that changes a club's domains (`club:apply` or its service), not a raw update.

9. **D1 ring label in Catalan (E5-T10 step 13 #9).** Two uses need two keys.
   - `scheduling.noRing` stays the short **column** label of `GET /day-grid` (ca «Sense», as on D3b).
   - The D1 rows get their own key, e.g. `dashboard.noRing` («Sense pista» / «Sin pista» / «No ring»), because «Sense» alone does not read on a row.
   - Update the tests that assert them.
10. **Staff projection of `GET /class-sessions/{id}` (web E4-W03 question 3).** Add `instructorNames[]` and `ring {id, name, color}` (the member projection already has `ring`). The web drawer on 23 and the E6 screen 21 need them. This is an additive OpenAPI change: regenerate the snapshot and add a `docs/openapi/CHANGELOG.md` entry.
11. **Risk-row statuses after the review time (ruling E37; S15 §6, amended 24-09).**
    - `GET /risk-review` (form A) returns `WILL_CANCEL`/`WILL_REVIEW` only when P2 will really run for that class: the `risk-review` job is enabled, and the class's `reviewAt` is still in the future.
    - Otherwise an at-risk class is `AT_RISK`, with `notified` when there was a notice. That covers the time after today's review, and a switched-off job.
    - D1 then never says «s'anul·larà … a les 7:30» about something that will not happen.
    
    Tests (T-15-15):
    - a class today at 17:40 with 0 booked, read at 10:00 → `AT_RISK`;
    - the job switched off → `AT_RISK`;
    - tomorrow's class, before the review → `WILL_CANCEL`.
12. **The club's dry-run counter past 500 items (E5-T13 review #2).** `JobRunner` counts the whole plan but traces only its first 500 items. `JobViews` (`:75`) subtracts only the platform items that are in the trace, so 500 tenant items plus 1 platform item show `WOULD_DELETE=501` to the club. Record the tenant and platform totals **before** the truncation, and build the club counters from them. Add an ADMIN-view test with more than 500 items.
13. **Releasing the platform claim together with `FAILED` (E5-T13 review #3).** `JobRunner` (`:229`) commits the run as `FAILED` before the release hook runs. A crash in between, or an exception swallowed at `:235-237`, keeps the claim until midnight, and the reaper looks only at `RUNNING` rows. Release the claim in the same transaction as the completion, or let the claim acquisition take over a claim whose run is `FAILED`. Test a release that fails after the failed row is committed: the next run of the day still starts.

14. **Follow-ups of the E4-T06 review (S07).**
   - **`ActivityRegistrationListItem.position` (review #1).** Make its description match the code: a waitlisted registration keeps its position after it is cancelled, and the position is `null` once promoted, or if it was never waitlisted. Assert it in `T_07_21_…`.
   - **`Activity.endsAt` and `MemberActivityDetail.endsAt` (review #2).** Describe them as derived instants: for an activity without `endTime` they are the next local 00:00, and clients display `startTime`/`endTime`.
   - **`fields=` on the D7 list (review #3).** A property that was not selected is omitted or `null`, never a primitive default such as `allRings: false`.
   - **The app rows (E4-T06 question 2, R-07-13).** `RegisteredActivity` and `ActivityRow` carry `startTime` and `endTime`, `null` when absent, so a date-only activity never reads «0:00».
   - These changes are additive.

## Fixed conventions
- The AGENTS.md rules apply.
- No new parameters, events, notifications or error codes.
- The OpenAPI changes are steps 10 and 14: regenerate the snapshot once and record both in the changelog.

## Verification (run all; evidence = command + exit code + last 40 lines; long outputs → `roadmap/evidence/E5-T15/*.log`)
- `./mvnw -q clean verify` → exit 0, plus a committed test summary.
- The lanes-off lines of steps 1 and 4 from that run.

## Definition of Done
- Every step is done, with a test named after the spec id where one applies.
- `clean verify` is green.

## Evidence to return
The report sections below, and the step → test table.

---

## Executor report
_(pending)_

## Organizer verification
_(pending)_
