---
id: E5-T05
title: Scheduled processes of E5 (S15 WP-15-C + the api half of WP-15-B): P1 week opening, P2 risk review with N-16/N-17/N-08a and the N-54 ClassBelowMinimum consumer, P6 waitlist FIFO, P7 payment timeouts, P9 cleanup, plus the /jobs* and /risk-review endpoints
stage: E5
repo: agilityhub-core-api
thread: A
order: 50
status: awaiting_verification
depends_on: [E5-T01, E5-T02, E5-T03, E5-T04]
branch:
pr:
updated: 2026-09-24
---

# E5-T05 · Scheduled processes of E5 (S15 WP-15-C + the api half of WP-15-B)

## Objective
Work package S15 WP-15-C plus the back half of WP-15-B: five of the ten processes become real `Job` beans on E5-T01's framework — `week-opening` (P1), `risk-review` (P2), `waitlist-fifo` (P6), `payment-timeouts` (P7) and `cleanup` (P9) — together with R-15-12b's immediate `ClassBelowMinimum` → **N-54** alert, and the admin API that drives them (`GET /jobs`, `/jobs/{name}/runs`, `/runs/{runId}`, `POST /jobs/{name}/trigger`, `PUT /jobs/{name}/switch`, `GET /risk-review`, `/platform/jobs/overview`, `/platform/clubs/{clubId}/jobs/{name}/trigger`). P3 and P8 are E6, P4 is E7, P5 and P10 are E8: their catalog rows stay declared and their `GET /jobs` rows absent until their `Job` bean exists.

## Spec references
S15 R-15-01 (catalog rows `WEEK_OPENING`, `RISK_REVIEW`, `WAITLIST_FIFO`, `PAYMENT_TIMEOUTS`, `CLEANUP` with their cadence, parameter, module and catch-up window), R-15-02…R-15-10 (the framework rules these five must honour), R-15-11 (**P1**), R-15-12 (**P2**), R-15-12b (`ClassBelowMinimum` → N-54; the review itself stays at 07:30), R-15-16 (**P6**), R-15-17 (**P7**), R-15-19 (**P9**), R-15-21, R-15-22, §3 (the marks `Week.openedAt/openingNotifiedAt`, `ClassSession.risk{…}`, `finishedAt`), §5, §6 (every `/jobs*` row, `GET /risk-review` form A, `/platform/jobs*`), §7, §8 (N-33, N-17, N-08a, N-16, N-42), §9, §10 back keys, §13 items 2, 3, 4, 5, 8, 10, 13, 14 · tests **T-15-08, T-15-09, T-15-11, T-15-12, T-15-13, T-15-14, T-15-15, T-15-24, T-15-25, T-15-27** and the scheduler contracts **T-08-34, T-08-35** · S06 R-06-10 (`ClassCancellationUseCase.cancel(classId, RISK_REVIEW, adminText)`), R-06-13 (`RiskEvaluator`) — both implemented in E4-T03 · S08 R-08-14, R-08-18 (`WaitlistService.offerNext`, `BookingCancellationService.cancel(by = SYSTEM, reason = PAYMENT_TIMEOUT)`) · S09 R-09-05 (nothing to reset) · S14 R-14-09 (`JOB_TRIGGERED`, added in E5-T01) · `CATALEG_NOTIFICACIONS.md` N-33, N-16, N-17, N-08a, N-42, **N-54** · `CATALEG_ESDEVENIMENTS.md` `WeekOpened`, `TrainingCounterReset`, `ClassAtRisk`, `ClassAutoCancelled`, `ClassBelowMinimum`, `ClassCancelledByClub`, `WaitlistExpired`, `SchedulerRun`, `JobFailed` + Annex A · `CATALEG_PARAMETRES.md` `jobs.*`, `classes.riskReviewTime`, `classes.riskLookaheadDays`, `classes.minDogs`, `classes.riskAutoCancelSameDay`, `messaging.notifyWeekOpening`, `bookings.weekOpensAt`, `bookings.paymentPendingMinutes`, `waitlist.mode`, `waitlist.notifyThresholdMinutes`, `jobs.retention.*` · `MATRIU_PERMISOS.md` row «Processos programats (S15)».

## Context to load
`docs/specs/S15-processos-programats.md` complete · `docs/specs/S06-planificacio-setmanes-i-calendari.md` R-06-10, R-06-13, §8 (N-08a) only · `docs/specs/S08-reserves-de-classes-i-llista-espera.md` R-08-11, R-08-13, R-08-14, R-08-16, R-08-18 only · `docs/specs/S14-tauler-auditoria-exportacions-rgpd.md` R-14-09 only · `roadmap/tasks/E5-T01.md` (the framework: `JobCatalog`, `Job`, `JobContext`, `JobOccurrences`, `SchedulerTick`, `JobLocks`, `JobRunner`, dry run, metrics, `JobFailed`/N-42, the `TEST_NOOP` job and `POST /test/clock`) · `roadmap/tasks/E5-T02.md`, `E5-T03.md`, `E5-T04.md` (the services these processes call) · `src/main/java/com/agilityhub/core/platform/application/jobs/**`, `platform/persistence/**` · `clubs/scheduling/application/{ClassSessionService,ClassCancellationUseCase,RingBlockService}.java`, `clubs/scheduling/domain/RiskEvaluator.java`, `clubs/scheduling/application/ports/ClassBookingsPort.java` · `clubs/bookings/application/**`, `clubs/training/application/**` · `clubs/messaging/application/SystemNotificationService.java`, `shared/application/{OutboxDispatcher,DomainEventHandler,EventPublisher}.java` · `clubs/common/application/**` and the export/S3 helpers P9 needs (`export_jobs`, the signup upload keys of E3, the local-profile file store) · `platform/application/audit/*` · `docs/playbooks/new-scheduler.md`, `new-event-consumer.md`, `new-endpoint.md` · this file.

## Steps
1. **`/jobs*` endpoints** (R-15-09, replacing E5-T01's 501 stubs; ADMIN only, impersonation tokens → 403): `GET /jobs` builds one row per catalog process **that has a registered `Job` bean and whose module is on**, with the localized name (`admin-settings:jobs.name.*` is front-side; the back returns `name`, `jobName`, `module`, `enabled`, `schedule{kind, localTime?, dayOfWeek?, dayOfMonth?}`, `nextScheduledForLocal`, `lastRun{runId, status, finishedAt, trigger, dryRun, counters}`); `GET /jobs/{name}/runs` = universal list over `job_runs` filtered by tenant; `GET /jobs/{name}/runs/{runId}` returns `effects.items`, `errors[]` and `parametersSnapshot`; `POST /jobs/{name}/trigger {dryRun}` runs the same code and the same lock with `scheduledFor = now` truncated to the minute and `trigger = MANUAL`, **even when the switch is off** (the switch stops the calendar, not the admin — principle 0.3.7), synchronous 200 with the `JobRun`, `409 JOB_ALREADY_RUNNING` when the lock is held, `404 MODULE_DISABLED` when the module is off, `JOB_UNKNOWN` for an unknown route id, `@Audited(JOB_TRIGGERED)`; `PUT /jobs/{name}/switch {enabled}` writes the parameter `jobs.<name>.enabled` through S02's parameter service (`ParameterChanged`, audited `PARAMETER_CHANGED`, config cache invalidated). Platform routes: `GET /platform/jobs/overview` (matrix club × process with `health OK|WARN|ALERT`: green = last success inside the period, amber = `PARTIAL`, red = `FAILED` or no success in more than two periods) and `POST /platform/clubs/{clubId}/jobs/{name}/trigger`, both AGILITYHUB_ADMIN. T-15-08, T-15-09.
2. **P1 `week-opening`** (R-15-11, `JobName.WEEK_OPENING`, weekly at `bookings.weekOpensAt`, catch-up 24 h, no module): **no business write** — `W0`/`W1` are functions of time (S08 R-08-01) and the training counter is per session week (S09 R-09-05). One transaction: invalidate the club's config and grid caches; warm the base cache of `GET /me/bookable-classes` (the `ACTIVE` classes of `W0…W2` with counters, no per-dog state, TTL 30 s — agree the cache key with E5-T06 and say in the report which class owns it); set `targetWeek.openedAt = opensAt` on the `Week` (get-or-create) that contains `openedWeekKey + 1 day`; emit `WeekOpened {openedWeekKey, isoWeekStart, currentWeekKey, opensAt, notified}` and, with `FREE_TRAINING`, `TrainingCounterReset {weekStart: opensAt}`. When `messaging.notifyWeekOpening` is true **and** the target week has at least one `ACTIVE` class → **N-33** to every `ACTIVE` member with at least one `ACTIVE` dog (APP + PUSH intent, variable `week_start` = `isoWeekStart` formatted «setmana del 12 d'octubre», action `OPEN_BOOKING`) and `openingNotifiedAt`; when the week is not validated yet the notice is **deferred**: a `WeekValidated` consumer sends it if `now ≥ Week.openedAt ∧ openingNotifiedAt == null`. Fan-out in batches (feed rows `insertMany` of 500, push in batches of 100 on a pool that does not share threads with the API). Dry run: `WOULD_OPEN {weekKey, activeClasses, recipients}`. T-15-11, T-15-12.
3. **P2 `risk-review`** (R-15-12, `JobName.RISK_REVIEW`, daily at `classes.riskReviewTime` 07:30, catch-up until the end of the local day, no module): scope = `ClassSession` `ACTIVE` with `date ∈ [today, today + classes.riskLookaheadDays]`, `risk.exempt = false`, `countedDogs < classes.minDogs`, where **`countedDogs` is recounted inside the transaction** (bookings `ACTIVE` + `PAYMENT_PENDING` through `ClassBookingsPort`), never trusted from `counters.booked`. Per class: (a) `date == today ∧ classes.riskAutoCancelSameDay` → `startsAt ≤ now` counts as `skippedStarted` (a late run never cancels a class that has begun), otherwise `ClassCancellationUseCase.cancel(classId, RISK_REVIEW, messages «scheduling.autoCancel.text»)` (E4-T03's transaction: bookings → `CANCELLED_BY_CLUB`, waitlist → `CANCELLED{CLASS_CANCELLED}`, packs refunded, holds deleted, counters zeroed) plus `ClassAutoCancelled {classId, dogsCount, affected[], waitlistIds[], adminText}`; (b) otherwise `ClassAtRisk {classId, dogsCount, reviewAt, newBookingIds[], notifyAdmins}` → **N-16** to every registrant not already in `risk.notifiedBookingIds` and to the ADMINS when `risk.adminNotifiedAt == null`, then update `risk`. Notifications: **N-17** «Classe anul·lada per manca d'alumnes» to the class's INSTRUCTORS + ADMINS (APP + EMAIL) on **every** auto-cancellation, including a class with 0 registrants (`dogs_count = 0`, decision §13-3); **N-08a** to the affected registrants (APP + EMAIL + SMS intent) — reuse E4-T03's `notifications.N-08a` handler, which already filters `reason ∈ {CLUB_MANUAL, DELETED, ACTIVITY}`: extend it to `RISK_REVIEW` **or** add the N-17/N-08a pair here and say exactly which handler owns which reason in the report; **N-16** «Possible anul·lació de classe» with `dog_name`, `class_date`, `class_time`, `review_time`, `review_day` (the class's day) and `auto_cancel`. Counters `{reviewed, cancelled, cancelledSilent, atRisk, notifiedMembers, exempt, skippedStarted}`; dry run `WOULD_CANCEL` / `WOULD_NOTIFY`. Each class's cancellation is its own transaction (R-15-10 exception). T-15-13, T-15-14.
4. **R-15-12b immediate low-class alert**: a consumer of `BookingCancelled{late: false}` and of `WaitlistExpired` (when the class ends up unchanged) that, inside its own transaction, checks the class is `ACTIVE`, future, `risk.exempt = false` and `countedDogs < classes.minDogs` and then emits `ClassBelowMinimum {classSessionId, countedDogs, minDogs}` → **N-54** «Classe amb pocs alumnes» to the class's INSTRUCTORS and the ADMINS (APP + EMAIL, variables `class_date`, `class_time`, `class_description`, `ring_name`, `dogs_count`, action `CHANGE_CLASS`) and **nothing else**: no student is warned and the class is untouched. Idempotency mark `ClassSession.risk.lowAlertSentAt`, **cleared** when `countedDogs` reaches `classes.minDogs` again so a second drop alerts again; nothing is sent when `date == today ∧ startsAt ≤ now`. E5-T02 already emits the event from the cancellation transaction: here implement the N-54 handler and the clearing path, and add the test that a second in-time cancellation on the same class sends nothing while the first alert stands. (Josep 08-09, decision B17.)
5. **P6 `waitlist-fifo`** (R-15-16, `JobName.WAITLIST_FIFO`, every minute, module `WAITLIST` **and** `waitlist.mode = FIFO`, continuous catch-up): `WaitlistEntry` `NOTIFIED` with `confirmBy ≤ now` → `EXPIRED` + `WaitlistExpired {entryId, classId}` (one transaction per entry); S08's `offerNext` consumer (E5-T03) notifies the next one while seats and time remain. In `ALL_AT_ONCE` the process records `SKIPPED{MODULE_OFF}` at most once per hour. Dry run `WOULD_EXPIRE {entryId, position}`. T-15-24, T-08-34.
6. **P7 `payment-timeouts`** (R-15-17, `JobName.PAYMENT_TIMEOUTS`, every minute, module `SINGLE_CLASS`, continuous): `Booking` `PAYMENT_PENDING` with `bookedAt + bookings.paymentPendingMinutes` (30) `≤ now` → `BookingCancellationService.cancel(bookingId, by = SYSTEM, reason = PAYMENT_TIMEOUT)` → `CANCELLED`, `SeatReleased`, N-40 (the handler is E5-T02's). Idempotent by state; two runs cancel once. Dry run `WOULD_CANCEL {bookingId, minutesPending}`. The module is off at the Cànic: test it on a fictional club that enables `SINGLE_CLASS` (and assert the `SKIPPED{MODULE_OFF}` row at the Cànic). T-15-25, T-08-35.
7. **P9 `cleanup`** (R-15-19, `JobName.CLEANUP`, daily at `jobs.dailyTime` 06:00, unlimited catch-up) — the **only** place where documents are physically deleted, and only technical ones: report (never delete) the `seat_holds`, `magic_link_tokens`, `idempotency_records` and `job_locks` whose TTL has not caught up yet (`ttlPending.*`); delete orphan `signup/{clubId}/…` S3 keys older than `jobs.retention.orphanUploadsHours` (48) not referenced by any `DogDocument.files[].fileKey` (`orphanUploadsDeleted`); purge finished `export_jobs` older than `jobs.retention.exportFilesDays` (7), deleting the file and marking the record `PURGED` (`exportsPurged`); delete `domain_events` with `processedAt < now − jobs.retention.domainEventsDays` (90) and **never** unprocessed ones (`domainEventsDeleted`); delete `stripe_events` older than `jobs.retention.stripeEventsDays` (400) if that collection exists yet — skip with a counter of 0 and say so if it does not (`stripeEventsDeleted`); delete `job_runs` with `finishedAt < now − jobs.retention.jobRunsDays` (90) **always keeping the last 5 runs of each process** (`jobRunsDeleted`). No business events; dry run = the same counters under `WOULD_DELETE`. T-15-27.
8. **`GET /risk-review`** (S15 §6 form A, ADMIN + INSTRUCTOR, MEMBER → 403): `reviewTime`, `lookaheadDays`, `minDogs`, `autoCancelSameDay` from the parameters and `items` = the `ACTIVE` at-risk classes plus the ones already `CANCELLED{RISK_REVIEW}` of `[date, date + lookaheadDays]`, ordered by `startsAt`, each with `dayLabel TODAY|TOMORROW|OTHER`, `bookedCount`, `status AUTO_CANCELLED|AT_RISK|WILL_CANCEL|WILL_REVIEW` (`WILL_REVIEW` when `classes.riskAutoCancelSameDay = false`), `reviewAt` and `notified[{memberName, dogName}]` resolved from `risk.notifiedBookingIds` (or from the cancellation's affected bookings). S14 embeds this in `GET /dashboard`: wire E3-T04's dashboard risk card to it and remove any remaining double. T-15-15.
9. **Registration and guards**: register the five `Job` beans in the catalog order of R-15-01, each declaring its module and catch-up window; verify through the framework that the switch, the module and the club status produce the right `SKIPPED{…}` rows (continuous jobs at most one per hour), that a `CATCH_UP` run behaves as R-15-05 describes (`risk-review` at 23:59 → `CATCH_UP`; at 00:01 the next day → `MISSED_WINDOW` + N-42; `week-opening` 26 h late → `MISSED_WINDOW` with no business effect lost), and that `plan` ≡ real `effects.items` for each of the five (T-15-06 extended). Every process reads its configuration through `ClubConfig`, never a constant, and writes `parametersSnapshot`.
10. **CLI**: expose each process as a `CoreCommand` (`jobs:run <route-id> [--club=] [--dry-run]`) so staging and `bin/e5-smoke` can drive them without the scheduler, documented in `README.md` next to the existing `scheduling:finish-ended`.

## Fixed conventions
- Statuses: `JOB_ALREADY_RUNNING` **409** (explicit in §1), `MODULE_DISABLED` **404**, `JOB_UNKNOWN` and `NOT_DUE` **422** by rule 0 (S15 §6 writes 404 for `JOB_UNKNOWN`; E5-T01 already filed the proposal to move it to §1 — do not change it again), `VALIDATION_ERROR`/`INVALID_FILTER` **400**, impersonation on any `/jobs*` route **403**.
- Assumptions in force (do not ask): there is **no** immediate risk review when a student cancels — only R-15-12b's alert; the action stays at 07:30 (§13-2, B17). N-17 is sent even with 0 registrants and N-16 reaches the admins only the first day (§13-3, §13-4). A class auto-cancelled by the review is never reactivated: the admin creates it again (§13-5). Manual execution is synchronous at R1 (§13-13). Processes run for clubs `ACTIVE` **and** `ONBOARDING` (§13-14, A18e). Retention values are the catalog's (§13-10). P3 and P8 are E6, P4 is E7, P5 and P10 are E8 — declare their catalog rows, do not implement them, and make sure `GET /jobs` simply omits the rows without a bean.
- Every item is applied in its own Mongo transaction with the outbox inside it, except P1 (one transaction) and each class cancellation of P2 (S06's transaction); a failed item goes to `errors[]` and the run is `PARTIAL`; a job-level exception is `FAILED` + `JobFailed` → N-42 (at most one per process and club-local day). Dry run writes nothing anywhere but the `JobRun`. `Clock` injected everywhere (the E5-T01 ArchUnit rule); occurrences computed in `club.timeZone`.
- Context direction (ArchUnit): the `Job` beans live in the vertical that owns the effect (`clubs.scheduling` for P2, `clubs.bookings` for P6/P7, `platform`/`clubs.common` for P1's cache warm-up and P9) and depend on `platform.application.jobs`; `platform` never imports `clubs`.
- Closed catalogs: no new parameter / event / notification / error code / audit action; proposals to the report and `MESSAGES.md`. Evidence rule: report = command + exit code + last 40 lines; long outputs → `roadmap/evidence/E5-T05/NN-<name>.log`; task file ≤ 120 KB; fictional data only (`@example.test`); no secrets, env vars only; never edit other tasks, `ROADMAP.md` or the Organizer verification section.

## Verification (run all; evidence = command + exit code + last 40 lines; long outputs → `roadmap/evidence/<ID>/*.log`)
- `./mvnw -q verify` green with the tests named after T-15-08, T-15-09, T-15-11, T-15-12, T-15-13, T-15-14, T-15-15, T-15-24, T-15-25, T-15-27, T-08-34, T-08-35 plus the R-15-12b/N-54 test, `E5ContractIT` updated (the `/jobs*` and `/risk-review` routes no longer answer 501), `ArchitectureTest`, `AuditContractTest` (`JOB_TRIGGERED` covered), `EventCatalogContractTest`, message parity ca/es/en for `notif.N-33.*`, `notif.N-16.*`, `notif.N-17.*`, `notif.N-54.*` and `scheduling.autoCancel.text`, coverage gates.
- OpenAPI snapshot byte-identical to a fresh `bin/openapi-snapshot` (or a `docs/openapi/CHANGELOG.md` entry for any field you had to add); `git diff --check` clean.

## Definition of Done
With the clock advanced to Sunday 20:00 the opening runs once, emits `WeekOpened` and queues N-33 for the right members; at 07:30 the review cancels today's under-strength classes in one transaction each with N-17 to the staff and N-08a to the students, warns tomorrow's once per registrant, and leaves exempt classes alone; an in-time cancellation that drops a class below the minimum alerts instructors and admins once with N-54 and does nothing else; a FIFO offer expires into the next member; a stale `PAYMENT_PENDING` booking is cancelled after 30 minutes and its seat released; cleanup purges only technical data and keeps the last five runs; the admin can list, simulate, run and switch every implemented process, and `GET /risk-review` feeds D1.

## Evidence to return
`verify` tail; the T-15 method list; for each of the five processes, the `JobRun` document of a dry run and of the real run over the same fixture (proving `plan ≡ effects`), with ids truncated; a `curl` sequence on the local stack: `GET /jobs` → `POST /jobs/risk-review/trigger {dryRun: true}` → the plan → `{dryRun: false}` → the summary → `GET /jobs/risk-review/runs/{runId}` → `GET /risk-review`; the `notifications` rows produced (N-17, N-08a, N-16, N-54) and the outbox events.

---

## Executor report

Done (executor claude-opus-5-5, session 2026-09-24). What ships:

- **Five processes** are real `Job` beans, in R-15-01 order:
  - P1 `week-opening` (`clubs.common`)
  - P2 `risk-review` (`clubs.scheduling`)
  - P6 `waitlist-fifo` and P7 `payment-timeouts` (`clubs.bookings`)
  - P9 `cleanup` (`clubs.common`)
- **R-15-12b:** the N-54 handler, plus the `WaitlistExpired` re-check.
- **API:** every `/jobs*`, `GET /risk-review` and `/platform/jobs*` route is served, and `bin/core jobs:run` runs a process from the CLI.
- **Verification:** the final `./mvnw -q verify` exits **0** with **452 unit/contract + 704 integration tests**, 0 failures, errors or skips. All gates hold.
- **OpenAPI:** a fresh `bin/openapi-snapshot` is byte-identical to the committed file. Its only change is the 8 S15 descriptions.
- **Local stack:** the curl rehearsal passes (dry run → real run → run sheet → `/risk-review` → N-17/N-08a/N-16 → N-54 → CLI).

Dependencies E5-T01…T04 were `awaiting_verification` when I started (allowed by roadmap/README §2).

### Branch / commits
I ran no git write command (no branch, add, commit, stash, reset or push). The work is in the working tree on `main` for the publish script. Read-only git only: `git status`, `git diff --check`, `git diff --stat`, `git ls-files --others`.

### Evidence (full command outputs)
Logs are in `roadmap/evidence/E5-T05/`. The `.gitignore` exception `!roadmap/evidence/E5-T05/*.log` was added, as in earlier tasks. No token or secret appears in any log (`grep eyJ` → 0; ids truncated).

**Earlier attempts** (one line each):
- `01-verify.log` (exit 1): `EventCatalogContractTest.E0_T04_everyEventImplementationEmitsACatalogName` failed. The new `clubs.common.domain.ForeignEvent` (the envelope of the deferred N-33 `WeekValidated` consumer) had no sample. I added the sample and its assertions.
- `02-verify.log` (exit 1): the jacoco gate failed on `clubs.common.domain`, branches 75 % < 80 %. I added `ForeignEventTest`.
- The first curl rehearsal showed two things:
  - the auto-cancel N-08a carried `action = OPEN_BOOKING` (the `ClassAutoCancelled` payload has no `reason`);
  - N-16/N-17/N-54 stored each other's variables.

  Fixed: `CHANGE_CLASS`, per-code catalog variables and a new assertion in `RiskReviewJobIT`. I then re-ran the full verify, the snapshot and the rehearsal (below).

**1. `./mvnw -q clean` then `./mvnw -q verify > roadmap/evidence/E5-T05/07-verify.log 2>&1`** → exit **0**. Full log: `07-verify.log` (4 983 lines).

Totals from `target/*-reports/TEST-*.xml` (`04-test-summary.log`, which also lists every T-15 method and its result):
```
surefire (unit/contract) classes 98 tests 452 failures 0 errors 0 skipped 0
failsafe (integration) classes 66 tests 704 failures 0 errors 0 skipped 0
```
Last 40 lines of `07-verify.log`. With `-q` the tail is test-context log noise: the monitor of a closed Testcontainers context. The build result is the exit code, and jacoco check, ArchUnit, contract tests and snapshot test all passed.
```
2026-09-24T04:26:45.340+02:00  INFO 22991 --- [agilityhub-core-api] [           main] t.c.s.AnnotationConfigContextLoaderUtils : Could not detect default configuration cl
2026-09-24T04:26:45.351+02:00  INFO 22991 --- [agilityhub-core-api] [           main] .b.t.c.SpringBootTestContextBootstrapper : Found @SpringBootConfiguration com.agilit

  .   ____          _            __ _ _
 /\\ / ___'_ __ _ _(_)_ __  __ _ \ \ \ \
( ( )\___ | '_ | '_| | '_ \/ _` | \ \ \ \
 \\/  ___)| |_)| | | | | || (_| |  ) ) ) )
  '  |____| .__|_| |_|_| |_\__, | / / / /
 =========|_|==============|___/=/_/_/_/

 :: Spring Boot ::               (v3.5.16)

2026-09-24T04:26:45.364+02:00  INFO 22991 --- [agilityhub-core-api] [           main] c.a.core.migration.PlayoffMigrationIT    : Starting PlayoffMigrationIT using Java 21
2026-09-24T04:26:45.364+02:00  INFO 22991 --- [agilityhub-core-api] [           main] c.a.core.migration.PlayoffMigrationIT    : The following 1 profile is active: "test"
2026-09-24T04:26:45.509+02:00  INFO 22991 --- [agilityhub-core-api] [           main] .s.d.r.c.RepositoryConfigurationDelegate : Bootstrapping Spring Data MongoDB reposit
2026-09-24T04:26:45.543+02:00  INFO 22991 --- [agilityhub-core-api] [           main] .s.d.r.c.RepositoryConfigurationDelegate : Finished Spring Data repository scanning
2026-09-24T04:26:45.573+02:00  INFO 22991 --- [agilityhub-core-api] [           main] org.mongodb.driver.client                : MongoClient with metadata {"driver": {"na
2026-09-24T04:26:45.576+02:00  INFO 22991 --- [agilityhub-core-api] [localhost:52426] org.mongodb.driver.cluster               : Monitor thread successfully connected to
2026-09-24T04:26:45.987+02:00  INFO 22991 --- [agilityhub-core-api] [           main] c.a.core.migration.PlayoffMigrationIT    : Started PlayoffMigrationIT in 0.627 secon
2026-09-24T04:26:54.578+02:00  INFO 22991 --- [agilityhub-core-api] [localhost:52635] org.mongodb.driver.cluster               : Exception in monitor thread while connect

com.mongodb.MongoSocketOpenException: Exception opening socket
	at com.mongodb.internal.connection.SocketStream.lambda$open$0(SocketStream.java:85) ~[mongodb-driver-core-5.5.2.jar:na]
	at java.base/java.util.Optional.orElseThrow(Optional.java:403) ~[na:na]
	at com.mongodb.internal.connection.SocketStream.open(SocketStream.java:85) ~[mongodb-driver-core-5.5.2.jar:na]
	at com.mongodb.internal.connection.InternalStreamConnection.open(InternalStreamConnection.java:233) ~[mongodb-driver-core-5.5.2.jar:na]
	at com.mongodb.internal.connection.DefaultServerMonitor$ServerMonitor.lookupServerDescription(DefaultServerMonitor.java:219) ~[mongodb-driver-core-5.5.2.jar:na]
	at com.mongodb.internal.connection.DefaultServerMonitor$ServerMonitor.run(DefaultServerMonitor.java:176) ~[mongodb-driver-core-5.5.2.jar:na]
Caused by: java.net.ConnectException: Connection refused
	at java.base/sun.nio.ch.Net.pollConnect(Native Method) ~[na:na]
	at java.base/sun.nio.ch.Net.pollConnectNow(Net.java:694) ~[na:na]
	at java.base/sun.nio.ch.NioSocketImpl.timedFinishConnect(NioSocketImpl.java:542) ~[na:na]
	at java.base/sun.nio.ch.NioSocketImpl.connect(NioSocketImpl.java:592) ~[na:na]
	at java.base/java.net.SocksSocketImpl.connect(SocksSocketImpl.java:327) ~[na:na]
	at java.base/java.net.Socket.connect(Socket.java:751) ~[na:na]
	at com.mongodb.internal.connection.SocketStreamHelper.initialize(SocketStreamHelper.java:76) ~[mongodb-driver-core-5.5.2.jar:na]
	at com.mongodb.internal.connection.SocketStream.initializeSocket(SocketStream.java:104) ~[mongodb-driver-core-5.5.2.jar:na]
	at com.mongodb.internal.connection.SocketStream.open(SocketStream.java:79) ~[mongodb-driver-core-5.5.2.jar:na]
	... 3 common frames omitted
```
The verification names are all in that run (`04-test-summary.log`):
- T-15-08, 09, 11, 12, 13, 14, 15 (inside `T_15_13_…`, the form-A half), 24, 25 and 27.
- T-08-34 (`BookingJobsIT.T_15_24_T_08_34_…` + E5-T03's `WaitlistIT.T_08_34_…`) and T-08-35.
- R-15-12b (`RiskReviewJobIT.R_15_12b_…`).
- `E5ContractIT` (38 tests; the S15 routes are now in `IMPLEMENTED`, so none answers 501).
- `ArchitectureTest`, `AuditContractTest` (`JOB_TRIGGERED` covered by `JobsApiIT.T_15_08_…` and `JobFrameworkIT.T_15_08_…`), `EventCatalogContractTest`, the ca/es/en message parity and the coverage gates.

**2. `bin/openapi-snapshot`** (after the final code) → exit **0** (`08-openapi-snapshot.log`, last line `Updated docs/openapi/openapi.json`).
- `cmp docs/openapi/openapi.json target/openapi-before-snapshot.json` → exit **0**: byte-identical to the previous generation.
- Against `HEAD`: `docs/openapi/openapi.json | 16 ++++++++--------`. Only the `description` of the 8 S15 operations changes («Contract only; returns 501…» removed). No path, parameter or schema change. There is an entry in `docs/openapi/CHANGELOG.md`.

**3. `git diff --check`** → exit **0**, no output. The new untracked files were also checked for trailing whitespace (none).

**4. Local-stack curl sequence (Evidence to return).** Command: `python3 roadmap/evidence/E5-T05/curl-sequence.py > roadmap/evidence/E5-T05/06-curl-sequence.log 2>&1` → exit **0**. Full log (135 lines) in `06-curl-sequence.log`.

It runs the final jar on a disposable Mongo 7 with `club:apply seeds/club-canic.yaml` + `seed:demo --seed=42`, with the scheduler and the outbox on. The admin first switches the `risk-review` calendar off, so the tick cannot race the manual runs; the manual runs still work (R-15-09). The test clock is then moved to Mon 05-10 07:25, the first day of the validated demo week.

Summary of the log:
```
curl GET /api/v1/jobs -> 200            week-opening (WEEKLY SUNDAY 20:00) · risk-review (DAILY 07:30, enabled false) · cleanup (DAILY 06:00)
                                         (payment-timeouts: SINGLE_CLASS off; waitlist-fifo: ALL_AT_ONCE → absent at the Cànic)
curl POST /api/v1/jobs/risk-review/trigger {dryRun:true} -> 200   counters {"reviewed": 20, "WOULD_CANCEL": 5, "WOULD_NOTIFY": 11}; outbox: none; notifications: none
curl POST /api/v1/jobs/risk-review/trigger {dryRun:false} -> 200  counters {"reviewed": 20, "cancelled": 5, "cancelledSilent": 4, "notifiedMembers": 0, "atRisk": 11}
  plan ≡ effects: the same 16 items, actions without WOULD_
  outbox events: ClassAtRisk ×11, ClassAutoCancelled ×5, ClassCancelledByClub ×5, NotificationQueued ×55, NotificationSent ×16, SchedulerRun ×1
  notifications: N-08a APP SENT ×1, N-08a EMAIL SENT ×1, N-08a SMS QUEUED ×1, N-16 APP SENT ×22, N-17 APP SENT ×15, N-17 EMAIL SENT ×15
  N-08a variables: {"dog_name": "Taca 30", "class_time": "17:40", "admin_text": "No s'ha arribat al mínim de 2 gossos: la classe queda anul·lada. …", "action": "CHANGE_CLASS", …}
curl GET /api/v1/jobs/risk-review/runs/{runId} -> 200   parametersSnapshot {"jobs.riskReview.enabled": false, "classes.riskReviewTime": "07:30", "classes.minDogs": 2, …}
curl GET /api/v1/risk-review -> 200     {"dayLabel":"TODAY","startTime":"17:40","displayDescription":"Teràpia","bookedCount":1,"status":"AUTO_CANCELLED","notified":[{"memberName":"Queralt","dogName":"Taca 30"}]} …
curl GET /api/v1/risk-review -> 403 FORBIDDEN   [member]
curl POST /api/v1/bookings/{id}/cancellation -> 200   [instructor «ha avisat», Thu 18:50 class with 2 registrants]
  outbox ClassBelowMinimum {"countedDogs": 1, "minDogs": 2, "classId": "6c0b7133…"}; notifications: N-05 APP ×1, N-54 APP SENT ×3, N-54 EMAIL SENT ×3
  class after the alert: ACTIVE booked=1 lowAlertSentAt=set
$ bin/core jobs:run risk-review --club=canic --dry-run -> exit 0   (real clock of the CLI process: no class in the window, counters {})
$ bin/core jobs:run cleanup --club=canic -> exit 0                  counters {ttlPendingSeatHolds=0, …, jobRunsDeleted=0}
$ bin/core jobs:run no-such-job --club=canic -> exit 1 (JOB_UNKNOWN)
PASS risk-review curl sequence
```

**5. The `JobRun` documents** of a dry run and a real run over the same fixture, for each of the five processes, are in `05-jobrun-documents.log` (ids truncated). In each pair the entityIds are equal, the actions are equal without the `WOULD_` prefix, and the details are equal. Examples:
- **risk-review:** dry `WOULD_CANCEL s08-c1 / WOULD_CANCEL s08-c2 / WOULD_NOTIFY s08-c3`; real `CANCEL / CANCEL / NOTIFY` with counters `reviewed 3, cancelled 2, cancelledSilent 1, atRisk 1, notifiedMembers 1`.
- **cleanup:** dry `WOULD_DELETE_orphanUploads 3, …_exports 1, …_domainEvents 1, …_jobRuns 3, …_stripeEvents 0`; real `orphanUploadsDeleted 3, exportsPurged 1, domainEventsDeleted 1, jobRunsDeleted 3, stripeEventsDeleted 0`.

### Files changed
**New (main)**
- `clubs/scheduling/application/{RiskReviewJob, RiskNotifications, RiskReviewQuery, WeekOpenings}`
- `clubs/bookings/application/{WaitlistFifoJob, PaymentTimeoutsJob, BookableClassesCache}`
- `clubs/common/application/{WeekOpeningJob, WeekOpeningNotifications, CleanupJob}`
- `clubs/common/persistence/CleanupRepository`
- `clubs/common/domain/ForeignEvent`
- `clubs/messaging/application/NotificationFanout`
- `platform/application/jobs/{JobAdminService, JobRunCommand}`

**Modified (main)**
- API:
  - `clubs/common/api/{JobsController, RiskReviewContracts}` (501 stubs → served; form-A mapping)
  - `platform/api/PlatformJobsController`
  - `platform/application/jobs/JobContractAccess` (unused `run` guard removed)
- Persistence and ports:
  - `platform/persistence/jobs/JobRunRepository` (`last`, `lastExecution`, `lastSuccess`, `byIds`)
  - `clubs/scheduling/application/ports/ClassBookingsPort` (default `bookings(ids)`, `clubCancelled(classId)`)
  - `clubs/bookings/application/ClassBookingsAdapter`
  - `clubs/bookings/persistence/{BookingRepository` (`byIds`, `pendingSince`), `WaitlistEntryRepository` (`notifiedDue`)`}`
  - `clubs/scheduling/persistence/ClassSessionRepository` (`startingBetween`)
  - `clubs/scheduling/application/ClassSessionBookingAccess` (`activeBetween`)
- Scheduling:
  - `SchedulingEvents` (publishes `SchedulerEvent`)
  - `SchedulingNotifications` (student-only N-08a path for RISK_REVIEW; E4-T03's `notifications.N-08a` filter unchanged)
  - `SchedulingDashboard` (D1 names from `RiskReviewQuery`)
  - `DemoPlanningSeeder` (`{minDogs}`)
- Bookings:
  - `WaitlistConsumers` (+`alerts.WaitlistExpired`)
  - `BookingCounters` (javadoc)
  - `BookingContext` (`lastOpening`)
- Other contexts:
  - `clubs/census/application/SchedulingRecipients` (`person`, `activeWithActiveDog`)
  - `clubs/training/application/TrainingConsumers` (`TrainingCounterReset` → grid invalidation)
  - `clubs/followup/{application/AttachmentStorage, persistence/LocalAttachmentStorage, persistence/S3AttachmentStorage}` (`delete`)
  - `clubs/common/persistence/ListExportRepository` (`purgeable`, `purged`)
  - `clubs/messaging/{application/SystemEmailRenderer` (N-16, N-17, N-54 sendable), `persistence/NotificationRepository` (`existingIds`, `insertBatch`, variable allowlist + `dogs_count`, `review_time`, `review_day`, `auto_cancel`, `week_start`)`}`
- Resources and docs:
  - `messages_{ca,es,en}.properties` (`notif.N-16/N-17/N-33/N-54.{title,body}`, `scheduling.autoCancel.text` with `{minDogs}`)
  - `docs/openapi/openapi.json`, `docs/openapi/CHANGELOG.md`, `CHANGELOG.md`, `README.md` (CLI), `.gitignore`

**Tests**
- New:
  - `clubs/bookings/api/{RiskReviewJobIT, BookingJobsIT, WeekOpeningJobIT, CleanupJobIT, JobsApiIT}` (on E5-T02's `BookingFixtures`)
  - `clubs/common/domain/ForeignEventTest`
- Modified:
  - `E5ContractIT` (S15 routes implemented; `billing-reminder` history 200 / trigger 422)
  - `JobFrameworkIT` (real beans are registered; filters on `TEST_NOOP`)
  - `EventCatalogContractTest` (common `ForeignEvent` sample)
  - `CalendarIT` (port double)
  - `DemoPlanningSeedIT` (`{minDogs}`)

### Rules and tests implemented (R-xx-nn / T-xx-nn)
**R-15-09 — `/jobs*` routes (T-15-08, T-15-09)**
- `GET /jobs` lists, in catalog order, only processes that have a bean and whose module (and `activeFor`) is on. Each row carries `schedule`, `nextScheduledForLocal` (null for continuous processes) and `lastRun`.
- `GET /jobs/{name}/runs` is a universal list over the tenant's `job_runs` of that process (filters `status`, `trigger`, `scheduledFor`, `dryRun`; sort `scheduledFor`, `startedAt`; default `startedAt,desc`).
- `GET /jobs/{name}/runs/{runId}` returns the run sheet; another process's or another club's run → 404.
- `POST /jobs/{name}/trigger`:
  - runs E5-T01's audited `JobTriggerService`, even with the switch off;
  - lock held → 409 `JOB_ALREADY_RUNNING`;
  - module off → 404; unknown id → 422 `JOB_UNKNOWN`;
  - impersonation, INSTRUCTOR or MEMBER → 403.
- `PUT /jobs/{name}/switch` writes the parameter through S02 `ParameterSettingsService.update` (`ParameterChanged`, audited `PARAMETER_CHANGED`, cache invalidated).
- Platform routes:
  - `POST /platform/clubs/{clubId}/jobs/{name}/trigger`: the same code inside the club scope.
  - `GET /platform/jobs/overview`: health OK / WARN / ALERT (T-15-10, overview half).

**R-15-11 P1 `week-opening` (T-15-11, T-15-12)**
- `opensAt` = the last `bookings.weekOpensAt` occurrence ≤ `scheduledFor`; `openedWeekKey` = `opensAt` + 7 days; the target ISO week is the one containing `openedWeekKey` + 1 day.
- One transaction:
  - config cache invalidation and warm-up of `BookableClassesCache`;
  - `Week.openedAt` (get-or-create);
  - `WeekOpened{openedWeekKey, isoWeekStart, currentWeekKey, opensAt, notified}`, plus `TrainingCounterReset{weekStart}` with FREE_TRAINING.
- N-33 goes out right away when the parameter is on and the week has ACTIVE classes (then `openingNotifiedAt`). Otherwise the `WeekValidated` consumer (`notifications.N-33.deferred`) sends it later.
- A second execution plans nothing (`alreadyOpened`).
- Dry run: `WOULD_OPEN {weekKey, activeClasses, recipients}`.
- 26 h late → `MISSED_WINDOW` + `JobFailed`; 3 h late → `CATCH_UP`.

**R-15-12 P2 `risk-review` (T-15-13, T-15-14, T-15-15)**
- Every rule of the spec row is implemented:
  - the review recounts ACTIVE + PAYMENT_PENDING bookings through the port inside the item transaction; `counters.booked` out of sync is ignored;
  - exempt classes are counted and never touched; DRAFT classes are never in scope;
  - a class that has already started is counted in `skippedStarted`;
  - today's classes go through S06 `cancel(RISK_REVIEW)` + `ClassAutoCancelled`;
  - later classes, or today's with `riskAutoCancelSameDay = false`, get `ClassAtRisk` + the `risk` marks.
- Counters: `reviewed`, `cancelled`, `cancelledSilent`, `atRisk`, `notifiedMembers`, `exempt`, `skippedStarted`.
- Dry run: `WOULD_CANCEL {classId, dogsCount, affected}` / `WOULD_NOTIFY {classId, newBookingIds, notifyAdmins}`.
- Notifications:
  - N-16 to the new registrants (APP + EMAIL) and, the first time only, the admins (APP), with `review_time`, `review_day` and `auto_cancel`;
  - N-17 to the admins + class instructors (APP + EMAIL), also with 0 registrants;
  - N-08a to the affected registrants (APP + EMAIL + SMS intent).
- `GET /risk-review` serves form A (`AUTO_CANCELLED`, `AT_RISK`, `WILL_CANCEL`, `WILL_REVIEW`; `dayLabel`, `notified` names). The same names now feed S14's D1 card.
- Catch-up: at 23:58 → `CATCH_UP`; at 00:01 → `MISSED_WINDOW` + N-42 (T-15-04 for P2).

**R-15-12b**
- `ClassBelowMinimum` → N-54 to the instructors + admins only; the class is untouched.
- A second in-time cancellation sends nothing while `lowAlertSentAt` stands.
- Recovering to the minimum clears the mark, so a new drop alerts again.
- `WaitlistExpired` re-checks the minimum (`alerts.WaitlistExpired`).

**R-15-16 P6 (T-15-24, T-08-34)**
- NOTIFIED entries with `confirmBy ≤ now` → EXPIRED + `WaitlistExpired{entryId, classId}`, one transaction per entry with the class's seat lock and a counter recount.
- E5-T03's `offerNext` notifies the next entry; a duplicate event offers nothing more.
- In ALL_AT_ONCE: `SKIPPED{MODULE_OFF}` at most once per hour, 404 on demand.

**R-15-17 P7 (T-15-25, T-08-35)**
- `bookedAt + bookings.paymentPendingMinutes ≤ now` → `cancelBySystem(PAYMENT_TIMEOUT)`: CANCELLED, `SeatReleased`, N-40 (E5-T02's handler).
- Idempotent by state: two runs cancel once.
- SINGLE_CLASS off → `SKIPPED{MODULE_OFF}` / 404.

**R-15-19 P9 (T-15-27)**
- Reports only: `ttlPendingSeatHolds`, `ttlPendingMagicLinkTokens`, `ttlPendingIdempotencyRecords`, `ttlPendingJobLocks`.
- Deletes:
  - `orphanUploadsDeleted` (48 h);
  - `exportsPurged` (7 days);
  - `domainEventsDeleted` (processed, 90 days; unprocessed ones are never touched);
  - `stripeEventsDeleted` (0: the collection is absent before E8);
  - `jobRunsDeleted` (90 days, keeping the last 5 per process).
- No business event.

**Framework (R-15-03/04/05/06/07/08/10)**
- The five processes use E5-T01's framework unchanged.
- Plan ≡ effects is tested for all five (T-15-06 extended: `RiskReviewJobIT.T_15_06_…`, `WeekOpeningJobIT.T_15_04_T_15_06_…`, the dry/real pairs in `BookingJobsIT` and `CleanupJobIT`).
- Every process reads its values through `JobContext.parameter`, so they appear in `parametersSnapshot`.

**Step 10 (CLI):** `bin/core jobs:run <route-id> [--club=] [--dry-run]`, documented in `README.md`.

### Assumptions
1. **N-08a ownership.** E4-T03's `notifications.N-08a` handler keeps ignoring `RISK_REVIEW`: it owns CLUB_MANUAL, DELETED and ACTIVITY, and its test T-06-25 asserts exactly that. The new `notifications.N-17` handler of `ClassAutoCancelled` owns both notices of an auto-cancellation:
   - N-17 to the staff;
   - N-08a to the affected registrants (+ live waiting-list entries, as E4-T03 does), through `SchedulingNotifications.autoCancelled`, with no staff copy.

   `admin_text` is `scheduling.autoCancel.text` in each recipient's language, rendered with the **current** `classes.minDogs` at delivery time. The event payload carries the club-default text.
2. **Variables follow the catalog rows.**
   - N-16: `dog_name`, `class_date`, `class_time`, `review_time`, `review_day`, plus `auto_cancel` (S15 §13 «variables noves»). The admins' copy has an empty `dog_name`.
   - N-17: `class_date`, `class_time`, `dogs_count`. S15 §8 also lists `class_description` and `ring_name`; see proposal 1.
   - N-54: `class_date`, `class_time`, `class_description`, `ring_name`, `dogs_count`.
   - N-33: `week_start` («12 d’octubre», ICU skeleton `dMMMM` in the recipient's language).
   - Every notice also carries `club_name`, `entityId` and `action` (CHANGE_CLASS / OPEN_BOOKING), like E4-T03.
3. **`ClassBelowMinimum` payload** keeps the catalog's `classId` (E5-T01/T02 decision). The N-54 handler reads `countedDogs`.
4. **P1.**
   - A manual run opens the last opening ≤ now.
   - The second-execution guard is `Week.openedAt ≥ opensAt`.
   - The audience is ACTIVE, non-erased members with at least one ACTIVE dog **and an app account**: N-33 is APP + PUSH only.
   - PUSH rows are intents (QUEUED, or SKIPPED_MODULE_OFF without PUSH): there is no push sender before E7.
   - The base cache owner is S08 `BookableClassesCache` (`clubs.bookings.application`):
     - key `{clubId}:{W0 key}`; the W0 key is the local date of the current opening, as in `BookingWeeks`;
     - it holds the ACTIVE classes of W0…W2 as `ClassSessionBookingAccess.Session`, TTL 30 s;
     - E5-T06's `BookableClassesQuery` should read `get()` and add the per-dog state.
   - The grid cache is invalidated by making S09's consumer treat `TrainingCounterReset` as a whole-club invalidation. It was «stateless» in E5-T04; S15 R-15-11 says «S09 només invalida cache».
5. **Fan-out.** `NotificationFanout` sits in messaging.
   - Feed rows are written with one `insertMany` per 500 inside one transaction per batch, skipping ids that already exist.
   - PUSH intents go in batches of 100 on its own 2-thread daemon pool, which is joined before returning.
   - Every row still emits `NotificationQueued`, as `appOnce` does.
6. **P2.**
   - Items exist only for classes that need an action (CANCEL/NOTIFY).
   - `reviewed`, `exempt`, `skippedStarted` and «at risk, nobody new to warn» are counted in `plan`, so a dry run shows them too.
   - If a class changed between plan and apply, the item traces `NOT_IN_SCOPE` / `NOT_AT_RISK` / `ALREADY_NOTIFIED` / `SKIPPED_STARTED` and writes nothing.
   - The S06 cancellation runs with a null actor (bookings `cancelledBy.role = SYSTEM`, reason `AUTO_CANCELLED`). The events carry the admin as actor on a manual run (`SchedulingEvents` actor rules).
7. **`GET /risk-review` statuses.**
   - `AT_RISK` = warned registrants (`notifiedBookingIds` non-empty or admins warned) **and** at least one booking.
   - Otherwise `WILL_CANCEL`, or `WILL_REVIEW` when `riskAutoCancelSameDay = false`. A class with 0 registrants whose admins were warned is still `WILL_CANCEL`, as S15 §6 example c4 shows.
   - `dayLabel` is relative to the requested `date`.
   - `bookedCount` of a cancelled class = `cancellation.affectedBookings`.
   - `notified` names come from the new port reads `bookings(ids)` and `clubCancelled(classId)`. They are default methods that return empty lists for test doubles.
8. **P9 mapping to this code base.**
   - **Orphan uploads.** They are enumerated from the `attachment_uploads` grants (SIGNUP_DOCUMENT, `signup/{clubId}/…`), not from an S3 listing. The grant's `createdAt` stands in for `LastModified` (uploads happen within the 15-minute grant). The object (new `AttachmentStorage.delete`) and the grant are deleted.
   - **Exports.** «Finished» = READY/FAILED, and the age is `createdAt` (`ExportJob` has no `finishedAt`). «Marked PURGED» = the existing E2-T08 tombstone (`EXPIRED`, files unset, `purgeAt = now`, so the TTL removes it), because `ExportStatus` has no `PURGED` (proposal 3).
   - **Outbox.** «Processed» = `status = PUBLISHED` and `publishedAt < cutoff`; `processedAt` is a per-handler map.
   - **`stripe_events`.** Its existence is checked in `plan`, because `listCollections` is not allowed inside a transaction.
   - **`job_runs`.** «The last 5 runs» counts any row of the process (SKIPPED and dry runs included).
   - **Counters.** Keys carry no dots (E5-T01 rule). Bulk targets are one `DELETE` item each; a dry run records `WOULD_DELETE_<target>` totals.
9. **Overview health.**
   - The period is 1 min / 1 day / 7 days / 31 days by cadence; ALERT after 2 periods + the 2-minute on-time tolerance without a success.
   - Neutral skips are ignored: DISABLED, MODULE_OFF, CLUB_INACTIVE, NOT_DUE.
   - A process switched off, or one that never ran, is OK.
   - `status=` keeps only the cells of that health.
10. **Catalog processes without a bean** (P3, P4, P5, P8, P10). Their `GET /jobs/{name}/runs` answers the (empty) history, and `PUT …/switch` writes their parameter (T-15-09 uses `reminders`). `POST …/trigger` answers 422 `JOB_UNKNOWN`, as E5-T01's runner already did.
11. **CLI.** `jobs:run` is a manual run with `actorAccountId = null`, on the process's own clock (the test clock only moves inside the API process).
12. **Observation on first deployment (R-15-05, no change).** On a club's first tick after 07:30 local, yesterday's `risk-review` occurrence is `MISSED_WINDOW` + one N-42 (seen on the local stack). Later restarts are unaffected: the occurrence is then already taken.

### Questions / catalog proposals
I introduced no new parameter, event, notification, error code or audit action.
1. **N-17 variables.** S15 §8 lists `class_description` and `ring_name` for N-17; the catalog row has only `class_date`, `class_time` and `dogs_count`. I implemented the catalog row. Proposal: add the two variables to the N-17 row.
2. **`ClassBelowMinimum` `classId` vs `classSessionId`** (repeated from E5-T01/T02). I kept the catalog's `classId`.
3. **Export «PURGED».** S15 R-15-19 says the record is «marcat PURGED», but S14's `ExportStatus` is closed (`QUEUED · RUNNING · READY · FAILED · EXPIRED`). I reused the `EXPIRED` tombstone with an immediate `purgeAt`. Proposal: amend R-15-19 to «EXPIRED + purge», or add `PURGED` to S14.
4. **Orphan-upload detection by grant.** R-15-19 describes an S3 `LastModified` scan. This code base records every signup key in `attachment_uploads`, so the grant is the source (assumption 8). Please confirm.
5. **N-16 admin copy.** The admins' N-16 uses the member template with an empty `dog_name`. The E7 templates may want a staff wording (a `select` on the audience).

## Organizer verification
_(pending)_
