---
id: E0-T10
title: Club-as-code: CLI runner, club:apply with dry-run diff, JSON schema, seeds canic / minim / template
stage: E0
repo: agilityhub-core-api
thread: A
order: 100
status: ready
depends_on: [E0-T05, E0-T06, E0-T09]
branch: 
pr: 
updated: 2026-09-05
---

# E0-T10 · Club-as-code: CLI runner, club:apply with dry-run diff, JSON schema, seeds canic / minim / template

## Objective
Work package WP-17-A (E0 part): a club is fully described in a YAML file and applied idempotently; three seeds exist. Catalog sections (levels, rings, plans, FAQ, message templates) are defined in the schema now and applied by later stages without changing the format.

## Spec references
`docs/specs/S17-consola-de-clubs.md` §3–6, R-17-01, R-17-02, tests **T-17-01** (E0 part), **T-17-02** (host uniqueness) · `docs/specs/S02-*.md` §7 (seeds) and **T-02-12** · `docs/MODEL_DADES_PLATAFORMA.md` §2 · `docs/specs/00-transversal/CATALEG_PARAMETRES.md` (Cànic values) · `docs/specs/00-transversal/CATALEG_MODULS.md` (presets).

## Context to load
S17 §3–6, §9 · S02 §7 · MODEL §2 · the two catalogs · this file.

## Steps
0. **Catalog defaults specified by the organizer (2026-09-06, answers E0-T05)** — set these product defaults in `src/main/resources/parameters/catalog.yaml` **and** in the repo copy `docs/specs/00-transversal/CATALEG_PARAMETRES.md` (same commit, so `T-02-03` stays green; the organizer mirrors the document to the source folder). `localizedText` defaults are given in `ca`; add `es` and `en` translations of the same meaning. Placeholders in braces are interpolated by the front from other parameters.
   - `signup.text.paymentDay`: ca «El rebut es carrega normalment l'últim dia de cada mes. Tens fins al dia {deadlineDay} per modificar l'emissió de rebuts del mes següent, des de la teva fitxa o posant-te en contacte amb el club.» (`{deadlineDay}` = `inactivity.requestDeadlineDay`).
   - `signup.text.monthlyPaymentIntro`: ca «La quota mensual per abonats es cobrarà normalment l'últim dia de cada mes. En el cas de packs no es genera cap càrrec mensual.»
   - `signup.text.cashConditions`: ca «Pagament en efectiu: la quota es paga mensualment al club. Posa't en contacte amb el club per coordinar-ho.»
   - `signup.text.freeTrainingConditions`: ca «Les classes són sempre amb instructor i se'n poden fer fins a dues per setmana. A més hi ha la possibilitat d'entrenament lliure fora d'hores de classe a partir de nivell D amb llicència esportiva.»
   - `signup.text.therapyIntro`: ca «Es poden fer també classes de teràpia individual, combinades amb les classes en grup o com a pas previ. Si la teràpia es combina amb classes en grup, selecciona l'opció d'Abonat o Pack; si d'entrada no faràs classes en grup, selecciona l'opció Teràpia.»
   - `signup.text.familyGroupIntro`: ca «Si a casa ja hi ha algú abonat, podeu formar un grup familiar amb un únic responsable del pagament: la quota mensual es redueix a partir del segon gos. Així, per dos gossos la quota mensual seria de {twoDogsMonthlyFee}. L'entrada sí que és per cada gos.»
   - `signup.text.closed`: ca «Les inscripcions estan tancades temporalment. Torna-ho a provar més endavant o posa't en contacte amb el club.»
   - `signup.rateLimit` (json, per IP and club, from S04 R-04-20): `{"identityChecksPerHour": 10, "familyGroupLookupsPerHour": 20, "uploadUrlsPerHour": 30, "signupPerHour": 5, "signupPerDay": 20, "checkoutSessionsAnonymousPerHour": 10, "townsPerHour": 60}`.
   - `leave.reasons` (json `[{key, label: localizedText, audience}]`): `LEARNED_ENOUGH` «He après el que volia» · `NO_TIME` «No trobo temps» · `NOT_EXPECTED` «No és el que esperava» · `EXTERNAL` «Condicionants aliens» · `OTHER` «Altres» (audience `MEMBER`) · `CLUB_DECISION` «Decisió del club» (audience `ADMIN`) · `PACK_EXPIRED` «Pack caducat» (audience `SYSTEM`).
   - `census.dogDocumentTypes` (json `[{key, label: localizedText, required}]`): `VACCINATION_CARD` «Cartilla de vacunes» required · `INSURANCE` «Assegurança» optional · `OTHER` «Altres» optional.
   - `messaging.email.fromAddress`: `no-reply@agilitydoghub.com` (a club may override it only with a verified sending domain — enforced in E1-T03).
   - `messaging.email.replyTo`: `""` (empty = the club's contact email at send time).
   - `messaging.sms.senderId`: `AgilityHub` (alphanumeric, ≤ 11 chars).
   - `learn.baseUrl`: `https://learn.agilitydoghub.com`.
1. `seeds/club-definition.schema.json` (JSON Schema 2020-12) with sections `club` (slug, name, locales, defaultLocale, timeZone, currency, countryProfile, status), `domains[] {host, primary}`, `theme`, `modules[]` or `preset`, `paymentProviders[]`, `parameters {key: value}`, `admins[] {email, name, locale}`, `catalogs {levels[], rings[], instructors[], plans[], prices[], faq[]}` (defined now; applied from E2), `messageTemplates[]` (applied from E7). Validate with networknt `json-schema-validator`.
2. CLI runner: when the app is started with `--core.command=<name> [args]`, run the command and exit with code 0/1 **without** starting the web server (`spring.main.web-application-type=none` when the property is present). Script `bin/core` = `java -jar target/core-api-*.jar --core.command="$@"` (builds the jar if missing). Register `identity:seed-test-accounts` from E0-T09.
3. `club:apply <file> [--dry-run]`: validates the file, computes a readable diff per section (`+ added`, `~ changed`, `= unchanged`), applies inside a transaction, writes an `AuditEntry CLUB_APPLIED` with the summary, publishes `ClubUpdated` / `ParameterChanged` (catalog names) **only if something changed**; idempotent (second run = 0 changes, no events); a second club with an already used host → error `HOST_ALREADY_USED` (T-17-02 part); a module without its dependencies → `MODULE_DEPENDENCY`. `club:export <slug>` prints the YAML of an existing club.
4. Seeds (fictional people only, `@example.test` emails): `seeds/club-canic.yaml` (Europe/Madrid, `ca` + `es`, EUR, ES, hosts `app.agilitycanic.cat` + `admin.agilitycanic.cat`, theme with the mockup colors — take them from `docs/pantalles/**/*.html` `:root` variables —, preset `CANIC`, `paymentProviders [SEPA_XML, MANUAL]`, every parameter whose Cànic value differs from the product default), `seeds/club-minim.yaml` («Club Mínim», `en`, GENERIC, AgilityHub theme, preset `MINIM`, `STRIPE` + `MANUAL`, hosts `minim.example.test`), `seeds/club-template-default.yaml` (template for cloning; no domains).
5. Expected fixtures for T-02-12: `src/test/resources/fixtures/branding-canic.json`, `parameters-canic.json`.

## Verification (run all; paste full output in the report)
- `bin/core club:apply seeds/club-canic.yaml --dry-run` → diff with every section `+`; `bin/core club:apply seeds/club-canic.yaml` → applied; run again → «0 changes»; `mongosh --eval 'db.domain_events.countDocuments()'` unchanged between the 2nd and 3rd run (events are named `ClubUpdated` / `ParameterChanged`).
- `bin/core club:apply seeds/club-minim.yaml`; `curl -H 'X-Club-Host: app.agilitycanic.cat' localhost:8080/api/v1/branding | jq .theme.colors.primary` and the same for `minim.example.test` → two different themes.
- A copy of the seed with `PACKS` but no `BILLING` → exit 1 with `MODULE_DEPENDENCY`; a copy with the Cànic host under another slug → exit 1 with `HOST_ALREADY_USED`.
- `./mvnw -q verify` → green with T-02-12, T-17-01 (E0 scope), T-17-02 and a schema test over the three seeds.

## Definition of Done
Schema + CLI + three seeds · idempotency proven · no personal data in seeds.

## Evidence to return
Outputs of all commands; the three YAML files.

---

## Executor report
_(not started)_

### Branch / commits
### Evidence (full command outputs)
### Files changed
### Rules and tests implemented (R-xx-nn / T-xx-nn)
### Assumptions
### Questions / catalog proposals

## Organizer verification
_(pending)_
