---
id: E5-T10
title: Follow-ups of the E5 round-2 reviews (jobs, PAY_TO_BOOK, risk review, dashboard) + ruling E30 and the platform purge
stage: E5
repo: agilityhub-core-api
thread: A
order: 100
status: ready
depends_on: [E5-T01, E5-T02, E5-T05, E5-T09]
branch:
pr:
updated: 2026-09-24
---

# E5-T10 · Follow-ups of the E5 round-2 reviews (jobs, PAY_TO_BOOK, risk review) + ruling E30 and the platform purge

## Objective
Close the minor findings that the organizer accepted from the round-2 reviews of E5-T01, E5-T02 and E5-T05: `roadmap/reviews/E5-T01-20260924-0751-claude.md`, `E5-T02-20260924-0819-claude.md` and `E5-T05-20260924-0840-claude.md`. Also implement two organizer rulings of 2026-09-24: E30 in `docs/DECISIONS_PENDENTS.md`, and the platform purge in `roadmap/MESSAGES.md`. Read each finding in its review first. The review gives the file, the scenario and a suggested fix.

## Spec references
- S15: R-15-04, R-15-06, R-15-10, R-15-12b, R-15-17, R-15-19, R-15-22, T-15-07, T-15-31.
- S08: R-08-08, R-08-18, §6 (the `Booking` resource carries `checkoutUrl`), T-08-24.
- S04/S12: a provider must be enabled for the club (`PAYMENT_PROVIDER_NOT_ENABLED`).
- S14 R-14-06, amended 24-09: D1 = form A of `/risk-review`.
- `CATALEG_NOTIFICACIONS.md`: N-16 (`audience`, added 24-09), N-17, N-33, N-40, N-54.
- `docs/DECISIONS_PENDENTS.md`: E30.

## Context to load
- This file.
- The three reviews above.
- The files each finding names.
- `roadmap/tasks/E5-T01.md`, `E5-T02.md` and `E5-T05.md`: the round-2 reports and the Organizer verification.

## Steps
1. **E5-T01 review #1 (R-15-22).** `ArchitectureRules.TIME_FROM_CLOCK` must also forbid, outside the clock configuration:
   - the `now(ZoneId)` overloads of `LocalDate`, `LocalDateTime`, `LocalTime`, `ZonedDateTime` and `OffsetDateTime`;
   - `Calendar.getInstance()`.

   Add a `ClockRuleTest` case that proves a `LocalDate.now(ZoneId)` violation is reported.
2. **E5-T01 #2: a reaped holder stops.** When `locks.renew(...)` returns `false`, the runner applies no further item of its plan. It ends as a reaped run: its row is already `FAILED`, and it writes nothing more. Test with a two-item plan that loses its lease after the first item. The slow holder must never apply the second item.
3. **E5-T01 #3 and #4.**
   - `reap()` records `jobs.run.duration{status=FAILED}` when it closes a row.
   - When a slow holder finds its row already reaped, its WARN logs its local counters.
4. **E5-T02 review #1: the provider-failure path.** In `BookingConfirmationService`, when the provider call fails:
   - cancel only if the booking is still `PAYMENT_PENDING`, re-read inside the locked transaction;
   - rethrow the original failure (any later exception is added with `addSuppressed`);
   - release the idempotency key; a 422 is never stored for it.

   Test the race: the `UpfrontPaymentFailed` consumer cancels the booking first.
5. **E5-T02 #2.** `SingleClassCharges.prepare` checks that the club has the provider enabled, as the signup checkout does (`422 PAYMENT_PROVIDER_NOT_ENABLED`). Test.
6. **E5-T02 #3: `checkoutUrl` on the booking.** Per S08 §6, the `Booking` resource carries `checkoutUrl`.
   - Persist it on the booking while it is `PAYMENT_PENDING`, so that `GET /bookings/{id}` returns it.
   - Clear it when the booking leaves `PAYMENT_PENDING`.

   With this, a failed store of the 201 no longer loses the link. Test.
7. **E5-T02 #4: P7 closes the provider side.** When P7 (`PaymentTimeoutsJob`) cancels a `PAYMENT_PENDING` booking, it also expires that booking's checkout session and line (`CheckoutService.expire` / `SingleClassChargePort.abandon`). A late provider success can then no longer capture money for a cancelled booking. Test.
8. **Ruling E30 (E5-T02 #5).** A booking cancelled because the provider call failed synchronously sends **no N-40**: the member already sees the error in the app.
   - Propose the mechanism in the report. It must be payload-only: no new reason value, event or notification.
   - Test that the P7 timeout path still sends N-40.
9. **E5-T05 review #2.** `fixtures/contracts/e5-notifications.json` also covers N-16 (both audiences), N-17, N-33 and N-54, with their catalog variables (`E5ResponseContractTest`).
10. **E5-T05 #3.**
    - Rename the boolean of `recount`, for example to `alertIfBelowMinimum`.
    - State in the report how you read R-15-12b for P6: an expiry that leaves the class below the minimum alerts, even without a drop.
11. **E5-T05 #4.** D1's risk rows come from one batched read: one `$in` over the class ids of the window, with batched census names. Nothing is read one class at a time. Test the query count, as `T_08_47` does.
12. **E5-T05 #5.** `bin/e5-smoke` asserts, on the running stack, that the `riskReview` of `GET /dashboard` equals `GET /risk-review` with the statuses mapped.
13. **E5-T05 #6, #8 and #9.**
    - The report names the parity IT as the place where the S14 T-14-05 exclusions are tested.
    - The unused imports in `WaitlistConsumers` are removed.
    - The ring-name placeholder and the locale move from `SchedulingDashboard` into the projection.
14. **Platform purge (organizer ruling on the E5-T05 round-2 question 2).** P9 also purges the `domain_events` and `job_runs` that have no `clubId` (platform and identity rows).
    - The retention rules are the same as for the club pass (R-15-19): 90 days, and the last five real executions per job are kept.
    - The platform pass runs once per P9 cycle, not once per club.

    Test with two clubs: the platform rows are purged once, and the platform pass touches no club row.

## Fixed conventions
- The AGENTS.md rules apply.
- No new parameters, events, notifications, error codes or reason values.
- If a response shape changes (step 6), regenerate the snapshot and say whether the web must adopt it.

## Verification (run all; evidence = command + exit code + last 40 lines; long outputs → `roadmap/evidence/E5-T10/*.log`)
- `./mvnw -q clean verify` → exit 0. Also commit a test summary read from the Surefire/Failsafe reports.
- `bin/openapi-snapshot` and `git diff --stat docs/openapi/`.
- `bin/e5-smoke` → exit 0 (step 12).

## Definition of Done
- Every step is done, with a test named after the spec id where one applies.
- The report lists each step with its file and its test.
- `clean verify` is green.

## Evidence to return
The report sections below, and the step → test table.

---

## Executor report
_(pending)_

## Organizer verification
_(pending)_
