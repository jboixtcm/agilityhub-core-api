---
id: E5-T26
title: Signed local file URLs, the rest (api) — a member's add-dog upload while signup is closed, downloads with the stored content type, the CORS preflight of the signed routes, the grant's club as the tenant
stage: E5
repo: agilityhub-core-api
thread: A
order: 260
status: awaiting_verification
depends_on: [E5-T24]
branch:
pr:
updated: 2026-09-26
---

# E5-T26 · Signed local file URLs, the rest

## Objective
Api E5-T24 made the local profile's signed file URLs authorise themselves (`CONVENCIONS_API.md` §5, A31). The independent review of E5-T24 (`roadmap/reviews/E5-T24-20260926-1636-claude.md`) and its report (question 2) found what is still missing:
- A MEMBER adding a dog while signup is closed cannot upload. The web sends no bearer to the signed URL, as §5 says, and `PUT /api/v1/signup/uploads` then answers `422 SIGNUP_CLOSED` (R-04-27).
- The review could not check that an `<img>` shows a local download, nor the CORS preflight of the signed routes.
- The signed routes run without a tenant, although a Javadoc says they have one.

The organizer confirmed and decided them all on 26-09. Until the release, the local stack is what runs (A31), so these are real flows.

The nits of this task's own review go to `docs/INCIDENCIES_OBERTES.md`. Its minors go there too, unless they block the web.

**Each fix comes with a test that fails before the fix.**

## Spec references
- `CONVENCIONS_API.md` §5 (a signed URL authorises itself, on the local profile too; export links are the exception; amended 26-09).
- S04 R-04-27 (a MEMBER adding a dog keeps the signup's upload routes), R-04-08.
- A31 (the local stack until the release).

## Context to load
- The E5-T24 review (findings #1 and #3, «Not checked») and report (question 2, assumptions 2–4).
- `AttachmentService` (near `:43`–`:47` and `:106`–`:113`: `putSignupLocal`, `requireSignupOpen`; `putLocal`, `openLocal`), `UploadGrantRepository` (near `:12`–`:18`), `SignedFileRequests`, `SignupFilesController`, `AttachmentsController`, `SecurityConfiguration` and the CORS configuration, `TenantFilter`.

## Steps
1. **A member's add-dog upload while signup is closed (review #1, question 2, R-04-27).**
   - When an authenticated MEMBER calls `POST /signup/upload-urls`, the grant stores the member's account.
   - `PUT /api/v1/signup/uploads` with such a grant skips `requireSignupOpen()`: its signature already authorises it, with no bearer.
   - An anonymous grant still answers `422 SIGNUP_CLOSED` while signup is closed.
   - IT with `signup.enabled = false`:
     - a MEMBER's upload URL, then a `PUT` with `Upload.headers` only, answers 2xx;
     - the member's `POST /me/dogs/signup` then claims the file;
     - an anonymous grant's `PUT` answers 422.
2. **Local downloads answer like S3 (the review's «Not checked»).**
   - `GET /api/v1/attachments/files/{id}` and `GET /api/v1/signup/files` answer the file's stored MIME type, not `application/octet-stream`.
   - Images come with `Content-Disposition: inline`, and other files with `attachment`, with the stored name.
   - An `<img>` must show a dog's photo or an activity's image, even from another origin (the web's app runs on its own origin).
   - IT: the headers of an image and of a PDF.
3. **The CORS preflight of the signed routes (the review's «Not checked»).**
   - A cross-origin `PUT` from the club's web origin passes the preflight on `/api/v1/attachments/uploads/{id}` and `/api/v1/signup/uploads`, as it does on the api's other routes. These routes have no tenant, so check that the allowed origins do not depend on one.
   - IT: an `OPTIONS` with `Origin` and `Access-Control-Request-Method: PUT` from an allowed origin answers the CORS headers. An unknown origin gets none.
4. **The grant's club as the tenant (review #3).**
   - On the signed routes, the grant's `clubId` opens the `TenantContext` for the handler, as the `UploadGrantRepository` Javadoc already says.
   - Code that calls `TenantContext.require()` there then works.
   - Unit test or IT.

## Fixed conventions
- No contract change. The signed routes are hidden from the snapshot.
- Export links keep requiring the caller's session (`CONVENCIONS_API.md` §5, amended 26-09: S14 R-14-12).
- Heavy runs go through the host lock.
- Fictional data only.

## Verification (run all; evidence = command + exit code + last 40 lines; long outputs → `roadmap/evidence/E5-T26/*.log`)
- `./mvnw -q clean verify`.
- `bin/e3-smoke` twice, on a fresh local stack. Add the member's add-dog upload with signup closed if the smoke already closes signup.

## Definition of Done
- Steps 1–4 are done, and each has a test that fails before the fix.

## Evidence to return
- The command outputs.
- A step → test table.

---

## Executor report

### Summary

Steps 1–4 are done, on `main` at `c70adf9`. E5-T24, the only dependency, is verified.

- **Contract:** no change. The signed routes are hidden. `OpenApiSnapshotTest` passes on the unchanged `docs/openapi/openapi.json`, and `docs/openapi/` is untouched.
- **Verification** (the final runs are on the final tree):
  - `./mvnw -q clean verify` through the host lock exits 0: 588 unit and 1038 integration tests, 0 failures. The summary is committed (log `09`).
  - `bin/e3-smoke` exits 0 twice through the host lock, each time on a fresh disposable stack, with `0 changes` on each seed's second apply.
  - The smoke already closed signup at its end, so it now also runs the member's add-dog upload with signup closed:
    - the CORS preflight;
    - the PUT with the upload's headers only, without a bearer or the club's host;
    - an anonymous upload URL taken before the closure, which answers `422`;
    - the claim;
    - the image download.
- **Before-fix** (trees under `target/`, built by a script; the working tree was never reverted). Every test this task adds fails on a tree without its fix, each at its intended assertion:
  - tree A (HEAD's main code): the 4 new ITs;
  - tree B (only step 4 undone): step 4's unit test and 2 ITs;
  - tree C (only step 1's grant change undone): step 1's unit test and IT.
- **Step 3** needed no code change for the preflight itself. On tree A the preflights of both routes already pass, and so do the admin route's cross-origin PUT and the unknown-origin checks. The test fails there on the signup route's cross-origin PUT: without the club's host it answered `404 UNKNOWN_HOST`, fixed by step 4. So the preflight part of the test is a pin, not a before/after proof.
- **Beyond the list:**
  - `PUT /api/v1/signup/uploads` is now a signed route like the other three: no bearer is read, and the grant's club is the tenant whatever the host (assumption 1).
  - `PublicActivitiesController` follows `openLocal`'s new return type. Its behaviour is unchanged: it already answered the activity file's type, inline.

### What was done, per step

1. **A member's add-dog upload while signup is closed (R-04-27, R-04-08).**
   - A MEMBER's `POST /signup/upload-urls` stores the member's account on the grant (`AttachmentService.signupUpload`). An anonymous one still stores none.
   - `PUT /api/v1/signup/uploads` checks the closure only for a grant without an account (`requireSignupOpen(boolean member)`), so the member's PUT passes with the upload's headers only.
   - The route is now in `SignedFileRequests`: the bearer is never read, and the host plays no part. The separate `permitAll` and the controller's `@PreAuthorize` are gone, because the signature authorises the PUT.
   - An anonymous grant answers `422 SIGNUP_CLOSED`, decided on the grant's club.
2. **Local downloads answer like S3.**
   - `openLocal` returns a `Download`: the grant's file name, and the stored MIME type and length (the `.mime` file written at the PUT, as S3 answers its object's content type).
   - `AttachmentsController.download` (shared with `SignupFilesController`) sends that `Content-Type` and `Content-Length`, and `Cache-Control: no-store`.
   - `Content-Disposition` is `inline` for `image/*` and `attachment` otherwise, with the stored name (Spring's `ContentDisposition` with UTF-8).
   - Spring Security still adds `nosniff` and the API's CSP. It adds no `Cross-Origin-Resource-Policy`, so an `<img>` of another origin shows the file. An origin allowed by the CORS configuration gets `Access-Control-Allow-Origin` (for `<img crossorigin>` or `fetch`).
3. **CORS preflight of the signed routes.**
   - The allowed origins come from `ClubCorsConfigurationSource`, which checks the `Origin` against every club's registered hosts (`HostTenantResolver.isCorsHost`), never against the request's tenant. Spring Security's `CorsFilter` answers the preflight before any tenant or authentication filter.
   - The IT covers both upload routes:
     - the preflight from the club's web origin;
     - the cross-origin PUT with its CORS header;
     - an unknown origin: `403`, and no `Access-Control-*` header.
4. **The grant's club as the tenant (review E5-T24 #3).**
   - `AttachmentService.signed(...)` checks the signature, loads the grant (`UploadGrantRepository.signed`) and runs the handler inside `TenantContext.open(grant.clubId())`. It is used by the four signed routes.
   - `requireSignupOpen`'s `TenantContext.require()` now works on `PUT /signup/uploads` whatever the host. Nothing is left open afterwards.
   - The Javadocs of `UploadGrantRepository`, `SignedFileRequests` and `TenantFilter` say so.

### Evidence

Logs are in `roadmap/evidence/E5-T26/`. No token, signature or signed URL is in them: checked with `grep -cE "eyJ[A-Za-z0-9_-]{10,}|Bearer [A-Za-z0-9._-]{20,}|signature=[A-Za-z0-9_-]{20,}|access_token\""` → 0 in every log.

**Development runs** (not Verification commands):
- `./mvnw -q '-Dtest=AttachmentServiceTest' -Dsurefire.failIfNoSpecifiedTests=false -Djacoco.skip=true test`:
  - first try: exit 1, a compilation error in `PublicActivitiesController`, which also calls `openLocal`. Fixed with `.content()`. That log was overwritten by the rerun, so only the rerun is committed.
  - rerun: exit 0 (`00-dev-unit.log`).
- `./mvnw '-Dtest=SignupSecurityFixesIT,ActivityIT,SignupIT,AttachmentServiceTest' … test`: exit 0, 124 tests (`01-dev-targeted-1.log`).
- After adding the CSP assertion, `./mvnw '-Dtest=SignupSecurityFixesIT,AttachmentServiceTest' … test`: exit 0, 61 tests (`01-dev-targeted-2.log`).

**Before-fix runs:**
- **Tree A.** `python3 roadmap/evidence/E5-T26/02-before-fix-tree.py`, exit 0 (`02-before-fix-tree.log`). It restores every file under `src/main` that differs from HEAD, and HEAD's `AttachmentServiceTest`, which cannot compile without `Download`.
  - `./mvnw -f target/before-fix/pom.xml '-Dtest=SignupSecurityFixesIT#R_04_27_R_04_08_aMembersAddDogUploadPassesWhileSignupIsClosedAndAnAnonymousOneDoesNot+CONVENCIONS_API_5_R_04_27_theSignupUploadRouteTakesTheGrantsClubWhateverTheHostOrBearer+CONVENCIONS_API_5_localDownloadsAnswerTheStoredTypeAndNameAndShowImagesInline+CONVENCIONS_API_5_T_01_25_theSignedUploadRoutesPassTheCorsPreflightOfTheClubsWebOrigin' -Dsurefire.failIfNoSpecifiedTests=false -Djacoco.skip=true test`: exit 1, 4 tests, 4 failures (`03-before-fix-targeted.log`).
  - The CSP assertion was added to step 2's IT after that run. So tree A was rebuilt (`12-before-fix-tree-again.log`, exit 0) and the same command was run again on the final tests: exit 1, the same 4 failures (`13-before-fix-final-tests.log`). Its failures, verbatim:
    ```
    [ERROR]   SignupSecurityFixesIT.CONVENCIONS_API_5_R_04_27_theSignupUploadRouteTakesTheGrantsClubWhateverTheHostOrBearer:1029 [a host of no club and an invalid bearer, code=UNAUTHENTICATED] 
    expected: 204
     but was: 401
    [ERROR]   SignupSecurityFixesIT.CONVENCIONS_API_5_T_01_25_theSignedUploadRoutesPassTheCorsPreflightOfTheClubsWebOrigin:1097 [/api/v1/signup/uploads code=UNKNOWN_HOST] 
    expected: 204
     but was: 404
    [ERROR]   SignupSecurityFixesIT.CONVENCIONS_API_5_localDownloadsAnswerTheStoredTypeAndNameAndShowImagesInline:1059 
    expected: "image/png"
     but was: "application/octet-stream"
    [ERROR]   SignupSecurityFixesIT.R_04_27_R_04_08_aMembersAddDogUploadPassesWhileSignupIsClosedAndAnAnonymousOneDoesNot:1002 [the member's PUT with the upload's headers only, code=SIGNUP_CLOSED] 
    expected: 204
     but was: 422
    [ERROR] Tests run: 4, Failures: 4, Errors: 0, Skipped: 0
    ```
- **Tree B** (step 4 only). `python3 roadmap/evidence/E5-T26/02-before-fix-tree.py --without-tenant`, exit 0 (`04-before-fix-tree-b.log`). The handler runs without `TenantContext.open(grant.clubId())`.
  - `./mvnw -f target/before-fix-b/pom.xml '-Dtest=AttachmentServiceTest#CONVENCIONS_API_5_R_04_27_signedRoutesRunWithTheGrantsClubAsTheTenant,SignupSecurityFixesIT#R_04_27_R_04_08_aMembersAddDogUploadPassesWhileSignupIsClosedAndAnAnonymousOneDoesNot+CONVENCIONS_API_5_R_04_27_theSignupUploadRouteTakesTheGrantsClubWhateverTheHostOrBearer' -Dsurefire.failIfNoSpecifiedTests=false -Djacoco.skip=true test`: exit 1, 3 tests, 3 failures (`05-before-fix-b-targeted.log`):
    ```
    [ERROR]   SignupSecurityFixesIT.CONVENCIONS_API_5_R_04_27_theSignupUploadRouteTakesTheGrantsClubWhateverTheHostOrBearer:1029 [a host of no club and an invalid bearer, code=NO_MEMBERSHIP] 
    expected: 204
     but was: 403
    [ERROR]   SignupSecurityFixesIT.R_04_27_R_04_08_aMembersAddDogUploadPassesWhileSignupIsClosedAndAnAnonymousOneDoesNot:1011 [an anonymous grant] 
    expected: 422
     but was: 403
    [ERROR]   AttachmentServiceTest.CONVENCIONS_API_5_R_04_27_signedRoutesRunWithTheGrantsClubAsTheTenant:212 [the grant's club while the storage writes and reads] 
    Expecting actual:
      [null, null]
    to contain exactly (and in same order):
      ["another-club", "another-club"]
    ```
    The ITs get `403 NO_MEMBERSHIP`: `TenantContext.require()` fails in the handler.
- **Tree C** (step 1's grant change only). `python3 roadmap/evidence/E5-T26/02-before-fix-tree.py --without-member-account`, exit 0 (`06-before-fix-tree-c.log`). A MEMBER's grant stores no account.
  - `./mvnw -f target/before-fix-c/pom.xml '-Dtest=AttachmentServiceTest#R_04_27_aMembersSignupUploadGrantCarriesTheAccount,SignupSecurityFixesIT#R_04_27_R_04_08_aMembersAddDogUploadPassesWhileSignupIsClosedAndAnAnonymousOneDoesNot' -Dsurefire.failIfNoSpecifiedTests=false -Djacoco.skip=true test`: exit 1, 2 tests, 2 failures (`07-before-fix-c-targeted.log`):
    - the IT at `:1002`: `[the member's PUT with the upload's headers only, code=SIGNUP_CLOSED] expected: 204 but was: 422`;
    - the unit test at `:241` (the grants' `accountId`s are `[null, null]`, not `["example-account", null]`).

**Verification 1: `/Users/jordib/Dropbox/Documents/SOFTWARE_CANIC/05-desenvolupament/roadmap-kit/mac/heavy.sh ./mvnw -q clean verify > roadmap/evidence/E5-T26/08-clean-verify.log 2>&1; echo "exit $?"`**
- Exit 0. Full output: `08-clean-verify.log` (5960 lines). The tail is Testcontainers and Mongo logging: with `-q`, a green run prints no summary.
- Last 40 lines:
```
2026-09-26T17:47:46.235+02:00  INFO 98033 --- [agilityhub-core-api] [           main] org.mongodb.driver.client                : MongoClient with metadata {"driver": {"name": "mongo-java-driver|sync|spring-boot", "version": "5.5.2"}, "os": {"type": "Darwin", "name": "Mac OS X", "architecture": "aarch64", "version": "26.4.1"}, "platform": "Java/Homebrew/21.0.12.1"} created with settings MongoClientSettings{readPreference=primary, writeConcern=WriteConcern{w=null, wTimeout=null ms, journal=null}, retryWrites=true, retryReads=true, readConcern=ReadConcern{level=null}, credential=null, transportSettings=null, commandListeners=[io.micrometer.core.instrument.binder.mongodb.MongoMetricsCommandListener@10f3f34e], codecRegistry=ProvidersCodecRegistry{codecProviders=[ValueCodecProvider{}, BsonValueCodecProvider{}, DBRefCodecProvider{}, DBObjectCodecProvider{}, DocumentCodecProvider{}, CollectionCodecProvider{}, IterableCodecProvider{}, MapCodecProvider{}, GeoJsonCodecProvider{}, GridFSFileCodecProvider{}, Jsr310CodecProvider{}, JsonObjectCodecProvider{}, BsonCodecProvider{}, EnumCodecProvider{}, com.mongodb.client.model.mql.ExpressionCodecProvider@75f0a654, com.mongodb.Jep395RecordCodecProvider@1c60b49c, com.mongodb.KotlinCodecProvider@5573267]}, loggerSettings=LoggerSettings{maxDocumentLength=1000}, clusterSettings={hosts=[localhost:55870], srvServiceName=mongodb, mode=SINGLE, requiredClusterType=UNKNOWN, requiredReplicaSetName='null', serverSelector='null', clusterListeners='[]', serverSelectionTimeout='30000 ms', localThreshold='15 ms'}, socketSettings=SocketSettings{connectTimeoutMS=10000, readTimeoutMS=0, receiveBufferSize=0, proxySettings=ProxySettings{host=null, port=null, username=null, password=null}}, heartbeatSocketSettings=SocketSettings{connectTimeoutMS=10000, readTimeoutMS=10000, receiveBufferSize=0, proxySettings=ProxySettings{host=null, port=null, username=null, password=null}}, connectionPoolSettings=ConnectionPoolSettings{maxSize=100, minSize=0, maxWaitTimeMS=120000, maxConnectionLifeTimeMS=0, maxConnectionIdleTimeMS=0, maintenanceInitialDelayMS=0, maintenanceFrequencyMS=60000, connectionPoolListeners=[io.micrometer.core.instrument.binder.mongodb.MongoMetricsConnectionPoolListener@5dc3c423], maxConnecting=2}, serverSettings=ServerSettings{heartbeatFrequencyMS=10000, minHeartbeatFrequencyMS=500, serverMonitoringMode=AUTO, serverListeners='[]', serverMonitorListeners='[]'}, sslSettings=SslSettings{enabled=false, invalidHostNameAllowed=false, context=null}, applicationName='null', compressorList=[], uuidRepresentation=JAVA_LEGACY, serverApi=null, autoEncryptionSettings=null, dnsClient=null, inetAddressResolver=null, contextProvider=null, timeoutMS=null}
2026-09-26T17:47:46.236+02:00  INFO 98033 --- [agilityhub-core-api] [localhost:55870] org.mongodb.driver.cluster               : Monitor thread successfully connected to server with description ServerDescription{address=localhost:55870, type=REPLICA_SET_PRIMARY, cryptd=false, state=CONNECTED, ok=true, minWireVersion=0, maxWireVersion=21, maxDocumentSize=16777216, logicalSessionTimeoutMinutes=30, roundTripTimeNanos=1355625, minRoundTripTimeNanos=0, setName='docker-rs', canonicalAddress=f1b1ba196905:27017, hosts=[f1b1ba196905:27017], passives=[], arbiters=[], primary='f1b1ba196905:27017', tagSet=TagSet{[]}, electionId=7fffffff0000000000000001, setVersion=1, topologyVersion=TopologyVersion{processId=6ab7e7fd304d5fb45abf887e, counter=6}, lastWriteDate=Sat Sep 26 17:47:45 CEST 2026, lastUpdateTimeNanos=241471438088041}
2026-09-26T17:47:46.681+02:00  INFO 98033 --- [agilityhub-core-api] [           main] c.a.core.migration.PlayoffMigrationIT    : Started PlayoffMigrationIT in 0.64 seconds (process running for 295.27)
E5-T07 outbox backlog discarded before PlayoffMigrationIT: 158 PENDING records (more than one dispatch() batch)
E5-T07 outbox backlog discarded before PlayoffMigrationIT: 158 PENDING records (more than one dispatch() batch)
E5-T07 outbox backlog discarded before PlayoffMigrationIT: 158 PENDING records (more than one dispatch() batch)
2026-09-26T17:47:53.526+02:00  INFO 98033 --- [agilityhub-core-api] [localhost:56241] org.mongodb.driver.cluster               : Exception in monitor thread while connecting to server localhost:56241

com.mongodb.MongoSocketOpenException: Exception opening socket
	at com.mongodb.internal.connection.SocketStream.lambda$open$0(SocketStream.java:85) ~[mongodb-driver-core-5.5.2.jar:na]
	at java.base/java.util.Optional.orElseThrow(Optional.java:403) ~[na:na]
	at com.mongodb.internal.connection.SocketStream.open(SocketStream.java:85) ~[mongodb-driver-core-5.5.2.jar:na]
	at com.mongodb.internal.connection.InternalStreamConnection.open(InternalStreamConnection.java:233) ~[mongodb-driver-core-5.5.2.jar:na]
	at com.mongodb.internal.connection.DefaultServerMonitor$ServerMonitor.lookupServerDescription(DefaultServerMonitor.java:219) ~[mongodb-driver-core-5.5.2.jar:na]
	at com.mongodb.internal.connection.DefaultServerMonitor$ServerMonitor.run(DefaultServerMonitor.java:176) ~[mongodb-driver-core-5.5.2.jar:na]
Caused by: java.net.ConnectException: Connection refused
	at java.base/sun.nio.ch.Net.pollConnect(Native Method) ~[na:na]
	at java.base/sun.nio.ch.Net.pollConnectNow(Net.java:694) ~[na:na]
	at java.base/sun.nio.ch.NioSocketImpl.timedFinishConnect(NioSocketImpl.java:542) ~[na:na]
	at java.base/sun.nio.ch.NioSocketImpl.connect(NioSocketImpl.java:592) ~[na:na]
	at java.base/java.net.SocksSocketImpl.connect(SocksSocketImpl.java:327) ~[na:na]
	at java.base/java.net.Socket.connect(Socket.java:751) ~[na:na]
	at com.mongodb.internal.connection.SocketStreamHelper.initialize(SocketStreamHelper.java:76) ~[mongodb-driver-core-5.5.2.jar:na]
	at com.mongodb.internal.connection.SocketStream.initializeSocket(SocketStream.java:104) ~[mongodb-driver-core-5.5.2.jar:na]
	at com.mongodb.internal.connection.SocketStream.open(SocketStream.java:79) ~[mongodb-driver-core-5.5.2.jar:na]
	... 3 common frames omitted

E5-T07 outbox backlog discarded before PlayoffMigrationIT: 162 PENDING records (more than one dispatch() batch)
E5-T07 outbox backlog discarded before PlayoffMigrationIT: 158 PENDING records (more than one dispatch() batch)
E5-T07 outbox backlog discarded before PlayoffMigrationIT: 158 PENDING records (more than one dispatch() batch)
E5-T07 outbox backlog discarded before PlayoffMigrationIT: 158 PENDING records (more than one dispatch() batch)
E5-T07 outbox backlog discarded before PlayoffMigrationIT: 158 PENDING records (more than one dispatch() batch)
E5-T07 outbox backlog discarded before PlayoffMigrationIT: 158 PENDING records (more than one dispatch() batch)
E5-T07 outbox backlog discarded before PlayoffMigrationIT: 158 PENDING records (more than one dispatch() batch)
E5-T07 outbox backlog discarded before PlayoffMigrationIT: 159 PENDING records (more than one dispatch() batch)
E5-T07 outbox backlog discarded before PlayoffMigrationIT: 158 PENDING records (more than one dispatch() batch)
E5-T07 outbox backlog discarded before PlayoffMigrationIT: 158 PENDING records (more than one dispatch() batch)
E5-T07 outbox backlog discarded before PlayoffMigrationIT: 158 PENDING records (more than one dispatch() batch)
E5-T07 outbox backlog discarded before PlayoffMigrationIT: 158 PENDING records (more than one dispatch() batch)
E5-T07 outbox backlog discarded before PlayoffMigrationIT: 159 PENDING records (more than one dispatch() batch)
```
- Test summary: `python3 roadmap/evidence/E5-T26/09-test-summary.py > roadmap/evidence/E5-T26/09-test-summary.log 2>&1`, exit 0. It reads `target/surefire-reports` and `target/failsafe-reports` of that run:
```
surefire totals: tests=588 failures=0 errors=0 skipped=0 (128 classes)
failsafe totals: tests=1038 failures=0 errors=0 skipped=0 (88 classes)
Classes this task changes, and the classes that exercise the routes it touches:
  [surefire] com.agilityhub.core.clubs.followup.application.AttachmentServiceTest: tests=12 failures=0 errors=0 skipped=0
  [failsafe] com.agilityhub.core.clubs.census.api.SignupSecurityFixesIT: tests=49 failures=0 errors=0 skipped=0
  [failsafe] com.agilityhub.core.clubs.census.api.SignupIT: tests=33 failures=0 errors=0 skipped=0
  [failsafe] com.agilityhub.core.clubs.census.api.SignupFollowUpFixesIT: tests=14 failures=0 errors=0 skipped=0
  [failsafe] com.agilityhub.core.clubs.census.api.SignupS3UploadIT: tests=1 failures=0 errors=0 skipped=0
  [failsafe] com.agilityhub.core.clubs.census.api.CensusIT: tests=41 failures=0 errors=0 skipped=0
  [failsafe] com.agilityhub.core.clubs.activities.application.ActivityIT: tests=30 failures=0 errors=0 skipped=0
  [surefire] com.agilityhub.core.shared.api.TenantFilterTest: tests=3 failures=0 errors=0 skipped=0
  [failsafe] com.agilityhub.core.identity.api.CorsIT: tests=3 failures=0 errors=0 skipped=0
  [failsafe] com.agilityhub.core.configuration.OpenApiSnapshotTest: tests=10 failures=0 errors=0 skipped=0
E5-T26 test methods (added or changed) and the existing ones on the same routes:
  PASS AttachmentServiceTest#CONVENCIONS_API_5_R_04_27_signedRoutesRunWithTheGrantsClubAsTheTenant
  PASS AttachmentServiceTest#R_04_27_aMembersSignupUploadGrantCarriesTheAccount
  PASS AttachmentServiceTest#CONVENCIONS_API_5_localSignedUrlsAuthoriseThemselves
  PASS SignupSecurityFixesIT#R_04_27_R_04_08_aMembersAddDogUploadPassesWhileSignupIsClosedAndAnAnonymousOneDoesNot
  PASS SignupSecurityFixesIT#CONVENCIONS_API_5_R_04_27_theSignupUploadRouteTakesTheGrantsClubWhateverTheHostOrBearer
  PASS SignupSecurityFixesIT#CONVENCIONS_API_5_localDownloadsAnswerTheStoredTypeAndNameAndShowImagesInline
  PASS SignupSecurityFixesIT#CONVENCIONS_API_5_T_01_25_theSignedUploadRoutesPassTheCorsPreflightOfTheClubsWebOrigin
  PASS SignupSecurityFixesIT#CONVENCIONS_API_5_R_04_19_aSignedUploadUrlTakesAPutWithItsHeadersOnlyAndItsFileIsClaimed
  PASS SignupSecurityFixesIT#CONVENCIONS_API_5_signedDownloadUrlsAnswerWithoutABearerAndRefuseAnotherFilesSignature
  PASS SignupSecurityFixesIT#R_04_27_T_04_23_anonymousSignupRoutesAreClosedWithTheForm
  PASS SignupSecurityFixesIT#R_04_08_T_04_13_signupUploadUrlReturnsTheHeadersTheStorageSigned
```

**Verification 2: `bin/e3-smoke`, twice, each on a fresh disposable Compose stack**
- Run 1: `/Users/jordib/Dropbox/Documents/SOFTWARE_CANIC/05-desenvolupament/roadmap-kit/mac/heavy.sh bin/e3-smoke > roadmap/evidence/E5-T26/10-e3-smoke-run1.log 2>&1; echo "exit $?"`
  - Exit 0. Full output: `10-e3-smoke-run1.log` (83 lines). Its head shows `club:apply` and `seed:demo` with `0 changes` on their second run.
  - Last 40 lines:
```
curl POST /api/v1/members/1c2be251-1061-44ca-853e-a674798358ee/validation -> 200
PASS N-37 in the member APP inbox, scoped to account and entity
PASS D1 dashboard + counters: pending=3, olderThanWarn=1, activeMembers=185
curl POST /api/v1/signup -> 201
PASS D1 dashboard + counters: pending=4, olderThanWarn=1, activeMembers=185
curl GET /api/v1/members/8362f6ce-18c3-4a8a-a4a5-9a425ed2f963/signup -> 200
curl POST /api/v1/members/8362f6ce-18c3-4a8a-a4a5-9a425ed2f963/rejection -> 200
PASS mailbox N-03 correlated by notificationId; locale=ca
PASS D1 dashboard + counters: pending=3, olderThanWarn=1, activeMembers=185
PASS signup without family -> rejection -> N-03; D1 counters restored
curl POST /api/v1/signup -> 201
PASS D1 dashboard + counters: pending=4, olderThanWarn=1, activeMembers=185
curl GET /api/v1/members/f5925550-ccc4-3b2d-b6df-e2ee80165a20/signup -> 200
PASS readmission of the demo's LEFT member reuses its INACTIVE seed dog and its received card
curl POST /api/v1/attachments/upload-url -> 201
curl PUT /api/v1/attachments/uploads/38b4bc39-e28d-42a2-a533-fe31eb8f7721 -> 204
curl PATCH /api/v1/dogs/fcbdf40a-3d75-32c3-bc8f-8d2103c8166d -> 200
curl GET /api/v1/members/f5925550-ccc4-3b2d-b6df-e2ee80165a20/signup -> 200
curl GET /api/v1/attachments/files/38b4bc39-e28d-42a2-a533-fe31eb8f7721 -> 200
curl GET /api/v1/attachments/files/38b4bc39-e28d-42a2-a533-fe31eb8f7721 -> 200
PASS D2 adds a file with the ADMIN's DOG_DOCUMENT upload; the signed upload and download URLs need no bearer; the download is application/pdf, attachment, with the stored name
curl POST /api/v1/members/f5925550-ccc4-3b2d-b6df-e2ee80165a20/rejection -> 200
PASS D1 dashboard + counters: pending=3, olderThanWarn=1, activeMembers=185
PASS rejected readmission restores the LEFT member and its INACTIVE dog; D1 counters restored
curl POST /api/v1/signup/upload-urls -> 200
curl GET /api/v1/parameters/signup.enabled -> 200
curl PUT /api/v1/parameters/signup.enabled -> 200
curl POST /api/v1/signup -> 422 SIGNUP_CLOSED
PASS signup.enabled=false closes the form; canonical catalog status is 422 SIGNUP_CLOSED
curl POST /api/v1/signup/upload-urls -> 200
curl OPTIONS /api/v1/signup/uploads -> 200
curl PUT /api/v1/signup/uploads -> 204
curl PUT /api/v1/signup/uploads -> 422
PASS signup closed: the member's signed upload passes the CORS preflight and the PUT with its headers only, without a bearer or the club's host; an anonymous one answers 422 SIGNUP_CLOSED
curl POST /api/v1/me/dogs/signup -> 201
curl GET /api/v1/members/1c2be251-1061-44ca-853e-a674798358ee/signup -> 200
curl GET /api/v1/signup/files -> 200
PASS the member's add-dog claims the file; its download from the web's origin is image/png, inline, with the stored name
PASS T-04-34 / T-14-11: E3 backend gate completed; credentials and capabilities withheld
Removed disposable Compose services, volumes and private mailbox; existing stacks preserved
```
- Run 2: `/Users/jordib/Dropbox/Documents/SOFTWARE_CANIC/05-desenvolupament/roadmap-kit/mac/heavy.sh bin/e3-smoke > roadmap/evidence/E5-T26/11-e3-smoke-run2.log 2>&1; echo "exit $?"`
  - Exit 0. Full output: `11-e3-smoke-run2.log` (83 lines).
  - Last 40 lines:
```
curl POST /api/v1/members/0e1b9841-d04d-4d00-9139-e81aa1a34c83/validation -> 200
PASS N-37 in the member APP inbox, scoped to account and entity
PASS D1 dashboard + counters: pending=3, olderThanWarn=1, activeMembers=185
curl POST /api/v1/signup -> 201
PASS D1 dashboard + counters: pending=4, olderThanWarn=1, activeMembers=185
curl GET /api/v1/members/1ec1c567-4d80-411c-9ce4-28bd9f9c0ffe/signup -> 200
curl POST /api/v1/members/1ec1c567-4d80-411c-9ce4-28bd9f9c0ffe/rejection -> 200
PASS mailbox N-03 correlated by notificationId; locale=ca
PASS D1 dashboard + counters: pending=3, olderThanWarn=1, activeMembers=185
PASS signup without family -> rejection -> N-03; D1 counters restored
curl POST /api/v1/signup -> 201
PASS D1 dashboard + counters: pending=4, olderThanWarn=1, activeMembers=185
curl GET /api/v1/members/46f1ac2b-674d-34ca-8162-57f6a0dcbb1f/signup -> 200
PASS readmission of the demo's LEFT member reuses its INACTIVE seed dog and its received card
curl POST /api/v1/attachments/upload-url -> 201
curl PUT /api/v1/attachments/uploads/4944e3e7-e935-40b8-8760-fe59b4663c8a -> 204
curl PATCH /api/v1/dogs/65c8d2e7-f17c-3bcc-abc7-72e833b9a7e0 -> 200
curl GET /api/v1/members/46f1ac2b-674d-34ca-8162-57f6a0dcbb1f/signup -> 200
curl GET /api/v1/attachments/files/4944e3e7-e935-40b8-8760-fe59b4663c8a -> 200
curl GET /api/v1/attachments/files/4944e3e7-e935-40b8-8760-fe59b4663c8a -> 200
PASS D2 adds a file with the ADMIN's DOG_DOCUMENT upload; the signed upload and download URLs need no bearer; the download is application/pdf, attachment, with the stored name
curl POST /api/v1/members/46f1ac2b-674d-34ca-8162-57f6a0dcbb1f/rejection -> 200
PASS D1 dashboard + counters: pending=3, olderThanWarn=1, activeMembers=185
PASS rejected readmission restores the LEFT member and its INACTIVE dog; D1 counters restored
curl POST /api/v1/signup/upload-urls -> 200
curl GET /api/v1/parameters/signup.enabled -> 200
curl PUT /api/v1/parameters/signup.enabled -> 200
curl POST /api/v1/signup -> 422 SIGNUP_CLOSED
PASS signup.enabled=false closes the form; canonical catalog status is 422 SIGNUP_CLOSED
curl POST /api/v1/signup/upload-urls -> 200
curl OPTIONS /api/v1/signup/uploads -> 200
curl PUT /api/v1/signup/uploads -> 204
curl PUT /api/v1/signup/uploads -> 422
PASS signup closed: the member's signed upload passes the CORS preflight and the PUT with its headers only, without a bearer or the club's host; an anonymous one answers 422 SIGNUP_CLOSED
curl POST /api/v1/me/dogs/signup -> 201
curl GET /api/v1/members/0e1b9841-d04d-4d00-9139-e81aa1a34c83/signup -> 200
curl GET /api/v1/signup/files -> 200
PASS the member's add-dog claims the file; its download from the web's origin is image/png, inline, with the stored name
PASS T-04-34 / T-14-11: E3 backend gate completed; credentials and capabilities withheld
Removed disposable Compose services, volumes and private mailbox; existing stacks preserved
```

### Step → test table

| Step | Test | Seen failing before the fix |
|---|---|---|
| 1 | IT `SignupSecurityFixesIT#R_04_27_R_04_08_aMembersAddDogUploadPassesWhileSignupIsClosedAndAnAnonymousOneDoesNot`: the member's upload URL, the PUT with `Upload.headers` only (204), the grant's account, the add-dog's claim, and an anonymous grant's `422` | tree A and tree C: `:1002`, the member's PUT answers `422 SIGNUP_CLOSED` |
| 1 | unit `AttachmentServiceTest#R_04_27_aMembersSignupUploadGrantCarriesTheAccount` | tree C: `:241`, no account on the grant |
| 1 | `bin/e3-smoke` `closed_signup_upload` | not run on an unfixed tree (the ITs above cover it) |
| 2 | IT `SignupSecurityFixesIT#CONVENCIONS_API_5_localDownloadsAnswerTheStoredTypeAndNameAndShowImagesInline`: an image, a PDF and a signup PDF; type, length, disposition and name (non-ASCII and quotes); `nosniff`, CSP, no CORP; CORS for the club's origin | tree A: `:1059`, `application/octet-stream` |
| 2 | unit `…#CONVENCIONS_API_5_R_04_27_signedRoutesRunWithTheGrantsClubAsTheTenant` (the `Download`'s name and type) | new API, only in trees B and C (fails in B for step 4) |
| 3 | IT `SignupSecurityFixesIT#CONVENCIONS_API_5_T_01_25_theSignedUploadRoutesPassTheCorsPreflightOfTheClubsWebOrigin` | tree A: `:1097`, the signup route's cross-origin PUT answers `404 UNKNOWN_HOST`. The preflight assertions pass on tree A (see Summary). |
| 4 | unit `AttachmentServiceTest#CONVENCIONS_API_5_R_04_27_signedRoutesRunWithTheGrantsClubAsTheTenant` | tree B: `:212`, tenants `[null, null]` |
| 4 | IT `SignupSecurityFixesIT#CONVENCIONS_API_5_R_04_27_theSignupUploadRouteTakesTheGrantsClubWhateverTheHostOrBearer` | tree A: `:1029`, `401` (the bearer was read). Tree B: `:1029`, `403 NO_MEMBERSHIP` (no tenant). |

Changed test: `AttachmentServiceTest#CONVENCIONS_API_5_localSignedUrlsAuthoriseThemselves` (E5-T24). It now runs with no tenant open, as a signed request does: `TenantFilter` opens none. Opening the grant's club over another open tenant would be `TENANT_MISMATCH`, by `TenantContext`'s rule. It also reads the stream from `Download.content()`.

### Files changed
- Main:
  - `clubs/followup/application/AttachmentService` (`Download`, `signed(...)`, the member's grant, `requireSignupOpen(boolean)`);
  - `clubs/followup/api/AttachmentsController` (`download(...)`) and `SignupFilesController`;
  - `clubs/followup/persistence/UploadGrantRepository` (Javadoc);
  - `clubs/activities/api/PublicActivitiesController` (`.content()`);
  - `shared/api/SignedFileRequests` and `TenantFilter` (comment);
  - `configuration/SecurityConfiguration`.
- Tests: `SignupSecurityFixesIT` (4 new ITs and their helpers) and `AttachmentServiceTest` (2 new tests, 1 changed).
- Script: `bin/e3-smoke` (`fictional_png`, `exchange`, `closed_signup_upload`, and the PDF download's headers).
- `CHANGELOG.md`.
- Evidence: `roadmap/evidence/E5-T26/*` (logs `00`–`13`, and the scripts `02-before-fix-tree.py` and `09-test-summary.py`). Also `roadmap/tasks/E5-T26.md`, `roadmap/STATUS.md` and `roadmap/MESSAGES.md` (one entry).
- **Not mine:** `docs/INCIDENCIES_OBERTES.md`, `docs/specs/S08-reserves-de-classes-i-llista-espera.md` and `roadmap/tasks/E5-T25.md` changed in the working tree during this session. That looks like the organizer's E5-T25 verification. I did not edit them, and the publish script will commit them with this session.

### Rules and tests implemented
- **Rules:**
  - S04 R-04-27 (a MEMBER adding a dog keeps the signup's upload routes) and R-04-08;
  - `CONVENCIONS_API.md` §5 (amended 26-09: a signed URL authorises itself, on the local profile too) and §9 (CORS for the club hosts);
  - A31.
  - Export links are untouched (§5's exception, S14 R-14-12).
- **Test ids:** the spec has no `T-xx-nn` for these rows, so the new tests are named after their rules and conventions (`R_04_27_…`, `CONVENCIONS_API_5_…`). Step 3's IT carries `T_01_25` (the CORS test id of `CorsIT`).

### Assumptions
1. **`PUT /api/v1/signup/uploads` is a signed route** (steps 1, 3 and 4). Step 1 says its signature authorises it with no bearer, step 3 says «these routes have no tenant», and step 4 speaks of «the signed routes». E5-T24 kept this route's host and member bearer, but the grant now says what the bearer said.
   - A PUT from a host of no club, or with any bearer, passes on a valid signature.
   - The web's E4-W13 workaround (a bearer on the PUT) still works: the bearer is ignored.
   - A change: an anonymous grant's PUT with a member's bearer, while closed, now answers `422`. Before, the bearer let it pass. The web's add-dog asks its URL with the bearer, so its grant is a member's.
2. **Order of checks on `PUT /signup/uploads`:** the signature first (`403`), then the grant (`400 FILE_NOT_FOUND` if it is gone), then the closure (`422`, anonymous grants only), then the purpose and state (`400 FILE_NOT_FOUND`), then the type. Before, the closure came first, so a wrong signature while closed answered `422`. It now answers `403`, as on the other signed routes (E5-T24 assumption 4).
3. **«The stored name» is the grant's `fileName`:** the name given to the upload URL, sanitised for signup files. The name a document gives the file later (`files[].name`, or D2's PATCH) is not in the signed URL, so the download cannot know it.
4. **«Images» are the stored types `image/*`.** SVG is included, because `files.allowedTypes` allows `image/*`.
   - The response keeps `X-Content-Type-Options: nosniff` and the API's CSP (`default-src 'self'`, no `unsafe-inline`). So an inline SVG opened on the API's origin runs no inline script. The IT pins both headers.
   - A stored type that does not parse falls back to `application/octet-stream` and `attachment`.
5. **S3 is unchanged.** Its download URLs still ask `response-content-disposition=attachment`, with the object's stored type. An `<img>` shows them anyway, because a disposition does not apply to an image in a page. The task scopes step 2 to the local routes.
6. **The member's grant stores `CurrentUser.accountId()`** (the token's subject). It is a marker only. The add-dog's claim still checks the club, the purpose and the binding, as before. A member's grant also passes when the club is not `ACTIVE`, as the member's bearer already did at the upload URL (R-04-27: «A MEMBER adding a dog keeps them»).

### Questions / catalog proposals
1. No new parameter, event, notification or error code.
2. No command was denied that the task needed. Two helper commands were denied and replaced with plain ones; nothing was skipped:
   - a subshell `( … )` that built trees B and C: I ran the two builds as separate commands;
   - an inline `python3 -c` with a `#` comment: I used a file under `target/` instead.

### Web
- `PUT` the signup upload URL with `Upload.headers` only, in add-dog mode too. The E4-W13 bearer workaround can go once the core image's revision includes E5-T26.
- Local download URLs (`/api/v1/attachments/files/…`, `/api/v1/signup/files?…`) now answer the stored type, so an `<img src>` shows a dog's photo or an activity's image. A non-image arrives as an `attachment`, with its stored name.

## Organizer verification
_(pending)_
