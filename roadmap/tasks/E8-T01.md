---
id: E8-T01
title: Contract S12 + S13 (invoicing, collections, remittances, billing runs and simulations, upfront payments, packs, pending charges, Stripe events, inactivity periods, leave requests) — OpenAPI forms, Mongo documents + indexes, error status audit, audit actions, 501 stubs
stage: E8
repo: agilityhub-core-api
thread: A
order: 10
status: ready
depends_on: [E5-T06, E3-T05]
branch:
pr:
updated: 2026-09-19
---

# E8-T01 · Contract S12 + S13

## Objective
Work packages S12 WP-12-A and S13 WP-13-A. Every route of S12 §6 and S13 §6 exists in `docs/openapi/openapi.json` with typed schemas, roles, modules, idempotency headers and canonical error statuses, answering `501 NOT_IMPLEMENTED` behind the real security / tenant / module guards, so the web builds mocks-first while E8-T02…T06 implement. In the same task every collection of the two verticals lands as a Mongo document with its indexes, the new `ErrorCode` values are audited against the closed catalog, the new events, parameters and audit actions are declared, and the notification fixtures exist. Same pattern as `roadmap/tasks/E4-T01.md` and `E5-T01.md`.

## Spec references
S12 §2, §3 (**every field of** `Invoice`, `InvoiceLine`, `Collection`, `Remittance`, `BillingRun`, `BillingSimulation`, `UpfrontPayment`, `PackBalance`, `PendingCharge`, `StripeEvent`, `CLUB.paymentProviders` + `CLUB.billing`), §4 R-12-01…R-12-30 (read all; this task encodes the shapes, not the behaviour), §5 (the four state machines and the transition table), §6 (**every row, the simulation JSON, the internal-service list**), §7, §8, §9, §12 WP-12-A, §13 · S13 §2, §3, §4 R-13-01…R-13-20, §5, §6 (every row + the `GET /me/inactivity-periods` JSON + the `ErrorCode` list), §7, §8, §9, §10, §12 WP-13-A, §13 · tests whose contract half lands here: **T-12-21, T-12-22, T-13-24, T-13-25** · `docs/specs/00-transversal/CATALEG_ERRORS.md` rows S12 and S13 + §1 + §3 rule 0 · `CATALEG_ESDEVENIMENTS.md` (billing and inactivity/leave rows plus the proposals block: `BillingRunCreated`, `BillingRunCompleted`, `PackAdjusted`, `MemberCardInvalidated`, `InactivityChanged`, `InactivityCancelled`, `LeaveCancelled`, `RemittanceReminderDue`) · `CATALEG_NOTIFICACIONS.md` N-10, N-11a, N-11b, N-14, N-18a…N-18d, N-28, N-30, N-35, N-38, N-41 · `CATALEG_PARAMETRES.md` blocks «Quotes i cobraments (`billing.*`)» and «Inactivitat i baixa (`inactivity.*`, `leave.*`)» plus the new-key block listed in step 8 · `MATRIU_PERMISOS.md` rows «Facturació (rebuts, remeses, cobraments, exports)» (MEMBER = read own invoices only; ADMIN = RW + X; INSTRUCTOR = nothing) and «Inactivitat / baixa» (MEMBER = write own request; ADMIN = RW + X; INSTRUCTOR = nothing) · `CONVENCIONS_API.md` §2…§7 · S14 R-14-09 (audit actions) · ADR-009.

## Context to load
`docs/specs/S12-facturacio-i-pagaments.md` and `docs/specs/S13-inactivitat-i-baixa.md` **complete** · the catalogs above · `docs/MODEL_DADES_PLATAFORMA.md` §0 glossary, §4 «Pagaments (ADR-009)», §7 (isolation and indexes) and the Annex B rows for the two verticals · `docs/decisions/ADR-009-pagaments-multiproveidor.md` · `roadmap/tasks/E4-T01.md` and `E5-T01.md` (copy the approach exactly) plus `src/main/java/com/agilityhub/core/configuration/E4ContractConfiguration.java`, `src/test/java/com/agilityhub/core/configuration/{E4ContractIT,E4ResponseContractTest,OpenApiRequiredContractTest,ErrorCatalogContractTest,EventCatalogContractTest,AuditContractTest}.java`, `src/test/resources/fixtures/contracts/e4-*.json`, `e5-*.json` · **existing code this stage builds on**: `payments/**` (E3-T03: `UpfrontPayment`, `SignupCheckoutSession`, `PaymentProvider`, `FakeCheckoutGateway`, `CheckoutService`, `CheckoutController`) · `clubs/catalogs/{persistence/{Plan,Price},application/{PriceResolver,PlanService,PriceService}}.java` (E2-T05: `Plan.billingMode`, `dogsIncluded`, `entryFee`, `pack`, `singleClass`) · `clubs/census/persistence/Member.java` and `clubs/census/application/MemberService.java` (`paymentMethod`, `nextInvoiceDate`, masking via `CensusRules.maskedIban`) · `clubs/signup/domain/{FirstMonthCalculator,UpfrontAllocator,UpfrontLines,FamilyHolderMatcher}.java` (E3-T02) · `clubs/bookings/application/ports/{PackBalancePort,InactivityPort}.java` (E5-T02 null objects — **E8-T05 replaces them; do not touch here**) · `clubs/common/**` (universal list, `ExportJob`, `ExportPolicy`, `SavedView`) · `platform/application/{ClubConfig,ClubConfigService,Module,ModuleGuard,RequiresModule}.java`, `platform/application/audit/*`, `platform/application/jobs/**` (E5-T01's `JobCatalog` already declares `expirations` and `billing-reminder`; they stay beanless until E8-T06) · `shared/application/{ClubClock,EventPublisher,OutboxDispatcher,TenantContext}.java`, `shared/application/lists/*`, `shared/domain/{Money,ErrorCode,ApiException,LocalizedText}.java` · `docs/playbooks/new-endpoint.md`, `new-entity.md`, `checklist.md` · this file.

## Steps
1. **S12 controllers + DTOs** (`payments/api`, springdoc annotations, bodies throw `UnsupportedOperationException` → `501`, guards real, `@RequiresModule(Module.BILLING)` on everything): `GET /billing/periods/{period}` · `POST /billing/simulations {period}` · `POST /billing/runs {period, simulationId, collectionDate?}` (`Idempotency-Key`) · `GET /billing/runs/{id}` · `POST /billing/runs/{id}/card-charges` (202) · `POST /billing/runs/{id}/rollback {reason, confirmation}` · `GET /remittances` (universal list, `x-filterable: period, status`) · `GET /remittances/{id}` · `GET /remittances/{id}/file` (signed URL, `Content-Disposition`) · `POST /remittances/{id}/submission` · `GET /invoices` (universal list, `x-filterable: period, status, memberId, paymentMethodType, runId, remittanceId, issueDate, total, kind`; `x-sortable: number, issueDate, total, memberLastName`; free-text `q` over number and member) · `GET /invoices/{id}` · `POST /invoices {memberId, lines[], includeInNextRun?, note}` · `POST /invoices/{id}/payment` and `POST /invoices/payments` · `POST /invoices/{id}/failure` · `POST /invoices/{id}/retry` · `POST /invoices/{id}/refund` · `POST /invoices/{id}/cancellation` · `GET /invoices/{id}/document` and `GET /me/invoices/{id}/document` (`application/pdf`) · `GET /me/invoices`, `GET /me/invoices/{id}` · `GET /upfront-payments?memberId=&status=` · `POST /upfront-payments` · `POST /upfront-payments/{id}/refund` · `POST /checkout-sessions` and `GET /checkout-sessions/{id}` (**already published by E3-T03: extend, do not redeclare** — add `bookingId`, `upfrontPaymentIds` and the `PENDING · PAID · EXPIRED` status enum, and say in the report what changed) · `POST /members/{id}/card-setup-link` and `POST /me/card-setup` · `GET /pack-balances?memberId=&dogId=`, `GET /me/pack-balances`, `POST /pack-balances`, `POST /pack-balances/{id}/adjustments` (all `@RequiresModule(Module.PACKS)`) · `GET /members/{id}/pending-charges` (`@RequiresModule(Module.SINGLE_CLASS)`) · `GET /billing/exports?period=&format=` · `POST /webhooks/stripe/{clubId}` (no JWT, signature-authenticated, outside the tenant filter — document the security scheme explicitly). Statuses as S12 §6 writes them (`201` on the creating routes, `202` on `card-charges`, `retry` and the refunds). Roles: ADMIN everywhere except the `/me/*` routes (MEMBER, impersonation accepted) and the webhook; INSTRUCTOR → `403` on every route of this vertical (`MATRIU_PERMISOS`); impersonation tokens on ADMIN routes → `403 IMPERSONATION_DENIED`.
2. **S13 controllers + DTOs** (`clubs/census/api`; `@RequiresModule(Module.INACTIVITY)` on every `*inactivity*` route, **none** on the leave routes): `GET /me/inactivity-periods` · `GET /me/inactivity-periods/preview?fromMonth=&toMonth=` · `POST /me/inactivity-periods` (201, `Idempotency-Key`) · `PATCH /me/inactivity-periods/{id}` (`version`) · `POST /me/inactivity-periods/{id}/cancellation` · `GET /me/leave-requests` · `POST /me/leave-requests` (201, `Idempotency-Key`) · `POST /me/leave-requests/{id}/cancellation` · `GET /inactivity-periods` (universal list, `x-filterable: memberId, state, fromMonth, toMonth, origin, requestedAt`; `x-sortable: fromMonth, requestedAt, memberLastName`) · `GET /inactivity-periods/{id}` · `POST /inactivity-periods` (201) · `POST /inactivity-periods/{id}/decision` · `PATCH /inactivity-periods/{id}` · `POST /inactivity-periods/{id}/termination` · `POST /inactivity-periods/{id}/cancellation` · `GET /leave-requests` (`x-filterable: memberId, state, source, requestedDate, effectiveDate, reasonKey, nps`; `x-sortable: requestedAt, requestedDate, effectiveDate`) · `GET /leave-requests/{id}` · `POST /leave-requests/{id}/decision` · `POST /members/{id}/leave` (201) · `DELETE /members/{id}/planned-leave` (204) · `POST /members/{id}/reactivation`. The `/me/*` routes accept the impersonation token and record `origin = BACKOFFICE` (R-13-18); a MEMBER may only reach **their own** period or request — another member's id, **including one of their own family group**, is `404` (R-13-18, T-13-24).
3. **Schemas** — the names below are the contract and each field list is the **complete** field table or JSON example of the owning spec (transcribe, do not summarize; required by default, nullable marked): `Invoice` (+ `InvoiceLine` with `origin MONTHLY_FEE · MAINTENANCE_FEE · INACTIVITY_FEE · SINGLE_CLASS · PACK · ADJUSTMENT · MIGRATED`, `status PENDING · COLLECTING · PAID · FAILED · CANCELLED`, `kind PERIODIC · MANUAL · MIGRATED`, `displayNumber`, `memberSnapshot`, frozen `paymentMethod {type SEPA_DD · CARD · MANUAL, maskedAccount?, holderName?, mandateRef?, last4?, channel?}`, `refundedTotal`) · `InvoiceListItem` (the D6 columns) · `Collection` (`status CREATED · SUBMITTED · SUCCEEDED · FAILED · REFUNDED`, `provider SEPA_XML · STRIPE · MANUAL`, `attempt`, `refunds[]`) · `Remittance` (`status GENERATED · SUBMITTED · ROLLED_BACK`, `messageId`, `requestedCollectionDate`, `sequenceBreakdown {FRST, RCUR}`, `xsdValidatedAt`, `creditor` snapshot **without the creditor IBAN in any response** — expose it masked) · `BillingRun` (`status GENERATED · CHARGING · COMPLETED · ROLLED_BACK`, `byProvider`, `rollbackable`, `rollbackBlockers[]`, `skipped[]`) · `BillingSimulation` (`incidents[] {memberId, memberName, code NO_BANK_ACCOUNT · NO_PLAN · NO_PRICE · CARD_INVALID · CURRENCY_MISMATCH · PROVIDER_DISABLED}`, `cashMembers[]`, `invoicesPreview[]`, `kpis`) · `BillingPeriod` (the `GET /billing/periods/{period}` form) · `UpfrontPayment` (`concept ENTRY_FEE · FIRST_MONTH · PACK · SINGLE_CLASS · ACTIVITY · OTHER`, `status DUE · CHECKOUT_PENDING · PARTIAL · PAID · CANCELLED · REFUNDED`) · `PackBalance` (+ `PackMovement {type OPEN · CONSUME · REFUND · ADJUST · EXPIRE, delta, bookingId?, reason?, at}`, `state ACTIVE · EXPIRED · CLOSED`, `remaining` derived) · `PendingCharge` · `InactivityPeriod` (`state REQUESTED · APPROVED · ACTIVE · FINISHED · DENIED · CANCELLED`, `feeSnapshot`, `history[]`, `cancelledBookings[]`, `editable {fromMonth, toMonth, cancel}`) · `MeInactivityContext` (the §6 JSON: `earliestFromMonth`, `proposedFromMonth`, `deadlineDay`, `fee?`, `periods[]`) · `InactivityPreview` (`bookingsInside {classes, waitlist, trainings, activities, total}`, `feeSchedule[]`, `earliestMonthViolation?`) · `LeaveRequest` (`state PENDING · APPROVED · DENIED · CANCELLED`, `source MEMBER · ADMIN · PACK_EXPIRED`, `decision {at, byAccountId, effectiveDate, note}`) · `MeLeaveContext` (`offerInactivity`, `fee?`, `defaultDate`, `fullMonthIfLater`, `npsEnabled`, `reasons[{key, label}]`, `plannedLeave?`, `requests[]`) · `PlannedLeave {date, source, requestId, since, cancellable}`. Money is always `Money {amountMinor, currency}`; months are `YYYY-MM` strings; dates are club-local `date`; instants are UTC.
4. **Error codes and status audit.** Every code below already exists in `ErrorCode` (E0-T04 loaded the catalog) except where noted: the work is the audit against `CATALEG_ERRORS.md` §1 + §3 rule 0, **and the catalog file wins over anything S12 §6 or S13 §6 writes**. Explicit in §1: `BILLING_BUSY`, `RUN_EXISTS`, `SIMULATION_STALE`, `RUN_NOT_ROLLBACKABLE`, `MAX_ATTEMPTS`, `INVALID_STATE`, `STALE_VERSION`, `IDEMPOTENCY_KEY_REUSED` **409** · `COLLECTION_DATE_TOO_SOON`, `CURRENCY_MISMATCH`, `PAYMENT_PROVIDER_NOT_ENABLED`, `MEMBER_NOT_ACTIVE`, `INACTIVITY_PERIOD` **422** · `WEBHOOK_SIGNATURE_INVALID` **401** (moved from 400 on 06-09) · `READ_ONLY`, `IMPERSONATION_DENIED`, `FORBIDDEN` **403** · `MODULE_DISABLED`, `NOT_FOUND` **404** · `VALIDATION_ERROR`, `INVALID_FILTER` **400** · `METHOD_NOT_ALLOWED` **405**. By rule 0 (`_EXISTS/_TAKEN/_IN_USE/_LOCKED/_OVERLAP/_CONFLICT` suffix, `ALREADY_`/`DUPLICATE_` prefix → 409; **everything else 422**): `INACTIVITY_OVERLAP` **409**; and **422** for `NO_INVOICES`, `SEPA_NOT_CONFIGURED`, `NO_PAYMENT_METHOD`, `AMOUNT_EXCEEDS_DUE`, `PLAN_NOT_PACK`, `PACK_NEGATIVE`, `REFUND_EXCEEDS_PAID` (S12 §6 writes 409), `INACTIVITY_DEADLINE_PASSED`, `INACTIVITY_INVALID_RANGE`, `INACTIVITY_INVALID_STATE` (S13 writes 409), `LEAVE_ALREADY_REQUESTED` and `LEAVE_ALREADY_SCHEDULED` (S13 writes 409; **only the bare `ALREADY_` prefix counts**, confirmed 09-09), `LEAVE_INVALID_STATE` (S13 writes 409), `LEAVE_DATE_INVALID`, `LEAVE_REASON_UNKNOWN` (S13 writes 400), `MEMBER_NOT_LEFT` (S13 writes 409), `NO_PLANNED_LEAVE` (S13 writes 409), `MEMBER_LEAVING`. Fix every enum value that contradicts the catalog and list the changes in the report. **`INACTIVITY_NOT_APPLICABLE`** (S13 R-13-02, pack plans) is **named by the spec and missing from `CATALEG_ERRORS.md` §2**: add it to the enum with **422** by rule 0, give it `error.INACTIVITY_NOT_APPLICABLE` in the three `messages_*.properties`, and file the catalog row proposal in the report and in `roadmap/MESSAGES.md` (the same precedent as `JOB_TRIGGERED` in E5-T01). `details` payloads the UI needs: `RUN_NOT_ROLLBACKABLE {reasons[]}` with `REMITTANCE_SUBMITTED · COLLECTION_SUBMITTED · INVOICE_PAID · MANUAL_INVOICE_AFTER`, `INACTIVITY_DEADLINE_PASSED {earliestMonth}`, `INACTIVITY_OVERLAP {periodId, hint: EXTEND}`, `COLLECTION_DATE_TOO_SOON {requested, earliest}`, `MAX_ATTEMPTS {attempts, max}`.
5. **Mongo documents + indexes** (`persistence` packages, `TenantEntity`, `clubId` on every document, `createdAt/By`, `updatedAt/By`, `version` where §3 says so; nothing is ever physically deleted): in **`payments/persistence`** — `Invoice` · `invoices` with unique `{clubId, series, number}`, plus `{clubId, period, status}`, `{clubId, memberId, issueDate}`, `{clubId, runId}`, `{clubId, remittanceId}` · `Collection` · `collections` with `{clubId, invoiceId, attempt}`, `{clubId, provider, status}`, `{clubId, remittanceId}` and unique sparse `{clubId, providerRef}` · `Remittance` · `remittances` with `{clubId, period, status}` and unique `{clubId, messageId}` · `BillingRun` · `billing_runs` with **partial unique** `{clubId, period}` on `status ∈ {GENERATED, CHARGING, COMPLETED}` (R-12-11 «one live run per club and period») · `BillingSimulation` · `billing_simulations` with unique `{clubId, period}` (only the last one per month is kept) · `UpfrontPayment` · `upfront_payments` — **already exists from E3-T03**: widen it to the S12 §3 field list (`concept`, `status`, `provider` sub-document, `bookingId`, `activityRegistrationId`, `packBalanceId`, `refunds[]`, `note`, `channel`, `reference`) **without breaking the signup flow**, keep the existing `signupConcept`, and say in the report exactly which fields you added · `PackBalance` · `pack_balances` with `{clubId, dogId, state, expiresOn}`, `{clubId, memberId, state}` and unique sparse `{clubId, upfrontPaymentId}` (idempotent opening) · `PendingCharge` · `pending_charges` with **unique** `{clubId, bookingId}` and `{clubId, memberId, invoiceId}` · `StripeEvent` · `stripe_events` with **unique** `{eventId}` and `{clubId, receivedAt}` · `billing_locks` (`_id = "{clubId}:billing"`, `holder`, `acquiredAt`, TTL on `expiresAt`, 10 min — R-12-11). In **`clubs/census/persistence`** — `InactivityPeriod` · `inactivity_periods` with `{clubId, memberId, state}`, `{clubId, state, fromMonth}` · `LeaveRequest` · `leave_requests` with `{clubId, memberId, state}`, `{clubId, state, requestedDate}`, `{clubId, source, state}`. New fields on `Member` (S13 §3): `leaveDate`, `leaveRequestId`, `leftAt`, `leftReason` (`SIGNUP_REJECTED · LEAVE_REQUEST · ADMIN · PACK_EXPIRED`), `leaveHistory[]`, and `paymentMethod.CARD.invalid` (S12 R-12-22) — add them to the document and to the `GET /members` projection filters `leaveSource`, `inactivityUntil`, `hasPendingRequest` (S13 R-13-17), all still answering 501 where behaviour is involved. `CLUB` (S02 document) gains `billing {invoiceSeriesPattern, nextNumber, resetYearly}` and `paymentProviders.STRIPE {publishableKey, secretKeyEnc, webhookSecretEnc, mode, accountId}` — **secrets never in the repo or in any response**: declare the fields, leave them empty, and mask them in every projection.
6. **Events + notification fixtures**: `BillingEvent` and `CensusLifecycleEvent` records (`domain` packages, `DomainEvent`) with the catalog names and the payloads of S12 §7 and S13 §7 — `InvoiceIssued`, `InvoiceCollecting`, `InvoicePaid`, `InvoiceFailed`, `InvoiceCancelled`, `RemittanceSimulated`, `RemittanceGenerated`, `RemittanceRolledBack`, `UpfrontPaymentRecorded`, `UpfrontPaymentSucceeded`, `UpfrontPaymentFailed`, `PackOpened`, `PackConsumed`, `PackRefunded`, `PackLowBalance`, `PackExpiring`, `PackExpired`, `StripeWebhookReceived`, `BillingRunCreated`, `BillingRunCompleted`, `PackAdjusted`, `MemberCardInvalidated`, `InactivityRequested`, `InactivityResolved`, `InactivityStarted`, `InactivityEnded`, `InactivityChanged`, `InactivityCancelled`, `LeaveRequested`, `LeaveResolved`, `LeaveCancelled`, `RemittanceReminderDue` — registered as samples in `EventCatalogContractTest` with fixtures `e8-events.json`. Notification fixtures `e8-notifications.json` for **N-10, N-11a, N-11b, N-14, N-18a, N-18b, N-18c, N-18d, N-28, N-30, N-35, N-38, N-41** with exactly the catalog variables (`member_name`, `invoice_number`, `amount`, `reason`, `dog_name`, `pack_remaining`, `pack_expiry`, `requested_date`, `from_month`, `to_month`, `decision`, `fee`, `admin_text`, `cancelled_count`, `effective_date`, `source`, `concept`, `retry_link`, `masked_account`, `period`, `pending_count`); rendering and sending stay with E7.
7. **Audit actions**: the existing S12/S13 rows of S14 R-14-09 — `INVOICE_MARKED_PAID`, `INVOICE_MARKED_FAILED`, `INVOICE_CANCELLED`, `REMITTANCE_GENERATED`, `REMITTANCE_ROLLED_BACK`, `UPFRONT_PAYMENT_RECORDED`, `PAYMENT_REFUNDED`, `MEMBER_PLAN_CHANGED`, `INACTIVITY_RESOLVED`, `LEAVE_RESOLVED`, `LEAVE_CANCELLED` — must all exist in `AuditAction`. S12 §13 additionally proposes **`PACK_ADJUSTED`, `INVOICE_CREATED_MANUAL`, `REMITTANCE_SUBMITTED`, `CARD_CHARGES_STARTED`**: add the four to the enum with their `@AuditCovers` placeholders (the real coverage arrives in E8-T02…T05) and file the catalog proposal in the report and `MESSAGES.md`, exactly as E5-T01 did for `JOB_TRIGGERED`. Sensitive masking (R-14-10): `paymentMethod.iban` → `@Sensitive(MASK_IBAN)`, the Stripe secrets → `@Sensitive(HIDE)`.
8. **Parameters**: register the whole `billing.*`, `inactivity.*` and `leave.*` families in the parameter catalog code (S02) with the exact keys, types and Cànic defaults of `CATALEG_PARAMETRES.md` — including **`billing.cashInvoicing = SEMESTER`**, **`billing.sepa.collectionDayOfMonth = 1`** (semantics: day of the **billed** month, `1–28`, `0` = last day), `billing.sepa.useFrst = false`, `billing.sepa.schema = pain.008.001.02`, `billing.invoiceSeriesPattern = {YYYY}`, `billing.invoiceResetYearly = true`, `billing.taxIncluded = true`, `billing.stripeMaxAttempts = 3`, `billing.remittanceReminderDay = 22`, `billing.packLowBalanceSessions = 1`, `billing.packExpiryWarningDays = 14`, `billing.singleClassCancelPolicy = REFUND`, `billing.upfrontCutoffDay = 25`, `billing.packToMemberEntryDiscountPercent = 40`, `billing.packToMemberMinSessions = 10`, `inactivity.requestDeadlineDay = 25`, `inactivity.cancelBookingsOnApproval = true`, `inactivity.maxStartMonthsAhead = 12`, `leave.npsEnabled = true`, `leave.fullMonthIfLater = true`, `leave.packExpiryGraceDays = 30`, and **`leave.reasons` as `json`** with the seven `{key, label: localizedText, audience}` entries of the catalog. All in the D11 block «Quotes, packs i remesa»; every key editable from D11 (A28/A29).
9. `bin/openapi-snapshot`, a `docs/openapi/CHANGELOG.md` entry (every operation and schema added), `E8ContractIT` (501 matrix: every route × ANON/MEMBER/INSTRUCTOR/ADMIN/AGILITYHUB_ADMIN, impersonation rules, cross-tenant `404` for invoices, runs, remittances, periods and leave requests of another club — T-12-21, T-13-24 —, `BILLING` off → `404 MODULE_DISABLED` on every S12 route and on `GET /me/invoices`, `PACKS` off → `404` on `/pack-balances*`, `SINGLE_CLASS` off → `404` on `/members/{id}/pending-charges`, `INACTIVITY` off → `404` on every `*inactivity*` route while `/leave-requests*` keeps answering — T-12-22, T-13-25 —, no writes on any collection), `E8ResponseContractTest` (fixture round-trip of the S12 §6 simulation JSON and the S13 §6 `GET /me/inactivity-periods` JSON), `OpenApiRequiredContractTest` updated.

## Fixed conventions
- Code names from `docs/MODEL_DADES_PLATAFORMA.md` §0 and Annex B: `Invoice`/`invoices`, `InvoiceLine` (embedded), `Collection`/`collections`, `Remittance`/`remittances`, `BillingRun`/`billing_runs`, `BillingSimulation`/`billing_simulations`, `UpfrontPayment`/`upfront_payments`, `PackBalance`/`pack_balances`, `PendingCharge`/`pending_charges`, `StripeEvent`/`stripe_events`, `InactivityPeriod`/`inactivity_periods`, `LeaveRequest`/`leave_requests`. **Contexts**: `payments` owns the whole of S12 **including `PackBalance`** (the §0 glossary row writes `clubs/billing`, a package that does not exist and that nothing else would fill — put it in `payments` and record the deviation in the report); `clubs.census` owns S13 in the sub-packages `census/inactivity` and `census/leave` (S13 §3).
- Dependency direction, enforced by `ArchitectureTest` (cross-context only through `application` packages, no cycles): `payments` → `clubs.catalogs.application`, `clubs.census.application`, `platform`, `shared`; `clubs.census` → `payments.application` **only through ports declared in `clubs/census/application/ports`**; `clubs.bookings` keeps its own `PackBalancePort`/`InactivityPort` (E5-T02) — `payments` and `clubs.census` supply the adapters in E8-T05, never the other way round.
- Closed catalogs: no new parameter, event or notification code. The only additions this task makes are the four audit actions and the `INACTIVITY_NOT_APPLICABLE` error code, all three named by S12 §13 / S13 §6; everything else a spec names and a catalog lacks goes to the report and `MESSAGES.md` as a proposal while the closest existing item ships. Catalog rule 0 is literal and `CATALEG_ERRORS.md` wins over any status written in S12, S13 or in this file.
- Assumptions in force (do not ask): cash is invoiced by natural half-years (`billing.cashInvoicing = SEMESTER`, Josep 08-09) **and the `MONTHLY` branch is product too** — both are configurable from D11 and both are tested (Jordi 09-09, A29); SEPA collection is day 1 of the **billed** month (`billing.sepa.collectionDayOfMonth = 1`, A28) with `0` = last day equally supported; every mandate is new (`{clubSlug}-{memberNumber}-1`, `mandateSignedAt` = cut-over date) and every remittance is `RCUR` (B5/B6); the Cànic has no VAT (`taxPercent = 0`, `billing.taxIncluded = true`) and the mechanism stays for other clubs (B3); the Teràpia plan carries the maintenance fee through `Plan.billingMode = MAINTENANCE` and `Member.billingMode` is retired (B10); unused pack sessions are lost at expiry (B11); pack plans cannot request inactivity (B12).
- Evidence rule: report = command + exit code + last 40 lines; long outputs → `roadmap/evidence/E8-T01/NN-<name>.log`; task file ≤ 120 KB. Fictional data only (`@example.test`, fake ids, fake IBANs from the `ES00` test range); **no secrets in the repo, env vars only** (Stripe keys, bank credentials). Never edit other tasks, `ROADMAP.md` or the Organizer verification section. English identifiers from the glossary; i18n keys never literals; ca/es/en always together.

## Verification (run all; evidence = command + exit code + last 40 lines; long outputs → `roadmap/evidence/<ID>/*.log`)
- `./mvnw -q verify` green with `E8ContractIT`, `E8ResponseContractTest`, `EventCatalogContractTest`, `ErrorCatalogContractTest`, `AuditContractTest`, `ArchitectureTest` (including the new context rules), message parity ca/es/en for every new `error.*` key and the `notif.N-*` keys listed in step 6, coverage gates.
- `bin/openapi-snapshot` run twice → byte-identical (`cmp`); `docs/openapi/CHANGELOG.md` lists the operations added (count them and state the number); `git diff --check` clean.

## Definition of Done
Every S12 and S13 route is published in the snapshot with its schemas, roles, modules, idempotency headers and canonical error statuses and answers `501` behind the guards; every collection and index of the two verticals exists (including the three guards E8 concurrency depends on: unique `{clubId, series, number}`, partial-unique live `{clubId, period}` on `billing_runs`, unique `{eventId}` on `stripe_events`); the parameter family, the events, the notification fixtures and the audit actions are registered; the organizer can stage the snapshot for the web tasks.

## Evidence to return
`verify` tail; snapshot `cmp` result; OpenAPI diff summary (routes + schemas added, existing schemas changed — in particular what you added to `UpfrontPayment` and `/checkout-sessions`); the list of `ErrorCode` values whose status you changed and why; the catalog proposals filed (`INACTIVITY_NOT_APPLICABLE`, `PACK_ADJUSTED`, `INVOICE_CREATED_MANUAL`, `REMITTANCE_SUBMITTED`, `CARD_CHARGES_STARTED`); the `mongosh` index listing of the eleven new collections.

---

## Executor report
_(not started)_

### Branch / commits

### Evidence (full command outputs)

### Files changed

### Rules and tests implemented (R-xx-nn / T-xx-nn)

### Assumptions

### Questions / catalog proposals

## Organizer verification
_(pending)_
