---
id: E3-T03
title: Signup endpoints (S04 WP-04-3): public flow, identity checks, uploads, family lookups, D2 validation/rejection, add-dog, PaymentProvider + FakeCheckoutGateway, rate limits, notifications N-01/02/03/37/39, seeds
stage: E3
repo: agilityhub-core-api
thread: A
order: 30
status: ready
depends_on: [E3-T02]
branch:
pr:
updated: 2026-09-10
---

# E3-T03 · Signup endpoints (S04 WP-04-3): public flow, identity checks, uploads, family lookups, D2 validation/rejection, add-dog, PaymentProvider + FakeCheckoutGateway, rate limits, notifications N-01/02/03/37/39, seeds

## Objective
Work package WP-04-3: a fictional person can apply from the public form, the club validates or rejects from D2, the member receives the welcome link, and an active member can add a dog — all on the real census entities of E2-T06.

## Spec references
S04 §4 R-04-05…08, R-04-10, R-04-11, R-04-18…27 · §5 transitions and effects · §6 (every route, statuses per the catalog) · §7 events · §8 notifications (N-01, N-02, N-03, N-37, N-39) · §9 modules matrix · tests **T-04-11…T-04-28** · S12 R-12-xx only as the `PaymentProvider` port (Stripe itself is E8) · S01 §14 (welcome link, `auth.welcomeLinkDays` proposal → use the catalog value if present, else `auth.magicLinkMinutes` for N-39 and 7 days for the welcome link, recorded as an assumption) · `docs/DECISIONS_PENDENTS.md` A14 (Checkout at R1: endpoint + fake gateway only), B13, B14.

## Context to load
S04 §4–§9, §11 (integration part) · E3-T01 contract · E3-T02 domain · E2-T06 census (`Member`, `Dog`, `DogDocument`, `FamilyGroup`, `MemberStatusService.transition`, attachments storage of S10 for the vaccination card files) · E1-T03/E1-T10 SYSTEM mail path (magic links, local mailbox) · E2-T02 parameters/club settings (`paymentProviders`, `legal`, `countryProfile`) · `IdempotencyFilter`, `RateLimitFilter`, `TenantFilter` (E0/E1) · this file.

## Steps
1. **`GET /signup`** (config in one call, cached 60 s per club + locale, invalidated by `PlanChanged`/`PriceChanged`/`ParameterChanged`/`ClubUpdated`): `enabled` (`signup.enabled` ∧ club `ACTIVE`, else only `closedText`), `steps` (4 with `FAMILY_GROUP`, 3 without), plans from `SignupPlanCatalog`, payment methods from the club's active providers (`SEPA_XML → SEPA_DD`, `STRIPE → CARD`, `MANUAL → MANUAL`, with `mandateText` = `signup:payment.mandate.{profile}` resolved with `[[club_legal_name]]`, `instructions` for MANUAL), texts from the `signup.text.*` parameters, `legal` from `CLUB.legal`; authenticated MEMBER → `member` block for the add-dog mode.
2. **`POST /signup/identity-checks`** (R-04-05): match on `idDocument.value` or primary email among `PENDING`/`ACTIVE` members → `NEW` · `SIGNUP_ALREADY_PENDING` · `VERIFICATION_SENT` (emit `SignupRecognitionRequested{memberId, accountId, redirect: /gossos/nou}` → N-39 through the SYSTEM magic-link path) · `CONTACT_CLUB` (ACTIVE without account); only `maskedEmail` leaves the server.
3. **`GET /signup/towns`**, **`POST /signup/upload-urls`** (signed PUT 15 min, key `signup/{clubId}/{yyyyMM}/{uuid}/{sanitisedName}`, `files.maxSizeMb`, `files.allowedTypes` image/pdf, ≤ 10 files, name ≤ 80 — reuse the S10 attachment storage of E2-T06 with a `SIGNUP_DOCUMENT` purpose), **`POST /signup/family-group-lookups`** (`FAMILY_GROUP`; `FamilyHolderMatcher`).
4. **`POST /signup`** (R-04-06…08, 10, 11, 17, 20, 27): honeypot `website` → `202` and nothing stored; body ≤ 64 KB; `Idempotency-Key` 24 h; identity re-check; readmission of a `LEFT` member keeps `number`; chip uniqueness (`DOG_CHIP_ALREADY_REGISTERED`, readmission re-activates the own `LEFT` dog); `DogDocument` `PENDING` + `DogDocumentPending` when no file (`DOG_DOCUMENT_REQUIRED` if `signup.requireDogDocumentAtSignup`); every `fileKey` must exist under the club prefix (`FILE_NOT_FOUND`); payment method validation (IBAN mod-97 optional, `holderName` default = applicant or holder, `CARD` only with Stripe configured); consents entries `{version, at, locale, ipHash}` immutable; family claim → `NONE·FOUND·NOT_FOUND_PENDING` (`leavePending` only with `signup.allowFamilyGroupPending`); `UpfrontPayment` lines `DUE` (E3-T02); one Mongo transaction (`Member` + `Dog` `PENDING`, documents, payments, consents, outbox `SignupSubmitted{source: PUBLIC, readmission, checkoutRequired}`); response `201 {memberId, signupToken (HMAC, 24 h, bound to memberId), upfront, checkout{required}}`; `SIGNUP_CLOSED` when disabled.
5. **`POST /checkout-sessions`** (R-04-26): `PaymentProvider` port (`createCheckoutSession`, `complete`, `expire`) with `FakeCheckoutGateway` in `local`/`test` (real Stripe = E8); ANON requires a valid `signupToken`; `mode = payment` (or `setup` when `totalDue = 0` ∧ `CARD`), one line per `UpfrontPayment`, metadata `{clubId, memberId, upfrontPaymentIds}`, `expires_at +24 h`; lines → `CHECKOUT_PENDING`; `fake.complete()` → `PAID` (+ `paymentMethod.CARD.last4`), `fake.expire()` → `DUE`; `INVALID_STATE` when nothing to pay; `PAYMENT_PROVIDER_NOT_ENABLED` without Stripe.
6. **`POST /me/dogs/signup`** (R-04-25; web E3-W01 asked how the add-dog flow reaches Checkout): `AddDogSignupResult.checkout` carries `{required, memberId}` (the caller's own member id, also available from `/me`), and `POST /checkout-sessions` for a MEMBER caller accepts only its own `memberId` (no `signupToken`); keep `required = false` unless Stripe is active for the club. MEMBER `ACTIVE` (impersonated → audited with both ids, `origin = BACKOFFICE`), `Dog` `PENDING`, document, `ENTRY_FEE` (+ `ADDITIONAL_DOG_FEE` per R-04-14 add-dog mode), `SignupSubmitted{source: APP_ADD_DOG}` → N-01 to the member (APP+EMAIL) and admins; `MEMBER_NOT_ACTIVE` otherwise.
7. **D2**: `GET /members/{id}/signup` (aggregate with `warnings[]` of R-04-18, `pendingDays`, `proposals` from `dryRun` logic), `PATCH /members/{id}` · `/dogs/{id}` allowed in `PENDING` emitting `SignupEdited` (not `MemberUpdated`) and auditing before/after (R-04-19; consents not editable → 400), **`POST /members/{id}/validation?dryRun=`** (R-04-13…16, 21, 22, 25: `LEVEL_REQUIRED` with `levels.enabled`, `NEXT_INVOICE_DATE_REQUIRED` for `MONTHLY` with `BILLING`, family group creation/joining, fare proposal, upfront allocation, atomic `number` (`findAndModify` counter per club, never reused), `Account` reuse-or-create (`AccountCreated{source: SIGNUP}`, locale = signup locale, no password) + `Membership {roles: [MEMBER]}` (`MEMBERSHIP_EXISTS`), dogs `ACTIVE` with `levelAssignedAt`, `mandateRef` when SEPA, all in one transaction with the outbox `MemberValidated` (+ `DogRegistered` and **no** `MemberValidated` for the add-dog case) → N-02 with the welcome magic link; `dryRun=true` writes nothing), **`POST /members/{id}/rejection`** (R-04-23: `reason` 3–500, `LEFT{SIGNUP_REJECTED}`, dogs `INACTIVE`, payments `CANCELLED` except `PAID` (warning), `SignupRejected` → N-03; second rejection `INVALID_STATE`; add-dog case only the pending dogs).
8. **Rate limits and anti-abuse** (R-04-20): per IP (`X-Forwarded-For`) and club through the existing `RateLimitFilter` policies — `identity-checks` 10/h, `family-group-lookups` 20/h, `upload-urls` 30/h, `POST /signup` 5/h and 20/day, anonymous `checkout-sessions` 10/h, `towns` 60/h → `429` + `Retry-After`; structured logs with `traceId`, `clubId`, `ipHash`.
9. **Notifications** N-01, N-02 (gender ICU, welcome link), N-03, N-37, N-39 delivered through the E1 SYSTEM path with the S11 template codes (the club templates arrive in E7; here: default templates in ca/es/en from the notification catalog, rendered in the applicant's `signup.locale` / each admin's locale).
10. **Catalog constraints (from E3-T02)**: `src/main/resources/parameters/catalog.yaml` — `signup.firstMonthSplitDay` and `billing.nextInvoiceDayOfMonth` get `constraints.min = 1` (a calendar day is 1–28/31, never 0); `documentType`/`documentDefault` unchanged (the doc/yaml contract test only compares those); `ParameterValidator` rejects 0.
11. **Seeds**: `seeds/club-canic.yaml` gets the signup configuration (`signup.*` parameters already in the catalog; `legal.legalTextsVersion`, `privacyPolicyUrl`); `seeds/club-minim.yaml` variants for T-04-26 (modules off).

## Fixed conventions
- **From E3-T01**: the `501` stubs of `POST /signup` and `POST /checkout-sessions` bypass the authenticated idempotency filter through a narrow exception — replace it here with anonymous idempotency scoped by host + `Idempotency-Key` (+ `signupToken` for checkout) **before** enabling the mutations; the exception must not survive this task.
- Statuses and codes from the catalog (rule 0 literal: `SIGNUP_ALREADY_PENDING`, `SIGNUP_CLOSED`, `DOG_CHIP_ALREADY_REGISTERED`, `ID_DOCUMENT_AMBIGUOUS` are 422; `MEMBER_ALREADY_EXISTS`/`MEMBERSHIP_EXISTS` 409); no new parameter/event/notification without a proposal in the report (S04 §13 proposals `auth.welcomeLinkDays`, `signup.text.*` already in the catalog: check before proposing).
- Never log or return IBANs, id documents or emails in clear beyond the masked forms of S04; `signupToken` is an HMAC with the server key, never persisted in clear.
- Fixtures fictional (`@example.test`, `ES00…`), no real Cànic data.

## Verification (run all; evidence = command + exit code + last 40 lines; long outputs → `roadmap/evidence/<ID>/*.log`)
- `./mvnw -q verify` green with T-04-11…T-04-28 (names carry the ids), `E3ContractIT` updated (routes no longer 501), `AuditContractTest` (`@AuditCovers` for the S04 actions already listed in S14 R-14-09: `MEMBER_VALIDATED`, `SIGNUP_REJECTED`, `SIGNUP_EDITED` (+ `DOG_LEVEL_CHANGED` at validation when a level is assigned) — the public `POST /signup` itself is not audited (anonymous; it is an event, `SignupSubmitted`); no new action), OpenAPI snapshot byte-identical; `bin/core club:apply` of both seeds idempotent.

## Definition of Done
Public signup → D2 validation → welcome mail works end to end on the local stack (documented `curl` sequence in the report, with the local mailbox), add-dog path included, all module variants covered by tests.

## Evidence to return
`verify` tail; the `curl` sequence with the fictional applicant (tokens truncated); seed apply tails.

---

## Executor report

### Result

Blocked on the add-dog API contract before implementation. S04 R-04-14 / decision
B13 requires TODAY and ALTERNATIVE (first of next month) billing choices and
`upfront.additionalDog {option, startDate, amountDue}`. The published
`AddDogSignupRequest` has no way to submit the choice, and `SignupUpfront` has no
way to return that period. `UpfrontLines` already implements both choices.
Exposing them requires a contract amendment, conflicting with this task's
Verification requirement, `OpenAPI snapshot byte-identical`.

Defaulting every add-dog request to TODAY would omit the approved entry-only
option and charge an additional monthly fee when the applicant wanted next-month
admission. Accepting an undocumented property would leave the published contract
incomplete. Neither is an acceptable assumption for completing R-04-14.

### Files changed

- `roadmap/tasks/E3-T03.md`: this report and task status.
- `roadmap/STATUS.md`: generated by the roadmap tool.
- `roadmap/MESSAGES.md`: blocking question and concrete contract proposal to @organizer.
- `CHANGELOG.md`: Unreleased entry recording the contract blocker.

No application code, tests, seeds, catalogs, or OpenAPI snapshot changed. No other
task file, Organizer verification section, or ROADMAP was edited. No Git write
command was run.

### Rules and tests implemented

None in this session. Inspected R-04-14 / B13, T-04-06, T-04-21 and the E3-T01
request/response schemas against E3-T02's existing domain implementation.
T-04-11…T-04-28 and the end-to-end flows remain unimplemented in E3-T03.

### Assumptions

- Exactly E3-T03 was selected by `--next`; its initial status was `ready`.
- Dependency E3-T02 was `awaiting_verification` when selected, allowed by the
  roadmap protocol. E3-T01 was verified.
- Preserve B13's two billing choices and the published contract until the
  organizer resolves their mismatch; do not invent a hidden request field or
  silently remove the ALTERNATIVE branch.
- Docker is available. This is a contract decision blocker, not an access or Git
  blocker.

### Questions / catalog proposals

@organizer May E3-T03 amend the published contract and regenerate the snapshot to
implement B13? Proposed amendment: optional
`AddDogSignupRequest.additionalDogOption: TODAY | ALTERNATIVE` (default TODAY),
optional `SignupUpfrontConfig.additionalDogOptions[] {option, startDate, amountDue}`
for the authenticated add-dog mode, and optional
`SignupUpfront.additionalDog {option, startDate, amountDue}` for the selected
period. Reuse the existing option enum and Money/date shapes. Preserve the
configured inclusive cutoff and existing domain rejection after it. Clarify
whether "byte-identical" means deterministic regeneration against the amended
snapshot, and coordinate the extension with the frontend contract. Assumption
on resume: implement both choices with these documented fields after alignment.
No parameter, event, notification or error-code additions are proposed.

### Evidence

Roadmap selection — command: `python3 roadmap/tools/check.py --next`; exit code:
`0`. Complete output (last 1 line):

```text
E3-T03
```

Start — command: `python3 roadmap/tools/check.py --set E3-T03 in_progress`; exit
code: `0`. Complete output (last 2 lines):

```text
E3-T03: status -> in_progress
STATUS.md rendered (46 tasks)
```

Docker availability — command: `docker info --format '{{.ServerVersion}}'`; exit
code: `0`. Complete output (last 1 line):

```text
29.7.2
```

Read-only contract inspection — exact command:

```sh
python3 - <<'PY'
import json
from pathlib import Path
schemas = json.loads(Path('docs/openapi/openapi.json').read_text())['components']['schemas']
for name in ('AddDogSignupRequest', 'SignupUpfront', 'SignupUpfrontConfig'):
    print(f"{name} properties: {', '.join(schemas[name]['properties'])}")
request = schemas['AddDogSignupRequest']['properties']
assert not any(key in request for key in ('firstMonthOption', 'additionalDogOption', 'startDate', 'payment'))
assert 'additionalDog' not in schemas['SignupUpfront']['properties']
print('Confirmed: add-dog request has no billing-date choice; upfront response has no additionalDog period.')
source = Path('src/main/java/com/agilityhub/core/clubs/signup/domain/UpfrontLines.java').read_text()
assert 'Option.ALTERNATIVE' in source and 'today.plusMonths(1).withDayOfMonth(1)' in source
print('Confirmed: domain already supports TODAY and ALTERNATIVE (first of next month).')
spec = Path('docs/specs/S04-alta-publica-i-validacio.md').read_text()
assert 'upfront.additionalDog {option, startDate, amountDue}' in spec
print('S04 R-04-14 requires upfront.additionalDog {option, startDate, amountDue}.')
task = Path('roadmap/tasks/E3-T03.md').read_text()
assert 'OpenAPI snapshot byte-identical' in task
print('E3-T03 Verification requires: OpenAPI snapshot byte-identical.')
print('Contract amendment is needed to expose both required billing choices.')
PY
```

Exit code: `0`. Complete output (last 8 lines):

```text
AddDogSignupRequest properties: consents, documents, dog, planIdRequested
SignupUpfront properties: lines, totalDue
SignupUpfrontConfig properties: firstMonthOptions, firstMonthSplitDay, today
Confirmed: add-dog request has no billing-date choice; upfront response has no additionalDog period.
Confirmed: domain already supports TODAY and ALTERNATIVE (first of next month).
S04 R-04-14 requires upfront.additionalDog {option, startDate, amountDue}.
E3-T03 Verification requires: OpenAPI snapshot byte-identical.
Contract amendment is needed to expose both required billing choices.
```

This inspection establishes the mismatch; it is not implementation verification.
Under the blocked-task stop rule, `./mvnw -q verify`, snapshot generation, both
seed applications/idempotency checks and the local curl/mailbox sequence were
not run. No passing implementation or end-to-end evidence is claimed.

Whitespace check — command: `git diff --check`; exit code: `0`; no output.

Final state — command: `python3 roadmap/tools/check.py --set E3-T03 blocked`;
exit code: `0`. Complete output (last 4 lines):

```text
E3-T03: status -> blocked
STATUS.md rendered (46 tasks)
WARN: E3-T04: ready but dependency E3-T03 is blocked
WARN: E3-T05: ready but dependency E3-T03 is blocked
```

## Organizer verification
_(pending)_
