---
id: E1-T06
title: Onboarding «Completa el teu perfil»: Account.onboardingPending and /me/onboarding
stage: E1
repo: agilityhub-core-api
thread: C
order: 60
status: not_open
depends_on: [E1-T02]
branch: 
pr: 
updated: 2026-09-06
---

# E1-T06 · Onboarding «Completa el teu perfil»: Account.onboardingPending and /me/onboarding

## Objective
The backend of the first-access screen for imported (Learn) and migrated (Playoff) accounts, decided by Jordi on 2026-09-05.

## Spec references
S01 §14 (onboarding block), R-01-14 · `docs/specs/S18-migracio-playoff.md` (migrated members) · `docs/DECISIONS_PENDENTS.md` A4, A12 · `docs/specs/00-transversal/CATALEG_PARAMETRES.md` (`signup.onboardingFields`, `club.privacyPolicyUrl`, legal version) · `docs/legal/POLITICA_PRIVACITAT_AGILITYHUB_ID.md` (link only).

## Context to load
S01 §14 · S18 §3 (migrated member fields) · DECISIONS A4/A12 · CATALEG_PARAMETRES · this file.

## Steps
1. `Account.onboardingPending` (set by the Learn import and the Playoff migration; `false` for accounts created by signup), `Account.consents[] {policy: PLATFORM|CLUB, version, acceptedAt}`.
2. `GET /me/onboarding` → `{pending, requiredConsent {policy, version, url}, fields[]}` (fields from `signup.onboardingFields`, values prefilled from Account/Member), `PUT /me/onboarding {consentAccepted: true, fields{…}}` → validates the consent (mandatory), stores optional fields (name, locale, phone → `Member` when there is one), sets `onboardingPending = false`, audit `ONBOARDING_COMPLETED`.
3. Policy-version change (A12): when the platform or club policy version increases, `GET /me/onboarding` reports `pending` again with `postponeRemaining` (parameter `legal.maxPostpones = 3` — propose it in the catalog if absent); `POST /me/onboarding/postpone` decrements; at 0 the front blocks until acceptance (the API only reports the state).
4. `GET /me` includes `onboardingPending`.

## Verification (run all; paste full output in the report)
- `./mvnw -q verify` → green with `OnboardingIT` (imported account → pending; accept → not pending; policy version bump → pending again; postpone 3 times then required).

## Definition of Done
Endpoints + consent storage + version bump logic tested.

## Evidence to return
`verify` output; example JSON of `GET /me/onboarding`.

---

## Executor report
_(not started)_

### Branch / commits
### Evidence (full command outputs)
### Files changed
### Rules and tests implemented (R-xx-nn / T-xx-nn)
### Assumptions
### Questions / catalog proposals

## Organizer verification
_(pending)_
