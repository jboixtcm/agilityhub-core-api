---
id: E3-T06
title: Hardening INC-01…INC-04 + INC-06: health endpoint independent of tenant/data, 500s logged with traceId, compose healthcheck on the real endpoint, configurable Mongo port, TokenResponse.scope always present
stage: E3
repo: agilityhub-core-api
thread: A
order: 5
status: in_progress
depends_on: [E2-T11]
branch:
pr:
updated: 2026-09-10
---

# E3-T06 · Hardening INC-01…INC-04 + INC-06: health endpoint independent of tenant/data, 500s logged with traceId, compose healthcheck on the real endpoint, configurable Mongo port, TokenResponse.scope always present

## Objective
Five small defects found by Jordi on 09-09 while checking gate E0 on his Mac (`docs/INCIDENCIES_OBERTES.md`). They block the promotion of `main → release` (gate E0) and make any staging incident undebuggable, so they are fixed now instead of in the E10 clean-up round.

## Spec references
`docs/INCIDENCIES_OBERTES.md` INC-01 (`GET /api/v1/health` → `500` with an **empty database**, reproduced with `docker compose down -v && docker compose up -d --build --wait && curl -sS -i localhost:8080/api/v1/health`; also with `Host: app.agilitycanic.cat`), INC-02 (the `500` leaves no trace in the logs), INC-03 (compose healthcheck hits the actuator on 8081, not `/api/v1/health` on 8080 → false «Healthy»), INC-04 (`compose.yaml` publishes `27017` fixed), INC-06 (`TokenResponse.scope` omitted when the granted scope set is empty while the OpenAPI marks it `required`; found by web E1-W04) · S02 R-02-01 (tenant by host; unknown host → `404 UNKNOWN_HOST`, never `500`) · E0-T02 (compose), E0-T04 (error handler), E1-T02/E1-T13 (token response).

## Context to load
`shared/api/HealthController`, `TenantFilter` (public/global route lists), `RequestLocaleFilter` + `RequestLocaleResolver` (they run for every request, including `/api/v1/health`), `ApiExceptionHandler` (E0-T04), `compose.yaml`, `Dockerfile` (actuator port), `identity` token serialisation (`TokenResponse`), `OpenApiSnapshotTest` · this file.

## Steps
1. **INC-01 — find the real cause first**: reproduce with an empty database (`docker compose down -v`, `up --build --wait`, `curl -i /api/v1/health`), capture the exception (after step 2 it will be in the log), and fix it at the source. Expected behaviour: `200 {status: UP, version, builtAt}` whenever the service and Mongo are alive, regardless of `Host` and of the database content (no club yet). `/api/v1/health` (and `/actuator/health`) must not depend on any tenant, locale or club lookup: make the request filters skip it explicitly (`TenantFilter` already lists it as global — check the other filters: locale resolution, rate limit, account state, idempotency) and add an IT that starts the context with an **empty** database and asserts `200` on `/api/v1/health` with and without a `Host` header, plus `404 UNKNOWN_HOST` (not `500`) for a tenant route with an unknown host.
2. **INC-02**: `ApiExceptionHandler` (or a fallback `@ExceptionHandler(Exception.class)`) logs every unhandled exception at `ERROR` with the full stack trace and the correlation `traceId` that the body already carries; expected business errors (4xx) stay at `DEBUG`/`WARN`. Test: an endpoint that throws in a test slice → one `ERROR` log line with the `traceId` of the response (use a log capture appender).
3. **INC-03**: `compose.yaml` (and `docker-compose.consumer.yml`) healthcheck of the `api` service checks `GET http://localhost:8080/api/v1/health` (the actuator check may stay as an additional condition); `docker compose up --wait` must report unhealthy when the endpoint fails. Verify with the reproduction of INC-01 before the fix (unhealthy) and after (healthy).
4. **INC-04**: `ports: "${MONGO_PORT:-27017}:27017"` for the Mongo service in `compose.yaml` (and the consumer file); `README.md` «Run locally» documents `MONGO_PORT=27018 docker compose up` for machines with a local Mongo.
5. **INC-06**: serialise `TokenResponse.scope` always (`""` when the granted set is empty) so the published OpenAPI stays truthful (`required`); IT on the password grant of an account without scopes; snapshot unchanged (or regenerated if the description changes).
6. `docs/INCIDENCIES_OBERTES.md` (synced doc): mark INC-01, 02, 03, 04, 06 as «resolta (E3-T06)» with the commit reference in your report only — the organizer updates the registry itself; do not edit `docs/`.

## Fixed conventions
- No new parameters/events/error codes; `UNKNOWN_HOST` and the existing codes only.
- Keep the fixes minimal and local; no refactor of the filter chain beyond what step 1 needs.

## Verification (run all; evidence = command + exit code + last 40 lines; long outputs → `roadmap/evidence/<ID>/*.log`)
- `./mvnw -q verify` green with the new ITs; the INC-01 reproduction (`docker compose down -v && docker compose up -d --build --wait && curl -sS -i localhost:8080/api/v1/health`) → `200` and `Healthy`; with `MONGO_PORT=27018` the stack starts next to a local Mongo; `docker compose logs api | grep -c ERROR` shows the trace of a forced 500 (e.g. a test-only route or a temporary reproduction, removed before publishing).

## Definition of Done
Gate E0 check «`/api/v1/health` → UP on a fresh stack» passes on Jordi's Mac; 500s are traceable; compose healthcheck is truthful; Mongo port configurable; token contract truthful.

## Evidence to return
`verify` tail, the reproduction before/after (curl + compose status), the log line of the forced 500.

---

## Executor report

### Outcome
Blocked on the missing generic HTTP 500 error contract (closed-catalog rule). No application code, tests, catalog, OpenAPI snapshot or synced documentation changed. The selected dependency E2-T11 was verified at start. No Git write commands were run; a commit reference does not exist yet because the external publish script runs after this session.

### Files changed
- `roadmap/tasks/E3-T06.md`: diagnostic report and blocked state.
- `roadmap/STATUS.md`: generated state only.
- `roadmap/MESSAGES.md`: catalog proposal/blocker and listener diagnosis for @organizer.
- `roadmap/evidence/E3-T06/01-before.log` through `05-cleanup.log`: complete sanitized diagnostic outputs.
- `.gitignore`: admit this task's evidence logs.
- `CHANGELOG.md`: Unreleased diagnostic/blocked entry; no behavior-change claim.

### Rules and tests implemented
- No new R/T implementation or automated tests. Work stopped before code changes under the blocked-task protocol.
- INC-01 baseline: a fresh build with zero documents in every application collection returns `200 {status: UP, version, builtAt}` with its ordinary Host and `Host: app.agilitycanic.cat`. Both containers become healthy.
- R-02-01 baseline: `/api/v1/branding` with `Host: unknown.example.test` returns `404 UNKNOWN_HOST` and a traceId.
- INC-01/02/03 diagnosis: native Java PID 36366 owns IPv6 port 8080; Docker owns IPv4 `127.0.0.1:8080`. `localhost` and `[::1]` reach the Java listener and return the reported ad-hoc 500; IPv4 reaches the Docker Core API and returns UP. No evidence ties that 500 to this repository's running container. The native process was not stopped or modified.
- INC-03 baseline: the running Docker image and source Dockerfile already check the real `/api/v1/health` endpoint. Compose inherits that check. An unhealthy before-state of the actual Core API was not reproduced, so none is claimed.
- INC-04 baseline: `compose.yaml` already exposes `MONGODB_PORT` as a configurable loopback host port. The disposable stack starts at 27018 alongside the original Mongo at 27017. The requested `MONGO_PORT` spelling/consumer change remains pending.
- INC-06 source diagnosis: the runtime response comes from Spring Authorization Server's `OAuth2AccessTokenResponseAuthenticationSuccessHandler` in `IdentityConfiguration`, rather than the `IdentityResponses.TokenResponse` documentation record. The shared success handler needs the empty-scope fix on resume.

### Assumptions
- Use an isolated `COMPOSE_PROJECT_NAME=e3-t06`, API port 18080 and Mongo port 27018 to reproduce empty storage without deleting the existing stack's data. The isolated project's named volumes were created from scratch and removed afterwards. The original API/Mongo services remain running.
- Use `BUILDX_CONFIG=/private/tmp/e3-t06-buildx` after the default Docker builder activity file was denied by the sandbox. This retry passed; Docker access is not the blocker.
- The reproducible local 500 belongs to the IPv6 listener. Use `127.0.0.1` or `curl -4` for the Mac Docker gate. No speculative locale/filter fix was made before a Core API failure was identified.
- No incident is marked resolved. Step 6 delegates registry updates to the organizer and prohibits edits under `docs/`; it remains pending with the rest of the task.

### Questions / catalog proposals
@organizer: Step 2 requires an unexpected HTTP 500 carrying a response traceId. `CATALEG_ERRORS.md` has no generic 500 code, while the OpenAPI already assigns the `ApiError` schema to 500. May the catalog approve `INTERNAL_ERROR` (500) and safe ca/es/en messages? Assumption on resume: use that approved code, keep `INVALID_STATE=409` and `NOT_IMPLEMENTED=501`, and log the original stack once with the response traceId. No proposed code was added. A fallback framework/ad-hoc body or reusing a business code at a noncanonical status would violate the repository contract. This unresolved decision is recorded in MESSAGES.md and blocks implementation.

### Evidence
The diagnostic commands below completed before stopping. Evidence normalizes line endings/trailing whitespace and truncates hashes. `./mvnw -q verify`, new ITs, the forced Core API 500/log-count probe, unhealthy-to-healthy regression, `MONGO_PORT=27018` alias test and snapshot verification were **not run** because implementation is blocked. The Docker build ran `./mvnw -B -q -DskipTests package`; that successful package build is not a passing test-suite claim. The localhost:8080 reproduction is captured, but the destructive full-stack reset was scoped to the disposable project.

Earlier attempt: `COMPOSE_PROJECT_NAME=e3-t06 SERVER_PORT=18080 MONGODB_PORT=27018 docker compose down -v && COMPOSE_PROJECT_NAME=e3-t06 SERVER_PORT=18080 MONGODB_PORT=27018 docker compose up -d --build --wait --wait-timeout 160` → exit 1: Docker builder activity-file write denied; retried with writable BUILDX_CONFIG. Full output: `roadmap/evidence/E3-T06/01-before.log`.

Command (exit **0**); complete output: `roadmap/evidence/E3-T06/02-before-build.log`.

```sh
BUILDX_CONFIG=/private/tmp/e3-t06-buildx COMPOSE_PROJECT_NAME=e3-t06 SERVER_PORT=18080 MONGODB_PORT=27018 docker compose up -d --build --wait --wait-timeout 160
```

Last 40 lines (or complete output if shorter):

```text
#22 exporting attestation manifest sha256:225ab2b…[truncated] done
#22 exporting manifest list sha256:6e257d5…[truncated] done
#22 naming to docker.io/library/e3-t06-api:latest done
#22 unpacking to docker.io/library/e3-t06-api:latest
#22 unpacking to docker.io/library/e3-t06-api:latest 0.4s done
#22 DONE 2.0s

#23 resolving provenance for metadata file
#23 DONE 0.0s
 Image e3-t06-api Built
 Volume e3-t06_mongo-data Creating
 Volume e3-t06_attachment-files Creating
 Network e3-t06_default Creating
 Volume e3-t06_export-files Creating
 Volume e3-t06_attachment-files Creating
 Volume e3-t06_export-files Creating
 Network e3-t06_default Creating
 Volume e3-t06_mongo-data Creating
 Volume e3-t06_attachment-files Created
 Volume e3-t06_attachment-files Created
 Volume e3-t06_export-files Created
 Volume e3-t06_export-files Created
 Volume e3-t06_mongo-data Created
 Volume e3-t06_mongo-data Created
 Network e3-t06_default Created
 Network e3-t06_default Created
 Container e3-t06-mongo-1 Creating
 Container e3-t06-mongo-1 Created
 Container e3-t06-api-1 Creating
 Container e3-t06-api-1 Created
 Container e3-t06-mongo-1 Starting
 Container e3-t06-mongo-1 Started
 Container e3-t06-mongo-1 Waiting
 Container e3-t06-mongo-1 Healthy
 Container e3-t06-api-1 Starting
 Container e3-t06-api-1 Started
 Container e3-t06-mongo-1 Waiting
 Container e3-t06-api-1 Waiting
 Container e3-t06-mongo-1 Healthy
 Container e3-t06-api-1 Healthy
```

Command (exit **0**); complete output: `roadmap/evidence/E3-T06/03-listener-diagnosis.log`.

```sh
{ curl -sS -i http://localhost:8080/api/v1/health; curl -sS -i http://127.0.0.1:8080/api/v1/health; curl -g -sS -i 'http://[::1]:8080/api/v1/health'; lsof -nP -iTCP:8080 -sTCP:LISTEN; docker inspect --format '{{json .Config.Healthcheck}} {{.State.Health.Status}}' agilityhub-core-api-api-1; }
```

Last 40 lines (or complete output if shorter):

```text
HTTP/1.1 500
Vary: Origin
Vary: Access-Control-Request-Method
Vary: Access-Control-Request-Headers
Content-Type: application/json
Transfer-Encoding: chunked
Date: Thu, 10 Sep 2026 04:58:06 GMT
Connection: close

{"status":500,"error":"Internal Server Error","message":"internal server error","path":"/api/v1/health","timestamp":"2026-09-10T06:58:06.931301"}HTTP/1.1 200
Vary: Origin
Vary: Access-Control-Request-Method
Vary: Access-Control-Request-Headers
Content-Language: ca
X-Content-Type-Options: nosniff
X-XSS-Protection: 0
Cache-Control: no-cache, no-store, max-age=0, must-revalidate
Pragma: no-cache
Expires: 0
X-Frame-Options: DENY
Content-Security-Policy: default-src 'self'; object-src 'none'; frame-ancestors 'none'; base-uri 'self'; form-action 'self'
Referrer-Policy: strict-origin-when-cross-origin
Content-Type: application/json
Transfer-Encoding: chunked
Date: Thu, 10 Sep 2026 04:58:06 GMT

{"status":"UP","version":"0.1.0-SNAPSHOT","builtAt":"2026-09-09T17:19:05.358Z"}HTTP/1.1 500
Vary: Origin
Vary: Access-Control-Request-Method
Vary: Access-Control-Request-Headers
Content-Type: application/json
Transfer-Encoding: chunked
Date: Thu, 10 Sep 2026 04:58:06 GMT
Connection: close

{"status":500,"error":"Internal Server Error","message":"internal server error","path":"/api/v1/health","timestamp":"2026-09-10T06:58:06.953751"}COMMAND     PID   USER   FD   TYPE             DEVICE SIZE/OFF NODE NAME
java      36366 jordib  128u  IPv6 0x866e72abec93e8cc      0t0  TCP *:8080 (LISTEN)
com.docke 99041 jordib  134u  IPv4 0xaa2157ffa37fcbfc      0t0  TCP 127.0.0.1:8080 (LISTEN)
{"Test":["CMD-SHELL","curl -fsS \"http://localhost:${SERVER_PORT:-8080}/api/v1/health\" || exit 1"],"Interval":5000000000,"Timeout":3000000000,"StartPeriod":40000000000,"Retries":12} healthy
```

Command (exit **0**); complete output: `roadmap/evidence/E3-T06/04-fresh-stack.log`.

```sh
export COMPOSE_PROJECT_NAME=e3-t06 SERVER_PORT=18080 MONGODB_PORT=27018
{ docker compose ps; curl -sS -i localhost:18080/api/v1/health; curl -sS -i localhost:18080/api/v1/health -H 'Host: app.agilitycanic.cat'; curl -sS -i localhost:18080/api/v1/branding -H 'Host: unknown.example.test'; docker compose exec -T mongo mongosh --quiet agilityhub --eval 'const counts = db.getCollectionNames().sort().map(name => ({collection: name, documents: db.getCollection(name).countDocuments({})})); printjson(counts); if (counts.some(item => item.documents !== 0)) { quit(1); }'; }
```

Last 40 lines (or complete output if shorter):

```text
    collection: 'oidc_flows',
    documents: 0
  },
  {
    collection: 'parameters',
    documents: 0
  },
  {
    collection: 'plans',
    documents: 0
  },
  {
    collection: 'prices',
    documents: 0
  },
  {
    collection: 'refresh_tokens',
    documents: 0
  },
  {
    collection: 'rings',
    documents: 0
  },
  {
    collection: 'saved_views',
    documents: 0
  },
  {
    collection: 'security_events',
    documents: 0
  },
  {
    collection: 'sendgrid_webhook_receipts',
    documents: 0
  },
  {
    collection: 'team_write_locks',
    documents: 0
  }
]
```

Command (exit **0**); complete output: `roadmap/evidence/E3-T06/05-cleanup.log`.

```sh
COMPOSE_PROJECT_NAME=e3-t06 SERVER_PORT=18080 MONGODB_PORT=27018 docker compose down -v
```

Last 40 lines (or complete output if shorter):

```text
 Container e3-t06-api-1 Stopping
 Container e3-t06-api-1 Stopped
 Container e3-t06-api-1 Removing
 Container e3-t06-api-1 Removed
 Container e3-t06-mongo-1 Stopping
 Container e3-t06-mongo-1 Stopped
 Container e3-t06-mongo-1 Removing
 Container e3-t06-mongo-1 Removed
 Network e3-t06_default Removing
 Volume e3-t06_attachment-files Removing
 Volume e3-t06_mongo-data Removing
 Volume e3-t06_export-files Removing
 Volume e3-t06_mongo-data Removed
 Volume e3-t06_attachment-files Removed
 Volume e3-t06_export-files Removed
 Network e3-t06_default Removed
```

### Resume checklist
1. Obtain the organizer's generic-500 catalog decision and align the localhost reproduction with Docker's IPv4 listener.
2. Complete the task's minimal filter guards/regressions, centralized traced exception logging, explicit Compose checks, MONGO_PORT alias/documentation and empty-scope serialization.
3. Run every Verification command, replace this interrupted report's pending claims with final evidence, and set awaiting_verification only after all required checks pass.


## Organizer verification
_(pending)_
