---
id: E5-T25
title: Contract gaps of the S08 member flow found by the web's E5-W01 (api) — the booked dog and the in-time deadline on Booking, the waiting dog on WaitlistEntry, the ring colour and the activity id on 03's rows, BookedBy.self, the Idempotency-Key of POST /bookings, BOOKING_NOT_CANCELLABLE's status
stage: E5
repo: agilityhub-core-api
thread: A
order: 250
status: ready
depends_on: []
branch:
pr:
updated: 2026-09-26
---

# E5-T25 · Contract gaps of the S08 member flow (web E5-W01)

## Objective
The web's E5-W01 built the S08 member flow mocks-first (screens 03, 04, 06/29, 07 and the waitlist detail). The published contract lacks some data those screens need, and the web covers the gaps with three `pending.json` overlays (`Booking.dog`, `Booking.lateCancelThresholdMinutes`, `WaitlistEntry.dog`). The web's report (questions 1–4 and 6) and the independent review of E5-W01 (`roadmap/reviews/E5-W01-20260926-1604-claude.md` in the web repo, finding 5) list what is missing. The organizer confirmed and decided them all on 26-09.

When this task is verified, the web's E5-W04 adopts the snapshot and prunes the overlays.

The nits of this task's own review go to `docs/INCIDENCIES_OBERTES.md`. Its minors go there too, unless they block the web.

**Each change comes with a test that fails before it.**

## Spec references
- S08:
  - §2: rows 03, 07 and the waitlist detail;
  - R-08-08 (`Idempotency-Key` is one UUID per body; amended 26-09);
  - R-08-10 (the in-time window, `bookings.lateCancelThresholdMinutes`; `422 BOOKING_NOT_CANCELLABLE`, amended 26-09);
  - §6.
- S07 §2 (03's activity rows carry the activity).
- `CATALEG_ERRORS.md` (`BOOKING_NOT_CANCELLABLE` is 422; amended 26-09).

## Context to load
- The web's E5-W01 report, questions 1–4 and 6, quoted here in the steps. The web's overlays are in its `packages/api-client/openapi/pending.json` (`x-schema-overlays`).
- `BookingsController` (near `:124`, the `Idempotency-Key` description), the `Booking` and `BookedBy` projections, `WaitlistEntry`'s projection, `MemberHomeQuery` (`ReservationRow`), `HoldDog` (already used by `SeatHoldResponse.dog`).

## Steps
1. **The booked dog on `Booking`.**
   - Every `Booking` that a MEMBER reads carries `dog`, a `HoldDog` (`id`, `name`, `sex`), as `SeatHoldResponse.dog` does. That covers `GET /bookings/{id}`, the `POST /bookings` answer and the cancellation answer.
   - Screen 07 writes «Classe {description} · amb la Duna»: the Catalan article depends on the sex.
2. **The in-time deadline on `Booking` (R-08-10).**
   - A MEMBER cannot read `/parameters`. Screen 07 still needs the threshold for its warning and its late note («Falten menys de 4 hores…»).
   - `Booking` therefore carries `lateCancelThresholdMinutes`: the booking's club value of `bookings.lateCancelThresholdMinutes`, 240 at the Cànic.
   - It also carries `cancellableInTimeUntil`: the class start minus that threshold, computed by the api, so the client does no date arithmetic.
   - Both come on every `Booking` that a MEMBER reads, the cancellation answer included: its late note names the threshold.
3. **The waiting dog on `WaitlistEntry`.** `WaitlistEntry` carries `dog` (`HoldDog`). `dogName` alone gives no sex for the Catalan article.
4. **03's rows (`ReservationRow`, `GET /me/home`).**
   - A class row carries `ringColor`, the ring's colour, for the mockup's coloured dot.
   - An `ACTIVITY` row carries `activityId`, as S07 §2 says `/me/home` delivers it.
5. **`BookedBy.self` (07's «Reservada per {name}»).**
   - `BookedBy` gains `self: boolean`: `true` when the reader's own account made the booking.
   - The web must stop deciding by comparing first names: an account name like «L. Serra», or two group members with the same first name, get it wrong.
   - `displayName` and `viaClub` are unchanged.
6. **The `Idempotency-Key` of `POST /bookings` (R-08-08, amended 26-09).**
   - The key is one client UUID per body. A retry of the same body reuses it. A different body, for example another booking chosen for the swap after a failed attempt, gets a new one.
   - The operation's description stops saying «Idempotency-Key = seatHoldId».
   - ITs:
     - the same key with the same body answers the same response, a stored `409` included;
     - a new key with a new body on the same hold is processed;
     - the same key with another body answers `IDEMPOTENCY_KEY_REUSED`.
7. **`BOOKING_NOT_CANCELLABLE` is 422** (`CATALEG_ERRORS.md` and S08 R-08-10, amended 26-09). The code already answers 422. Check that the snapshot publishes 422 on `POST /bookings/{id}/cancellation` and nowhere 409 for it. Contract test.

## Fixed conventions
- Contract changes as listed. Regenerate `docs/openapi/openapi.json`, and add one `docs/openapi/CHANGELOG.md` entry that tells the web which overlays it can prune.
- Heavy runs go through the host lock.
- Fictional data only.

## Verification (run all; evidence = command + exit code + last 40 lines; long outputs → `roadmap/evidence/E5-T25/*.log`)
- `./mvnw -q clean verify`.
- `bin/e5-smoke` (or `bin/e3-smoke` if the E5 smoke does not cover `/me/home` and bookings), on a fresh local stack.
- `bin/openapi-snapshot`, then `git diff --stat docs/openapi/`.

## Definition of Done
- Steps 1–7 are done, and each has a test that fails before the change. For step 7, the test is a contract test pinning 422.

## Evidence to return
- The command outputs.
- A step → test table.

---

## Executor report
_(pending)_

## Organizer verification
_(pending)_
