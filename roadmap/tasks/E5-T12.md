---
id: E5-T12
title: Josep's answers B29–B33 in the Playoff migration (MappingConfig v5, persones.csv, accounts only for ACTIVE) + seed level PENDENT + coverage over progression levels (E29, E32)
stage: E5
repo: agilityhub-core-api
thread: A
order: 120
status: awaiting_verification
depends_on: [E5-T11]
branch:
pr:
updated: 2026-09-24
---

# E5-T12 · Josep's answers B29–B33 in the migration + level PENDENT + coverage over progression levels

## Objective
On 2026-09-24 Josep accepted the five proposals B29–B33 (`docs/DECISIONS_PENDENTS.md`, Part B). The organizer measured case 5 (B33) on the real export:
- 20 email addresses appear in two active records;
- **17** of them are the same person with two dogs: Playoff makes one record per dog, and the second record carries a different NIF;
- **3** are families.

This task does three things:
- it applies the answers to the migration (S18 `MappingConfig` v5, the new optional auxiliary file `persones.csv`, accounts only for `ACTIVE` members);
- it adds the level «Pendent» to the Cànic seed;
- it finishes ruling E29: levels outside the progression do not appear in the D3 coverage table.

## Spec references
- S18: §3 `MappingConfig` v5, R-18-04 (f) (new), R-18-12 (amended), T-18-06 (amended), changelog of 24-09.
- S05 §3 `Level.progression` and the §12 seed (new row `PENDENT`).
- S06 R-06-06 (amended 24-09: progression levels only).
- `docs/DECISIONS_PENDENTS.md`: Part B (B29–B33), E29, E32.
- `docs/MAPATGE_CAMPS_PLAYOFF.md` §6.

## Context to load
- This file.
- `docs/specs/S18-migracio-playoff.md`: §3, and R-18-04 to R-18-12.
- `src/main/resources/migration/playoff-v1.yaml`, `src/main/java/com/agilityhub/core/migration/**` (the E2-T10 importer) and its tests and fixtures.
- `seeds/club-canic.yaml`.
- The coverage query of S06 (R-06-06) and its tests.

## Steps
1. **Mapping (B29–B32).** Update `playoff-v1.yaml`, or move to a `playoff-v2.yaml` and say which in the report:
   - `"familiar abonat/curs"` → `ABONAT_FAMILIAR`, with the family-group behaviour of `"familiar abonat"` (B31).
   - `"quota reduïda"` stays unresolved, with the `PLAN_UNMAPPED` warning (B30, confirmed).
   - `pendent` → level `PENDENT`, with the `LEVEL_PENDING` warning; remove it from `unresolvedLevels` (B32).
   - `Foto` → `Dog.photoFileKey` (B29, confirmed).
   - Every target code of `plans` and `levels` must exist in the Cànic seed. `cadells: CADELLS` does not match the seed code `CAD`: fix it, and add a test that fails when a mapped code is missing from the seed.
2. **Seed.** Add the level `PENDENT` to `seeds/club-canic.yaml`, following the S05 §12 row:
   - name ca «Pendent», es «Pendiente», en «Pending»;
   - order 90, color `#9AA0A6`, capacity 5;
   - `grantsFreeTraining = false`, `progression = false`;
   - it is in no template class.

   Check which tests assert the derived capacity row of D11 or the list of levels, and update them. If the web real-core e2e must change, say so in `roadmap/MESSAGES.md`.
3. **Coverage (R-06-06, E29).** The coverage table lists only the active **progression** levels, so Teràpia and Pendent do not appear. Update the tests.
4. **The same person across several records (R-18-04 (f), E32).** Add the optional auxiliary file `persones.csv`, with the columns `ID associat principal`, `ID associat unida` and `NIF`.
   - The joined record adds only its dog to the principal record's person.
   - The person keeps the NIF given in the file.
   - The report gets the warning `PERSON_MERGED`.
   - Without the file, each NIF is one person, as today.
   - The anonymizer handles the file with the same HMAC ids as the members file.
5. **Accounts (R-18-12, E32).**
   - Only `ACTIVE` members get an `Account`. Migrated `LEFT` members keep their email as a contact email only.
   - Among `ACTIVE` persons who share an email, the account goes to the record with the oldest `Data alta` (tie: the lowest member number).
   - The other person gets `EMAIL_SHARED`, and the report proposes a family group with the account holder.
6. **Tests (T-18-06).** Extend the synthetic export with four cases:
   - a same-person pair, confirmed through `persones.csv`;
   - an unconfirmed same-person pair;
   - a family pair;
   - an ACTIVE/LEFT pair that share an email.

   Assert the persons, dogs, accounts, warnings and report lines. Never use the real export: fictional data only (`@example.test`).

## Fixed conventions
- The AGENTS.md rules apply.
- No new parameters, events, notifications or error codes. `PERSON_MERGED` is a migration report warning, like `EMAIL_SHARED`.
- Personal data never enters the repository.

## Verification (run all; evidence = command + exit code + last 40 lines; long outputs → `roadmap/evidence/E5-T12/*.log`)
- `./mvnw -q clean verify` → exit 0, plus a committed test summary.
- `bin/core club:apply seeds/club-canic.yaml` twice on a fresh stack: the second run makes 0 changes, and `GET /levels` lists `PENDENT` with `progression = false`.
- The migration dry run on the anonymized fixtures, with its report summary: counts of `EMAIL_SHARED` and `PERSON_MERGED`.

## Definition of Done
- The mapping, seed and coverage changes are in place.
- `persones.csv` and the accounts-only-for-`ACTIVE` rule are implemented and tested.
- `clean verify` is green.

## Evidence to return
The report sections below, and the step → test table.

---

## Executor report
Executor: claude:claude-opus-5-5 · 2026-09-24 · one session. Dependency E5-T11 is `verified`.

### Branch / commits
None: git writes are blocked in this sandbox. The publish script commits the working tree on `main` after the session.

**Not mine.** During this session another process changed files in the working tree: `docs/DECISIONS_PENDENTS.md`, `docs/INCIDENCIES_OBERTES.md`, `docs/MODEL_DADES_PLATAFORMA.md`, `docs/specs/S04…`, `S05…`, `S09…`, `S15…`, `CATALEG_ESDEVENIMENTS.md`, `roadmap/tasks/E5-T09/T10/T11.md`, `E8-T04.md`, the new `E5-T13.md`/`E5-T14.md` and three `roadmap/reviews/*`. I did not edit them. They will be in the same publish commit.

### Evidence (full command outputs)
Logs are in `roadmap/evidence/E5-T12/`. I scanned them for `eyJ`, `Bearer ` and `$argon2`. The only match is the header literal in the helper's source (`stack-checks.py`), not in any log. The helper keeps its generated seed password, OIDC master key, anonymizer key and access token private, and sanitises every log.

Earlier attempts (one line each):
- Targeted IT runs during development (`-Dit.test=PlayoffMigrationIT,DemoSeedsIT,DashboardIT,…`, `-Djacoco.skip=true`). The Cànic club's locales are `ca`/`es`, so `CatalogService` rejected the `en` name «Pending» with `VALIDATION_ERROR`, and I removed it (see Assumptions). The seed counts also needed updating: 31 → 32 catalog changes, 70 → 71 domain events, and one more level in `DemoSeedsIT`/`DashboardIT`. These scratch logs were not kept.
- `./mvnw -q clean verify` attempt 1 → **exit 1**. 509 unit tests passed; 814 ITs ran with 1 failure: `ClubDefinitionsIT.T_02_12:88` expected 35 audit entries but got 36, because the PENDENT create is also audited. I changed the expectation to 36. Log: `01a-clean-verify-attempt1-failed.log`.

**1. `./mvnw -q clean verify`** → **exit 0** (`EXIT_0`). The complete log is `01-clean-verify.log` (5,583 lines; its last line records the exit). The test summary is `02-test-summary.log`, read from `target/surefire-reports` and `target/failsafe-reports`:
- surefire: **509** tests, 0 failures, 0 errors, 0 skipped;
- failsafe: **814** tests, 0 failures, 0 errors, 0 skipped;
- per class: `PlayoffAdapterTest` 6/0/0/0, `CoverageQueryTest` 2/0/0/0, `PlayoffMigrationIT` 12/0/0/0, `DemoSeedsIT` 9/0/0/0, `DashboardIT` 9/0/0/0, `ClubDefinitionsIT` 6/0/0/0.

The JaCoCo coverage check (`haltOnFailure`) passed as part of this verify. Last 40 lines of the log (each cut at 200 characters, as in E5-T11):
```text
2026-09-24T14:56:49.035+02:00  INFO 81352 --- [agilityhub-core-api] [           main] c.a.core.migration.PlayoffMigrationIT    : The following 1 profile is active: "test"
2026-09-24T14:56:49.468+02:00  INFO 81352 --- [agilityhub-core-api] [           main] .s.d.r.c.RepositoryConfigurationDelegate : Bootstrapping Spring Data MongoDB repositories in DEFAULT mode.
2026-09-24T14:56:49.503+02:00  INFO 81352 --- [agilityhub-core-api] [           main] .s.d.r.c.RepositoryConfigurationDelegate : Finished Spring Data repository scanning in 33 ms. Found 0 MongoDB repo
2026-09-24T14:56:49.576+02:00  INFO 81352 --- [agilityhub-core-api] [           main] org.mongodb.driver.client                : MongoClient with metadata {"driver": {"name": "mongo-java-driver|sync|s
2026-09-24T14:56:49.581+02:00  INFO 81352 --- [agilityhub-core-api] [localhost:51788] org.mongodb.driver.cluster               : Monitor thread successfully connected to server with description Server
2026-09-24T14:56:50.572+02:00  INFO 81352 --- [agilityhub-core-api] [           main] c.a.core.migration.PlayoffMigrationIT    : Started PlayoffMigrationIT in 1.548 seconds (process running for 385.00
2026-09-24T14:56:50.603+02:00  INFO 81352 --- [agilityhub-core-api] [           main] o.s.b.w.e.tomcat.GracefulShutdown        : Commencing graceful shutdown. Waiting for active requests to complete
2026-09-24T14:56:50.605+02:00  INFO 81352 --- [agilityhub-core-api] [tomcat-shutdown] o.s.b.w.e.tomcat.GracefulShutdown        : Graceful shutdown complete
2026-09-24T14:56:50.605+02:00  INFO 81352 --- [agilityhub-core-api] [           main] o.s.b.w.e.tomcat.GracefulShutdown        : Commencing graceful shutdown. Waiting for active requests to complete
2026-09-24T14:56:50.606+02:00  INFO 81352 --- [agilityhub-core-api] [tomcat-shutdown] o.s.b.w.e.tomcat.GracefulShutdown        : Graceful shutdown complete
2026-09-24T14:56:50.607+02:00  INFO 81352 --- [agilityhub-core-api] [           main] o.apache.catalina.core.StandardService   : Stopping service [Tomcat]
2026-09-24T14:56:57.000+02:00  INFO 81352 --- [agilityhub-core-api] [localhost:52913] org.mongodb.driver.cluster               : Exception in monitor thread while connecting to server localhost:52913

com.mongodb.MongoSocketOpenException: Exception opening socket
	at com.mongodb.internal.connection.SocketStream.lambda$open$0(SocketStream.java:85) ~[mongodb-driver-core-5.5.2.jar:na]
	at java.base/java.util.Optional.orElseThrow(Optional.java:403) ~[na:na]
	at com.mongodb.internal.connection.SocketStream.open(SocketStream.java:85) ~[mongodb-driver-core-5.5.2.jar:na]
	at com.mongodb.internal.connection.InternalStreamConnection.open(InternalStreamConnection.java:233) ~[mongodb-driver-core-5.5.2.jar:na]
	at com.mongodb.internal.connection.DefaultServerMonitor$ServerMonitor.lookupServerDescription(DefaultServerMonitor.java:219) ~[mongodb-driver-core-5.5.2.jar:na]
	at com.mongodb.internal.connection.DefaultServerMonitor$ServerMonitor.run(DefaultServerMonitor.java:176) ~[mongodb-driver-core-5.5.2.jar:na]
Caused by: java.net.ConnectException: Connection refused
	at java.base/sun.nio.ch.Net.pollConnect(Native Method) ~[na:na]
	at java.base/sun.nio.ch.Net.pollConnectNow(Net.java:694) ~[na:na]
	at java.base/sun.nio.ch.NioSocketImpl.timedFinishConnect(NioSocketImpl.java:542) ~[na:na]
	at java.base/sun.nio.ch.NioSocketImpl.connect(NioSocketImpl.java:592) ~[na:na]
	at java.base/java.net.SocksSocketImpl.connect(SocksSocketImpl.java:327) ~[na:na]
	at java.base/java.net.Socket.connect(Socket.java:751) ~[na:na]
	at com.mongodb.internal.connection.SocketStreamHelper.initialize(SocketStreamHelper.java:76) ~[mongodb-driver-core-5.5.2.jar:na]
	at com.mongodb.internal.connection.SocketStream.initializeSocket(SocketStream.java:104) ~[mongodb-driver-core-5.5.2.jar:na]
	at com.mongodb.internal.connection.SocketStream.open(SocketStream.java:79) ~[mongodb-driver-core-5.5.2.jar:na]
	... 3 common frames omitted

E5-T07 outbox backlog discarded before PlayoffMigrationIT: 158 PENDING records (more than one dispatch() batch)
E5-T07 outbox backlog discarded before PlayoffMigrationIT: 162 PENDING records (more than one dispatch() batch)
E5-T07 outbox backlog discarded before PlayoffMigrationIT: 158 PENDING records (more than one dispatch() batch)
E5-T07 outbox backlog discarded before PlayoffMigrationIT: 158 PENDING records (more than one dispatch() batch)
E5-T07 outbox backlog discarded before PlayoffMigrationIT: 158 PENDING records (more than one dispatch() batch)
E5-T07 outbox backlog discarded before PlayoffMigrationIT: 158 PENDING records (more than one dispatch() batch)
E5-T07 outbox backlog discarded before PlayoffMigrationIT: 158 PENDING records (more than one dispatch() batch)
E5-T07 outbox backlog discarded before PlayoffMigrationIT: 158 PENDING records (more than one dispatch() batch)
exit=0 (the command ran as ./mvnw -q clean verify > 01-clean-verify.log 2>&1 && echo EXIT_0 || echo EXIT_NONZERO and printed EXIT_0)
```
The `MongoSocketOpenException` is Testcontainers shutdown noise from a stopped container's monitor thread, the same as in E5-T11. The «outbox backlog discarded» lines are the E5-T07 test-support diagnostic: each migration apply writes about 160 `AccountCreated` events.

**2 and 3. `club:apply` twice on a fresh stack + `GET /levels`, and the migration dry run on the anonymized fixtures** → `python3 roadmap/evidence/E5-T12/stack-checks.py > roadmap/evidence/E5-T12/09-stack-checks-run.log 2>&1` → **exit 0** (`HELPER_EXIT_0`). The helper is adapted from E5-T11's `club-apply.py`. On a disposable `mongo:7` replica set with an empty database `e5_t12` and the local profile, it:
- runs `bin/core club:apply seeds/club-canic.yaml` twice on the jar that the verify built, and compares the complete Mongo snapshot around the second run;
- starts the jar as an API, logs in as the seeded admin and calls `GET /api/v1/levels` (`Host: app.agilitycanic.cat`);
- runs `bin/core migration:playoff src/test/resources/fixtures/playoff --dry-run --club=canic` and checks that Mongo is unchanged;
- runs `bin/core migration:anonymize` on those fixtures with a one-use key and dry-runs the derivative. The counts must be identical, so the `persones.csv` join survives the anonymizer.

Per-command logs: `03-club-apply-first.log`, `04-club-apply-second.log`, `05-get-levels.json` (full body), `06-migration-dry-run-fixtures.log` and `08-migration-dry-run-anonymized.log` (complete reports, one line per row), and `07-migration-anonymize.log`. Complete transcript (55 lines; the last 40 are shown, and the 15 before them are the club:apply lines and the first 5 stored levels):
```text
  D order=40 capacity=5 progression=true
  E order=50 capacity=4 progression=true
  F order=60 capacity=4 progression=true
  G order=70 capacity=4 progression=true
  TER order=80 capacity=1 progression=false
  PENDENT order=90 capacity=5 progression=false
curl GET /api/v1/levels (Host app.agilitycanic.cat, seeded ADMIN) -> 200
  CAD      order=0 capacity=5 color=#F5D67A progression=true name="Cadells"
  A        order=10 capacity=5 color=#C9E4F5 progression=true name="A"
  B        order=20 capacity=5 color=#A9D3F0 progression=true name="B"
  C        order=30 capacity=5 color=#85B8E8 progression=true name="C"
  D        order=40 capacity=5 color=#8FCE8F progression=true name="D"
  E        order=50 capacity=4 color=#F2B58C progression=true name="E"
  F        order=60 capacity=4 color=#E8A070 progression=true name="F"
  G        order=70 capacity=4 color=#E26A2A progression=true name="G"
  TER      order=80 capacity=1 color=#C9CDD3 progression=false name="Teràpia"
  PENDENT  order=90 capacity=5 color=#9AA0A6 progression=false name="Pendent"
PASS GET /levels: PENDENT and TER progression=false, the other 8 levels true (full body 05-get-levels.json)
$ bin/core migration:playoff src/test/resources/fixtures/playoff --dry-run --club=canic -> exit 0 (log 06-migration-dry-run-fixtures.log)
Playoff DRY_RUN
members: created=187 updated=0 skipped=4 errors=0
dogs: created=189 updated=0 skipped=0 errors=0
familyGroups: created=1 updated=0 skipped=0 errors=0 proposed=18
accounts: created=156 updated=0 skipped=31 errors=0
Incidents: {AGE_SUSPECT=2, BIRTHDATE_SUSPECT=1, CARD_NOT_MIGRATED=1, CHIP_MISSING=16, DOG_INFERRED=9, DOG_NAME_FROM_SURNAME=1, EMAIL_SHARED=19, IBAN_INVALID=1, INVALID_PHONE=9, LEVEL_PENDING=8, MAPPING_INVALID=2, NO_BANK_ACCOUNT=26, NUMBER_CONFLICT=4, PERSON_MERGED=1, PLAN_UNMAPPED=13, VALIDATION_ERROR=10}
Dry run summary (fixtures): EMAIL_SHARED=19 PERSON_MERGED=1 LEVEL_PENDING=8 PLAN_UNMAPPED=13 MAPPING_INVALID=2
PASS dry run on the fixtures: no Mongo write, EMAIL_SHARED=19, PERSON_MERGED=1, familyGroups proposed=18
$ bin/core migration:anonymize src/test/resources/fixtures/playoff /var/folders/xn/c1yrcx212h18679n6kwf4kxh0000gn/T/e5-t12-cli-qof_zjty/anonymized -> exit 0 (log 07-migration-anonymize.log)
Anonymized Playoff files written; no input values or key logged.
Anonymized files: equip.csv, family_groups.csv, nivell.csv, persones.csv, socis.csv, tipologia.csv
$ bin/core migration:playoff /var/folders/xn/c1yrcx212h18679n6kwf4kxh0000gn/T/e5-t12-cli-qof_zjty/anonymized --dry-run --club=canic -> exit 0 (log 08-migration-dry-run-anonymized.log)
Playoff DRY_RUN
members: created=187 updated=0 skipped=4 errors=0
dogs: created=189 updated=0 skipped=0 errors=0
familyGroups: created=1 updated=0 skipped=0 errors=0 proposed=18
accounts: created=156 updated=0 skipped=31 errors=0
Incidents: {AGE_SUSPECT=2, BIRTHDATE_SUSPECT=1, CARD_NOT_MIGRATED=1, CHIP_MISSING=16, DOG_INFERRED=9, DOG_NAME_FROM_SURNAME=1, EMAIL_SHARED=19, IBAN_INVALID=1, INVALID_PHONE=9, LEVEL_PENDING=8, MAPPING_INVALID=2, NO_BANK_ACCOUNT=26, NUMBER_CONFLICT=4, PERSON_MERGED=1, PLAN_UNMAPPED=13, VALIDATION_ERROR=10}
Dry run summary (anonymized): EMAIL_SHARED=19 PERSON_MERGED=1
PASS dry run on the anonymized derivative: same entity counts and incident counts (the persones.csv join survived)
HELPER_EXIT_0
```
The first 15 lines: `+ catalogs: 32 catalog changes`, `+ pages: 3 page changes`, `15 changes (applied)`, then `= catalogs: 0 catalog changes`, `= pages: 0 page changes`, `0 changes (applied)` and `PASS second club:apply: 0 changes and complete Mongo document snapshot unchanged`.

**Report summary asked by the task.** `EMAIL_SHARED` = **19**: the 20 shared pairs of the fixture, minus the one joined through `persones.csv`. `PERSON_MERGED` = **1**. Family groups proposed = **18**: the 19, minus the pair 61/81 that `family_groups.csv` already groups. Accounts skipped = 31: 7 without email, 19 `EMAIL_SHARED` and 5 migrated LEFT. `LEVEL_PENDING` = 8: 1 is the `pendent` → PENDENT dog, and the other 7 are records without a usable level row, as before this task. `MAPPING_INVALID` = 2: the two family-plan records without a `family_groups.csv` row (`familiar abonat` and now `familiar abonat/curs`). The photo no longer adds one.

**Not run.** I did not run `bin/openapi-snapshot`: no endpoint, schema or response changed. `CoverageQuery` returns fewer rows with the same shape, and the seed does not enter the OpenAPI. I did not run the web e2e (other repository); see MESSAGES.

### Step → file → test
| Step | Main change | Tests (all PASS in `02-test-summary.log`) |
|---|---|---|
| 1 Mapping B29–B32 | **Moved** to `src/main/resources/migration/playoff-v2.yaml` (`version: 2`, the adapter schema version; v1 deleted). Mapping changes: `"familiar abonat/curs": ABONAT_FAMILIAR` + `familyPlans` (B31); `"quota reduïda"` stays unresolved (B30); `pendent: PENDENT` + the new `levelWarnings: {pendent: LEVEL_PENDING}`, and `unresolvedLevels: []` (B32); `cadells: CAD`; `photoOwner: DOG` (B29). `MappingConfig` gets `levelWarnings`, `photoOwner` and `VERSION`/`DEFAULT`, and validates them: warnings only on mapped levels, only `LEVEL_PENDING`, owner `DOG`, and the six file keys. `PlayoffPlanner.linkLevel` warns and assigns. The photo goes to `Dog.sourceIds.playoffPhoto`, and the `MAPPING_INVALID` photo warning is gone. | `PlayoffAdapterTest.T_18_01_everyMappedPlanAndLevelCodeExistsInTheCanicSeed`: it reads `seeds/club-canic.yaml` and requires every mapped plan and level code to be in the seed. A copy with `cadells: CADELLS` and `instructors: INSTRUCTOR_FREE` must report exactly those two, and four invalid v2 configs must throw. `PlayoffMigrationIT.T_18_01_mappingV2AppliesJoseAnswersB29ToB32`: row 32 → `ABONAT_FAMILIAR` + `MAPPING_INVALID` (no group), with no `PLAN_UNMAPPED`; row 31 → `PLAN_UNMAPPED`; row 33 dog → `level-PENDENT` + `LEVEL_PENDING`; row 48 dog → `level-CAD`; row 34 dog → `sourceIds.playoffPhoto` and `photoFileKey` null, with no warning; the run has `mappingVersion` 2. |
| 2 Seed PENDENT | `seeds/club-canic.yaml` and `seeds/club-canic-consumer.yaml` (the consumer seed must equal the canonical one): `PENDENT` «Pendent»/«Pendiente», order 90, `#9AA0A6`, capacity 5, `grantsFreeTraining false`, `progression false`. It is in no template class (`demo-canic.yaml` lists level codes explicitly). | `DemoSeedsIT.T_05_21_catalogSeedAppliesExportsUpdatesAndRepeatsWithoutWrites`: 10 levels (API and service), TER + PENDENT outside the progression, the PENDENT fields, and 32 catalog changes. `DemoSeedsIT.T_05_21_E29_aStackSeeded…`: 2 catalog changes, two `LevelChanged {progression}`. `DashboardIT.T_14_11_…`: D1 `dogsByLevel` has 10 rows, because D1 lists every active level and E29 only affects D3. `ClubDefinitionsIT.T_02_12_…`: 71 events and 36 audit entries. Verification 2 on a real stack. |
| 3 Coverage E29 | `CoverageQuery.get` uses `catalog.activeProgression()` for the rows and the zero-count fill. | `CoverageQueryTest.T_06_05_E29_coverageListsOnlyTheActiveProgressionLevels`: a catalog of PENDENT, TER, G, an inactive F and CAD, with template classes {TER} and {G, TER}. Rows are exactly `[CAD, G]`; G keeps its 4 seats and its 2 dogs. Still green: `PlanningIT.T_06_18_…`, `CalendarIT.T_06_25_…`. |
| 4 `persones.csv` (R-18-04 (f)) | `playoff-v2.yaml` file `persons: persones.csv` (optional; `principalId`/`joinedId` anonymized as `id`, `NIF` as `document`, the same HMAC as `socis.csv`). `PlayoffPlanner.persons()` validates each row: known ids, not self, non-empty NIF, no chain, no second joining, one NIF per principal; otherwise `persons … ERROR INPUT_SCHEMA_MISMATCH`. Joined records are planned last. The principal takes the NIF of the file. A joined record is added as a dog + alias of the principal's person (`members SKIPPED` + `PERSON_MERGED`), or `ERROR` if the principal is not migrated. | `PlayoffMigrationIT.T_18_06_E32_samePersonFamilyAndActiveLeftPairsGetTheRightPersonsDogsAndAccounts` (1): 62/82 → one member, NIF `45128376F` from the file, aliases `[62, 82]`, 2 dogs, the account on the principal, `PERSON_MERGED` and no `EMAIL_SHARED`/account line on 82. Without the file, the same plan has 188 members and no `PERSON_MERGED`. `T_18_06_E32_invalidPersonsRowsAreErrorsAndNeverGuessAPerson`: six invalid shapes give one error each, and a principal that is not migrated (old leaver 190) → member `ERROR` on 83 and no member with its alias. `PlayoffAdapterTest.T_18_03_anonymizer…`: the persons ids and NIF equal `replace("id"/"document", …)` of the originals, so they match the members file. |
| 5 Accounts (R-18-12) | `PlayoffPlanner`: only `ACTIVE` gets an `Identity`; a LEFT record logs `accounts SKIPPED` and keeps `contactEmails`. Order: ACTIVE, the oldest `Data alta`, then the **lowest member number** (it was the row). An `EMAIL_SHARED` record is remembered with the owner's row. After `groups()`, `proposals()` adds `familyGroups PROPOSED EMAIL_SHARED field=holder@<row>` unless both are already in the same group. `MigrationReport.render` prints `proposed=` on the familyGroups line and keeps `PROPOSED` rows out of the incident counts. | `T_18_06_sharedEmailsHaveOneOwnerAndNoMailIsSent`: 19 `EMAIL_SHARED` warnings, 31 accounts skipped, 18 proposals, the rendered summary, no personal data, and 0 mails. `T_18_06_E32_…` (2)(3): 63/83 and 64/84 → two persons, the older owns the account, and the other gets `EMAIL_SHARED`, `accounts SKIPPED` and `holder@64`/`holder@65`. Tie 67/87 → 87 (lower number) owns, and 67 gets `holder@88`. (4) ACTIVE 55 / LEFT 186 (joined earlier): the account goes to 55; 186 has no account, no membership and no `EMAIL_SHARED`, and keeps the email as contact. The five migrated LEFT records have no account. |
| 6 Fixture cases (T-18-06) | `src/test/resources/fixtures/playoff/persones.csv` (new, fictional). `socis.csv`: 2 cells changed. Record 186 takes record 55's email and `Data alta` 2009-01-01; record 87 takes record 67's `Data alta`. Pairs 63/83 and 64/84 (already in the fixture) play the unconfirmed same person and the family. | See steps 4 and 5; T-18-01/02/04/08 counts updated (187 members created/updated). |

### Files changed
- **Migration:**
  - `src/main/resources/migration/playoff-v2.yaml` (new); `playoff-v1.yaml` deleted
  - `migration/domain/MappingConfig.java`, `migration/domain/MigrationReport.java`
  - `migration/application/PlayoffPlanner.java`
- **Scheduling:** `clubs/scheduling/application/CoverageQuery.java`
- **Seeds:** `seeds/club-canic.yaml`, `seeds/club-canic-consumer.yaml`
- **Tests:**
  - `migration/PlayoffAdapterTest.java`, `migration/PlayoffMigrationIT.java`
  - `clubs/scheduling/application/CoverageQueryTest.java`
  - `clubs/census/application/DemoSeedsIT.java`, `clubs/dashboard/api/DashboardIT.java`
  - `platform/application/definition/ClubDefinitionsIT.java`
- **Fixtures:** `src/test/resources/fixtures/playoff/persones.csv` (new), `socis.csv`
- **Docs:** `docs/migration/playoff-input.md` (v2, the persons file, accounts, photo, the fixture cases), `CHANGELOG.md`
- **Roadmap:** `roadmap/evidence/E5-T12/*`, this file, `roadmap/MESSAGES.md`, `roadmap/STATUS.md` (rendered)

### Rules and tests implemented (R-xx-nn / T-xx-nn)
- R-18-04 (f) → T-18-06 (`T_18_06_E32_*`, `T_18_03_anonymizer…` for the joins).
- R-18-12 (amended) → T-18-06 (`T_18_06_sharedEmails…`, `T_18_06_E32_samePerson…`).
- S18 §3 `MappingConfig` v5 (B29–B32) → T-18-01 (`T_18_01_mappingV2…`, `T_18_01_everyMappedPlanAndLevelCodeExistsInTheCanicSeed`).
- S05 §3/§12 seed row `PENDENT` → T-05-21 (`DemoSeedsIT`).
- S06 R-06-06 (E29, amended 24-09) → T-06-05 (`T_06_05_E29_coverageListsOnlyTheActiveProgressionLevels`).
- No new parameter, event, notification or error code. `PERSON_MERGED` is a report warning, like `EMAIL_SHARED`. `PROPOSED` is a report outcome, not a code.

### Assumptions
1. **Mapping file.** I moved it to `playoff-v2.yaml` / `version: 2` because the schema changed: new keys and a new file. Following the E2-T10 ruling, the adapter version is independent of S18's document revision (v5).
2. **INSTRUCTOR_FREE and COMPETICIO_1 are not in the S05 seed.** The new test requires every target to exist in the seed. So `instructors` and `competició 1 gos` now go to `unresolvedPlans` (`PLAN_UNMAPPED`), and instructors keep the INSTRUCTOR role. Before, they gave `LEGACY_PLAN` against the real seed. S18 §3 still maps them; see Questions (1).
3. **Photo (B29).** The export's value is a URL on Playoff's storage that is downloaded «al tall», and this command downloads nothing (census stage). So the reference is kept on the dog as `sourceIds.playoffPhoto` for the cutover step that fills `photoFileKey`; `photoFileKey` stays untouched, so a re-apply never clobbers an uploaded photo. On anonymized data the value is `[redacted]`. See Questions (2).
4. **PENDENT name in `en`.** The task asked for en «Pending». S05 §12 has only `name.ca`/`name.es`, and the Cànic's `locales` are `["ca","es"]`: `CatalogService` rejects an `en` name with `VALIDATION_ERROR` (seen in the targeted run). The seed follows the model and S05: ca + es only, like every other Cànic level.
5. **Family group proposal.** It is a report line `familyGroups PROPOSED EMAIL_SHARED field=holder@<row>`, using row ordinals only (no personal data, R-18-15), plus `proposed=N` on the familyGroups summary line. It is never applied. It is not emitted when both records are already in the same `family_groups.csv` group, and not when the account owner is outside this import (an existing account of another member).
6. **Joined record details.** A joined record contributes only its dog and its source id (`externalIds.playoff` alias). Its own phones, emails, NIF, member number and plan row are not copied to the person (R-18-04 (f): «només aporta el seu gos»). Its number still counts for `Club.nextMemberNumber`. A joined record can be LEFT: its dog is then INACTIVE.
7. **Supply `persones.csv` from the first apply.** If a club was already loaded without the file, the joined record's own member stays in the census, and when the file's NIF equals that record's NIF a later apply stops with `ID_DOCUMENT_ALREADY_EXISTS`. Staging can be wiped; production applies once. This is documented in `playoff-input.md`.
8. **Tie rule.** An empty member number sorts after any number, then the file row decides.

### Questions / catalog proposals
1. **@organizer: S18 §3 vs the S05 seed.** `instructors → INSTRUCTOR_FREE` and `competició 1 gos → COMPETICIO_1` point to plans that the S05 §12 seed does not have. Should the seed get these two plans (prices still pending for COMPETICIO_1), or should S18 §3 mark them unmapped until the club creates them? Assumption applied: unmapped (`PLAN_UNMAPPED`); instructors keep the role.
2. **@organizer: model proposal (not applied).** Add `Dog.sourceIds.playoffPhoto` (the source reference of the Playoff photo, consumed by the cutover download into `photoFileKey`) to the `Dog` row of `MODEL_DADES_PLATAFORMA.md` and to S18 §3 «Marques a les entitats destí».
3. **@organizer: S18 R-18-15.** The report now has the outcome `PROPOSED` (entity `familyGroups`) for the R-18-12 family-group proposal. Please name it in R-18-15 if you want it listed with the other report sections.
4. **Web e2e / D3:** `GET /levels` on the Cànic seed returns 10 levels, and `GET /coverage` no longer lists TER or PENDENT. Any web real-core e2e that counts levels or coverage rows must follow; see `roadmap/MESSAGES.md`.

### Round 2
Executor: claude:claude-opus-5-5 · 2026-09-24. Fixes point 1 of the Organizer verification (R-18-14 as amended 24-09), nothing else.

**What changed**
- `PlayoffPlanner` gets a map `unsupported` (source id → report field). A record in it produces exactly one report line, `members ERROR REEXECUTION_UNSUPPORTED field=<status|persons>`. The planner then skips it: no member, dog or identity change, so the apply never touches that record, its dog, its account or its membership.
  - **(a) `field=status`:** the record maps to `LEFT` (with a recent or an old leave date), and its **own** stored member has `sourceIds.playoffMemberId` equal to this record, status `ACTIVE`, and an `accountId` or a membership in the tenant. The membership check is the new `MigrationIdentityService.hasMembership(memberId)`, which uses `MembershipRepository.findByMemberId`. A joined record whose alias lives on its principal's member is not its own member, so it can still become LEFT (Assumption 6 of round 1).
  - **(b) `field=persons`:** after `persones.csv` is validated, a join is unsupported when an earlier load mapped the joined record's source id to a member other than its principal's member. Every join of that principal is then dropped, and so is the confirmed NIF. The principal is planned as in the earlier load, with its own NIF and aliases. This covers the case where the confirmed NIF is the joined record's own, and there is no `ID_DOCUMENT_ALREADY_EXISTS` and no alias conflict on `member_playoff_ids`.
- `MigrationReport`:
  - the constant `REEXECUTION_UNSUPPORTED`;
  - `hasBlockingErrors()`: every ERROR row except this code;
  - `unsupported()`: the count of these rows;
  - `render()` prints «Validation failed; no changes applied» only for blocking errors. When unsupported rows exist, it adds the summary line `REEXECUTION_UNSUPPORTED: N records are left untouched and the rest is applied (R-18-14). On staging, the way out is --reset and a new load.`
- `PlayoffImportService`: the dry-run/preview gate and the in-transaction re-plan gate use `hasBlockingErrors()`, so the rest is applied. `PlayoffCommand` still exits non-zero on any ERROR row, this code included (commented), so a rehearsal script notices.
- `docs/migration/playoff-input.md`: a new section «Re-execution (R-18-14)» replaces round 1's Assumption 7. The «Any input error prevents apply» line now names the exception.

**Tests** (both new ITs use the fixture and apply through `PlayoffImportService.importDirectory`, like the CLI does)
| Point | Test | What it asserts |
|---|---|---|
| (a) | `PlayoffMigrationIT.T_18_08_R_18_14_activeRecordWithAccountThatComesBackLeftIsRejectedAndLeftUntouched` | It imports a fixture copy, then sets record 63 (the account owner of the pair 63/83) to `Baixa` 2026-06-30. For both the dry run and the apply: record 63's only line is `members ERROR REEXECUTION_UNSUPPORTED field=status`; `hasBlockingErrors()` is false; the summary line and `--reset` appear, and «Validation failed» does not; the counterpart 83 keeps `EMAIL_SHARED` + `accounts SKIPPED` and gets no account; 186 members are UPDATED. The member, dog, account and membership documents of 63 are byte-equal to those after the first load. Both runs are COMPLETED. |
| (b) | `PlayoffMigrationIT.T_18_08_R_18_14_personsFileAfterAFirstLoadIsRejectedWithoutAnAliasConflict` | It imports a copy without `persones.csv`, then imports the fixture with the file (62 principal, 82 joined). For both the dry run and the apply: record 82's only line is `members ERROR REEXECUTION_UNSUPPORTED field=persons`; there is no `PERSON_MERGED` and no `ID_DOCUMENT_ALREADY_EXISTS`; nothing blocks; 187 members are UPDATED; member and dog 82 are unchanged; member 62 keeps its aliases `[62]` and its own `idDocument`. The apply does not throw, so there is no `member_playoff_ids` duplicate key. A plan whose confirmed NIF is 82's own gives the same error and no change for row 83. |
| report + CLI | `PlayoffAdapterTest.T_18_08_commandsValidateOptionsAndProductionProfiles` (extended) | `hasErrors`/`hasBlockingErrors`/`unsupported`; the render with only unsupported rows and with mixed rows; the command exits with an exception on an unsupported-only report. |

**Evidence (round 2)**
- Targeted run during development: `./mvnw -q verify -Dtest=PlayoffAdapterTest -Dit.test=PlayoffMigrationIT -Djacoco.skip=true -Dsurefire.failIfNoSpecifiedTests=false` → exit 0 (`EXIT_0`), log `10-round2-targeted.log`. `PlayoffMigrationIT` 14/0/0/0, `PlayoffAdapterTest` 6/0/0/0. It was green at the first attempt.
- **`./mvnw -q clean verify`** → **exit 0** (`EXIT_0`). The complete log is `11-round2-clean-verify.log` (5,593 lines; the last line records the exit). JaCoCo `haltOnFailure` passed within it. The test summary is `12-round2-test-summary.log`, written by `python3 roadmap/evidence/E5-T12/test-summary.py`:
  ```text
  surefire: tests=510 failures=0 errors=0 skipped=0
    com.agilityhub.core.migration.PlayoffAdapterTest 6/0/0/0
  failsafe: tests=818 failures=0 errors=0 skipped=0
    com.agilityhub.core.migration.PlayoffMigrationIT 14/0/0/0
  (tests/failures/errors/skipped, read from target/surefire-reports and target/failsafe-reports after ./mvnw -q clean verify, exit 0)
  ```
  510/818, against 509/814 in round 1: +1 unit and +2 IT from E6-T01 round 2, and +2 IT from this round. Last 40 lines of the log, each cut at 200 characters:
  ```text
  E5-T07 outbox backlog discarded before PlayoffMigrationIT: 158 PENDING records (more than one dispatch() batch)
  E5-T07 outbox backlog discarded before PlayoffMigrationIT: 162 PENDING records (more than one dispatch() batch)
  E5-T07 outbox backlog discarded before PlayoffMigrationIT: 158 PENDING records (more than one dispatch() batch)
  E5-T07 outbox backlog discarded before PlayoffMigrationIT: 158 PENDING records (more than one dispatch() batch)
  2026-09-24T15:36:17.719+02:00  INFO 6250 --- [agilityhub-core-api] [localhost:56078] org.mongodb.driver.cluster               : Exception in monitor thread while connecting to server localhost:56078

  com.mongodb.MongoSocketOpenException: Exception opening socket
  	at com.mongodb.internal.connection.SocketStream.lambda$open$0(SocketStream.java:85) ~[mongodb-driver-core-5.5.2.jar:na]
  	at java.base/java.util.Optional.orElseThrow(Optional.java:403) ~[na:na]
  	at com.mongodb.internal.connection.SocketStream.open(SocketStream.java:85) ~[mongodb-driver-core-5.5.2.jar:na]
  	at com.mongodb.internal.connection.InternalStreamConnection.open(InternalStreamConnection.java:233) ~[mongodb-driver-core-5.5.2.jar:na]
  	at com.mongodb.internal.connection.DefaultServerMonitor$ServerMonitor.lookupServerDescription(DefaultServerMonitor.java:219) ~[mongodb-driver-core-5.5.2.jar:na]
  	at com.mongodb.internal.connection.DefaultServerMonitor$ServerMonitor.run(DefaultServerMonitor.java:176) ~[mongodb-driver-core-5.5.2.jar:na]
  Caused by: java.net.ConnectException: Connection refused
  	at java.base/sun.nio.ch.Net.pollConnect(Native Method) ~[na:na]
  	at java.base/sun.nio.ch.Net.pollConnectNow(Net.java:694) ~[na:na]
  	at java.base/sun.nio.ch.NioSocketImpl.timedFinishConnect(NioSocketImpl.java:542) ~[na:na]
  	at java.base/sun.nio.ch.NioSocketImpl.connect(NioSocketImpl.java:592) ~[na:na]
  	at java.base/java.net.SocksSocketImpl.connect(SocksSocketImpl.java:327) ~[na:na]
  	at java.base/java.net.Socket.connect(Socket.java:751) ~[na:na]
  	at com.mongodb.internal.connection.SocketStreamHelper.initialize(SocketStreamHelper.java:76) ~[mongodb-driver-core-5.5.2.jar:na]
  	at com.mongodb.internal.connection.SocketStream.initializeSocket(SocketStream.java:104) ~[mongodb-driver-core-5.5.2.jar:na]
  	at com.mongodb.internal.connection.SocketStream.open(SocketStream.java:79) ~[mongodb-driver-core-5.5.2.jar:na]
  	... 3 common frames omitted

  E5-T07 outbox backlog discarded before PlayoffMigrationIT: 160 PENDING records (more than one dispatch() batch)
  E5-T07 outbox backlog discarded before PlayoffMigrationIT: 160 PENDING records (more than one dispatch() batch)
  E5-T07 outbox backlog discarded before PlayoffMigrationIT: 158 PENDING records (more than one dispatch() batch)
  E5-T07 outbox backlog discarded before PlayoffMigrationIT: 158 PENDING records (more than one dispatch() batch)
  E5-T07 outbox backlog discarded before PlayoffMigrationIT: 158 PENDING records (more than one dispatch() batch)
  E5-T07 outbox backlog discarded before PlayoffMigrationIT: 158 PENDING records (more than one dispatch() batch)
  exit=0 (the command ran as ./mvnw -q clean verify > 11-round2-clean-verify.log 2>&1 && echo EXIT_0 || echo EXIT_NONZERO and printed EXIT_0)
  ```
  The first 12 of the 40 lines are the Spring start/stop lines of `PlayoffMigrationIT`, the same as in round 1. The socket exception is Testcontainers shutdown noise, as in round 1. The only `argon2` matches in the log are Spring class names in an expected warning stack trace; there are no hashes.
- **Verifications 2 and 3, re-run on the round-2 jar:** `python3 roadmap/evidence/E5-T12/stack-checks.py > roadmap/evidence/E5-T12/09-stack-checks-run.log 2>&1` → exit 0 (`HELPER_EXIT_0`). It overwrote logs 03–09 of round 1; only the temp paths and timestamps differ. The results are identical:
  - `club:apply` twice: 15 changes, then 0 changes and a Mongo snapshot unchanged;
  - `GET /levels` → 200 with `PENDENT` and `TER` at `progression=false`;
  - the dry run on the fixtures and on the anonymized derivative: `EMAIL_SHARED=19`, `PERSON_MERGED=1`, `familyGroups proposed=18`, and no `REEXECUTION_UNSUPPORTED` (a first load on an empty club).
  
  The last 40 lines of `09-stack-checks-run.log` are the same block as the round-1 transcript above, except the temp directory name (`e5-t12-cli-8gbnfn7w`).
- **Not run:** `bin/openapi-snapshot`, because no endpoint or schema changed (`MigrationIdentityService` is internal); the web e2e (other repository).

**Files changed (round 2)**
- **Code:**
  - `migration/application/PlayoffPlanner.java`, `PlayoffImportService.java`, `PlayoffCommand.java` (comment only)
  - `migration/domain/MigrationReport.java`
  - `identity/application/MigrationIdentityService.java` (`hasMembership`)
- **Tests:** `migration/PlayoffMigrationIT.java` (+2 tests), `migration/PlayoffAdapterTest.java` (extended)
- **Docs and changelog:** `docs/migration/playoff-input.md`, `CHANGELOG.md` (Changed)
- **Evidence:** `roadmap/evidence/E5-T12/10-*.log`, `11-*.log`, `12-*.log`, `test-summary.py`, and the refreshed `03`–`09`
- **Roadmap:** this file, `roadmap/MESSAGES.md`, `roadmap/STATUS.md` (rendered)

**Rules and tests (round 2):** R-18-14 (the 24-09 amendment, «Transicions que no es reconcilien») → T-18-08 (`T_18_08_R_18_14_*` ×2, and the report/CLI part of `T_18_08_commandsValidate…`).

**Assumptions (round 2)**
1. **`REEXECUTION_UNSUPPORTED` is a migration report code**, like `EMAIL_SHARED`/`PERSON_MERGED`. S18 R-18-14 names it. It is not an `ErrorCode` enum entry, and no API returns it.
2. **Exit code.** The apply of the rest commits (`MigrationRun` COMPLETED, with `membersERROR` in its counters), but `migration:playoff` still exits non-zero because the report has an ERROR row. The organizer's point did not fix the exit code. I chose non-zero so that no rehearsal script reads a partial load as clean.
3. **`--reset` does not exist yet.** The summary line names it because S18 R-18-14 does. `playoff-input.md` says «No reset … switch is offered in this stage». See Question 1.
4. **(b) drops every join of the principal**, including a join over a record that was never loaded. The club's confirmation about that person is then not applied at all. Otherwise the principal would take a NIF that can belong to the untouched record and stop the whole plan with `ID_DOCUMENT_ALREADY_EXISTS`.

**Questions (round 2)**
1. **@organizer: `--reset`.** The summary line points to `--reset` on staging, but no command offers it. Which task owns it (E8-T06 or the E11 rehearsal)? Assumption meanwhile: text only, nothing implemented.
2. **@organizer: the reverse transition.** A club loads with `persones.csv`, then loads again without the file or without that row. The joined record's source id is then an alias of the principal's member. From reading the code (I did not test it), the planner gives it the principal's member id, so the planned changes contain two member changes for the same id, and the second overwrites the principal's fields. It is not in point 1, so I did not change it. Should it also be `REEXECUTION_UNSUPPORTED field=persons`? Assumption meanwhile: out of scope; production loads once.

### Round 3
Executor: claude:claude-opus-5-5 · 2026-09-24. Fixes points 1–3 of the round-2 verification (Codex review `E5-T12-20260924-1541`, #1–#4), nothing else.

**What changed** (`PlayoffPlanner`, `MigrationReport`)
- **Point 1: one destination, protected once.** A new pre-pass `destinations(candidates, skipped)` runs after the sort and before any change is planned. It resolves every record to its member id with the same rule as `memberAndDog`:
  - a joined record goes to its principal's member;
  - any other record goes to the member of its person key (NIF/passport, else email, else id), which is the key's first record in plan order (`memberBySource`, else the deterministic id).
  
  `memberAndDog` now uses that id, so the plan and the pre-pass cannot diverge. A member is protected (`protectedMembers`: member id → field) when:
  - a record's own transition is rejected: (a) `status`, or (b) `persons` from `persons()`; its stored member is protected;
  - two different person keys of this load resolve to one member id (`persons`). This covers the reverse of (b): without the join, record 82 resolves through its alias to member 62. It also covers a join changed to another record. The plan therefore never holds two member changes for one id;
  - a record that an earlier load put on one member resolves to another now (`persons`): both members are protected.
  
  `rejected(row)` runs first in `memberAndDog` and for the skipped rows (`SKIP`, old `LEFT`). A row whose member is protected gets exactly one line `members ERROR REEXECUTION_UNSUPPORTED` and no member, dog or identity change. Its field is:
  - the row's own transition, if it has one;
  - else `persons`, for a joined record;
  - else the reason its member is protected.
  
  **(a) per destination** (`importedActiveWithAccess`): the record is now `LEFT`, and its stored member is `ACTIVE` with an account or a membership. The earlier load must have imported the record `ACTIVE`: it is the member's own record (`sourceIds.playoffMemberId`), or it is an alias whose stored dog is `ACTIVE`. Codex #3 is covered: the same-NIF alias 184 of the protected member 1 is rejected too.
- **Point 2: dependants never block.**
  - A joined record whose principal is protected is `field=persons` (it resolves to the protected member). It is no longer `INPUT_SCHEMA_MISMATCH`.
  - In `groups()`, a group whose holder or member is unsupported or protected is left as it is, with the line `familyGroups ERROR REEXECUTION_UNSUPPORTED field=familyGroup` on its first row, before any other group check. The census write is a `$set`, so the other members keep their stored `familyGroupId`.
  - Neither is a blocking error (`hasBlockingErrors()` is unchanged).
- **Point 3: report wording** (`MigrationReport.render`).
  - With a blocking error, only «Validation failed; no changes applied…».
  - Otherwise the dry run prints `REEXECUTION_UNSUPPORTED: N records would be left untouched; the rest would be applied (R-18-14). On staging, the way out is --reset and a new load.`
  - The apply prints «… N records were left untouched; the rest was applied (R-18-14) …».
- `docs/migration/playoff-input.md` «Re-execution (R-18-14)» is rewritten for the per-member rule, the dependants and the three wordings. `--reset` points to E8-T06 step 10.

**Tests.** Each IT imports the fixture or a copy through `PlayoffImportService.importDirectory`, then runs the dry run **and** the apply. `graph(member)` compares the member, its dogs, its membership and its account byte for byte. `assertNothingPlannedFor(plan, member)` checks that the plan has no member, dog or identity change for that member and no member id twice.
| Point | Test (`PlayoffMigrationIT` unless noted) | What it asserts |
|---|---|---|
| 1, Codex #1 | `T_18_08_R_18_14_personsJoinRemovedOrChangedAfterALoadLeavesThePersonUntouched` | **Load with `persones.csv`, then without it:** 62 and 82 are both exactly `members ERROR REEXECUTION_UNSUPPORTED field=persons`; not blocking; 186 UPDATED; the graph of member 62 (member, 2 dogs, membership, account) is byte-equal. **Join changed to 62←83:** 62, 82 and 83 are `field=persons`; 185 UPDATED; members 62 and 83 are byte-equal. 3 runs COMPLETED. |
| 1 | `T_18_06_E32_withoutThePersonsFileEachNifIsOnePerson` (new, split from `T_18_06_E32_samePerson…`) | It replaces the assertion that was at line 171. On an empty club, without the file: 188 member changes with 188 **distinct** ids, no `PERSON_MERGED`, no ERROR. The old assertion ran after the load, which is exactly Codex #1: it held a duplicate id (log 13). |
| 2, Codex #2 | `T_18_08_R_18_14_principalAndFamilyHolderLeftStayUntouchedWithTheirDependantsAndNeverBlock` | 61 (the family holder) and 62 (the principal) become `Baixa`. 62 and 61 give `field=status`, and 82 gives `field=persons`. The only non-proposal familyGroups line is `groups:2 ERROR REEXECUTION_UNSUPPORTED familyGroup`. Not blocking; 4 unsupported; 185 UPDATED. Members 61 and 62 and the family group are byte-equal; 81 stays `ACTIVE` in its group. The plan has no `family_groups` change. |
| 1, Codex #3 | `T_18_08_R_18_14_sameNifAliasOfAnAccountHolderThatComesBackLeftIsUntouched` | In a copy, record 1 (NIF shared with 184, the older one) gets a fictional email `@example.test`, so it owns the account. After the load, 1 becomes `Baixa`: 1 and 184 are both `field=status`, and member 1 (the member, 2 dogs, the membership, the account) is byte-equal. |
| 3, Codex #4 | `PlayoffAdapterTest.T_18_08_commandsValidateOptionsAndProductionProfiles` + every IT above (`partial(dryRun, n)`) | Apply «were left untouched; the rest was applied», dry run «would be left untouched; the rest would be applied», and blocked «Validation failed; no changes applied», without «left untouched», «the rest» or `--reset`, in both modes. |
| round 2 | `T_18_08_R_18_14_activeRecordWithAccount…`, `…personsFileAfterAFirstLoad…` | Unchanged behaviour, with the new wording. |

**Evidence (round 3)**
- **Before the fix**, with the new tests on the round-2 planner (only the report wording changed): `./mvnw -q verify -Dtest=PlayoffAdapterTest -Dit.test=PlayoffMigrationIT -Djacoco.skip=true -Dsurefire.failIfNoSpecifiedTests=false` → **exit 1**, log `13-round3-before-fix-targeted.log`: `PlayoffMigrationIT` 17 tests, 4 failures.
  - Codex #1: a plan change for member 62, and «Found duplicate(s)» in the E32 assertion;
  - Codex #2: `entries(82)` was `["members ERROR INPUT_SCHEMA_MISMATCH"]` (blocking);
  - Codex #3: member and dog changes were planned for the protected member.
  
  (A first attempt before that did not compile: `var` in a compound declaration in the test. I fixed it and did not keep that log.)
- After the fix: the same targeted command → exit 1. My test also counted the normal `PROPOSED` familyGroups rows; I filtered them out. Then **exit 0**, log `14-round3-targeted.log`.
- **`./mvnw -q clean verify`** → **exit 0** (`EXIT_0`, the last line of `15-round3-clean-verify.log`, 5,625 lines, run as `./mvnw -q clean verify > 15-round3-clean-verify.log 2>&1 && echo EXIT_0 >> … || echo EXIT_NONZERO >> …`). JaCoCo `haltOnFailure` passed within it. The test summary `16-round3-test-summary.log` comes from `python3 roadmap/evidence/E5-T12/test-summary.py`:
  ```text
  surefire: tests=521 failures=0 errors=0 skipped=0
    com.agilityhub.core.migration.PlayoffAdapterTest 6/0/0/0
  failsafe: tests=852 failures=0 errors=0 skipped=0
    com.agilityhub.core.migration.PlayoffMigrationIT 18/0/0/0
  (tests/failures/errors/skipped, read from target/surefire-reports and target/failsafe-reports after ./mvnw -q clean verify, exit 0)
  ```
  521/852, against 510/818 in round 2: the other tasks published since then, plus 4 ITs of this round. Last 40 lines of the log, each cut at 200 characters:
  ```text
  2026-09-24T18:13:39.762+02:00  INFO 57759 --- [agilityhub-core-api] [           main] o.s.b.w.e.tomcat.GracefulShutdown        : Commencing graceful shutdown. Waiting for active requests to complete
  2026-09-24T18:13:39.763+02:00  INFO 57759 --- [agilityhub-core-api] [tomcat-shutdown] o.s.b.w.e.tomcat.GracefulShutdown        : Graceful shutdown complete
  2026-09-24T18:13:39.764+02:00  INFO 57759 --- [agilityhub-core-api] [           main] o.s.b.w.e.tomcat.GracefulShutdown        : Commencing graceful shutdown. Waiting for active requests to complete
  2026-09-24T18:13:39.764+02:00  INFO 57759 --- [agilityhub-core-api] [tomcat-shutdown] o.s.b.w.e.tomcat.GracefulShutdown        : Graceful shutdown complete
  2026-09-24T18:13:39.765+02:00  INFO 57759 --- [agilityhub-core-api] [           main] o.apache.catalina.core.StandardService   : Stopping service [Tomcat]
  E5-T07 outbox backlog discarded before PlayoffMigrationIT: 161 PENDING records (more than one dispatch() batch)
  E5-T07 outbox backlog discarded before PlayoffMigrationIT: 158 PENDING records (more than one dispatch() batch)
  2026-09-24T18:13:47.655+02:00  INFO 57759 --- [agilityhub-core-api] [localhost:64694] org.mongodb.driver.cluster               : Exception in monitor thread while connecting to server localhost:64694
  
  com.mongodb.MongoSocketOpenException: Exception opening socket
  	at com.mongodb.internal.connection.SocketStream.lambda$open$0(SocketStream.java:85) ~[mongodb-driver-core-5.5.2.jar:na]
  	at java.base/java.util.Optional.orElseThrow(Optional.java:403) ~[na:na]
  	at com.mongodb.internal.connection.SocketStream.open(SocketStream.java:85) ~[mongodb-driver-core-5.5.2.jar:na]
  	at com.mongodb.internal.connection.InternalStreamConnection.open(InternalStreamConnection.java:233) ~[mongodb-driver-core-5.5.2.jar:na]
  	at com.mongodb.internal.connection.DefaultServerMonitor$ServerMonitor.lookupServerDescription(DefaultServerMonitor.java:219) ~[mongodb-driver-core-5.5.2.jar:na]
  	at com.mongodb.internal.connection.DefaultServerMonitor$ServerMonitor.run(DefaultServerMonitor.java:176) ~[mongodb-driver-core-5.5.2.jar:na]
  Caused by: java.net.ConnectException: Connection refused
  	at java.base/sun.nio.ch.Net.pollConnect(Native Method) ~[na:na]
  	at java.base/sun.nio.ch.Net.pollConnectNow(Net.java:694) ~[na:na]
  	at java.base/sun.nio.ch.NioSocketImpl.timedFinishConnect(NioSocketImpl.java:542) ~[na:na]
  	at java.base/sun.nio.ch.NioSocketImpl.connect(NioSocketImpl.java:592) ~[na:na]
  	at java.base/java.net.SocksSocketImpl.connect(SocksSocketImpl.java:327) ~[na:na]
  	at java.base/java.net.Socket.connect(Socket.java:751) ~[na:na]
  	at com.mongodb.internal.connection.SocketStreamHelper.initialize(SocketStreamHelper.java:76) ~[mongodb-driver-core-5.5.2.jar:na]
  	at com.mongodb.internal.connection.SocketStream.initializeSocket(SocketStream.java:104) ~[mongodb-driver-core-5.5.2.jar:na]
  	at com.mongodb.internal.connection.SocketStream.open(SocketStream.java:79) ~[mongodb-driver-core-5.5.2.jar:na]
  	... 3 common frames omitted
  
  E5-T07 outbox backlog discarded before PlayoffMigrationIT: 162 PENDING records (more than one dispatch() batch)
  E5-T07 outbox backlog discarded before PlayoffMigrationIT: 160 PENDING records (more than one dispatch() batch)
  E5-T07 outbox backlog discarded before PlayoffMigrationIT: 162 PENDING records (more than one dispatch() batch)
  E5-T07 outbox backlog discarded before PlayoffMigrationIT: 158 PENDING records (more than one dispatch() batch)
  E5-T07 outbox backlog discarded before PlayoffMigrationIT: 158 PENDING records (more than one dispatch() batch)
  E5-T07 outbox backlog discarded before PlayoffMigrationIT: 160 PENDING records (more than one dispatch() batch)
  E5-T07 outbox backlog discarded before PlayoffMigrationIT: 160 PENDING records (more than one dispatch() batch)
  E5-T07 outbox backlog discarded before PlayoffMigrationIT: 158 PENDING records (more than one dispatch() batch)
  E5-T07 outbox backlog discarded before PlayoffMigrationIT: 158 PENDING records (more than one dispatch() batch)
  E5-T07 outbox backlog discarded before PlayoffMigrationIT: 158 PENDING records (more than one dispatch() batch)
  E5-T07 outbox backlog discarded before PlayoffMigrationIT: 158 PENDING records (more than one dispatch() batch)
  EXIT_0
  ```
  As in rounds 1–2, the socket exception is Testcontainers shutdown noise. The «outbox backlog» lines are the E5-T07 test-support diagnostic. A scan of logs 13–15 for `eyJ`, `Bearer ` and `$argon2` finds 0 matches.
- **Verifications 2 and 3, re-run on the round-3 jar:** `python3 roadmap/evidence/E5-T12/stack-checks.py > roadmap/evidence/E5-T12/09-stack-checks-run.log 2>&1` → **exit 0** (`HELPER_EXIT_0`). It refreshes logs 03–09; only the temp paths and timestamps differ.
  - `club:apply` twice: 15 changes, then 0 changes, and the Mongo snapshot is unchanged;
  - `GET /levels` → 200, with `PENDENT` and `TER` at `progression=false`;
  - both dry runs (the fixtures and the anonymized derivative): `EMAIL_SHARED=19`, `PERSON_MERGED=1`, `familyGroups proposed=18`, and no `REEXECUTION_UNSUPPORTED` (a first load).
  
  The last 40 lines of `09-stack-checks-run.log` are the same block as the round-1 transcript above (temp directory `e5-t12-cli-i4i4eu3c`).
- **Not run:** `bin/openapi-snapshot`, because no endpoint or schema changed; the web e2e (other repository).

**Files changed (round 3)**
- Code: `migration/application/PlayoffPlanner.java`, `migration/domain/MigrationReport.java`
- Tests: `migration/PlayoffMigrationIT.java` (+4 tests, the assertion at line 171 replaced, round-2 wording updated), `migration/PlayoffAdapterTest.java` (the three wordings)
- Docs and changelog: `docs/migration/playoff-input.md`, `CHANGELOG.md` (Changed)
- Evidence: `roadmap/evidence/E5-T12/13-*.log` … `16-*.log`, the refreshed `03`–`09`, and `test-summary.py` (docstring)
- Roadmap: this file, `roadmap/MESSAGES.md`, `roadmap/STATUS.md` (rendered)

**Rules and tests (round 3):** R-18-14 («Transicions que no es reconcilien», per destination member) and R-18-15 (the report tells a proposal, a blocked run and a partial apply apart) → T-18-08 (`T_18_08_R_18_14_*` ×5, the report part of `T_18_08_commandsValidate…`), plus T-18-06 (`T_18_06_E32_withoutThePersonsFileEachNifIsOnePerson`).

**Assumptions (round 3)**
1. **Both rows of a collision are rejected.** The ruling says «on the second row». It also says every member of an unsupported record is protected. So the first row cannot plan that member either, and both rows get `field=persons`. Otherwise the first row would still rewrite the member (for example 62 without its confirmed NIF), which is the silent overwrite that R-18-14 forbids.
2. **(a) for an alias:** it counts only when the earlier load imported that record `ACTIVE`, that is, when its stored dog is `ACTIVE`. An alias that was already `LEFT` and stays `LEFT` (one of a person's dogs left) is not a transition, so an unchanged reapply stays clean.
3. **A group is rejected as a whole**, with one `familyGroups` line on its first `family_groups.csv` row, even if only one of its members is protected. Its other members are still applied, without a group change.
4. **«N records»** counts every `REEXECUTION_UNSUPPORTED` line, including the `familyGroup` lines (each is a source row of `family_groups.csv`).

**Questions (round 3):** none.

## Organizer verification
Result: changes_requested · 2026-09-24 · organizer (Claude)

Most of the task is done and well tested:
- mapping v2 with B30–B32, and the seed-consistency test that caught `cadells: CADELLS`;
- the `PENDENT` seed level;
- the coverage over progression levels only (E29);
- `persones.csv` with its validations and anonymizer parity;
- accounts only for `ACTIVE` persons, with the oldest-`Data alta` rule and the family-group proposals.

`clean verify`: 509 + 814. The stack checks: `club:apply` ×2, `GET /levels`, the dry run on the fixtures and on their anonymized derivative. CI for `f162cd3` was cancelled by the next push, and the run of `7cd42b0` covers the same code.

The independent review `roadmap/reviews/E5-T12-20260924-1506-codex.md` (Codex) found three gaps. The organizer's reading:
- **#1 and #2: re-execution (R-18-14).** A later load can change what an earlier load created, and today the planner does not see it:
  - a record imported `ACTIVE` with an account and a membership comes back `LEFT`;
  - `persones.csv` arrives after the joined record was already loaded as its own person. The dry run then reports success and the apply conflicts on `member_playoff_ids`, or the plan stops on `ID_DOCUMENT_ALREADY_EXISTS`.
  
  A dry run that passes and an apply that then fails is the real defect. Production allows one `APPLY` (R-18-14), and staging has `--reset`, so these transitions are **rejected, not reconciled**. S18 R-18-14 now says so.
- **#3: photos (B29).** Downloading from Playoff's storage is a cutover step, not a census import step. Keeping the reference in `Dog.sourceIds.playoffPhoto` and never touching `photoFileKey` is accepted. The download is now **E8-T06** step 4 (`migration:photos`), before the E11 rehearsal. S18 §3 and the model now say so.

Round 2 — fix exactly this point:
1. **Unsupported re-execution transitions are planning errors.** When the plan meets one of the two transitions below:
   - the dry run reports `ERROR REEXECUTION_UNSUPPORTED` on that record, with `field=status` or `field=persons`;
   - the apply leaves that record untouched and applies the rest;
   - the summary line says that on staging the way out is `--reset`.
   
   The two transitions:
   - **(a)** a record that an earlier load imported as `ACTIVE` with an account or a membership is now `LEFT`;
   - **(b)** `persones.csv` joins a record that an earlier load imported as its own person. This includes the case where the confirmed NIF is the joined record's own.
   
   Tests on the fixture:
   - (a) import, then re-plan with the record `LEFT` and an `ACTIVE` counterpart that shares its email → the error, and nothing written for that record or its account;
   - (b) import without `persones.csv`, then with it → the error in the dry run and in the apply, and no conflict on `member_playoff_ids`.
   
   Update `docs/migration/playoff-input.md` (Assumption 7 becomes this rule).

**Rulings on your questions and assumptions:**
- **Q1** (`INSTRUCTOR_FREE`, `COMPETICIO_1` are not in the S05 seed). Your assumption stands: both stay `PLAN_UNMAPPED`, and instructors keep the role. The organizer asks Josep whether the two plans should exist and what «Competició 1 gos» costs (**B34** in `DECISIONS_PENDENTS.md`). S18 §3 marks both lines as pending.
- **Q2 is accepted.** `Dog.sourceIds.playoffPhoto` is in the `Dog` row of `MODEL_DADES_PLATAFORMA.md` and in S18 §3.
- **Q3 is accepted.** R-18-15 names the outcome `PROPOSED` of `familyGroups`.
- **Q4** → the web is told in its `MESSAGES.md` (10 levels on the Cànic seed; D3 coverage without TER or PENDENT).
- **Assumption 4 is accepted.** The Cànic's locales are `ca`/`es`, so the task's en «Pending» was wrong.
- Assumptions 1, 2, 3, 5, 6 and 8 are accepted.

### Round 2 verification
Result: changes_requested · 2026-09-24 · organizer (Claude)

Point 1 is implemented as asked, and it works for the two cases it names:
- `REEXECUTION_UNSUPPORTED` with `field=status|persons`;
- the rest of the load is applied;
- the summary line;
- the two ITs through `importDirectory`, with byte-equal documents.

`clean verify` exits 0 (510 + 818), and CI is green at `2960329`.

The independent review `roadmap/reviews/E5-T12-20260924-1541-codex.md` found that the protection is still per **source row**, not per **destination member**. The organizer traced the three majors in `PlayoffPlanner`, and they hold:
- **#1:** without its join, record 82 resolves through `memberBySource.getOrDefault` to member 62, so the plan has two changes for one member, and the second overwrites the first. You raised the same case in your question 2.
- **#2:** a principal or a family holder that is skipped as unsupported makes its dependants fail with blocking errors. `personBySource.get(principal)` is `null` → `INPUT_SCHEMA_MISMATCH`, and `groups()` meets a group with one planned member. The whole load then stops.
- **#3:** `importedWithAccess` protects only the row whose own `playoffMemberId` matches. Another row of the same person (a same-NIF alias) still plans writes to that member.

Each of these either overwrites a person silently or blocks a load that should apply. That is what R-18-14 forbids.

Round 3 — fix exactly these points:
1. **One destination, protected once.**
   - Keep the set of **protected member ids**: every member of a record that is `REEXECUTION_UNSUPPORTED`. No row, own or alias, plans a member, dog or identity change for a protected member. An alias row that resolves to one gets the same `REEXECUTION_UNSUPPORTED` line.
   - A plan never holds two member changes for one member id. If it would, the case is `REEXECUTION_UNSUPPORTED field=persons` on the second row, never a silent overwrite.
   - Removing or changing an existing join after a load (the reverse of (b)) is `REEXECUTION_UNSUPPORTED field=persons`.
2. **Dependants of a protected record are left untouched too, and never block.**
   - A joined record whose principal is protected → `REEXECUTION_UNSUPPORTED field=persons`.
   - A family group with a protected holder or member → no group change, reported as `REEXECUTION_UNSUPPORTED field=familyGroup`.
   - Neither is a blocking error: the rest of the load applies.
3. **The report says what happened (Codex #4).**
   - Dry run: «N records would be left untouched; the rest would be applied».
   - Apply: «… were left untouched; the rest was applied».
   - Any blocking error: only «Validation failed; no changes applied», without the partial line.

Tests on the fixture, each in the dry run **and** the apply, with byte-for-byte preservation of the protected documents:
- import with `persones.csv`, then without it (Codex #1): replace the assertion at `PlayoffMigrationIT.java:171`, which accepts 188 changes without checking for distinct ids;
- principal 62 `LEFT`, and holder 61 `LEFT` (Codex #2);
- a same-NIF pair whose older record owns the account, changed to `LEFT` (Codex #3);
- the three report wordings.

**Rulings on your round-2 questions and assumptions:**
- **Q1 (`--reset`) → E8-T06 step 10.** Keep the summary line as it is.
- **Q2** → point 1 above.
- **Assumption 2 is accepted:** a non-zero exit code whenever there is an ERROR row.
- **Assumption 4 is accepted:** every join of a rejected principal is dropped.
