---
id: E5-T04
title: Free training (S09 WP-09-B + WP-09-C): TrainingSlotService computed grid, eligibility, TrainingBookingService with the partial unique index, «Qualsevol» ring choice, cancellation, and the real TrainingOccupancyPort / TrainingConflictPort / TrainingBookingsQuery adapters behind /ring-blocks
stage: E5
repo: agilityhub-core-api
thread: A
order: 40
status: awaiting_verification
depends_on: [E5-T01, E5-T02]
branch:
pr:
updated: 2026-09-24
---

# E5-T04 · Free training (S09 WP-09-B + WP-09-C)

## Objective
Work packages S09 WP-09-B and WP-09-C: a dog with the right to train alone books half-hour ring slots inside the booking window, at most three per week counted **by session date**, with the partial unique index as the final guarantee against double booking; an instructor reserves or blocks a ring without a student through the `/ring-blocks` endpoints E4-T03 already owns; and the two ports S06 has been calling with null objects (`TrainingOccupancyPort`, `TrainingConflictPort`) plus E3-T04's `TrainingBookingsQuery` get their real implementations, so the day grids (10, 23, D12, D4) show «Ocupada»/«Entren.» and class creation, ring deactivation and activity publication see real conflicts.

## Spec references
S09 R-09-01 (`canFreeTrain`, eligible dogs, family group), R-09-02 (bookable rings and capacity), R-09-03 (grid computation, holidays, DST, cache and its invalidation), R-09-04 (booking window), R-09-05 (training week and counter by session date), R-09-06 (one dog per ring and slot, the seven-step transaction, `seatIndex`, `Dog.trainingSeq`/`Member.trainingSeq`), R-09-07 («Qualsevol» and catalog order), R-09-08 (member conditions), R-09-09 (`Member.lastDogForTraining`, `defaultDogId`), R-09-10 (cancellation, `training.cancelThresholdMinutes`, admin-late with reason), R-09-11 (ring reservation/block from screen 24), R-09-12 (`TrainingOccupancyService` and the redaction per viewer role), R-09-13 (`TrainingConflictService` and `cancelBookings: true`), R-09-14 (system cancellations), R-09-15 (`RingSetup` in the grid), R-09-16 (origin, impersonation, `override`, audit), §3 (both documents, the indexes, the fields written on other aggregates), §5, §6 (every row, the four JSON forms, the `details` shapes), §7, §8 (N-06, N-07, N-47), §9, §10 back keys, §13 items 1…12 · tests **T-09-01…T-09-23, T-09-25…T-09-35** (T-09-24 is the contract test of E5-T01; T-09-36 is E8) · S06 R-06-11 and `roadmap/tasks/E4-T03.md` step 6 (the `RingBlockService` this task feeds) · S14 R-14-09 audit actions `TRAINING_BOOKED_BY_CLUB`, `TRAINING_CANCELLED_BY_CLUB` · `CATALEG_PARAMETRES.md` block «Entrenaments lliures» + `ringBlocks.maxHorizonDays`, `bookings.weekOpensAt`, `bookings.limitUnit`, `levels.enabled`, `classes.slotMinutes`, `club.openingHours`, `club.holidays`, `club.timeZone`, `courses.showSetupToMembers`, `inactivity.cancelBookingsOnApproval` · `CATALEG_ERRORS.md` §1 + rule 0 · `CATALEG_NOTIFICACIONS.md` N-06, N-07, N-47 · `CATALEG_ESDEVENIMENTS.md` training and ring-block rows + Annex A (`TrainingCancelled{by, cancelReason, late}`) · `docs/DECISIONS_PENDENTS.md` B15, part C S09.

## Context to load
`docs/specs/S09-entrenaments-lliures.md` complete · `docs/specs/S06-planificacio-setmanes-i-calendari.md` R-06-11, R-06-12 and §6 form D only · `docs/specs/S16-recorreguts-rings-i-muntatge.md` only the `RingSetup`/`Ring.activeSetupId` fields R-09-15 reads · `roadmap/tasks/E5-T01.md` (contract, `training_bookings` document and its partial unique index) · `roadmap/tasks/E4-T03.md` and `src/main/java/com/agilityhub/core/clubs/scheduling/application/` (`RingBlockService`, `ClassSessionService`, `PlanningViews`, `DayGridQuery`, `SchedulingTransactions`) + `clubs/scheduling/application/ports/{TrainingOccupancyPort,TrainingConflictPort,SchedulingPortDefaults}.java` · `roadmap/tasks/E5-T02.md` and `clubs/bookings/application/**` (`BookingWeeks`, `BookingEligibility`, `BookingTransactions`, the booking repository this task queries for overlaps) · `clubs/dashboard/application/ports/TrainingBookingsQuery.java`, `DashboardPortDefaults.java` · `clubs/catalogs/application/**` (`Ring`, `Level`, `Ring.order`, `allowsFreeTraining`, `trainingCapacity`, `activeSetupId`) · `clubs/census/application/**` (`Dog.freeTrainingOverride`, `Dog.status`, `Member.bookingBlock`, family groups) · `platform/application/{ClubConfigService,ModuleGuard,RequiresModule}.java`, `shared/application/{ClubClock,EventPublisher,DomainEventHandler}.java`, `platform/application/audit/*` · `docs/playbooks/new-endpoint.md`, `new-entity.md`, `new-event-consumer.md` · this file.

## Steps
1. **`TrainingEligibilityService`** (R-09-01): `canFreeTrain(dog) = dog.status == ACTIVE ∧ (dog.freeTrainingOverride ?? level(dog).grantsFreeTraining)`; with `levels.enabled = false` only an explicit `freeTrainingOverride == true` grants it (null = no). `eligibleDogs(memberId)` = own dogs plus, with `FAMILY_GROUP`, the dogs of the group's members whose owner is `ACTIVE`, ordered own-first then by `Dog.name`, each with `rightSource LEVEL|MANUAL` and `ownerName` (null for own). Expose `membersWithRight()` for S16's N-31 (E9). T-09-01, T-09-02.
2. **`TrainingSlotService`** (R-09-03, R-09-04, R-09-15) — slots are **computed, never persisted** (`training_slots` is not created at R1): for each local day of `[from, to]`, closed when the date is in `club.holidays` or `club.openingHours[dayOfWeek]` is null; otherwise walk `open → close` in `training.slotMinutes` (30) steps, building instants with `ZonedDateTime.of(date, time, club.timeZone)` (Java semantics: a gap shifts forward, an overlap takes the **first** occurrence); load in one query per collection the overlapping `ClassSession` `DRAFT|ACTIVE` with a ring, `RingBlock` `ACTIVE` and `TrainingBooking` `ACTIVE`; per (ring, slot) apply the priority `BLOCKED/CLASS` → `BLOCKED/RING_BLOCK` → `BOOKED/TRAINING` (or `OWN_TRAINING` when one of the bookings is the queried `dogId`) → `FREE`, with «overlap» = `a.from < slot.endsAt ∧ a.to > slot.startsAt`; rings exist only when `Ring.active ∧ Ring.allowsFreeTraining`, `capacity = Ring.trainingCapacity ?? training.capacityPerRingSlot`; `bookable = FREE ∧ startsAt > now ∧ inside the window` for MEMBER, `anyFree` per slot. MEMBER requests are clipped to `[today, today + training.bookingWindowDays]`, other roles to ≤ 31 days. INSTRUCTOR/ADMIN cells additionally carry `occupants[]`, `block` and `classSession`. With `COURSES` and (`courses.showSetupToMembers` or role ≠ MEMBER) each ring carries `setup` from `Ring.activeSetupId`. Cache: a static layer (steps 1–3 without bookings) keyed `{clubId, date}`, TTL 60 s, invalidated by the consumers of step 8; the booking layer is always live and **the unique index, never the cache, is the source of truth for concurrency**. T-09-03, T-09-04, T-09-05, T-09-06, T-09-22, T-09-29.
3. **Training week and counter** (R-09-05, decision B15): `weekStart` of a slot = the last local occurrence of `bookings.weekOpensAt` ≤ `startsAt` (reuse E5-T02's `BookingWeeks.lastOpening`, do not re-implement it); the counter of a unit (`bookings.limitUnit`: `DOG` → `dogId`, `MEMBER` → `memberId`) = `ACTIVE` bookings with that `weekStart`, **including sessions already done**, against `training.maxPerWeek` (3). The count is by **session date**, never by booking date. `GET /me/training-summary?dogId=&date=` returns `eligibleDogs`, `defaultDogId` (= `Member.lastDogForTraining` while still eligible, else the first eligible), `limitUnit`, `weekOpensAt`, the week of `date` (default today), `counter{used, limit, remaining, resetsAt}`, `cancellableBookings[]` (future `ACTIVE` still inside the cancellation threshold) and `bookingBlock`/`inactivity`. T-09-08, T-09-09, T-09-10, T-09-35.
4. **`TrainingBookingService.book(dogId, startsAt, ringId?, override?)`** (R-09-06, R-09-07, R-09-08), `Idempotency-Key`, one Mongo transaction retried whole (≤ 3) on `DuplicateKey`/`WriteConflict`: (1) `$inc` `Dog.trainingSeq` (unit `DOG`) or `Member.trainingSeq` (unit `MEMBER`) to serialize the counter; (2) eligibility (`422 DOG_NOT_ALLOWED`), ring bookable (`422 RING_NOT_RESERVABLE`), member conditions (`422 MEMBER_NOT_ACTIVE` / `422 BOOKING_BLOCKED` / `422 INACTIVITY_PERIOD`, reusing E5-T02's `InactivityPort`), slot on the grid (`422 SLOT_NOT_ON_GRID` — S09 writes 400, catalog rule 0 gives 422, see Fixed conventions) and inside the window (`422 SLOT_OUT_OF_WINDOW`, `422 CLUB_CLOSED` for a holiday or out-of-hours date); (3) no overlapping `TrainingBooking` or class `Booking` of the same dog → `422 DOG_ALREADY_BOOKED` (query `clubs.bookings.application` directly — that direction is allowed); (4) counter < limit → `409 TRAINING_LIMIT_REACHED {limit, used, weekStart, weekEnd, cancellableBookings[]}`; (5) slot state → `409 SLOT_TAKEN {ringId, startsAt, reason, freeRings[]}`; (6) insert with the lowest free `seatIndex`, the partial unique index `{clubId, ringId, startsAt, seatIndex}` on `ACTIVE` being the final guarantee; (7) `Member.lastDogForTraining`, `AuditEntry` when impersonated, `TrainingBooked` on the outbox. Without `ringId` («Qualsevol») pick the first `FREE` ring by `Ring.order` then `name`, moving to the next one on retry; none free → `SLOT_TAKEN`. `override {limit, reason}` is accepted **only** with an impersonation token (else `422 OVERRIDE_NOT_ALLOWED`) and skips step 4 only — never the window and never the uniqueness; audit `TRAINING_BOOKED_BY_CLUB` with `reason`. T-09-07, T-09-11, T-09-14, T-09-15, T-09-16, T-09-17, T-09-18, T-09-23, T-09-32, T-09-33, T-09-34.
5. **Cancellation** (R-09-10, R-09-14): `POST /training-bookings/{id}/cancellation {reason?}` allowed while `now ≤ startsAt − training.cancelThresholdMinutes` (instant arithmetic, DST-proof), else `409 TRAINING_CANCEL_TOO_LATE {thresholdMinutes, minutesBefore}`; an ADMIN with an impersonation token may cancel late with a mandatory `reason` (`cancelReason = ADMIN_LATE`, audited `TRAINING_CANCELLED_BY_CLUB`); a booking block never prevents cancelling. The seat and the week counter are freed, `TrainingCancelled {…, by, cancelReason, late}` is emitted; a second call → `409 INVALID_STATE`, the same `Idempotency-Key` → the same 200. Service methods for the system paths, callable inside another transaction and **not scheduled here**: `cancelFutureByMember(memberId, by, cancelReason)` (S15 P5c, E8), `cancelForInactivity(memberId, from, to)` (S13 `InactivityResolved`, E8), `cancelForDog(dogId)` (`DogDeactivated`) and `cancelByClub(bookingIds, cancelReason)` (the `TrainingConflictPort` half, step 7) → `CANCELLED_BY_CLUB` with `cancelReason ∈ RING_BLOCK · CLASS_CONFLICT · RING_NOT_RESERVABLE`. T-09-12, T-09-25, T-09-28.
6. **`TrainingOccupancyService`** (R-09-12) → the real `TrainingOccupancyPort`: intervals `{ringId, from, to, type: TRAINING|RING_BLOCK, reason, memberName?, dogName?, note?, id}` for a range; for a MEMBER viewer the result is redacted to `{type, reason}` (the grid cell reads «Ocupada» plus the lower-case reason, never who nor the note); INSTRUCTOR/ADMIN get names, reason and note. Training bookings occupy `training.slotMinutes`. Check against E4-T03's `DayGridQuery` that cells render as specified for both views and that nothing leaks in `view=member`. T-09-19, T-09-27.
7. **`TrainingConflictService`** (R-09-13) → the real `TrainingConflictPort`: `findActiveBookings(ringId, from, to)` used by S05 (deactivating a ring or clearing `allowsFreeTraining`), S06 (creating/moving a class, blocking from D4 or screen 24) and S07 (activity publication); live bookings → the caller answers `422 RING_HAS_BOOKINGS {bookings[]}` unless an ADMIN sends `cancelBookings: true`, in which case `cancelByClub` runs **inside the caller's transaction** (`Propagation.MANDATORY`) with the right `cancelReason` and emits `TrainingCancelled{by: ADMIN, origin: BACKOFFICE}`. Verify end to end with E4-T03's `RingBlockService` and E4-T04's activity publication. T-09-20, T-09-21, T-09-28.
8. **`/ring-blocks` behaviour that belongs to S09** (R-09-11, on top of E4-T03's implementation — do not duplicate it): `kind = RESERVATION` requires `FREE_TRAINING` (`404 MODULE_DISABLED` with the module off, `BLOCK` still works); the reason combinations (`RESERVATION` → `PRIVATE_CLASS`, `THERAPY`, `PREPARATION`, `OTHER`; `BLOCK` → `MAINTENANCE`, `OTHER`; `ACTIVITY` only through S07); `from/to` aligned to `classes.slotMinutes` with `to − from ≥ training.slotMinutes`; horizon `ringBlocks.maxHorizonDays` (60); a live training booking in the window → `422 RING_HAS_BOOKINGS`. If E4-T03 already covers a rule, say so in the report instead of re-implementing it; add only the missing half and the tests. T-09-13, T-09-19, T-09-26.
9. **Consumers** (idempotent by `eventId`, consumer name `training`): `ClassSessionCreated/Updated`, `ClassCancelledByClub`, `ClassAutoCancelled`, `WeekGenerated`, `WeekValidated`, `RingBlockCreated/Cancelled/Updated`, `ActivityPublished/Cancelled` → invalidate the grid cache of the affected days; `ParameterChanged{training.*, club.openingHours, club.holidays, bookings.weekOpensAt, bookings.limitUnit}`, `RingChanged`, `ClubModulesChanged` → invalidate the whole club cache; `DogFreeTrainingChanged`, `DogLevelChanged`, `LevelChanged` → no state (eligibility is computed); `WeekOpened`, `TrainingCounterReset` → invalidate the summary cache only (**nothing to reset**: the counter is per session week). T-09-29, T-09-35.
10. **Notifications in the dispatcher** (`notifications.N-06`, `notifications.N-07`, `notifications.N-47`; idempotent per (`eventId`, recipient, channel), deterministic ids): **N-06** on `TrainingBooked` → MEMBER (the booker) APP, variables `dog_name`, `date`, `time` («8:30–9:00»), `ring_name`; **N-07** on `TrainingCancelled` except `cancelReason = MEMBER_LEFT` → MEMBER APP; **N-47** on `TrainingBooked/TrainingCancelled` with `origin = BACKOFFICE` or `by ≠ MEMBER` (category CLUB_CHANGES) → MEMBER APP + EMAIL + **SMS intent** (`Notification` row `channel = SMS`, `status = QUEUED`, ≤ 160 GSM-7, or `SKIPPED_MODULE_OFF`), variables plus `admin_text`; when N-47 applies, N-07 is **not** also sent. Back keys `notif.N-06.*`, `notif.N-07.*`, `notif.N-47.*` (+ `.sms`) in ca/es/en, recipient's locale, club time zone. §13-6's assumption («meanwhile N-07») is superseded: N-47 is in the closed catalog, so use it — record that in the report.
11. **Reads and lists**: `GET /training-bookings/{id}` (member own, instructor, admin); `GET /me/training-bookings` with `cancellableUntil` per row (screen 03's TRAINING rows come from here — E5-T06 wires the port); `GET /training-bookings` + `/export` = the universal list of `CONVENCIONS_API` §4 with the fields E5-T01 declared (ADMIN RW, INSTRUCTOR R, MEMBER 403, undeclared filter → `400 INVALID_FILTER`); implement E3-T04's `TrainingBookingsQuery` for the dashboard. T-09-24, T-09-27, T-09-30.
12. **Module and tenant** (§9, T-09-31): `FREE_TRAINING` off → `/training-slots`, `/training-bookings*`, `/me/training-*` all `404 MODULE_DISABLED`, `POST /ring-blocks {kind: RESERVATION}` 404 while `{kind: BLOCK}` still returns 201, `TrainingOccupancyPort` returns blocks only, no `TrainingCounterReset` effect. `FAMILY_GROUP` off → own dogs only. `COURSES` off → no `setup`. `INACTIVITY` off → no period check. Cross-tenant 404 on every route; MEMBER on `POST /ring-blocks` → 403; INSTRUCTOR booking for a dog that is not theirs → 404.

## Fixed conventions
- Statuses (catalog rule 0, literal; the catalog file wins over S09 §6): `SLOT_TAKEN`, `TRAINING_LIMIT_REACHED`, `RING_BLOCK_CONFLICT`, `CLASS_CONFLICT`, `STALE_VERSION`, `INVALID_STATE`, `IDEMPOTENCY_KEY_REUSED` **409** · `DOG_NOT_ALLOWED`, `RING_NOT_RESERVABLE`, `SLOT_OUT_OF_WINDOW`, `CLUB_CLOSED`, `MEMBER_NOT_ACTIVE`, `BOOKING_BLOCKED`, `INACTIVITY_PERIOD`, `TRAINING_CANCEL_TOO_LATE`, `RING_HAS_BOOKINGS`, `RING_BLOCK_MANAGED_BY_ACTIVITY`, **`SLOT_NOT_ON_GRID`** (S09 writes 400) and **`DOG_ALREADY_BOOKED`** (S09 writes 409 — rule 0 counts only the `ALREADY_` *prefix*) and **`OVERRIDE_NOT_ALLOWED`** (S09 writes 403) **422** · `INVALID_TIME_RANGE`, `VALIDATION_ERROR`, `INVALID_FILTER` **400** · `MODULE_DISABLED`, `NOT_FOUND`, `DOG_NOT_ACCESSIBLE` **404**. E5-T01 already wrote the proposals to move the three surprising ones to §1: do not change them again here.
- Assumptions in force (do not ask): the weekly counter is by **session date** (B15, R-09-05); `DRAFT` classes block slots (§13-2); the grid is a fixed 30-minute grid anchored at the opening hour, also on screen 24 (§13-3); `slotId = "{ringId}_{startsAt ISO UTC}"` is a documented exception to the UUID convention and `training_slots` is not created (§13-9); `training.freeTrainingRequiresLicense` is informative and never validated (§13-10); a slot that has already started is never bookable, for anyone (§13-11); the morning/afternoon split at 14:00 is a front-end constant, not a parameter (§13-12); the desktop «Entrenaments» screen is the universal list of `/training-bookings` + `/ring-blocks` (§13-8).
- Every mutation is a Mongo transaction with the outbox inside it and is retried whole on `DuplicateKey`/`WriteConflict`; notifications only from outbox consumers; `Clock` and `ClubClock` injected (the E5-T01 ArchUnit rule); instants UTC on the wire, business dates club-local.
- Context direction (ArchUnit): `clubs.training` may import `clubs.bookings.application`, `clubs.scheduling.application`, `clubs.catalogs.application`, `clubs.census.application`, `platform`, `shared`; nothing imports `clubs.training` except through the ports declared in `clubs.scheduling.application.ports`, `clubs.dashboard.application.ports` and `clubs.bookings.application.ports`.
- Closed catalogs: no new parameter / event / notification / error code / audit action; proposals to the report and `MESSAGES.md` (S09 §13-7 lists several that are already in the catalogs — verify before proposing). Evidence rule: report = command + exit code + last 40 lines; long outputs → `roadmap/evidence/E5-T04/NN-<name>.log`; task file ≤ 120 KB; fictional data only (`@example.test`); no secrets; never edit other tasks, `ROADMAP.md` or the Organizer verification section.

## Verification (run all; evidence = command + exit code + last 40 lines; long outputs → `roadmap/evidence/<ID>/*.log`)
- `./mvnw -q verify` green with the tests named after T-09-01…T-09-23 and T-09-25…T-09-35 (T-09-01…T-09-15 as unit tests with a `MutableClock`, two time zones `Europe/Madrid` and `America/Argentina/Buenos_Aires`; T-09-32…T-09-34 as real-Mongo concurrency tests), `E5ContractIT` updated (the S09 routes no longer answer 501), `ArchitectureTest`, `AuditContractTest` (both training actions covered), `EventCatalogContractTest`, message parity ca/es/en for `notif.N-06.*`, `notif.N-07.*`, `notif.N-47.*` (+ `.sms`) and every new `error.*`, coverage gates.
- OpenAPI snapshot byte-identical to a fresh `bin/openapi-snapshot` (or a `docs/openapi/CHANGELOG.md` entry for any field you had to add); `bin/e4-smoke` still exits 0 (the real occupancy/conflict ports must not break the E4 scenario); `git diff --check` clean.

## Definition of Done
The grid of a real club day is computed from opening hours, holidays, classes, blocks and bookings with the right DST instants; twenty parallel bookings of one capacity-1 slot leave exactly one `ACTIVE`; the third booking of a week is refused with the cancellable ones listed; «Qualsevol» assigns rings deterministically in catalog order; a late cancellation is refused for the member and allowed, audited, for an impersonating admin; the day grids and the conflict checks of S05/S06/S07 run against real training data instead of null objects.

## Evidence to return
`verify` tail; the T-09 method list; the concurrency test output (counts per scenario); a `curl` sequence on the local stack: `GET /me/training-summary` → `GET /training-slots` for the window → `POST /training-bookings` without `ringId` → the slot turns `BOOKED/OWN_TRAINING` → a second dog on the same slot → `409 SLOT_TAKEN {freeRings}` → `POST /ring-blocks` over the booking → `422 RING_HAS_BOOKINGS` → the same with `cancelBookings: true` → booking `CANCELLED_BY_CLUB` + N-47 rows → cancellation past the threshold → `409 TRAINING_CANCEL_TOO_LATE` (ids truncated); the `GET /day-grid` output for both views over the same day.

---

## Executor report
Session 2026-09-24 (claude:claude-opus-5-5). Status: all Verification commands pass. Dependencies E5-T01 and E5-T02 were `awaiting_verification` when I started (allowed by the protocol). If either one gets `changes_requested`, this task must be re-checked against it.

### Branch / commits
No git writes (the sandbox forbids them). The work is in the working tree on `main`; the publish script commits it after the session. I claim no CI result.

### Evidence (full command outputs)

**Earlier failing attempts (one line each):**
- Attempt 1, `./mvnw -q clean verify` → exit 1. Three integration tests failed, plus the stale OpenAPI snapshot. (a) `E5ContractIT`: INSTRUCTOR got 403 on `/training-bookings/export`. The shared `ExportPolicy` (S14 R-14-12) allows ADMIN only, so I made the route ADMIN-only and updated the E5 route fixture. (b) `PlatformIT.T_02_01`: my `training` consumers read the event type from the JSON, but `ParameterChanged` does not serialise it. The resulting NPE stalled the outbox for later consumers; each handler now passes its own type. (c) `OpenApiSnapshotTest`: fixed by `bin/openapi-snapshot`. Excerpt: `roadmap/evidence/E5-T04/01a-verify-attempt1-failures.log`.
- Attempt 2, `./mvnw -q clean verify` → exit 1. All tests passed, but the JaCoCo gate failed: `clubs.training.application` had 79.7 % branch coverage against an 80 % minimum. I added `TrainingApplicationTest` (retry policy, N-06/N-07/N-47 routing, reason mapping) and tightened `TrainingIT`. Excerpt: `02a-verify-attempt2-coverage.log`.
- Between attempts, the first curl rehearsal showed that the APP-content allowlist in `NotificationRepository` dropped the catalog variable `time`. I added `time` and `has_admin_text`, and `T_09_16` now asserts `time = "8:30–9:00"`.

**1. `./mvnw clean verify`** → exit **0**. Full log: `roadmap/evidence/E5-T04/01-verify.log`. Surefire: `Tests run: 451, Failures: 0, Errors: 0, Skipped: 0`. Failsafe: `Tests run: 689, Failures: 0, Errors: 0, Skipped: 0`. `All coverage checks have been met.` The last 40 lines begin with the harmless Mongo monitor noise from a stopped Testcontainers node:
```
	at java.base/java.util.Optional.orElseThrow(Optional.java:403) ~[na:na]
	at com.mongodb.internal.connection.SocketStream.open(SocketStream.java:85) ~[mongodb-driver-core-5.5.2.jar:na]
	at com.mongodb.internal.connection.InternalStreamConnection.open(InternalStreamConnection.java:233) ~[mongodb-driver-core-5.5.2.jar:na]
	at com.mongodb.internal.connection.DefaultServerMonitor$ServerMonitor.lookupServerDescription(DefaultServerMonitor.java:219) ~[mongodb-driver-core-5.5.2.jar:na]
	at com.mongodb.internal.connection.DefaultServerMonitor$ServerMonitor.run(DefaultServerMonitor.java:176) ~[mongodb-driver-core-5.5.2.jar:na]
Caused by: java.net.ConnectException: Connection refused
	at java.base/sun.nio.ch.Net.pollConnect(Native Method) ~[na:na]
	at java.base/sun.nio.ch.Net.pollConnectNow(Net.java:694) ~[na:na]
	at java.base/sun.nio.ch.NioSocketImpl.timedFinishConnect(NioSocketImpl.java:542) ~[na:na]
	at java.base/sun.nio.ch.NioSocketImpl.connect(NioSocketImpl.java:592) ~[na:na]
	at java.base/java.net.SocksSocketImpl.connect(SocksSocketImpl.java:327) ~[na:na]
	at java.base/java.net.Socket.connect(Socket.java:751) ~[na:na]
	at com.mongodb.internal.connection.SocketStreamHelper.initialize(SocketStreamHelper.java:76) ~[mongodb-driver-core-5.5.2.jar:na]
	at com.mongodb.internal.connection.SocketStream.initializeSocket(SocketStream.java:104) ~[mongodb-driver-core-5.5.2.jar:na]
	at com.mongodb.internal.connection.SocketStream.open(SocketStream.java:79) ~[mongodb-driver-core-5.5.2.jar:na]
	... 3 common frames omitted

[INFO] Tests run: 9, Failures: 0, Errors: 0, Skipped: 0, Time elapsed: 10.00 s -- in com.agilityhub.core.migration.PlayoffMigrationIT
[INFO] 
[INFO] Results:
[INFO] 
[INFO] Tests run: 689, Failures: 0, Errors: 0, Skipped: 0
[INFO] 
[INFO] 
[INFO] --- failsafe:3.5.6:verify (default) @ agilityhub-core-api ---
[INFO] 
[INFO] --- jacoco:0.8.14:report (coverage) @ agilityhub-core-api ---
[INFO] Loading execution data file /Users/jordib/dev/agilityhub/agilityhub-core-api/target/jacoco.exec
[INFO] Analyzed bundle 'AgilityHub Core API' with 1593 classes
[INFO] 
[INFO] --- jacoco:0.8.14:check (coverage) @ agilityhub-core-api ---
[INFO] Loading execution data file /Users/jordib/dev/agilityhub/agilityhub-core-api/target/jacoco.exec
[INFO] Analyzed bundle 'agilityhub-core-api' with 1593 classes
[INFO] All coverage checks have been met.
[INFO] ------------------------------------------------------------------------
[INFO] BUILD SUCCESS
[INFO] ------------------------------------------------------------------------
[INFO] Total time:  03:11 min
[INFO] Finished at: 2026-09-24T03:31:20+02:00
[INFO] ------------------------------------------------------------------------
```
Named suites in that run: `ArchitectureTest` (6), `ArchitectureRulesTest`, `ClockRuleTest`, `AuditContractTest` (both training actions covered by `TrainingIT.T_09_23`), `EventCatalogContractTest` (2), `MessageParityTest` (ca/es/en parity for `notif.N-06.*`, `notif.N-07.*`, `notif.N-47.*` incl. `.sms`; no new `error.*`), `E5ContractIT` (38, the S09 routes are no longer 501) and `CatalogsIT` (19). All pass.

**2. T-09 method list and coverage**: `python3 roadmap/evidence/E5-T04/test-summary.py` → exit 0. Full output: `02-test-summary.log`. `TrainingRulesTest` has 14 methods: T-09-01, 03, 04, 05, 06, 07, 08, 09, 10, 11, 12, 14 and 15 with a `MockClock` in Madrid and Buenos Aires, plus an SMS test. `TrainingApplicationTest` has 4. `TrainingIT` has 21 (T-09-02…31 and T-09-35). `TrainingConcurrencyIT` has 3 (T-09-32…34). Coverage per package (lines / branches): training.domain 100 / 96.7 %, training.application 99.5 / 83.9 %, training.api 96.2 / 80.8 %, census.application 98.8 / 81.8 %, catalogs.application 99.4 / 90.8 %, scheduling.application 98.7 / 86.5 %, bookings.application 99.4 / 84.9 %, messaging.application 99.0 / 87.7 %.

**3. Concurrency output** (real Mongo replica set, from `01-verify.log`):
```
T-09-32 capacity 1: {201=1, 409 SLOT_TAKEN=19}
T-09-32 capacity 2: {201=2, 409 SLOT_TAKEN=18}
T-09-33 Rock at 2/3, five parallel slots: {201=1, 409 TRAINING_LIMIT_REACHED=4}
T-09-33 Dog.trainingSeq after the run: 3
T-09-34 round 0 («Qualsevol» ×3): {201=3} rings [s09-r-car, s09-r-cen, s09-r-mun]
T-09-34 round 1 («Qualsevol» ×3): {201=3} rings [s09-r-car, s09-r-cen, s09-r-mun]
T-09-34 round 2 («Qualsevol» ×3): {201=3} rings [s09-r-car, s09-r-cen, s09-r-mun]
```
In the capacity-2 scenario, the seat indexes of the two ACTIVE bookings are {0, 1}. In T-09-33, the refused attempts' `$inc` rolled back, so `trainingSeq` is 3.

**4. OpenAPI snapshot**: `cp docs/openapi/openapi.json target/openapi-before.json` → `bin/openapi-snapshot` (exit 0; log `03-openapi-snapshot.log`, last line `Updated docs/openapi/openapi.json`) → `cmp docs/openapi/openapi.json target/openapi-before.json` → exit **0**, so the fresh run is byte-identical. Against `HEAD`, `git diff --stat -- docs/openapi/openapi.json` shows `1 file changed, 8 insertions(+), 8 deletions(-)`. The changes are only the 8 S09 operation descriptions: «Contract only; 501» removed, plus the export's ADMIN-only role. No schema, field or parameter changed, so there is no `docs/openapi/CHANGELOG.md` entry and nothing for the web to regenerate.

**5. `bin/e4-smoke > roadmap/evidence/E5-T04/05-e4-smoke.log 2>&1`** → exit **0** (110 lines). It runs on the real occupancy/conflict ports. Last 40 lines:
```
curl GET /api/v1/public/canic/activities -> 403 INVALID_API_KEY
curl GET /api/v1/public/canic/activities -> 200
PASS GET /public/canic/activities: 403 without key; 200 with key (masked) · places.free=9; no member*/phone/person keys; registrant name and phone absent
curl POST /api/v1/activities/<id>/cancellation -> 200
curl GET /api/v1/activity-registrations/<id> -> 200
PASS POST /activities/{id}/cancellation: 200 · registration CANCELLED{ACTIVITY_CANCELLED}; N-32c rows
curl POST /api/v1/ring-blocks/<id>/cancellation -> 200
PASS POST /ring-blocks/{smoke}/cancellation: 200 · smoke block released
PASS bin/core activities:finish-ended --club=canic: exit 0
PASS bin/core scheduling:finish-ended --club=canic: exit 0

SUMMARY (step · status · key values)
  seed club:apply + seed:demo --seed=42 (x2)                     · second runs 0 changes · week start 2026-09-21
  GET /week-templates                                            · 200 · 3 templates; Setmana B inconsistencyCount=1
  POST /weeks/{week+3}/generation (Setmana B)                    · 422 TEMPLATE_INCONSISTENT
  GET /weeks/generation-candidates                               · 200 · proposed=2026-10-12 (week +3)
  POST /weeks/{week+3}/generation (A + Dissabtes)                · 200 · classCount=38, skipped=[]
  GET /weeks/{week+3}/calendar?filter=DRAFT                      · 200 · draftCount=38, canValidate=true
  member GET /day-grid (draft week)                              · 200 · 0 classes
  POST /weeks/{week+3}/validation                                · 200 · validatedClassIds=38
  day-grid member / instructor                                   · 200 · member 7 classes without occupancy/instructor; instructor sees occupancy
  POST /class-sessions/{Wed 18:50 B+C}/cancellation without text · 422 ADMIN_TEXT_REQUIRED
  POST /class-sessions/{Wed 18:50 B+C}/cancellation              · 200 CANCELLED · affectedBookings=4, affectedWaitlist=2
  outbox ClassCancelledByClub                                    · committed · event 787d095e…: affected=4, waitlistIds=2
  notifications N-08a                                            · 6 APP + 6 EMAIL (mailbox) + 6 SMS QUEUED · 4 bookings + 2 waitlist
  POST /ring-blocks over an ACTIVE class                         · 409 RING_BLOCK_CONFLICT
  POST /ring-blocks on a free slot                               · 201 · member grid OCCUPIED, instructor grid BLOCK
  GET /activities/{id}/ring-conflicts                            · 200 · 1 CLASS conflict (Sat 18:30 Central)
  POST /activities/{id}/publication                              · 409 RING_BLOCK_CONFLICT
  POST /activities/{id}/publication {cancelClasses}              · 200 PUBLISHED · ringBlockIds=1; class CANCELLED{ACTIVITY}
  GET /weeks/{week+3}/calendar                                   · 200 · activity block with activityTitle
  member GET /me/activities                                      · 200 · rowState=OPEN
  member POST /activity-registrations                            · 201 ACTIVE
  GET /public/canic/activities                                   · 403 without key; 200 with key (masked) · places.free=9; no member*/phone/person keys; registrant name and phone absent
  POST /activities/{id}/cancellation                             · 200 · registration CANCELLED{ACTIVITY_CANCELLED}; N-32c rows
  POST /ring-blocks/{smoke}/cancellation                         · 200 · smoke block released
  bin/core activities:finish-ended --club=canic                  · exit 0
  bin/core scheduling:finish-ended --club=canic                  · exit 0
PASS E4 backend gate: week generated from A/B + Saturdays and validated; class with fictional registrants cancelled with ClassCancelledByClub + N-08a; published activity blocks a ring and is listed for the member
Removed disposable Compose services, volumes and private mailbox; existing stacks preserved
```

**6. `git diff --check`** → exit **0**, no output. It checks tracked files only. The new files were checked separately with `grep -rnE "[[:blank:]]+$"` over the new sources, tests and script, which found nothing.

**7. Local-stack curl sequence (Evidence to return)**: `python3 roadmap/evidence/E5-T04/curl-sequence.py > roadmap/evidence/E5-T04/04-curl-sequence.log 2>&1` → exit **0**. It runs the final jar on a disposable Mongo with `club:apply seeds/club-canic.yaml` + `seed:demo --seed=42`. Full log: `04-curl-sequence.log` (81 lines; ids truncated, no token). Steps 1–5 (the first 41 lines of the log):
- summary: `defaultDogId`, `limitUnit DOG`, eligible «Suro 190» (LEVEL, E) + «Bruc 189» (owner Laia's group)
- slots: window 4 days, rings Muntanya/Central/Carretera, 30 slots per day
- `POST /training-bookings` without `ringId` → 201 Muntanya, counter 1/3, `TrainingBooked` + N-06
- the cell turns `BOOKED/OWN_TRAINING`
- a second dog on the same ring → `409 SLOT_TAKEN {freeRings: [2 rings]}`

Last 40 lines:
```
  details {"ringId": "665165a6…", "startsAt": "2026-09-25T05:00:00Z", "reason": "TRAINING", "freeRings": ["1d15867a…", "ad48a326…"]}

6. the instructor blocks the ring over the booking
curl POST /api/v1/ring-blocks -> 422 RING_HAS_BOOKINGS   [instructor]
  details {"bookings": [{"id": "bd1118c9…", "ringId": "665165a6…", "from": "2026-09-25T05:00:00Z", "to": "2026-09-25T05:30:00Z", "memberName": "Laia", "dogName": "Suro 190"}]}

7. the admin repeats it with cancelBookings: true
curl POST /api/v1/ring-blocks -> 201   [admin]
  block c4601e1a… 07:00–08:00 BLOCK/MAINTENANCE
curl GET /api/v1/training-bookings/bd1118c9… -> 200   [member reads the booking]
  booking {"state": "CANCELLED_BY_CLUB", "cancelledBy": "ADMIN", "cancelReason": "RING_BLOCK"}
  outbox TrainingCancelled PUBLISHED bd1118c9… {"trainingBookingId":"bd1118c9…","slotId":"665165a6-1f61-4f03-a09d-01a6938d20e6_2026-09-25T05:00:00Z","ringId":"665165a6…","memberId":"0dceff33…","dogId":"d7283096…","origin":"BACKOFFICE","by":"ADMIN","cancelReason":"RING_BLOCK","late":false}
  outbox RingBlockCreated PUBLISHED c4601e1a… {"blockId":"c4601e1a…","ringId":"665165a6…","from":{"$date":"2026-09-25T05:00:00Z"},"to":{"$date":"2026-09-25T06:00:00Z"},"reason":"MAINTENANCE"}
  outbox NotificationQueued PUBLISHED 1b5c1347…:0dceff33…:app {"notificationId":"1b5c1347…:0dceff33…:app","channel":"EMAIL"}
  outbox NotificationQueued PUBLISHED 1b5c1347…:0dceff33…:email {"notificationId":"1b5c1347…:0dceff33…:email","channel":"EMAIL"}
  outbox NotificationSent PUBLISHED 1b5c1347…:0dceff33…:email {"notificationId":"1b5c1347…:0dceff33…:email","channel":"EMAIL"}
  outbox NotificationQueued PUBLISHED 1b5c1347…:0dceff33…:sms {"notificationId":"1b5c1347…:0dceff33…:sms","channel":"EMAIL"}
  notification N-47 APP SENT account 40f1adb4… {"club_name":"Club Agility Cànic","dog_name":"Suro 190","entityId":"bd1118c9…","action":"OPEN_BOOKING","admin_text":"","date":"divendres, 25 de setembre de 2026","ring_name":"Muntanya","change":"CANCELLED","time":"7:00–7:30","has_admin_text":false}
  notification N-47 EMAIL SENT account 40f1adb4…
  notification N-47 SMS QUEUED account 40f1adb4… sms(128): Club Agility Canic: el club ha anul lat l'entrenament de Suro 190 del divendres, 25 de setembre de 2026 de 7:00-7:30 (Muntanya).

8. a cancellation past the threshold
curl POST /api/v1/training-bookings -> 201   [book another slot]
  booking 34dfde80… 09:00 Muntanya cancellableUntil 2026-09-25T05:00:00Z
  outbox TrainingBooked PUBLISHED 34dfde80… {"trainingBookingId":"34dfde80…","slotId":"665165a6-1f61-4f03-a09d-01a6938d20e6_2026-09-25T07:00:00Z","ringId":"665165a6…","memberId":"0dceff33…","dogId":"d7283096…","origin":"APP"}
  outbox NotificationQueued PUBLISHED b04af955…:0dceff33…:app {"notificationId":"b04af955…:0dceff33…:app","channel":"EMAIL"}
  notification N-06 APP SENT account 40f1adb4… {"club_name":"Club Agility Cànic","dog_name":"Suro 190","entityId":"34dfde80…","date":"divendres, 25 de setembre de 2026","ring_name":"Muntanya","time":"9:00–9:30"}
curl POST /api/v1/test/clock -> 200   [test clock]
  test clock -> 2026-09-25T05:01:00Z (club-local Fri 25-09 07:01)
curl POST /api/v1/training-bookings/34dfde80…/cancellation -> 422 TRAINING_CANCEL_TOO_LATE   [member, 119 min before]
  details {"minutesBefore": 118, "thresholdMinutes": 120}

9. GET /day-grid for 2026-09-25, both views
curl GET /api/v1/day-grid -> 200   [view=member]
curl GET /api/v1/day-grid -> 200   [view=instructor]
  view=member: [{"time": "07:00", "ringId": "665165a6…", "kind": "OCCUPIED", "reason": "MAINTENANCE", "endTime": "08:00"}, {"time": "09:00", "ringId": "665165a6…", "kind": "OCCUPIED", "reason": "TRAINING", "endTime": "09:30"}]
  view=instructor: [{"time": "07:00", "ringId": "665165a6…", "kind": "BLOCK", "reason": "MAINTENANCE", "note": "Reg de la pista (fictici)", "blockId": "c4601e1a…", "endTime": "08:00"}, {"time": "09:00", "ringId": "665165a6…", "kind": "TRAINING", "reason": "TRAINING", "who": ["Laia + Suro 190"], "trainingBookingIds": ["34dfde80…"], "endTime": "09:30"}]

PASS free-training curl sequence
Removed the disposable Mongo container and runtime directory
```
Notes on this log:
- `minutesBefore` is 118, not 119. The local test clock keeps ticking after it is set to T−119 min, and a few seconds passed before the call. The value is floored whole minutes; T-09-12 asserts 119 exactly with a fixed clock.
- The `"channel":"EMAIL"` in `NotificationQueued` payloads is pre-existing behaviour of `SystemNotificationService`. It is not from this task.
- The same N-47 flow runs in `TrainingIT.T_09_20_T_09_21`: APP:SENT, EMAIL:SENT, SMS:QUEUED, and no N-07.

**Commands denied by the sandbox** (none blocked a Verification item):
- `shasum -a 256 docs/openapi/openapi.json` and `git hash-object docs/openapi/openapi.json`. I replaced them with `cp` + `cmp`.
- A `python3 - <<'EOF'` heredoc to edit the message bundles. I used the Edit tool instead.
- A `./mvnw … > /tmp/…` redirection. I ran it with a pipe instead.

### Files changed
- **New, `clubs/training`**:
  - `domain/{TrainingGrid, TrainingRules, TrainingWeek, TrainingSms, TrainingForeignEvent}`
  - `application/{TrainingContext, TrainingTransactions, TrainingActor, TrainingEvents, TrainingAudit, TrainingEligibilityService, TrainingGridCache, TrainingSlotService, TrainingBookingService, TrainingQueryService, TrainingOccupancyService, TrainingConflictService, TrainingNotifications, TrainingConsumers, TrainingAutoConfiguration}`
  - `application/ports/RingSetupPort`
- **Changed, `clubs/training`**: `api/TrainingController` (501 stubs → services), `application/TrainingContractAccess`, `persistence/TrainingBookingRepository` (queries + CAS update).
- **Other contexts (new boundaries only)**:
  - `census/application/TrainingMemberAccess` (new)
  - `census/persistence/CensusRepository.increment`
  - `scheduling/application/RingScheduleAccess` (new)
  - `catalogs/application/PlanningCatalogAccess.{trainingRings, trainingLevels}`
  - `catalogs/persistence/MongoUsageCounter` (`state = ACTIVE`, the E5-T01 handoff)
  - `bookings/application/BookingContext.weekSpan` (reuses `BookingWeeks`)
  - `bookings/application/BookingQueryService.dogInClass`
  - `bookings/persistence/BookingRepository.liveOverlapping`
  - `messaging/application/SystemEmailRenderer` (N-47 sendable)
  - `messaging/persistence/NotificationRepository` (`time`, `has_admin_text` in the APP variables allowlist)
  - `platform/application/audit/AuditAction` (+`TRAINING_BOOKED_BY_CLUB`, `TRAINING_CANCELLED_BY_CLUB`, already in `AuditActionName`/S14)
  - `shared/api/IdempotencyFilter` (training routes in the retried branch)
- **Resources**: `META-INF/spring/…AutoConfiguration.imports`; `messages_{ca,es,en}.properties` (`notif.N-06.*`, `notif.N-07.*`, `notif.N-47.*` + `.sms`).
- **Tests**:
  - New: `training/domain/TrainingRulesTest`, `training/application/TrainingApplicationTest`, `training/api/{TrainingFixtures, TrainingIT, TrainingConcurrencyIT}`.
  - Updated: `E5ContractIT` (S09 routes implemented), `EventCatalogContractTest` (new envelope), `CatalogsIT` (`training_bookings.state`), `fixtures/contracts/e5-routes.json` (export ADMIN only).
- **Docs and config**: `docs/openapi/openapi.json` (8 descriptions), `CHANGELOG.md`, `.gitignore` (E5-T04 evidence), `roadmap/evidence/E5-T04/*`, this report, `roadmap/MESSAGES.md`.

### Rules and tests implemented (R-xx-nn / T-xx-nn)
**Rules:**
- R-09-01: `TrainingRules.canFreeTrain`, `TrainingEligibilityService.eligibleDogs` / `membersWithRight`.
- R-09-02: bookable rings and `capacity` (ring-scoped parameter).
- R-09-03: computed grid, holidays, DST, `TrainingGridCache`.
- R-09-04: window (MEMBER clip) and `startsAt > now`.
- R-09-05: session-week counter via `BookingWeeks`.
- R-09-06: seven-step transaction, seat index, `trainingSeq`.
- R-09-07: «Qualsevol».
- R-09-08: member conditions.
- R-09-09: `lastDogForTraining`, `defaultDogId`.
- R-09-10: cancellation, ADMIN_LATE.
- R-09-11: RESERVATION needs FREE_TRAINING. Already covered by E4-T03 (reason combinations, alignment, `ringBlocks.maxHorizonDays`, RING_HAS_BOOKINGS, 403 for an instructor with `cancelBookings`). I added only the real port behind it and the tests.
- R-09-12: `TrainingOccupancyService` with MEMBER redaction. The day grid was checked in both views.
- R-09-13: `TrainingConflictService`, `cancelByClub` with MANDATORY propagation.
- R-09-14: system cancellation methods. They are not scheduled here.
- R-09-15: `setup` via `RingSetupPort`.
- R-09-16: origin, impersonation, `override`, audit.
- §7 consumers (`training*` beans) and §8 N-06, N-07, N-47.

**Tests:**
- T-09-01, 03, 04, 05, 06, 07, 08, 09, 10, 11, 12, 14 and 15 are unit tests (`TrainingRulesTest`, with a `MockClock` in two zones).
- T-09-02, 03/04/22, 06/27, 07/14/15, 08/35, 10, 11, 12/25, 13/19/26, 16, 17, 18, 20/21, 23, 24/30, 27, 28, 29 (two methods) and 31 are HTTP/real-Mongo tests in `TrainingIT`.
- T-09-32, 33 and 34 are in `TrainingConcurrencyIT`.
- T-09-16/21 (routing), 28 (reason mapping) and 32 (retry policy) are also in `TrainingApplicationTest`.
- T-09-24 is also in `E5ContractIT` (contract + INVALID_FILTER).
- T-09-36 is E8, as the task says.

### Assumptions
1. **Error statuses follow the fixed conventions (catalog rule 0), not S09 §6.** These are 422: `SLOT_NOT_ON_GRID`, `DOG_ALREADY_BOOKED`, `OVERRIDE_NOT_ALLOWED`, `TRAINING_CANCEL_TOO_LATE`, `RING_BLOCK_MANAGED_BY_ACTIVITY`, `RING_HAS_BOOKINGS`.
2. **T-09-20.** S09 expects `409 RING_BLOCK_CONFLICT {TRAINING}`. I implemented `422 RING_HAS_BOOKINGS {bookings[]}`, as step 8 / R-09-13 and E4-T03 already do. The ADMIN `cancelBookings: true` path covers T-09-21.
3. **T-09-13.** A misaligned `from/to` answers E4-T03's `400 INVALID_SLOT_GRANULARITY` (S06 catalog), and a block shorter than 30 min answers `400 INVALID_TIME_RANGE`. I did not change E4-T03.
4. **T-09-28 / S05.** S05 R-05-08 / T-05-28 (verified) keeps `409 RING_IN_USE {futureTrainingBookings}` when clearing `allowsFreeTraining` with live bookings. I did not add `cancelBookings` to `PATCH /rings`. The `RING_NOT_RESERVABLE` cancellation is implemented and tested at the port (`TrainingConflictPort.cancelByClub`), and the S05 counter now reads the real `state` (see question 1).
5. **N-47 applies when `origin = BACKOFFICE` or `by = ADMIN`.** SYSTEM cancellations are excluded. `INACTIVITY` sends N-07 (R-09-14) and `MEMBER_LEFT` sends nothing. When N-47 applies, N-07 is not sent. N-06 is sent for every booking (catalog: «qualsevol origen»), also when N-47 applies. This supersedes §13-6's «meanwhile N-07», because N-47 is now in the closed catalog.
6. **N-47 extra variables.** It carries `change` (BOOKED / CANCELLED), like N-36, plus `admin_text` (the cancellation note, if any) and the ICU select `has_admin_text`.
7. **Training week of the summary.** For `date = today` it is the week of now (T-09-35). For another date it is the week containing that date's local midnight. On the opening Sunday, a date before 20:00 reads the ending week.
8. **R-09-05 example.** The example books on Monday 12-10, but 12-10 is the T-09-04 holiday. The test uses Sunday 11-10 20:30 instead (next week, counted by session date).
9. **`POST /training-bookings` with an INSTRUCTOR token** returns 403 (the E5-T01 MEMBER-only guard). The «404 for a dog that is not theirs» of T-09-30 is tested with a MEMBER: `404 DOG_NOT_ACCESSIBLE`.
10. **`/training-bookings/export` is ADMIN-only**, following S14 R-14-12 and the shared `ExportPolicy`. The E5-T01 contract said INSTRUCTOR too. The guard, the description and the E5 route fixture now say ADMIN.
11. **`RingSetupPort`** is a null object until S16 (courses) exists. No `ring_setups` collection is read yet. T-09-22 uses a test implementation.
12. **Ring-scoped parameter.** `training.capacityPerRingSlot` with a ring override is read with `scopeRef = ringId`.
13. **Counter unit MEMBER** counts the booking member (`TrainingBooking.memberId`, per S09 §3), not the dog's owner. The `$inc` goes on that member.
14. **Cache and invalidation.**
    - `WeekOpened` / `TrainingCounterReset`: nothing to reset and no summary cache (the summary is computed live).
    - Invalidated by whole club: week, block cancel/update and activity events.
    - Invalidated by day: class events and `RingBlockCreated`.
    - The in-memory cache is per JVM. Other replicas rely on the 60 s TTL. The booking path never reads the cache.
15. **Lock lanes.** Training mutations run in their own transaction with in-process lanes (dog, member, slot instant), like S08. They do **not** take the catalog reference lock. The ring-block and class callers take it themselves.

### Questions / catalog proposals
1. **S05 vs S09 (T-09-28).** Should clearing `allowsFreeTraining` / deactivating a ring offer `cancelBookings: true` (S09 R-09-13, `422 RING_HAS_BOOKINGS`)? Or does S05's `409 RING_IN_USE` stand? Meanwhile S05 is unchanged. Adding it needs a catalogs-side port (training → catalogs is the allowed direction) and a new `RingPatch` field.
2. **Catalog N-47 row.** Proposal: list `admin_text` (S09 §8) and a created/cancelled discriminator (`change`, as asked for N-36 in E5-T02), or split the row.
3. **Export roles.** Confirm that `/training-bookings/export` is ADMIN-only (S14 R-14-12) and fix the S09 §6 / E5-T01 wording («ADMIN RW, INSTRUCTOR R» only covers the list).
No new parameter, event, notification or error code was introduced. The two audit actions were already in S14 R-14-09 and `AuditActionName`.

## Organizer verification
_(pending)_
