---
id: E5-T09
title: Review follow-ups for S09/S15/S06 and seeds (jobs, training, dashboard, demo seed) + the organizer rulings that need code (error statuses of CATALEG_ERRORS §1, S05 ring «no reservable» per R-09-13)
stage: E5
repo: agilityhub-core-api
thread: A
order: 90
status: verified
depends_on: [E5-T01, E5-T05, E5-T08]
branch:
pr:
updated: 2026-09-24
---

# E5-T09 · Review follow-ups for S09/S15/S06 + seeds, and rulings that need code

## Objective
Close the minor findings the organizer accepted from the independent reviews of E4-T05, E5-T01, E5-T04 and E5-T05 (`roadmap/reviews/`), and implement two organizer rulings of 2026-09-24 (MESSAGES) that change code. Read each finding in its review first: it has the file, the scenario and a suggested fix.

## Spec references
S05 R-05-08 / T-05-28 (amended 2026-09-24), S09 R-09-13 / T-09-28, S15 (R-15-xx cited by each finding), S14 R-14-12, `docs/specs/00-transversal/CATALEG_ERRORS.md` §1 and rule 0.

## Context to load
This file · `roadmap/reviews/E4-T05-*.md`, `roadmap/reviews/E5-T01-*.md`, `roadmap/reviews/E5-T04-*.md`, `roadmap/reviews/E5-T05-*.md` · the files each finding names · `seeds/README.md`.

## Steps
1. **Ruling — explicit HTTP statuses** (your E5-T01 proposal 2, accepted): `OVERRIDE_NOT_ALLOWED` → 403, `JOB_UNKNOWN` → 404, `SLOT_NOT_ON_GRID` → 400 (S09 §6 and S15 §6 already show these statuses). Change `ErrorCode` **and**, in the same change, add the three codes to §1 of the repo copy `docs/specs/00-transversal/CATALEG_ERRORS.md` so the error-catalog contract tests stay green (you are authorized to edit that section of that file for this; the organizer copies it to the source documentation at verification). Update the tests and the OpenAPI descriptions; regenerate the snapshot.
2. **Ruling — S05 R-05-08 follows R-09-13**: turning `allowsFreeTraining` off (or deactivating a ring) with live training bookings answers `409 RING_HAS_BOOKINGS {bookings[]}` unless the ADMIN sends `cancelBookings: true`, which cancels them as `CANCELLED_BY_CLUB` / `RING_NOT_RESERVABLE` in the same transaction (S09 R-09-13, T-09-28). `RING_IN_USE` stays for the other R-05-07 cases. Update T-05-28 (S05 text amended by the organizer).
3. **E4-T05 #1** — `seeds/README.md` says that demo phone numbers are real-format numbers and that SMS must never be sent from a non-production stack (E7-T02 adds the `SMS_ALLOWED_NUMBERS` allow-list).
4. **E4-T05 #2** — an ArchUnit rule: only `Demo*` seed classes may call `DemoSeedActor` (or it checks the `local`/`test` profile itself).
5. **E4-T05 #6** — the demo planning can be refreshed on a long-lived stack (staging demos): an option of `seed:demo` that re-anchors the planning weeks to the run date (idempotent per run date), documented in `seeds/README.md`.
6. **E4-T05 #8** — `ClassSessionService.bookingCounters` is restricted to the S08 writer path (package-private port or an ArchUnit rule) and rejects `booked > capacity`.
7. **E5-T01 #4** — `jobs.tick.overrun` measures overruns (a tick that starts after the previous one should have ended).
8. **E5-T01 #5** — a club or job with no run history has a defined baseline (document it in the report; test).
9. **E5-T01 #6** — `BookingContractAccess` ownership also accepts the family-group responsible where S08 lets them act (cite the rule) — or document why not.
10. **E5-T01 #7** — a dry run takes no lease (no write to `job_locks`).
11. **E5-T04 #5** — the MEMBER occupancy view carries no block id.
12. **E5-T04 #6** — an impersonating admin can cancel late until the rule in S09 says (cite it); test at `now ≥ endsAt`.
13. **E5-T04 #7, #9, #10** — day-level invalidation for `RingBlockCreated`; the weak assertions listed in #9; the API contract notes of #10 in the report.
14. **E5-T05 #6** — P1 invalidates the grids in its own transaction whatever `FREE_TRAINING` is (R-15-11 step 1).
15. **E5-T05 #10** — `ForeignEvent` cannot be published by mistake (it does not implement `DomainEvent`, or an ArchUnit rule forbids publishing it).

## Fixed conventions
AGENTS.md rules · the only documentation you edit is the §1 table of `docs/specs/00-transversal/CATALEG_ERRORS.md` (step 1) · no new parameters, events, notifications or codes.

## Verification (run all; evidence = command + exit code + last 40 lines; long outputs → `roadmap/evidence/E5-T09/*.log`)
- `./mvnw -q clean verify` → exit 0.
- `bin/openapi-snapshot` and `git diff --stat docs/openapi/` (say whether the web must adopt it).
- `bin/core seed:demo` twice on a fresh stack, and once with the re-anchor option (step 5).

## Definition of Done
Every step done with a test named after the spec id where one applies; the report lists each step with the file and the test; `clean verify` green.

## Evidence to return
Report sections below; the step → test table; the `CATALEG_ERRORS.md` diff of step 1.

---

## Executor report
Executor: claude:claude-opus-5-5 · 2026-09-24 · one session. Dependencies E5-T01, E5-T05 and E5-T08 were `awaiting_verification`
or `verified` when this task started (E5-T08 awaiting: this task edits none of its files' behaviour).

### Branch / commits
None (git writes are blocked in this sandbox). The publish script commits the working tree on `main` after the session.

### Evidence (full command outputs)
Logs in `roadmap/evidence/E5-T09/` (committed; the `.gitignore` rule `!roadmap/evidence/E5-*/*.log` covers them).
Intermediate runs (one line each): `01-targeted-its.log` — the 11 touched IT classes, 139 tests, 1 expected failure
(`T_09_28` still asserted the old `RING_IN_USE`) → rewrote it as `T_09_28_T_05_28_…`; `02-trainingit.log` — `TrainingIT` 22/22 green.
A first full `clean verify` (same code, exit status not captured) was replaced by the final run below.

**1. `./mvnw -q clean verify`** → **exit 0** (`MVNW_EXIT_0`). Full output `04-clean-verify.log` (5262 lines). Totals from the
surefire/failsafe XML (`05-test-summary.log`, produced by `roadmap/evidence/E5-T01/test-summary.py`): **unit/contract 467 tests,
0 failures · integration 751 tests, 0 failures**; jacoco `check` passed (it is part of verify). Touched packages
(`06-coverage-summary.log`, from `target/site/jacoco/jacoco.csv`):
```text
platform.application.jobs        lines  95.7%  branches  85.1%
clubs.catalogs.application       lines  98.8%  branches  90.9%
clubs.training.application       lines  99.5%  branches  83.9%
clubs.scheduling.application     lines  98.9%  branches  85.5%
clubs.bookings.application       lines  99.3%  branches  84.2%
clubs.common.application         lines  98.8%  branches  84.7%
clubs.census.application         lines  98.8%  branches  82.3%
shared.application               lines  98.4%  branches  91.7%
clubs.catalogs.api               lines 100.0%  branches  95.2%
clubs.training.api               lines  96.2%  branches  80.8%
```
Last 40 lines of `04-clean-verify.log` (with `-q` Maven prints nothing on success; this is the last IT's Spring log, lines cut at 160 chars):
```text
 \\/  ___)| |_)| | | | | || (_| |  ) ) ) )
  '  |____| .__|_| |_|_| |_\__, | / / / /
 =========|_|==============|___/=/_/_/_/

 :: Spring Boot ::               (v3.5.16)

2026-09-24T10:28:06.781+02:00  INFO 94244 --- [agilityhub-core-api] [           main] c.a.core.migration.PlayoffMigrationIT    : Starting PlayoffMigrationIT usi
2026-09-24T10:28:06.781+02:00  INFO 94244 --- [agilityhub-core-api] [           main] c.a.core.migration.PlayoffMigrationIT    : The following 1 profile is acti
2026-09-24T10:28:06.912+02:00  INFO 94244 --- [agilityhub-core-api] [           main] .s.d.r.c.RepositoryConfigurationDelegate : Bootstrapping Spring Data Mongo
2026-09-24T10:28:06.928+02:00  INFO 94244 --- [agilityhub-core-api] [           main] .s.d.r.c.RepositoryConfigurationDelegate : Finished Spring Data repository
2026-09-24T10:28:06.954+02:00  INFO 94244 --- [agilityhub-core-api] [           main] org.mongodb.driver.client                : MongoClient with metadata {"dri
2026-09-24T10:28:06.956+02:00  INFO 94244 --- [agilityhub-core-api] [localhost:49615] org.mongodb.driver.cluster               : Monitor thread successfully con
2026-09-24T10:28:07.405+02:00  INFO 94244 --- [agilityhub-core-api] [           main] c.a.core.migration.PlayoffMigrationIT    : Started PlayoffMigrationIT in 0
E5-T07 outbox backlog discarded before PlayoffMigrationIT: 163 PENDING records (more than one dispatch() batch)
E5-T07 outbox backlog discarded before PlayoffMigrationIT: 167 PENDING records (more than one dispatch() batch)
E5-T07 outbox backlog discarded before PlayoffMigrationIT: 163 PENDING records (more than one dispatch() batch)
E5-T07 outbox backlog discarded before PlayoffMigrationIT: 163 PENDING records (more than one dispatch() batch)
E5-T07 outbox backlog discarded before PlayoffMigrationIT: 163 PENDING records (more than one dispatch() batch)
2026-09-24T10:28:15.907+02:00  INFO 94244 --- [agilityhub-core-api] [localhost:50755] org.mongodb.driver.cluster               : Exception in monitor thread whi

com.mongodb.MongoSocketOpenException: Exception opening socket
	at com.mongodb.internal.connection.SocketStream.lambda$open$0(SocketStream.java:85) ~[mongodb-driver-core-5.5.2.jar:na]
	at java.base/java.util.Optional.orElseThrow(Optional.java:403) ~[na:na]
	at com.mongodb.internal.connection.SocketStream.open(SocketStream.java:85) ~[mongodb-driver-core-5.5.2.jar:na]
	at com.mongodb.internal.connection.InternalStreamConnection.open(InternalStreamConnection.java:233) ~[mongodb-driver-core-5.5.2.jar:na]
	at com.mongodb.internal.connection.DefaultServerMonitor$ServerMonitor.lookupServerDescription(DefaultServerMonitor.java:219) ~[mongodb-driver-core-5.5.2.jar:na
	at com.mongodb.internal.connection.DefaultServerMonitor$ServerMonitor.run(DefaultServerMonitor.java:176) ~[mongodb-driver-core-5.5.2.jar:na]
Caused by: java.net.ConnectException: Connection refused
	at java.base/sun.nio.ch.Net.pollConnect(Native Method) ~[na:na]
	at java.base/sun.nio.ch.Net.pollConnectNow(Net.java:694) ~[na:na]
	at java.base/sun.nio.ch.NioSocketImpl.timedFinishConnect(NioSocketImpl.java:542) ~[na:na]
	at java.base/sun.nio.ch.NioSocketImpl.connect(NioSocketImpl.java:592) ~[na:na]
	at java.base/java.net.SocksSocketImpl.connect(SocksSocketImpl.java:327) ~[na:na]
	at java.base/java.net.Socket.connect(Socket.java:751) ~[na:na]
	at com.mongodb.internal.connection.SocketStreamHelper.initialize(SocketStreamHelper.java:76) ~[mongodb-driver-core-5.5.2.jar:na]
	at com.mongodb.internal.connection.SocketStream.initializeSocket(SocketStream.java:104) ~[mongodb-driver-core-5.5.2.jar:na]
	at com.mongodb.internal.connection.SocketStream.open(SocketStream.java:79) ~[mongodb-driver-core-5.5.2.jar:na]
	... 3 common frames omitted

E5-T07 outbox backlog discarded before PlayoffMigrationIT: 163 PENDING records (more than one dispatch() batch)
```
(The `MongoSocketOpenException` is the driver monitor of a Testcontainers Mongo that was already stopped; it is logged at INFO and is not a failure.)

**2. `bin/openapi-snapshot`** → **exit 0** (`SNAPSHOT_EXIT_0`, log `03-openapi-snapshot.log`, last line `Updated docs/openapi/openapi.json`);
the final `clean verify` ran `OpenApiSnapshotTest` green on the same snapshot. `git diff --stat docs/openapi/`:
```text
 docs/openapi/CHANGELOG.md | 14 ++++++++++++++
 docs/openapi/openapi.json | 36 ++++++++++++++++++++----------------
 2 files changed, 34 insertions(+), 16 deletions(-)
```
Changes: `RingPatch.cancelBookings` (new optional boolean) and the `PATCH /rings/{id}` description + its `422` now documented as
`RING_HAS_BOOKINGS`; `JOB_UNKNOWN` moves from the 422 to the 404 response of the five `/jobs/{name}*` and
`/platform/clubs/{clubId}/jobs/{name}/trigger` operations; `POST /training-bookings`: 400 adds `SLOT_NOT_ON_GRID`, 403 is
`OVERRIDE_NOT_ALLOWED`, the `override` description says 403. **The web must adopt it**: regenerate the types (`cancelBookings`)
and, on D16, handle `RING_HAS_BOOKINGS` with a confirmation that resends `cancelBookings: true` (entry in `docs/openapi/CHANGELOG.md`).

**3. `bin/core seed:demo` twice on a fresh stack, then with `--reanchor`** → `python3 roadmap/evidence/E5-T09/cli-seed.py`,
**exit 0** (`HELPER_EXIT_0`). It starts a disposable `mongo:7` replica set (local profile, db `e5_cli`, generated
`SEED_PASSWORD`/`OIDC_MASTER_KEY` never printed), runs `bin/core` on the jar built by the verify, compares the full Mongo
document snapshot around the idempotent runs and removes the container. The CLI runs on the real clock (run date 2026-09-24,
Monday 2026-09-21), so the re-anchor passes `--week-start=2026-10-05`, the Monday a run two weeks later would compute.
Transcript `10-cli-seed-run.log` (complete, 40 lines); per-command logs `05-club-apply.log` … `09-seed-demo-reanchor-again.log`:
```text
Disposable Mongo 7 replica set on a random localhost port, database e5_cli, local profile; run date 2026-09-24, run Monday 2026-09-21, re-anchor week start 2026-10-05; generated credentials private.
$ bin/core club:apply seeds/club-canic.yaml -> exit 0 (log 05-club-apply.log)
+ catalogs: 31 catalog changes
+ pages: 3 page changes
15 changes (applied)
$ bin/core seed:demo --club=canic --seed=42 -> exit 0 (log 06-seed-demo-first.log)
{activeMembers=184, pendingMembers=3, inactiveMembers=4, leftMembers=3, members=194, dogs=245, familyGroups=12, activeDogs=242, pendingDogs=3, instructors=3, administrators=2, receivedDocuments=6, pendingDocuments=239}
451 changes (demo seed)
{templateClasses=39, templates=3, weeks=3, generatedClasses=93, validatedWeeks=2, looseClasses=1, ringBlocks=1, scenarioRingBlocks=0, riskCancellations=1, classBookings=23, classWaitlist=3, activities=4, publishedActivities=3, activityRegistrations=44, activityWaitlist=2, activityFiles=2, scenarioActivityRegistrations=0, scenarioClassBookings=0, scenarioCancellations=0, scenarioWaitlist=0, scenarioOffers=0, scenarioRiskExempt=0, scenarioBookingBlocks=0, scenarioTrainingOverrides=0, scenarioTrainingBookings=0}
224 changes (demo planning, week start 2026-09-21)
$ bin/core seed:demo --club=canic --seed=42 -> exit 0 (log 07-seed-demo-second.log)
{activeMembers=184, pendingMembers=3, inactiveMembers=4, leftMembers=3, members=194, dogs=245, familyGroups=12, activeDogs=242, pendingDogs=3, instructors=3, administrators=2, receivedDocuments=6, pendingDocuments=239}
0 changes (demo seed)
{templateClasses=39, templates=3, weeks=3, generatedClasses=93, validatedWeeks=2, looseClasses=1, ringBlocks=1, scenarioRingBlocks=0, riskCancellations=1, classBookings=23, classWaitlist=3, activities=4, publishedActivities=3, activityRegistrations=44, activityWaitlist=2, activityFiles=2, scenarioActivityRegistrations=0, scenarioClassBookings=0, scenarioCancellations=0, scenarioWaitlist=0, scenarioOffers=0, scenarioRiskExempt=0, scenarioBookingBlocks=0, scenarioTrainingOverrides=0, scenarioTrainingBookings=0}
0 changes (demo planning, week start 2026-09-21)
PASS second seed:demo run: 0 changes and complete Mongo document snapshot unchanged
Weeks after the first run:
2026-09-21 VALIDATED 17 classes, 0 bookings
2026-09-28 GENERATED 38 classes, 0 bookings
2026-10-05 VALIDATED 39 classes, 23 bookings
$ bin/core seed:demo --club=canic --seed=42 --reanchor --week-start=2026-10-05 -> exit 0 (log 08-seed-demo-reanchor.log)
{activeMembers=184, pendingMembers=3, inactiveMembers=4, leftMembers=3, members=194, dogs=245, familyGroups=12, activeDogs=242, pendingDogs=3, instructors=3, administrators=2, receivedDocuments=6, pendingDocuments=239}
0 changes (demo seed)
{keptWeeks=1, weeks=2, generatedClasses=76, validatedWeeks=1, looseClasses=1, ringBlocks=1, riskCancellations=1, classBookings=23, classWaitlist=3}
109 changes (demo planning, week start 2026-10-05)
Weeks after the re-anchor:
2026-09-21 VALIDATED 17 classes, 0 bookings
2026-09-28 GENERATED 38 classes, 0 bookings
2026-10-05 VALIDATED 39 classes, 23 bookings
2026-10-12 GENERATED 38 classes, 0 bookings
2026-10-19 VALIDATED 39 classes, 23 bookings
$ bin/core seed:demo --club=canic --seed=42 --reanchor --week-start=2026-10-05 -> exit 0 (log 09-seed-demo-reanchor-again.log)
{activeMembers=184, pendingMembers=3, inactiveMembers=4, leftMembers=3, members=194, dogs=245, familyGroups=12, activeDogs=242, pendingDogs=3, instructors=3, administrators=2, receivedDocuments=6, pendingDocuments=239}
0 changes (demo seed)
{keptWeeks=1, weeks=2, generatedClasses=76, validatedWeeks=1, looseClasses=1, ringBlocks=1, riskCancellations=1, classBookings=23, classWaitlist=3}
0 changes (demo planning, week start 2026-10-05)
PASS re-anchor: new weeks generated once; the second re-anchor on the same week reports 0 changes and the snapshot is unchanged
Removed the disposable Mongo container and runtime directory
```

**Step 1 — `CATALEG_ERRORS.md` §1 diff** (the only documentation edited; codes appended to the 400/403/404 rows):
```diff
-| 400 | … `INPUT_SCHEMA_MISMATCH`, `MAPPING_INVALID` |
+| 400 | … `INPUT_SCHEMA_MISMATCH`, `MAPPING_INVALID`, `SLOT_NOT_ON_GRID` (S09 R-09-03: hora no alineada a la graella; afegit 24-09, E5-T09) |
-| 403 | … `READ_ONLY`, `INVALID_API_KEY` |
+| 403 | … `READ_ONLY`, `INVALID_API_KEY`, `OVERRIDE_NOT_ALLOWED` (S09 R-09-16: `override` sense token d'impersonació; afegit 24-09, E5-T09) |
-| 404 | … `TEMPLATE_CLUB_NOT_FOUND`, `CLUB_NOT_FOUND` |
+| 404 | … `TEMPLATE_CLUB_NOT_FOUND`, `CLUB_NOT_FOUND`, `JOB_UNKNOWN` (S15 §6: `{name}` de procés desconegut; afegit 24-09, E5-T09) |
```

### Files changed
- Step 1: `shared/domain/ErrorCode.java` (`JOB_UNKNOWN` 404, `OVERRIDE_NOT_ALLOWED` 403, `SLOT_NOT_ON_GRID` 400) · `docs/specs/00-transversal/CATALEG_ERRORS.md` §1 ·
  `training/api/TrainingRequests.java` (description) · tests `ErrorCatalogContractTest`, `E5ContractIT`, `JobsApiIT`, `TrainingIT`.
- Step 2: new `clubs/catalogs/application/RingTrainingBookings.java` (port) and `clubs/training/application/RingReservability.java`
  (S09 adapter, bean in `TrainingAutoConfiguration`) · `CatalogService.update` (`stopsBeingReservable`) · `CatalogRequests.RingPatch.cancelBookings` ·
  `CatalogsController` (`RING_HAS_BOOKINGS`, description) · tests `CatalogsIT`, `TrainingIT`.
- Step 3/5: `seeds/README.md`, `README.md` · `DemoSeedCommand` (`--reanchor`), `DemoPlanningService`, `shared/application/DemoSeedStep`
  (`Input.reanchor`, `reanchors()`), `DemoPlanningSeeder`, `DemoBookingSeeder` · test `DemoPlanningSeedIT`.
- Step 4/6/15: `src/test/.../arch/ArchitectureRules.java` (`DEMO_SEED_ACTOR`, `COUNTER_WRITERS`, `CONSUMER_ENVELOPES`), `ArchitectureTest`,
  new `SeedAndWriterRulesTest` · `ClassSessionBookingAccess.counters` (capacity guard) · test `BookingsIT`.
- Step 7/8/10: `SchedulerTick`, `JobMetrics`, `JobLockRepository.expireAt`, `JobRunner` (dry run without lease, reaping of dry runs),
  `TestNoopJob.duringNextPlan` · test `JobFrameworkIT`.
- Step 11/12/13: `TrainingOccupancyService`, `DayGridQuery`, `TrainingBookingService.cancel` · tests `TrainingIT`, `TrainingConcurrencyIT`.
- Step 14: new `clubs/common/application/ClubGridCaches.java`, `TrainingGridCache implements ClubGridCaches`, `WeekOpeningJob` · test `WeekOpeningJobIT`.
- Step 15: `bookings/domain/ForeignEvent`, `common/domain/ForeignEvent`, `training/domain/TrainingForeignEvent` (no longer `DomainEvent`),
  `shared/application/DomainEventHandler` (bound relaxed), `OutboxDispatcher` · new test support `support/TestEvent.java`; `BookingsIT`,
  `WaitlistIT`, `TrainingIT` publish `TestEvent` instead of an envelope · `EventCatalogContractTest` (three envelope samples removed).
- `docs/openapi/openapi.json`, `docs/openapi/CHANGELOG.md`, `CHANGELOG.md`, `roadmap/evidence/E5-T09/*`.

### Rules and tests implemented (R-xx-nn / T-xx-nn)
| Step | Change | Test |
|---|---|---|
| 1 Error statuses | `ErrorCode` + §1 | `ErrorCatalogContractTest.E0_T04_…`, `T_08_47_T_09_24_T_15_29_bookingTrainingAndJobErrorsFollowTheCatalogStatuses`; `E5ContractIT` (404 `JOB_UNKNOWN` ×3), `JobsApiIT.T_15_08…`, `TrainingIT.T_09_07_T_09_14_T_09_15…` (400), `TrainingIT.T_09_23…` (403) |
| 2 R-05-08 → R-09-13 | `CatalogService.stopsBeingReservable`, `RingReservability` | `TrainingIT.T_09_28_T_05_28_conflictsSeeLiveBookingsAndARingThatStopsBeingReservableCancelsThemOnlyOnRequest` (422 + details, nothing changes, `cancelBookings` → `CANCELLED_BY_CLUB/RING_NOT_RESERVABLE`, `TrainingCancelled{by: ADMIN}`, slots gone; deactivation too); `CatalogsIT.T_05_05_T_05_12_…` |
| 3 E4-T05 #1 | `seeds/README.md` (real-format numbers, never SMS from non-prod, `SMS_ALLOWED_NUMBERS` in E7-T02) | — (docs) |
| 4 E4-T05 #2 | ArchUnit `DEMO_SEED_ACTOR` | `ArchitectureTest.E5_T09_onlyDemoSeedClassesRunAsTheSeedActor`, `SeedAndWriterRulesTest.E5_T09_onlyDemoSeedClassesMayRunAsTheSeedActor` |
| 5 E4-T05 #6 | `seed:demo --reanchor` | `DemoPlanningSeedIT.T_06_28_reanchorRefreshesThePlanningWeeksToTheRunDateOncePerWeek`; CLI evidence 3 above |
| 6 E4-T05 #8 | `counters` refuses `booked > capacity` on a raise; ArchUnit `COUNTER_WRITERS` | `BookingsIT.E5_T09_theCounterWriterRefusesABookedCountAboveTheCapacity`, `ArchitectureTest.E5_T09_onlyTheBookingWritersSetTheClassCounters`, `SeedAndWriterRulesTest.E5_T09_classCountersAreWrittenOnlyByTheBookingContext` |
| 7 E5-T01 #4 (R-15-01) | overrun = minute ticks skipped by a long tick; renew after 30 s, settle at the end | `JobFrameworkIT.T_15_01_aTickThatLastsPastTheNextMinuteIsAnOverrunAndSettlesItsLease`, `T_15_01_tickRunsEveryClubInCatalogOrderOncePerMinute` (same-minute contention no longer counted) |
| 8 E5-T01 #5 (R-15-05) | baseline documented (Assumption 5), no code change | `JobFrameworkIT.T_15_05_aClubWithNoRunHistoryStartsFromItsLatestOccurrenceOnly`, `T_15_05_missedWindowSkipsAndAlerts` |
| 9 E5-T01 #6 | already done since E5-T02 (Assumption 6) | `BookingsIT.T_08_26_rolesTenantAndOwnershipPerEndpoint` (Joan reads Laura's booking; 404 with `FAMILY_GROUP` off) |
| 10 E5-T01 #7 (R-15-08) | dry run takes no lease | `JobFrameworkIT.T_15_06_dryRunRecordsOnlyThePlanAndThePlanMatchesTheRealEffects` (`job_locks` now inside the "nothing written" comparison, dry run while a real run holds the lease, dead dry run reaped after 300 s) |
| 11 E5-T04 #5 (R-09-12) | MEMBER intervals carry no id; `DayGridQuery` de-duplicates by ring + time | `TrainingIT.T_09_13_T_09_19_T_09_26_…` (all MEMBER ids null), `TrainingIT.T_09_27_…` (no block id, one cell per block in both views) |
| 12 E5-T04 #6 (R-09-10, R-09-16) | no end to the admin's late cancellation | `TrainingIT.T_09_23_…` (cancel at `now = endsAt` → 200 `ADMIN_LATE`; member 422; already cancelled → 409) |
| 13 E5-T04 #7 | per-day `RingBlockCreated` proven with a real event | `TrainingIT.T_09_29_aRealRingBlockEventInvalidatesOnlyTheDaysOfTheBlock` |
| 13 E5-T04 #9 | weak assertions | `TrainingIT.T_09_13_…` asserts exactly `INVALID_SLOT_GRANULARITY`; `TrainingConcurrencyIT.T_09_34_…` keeps 4 reservable rings: 3 parallel «Qualsevol» take exactly MUN/CEN/CAR (never CAD), the 4th takes CAD, the 5th `SLOT_TAKEN` |
| 13 E5-T04 #10 | contract notes (Assumption 10) | — |
| 14 E5-T05 #6 (R-15-11 step 1) | P1 drops the grid caches itself (`ClubGridCaches`) | `WeekOpeningJobIT.T_15_11_withoutTheNoticeOrFreeTrainingOnlyTheOpeningHappens` (FREE_TRAINING off: the club's grid reloads, another club's is kept) |
| 15 E5-T05 #10 | envelopes are not `DomainEvent`s; ArchUnit `CONSUMER_ENVELOPES` | `ArchitectureTest.E5_T09_consumerEnvelopesAreNotPublishable`, `SeedAndWriterRulesTest.E5_T09_aConsumerEnvelopeThatIsADomainEventIsAViolation`, `ForeignEventTest`, `BookingPortsTest` |

### Assumptions
1. **`RING_HAS_BOOKINGS` stays 422.** The ruling and the amended S05 R-05-08/T-05-28 write «409», but `CATALEG_ERRORS.md` rule 0
   names `RING_HAS_BOOKINGS` explicitly as 422 (E4 opening, 16-09) and S06/S07/S09 already answer 422 with it. I kept one status
   for one code (catalog wins; step 1 authorized only the three codes). See Questions 1.
2. **Deactivation.** Following the ruling («or deactivating a ring»), `active: false` with live training bookings is also
   `RING_HAS_BOOKINGS` / `cancelBookings`; future live classes are checked first and stay `409 RING_IN_USE` (R-05-07). The live
   bookings are the S09 ACTIVE bookings starting after now, whatever `FREE_TRAINING` says (so they are never lost silently).
   The cancellation is `TrainingBookingService.cancelByClub(…, RING_NOT_RESERVABLE)`: `TrainingCancelled{by: ADMIN, origin:
   BACKOFFICE}` and N-47, inside the catalog transaction. `catalogs` cannot import `training` (training → catalogs exists), so the
   port lives in `catalogs.application` with a no-op fallback.
3. **Overrun (R-15-01).** `jobs.tick.overrun` is incremented by the tick that overran, by the number of minute ticks it made the
   scheduler skip (`floor((end − minute)/1 min)`): Spring's single scheduler thread does not fire them, and a tick still running
   after 30 s renews the `tick` lease before each club so no other instance starts one. At the end the lease is settled to
   `max(minute + 55 s, end)` (never released early, never held after the tick). Same-minute contention between instances is
   normal and no longer counted.
4. **Dry runs (R-15-08).** No lease is taken, so a dry run is neither blocked by a running execution (no more
   `JOB_ALREADY_RUNNING` for `dryRun: true`) nor blocks one. A dry run left `RUNNING` by a dead process is reaped once
   `startedAt + 300 s` (the lease time) has passed; it publishes nothing.
5. **Baseline with no run history (R-15-05).** No code change: the first tick of a club or process judges only the latest
   occurrence ≤ now, exactly like any tick (older ones are never enumerated): on time → `SCHEDULE`, inside the window →
   one `CATCH_UP`, outside → `SKIPPED{MISSED_WINDOW}` + `JobFailed` (N-42 at most once per process and local day). This is what
   the organizer accepted for E5-T05 (6); it is now pinned by `T_15_05_aClubWithNoRunHistoryStartsFromItsLatestOccurrenceOnly`.
6. **Family group (step 9).** Nothing to change: since E5-T02 the ownership check is `BookingQueryService.visible` /
   `WaitlistService.visible`, which accept every member of the actor's ACTIVE family group (`BookingMemberAccess.reachableMembers`,
   only with `FAMILY_GROUP`). Rules: S08 «Per a» («membres d'un grup familiar (reserven per a qualsevol gos del grup)»), R-08-02
   («Amb `FAMILY_GROUP`, qualsevol membre del grup reserva per a qualsevol gos del grup») and S08 §6 («MEMBER own or family
   group»). `BookingContractAccess` no longer carries an ownership guard.
7. **Counter guard (step 6).** `CLASS_FULL` only when a write *raises* `booked` above the capacity; a lower count is always
   written, so a class left over capacity by older data or a hand-lowered capacity can still release seats.
8. **Day invalidation (step 13).** Consumers read the outbox `eventJson`, where `RingBlockCreated.from/to` are ISO strings (the
   `{"$date"}` the review saw is the separate BSON `payload` copy), so the per-day path already worked; the new test proves it with a
   real `POST /ring-blocks`. A class date is not patchable (`ClassSessionService.patch`: ring, times, levels… never `date`), so a
   `ClassSessionUpdated` never moves a class to another day: no old day to invalidate.
9. **`T-09-13` misaligned block.** `POST /ring-blocks` is the S06 endpoint (R-06-11, «un sol endpoint»); a start off
   `classes.slotMinutes` answers `400 INVALID_SLOT_GRANULARITY` (S06 §6), while T-09-13 writes `INVALID_TIME_RANGE` for «no alineats».
   The test now asserts the code the code returns; see Questions 2.
10. **E5-T04 #10 contract notes.** (a) `/training-bookings/export` is ADMIN-only (S14 R-14-12), accepted by the organizer on
    2026-09-24. (b) The S05 vs T-09-28 gap is closed by step 2: the `RING_NOT_RESERVABLE` cancellation is now tested through
    `PATCH /rings/{id}`, not only at the port.
11. **Envelopes (step 15).** All three `*ForeignEvent` records (S08, S09, clubs.common) dropped `implements DomainEvent`;
    `DomainEventHandler<T>` no longer requires a `DomainEvent`. Tests that simulated another context's event by publishing an envelope
    now publish `support.TestEvent` (same JSON). `activities.domain.ActivityExternalEvent` is also a consumer envelope that implements
    `DomainEvent`; it is outside the review finding and unchanged (Questions 4).
12. **Re-anchor (step 5).** `--reanchor` needs the first run (same seed and file); anchor = club-local Monday of the run date
    (`--week-start` overrides). It reuses the D3 templates by name, keeps every existing week as it is, dates the loose classes, blocks
    and risk cancellations only into weeks it generated, books registrants only into ACTIVE classes of those weeks without
    registrants, and never re-applies the activities or the E5 scenario. Recorded as `demo_seed_runs` `<clubId>:planning:<weekStart>`.
13. **P1 grids (step 14).** The only grid cache is S09's `TrainingGridCache` (S06 day grids are read live; `TemplateQuery` caches
    templates, not grids). `clubs.common` cannot import `training` (training → common exists), hence the `ClubGridCaches` interface.

### Questions / catalog proposals
1. @organizer `RING_HAS_BOOKINGS`: S05 R-05-08/T-05-28 (amended) and the ruling say 409, the catalog (rule 0) says 422 and every
   endpoint answers 422. Keep 422 (and align the S05 wording), or add it to the 409 row of §1 for all endpoints? Meanwhile: 422.
2. @organizer T-09-13 «`from/to` no alineats → 400 `INVALID_TIME_RANGE`»: the S06 endpoint answers `INVALID_SLOT_GRANULARITY`
   (S06 R-06-11/§6). Align T-09-13 with S06? Meanwhile the test asserts `INVALID_SLOT_GRANULARITY`.
3. @organizer First deployment of a process (R-15-05 as written): every club gets `SKIPPED{MISSED_WINDOW}` + one N-42 for
   `week-opening` unless the deploy lands within 24 h of Sunday 20:00. If that alert is unwanted, a rule «no previous `JobRun` of
   the process in the club → record the missed baseline without `JobFailed`» would need a line in R-15-05. No change meanwhile.
4. @organizer `ActivityExternalEvent` (S07 consumer envelope) still implements `DomainEvent`; the same treatment as the
   `*ForeignEvent`s is a small follow-up if wanted.
No new parameter, event, notification or error code. The only catalog edit is the authorized §1 of `CATALEG_ERRORS.md` (diff above).

## Organizer verification
Result: verified · 2026-09-24 · organizer (Claude)

The 15 steps are done, each with a test (see the step → test table). The organizer checked the two rulings in the code:
- the `ErrorCode` statuses, and §1 of `CATALEG_ERRORS.md` in the same change;
- the S05 ring path through `RingReservability`.

`clean verify`: 467 + 751.

**CI.** The run of `cc21a4f` was cancelled by the next push. The runs of `538afd0`, `5765a72` and `28d35f8` failed only in impersonation ITs: `SigningKeys.ring()` ← the seed ITs that wiped `signing_keys`. The organizer read the log of `28d35f8`. E5-T07 round 2 fixed that cause, and the code is green at `dfcd02e`, `fe678fe` and `a202870`.

**Docs.** The organizer propagated the §1 edit of `CATALEG_ERRORS.md` to the source documentation, so `sync-docs` keeps it.

**Rulings on your questions:**
1. `RING_HAS_BOOKINGS` stays **422** (the catalog, rule 0). S05 R-05-07, R-05-08, T-05-12 and T-05-28 are amended, and DECISIONS E22 is corrected.
2. T-09-13 is aligned with S06: a misaligned block answers `INVALID_SLOT_GRANULARITY`.
3. Accepted: ruling **E33**. The first run of a process without history that falls outside its window records `SKIPPED{MISSED_WINDOW}` without an alert; S15 R-15-05 is amended → **E5-T13** step 1.
4. `ActivityExternalEvent` → **E5-T14** step 5.

**Independent review** `roadmap/reviews/E5-T09-20260924-1034-claude.md`:
- #1: the S05 path is not serialized with training bookings. It was assigned to E5-T07 round 2, point 3, and is verified there.
- #2: resolved by ruling 1 above.
- #3, #4, #7 and #10 → **E5-T14** steps 1–4.
- #5, #6 and #8 → **E5-T13** steps 8–10.
- #9: those documents were the organizer's own changes; the publish commit picked them up.
