---
id: E5-T03
title: Class waitlist (S08 WP-08-C): WaitlistEntry join / leave / claim with hold + 30 s confirmation, modes ALL_AT_ONCE and FIFO, SeatReleased and WaitlistExpired consumers, demotion of NOTIFIED entries, N-15 and N-46
stage: E5
repo: agilityhub-core-api
thread: A
order: 30
status: awaiting_verification
depends_on: [E5-T01, E5-T02]
branch:
pr:
updated: 2026-09-24
---

# E5-T03 · Class waitlist (S08 WP-08-C)

## Objective
Work package S08 WP-08-C: a member whose class is full joins the waitlist, is notified when a seat is released (everybody at once at the Cànic, or one at a time in FIFO clubs), takes the seat with the same hold + confirm flow as a normal booking, and leaves or is removed by the club. The two modes, the limits that decide who may join at all, the demotion of the notified entries when the seat is taken, and the `SeatReleased` / `WaitlistExpired` consumers are the deliverable. The FIFO expiry job that produces `WaitlistExpired` is E5-T05 (P6); the sweep of entries whose class has started is E6 (P8).

## Spec references
S08 R-08-12 (joining and its four limits), R-08-13 (`ALL_AT_ONCE`), R-08-14 (`FIFO`), R-08-15 (claim), R-08-16 (leaving and cancel reasons), R-08-11 (the `notifyWaitlist` flag this task consumes), R-08-08 (consolidation on a direct booking), R-08-21 (`cancelByClub`), R-08-23 (`Member.lastDogForClass` on `WaitlistJoined`), §3 (`WaitlistEntry` fields and indexes), §5 (the waitlist state machine and its transition rows), §6 (`/waitlist-entries*`, the `claim` skeleton, error list), §7 (emitted `WaitlistJoined`, `WaitlistLeft`, `WaitlistNotified`, `WaitlistConsolidated`; consumed `SeatReleased`, `WaitlistExpired`, `BookingCreated` that leaves `freeSeats = 0`), §8 (N-15, N-46), §9 (`waitlist.*` parameters, `WAITLIST` off behaviour), §13 items 3, 4, 5 · tests **T-08-08, T-08-19, T-08-20, T-08-21, T-08-22, T-08-30, T-08-34, T-08-45** · S15 R-15-16 (the FIFO process contract: `offerNext` must be idempotent) · `CATALEG_NOTIFICACIONS.md` N-15 (SMS enabled on the template, action `CLAIM_SEAT`) and N-46 · `CATALEG_ESDEVENIMENTS.md` waitlist rows + Annex A (`WaitlistNotified{mode}`) · `CATALEG_PARAMETRES.md` block «Llista d'espera» · `CATALEG_ERRORS.md` §1 + rule 0 · `docs/DECISIONS_PENDENTS.md` A20a, part C S08.

## Context to load
`docs/specs/S08-reserves-de-classes-i-llista-espera.md` §3, R-08-08, R-08-11…R-08-16, R-08-21, R-08-23, §5, §6, §7, §8, §9, §13 · `docs/specs/S15-processos-programats.md` R-15-16, R-15-18(a) only · `roadmap/tasks/E5-T01.md` (contract, `waitlist_entries` document and indexes) and `roadmap/tasks/E5-T02.md` (`BookingTransactions`, `SeatHoldService`, `BookingConfirmationService`, `BookingEligibility`, `BookingLimits`, `WaitlistConsolidationPort`, the `ClassBookingsPort` implementation whose `liveWaitlist`/waitlist half this task completes) · `src/main/java/com/agilityhub/core/clubs/bookings/**` · `clubs/scheduling/application/ports/ClassBookingsPort.java` · `clubs/messaging/application/SystemNotificationService.java`, `shared/application/{DomainEventHandler,OutboxDispatcher,EventPublisher}.java` · `platform/application/{ClubConfigService,ModuleGuard,RequiresModule}.java` · `docs/playbooks/new-event-consumer.md`, `new-endpoint.md` · this file.

## Steps
1. **`WaitlistService.join(classSessionId, dogId)`** (R-08-12, `@RequiresModule(Module.WAITLIST)`), one transaction with the `seat_locks` lock of the class: the class must be full **by bookings alone** (`ACTIVE` + `PAYMENT_PENDING` ≥ capacity, holds excluded) → else `422 CLASS_NOT_FULL`; the dog must have no live booking (`409 ALREADY_BOOKED`) and no live entry (`409 ALREADY_ON_WAITLIST`) for that class; the full `BookingEligibility` chain of E5-T02 runs (block, inactivity, member status, level, pack, `LEAVING`, `NOT_YET_OPEN`); then the four waitlist limits — live entries of the class < `waitlist.maxPerClass` (3) → `409 WAITLIST_LIMIT {scope: CLASS}`; live entries of the dog in the class's booking week < `waitlist.maxPerDogPerWeek` (2), or < `waitlist.maxPerDogPerWeekIfAttended` (1) when the dog already has a counted booking of that week with `classStartsAt ≤ now` or `CANCELLED_LATE` → `409 WAITLIST_LIMIT {scope: DOG_WEEK}`; and the entry must be *acceptable*: the week counter is below the limit **or** at least one `swappable` booking exists, else `409 BOOKING_LIMIT_REACHED` with the same `details.limit` shape as the hold. With `bookings.limitUnit = MEMBER` the «per dog» limits apply per owner. Persist `position = max + 1` (kept in both modes, used only by FIFO), `classStartsAt` and `bookingWeekKey` denormalized, write `Member.lastDogForClass` of the joiner, emit `WaitlistJoined`. T-08-08, T-08-19.
2. **Leaving** (R-08-16): `POST /waitlist-entries/{id}/cancellation` (member own or group; ADMIN too, from D4/D12) → `CANCELLED` with `cancelReason = MEMBER` or `ADMIN` + `WaitlistLeft`; only from `ACTIVE`/`NOTIFIED` (`422 WAITLIST_ENTRY_NOT_LIVE`). Service methods for the other reasons, called inside the callers' transactions and emitting nothing of their own: `cancelAll(classSessionId)` → `CANCELLED{CLASS_CANCELLED}` (completes `ClassBookingsPort.cancelAllByClub` of E5-T02, so S06's D4c flow and S15's risk review include the waiting members in `waitlistIds[]` and N-08a), `sweepStarted(now)` → `CANCELLED{CLASS_STARTED}`, silent (exposed as `WaitlistService.sweepStarted` for **E6**'s P8; do not schedule it here, state that in the report), `cancelByMember(memberId, reason)` → for S13/S15 leave handling (reason `MEMBER_LEFT`, a **catalog proposal** of S15 §13: use it and write the proposal in the report). Nothing is ever deleted (BR-12). T-08-19, T-08-25.
3. **`WaitlistService.offerSeats(classSessionId, freeSeats)`** — the `SeatReleased{notifyWaitlist = true}` consumer, idempotent per `eventId` (R-08-13, R-08-14): `waitlist.mode = ALL_AT_ONCE` → every `ACTIVE` entry of the class → `NOTIFIED` (`notifiedAt`, no `confirmBy`), one `WaitlistNotified {entryIds[], classId, mode}`; already `NOTIFIED` entries are **not** notified again. `FIFO` → the `freeSeats` lowest `position` `ACTIVE` entries → `NOTIFIED` with `confirmBy = min(now + waitlist.fifoConfirmMinutes, classStartsAt)`, one `WaitlistNotified` carrying `confirmBy` per entry. In both modes nothing happens when `minutesBefore ≤ waitlist.notifyThresholdMinutes` (the emitter already decides this, but re-check defensively) or when `WAITLIST` is off. `offerNext(classSessionId)` — the `WaitlistExpired` consumer (FIFO) — notifies the next entry while seats and time remain; two deliveries of the same `WaitlistExpired` must produce a single offer (T-08-34). T-08-20, T-08-21, T-08-45.
4. **Demotion** (R-08-13, `ALL_AT_ONCE` only): a consumer of `WaitlistConsolidated` and of `BookingCreated` that leaves `freeSeats = 0` turns the remaining `NOTIFIED` entries back into `ACTIVE`, keeping `notifiedAt` for the record, and produces **N-46** (MEMBER → APP only). A later release notifies them again. T-08-20, T-08-30.
5. **Claim** (R-08-15): `POST /seat-holds {classSessionId, dogId, waitlistEntryId}` is E5-T02's endpoint — add the checks it delegates here: the entry must be `NOTIFIED` (`422 WAITLIST_NOT_NOTIFIED`) and, in FIFO, `confirmBy > now` (`422 WAITLIST_OFFER_EXPIRED`); no seat available → `409 SEAT_TAKEN` (not `CLASS_FULL`); the waitlist limits do **not** apply to a claim, but BR-01 does: at the limit with a live swappable booking the hold answers `limit.swappable` and screen 06 proposes the swap, with none → `409 BOOKING_LIMIT_REACHED`. `POST /waitlist-entries/{id}/claim {seatHoldId, swapBookingId?}` with `Idempotency-Key`: one transaction that calls `BookingConfirmationService.confirm(seatHoldId, swapBookingId)` (same code path, same guarantees), asserts `hold.waitlistEntryId == entryId`, sets the entry to `CONSOLIDATED` with `bookingId`, emits `WaitlistConsolidated {entryId, bookingId}` and, in `ALL_AT_ONCE` with `freeSeats = 0`, demotes the rest (step 4). The 201 body is the same `Booking` schema as `POST /bookings`. T-08-22, T-08-30.
6. **Consolidation on a direct booking** (R-08-08): implement the `WaitlistConsolidationPort` E5-T02 declared — when a dog with a live entry books that class directly, the entry becomes `CONSOLIDATED` (`cancelReason = BOOKED_DIRECTLY` in the audit trail sense; the state is `CONSOLIDATED`, never `CANCELLED`) inside the confirmation transaction. T-08-19.
7. **Reads**: `GET /waitlist-entries/{id}` (member own, instructor, admin) with the class card, `position` (FIFO only), `confirmBy`, `state`; `GET /class-sessions/{id}/waitlist-entries` (instructor, admin) for 21/D4/D12 and for S06's cancellation preview; the `waiting` counter that `ClassSession.counters.waiting` carries is written in the same transactions as every state change (join, leave, consolidate, cancel-all) — never recomputed lazily.
8. **Notifications in the dispatcher** (`notifications.N-15`, `notifications.N-46`; idempotent per (`eventId`, recipient, channel), deterministic ids, same shape as E4-T03's `notifications.N-08a`): **N-15** «S'ha alliberat una plaça!» on `WaitlistNotified` → the notified members, APP + **SMS intent** (`Notification` row `channel = SMS`, `status = QUEUED`, body ≤ 160 GSM-7, or `SKIPPED_MODULE_OFF` when `SMS` is off; no sender until E7) + PUSH intent, variables `dog_name`, `class_date`, `class_time`, `confirm_by` (FIFO only), native action `CLAIM_SEAT`; exactly **one** N-15 per entry and offer even when several seats are released in the same event. **N-46** «La plaça ja s'ha ocupat» on demotion → APP only, variables `dog_name`, `class_date`, `class_time`. Back keys `notif.N-15.*` (+ `.sms`), `notif.N-46.*` in ca/es/en, rendered in the recipient's locale with the club time zone. T-08-20.
9. **Module and tenant behaviour** (§9): `WAITLIST` off → every `/waitlist-entries*` route `404 MODULE_DISABLED`, `waitlistEntryId` on a hold → 404, `SeatReleased.notifyWaitlist` always false, full rows are plain `FULL`, `counters.waiting` stays 0. Role matrix per endpoint (member own / group / foreign 404 / instructor / admin / anon), impersonation allowed on the member routes with `origin = BACKOFFICE` and the entry's `accountId` = the admin's account, cross-tenant 404 everywhere.

## Fixed conventions
- Statuses (catalog rule 0, literal; the catalog file wins over S08 §6, which writes 409 for several of these): `ALREADY_BOOKED`, `ALREADY_ON_WAITLIST`, `WAITLIST_LIMIT`, `BOOKING_LIMIT_REACHED`, `SEAT_TAKEN`, `SEAT_HOLD_EXPIRED`, `IDEMPOTENCY_KEY_REUSED` **409** · `CLASS_NOT_FULL`, `WAITLIST_NOT_NOTIFIED`, `WAITLIST_OFFER_EXPIRED`, `WAITLIST_ENTRY_NOT_LIVE`, `WAITLIST_FULL`, `SWAP_NOT_ALLOWED`, `MEMBER_NOT_ACTIVE`, `BOOKING_BLOCKED`, `INACTIVITY_PERIOD`, `LEVEL_NOT_ALLOWED`, `PACK_EMPTY` **422** · `MODULE_DISABLED`, `NOT_FOUND`, `DOG_NOT_ACCESSIBLE` **404**.
- Assumptions in force (do not ask): the seat goes to whoever holds **and confirms** first, not to whoever confirms first in absolute terms (A20a, R-08-15); «ja ha fet classe» for the `maxPerDogPerWeekIfAttended` limit = a counted booking of that week already started or `CANCELLED_LATE` (§13-3, part C S08); a dog's level change never touches its entries (R-08-16); the confirmation window is `bookings.seatHoldSeconds` (30 s) for the hold and `waitlist.fifoConfirmMinutes` (30 min) for the FIFO offer — two different clocks, both parameters.
- Every mutation is a Mongo transaction with the outbox inside it, serialized per class with `seat_locks` (lock order `seat_locks` → `class_sessions`, `BookingTransactions` retry); notifications only from outbox consumers; `Clock` injected; instants UTC, business dates club-local.
- Context direction (ArchUnit): this code lives in `clubs.bookings` and imports nothing from `clubs.training` or `clubs.activities`.
- Closed catalogs: no new parameter / event / notification / error code / audit action. `WaitlistEntry.cancelReason = MEMBER_LEFT` is an S15 §13 proposal — use it and record the proposal. Evidence rule: report = command + exit code + last 40 lines; long outputs → `roadmap/evidence/E5-T03/NN-<name>.log`; task file ≤ 120 KB; fictional data only (`@example.test`); no secrets; never edit other tasks, `ROADMAP.md` or the Organizer verification section.

## Verification (run all; evidence = command + exit code + last 40 lines; long outputs → `roadmap/evidence/<ID>/*.log`)
- `./mvnw -q verify` green with the tests named after T-08-08, T-08-19, T-08-20, T-08-21, T-08-22, T-08-30 (10 notified entries claiming one seat in parallel over real Mongo: 1 `CONSOLIDATED`, 9 `SEAT_TAKEN`, 9 back to `ACTIVE`; with 2 seats: 2 and 8), T-08-34 (a duplicate `WaitlistExpired` produces a single `offerNext`), T-08-45 (2 seats released at once in FIFO), `E5ContractIT` updated (the WP-08-C routes no longer answer 501), `ArchitectureTest`, `EventCatalogContractTest`, `AuditContractTest` unchanged, message parity ca/es/en for `notif.N-15.*` (+ `.sms`) and `notif.N-46.*`, coverage gates.
- OpenAPI snapshot byte-identical to a fresh `bin/openapi-snapshot` (or a `docs/openapi/CHANGELOG.md` entry for any field you had to add); `git diff --check` clean.

## Definition of Done
A full class accepts up to three waiting entries under the per-dog and per-week rules; an in-time cancellation more than 30 minutes before the start notifies every waiting member at the Cànic (or only the first in a FIFO club), the first to hold and claim gets the seat, the rest come back to `ACTIVE` with N-46; the FIFO offer expires into the next member when E5-T05's process delivers `WaitlistExpired`; cancelling the class cancels every entry inside S06's transaction; `WAITLIST` off removes the whole surface.

## Evidence to return
`verify` tail; the T-08 method list; the concurrency test output (counts per scenario); a `curl` sequence on the local stack for both modes: fill a class → join → cancel a booking 4 h before → N-15 rows → claim → N-46 rows for the others → leave; plus the FIFO variant driven by a manual `WaitlistExpired` (outbox events and `notifications` rows after each step, ids truncated).

---

## Executor report

Done (executor claude-opus-5-5, session 2026-09-24). S08 WP-08-C is served: the five waiting-list routes no longer answer 501.

- **Join, leave, reads:** join with its four limits, and leave. Read one entry, or the list of a class.
- **Claim:** it runs the R-08-08 confirmation transaction, including swap and BR-01.
- **Offers:** ALL_AT_ONCE notifies everyone; FIFO notifies one entry per free seat, with `confirmBy`. Both consumers are idempotent by state.
- **Demotion and consolidation:** the booking that takes the last seat demotes the other NOTIFIED entries in the same transaction. A direct booking consolidates the dog's entry.
- **Silent cancellations** for S06, S15 and S13.
- **Notifications:** N-15 (APP + SMS intent + PUSH intent) and N-46 (APP), only from outbox consumers.

Final results:
- `./mvnw -q clean verify` exits **0**: **433 unit/contract + 665 integration tests**, 0 failures, errors or skips. Every coverage, architecture, audit, event-catalog and message-parity gate holds.
- A fresh `bin/openapi-snapshot` is byte-identical. The only contract change is the new enum value `MEMBER_LEFT`, with a `docs/openapi/CHANGELOG.md` entry.
- `git diff --check` is clean.
- The local-stack curl rehearsal passes in both modes.

Dependencies: E5-T01 and E5-T02 are `awaiting_verification`, which roadmap/README §2 allows. An independent review of E5-T02 (`roadmap/reviews/E5-T02-20260924-0200-claude.md`) asks for changes. If E5-T02 comes back `changes_requested`, this task may need a rebase. I touched none of that review's findings, except that I added this task's own `.gitignore` evidence exception (its finding 2).

### Branch / commits
None. Per AGENTS.md, I ran no git writes; the publish script commits this working tree on `main`.

### Evidence (full command outputs)
**Earlier attempts** (one line each; I overwrote their logs with the final run, so only these lines remain):
1. A targeted run (`./mvnw -q verify -Dtest=WaitlistRulesTest -Dit.test=WaitlistIT … -Djacoco.skip=true`) exited 1. In `T_08_26`, `eventsOf("WaitlistJoined").getLast()` read Laura's own join, because the query has no order. I changed the test to select the event by aggregate id, and to take the latest `SeatReleased` by `occurredAt`. After that, the targeted run of WaitlistIT, BookingConcurrencyIT, BookingsIT and E5ContractIT exited 0.
2. A non-clean `./mvnw -q verify` exited 0, but I did not trust its numbers:
   - `target/surefire-reports` still held a stale `DemoBookingsTest.xml` (3 tests), a class E5-T02 deleted.
   - `jacoco.exec` appends across runs that are not clean.
   - So I switched to `clean verify`.
3. `./mvnw -q clean verify` exited 1 at `jacoco:check`: `clubs.scheduling.application.ports` had 60 % lines (< 85 %).
   - The cause is the S06 null-object `ClassBookingsPort` in `SchedulingPortDefaults`, which has had no caller since E5-T02 replaced it. This package is not touched by this task.
   - I added `SchedulingPortDefaultsTest`, which brings it to 100 % (20/20 lines).

**1. `./mvnw -q clean verify`** (a superset of `./mvnw -q verify`): exit **0**.
- Full output: `roadmap/evidence/E5-T03/01-verify.log` (4 892 lines). The only credential-like strings are the Mongo driver's `password=null`, and there is no `eyJ` token.
- With `-q`, a green run prints only the test JVMs' logs. This tail is the last IT context, followed by the Testcontainers monitor after shutdown (the same shape as E5-T02).

Last 40 lines, width cut at 150:
```
2026-09-24T02:40:59.253+02:00  INFO 5534 --- [agilityhub-core-api] [           main] t.c.s.AnnotationConfigContextLoaderUtils : Could not detect defau
2026-09-24T02:40:59.263+02:00  INFO 5534 --- [agilityhub-core-api] [           main] .b.t.c.SpringBootTestContextBootstrapper : Found @SpringBootConfi

  .   ____          _            __ _ _
 /\\ / ___'_ __ _ _(_)_ __  __ _ \ \ \ \
( ( )\___ | '_ | '_| | '_ \/ _` | \ \ \ \
 \\/  ___)| |_)| | | | | || (_| |  ) ) ) )
  '  |____| .__|_| |_|_| |_\__, | / / / /
 =========|_|==============|___/=/_/_/_/

 :: Spring Boot ::               (v3.5.16)

2026-09-24T02:40:59.276+02:00  INFO 5534 --- [agilityhub-core-api] [           main] c.a.core.migration.PlayoffMigrationIT    : Starting PlayoffMigrat
2026-09-24T02:40:59.276+02:00  INFO 5534 --- [agilityhub-core-api] [           main] c.a.core.migration.PlayoffMigrationIT    : The following 1 profil
2026-09-24T02:40:59.396+02:00  INFO 5534 --- [agilityhub-core-api] [           main] .s.d.r.c.RepositoryConfigurationDelegate : Bootstrapping Spring D
2026-09-24T02:40:59.412+02:00  INFO 5534 --- [agilityhub-core-api] [           main] .s.d.r.c.RepositoryConfigurationDelegate : Finished Spring Data r
2026-09-24T02:40:59.437+02:00  INFO 5534 --- [agilityhub-core-api] [           main] org.mongodb.driver.client                : MongoClient with metad
2026-09-24T02:40:59.439+02:00  INFO 5534 --- [agilityhub-core-api] [localhost:49514] org.mongodb.driver.cluster               : Monitor thread success
2026-09-24T02:40:59.868+02:00  INFO 5534 --- [agilityhub-core-api] [           main] c.a.core.migration.PlayoffMigrationIT    : Started PlayoffMigrati
2026-09-24T02:41:08.511+02:00  INFO 5534 --- [agilityhub-core-api] [localhost:49700] org.mongodb.driver.cluster               : Exception in monitor t

com.mongodb.MongoSocketOpenException: Exception opening socket
	at com.mongodb.internal.connection.SocketStream.lambda$open$0(SocketStream.java:85) ~[mongodb-driver-core-5.5.2.jar:na]
	at java.base/java.util.Optional.orElseThrow(Optional.java:403) ~[na:na]
	at com.mongodb.internal.connection.SocketStream.open(SocketStream.java:85) ~[mongodb-driver-core-5.5.2.jar:na]
	at com.mongodb.internal.connection.InternalStreamConnection.open(InternalStreamConnection.java:233) ~[mongodb-driver-core-5.5.2.jar:na]
	at com.mongodb.internal.connection.DefaultServerMonitor$ServerMonitor.lookupServerDescription(DefaultServerMonitor.java:219) ~[mongodb-driver-core-5.
	at com.mongodb.internal.connection.DefaultServerMonitor$ServerMonitor.run(DefaultServerMonitor.java:176) ~[mongodb-driver-core-5.5.2.jar:na]
Caused by: java.net.ConnectException: Connection refused
	at java.base/sun.nio.ch.Net.pollConnect(Native Method) ~[na:na]
	at java.base/sun.nio.ch.Net.pollConnectNow(Net.java:694) ~[na:na]
	at java.base/sun.nio.ch.NioSocketImpl.timedFinishConnect(NioSocketImpl.java:542) ~[na:na]
	at java.base/sun.nio.ch.NioSocketImpl.connect(NioSocketImpl.java:592) ~[na:na]
	at java.base/java.net.SocksSocketImpl.connect(SocksSocketImpl.java:327) ~[na:na]
	at java.base/java.net.Socket.connect(Socket.java:751) ~[na:na]
	at com.mongodb.internal.connection.SocketStreamHelper.initialize(SocketStreamHelper.java:76) ~[mongodb-driver-core-5.5.2.jar:na]
	at com.mongodb.internal.connection.SocketStream.initializeSocket(SocketStream.java:104) ~[mongodb-driver-core-5.5.2.jar:na]
	at com.mongodb.internal.connection.SocketStream.open(SocketStream.java:79) ~[mongodb-driver-core-5.5.2.jar:na]
	... 3 common frames omitted
```

**2. Test and coverage summary** of that clean run: `python3 roadmap/evidence/E5-T03/test-summary.py` → exit 0. Full output in `02-test-summary.log`; key lines:
```
  WaitlistRulesTest          tests=3   failures=0 errors=0 skipped=0
      T_08_08_waitingListLimitsPerClassPerDogWeekAttendedAndAcceptance
      T_08_20_T_08_21_T_08_45_offersPerModeNeverRepeatAnOpenOffer
      T_08_21_fifoConfirmByIsTheShorterOfTheWindowAndTheClassStart
  ArchitectureTest tests=6 · AuditContractTest tests=1 · MessageParityTest tests=1 · EventCatalogContractTest tests=2 · BookingPortsTest tests=8 — all 0 failures
unit/contract (surefire): tests=433 failures=0 errors=0 skipped=0
  BookingConcurrencyIT       tests=5   (… T_08_30_tenNotifiedEntriesClaimInParallelOnlyTheFreeSeatsAreTakenAndTheRestGoBackToActive …)
  WaitlistIT                 tests=9   failures=0 errors=0 skipped=0
      R_08_16_R_08_21_silentCancellationsSweepStartedClassesMembersWhoLeaveAndClassCancellations
      T_08_08_T_08_19_joiningNeedsAFullClassAndRespectsTheClassDogWeekAndAcceptanceLimits
      T_08_19_joinLeaveAndADirectBookingConsolidatesTheEntry
      T_08_20_allAtOnceNotifiesEveryWaitingMemberOnceTheFirstClaimWinsAndTheRestReturnToActiveWithN46
      T_08_21_fifoOffersTheFirstEntryUntilConfirmByAndTheExpiryMovesToTheNext
      T_08_22_claimNeedsTheOfferedHoldAndStillRespectsTheWeeklyLimitWithSwap
      T_08_26_waitlistRolesTenantImpersonationAndModuleOff
      T_08_34_aDuplicateWaitlistExpiredProducesASingleOffer
      T_08_45_fifoWithTwoSeatsReleasedAtOnceNotifiesTheFirstTwoEachWithItsConfirmBy
  BookingsIT tests=15 · E5ContractIT tests=38 — 0 failures
integration (failsafe): tests=665 failures=0 errors=0 skipped=0
coverage (merged unit + integration, jacoco.csv):
  clubs.bookings.domain 100.0 % / 93.4 % · clubs.bookings.application 99.4 % / 84.8 % · clubs.bookings.application.ports 100 % / 100 %
  clubs.bookings.api 94.7 % / 85.7 % · clubs.messaging.application 99.0 % / 87.7 % · clubs.scheduling.application.ports 100 % (20/20 lines)
```
**T-08-30 concurrency output** (`01-verify.log`, from `BookingConcurrencyIT`; each thread holds through the offer, then claims):
```
T-08-30 1 seat(s), 10 notified entries hold + claim in parallel: {201=1, 409 SEAT_TAKEN=9} → entries {ACTIVE=9, CONSOLIDATED=1, NOTIFIED=0}
T-08-30 2 seat(s), 10 notified entries hold + claim in parallel: {201=2, 409 SEAT_TAKEN=8} → entries {ACTIVE=8, CONSOLIDATED=2, NOTIFIED=0}
```
The same test also asserts the live bookings (1 / 2), `counters {booked, waiting}` (1/9, 2/8) and the N-46 rows (9 / 8).

**3. OpenAPI snapshot.**
- `bin/openapi-snapshot` → exit 0. The log (`03-openapi-snapshot.log`) ends with `Updated docs/openapi/openapi.json`.
- A second run compared against a copy of the first (`cmp docs/openapi/openapi.json target/e5t03-openapi-before.json`) prints `BYTE_IDENTICAL`.
- The diff against `main` has 6 lines:
  - `WaitlistEntry.cancelReason` enum `+ "MEMBER_LEFT"`;
  - the five waiting-list operation descriptions no longer say «501».
- Entry added to `docs/openapi/CHANGELOG.md`.

**4. `git diff --check`** → `DIFF_CHECK_CLEAN`. The new untracked files were grepped for trailing whitespace: `NO_TRAILING_WHITESPACE_IN_NEW_FILES`.

**5. Local-stack curl sequence (Evidence to return):** `python3 roadmap/evidence/E5-T03/curl-sequence.py` → exit **0**. Full log: `roadmap/evidence/E5-T03/04-curl-sequence.log` (191 lines, ids and tokens truncated).
- **Setup:**
  - A disposable Mongo 7 replica set, then `bin/core club:apply seeds/club-canic.yaml` (exit 0, 15 changes) and `bin/core seed:demo --club=canic --seed=42` (exit 0).
  - The API jar runs under the local profile, and the test clock is set to 5 h before each class.
  - For each scenario, the admin sets the chosen demo class's capacity to its seeded bookings + 1 through `PATCH /class-sessions/{id}`, so one login booking fills it.
  - The seed login dogs I first picked have no level, so the script uses the first login dog whose level the class admits.
- **FIFO:** the API is restarted with the club parameter `waitlist.mode = FIFO` written to Mongo before startup. The expiry is done the way S15 P6 will do it: the entry is set to EXPIRED, then a hand-written `WaitlistExpired` outbox record is inserted.

Condensed excerpt:
```
=== ALL_AT_ONCE: class 2026-10-05 08:30 capacity 4, 3 seeded bookings, 1 login booking(s) to fill it, waiting: member.2, member.3, member.4
2. curl POST /api/v1/waitlist-entries -> 201 ×3 (positions 1,2,3) · class counters {"booked":4,"waiting":3} · outbox WaitlistJoined ×3
3. curl POST /api/v1/bookings/1a60d047…/cancellation -> 200 · state=CANCELLED late=False minutesBefore=299
  outbox SeatReleased PUBLISHED ff86c98f… {"classId":"ff86c98f…","freeSeats":1,"minutesBefore":299,"notifyWaitlist":true}
  outbox WaitlistNotified PUBLISHED ff86c98f… {"entryIds":["6ba1c9f2…","b17d2698…","357a3c87…"],"classId":"ff86c98f…","mode":"ALL_AT_ONCE"}
  notification N-15 APP SENT ×3 · N-15 PUSH QUEUED ×3 · N-15 SMS QUEUED ×3, e.g.
  notification N-15 SMS QUEUED account 24d10f1a… sms(123): Club Agility Canic: placa lliure a la classe de Ginesta 191 del dilluns, 5 d'octubre de 2026 a les 08:30. Agafa-la a l'app.
4. curl POST /api/v1/seat-holds -> 201 [hold] · curl POST /api/v1/seat-holds -> 409 SEAT_TAKEN [another waiting member while the seat is held]
   curl POST /api/v1/waitlist-entries/b17d2698…/claim -> 201 [claim]
  outbox WaitlistConsolidated PUBLISHED b17d2698… {"entryId":"b17d2698…","bookingId":"8ca9a73d…"}
  outbox BookingCreated PUBLISHED 8ca9a73d… {…,"origin":"APP","waitlistEntryId":"b17d2698…"}
  notification N-04 APP SENT account 56906328… · notification N-46 APP SENT account 24d10f1a… · notification N-46 APP SENT account e64c7b4f…
  member.2: ACTIVE (notifiedAt kept) · member.3: CONSOLIDATED bookingId 8ca9a73d… · member.4: ACTIVE · class counters {"booked":4,"waiting":2}
5. curl POST /api/v1/waitlist-entries/357a3c87…/cancellation -> 200 · cancelReason=MEMBER · outbox WaitlistLeft · counters {"booked":4,"waiting":1}
=== FIFO: class 2026-10-08 18:50 capacity 5 → 3 (2 seeded bookings), waiting: member.6, member.7, member.8
3. cancellation 299 min before → outbox WaitlistNotified {"entryIds":["e92692d4…"],…,"confirmBy":{"$date":"2026-10-08T12:20:16.679Z"},"mode":"FIFO"}
  notification N-15 APP SENT account d1632925… {…,"confirm_by":"14:20","mode":"FIFO"} · N-15 PUSH QUEUED · N-15 SMS QUEUED sms(140): … Confirma-la a l'app abans de les 14:20.
  member.6: NOTIFIED confirmBy 12:20Z · member.7: ACTIVE · member.8: ACTIVE
4. test clock +31 min, entry → EXPIRED, manual WaitlistExpired:
  outbox WaitlistExpired PUBLISHED e92692d4… {"entryId":"e92692d4…","classId":"697cf618…"}
  outbox WaitlistNotified PUBLISHED 697cf618… {"entryIds":["edcb704e…"],…,"confirmBy":{"$date":"2026-10-08T12:51:00.933Z"},"mode":"FIFO"}
  notification N-15 APP/PUSH/SMS for member.7 (confirm_by 14:51) · curl POST /api/v1/seat-holds -> 422 WAITLIST_OFFER_EXPIRED [expired offer]
5. member.7 hold -> 201 · claim -> 201 → WaitlistConsolidated, BookingCreated{waitlistEntryId}, N-04 · member.8 stays ACTIVE (FIFO never demotes)
6. member.8 leaves → WaitlistLeft · class counters {"booked":3,"waiting":0}
PASS waiting-list curl sequence (ALL_AT_ONCE and FIFO)
```

**Not run:** `bin/e4-smoke`, and the second `seed:demo` idempotency run, because neither is in this task's Verification list. `seed:demo` ran once, successfully, inside the curl script. `DemoPlanningSeedIT` (S06 D4c on the real adapters) passes in the verify run. I claim no CI result.

### Files changed
**New (main):**
- `clubs/bookings/domain/{WaitlistMode, WaitlistRules}`
- `clubs/bookings/application/{WaitlistService, WaitlistTransitions, WaitlistNotifications, WaitlistConsumers}`

**Modified (main):**
- `clubs/bookings/domain/WaitlistCancelReason` (+ `MEMBER_LEFT`)
- `clubs/bookings/persistence/WaitlistEntryRepository` (live by dog, per week, max position, by member, started, per class)
- `clubs/bookings/application/`:
  - `ports/WaitlistConsolidationPort`: new signature, plus `demoteIfFull`.
  - `BookingConfirmationService`: consolidates with claim/direct, and demotes.
  - `BookingCancellationService`: `cancelByClub` delegates to `WaitlistTransitions.cancelAll`.
  - `SeatHoldService`: claim hold → `SEAT_TAKEN` / `WAITLIST_OFFER_EXPIRED`.
  - `BookingCounters`: `waiting` is 0 with WAITLIST off.
  - `BookingContext` (`waitlistMode`), `BookingActors` (`admin()`).
  - `BookingContractAccess`: the waiting-list guard moved to `WaitlistService.visible`.
  - `BookingViews`: `waitlistEntry` view, and a shared class card.
  - `BookingConsumers`: also refreshes the class start and week copied onto entries.
  - `BookingsAutoConfiguration`: the null port is removed.
  - Javadoc only: `ClassBookingsAdapter`, `DemoBookingSeeder`.
- `clubs/bookings/api/BookingsController` (the five waiting-list routes)
- `clubs/messaging/application/SystemNotificationService` (`pushIntentOnce`)
- `clubs/messaging/persistence/NotificationRepository` (`content(id, channel, …)`, with `confirm_by` and `mode` allow-listed)
- `messages_{ca,es,en}.properties` (`notif.N-15.{title,body,sms}`, `notif.N-46.{title,body}`)

**Tests:**
- New: `clubs/bookings/domain/WaitlistRulesTest`, `clubs/bookings/api/WaitlistIT`, `clubs/scheduling/application/ports/SchedulingPortDefaultsTest`.
- Modified: `BookingFixtures` (waiting-list helpers), `BookingConcurrencyIT` (T-08-30), `E5ContractIT` (five more routes implemented; staff reads an entry).

**Docs, configuration, roadmap:** `docs/openapi/openapi.json`, `docs/openapi/CHANGELOG.md`, `CHANGELOG.md`, `.gitignore` (`!roadmap/evidence/E5-T03/*.log`), `roadmap/MESSAGES.md`, `roadmap/STATUS.md` (rendered), this file.

**Evidence:** `roadmap/evidence/E5-T03/{01-verify, 02-test-summary, 03-openapi-snapshot, 04-curl-sequence}.log`, plus the scripts `test-summary.py` and `curl-sequence.py`.

### Rules and tests implemented (R-xx-nn / T-xx-nn)
- **R-08-12 (join):**
  - Implemented in `WaitlistService.join` and `WaitlistRules.join`: WAITLIST guard; eligibility chain (`BookingChecks.subject`); full by live bookings only (a hold does not count); ALREADY_BOOKED, ALREADY_ON_WAITLIST; `maxPerClass`; `maxPerDogPerWeek` / `…IfAttended` (per owner with `limitUnit = MEMBER`); acceptance (`!limit.done()` → otherwise `BOOKING_LIMIT_REACHED`, with the hold's `details`).
  - On success: `position = max + 1` over every state; start and week copied onto the entry; `lastDogForClass` of the joiner; `WaitlistJoined`; counters updated.
  - Tests: T-08-08 (unit + IT), T-08-19.
- **R-08-13 (ALL_AT_ONCE) and R-08-14 (FIFO):**
  - `offerSeats` / `offerNext` with `WaitlistRules.toNotify` and `confirmBy`. Already NOTIFIED entries are never notified again, and nothing is offered at or below `waitlist.notifyThresholdMinutes`, or with WAITLIST off.
  - Demotion: `WaitlistTransitions.demoteIfFull` inside every confirmation, and again in the N-46 consumers.
  - Tests: T-08-20, T-08-21, T-08-30, T-08-34, T-08-45.
- **R-08-15 (claim):**
  - The hold checks (`SeatHoldService.offer`) and `WaitlistService.claim`: NOTIFIED, FIFO `confirmBy > now`, the caller's live hold taken for this entry, then the R-08-08 confirmation in the same transaction.
  - The waiting-list limits do not apply, BR-01 does: swap, or `BOOKING_LIMIT_REACHED`.
  - `SEAT_TAKEN` when the seat is gone; `Idempotency-Key` replay.
  - Tests: T-08-22, T-08-30.
- **R-08-16 (leaving and silent cancellations):**
  - `MEMBER` / `ADMIN` + `WaitlistLeft`, from ACTIVE or NOTIFIED only.
  - `CLASS_CANCELLED` (`cancelAll`, inside S06), `CLASS_STARTED` (`sweepStarted`), `MEMBER_LEFT` (`cancelByMember`), `BOOKED_DIRECTLY` → CONSOLIDATED. Nothing is deleted.
  - Tests: T-08-19, `R_08_16_…`, and T-08-25 in BookingsIT, which still passes.
- **R-08-08 (consolidation on a direct booking):** `WaitlistTransitions.consolidate` via the port. Test: T-08-19.
- **R-08-11:** the `notifyWaitlist` flag is consumed. Tests: T-08-21 (30 min exactly → no offer), T-08-26 (WAITLIST off).
- **R-08-21:** entries keep their state on level or time changes; their copied class start and week follow `ClassSessionUpdated`. Test: `R_08_16_R_08_21_…`.
- **R-08-23:** `Member.lastDogForClass` on join. Test: T-08-19.
- **§6 reads:** `GET /waitlist-entries/{id}` (class card, `position`, `confirmBy`, state) and `GET /class-sessions/{id}/waitlist-entries`. Tests: T-08-21, T-08-26, E5ContractIT.
- **§7:**
  - Emits `WaitlistJoined`, `WaitlistLeft`, `WaitlistNotified{entryIds, classId, confirmBy?, mode}` and `WaitlistConsolidated{entryId, bookingId}`, with payloads equal to `e5-events.json`.
  - Consumes `SeatReleased`, `WaitlistExpired`, `WaitlistConsolidated` and `BookingCreated`.
- **§8:**
  - N-15: APP + SMS intent (≤ 160 GSM-7) + PUSH intent; `CLAIM_SEAT`; `confirm_by` in FIFO only; one per entry and offer.
  - N-46: APP only.
  - Tests: T-08-20, T-08-21, T-08-45; parity in `MessageParityTest`.
- **§9:** WAITLIST off → every route 404 `MODULE_DISABLED`, `waitlistEntryId` on a hold 404, `notifyWaitlist` false, `counters.waiting` 0. Tests: T-08-26, E5ContractIT.
- **S15 R-15-16:** `offerNext` is idempotent. Test: T-08-34.
- **S15 R-15-18(a):** `sweepStarted` is exposed but not scheduled (E6's P8).
- **Tenant and roles:** member own / group / foreign 404, instructor, admin, anon, impersonation (`accountId` = the admin's, reason ADMIN), cross-tenant. Tests: T-08-26, E5ContractIT (every route × 5 roles, TENANT_MISMATCH, NO_MEMBERSHIP, cross-tenant 404, impersonation, module off).

### Assumptions
1. **Check order of the join.** The eligibility chain runs first, as for a hold (dog access, block, level, pack, not yet open …). Then come `CLASS_NOT_FULL`, `ALREADY_BOOKED`, `ALREADY_ON_WAITLIST` and the limits. The limits run in this order: class, then dog-week, then acceptance.
2. **Who the entry belongs to.** `WaitlistEntry.memberId` is the **dog's owner**, like `Booking.memberId`: the per-owner limits need it. `accountId` is the acting account (the admin's when impersonating). A family-group member may read, leave and claim a group dog's entry, as with bookings. S08 §6 writes «pròpia»; task step 2 says «own or group».
3. **Leave reason.** An administrator gets `cancelReason = ADMIN` whether acting from D4/D12 or impersonating; a member gets `MEMBER`.
4. **Demotion is synchronous.**
   - `demoteIfFull` runs inside every confirmation transaction: claim, direct booking and PAYMENT_PENDING. So the seat is never offered to demoted entries.
   - The consumers of `WaitlistConsolidated` / `BookingCreated` (`notifications.N-46`, `notifications.N-46.booked`) first re-apply it idempotently in their own transaction, then send N-46.
   - N-46 recipients are the ACTIVE entries with `notifiedAt` (offers that were taken).
   - The id is `N-46:{entryId}:{notifiedAt ms}:app`. This is idempotent **per lost offer**, rather than per (eventId, recipient, channel). A claim emits both events, and either may be delivered first.
5. **Codes on a taken or expired offer.**
   - A demoted entry (ACTIVE with `notifiedAt`) whose class is full answers `409 SEAT_TAKEN` on the hold and on the claim. This is what makes the T-08-30 losers `SEAT_TAKEN` even after the winner's claim.
   - An EXPIRED entry answers `422 WAITLIST_OFFER_EXPIRED`.
   - A claim whose hold lacks this `waitlistEntryId` answers `422 WAITLIST_NOT_NOTIFIED`. The catalog has no dedicated code.
6. **`WaitlistNotified` shape.** ALL_AT_ONCE sends one event with every `entryId` and no `confirmBy`. FIFO sends one event **per offered entry**, each with its own `confirmBy` (the T-08-45 wording). Both carry `mode` (Annex A).
7. **How many seats are offered.** `offerSeats` offers `min(event freeSeats, seats currently free)`, minus the open FIFO offers.
8. **Defensive expiry.** `offerNext(classId, entryId)` first expires the named entry when it is still NOTIFIED past `confirmBy` (defensive; S15 P6 owns that transition in E5-T05). This makes a manual `WaitlistExpired` usable, and T-08-34 covers it.
9. **N-15 recipients and channels.**
   - N-15 goes to the entry's member, the dog's owner. Unlike N-04 (A20b), it does not also go to a group member who joined.
   - Ids are `eventId:entryId:channel`.
   - The PUSH intent is a `Notification` row with `channel = PUSH`: `QUEUED`, or `SKIPPED_MODULE_OFF` without PUSH. It uses the new `SystemNotificationService.pushIntentOnce`. No sender exists until E7.
   - The `NotificationQueued` payload says `channel: EMAIL` for APP/SMS/PUSH rows. This is pre-existing behaviour (E5-T02 assumption 15), and I did not change it.
10. **Not scheduled or wired here.** `sweepStarted` is not scheduled (E6's P8). `cancelByMember` is not called by S13/S15 yet (E5-T05/E8). Both are tested.
11. **Demo seed.** Waiting registrants are still inserted directly. The demo rows set their waiting counts independently of the class capacity, and `join` would refuse a class that is not full. E5-T06 reshapes the seed.
12. **`@ContractErrors` of claim.** I kept the list from E5-T01, which comes from S08 §6, to avoid contract churn. The confirmation it reuses can also answer the eligibility codes (`BOOKING_BLOCKED`, `MEMBER_NOT_ACTIVE`, …).
13. **Coverage evidence.**
    - Non-clean `verify` runs append to `jacoco.exec` and keep report files of deleted tests. The final evidence is therefore a `clean verify`.
    - That clean run showed the S06 `SchedulingPortDefaults` null objects uncovered since E5-T02 retired them from the application context. It is fixed by `SchedulingPortDefaultsTest`.
    - E5-T02's reported "432 unit" count probably included the stale `DemoBookingsTest` report.

### Questions / catalog proposals
No parameter, event, notification, error code or audit action was added. The following are the proposals, repeated in `MESSAGES.md`:
1. **`WaitlistEntry.cancelReason = MEMBER_LEFT`** (the S15 §13 proposal) is used by `cancelByMember` and published in the contract. Please add it to S08 §3.
2. **N-15 rendering variable `mode`** (`ALL_AT_ONCE` / `FIFO`) selects the wording with or without `{confirm_by}`, as N-36's `change` does. Please add it to the N-15 row, or split the row.
3. **N-46 trigger.** The catalog writes «`WaitlistNotified` revertit», but there is no demotion event in the closed catalog. The notification is produced by the consumers of `WaitlistConsolidated` / `BookingCreated`, as S08 §7 lists them. Please confirm.
4. **Claim with a hold not taken through the offer** answers `422 WAITLIST_NOT_NOTIFIED`. Is a dedicated code wanted?

## Organizer verification
_(pending)_
