---
id: E6-T03
title: Tasks, attachments, observations and student follow-up (S10 WP-10-C): Task lifecycle with N-20/N-21, TASK and DOG_OBSERVATIONS attachments on the existing signed-URL storage, Dog.remarks, the FollowupItem projection with per-user read marks, and the MemberNoteChanged / DogTransferred consumers
stage: E6
repo: agilityhub-core-api
thread: A
order: 30
status: ready
depends_on: [E6-T01]
branch:
pr:
updated: 2026-09-19
---

# E6-T03 · Tasks, attachments, observations and follow-up (S10 WP-10-C)

## Objective
Work package S10 WP-10-C. An instructor writes a task for a dog with a video attached and the member sees it and ticks it done; the instructor keeps a private observation on the dog that no member ever sees; the member's own note to the instructors is read-only here but lands in the same follow-up inbox; and D14 shows every one of those movements as a list where each instructor has their **own** unread marks. The task runs in parallel with E6-T02 — it depends only on the contract — and it also delivers the `DogFollowupPort` implementation that gives the attendance sheet its `pendingTasksCount` and the student card its three blocks.

## Spec references
S10 §3 (`Task`, `Attachment`, `FollowupItem`, `FollowupReadMark` field lists and indexes), **R-10-10** (task lifecycle, who may do what, N-20 and N-21), **R-10-11** (attachments: signed upload, purposes, limits, who may read, removal), **R-10-12** (observations = `Dog.remarks`; the member note is read-only and its `MemberNoteChanged` feeds D14), **R-10-13** (follow-up rows, the `unread` formula, ordering, read-all, filters), R-10-00, R-10-01, R-10-16 (`levels.enabled` → `levelCode: null`), §5 (task state machine and its transition table), §6 (the task rows, the `Task` and `FollowupItem` item shapes, the error list, the mandatory transactions: task creation with attachments, `read-all`), §7 (emitted `TaskCreated`/`TaskUpdated`/`TaskDeleted`/`TaskCompleted`/`TaskReopened`/`AttachmentAdded`/`AttachmentRemoved`/`DogUpdated{diff: remarks}`; consumed `MemberNoteChanged`, `DogTransferred`, `DogDeactivated`), §8 (**N-20**, **N-21**, **N-22**), §9 (the `TASKS` module row), §12 WP-10-C, §13 (doubts 9, 11, 12, 13) · tests **T-10-06, T-10-15, T-10-16, T-10-17, T-10-18, T-10-25** and the follow-up half of **T-10-22** · S03 R-03-14 (`DogTransferred`: the task follows the dog), R-03-17 (`Dog.instructorNote` and its `MemberNoteChanged`), R-03-19b (`handlerName`), §3 (`Dog.remarks`, `remarksMeta`) · S14 R-14-09 (`DOG_UPDATED`) and R-14-10 (`@Audited`) · `CATALEG_ERRORS.md` row S10 + rule 0 · `CATALEG_NOTIFICACIONS.md` N-20 (`PERSONAL`, MEMBER → APP+EMAIL, action `OPEN_TASKS`, variables `dog_name`, `instructor_name`, `task_excerpt`), N-21 (`OPERATIONAL`, INSTRUCTORS → APP, action `OPEN_DOG`, variables `member_name`, `dog_name`, `task_excerpt`), N-22 (`OPERATIONAL`, INSTRUCTORS → APP, action `OPEN_DOG`) · `CATALEG_ESDEVENIMENTS.md` rows `TaskCreated/TaskUpdated/TaskDeleted/TaskCompleted`, `AttachmentAdded`, `MemberNoteChanged`, `DogUpdated`, `DogTransferred` + Annex A (`TaskReopened`, `AttachmentRemoved`) · `CATALEG_PARAMETRES.md` `files.maxSizeMb` (25), `files.allowedTypes`, `files.maxAttachmentsPerEntity` (10), `levels.enabled` · `MATRIU_PERMISOS.md` row «Tasques, observacions, seguiment D14» · `CATALEG_MODULS.md` `TASKS` · `CONVENCIONS_API.md` §4 (universal list) and §5 (signed URLs).

## Context to load
`roadmap/tasks/E6-T01.md` (the contract you are filling in) and `roadmap/tasks/E6-T02.md` (the port you must satisfy) · `docs/specs/S10-assistencia-tasques-i-seguiment.md` complete · `docs/specs/S03-*.md` R-03-14, R-03-17, R-03-19b and §3 · **already in the repo, reuse do not recreate**: `clubs/followup/persistence/{Attachment,AttachmentRepository,UploadGrant,UploadGrantRepository,LocalAttachmentStorage,S3AttachmentStorage}.java`, `clubs/followup/application/{AttachmentService,AttachmentStorage,AttachmentConfiguration}.java`, `clubs/followup/api/{AttachmentsController,SignupFilesController}.java`, `clubs/followup/domain/AttachmentAdded.java` — read `AttachmentService` end to end first: `upload(purpose, name, type, size)` issues the grant and the signed PUT, `claim(key, purpose, entity)` binds it after verifying the real object's size and mime, `addNote(...)` is the existing `INSTRUCTOR_NOTE` registration with its limit check and `AttachmentAdded`, `url(fileKey, name)` signs a 5-minute download, `putLocal`/`openLocal` back the `local` profile · `clubs/census/application/DogService.java` (it owns `Dog.instructorNote` and emits `MemberNoteChanged`; `Dog.remarks` and `remarksMeta` are fields of `clubs/census/persistence/Dog.java`), `MemberService`, `clubs/census/persistence/{Dog,Member}.java` · `shared/application/{DogOwnerAccess,EventPublisher,OutboxDispatcher,DomainEventHandler,TenantContext,CurrentUser,IcuMessageSource}.java`, `shared/application/lists/*` (universal list engine + `x-filterable` declaration), `shared/domain/{ApiException,ErrorCode}.java` · `clubs/messaging/application/SystemNotificationService.java` and the `notif.N-xx.title/body` keys in `src/main/resources/messages/messages_{ca,es,en}.properties` (E4-T03's `notifications.N-08a` is the pattern to copy for fan-out) · `platform/application/{ClubConfig,ClubConfigService,Module,ModuleGuard,RequiresModule}.java`, `platform/application/audit/*` · `clubs/catalogs/**` (`Instructor`, `Level`) to resolve the active instructors of the club and the level code · `docs/playbooks/{new-endpoint,new-entity,new-event-consumer,checklist}.md` · mockups `docs/pantalles/mobil/26-*.png`, `22-*.png` and `docs/pantalles/escriptori/D13-*.png`, `D14-*.png`.

## Steps
1. **`TaskService`** (`clubs/followup/application`) over the `tasks` document of E6-T01 — R-10-10, §5: `create(dogId, text, attachmentIds?)` (INSTRUCTOR, ADMIN; dog must be `ACTIVE` → `422 DOG_NOT_ACTIVE`; `text` 1–2000 → `400 VALIDATION_ERROR`; `memberId` = the dog's owner; `Idempotency-Key`) in **one transaction** with the attachment registrations, the `FollowupItem` upsert and the outbox → `TaskCreated` → **N-20** to the owner only (never the family group). `updateText(id, text, version)` → `409 STALE_VERSION` on a stale version → `TaskUpdated`, no notification, refresh `textExcerpt` (≤ 120) of the D14 row. `delete(id)` (also for done tasks) → logical `deletedAt` + `deletedBy` → `TaskDeleted` → the D14 row becomes `hidden`; it disappears from `GET /tasks` unless `includeDeleted=true` (ADMIN only). `complete(id)` by the **owner** (from screen 13, also impersonated and then audited) or by any instructor/admin → `DONE`, `doneAt`, `doneBy {accountId, role, displayName, gender}`, `completedAt` on the D14 row **without touching `activityAt`** → `TaskCompleted` → **N-21 to every active `Instructor` of the club**; already done → `422 TASK_ALREADY_DONE`; deleted → `404`. `reopen(id)` (INSTRUCTOR, ADMIN only — §13-12) → back to `PENDING`, clears `doneAt`/`doneBy`/`completedAt` → `TaskReopened` (Annex A proposal: emit it and file the proposal), not done → `422 TASK_NOT_DONE`; a MEMBER calling it → `403`. Visibility: a MEMBER only reaches tasks of their **own** dogs, not the family group's (§13-13) → `404 DOG_NOT_ACCESSIBLE`. **T-10-15, T-10-25**.
2. **`GET /tasks`** — mandatory `dogId`, optional `state`, `includeDone=true` (the «Veure l'historial complet ›» of 26 and the «Veure l'historial ›» of 13), `includeDeleted` (ADMIN), `page`, `size`; index `{clubId, dogId, deletedAt, state, createdAt}`; each item carries its `attachments[]` with a freshly signed `url`. `GET /tasks/{id}` same projection.
3. **Attachments for `TASK` and `DOG_OBSERVATIONS`** — R-10-11, built **on the existing `AttachmentService`**, not beside it: `POST /attachments/upload-url {purpose, fileName, mimeType, sizeBytes}` already validates `files.allowedTypes` (`400 FILE_TYPE_NOT_ALLOWED`) and `files.maxSizeMb · 2^20` (`400 FILE_TOO_LARGE {maxSizeMb}`) — E6-T01 added the two purposes to its allow-list; now implement `POST /attachments {entityType, entityId, fileKey, name}` for them by generalizing the existing `addNote` into `add(entityType, entityId, key, name)`: lock the entity, dedupe on `fileKey` (same key twice → the same attachment, no second event), enforce `files.maxAttachmentsPerEntity` (10) → `422 ATTACHMENT_LIMIT_REACHED {max}`, verify the grant's `purpose` matches `entityType` → `422 ATTACHMENT_ENTITY_MISMATCH` (a `fileKey` issued for `DOG_DOCUMENT` must fail), keep `name ≤ 80`, emit `AttachmentAdded`, bump `Task.attachmentCount`. Who may write: `TASK` and `DOG_OBSERVATIONS` → INSTRUCTOR/ADMIN; `INSTRUCTOR_NOTE` → the dog's owner (also impersonated), unchanged. Who may read: `TASK` → owner + instructors/admin; `INSTRUCTOR_NOTE` → the same; **`DOG_OBSERVATIONS` → never a member (`404`, not `403`)**. `GET /attachments?entityType&entityId` returns the live ones with a 5-minute signed `url`; `DELETE /attachments/{id}` marks `removedAt`/`removedByAccountId` (the file survives until the RGPD erasure of S14), emits `AttachmentRemoved` (Annex A proposal: emit and file it) and decrements `attachmentCount`. **T-10-16**.
4. **Observations** — R-10-12: `PUT /dogs/{id}/observations {text, version}` (INSTRUCTOR, ADMIN, `TASKS`, `Idempotency-Key`) writes `Dog.remarks` (single private field, ≤ 2000) and `Dog.remarksMeta {updatedAt, updatedByAccountId, updatedByName}` through `DogService` (census owns the `Dog` document — call the service, do not write the collection from `followup`), emits `DogUpdated {dogId, diff: {remarks}}` and writes `AuditEntry{action: DOG_UPDATED}` with `@Audited`; stale `version` → `409 STALE_VERSION`. **No notification and no D14 row.** `Dog.remarks` must never appear in any `/me/*` response — add the assertion to the S03 projection test as well. **T-10-17**.
5. **`FollowupItem` projection** — R-10-13: one row per task (`kind: TASK`) and **exactly one row per dog** for the member note (`kind: MEMBER_NOTE`, upserted on every change). Fields as S10 §3; `activityAt` = the task's creation or the note's last change (completing a task updates `completedAt` only); member name, dog name and `levelCode` are resolved **at read time** (pages ≤ 50 rows) so a rename never needs a backfill; `levels.enabled = false` → `levelCode: null`. Consumers, idempotent by `eventId`: **`MemberNoteChanged {dogId, memberId}`** (emitted by `DogService`) → upsert the `MEMBER_NOTE` row with `activityAt = occurredAt`, `textExcerpt` from `Dog.instructorNote.text`, `authorAccountId` = whoever wrote it, which makes it unread again for everyone except the author, and fire **N-22** to every active instructor; **`DogTransferred`** (S03 R-03-14) → move `Task.memberId` and the dog's D14 rows to the new owner (the task follows the dog); **`DogDeactivated`** → nothing (tasks and rows are kept); **`TaskDeleted`** → `hidden = true`. Consuming the same `MemberNoteChanged` twice leaves **one** row. **T-10-18**.
6. **Read marks and D14 queries** — R-10-13: `FollowupReadMark {clubId, accountId (unique), readAllAt, readItemIds[]}`; `unread(item, me) = item.activityAt > readAllAt(me) ∧ item.id ∉ readItemIds(me) ∧ item.authorAccountId ≠ me`. `GET /followup` = universal list with `x-filterable: kind, memberId, dogId, authorAccountId, unread` and ordering **unread first (`activityAt` desc), then the rest (`activityAt` desc)** — implement the ordering in the query, not in the front; an undeclared filter → `400 INVALID_FILTER`. `GET /followup/unread-count` → `{count}` (the admin menu badge, refetched every 60 s by the front; it is also what `GET /dashboard/counters.followUpUnread` of S14 R-14-08 reads — wire it to the existing dashboard counter and say so in the report). `POST /followup/{id}/read` → `readItemIds += id` (prune ids whose `activityAt < readAllAt`). `POST /followup/read-all` → `readAllAt = now`, `readItemIds = []` in **one transaction**, O(1) whatever the row count. **T-10-06, T-10-18**.
7. **`DogFollowupPort` implementation** — the adapter in `clubs/followup/application` for the port E6-T02 declares in `clubs.bookings.application.ports`: `pendingTaskCounts(Collection<String> dogIds)` (one query, index `{clubId, dogId, deletedAt, state, createdAt}`) for the attendance sheet rows, and `cardBlocks(dogId)` returning `{instructorNote {text, updatedAt, attachments[]}, tasks {pendingCount, doneCount, latest}, observations {text, updatedAt, updatedByName, attachments[], version}}` for `GET /dogs/{id}/instructor-card`. With `TASKS` off the bean is not registered and the null-object default answers empty, which is exactly the module-off shape (fields absent). Never return `observations` to a member-facing caller.
8. **Notifications in the dispatcher** (pattern of E4-T03's `notifications.N-08a`): **N-20** from `TaskCreated` → the dog's owner, APP+EMAIL, category `PERSONAL` (so the member's own preferences apply), action `OPEN_TASKS`, variables `dog_name`, `instructor_name`, `task_excerpt` (≤ 120, the same excerpt as the D14 row); **N-21** from `TaskCompleted` (whoever the `by` is, including an instructor) → **all active instructors** of the club, APP, action `OPEN_DOG`, variables `member_name`, `dog_name`, `task_excerpt`; **N-22** from `MemberNoteChanged` → all active instructors, APP, action `OPEN_DOG`, variables `member_name`, `dog_name`. Keys `notif.N-20.title/body`, `notif.N-21.*`, `notif.N-22.*` in the three `messages_{ca,es,en}.properties`, rendered **in each recipient's language**, with the personal article ICU where the mockups use it («feta per la Laura») and `{gender, select, female {alumna} other {alumne}}` for the D14 author label. Deterministic notification id per `(eventId, recipient, channel)` so a replayed event notifies once. **No notification** for `TaskUpdated`, `TaskDeleted`, `TaskReopened`, attachments or observations.
9. **Module guard.** With `TASKS` off, `/tasks*`, `/followup*`, `/dogs/{id}/observations` and the two new attachment purposes answer `404 MODULE_DISABLED`, `pendingTasksCount` and the three card blocks disappear, and no N-20/N-21/N-22 is ever queued — assert all of it on the `club-minim` seed (`WAITLIST`, `FAQ`, `PUSH` only), as T-10-33 does for the attendance half.
10. **Roles, tenant and impersonation** (follow-up half of **T-10-22**): MEMBER on `/followup*`, `PUT /dogs/{id}/observations`, `PATCH`/`DELETE /tasks/{id}` or `POST /tasks` → `403`; MEMBER on `GET /tasks?dogId=` of a family-group dog → `404 DOG_NOT_ACCESSIBLE`; impersonation tokens are **accepted** on `GET /tasks` (own), `POST /tasks/{id}/completion` and the `INSTRUCTOR_NOTE` attachment routes (audited, `origin = BACKOFFICE`) and **rejected** on `/followup*` and every instructor write; any resource of another club (task, attachment, follow-up item, dog) → `404`.

## Fixed conventions
- Closed catalogs: no new parameter, event, notification, error code or audit action. `TaskReopened` and `AttachmentRemoved` are already listed in `CATALEG_ESDEVENIMENTS.md` **Annex A as proposals**: emit them with the payloads S10 §7 writes and file the proposal in the report and `roadmap/MESSAGES.md`. Observations audit under the existing `DOG_UPDATED`.
- **Rule 0 statuses** (the catalog beats S10 §6): `TASK_ALREADY_DONE`, `TASK_NOT_DONE`, `ATTACHMENT_LIMIT_REACHED`, `ATTACHMENT_ENTITY_MISMATCH`, `DOG_NOT_ACTIVE` → **422**; `STALE_VERSION` → 409; `DOG_NOT_ACCESSIBLE`, `MODULE_DISABLED`, `NOT_FOUND` → 404; `FILE_TOO_LARGE`, `FILE_TYPE_NOT_ALLOWED`, `VALIDATION_ERROR`, `INVALID_FILTER` → 400; `FORBIDDEN`, `IMPERSONATION_DENIED` → 403.
- Nothing is ever physically deleted (BR-12): tasks carry `deletedAt`, attachments `removedAt`, follow-up rows `hidden`. The stored file is only destroyed by the RGPD erasure of S14.
- `clubs.followup` → `clubs.bookings.application` and `clubs.census.application`; `clubs.bookings` never imports `clubs.followup` (the port inverts it); `clubs.followup` never writes the `dogs` or `members` collections directly — it calls `DogService`/`MemberService`. `ArchitectureTest` enforces both.
- Parameter keys never literals: `files.maxSizeMb` (25), `files.allowedTypes`, `files.maxAttachmentsPerEntity` (10), `files.dogPhotoMaxMb` (8), `levels.enabled`, `club.timeZone`. Signed download URLs last 5 minutes (assumption in force, S10 R-10-11); upload grants keep the 300 s of the existing service.
- Vocabulary: never «parella/parelles»; observations are «privades (instructors i administració)», the member's note is read-only here. Texts of tasks, notes and observations are **member content**: never translated, never truncated in storage (only `textExcerpt` is).
- Evidence rule: report = command + exit code + last 40 lines; long outputs → `roadmap/evidence/E6-T03/NN-<name>.log`; task file ≤ 120 KB. Fictional data only (`@example.test`); no secrets, env vars only. Never edit other tasks, `ROADMAP.md` or the Organizer verification section. English identifiers from the glossary; i18n keys never literals; ca/es/en together.

## Verification (run all; evidence = command + exit code + last 40 lines; long outputs → `roadmap/evidence/<ID>/*.log`)
- `./mvnw -q verify` green with test methods named after **T-10-06, T-10-15, T-10-16, T-10-17, T-10-18, T-10-25** and the follow-up half of **T-10-22**, plus `ArchitectureTest`, `AuditContractTest`, `EventCatalogContractTest` (the two Annex A events registered), message parity ca/es/en including `notif.N-20.*`, `notif.N-21.*`, `notif.N-22.*` and every `error.*` used here, coverage gates.
- `E6ContractIT` updated: the routes of this task no longer answer 501 (attendance and instructor routes still do until E6-T02 lands); `bin/openapi-snapshot` byte-identical or a `docs/openapi/CHANGELOG.md` entry naming the shape that changed; `git diff --check` clean.
- Manual on the local stack: upload a 20 MB `video/quicktime` through the signed URL and attach it to a new task (show the `201`, the `AttachmentAdded` outbox row and the N-20 `notifications` row); then a 30 MB file → `FILE_TOO_LARGE {maxSizeMb: 25}`; an `.exe` → `FILE_TYPE_NOT_ALLOWED`; an eleventh attachment → `ATTACHMENT_LIMIT_REACHED`; a `fileKey` issued for `DOG_DOCUMENT` → `ATTACHMENT_ENTITY_MISMATCH`; complete the task as the owner → N-21 rows for the three instructors; change the member note → one `MEMBER_NOTE` row, unread for everyone but the author, N-22 rows; `GET /followup/unread-count` for two different accounts; `read-all` for one of them → 0 while the other stays.

## Definition of Done
Instructors create, edit, delete, complete and reopen tasks with attachments on the storage that already exists; the member ticks their own tasks and gets N-20, the instructors get N-21 and N-22; private observations live on `Dog.remarks`, are audited and never leak to a member; D14 lists tasks and member notes with per-user unread marks, universal filters and an O(1) read-all; the attendance sheet and the student card get their task data through `DogFollowupPort`; with `TASKS` off the whole surface is a clean 404.

## Evidence to return
`verify` tail with the test-method list mapped to T-10 ids; the manual attachment sequence (each status code with its `ApiError` body, ids truncated); the outbox rows for `TaskCreated`, `TaskCompleted`, `AttachmentAdded`, `AttachmentRemoved`, `TaskReopened` and the `notifications` rows for N-20/N-21/N-22 in two languages; the `GET /followup` first page for two accounts showing different `unread` values; the `unread-count` before and after `read-all`; the diff of `AttachmentService` (what you generalized from `addNote`); the Annex A proposals you filed in `MESSAGES.md`.

---

## Executor report
_(not started)_

### Branch / commits

### Evidence (full command outputs)

### Files changed

### Rules and tests implemented (R-xx-nn / T-xx-nn)

### Assumptions

### Questions / catalog proposals

## Organizer verification
_(pending)_
