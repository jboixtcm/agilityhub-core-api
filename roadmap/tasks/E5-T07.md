---
id: E5-T07
title: Concurrency guarantees proven on Mongo (shared, configurable local lanes keyed per aggregate) and deterministic outbox dispatch in integration tests
stage: E5
repo: agilityhub-core-api
thread: A
order: 70
status: verified
depends_on: [E4-T04, E5-T02, E5-T04, E5-T05]
branch:
pr:
updated: 2026-09-24
---

# E5-T07 · Concurrency guarantees proven on Mongo + deterministic outbox dispatch in ITs

## Objective
Three independent reviews (E4-T04 #1, E5-T02 #3, E5-T04 #1–#3) found the same pattern: `ActivityTransactions`, `BookingTransactions` and `TrainingTransactions` each keep 256 in-JVM fair `ReentrantLock` "lanes" held around the whole retried transaction. Inside one JVM they serialize the writers, so the concurrency tests (T-07-24/25, T-08-29/31/32, T-09-32/33) never reach the mechanism the specs name as the guarantee: the `$inc` sequences with `WriteConflict` retries (R-07-08, R-08-07) and the partial unique index `training_active_seat` (R-09-06). They would still pass if that mechanism were broken. R1 runs **one API instance** (ADR-003), so the lanes are acceptable as a contention bound, but the Mongo mechanism must be proven on its own, the lanes must be one shared component keyed per aggregate (the activity lanes are keyed by tenant, so unrelated clubs block each other), and the single-instance assumption must be written down.

The same reviews and the CI history found a second systemic problem: integration tests that call `dispatch()` on the outbox are flaky on the CI runner (T-07-23 in E4-T04, masked with Awaitility in round 2; `BookingsIT.T_08_24` failed and `T_08_26`/`T_08_27` errored at `BookingFixtures.impersonating:126 NoSuchElement` on the run of `8ce1b5c`, then passed on the next run with no test change). Find the root cause and fix it at the root.

## Spec references
S07 R-07-08, T-07-23/24/25 · S08 R-08-07, T-08-29/31/32 · S09 R-09-06, R-09-13, T-09-28/32/33 · `docs/decisions/ADR-003-hosting-i-entorns.md` (one droplet, one API instance, single-node replica set) · `docs/PLA_BACKEND.md` (testing layers).

## Context to load
This file · `roadmap/reviews/E4-T04-*.md` (#1, #9), `roadmap/reviews/E5-T02-*.md` (#3), `roadmap/reviews/E5-T04-*.md` (#1–#3) · `ActivityTransactions`, `BookingTransactions`, `TrainingTransactions`, `TrainingBookingService`, `RingBlockService`, `ClassSessionService` (S06 side of R-09-13) · `OutboxDispatcher` and every test helper that calls `dispatch()` · `ActivityIT`, `BookingsIT`, `BookingConcurrencyIT`, `WaitlistIT`, `TrainingConcurrencyIT`, `BookingFixtures`.

## Steps
1. **One `LocalLanes` component** (in `shared.application`) replaces the three copies: fair locks taken in lane order, keyed **per aggregate** (activity id for S07, class id for S08, the S09 keys `dog:`, `member:`, `slot:`), never per tenant. It is switched by an application property `core.concurrency.local-lanes` (default `true`; infrastructure setting, not a club parameter), documented in `application.yml` and in `docs/DEPLOY.md`: «R1 runs a single API instance (ADR-003). The local lanes only bound contention inside one process; with more than one instance they protect nothing and the Mongo mechanisms below are the guarantee.»
2. **Mongo-path tests with the lanes off** (a test context with `core.concurrency.local-lanes=false`), one per mechanism, each asserting the invariant and proving the mechanism ran (for example a retry counter or metric incremented, or a `DuplicateKey` observed):
   - S07 (T-07-24/25): 20 concurrent registrations for 1 place → exactly one `ACTIVE`, never two; every other request ends `WAITLISTED` or `409 STALE_VERSION`; `registrationSeq` consistent.
   - S08 (T-08-29/31/32): last-seat burst → never overbooked; each request ends `201`, `409 CLASS_FULL` or `409 STALE_VERSION`; the `seat_locks` write conflict and its retry are exercised.
   - S09 (T-09-32/33): two direct inserts of an `ACTIVE` booking on the same `{clubId, ringId, startsAt, seatIndex}` → `DuplicateKeyException` → the service answers `SLOT_TAKEN`; a burst on one dog → the `trainingSeq` write conflict → retry → `TRAINING_LIMIT_REACHED` where due.
   Record in the report how many requests end in `STALE_VERSION` with the lanes off. Do not change the retry budgets of R-07-08/R-08-07/R-09-06 (spec values): if the numbers look too high, propose a value in the report.
3. **Same dog on two rings** (review E5-T04 #2): under `bookings.limitUnit = MEMBER`, two members of one family group can book the same shared dog at the same time on two rings, because their `$inc` hit different `Member` documents and the index is per ring and seat. Make every training booking also touch the dog (for example `$inc` a `Dog.trainingSeq`, whatever the unit), so the two transactions conflict in Mongo. Test with the lanes off.
4. **Write skew training ↔ ring block / class** (review E5-T04 #3, R-09-13): the booking reads «no class, no block» and inserts, while the S06 side reads «no live booking» through `TrainingConflictPort` and inserts; they share no document. Make both sides write one shared document per ring and day (for example a `$inc` on a ring-day sequence) so Mongo raises a write conflict and the retried side sees the other. Test with the lanes off: a concurrent block and booking on the same slot never leave a block over an `ACTIVE` booking without `RING_HAS_BOOKINGS`.
5. **Deterministic outbox dispatch in ITs**: find why `dispatch()` sometimes finds nothing to process on the CI runner (a likely cause: several cached Spring test contexts each run a background `OutboxDispatcher` against the same database and claim other test classes' events; or a lease/backoff in the dispatcher). Fix it at the root — for example no background polling in the test profile, or dispatch isolated per context — and remove the polling that only compensates for it (T-07-23's Awaitility loop). Then run `ActivityIT`, `BookingsIT`, `WaitlistIT` and `TrainingIT` **five times in a row** with `-XX:ActiveProcessorCount=2` (the CI runner has 2 cores) and paste the tails.

## Fixed conventions
AGENTS.md rules · no change to catalogs, error codes, parameters or spec values · one commit-able change set (the publish script commits it) · evidence under `roadmap/evidence/E5-T07/`.

## Verification (run all; evidence = command + exit code + last 40 lines; long outputs → `roadmap/evidence/E5-T07/*.log`)
- `./mvnw -q clean verify` → exit 0.
- The five repeated runs of step 5 (one log each).
- A search that shows no remaining `ReentrantLock[]` lanes outside `LocalLanes`: `grep -rn "ReentrantLock" src/main/java`.

## Definition of Done
One `LocalLanes` component keyed per aggregate and switchable; every concurrency mechanism the specs name is proven by a test with the lanes off; the dog and ring-day conflicts exist in Mongo; the single-instance assumption is in `application.yml` and `docs/DEPLOY.md`; the flaky `dispatch()` root cause is explained in the report and fixed, with the five clean repeated runs; `clean verify` green.

## Evidence to return
The report sections below, the repeated-run logs, the `STALE_VERSION` numbers with the lanes off, and the root-cause paragraph of step 5.

---

## Executor report
Executor: claude:claude-opus-5-5 · 2026-09-24. Dependencies E4-T04, E5-T04 are `verified`; E5-T02 and E5-T05 are `awaiting_verification` (round 2), allowed by the protocol. If either comes back as `changes_requested`, this work must be rebased onto it.

**Summary.**
- **Step 1.** One `shared.application.LocalLanes` replaces the three 256-lane arrays. It keeps one fair lock per aggregate key while the key is held or awaited: `activity:<id>`, `class:<id>`, `dog:`/`member:`/`slot:`/`day:`, each prefixed with the open tenant. It never locks per tenant, and it takes keys in sorted order. The property `core.concurrency.local-lanes` switches it (default `true`, documented in `application.yml` and `docs/DEPLOY.md`).
- **Step 2.** Every mechanism is proven with the lanes off in three new ITs. Each has a burst for the invariant and a deterministic test: a Mongo transaction held open in a background thread makes the request under test meet the write, and the new retry meter shows the retry.
- **Step 3.** Every training booking `$inc`s `Dog.trainingSeq`.
- **Step 4.** A training booking and the S06 writes that check the ring's bookings share a ring-day sequence (`ring_day_locks`).
- **Step 5.** The outbox root cause was found and reproduced with a counterfactual run, then fixed in `AbstractIntegrationTest`. The T-07-23 Awaitility loop is removed.
- **Result.** `./mvnw -q clean verify` exits 0: 459 unit + 739 IT, all gates. The five repeated runs on 2 CPUs are green.

### Branch / commits
None. Git writes are blocked for the executor. The publish script commits the working tree on `main` after the session.

### Evidence (full command outputs)

**1. `./mvnw -q clean verify`** → **exit 0**. Full log: `roadmap/evidence/E5-T07/07-final-clean-verify.log`. This is the final run, after every code change. `-q` prints no summary, so the counts come from the reports:

- `target/surefire-reports`: 100 classes, **459 tests, 0 failures, 0 errors, 0 skipped**.
- `target/failsafe-reports`: 72 classes, **739 tests, 0 failures, 0 errors, 0 skipped**.

The JaCoCo `check` (domain+application ≥ 85 % lines / 80 % branches, api ≥ 70 %), ArchUnit and `OpenApiSnapshotTest` run inside the same `verify`. The OpenAPI is unchanged: no controller signature changed.

New or changed classes in `target/site/jacoco/jacoco.csv`:

| Class | Lines | Branches |
|---|---|---|
| `LocalLanes` | 20/20 | 16/16 |
| `TransactionRetries` | 13/13 | 10/10 |
| `ActivityTransactions` | 15/15 | 14/14 |
| `BookingTransactions` | 16/16 | 13/14 |
| `TrainingTransactions` | 18/18 | 18/18 |
| `RingDayLockRepository` | 5/5 | – |
| `RingScheduleAccess` | 12/12 | 12/14 |

Its last 40 lines are in `07-final-clean-verify.log`. They were cut from this report in round 2, because only the final run keeps its tail (Round 2 below).

**2. The five repeated runs of step 5** (ActivityIT, BookingsIT, WaitlistIT, TrainingIT, `-XX:ActiveProcessorCount=2`). The exact command, identical for runs 1–5:

`./mvnw verify -Dtest=NoSuchTest -Dsurefire.failIfNoSpecifiedTests=false -Dit.test='ActivityIT,BookingsIT,WaitlistIT,TrainingIT' -Djacoco.skip=true -DargLine=-XX:ActiveProcessorCount=2 -Dfailsafe.runOrder=random`

`-Dfailsafe.runOrder=random` also varies the class order between runs. `-Djacoco.skip=true` is needed so that `argLine` is not replaced by the JaCoCo agent; the coverage gate itself is proven by run 1 above. Every run: **exit 0**, `Tests run: 69, Failures: 0, Errors: 0, Skipped: 0`, `BUILD SUCCESS`, `Integration JVM availableProcessors=2`. Class order:

| Run | Class order |
|---|---|
| 1 | Activity, Waitlist, Training, Bookings |
| 2 | Training, Activity, Bookings, Waitlist |
| 3 | Training, Bookings, Waitlist, Activity |
| 4 | Training, Waitlist, Activity, Bookings |
| 5 | Bookings, Waitlist, Activity, Training |

Their last 40 lines are in `02-repeat-1.log` … `02-repeat-5.log`. They were cut in round 2; the final runs are in Round 2 below.

**3. `grep -rn "ReentrantLock" src/main/java`** → exit 0. Complete output (only `LocalLanes`):
```
src/main/java/com/agilityhub/core/shared/application/LocalLanes.java:7:import java.util.concurrent.locks.ReentrantLock;
src/main/java/com/agilityhub/core/shared/application/LocalLanes.java:25:    private static final class Lane { final ReentrantLock lock = new ReentrantLock(true); int users; }
src/main/java/com/agilityhub/core/shared/application/LocalLanes.java:49:    private ReentrantLock acquire(String key) {
```

**4. Step 5: the root cause, and why the fix is at the root.** Every cached Spring test context uses the same database, `agilityhub_test`. `OutboxDispatcher.dispatch()` claims records across tenants (`OutboxRepository.claim`): PENDING records with `nextAttemptAt ≤ now`, oldest `nextAttemptAt` first, at most 100 per call. `nextAttemptAt` is stamped with each context's `MockClock`, and many tests run at `2026-01-01`, far earlier than the S07/S08 fixtures (`2026-09-14`, `2026-10-06`). Test classes that publish events without dispatching them leave them PENDING.

A test's few synchronous calls (`BookingFixtures.dispatch()` = 6 × 100, `ActivityIT` = 5 × 100) therefore first spend their claims on other tests' older events. If the claimable backlog exceeds 500–600, the test's own events are never reached. The assertion then sees nothing: `0 of 6` N-32b rows in T-07-23, or an entry still `ACTIVE` after `cancel(); dispatch();`.

How big the backlog is depends on which classes ran before, so on the class order. Failsafe's default `filesystem` order is directory order: stable on macOS, arbitrary on the Linux CI runner. That is why the CI failed and then passed without a test change. Awaitility "fixed" T-07-23 only because its 100 ms polling calls `dispatch()` up to about 100 times (about 10 000 claims), enough to drain the backlog.

Measured:

- The final clean verify prints the PENDING backlog each test starts with when it is above one batch: 446 before `DashboardIT`, 158 before `SignupIT`, 651/724 before `DemoScenarioSeedIT`, 651–663 before `DemoPlanningSeedIT`/`DemoSeedsIT`, and 163–167 before `PlayoffMigrationIT`. These are 15 lines `E5-T07 outbox backlog discarded…` in `07-final-clean-verify.log`.
- **Counterfactual** (`05-counterfactual-heavy-backlog-first.log`, exit 1): with the discard disabled (`-Dit.keepOutboxBacklog=true` in the forked JVM) and the backlog-heavy classes first (`PlayoffMigrationIT, DashboardIT, Demo*SeedIT, SignupIT` before `WaitlistIT, BookingsIT, ActivityIT`, `-Dfailsafe.runOrder=reversealphabetical`), `WaitlistIT.T_08_21` fails the CI way: `["state"="ACTIVE" (expected: "NOTIFIED")]` right after `cancel(...); dispatch();`. Result: `Tests run: 114, Failures: 1`.
- **Control** (`06-control-heavy-backlog-first-fixed.log`, exit 0): the same classes, order and 2 CPUs with the discard on give `Tests run: 114, Failures: 0`.
- `OutboxIT.E5_T07_anEarlierPendingBacklogIsClaimedFirstAndDelaysALaterEventByWholeBatches` reproduces the starvation deterministically: 150 older records, then 1 dispatch leaves our event PENDING and a second one delivers it.
- Honest note: two other counterfactual runs **passed** with the backlog kept. One ran `DemoScenarioSeedIT` first and then the three classes (`03-…`, exit 0). The other was the whole suite in the local filesystem order (`04-…`, 739 tests, exit 0). In those orders the claimable backlog stays under the 500–600 claims. Only an order like the one above reaches it.

The fix: `AbstractIntegrationTest.discardOutboxBacklog()` (`@BeforeEach`, runs before every subclass fixture) deletes the PENDING outbox records that earlier tests left behind. Each test starts with an empty backlog, so `dispatch()` only ever sees the test's own events and the 100-per-call batch holds for any class order. Tests never run in parallel (one failsafe JVM, no JUnit parallelism), and every fixture rebuilds its own data, so nothing else depends on another test's pending events. No background dispatcher runs in the ITs: all Spring test contexts set `shared.scheduling.enabled=false`. The T-07-23 Awaitility loop is replaced by the plain `dispatch()` + assertion.

**Not explained:** the CI `BookingFixtures.impersonating:126 NoSuchElement` of `8ce1b5c` (T_08_26/27). Reading the call path, the only `NoSuchElementException` sources are in `SigningKeys`: `ring()`'s `findById("active").orElseThrow()` or `keys().getFirst()`. I could not tell which one, or why, without the CI stack trace. `gh run list --branch main --limit 30` was denied in this session (see Questions 4). It did not reproduce in any local run.

**5. STALE_VERSION with the lanes off** (burst of 20, final clean verify; the other runs in `01`, `08`, `09` agree):

| Burst | Result | Retries | Exhausted (`409 STALE_VERSION`) |
|---|---|---|---|
| S07 T-07-24: 20 registrations, 1 place, waiting list on | `201 ACTIVE=1, 201 WAITLISTED=13, 409 STALE_VERSION=6` | 43 | 6/20 (other runs: 6–7) |
| S08 T-08-29: 20 holds for the last seat | `201=1, 409 CLASS_FULL=11, 409 STALE_VERSION=8` | 32 | 8/20 (other runs: 7–9) |
| S09 T-09-32: 20 dogs, capacity 1 | `201=1, 409 SLOT_TAKEN=19` | 19 | 0 |
| S09 T-09-32: 20 dogs, capacity 2 | `201=2, 409 SLOT_TAKEN=18` | 26–30 | 0 |
| S09 T-09-33: Rock at 2/3, five slots | `201=1, 409 TRAINING_LIMIT_REACHED=4` | 4–5 | 0 |

T-07-25 (two concurrent cancellations, 3 rounds) gives `200 ×2` in every round, with the counters coherent and one promotion. T-08-32 (swap vs cancellation, 3 rounds) gives `422 SWAP_NOT_ALLOWED` + `200`. The step-3 bursts give one `201` + one `422 DOG_ALREADY_BOOKED`. In the R-09-13 bursts (6 rounds) the block always won locally, with `409 SLOT_TAKEN / 201`. The invariant was never broken.

The seat-index conflict inside a transaction surfaces as a `WriteConflict` (112), not a `DuplicateKey`: `duplicate_key 0`, `write_conflict` only. `TrainingTransactions.retryable` handles both. The retry budgets are not changed (Questions 2).

**Earlier attempts (one line each):**
- `08-attempt1-lanes-off-its.log`: first run of the three new ITs, exit 1. Two of my expectations were wrong (`DOG_ALREADY_BOOKED` and `BOOKING_NOT_CANCELLABLE` are HTTP 422, not 409), so I fixed the expected statuses, including 422 for `SWAP_NOT_ALLOWED`.
- `09-targeted-its.log`: the new ITs plus ActivityIT, BookingsIT, WaitlistIT, TrainingIT, both concurrency ITs, OutboxIT and CalendarIT, exit 0 (113 tests).
- `01-clean-verify.log`: first `./mvnw -q clean verify`, exit 0 (459 + 739). I then added the processor-count print and the counterfactual switch to `AbstractIntegrationTest`, which is why run 1 above was repeated.

### Files changed
- **Main.**
  - `shared/application/LocalLanes.java` (new).
  - `shared/application/TransactionRetries.java` (new).
  - `clubs/activities/application/ActivityTransactions.java`: lanes per activity id, metrics. The retry budget is unchanged (4 attempts).
  - `ActivityService`, `ActivityLifecycleService`, `ActivityRegistrationService` (`activityOf`), `ActivityApiService` (`registrationActivityId`) and `api/ActivitiesController.java` (`mutation(activityId, …)`): every write names its activity lane.
  - `clubs/bookings/application/BookingTransactions.java`: `class:` lanes, metrics.
  - `clubs/training/application/TrainingTransactions.java`: shared lanes, metrics.
  - `TrainingBookingService.java`: `lanes()` is now an instance method with a `day:` key; step (5) calls `schedule.lockRingDay(chosen, date)`.
  - `training/api/TrainingController.java`.
  - `clubs/census/application/TrainingMemberAccess.java`: `touchTrainingSeq(dogUnit, dogId, memberId)` always `$inc`s the dog.
  - `clubs/scheduling/persistence/RingDayLock.java` and `RingDayLockRepository.java` (new).
  - `scheduling/application/RingScheduleAccess.java` (`lockRingDay`, `MANDATORY`).
  - `RingBlockService.java`: `lockDay` in create, patch and `syncForActivity`, with the `FREE_TRAINING` module on.
  - `ClassSessionService.java`: ring-day touch when a class moves onto a ring.
  - `src/main/resources/application.yml` (`core.concurrency.local-lanes`).
- **Tests.**
  - `support/AbstractIntegrationTest.java`: backlog discard, processor print, counterfactual switch.
  - `support/ConcurrencySupport.java` (new).
  - `activities/application/ActivityFixtures.java` (new, extracted from `ActivityIT`) and `ActivityIT.java`: Awaitility removed.
  - `ActivityConcurrencyIT.java`, `bookings/api/BookingLanesOffIT.java`, `training/api/TrainingLanesOffIT.java` (new).
  - `training/api/TrainingFixtures.java` (`ring_day_locks` cleanup).
  - `shared/persistence/OutboxIT.java` (new test).
  - `shared/application/LocalLanesTest.java` (new).
  - `BookingPortsTest`, `TrainingApplicationTest` and `ActivityInfrastructureTest` (new constructors, meter assertions).
- **Docs.** `docs/DEPLOY.md` (E5 section: «One API instance and the local lanes») and `CHANGELOG.md`.
- **Evidence.** `roadmap/evidence/E5-T07/01…09-*.log`.

### Rules and tests implemented (R-xx-nn / T-xx-nn)
- **R-07-08, T-07-24/25 (Mongo path).**
  - `ActivityConcurrencyIT.T_07_24_lanesOffTwentyRegistrationsForOnePlaceLeaveExactlyOneActiveAndTheRestWaitlistedOrStale` checks exactly 1 ACTIVE, unique FIFO positions, `registrationSeq` delta = committed registrations, retries > 0, and exhausted = `STALE_VERSION` replies.
  - `T_07_24_aRegistrationThatMeetsAHeldRegistrationSeqIncIsRetriedAndCommitsAfterIt` (deterministic, `write_conflict` meter).
  - `T_07_25_lanesOffConcurrentCancellationsPromoteAtMostOnceAndKeepCountersCoherent`.
  - T-07-23 is deterministic again.
- **R-08-07, T-08-29/31/32 (Mongo path).**
  - `BookingLanesOffIT.T_08_29_lanesOffTheLastSeatBurstNeverOverbooksAndEveryRequestEndsHeldFullOrStale`.
  - `T_08_29_aHoldThatMeetsAHeldSeatLockIsRetriedAndThenAnswered`: the `seat_locks` write conflict, then the retry, then `201`; `seat_locks.version` +2.
  - `T_08_31_lanesOffTwoKeysForOneHoldGiveOneBooking`.
  - `T_08_32_lanesOffASwapRacingACancellationOfTheSameOldBookingLeavesCoherentCounters`.
- **R-09-06, T-09-32/33 (Mongo path).**
  - `TrainingLanesOffIT.T_09_32_twoDirectActiveInsertsOnOneSeatHitThePartialUniqueIndex`: `DuplicateKeyException`, and a CANCELLED row does not count.
  - `T_09_32_aBookingThatMeetsAnUncommittedRowOnItsSeatIsRetriedAndAnswersSlotTaken`: the index conflict, then the retry, then `409 SLOT_TAKEN{reason: TRAINING}`.
  - `T_09_32_lanesOffTwentyDogsOnOneSlotNeverShareASeat` (capacity 1 and 2).
  - `T_09_33_aBookingThatMeetsAHeldTrainingSeqIncIsRetriedAndThenReachesTheLimit`: the `trainingSeq` write conflict, then the retry, then `TRAINING_LIMIT_REACHED{used: 3}`.
  - `T_09_33_lanesOffFiveParallelBookingsOfADogAtTwoOfThreeNeverPassTheLimit`.
- **Step 3 (R-09-06, review E5-T04 #2).** `T_09_33_underMemberUnitTwoFamilyMembersCannotBookTheSharedDogOnTwoRingsAtOnce`: deterministic `422 DOG_ALREADY_BOOKED` through the dog's `trainingSeq`, plus two lanes-off bursts.
- **Step 4 (R-09-13, T-09-28, review E5-T04 #3).**
  - `T_09_28_aRingBlockDuringAnUncommittedBookingConflictsInsteadOfSkippingIt`: the S06 side gets `409 STALE_VERSION`, never `201`, and after the commit gets `422 RING_HAS_BOOKINGS`.
  - `T_09_28_aBookingDuringAnUncommittedRingBlockIsRetriedAndSeesTheBlock`: `409 SLOT_TAKEN{reason: RING_BLOCK}`.
  - `T_09_28_lanesOffAConcurrentBlockAndBookingOnOneSlotNeverBothCommit` (6 rounds).
- **Lanes and meters.** `LocalLanesTest` covers per-key serialisation, other aggregates and other clubs not blocking, key-order no-deadlock, release on failure, the disabled mode, and the meters by cause.
- **Outbox.** `OutboxIT.E5_T07_anEarlierPendingBacklogIsClaimedFirstAndDelaysALaterEventByWholeBatches`.

### Assumptions
1. **Lane keys** carry the open tenant as a prefix, so the same id in two clubs never shares a lane. That is still "per aggregate", not per tenant. Training lanes add `day:<club-local date>` because the bookings of one day now share ring-day documents; the lane queues them locally instead of spending retries. `slot:` is kept as the task names it.
2. **The ring-day document** is keyed `clubId:ringId:date` (club-local date).
   - Blocks and classes are single-day (`RingBlockService.validate` and the class rules), so one document per write is enough.
   - S06 touches it only on the write paths that already check the training bookings: block create/patch, `syncForActivity`, and a class moved onto a ring. The read-only previews (`ring-conflicts`, the calendar) never write.
   - The touch is skipped when `FREE_TRAINING` is off, because then there are no bookings to conflict with.
3. **The `ActivitiesController` mutations** lane on the activity they write. `POST /activities` (create) has no existing activity, so it takes no lane. A registration cancellation lanes on its registration's activity, read before the transaction; the id never changes. `cancelMatching` (inactivity or member left) lanes on the member's registrations' activities.
4. **The outbox discard** deletes only PENDING records (PUBLISHED/FAILED stay for tests that count events). It runs in the base class `@BeforeEach`, before every subclass fixture. `-Dit.keepOutboxBacklog=true` disables it for the counterfactual evidence only. It is a JVM system property of the forked test JVM and never set by the build.
5. **Test support.** `AbstractIntegrationTest` prints `Integration JVM availableProcessors=N` once per JVM. It proves the `-XX:ActiveProcessorCount=2` runs.
6. **The retry meters** (`core.transactions.*`) are observability, not catalog items (parameters, events, notifications, error codes), so no catalog change applies.

### Questions / catalog proposals
1. **Model proposal (collection).** Please add the technical collection `ring_day_locks {_id: clubId:ringId:date, ringId, date, sequence}` (S09 R-09-13, owner `clubs.scheduling`) to `MODEL_DADES_PLATAFORMA.md`, next to `seat_locks`. It is the shared document step 4 asks for. The model does not list it yet; I implemented it as the task specifies.
2. **Retry budgets (not changed).** With the lanes off, 30–45 % of a 20-request burst on one S07 activity or S08 last seat ends `409 STALE_VERSION`: 6–7/20 and 7–9/20. That is because the burst's winners commit one by one while the losers have only 3 more attempts (S07) or 3 attempts in total (S08) at 50–150 ms. S09 bursts end without exhaustion, because a loser's retry already sees the winner. If more than one API instance is ever run, I propose raising R-07-08/R-08-07 to 5 attempts with jittered exponential backoff (50, 100, 200, 400 ms). Alternatively, keep one instance, as `docs/DEPLOY.md` now says.
3. **S06 retry has no backoff.** `SchedulingTransactions` retries 6 times with no backoff. While a training booking of the same ring-day is uncommitted, a ring block therefore answers `409 STALE_VERSION` within milliseconds (`T_09_28_aRingBlockDuringAnUncommittedBookingConflictsInsteadOfSkippingIt`). It is correct and safe, and the admin can retry, but a 50–150 ms backoff like the other contexts would usually hide it. Not changed (outside this task's scope). Should E5-T08/T09 take it?
4. **CI log access.** Denied command: `gh run list --branch main --limit 30` (and so `gh run view <id> --log-failed`). I needed it to read the `8ce1b5c` stack trace of `BookingFixtures.impersonating:126 NoSuchElement`. The organizer, who has `gh`, can check whether the trace ends in `SigningKeys.ring()` (`findById("active").orElseThrow()`) or in `keys().getFirst()`. Nothing in the test sources deletes `signing_keys`. The backlog fix does not explain an impersonation failure, so this item stays open. My assumption in the meantime: it is a separate flake to watch in the next CI runs. *(Round 2: that sentence was wrong — the two Demo seed ITs did delete `signing_keys`; see Round 2, point 1.)*

### Round 2
Executor: claude:claude-opus-5-5 · 2026-09-24. This round fixes exactly the six points of the Organizer verification. The dependencies are unchanged. E5-T09, whose ring update (`RING_HAS_BOOKINGS` + `cancelBookings`) point 3 builds on, is `awaiting_verification`.

**Summary.**
- The `impersonating:126` flake is explained, reproduced and fixed (point 1).
- The R-09-13 serialisation now uses one document per **ring slot** (`ring_slot_locks`). The ring-day document made 15 of 20 bookings on 20 different slots of one day exhaust their retries (point 4).
- S05 conflicts in Mongo too (point 3).
- `./mvnw -q clean verify` exits 0: 483 unit + 767 IT, 0 failures.

#### Point by point
1. **`signing_keys` (review #1).**
   - **Cause.** `SigningKeys` writes the encrypted key ring once, in its constructor, and reads it on every token (`ring()` → `findById("active").orElseThrow()`, `SigningKeys.java:43`). `DemoScenarioSeedIT` and `DemoPlanningSeedIT` emptied every non-`system.` collection, `signing_keys` included. After that, any Spring context created before them failed its next impersonation with `NoSuchElementException`. A context created later still passed, because its constructor rebuilt the ring. So the failure depends on the class order.
   - **Fix.**
     - `AbstractIntegrationTest.wipeDatabaseKeepingBootstrap()` empties every collection except `system.*` and `BOOTSTRAP_COLLECTIONS = {signing_keys}`. Both Demo seed ITs use it.
     - No other collection is bootstrap-owned. OIDC clients live in memory (`InMemoryRegisteredClientRepository`). The other startup runners (identity, audit, platform, email, attachments, exports, shared, security events) only create collections and indexes, which `remove` keeps.
   - **Other ITs checked.** None of them wipes the shared database:
     - `HealthIndependenceIT` has its own container and database (`health_empty`) and an ephemeral key (`core.oidc.master-key=`). It only counts documents.
     - `E4ContractIT` and `E5ContractIT` read `getCollectionNames()` only for their before/after snapshots.
   - **No production change.** A missing ring still fails loudly.
   - **Counterfactual.**
     - `-Dit.wipeBootstrap=true` in the forked JVM restores the old wipe; it exists for this evidence only.
     - Command (**exit 1**): `./mvnw verify -Dtest=NoSuchTest -Dsurefire.failIfNoSpecifiedTests=false -Dit.test='BookingsIT,DemoScenarioSeedIT,JobsApiIT' -Dfailsafe.runOrder=random -Dfailsafe.runOrder.random.seed=1 -Djacoco.skip=true -DargLine=-Dit.wipeBootstrap=true` → `13-r2-counterfactual-old-wipe.log`.
     - The order is `JobsApiIT` → `DemoScenarioSeedIT` → `BookingsIT`. `BookingsIT` reuses the `BookingFixtures` context built for `JobsApiIT`, and `BookingsIT.T_08_27` errors the CI way: `BookingFixtures.impersonating:126 » NoSuchElement No value present` at `SigningKeys.ring(SigningKeys.java:43)`.
     - The control, with the same command and seed but without the switch (`14-r2-control-helper.log`, **exit 0**), gives `Tests run: 26, Failures: 0, Errors: 0`.
     - Two earlier orders (alphabetical, then reverse alphabetical with `ClubPagesIT` first) passed even with the old wipe. In both, no `BookingFixtures` context existed before the wipe. `BookingFixtures` has its own context key, and `clubs.bookings` sorts before `clubs.census`. The seeded random order fixes that. Those logs were overwritten by the seed-1 run.
2. **Test names (review #2).** The three ring-block tests of `TrainingLanesOffIT` are now `R_09_13_aRingBlockDuringAnUncommittedBookingConflictsInsteadOfSkippingIt`, `R_09_13_aBookingDuringAnUncommittedRingBlockIsRetriedAndSeesTheBlock` and `R_09_13_lanesOffAConcurrentBlockAndBookingOnOneSlotNeverBothCommit`. The `T_09_28_*` names now belong to the S05 tests of point 3, which is what T-09-28 describes.
3. **S05 side of R-09-13 (review #3).**
   - **Change.** `RingTrainingBookings.lockBookableSlots(ringId)` (`MANDATORY`) runs in `CatalogService.stopsBeingReservable`, before `futureActive`, whenever a ring is deactivated or loses `allowsFreeTraining`. The S09 adapter (`RingReservability` → `TrainingSlotLocks.bookable`) `$inc`s every grid slot of the ring from now to the end of the booking window (`training.bookingWindowDays`). Those are the only slots a concurrent booking can take.
   - **Tests, all with the lanes off:**
     - `T_09_28_aRingMadeNotReservableDuringAnUncommittedBookingConflictsInsteadOfSkippingIt`: the PATCH meets the write conflict → `409 STALE_VERSION`, never 200. After the commit it gets `422 RING_HAS_BOOKINGS`.
     - `T_09_28_aBookingDuringAnUncommittedRingChangeIsRetriedAndSeesTheRingNotReservable`: a `write_conflict` retry → `422 RING_NOT_RESERVABLE`.
     - `T_09_28_lanesOffAConcurrentRingChangeAndBookingNeverLeaveALiveBookingOnANonReservableRing`: 4 rounds over `allowsFreeTraining` and `active`.
   - `CatalogService` has no retry loop of its own, so its side answers `409 STALE_VERSION`, as the existing S05 write-conflict mapping does.
4. **Ring-day hot spot (review #4).**
   - **Measured on the ring-day design.** `R_09_13_lanesOffTwentyBookingsOnTwentySlotsOfOneRingDayNeverExhaustTheirRetries` (20 dogs, 20 different 30-min slots of one ring-day, lanes off) gave `{201=5, 409 STALE_VERSION=15} · retries 36 · exhausted 15` (`10-r2-ring-day-hotspot-before.log`, exit 1).
   - **Change.** Requests exhausted their retries, so as instructed I switched to one document per ring slot and dropped the `day:` lane:
     - `RingSlotLock` / `RingSlotLockRepository` (`ring_slot_locks {_id: clubId:ringId:startsAt, clubId, ringId, startsAt, sequence}`, owner `clubs.scheduling`) replace `RingDayLock*`.
     - A booking touches its own slot (`RingScheduleAccess.lockRingSlots`).
     - `RingBlockService` (create, patch, `syncForActivity`) and `ClassSessionService` (class moved onto a ring) call the new `TrainingConflictPort.lockSlots(ringId, from, to)`. The S09 `TrainingSlotLocks.overlapping` touches every training grid slot the range overlaps. It computes the grid with `TrainingGrid.slots` from `club.openingHours`, `club.holidays` and `training.slotMinutes`, the same function the booking's placement uses, over every club-local day the range touches.
     - Lanes are `dog:`/`member:`/`slot:`.
   - **After the change.** `{201=20} · retries 0 · exhausted 0` in every run.
   - **Found on the way.** The repository pre-creates the collection. In `11-r2-targeted-its.log` (exit 1), two transactions that both created `ring_slot_locks` implicitly aborted each other at commit: `WriteConflict … Collection namespace 'agilityhub_test.ring_slot_locks' is already in use`. The first booking and the first block of a new deployment would hit the same abort.
5. **S06 backoff.** `SchedulingTransactions` sleeps 50–150 ms (jittered) between attempts, as the other contexts do. The budget is unchanged: 6 attempts. `SchedulingTransactionsTest.T_06_23_retriesWaitAJitteredBackoffAndStopWhenInterrupted` checks at least 250 ms before exhaustion, and that an interrupt stops the retries with the flag restored.
6. **Notes.**
   - **`discardOutboxBacklog` Javadoc.** It now says that production is unaffected: one real clock, and the background dispatcher drains the oldest records first on every tick. It also says that the discard removes the PENDING rows of every tenant, so no IT may rely on events from an earlier test or `@BeforeAll`.
   - **`RingBlockService.lockDay`.** It is replaced by `lockSlots(ring, from, to)`, which covers every day the range touches, so it no longer depends on blocks being single-day. The Javadoc says so.
   - **`cancelMatching`.** It lanes only the activities of registrations that are not CANCELLED.

**STALE_VERSION with the lanes off** (final clean verify, `15-r2-final-clean-verify.log`):

| Burst | Result | Retries | Exhausted |
|---|---|---|---|
| S07 T-07-24: 20 registrations, 1 place | `201 ACTIVE=1, 201 WAITLISTED=11, 409 STALE_VERSION=8` | 46 | 8/20 |
| S08 T-08-29: 20 holds, last seat | `201=1, 409 CLASS_FULL=12, 409 STALE_VERSION=7` | 33 | 7/20 |
| S09 T-09-32: 20 dogs, capacity 1 / 2 | `201=1, 409 SLOT_TAKEN=19` / `201=2, 409 SLOT_TAKEN=18` | 19 / 23 | 0 |
| S09 T-09-33: Rock at 2/3, five slots | `201=1, 409 TRAINING_LIMIT_REACHED=3, 409 STALE_VERSION=1` | 7 | 1/5 |
| **S09 R-09-13: 20 dogs on 20 slots of one ring-day** | `201=20` (ring-day design: `201=5, 409 STALE_VERSION=15`) | 0 | **0** (was 15) |
| S09 R-09-13 vs S05, 4 rounds | booking `201` / ring change `409 STALE_VERSION` every round | – | – |
| S09 R-09-13 vs S06 block, 6 rounds | booking `409 SLOT_TAKEN` / block `201` every round | – | – |

The Rock burst now had one exhausted request. In round 1 it had none; that burst is one dog, so every request meets the same `Dog.trainingSeq`. The test allows it, and the invariant (never past 3) held. The budgets stay as ruled (E26).

#### Evidence (Round 2, final runs after every code change)

**1. `./mvnw -q clean verify`** → **exit 0**. Full log: `roadmap/evidence/E5-T07/15-r2-final-clean-verify.log`. Summary from the reports:
- `target/surefire-reports`: 105 classes, **483 tests, 0 failures, 0 errors, 0 skipped**.
- `target/failsafe-reports`: 73 classes, **767 tests, 0 failures, 0 errors, 0 skipped**.

The JaCoCo check, ArchUnit and `OpenApiSnapshotTest` run inside it. The OpenAPI is unchanged: no controller signature changed. `NoSuchElement` appears 0 times in the log.

Changed classes in `target/site/jacoco/jacoco.csv`:

| Class | Lines | Branches |
|---|---|---|
| `TrainingSlotLocks` (new) | 15/15 | 12/12 |
| `RingScheduleAccess` | 12/12 | 12/14 |
| `SchedulingTransactions` | 14/14 | 13/16 |
| `TrainingConflictService` | 15/15 | 5/5 |
| `RingReservability` | 10/10 | – |
| `TrainingBookingService` | 121/121 | 90/107 |
| `RingBlockService` | 94/94 | 107/120 |
| `ClassSessionService` | 76/77 | 83/86 |
| `CatalogService` | 137/139 | 109/119 |
| `ActivityRegistrationService` | 74/75 | 91/102 |
| `LocalLanes` / `TrainingTransactions` | 20/20 · 18/18 | 16/16 · 18/18 |
| `RingSlotLockRepository` (persistence, new) | 9/9 | 2/4 |

Last 40 lines (lines cut at 150 characters):
```

2026-09-24T12:19:38.569+02:00  INFO 36462 --- [agilityhub-core-api] [           main] c.a.core.migration.PlayoffMigrationIT    : Starting PlayoffMigr…
2026-09-24T12:19:38.569+02:00  INFO 36462 --- [agilityhub-core-api] [           main] c.a.core.migration.PlayoffMigrationIT    : The following 1 prof…
2026-09-24T12:19:38.720+02:00  INFO 36462 --- [agilityhub-core-api] [           main] .s.d.r.c.RepositoryConfigurationDelegate : Bootstrapping Spring…
2026-09-24T12:19:38.738+02:00  INFO 36462 --- [agilityhub-core-api] [           main] .s.d.r.c.RepositoryConfigurationDelegate : Finished Spring Data…
2026-09-24T12:19:38.765+02:00  INFO 36462 --- [agilityhub-core-api] [           main] org.mongodb.driver.client                : MongoClient with met…
2026-09-24T12:19:38.767+02:00  INFO 36462 --- [agilityhub-core-api] [localhost:58123] org.mongodb.driver.cluster               : Monitor thread succe…
2026-09-24T12:19:39.201+02:00  INFO 36462 --- [agilityhub-core-api] [           main] c.a.core.migration.PlayoffMigrationIT    : Started PlayoffMigra…
2026-09-24T12:19:39.219+02:00  INFO 36462 --- [agilityhub-core-api] [           main] o.s.b.w.e.tomcat.GracefulShutdown        : Commencing graceful …
2026-09-24T12:19:39.220+02:00  INFO 36462 --- [agilityhub-core-api] [tomcat-shutdown] o.s.b.w.e.tomcat.GracefulShutdown        : Graceful shutdown co…
2026-09-24T12:19:39.220+02:00  INFO 36462 --- [agilityhub-core-api] [           main] o.s.b.w.e.tomcat.GracefulShutdown        : Commencing graceful …
2026-09-24T12:19:39.221+02:00  INFO 36462 --- [agilityhub-core-api] [tomcat-shutdown] o.s.b.w.e.tomcat.GracefulShutdown        : Graceful shutdown co…
2026-09-24T12:19:39.221+02:00  INFO 36462 --- [agilityhub-core-api] [           main] o.apache.catalina.core.StandardService   : Stopping service [To…
E5-T07 outbox backlog discarded before PlayoffMigrationIT: 163 PENDING records (more than one dispatch() batch)
E5-T07 outbox backlog discarded before PlayoffMigrationIT: 167 PENDING records (more than one dispatch() batch)
E5-T07 outbox backlog discarded before PlayoffMigrationIT: 163 PENDING records (more than one dispatch() batch)
2026-09-24T12:19:47.668+02:00  INFO 36462 --- [agilityhub-core-api] [localhost:58505] org.mongodb.driver.cluster               : Exception in monitor…

com.mongodb.MongoSocketOpenException: Exception opening socket
	at com.mongodb.internal.connection.SocketStream.lambda$open$0(SocketStream.java:85) ~[mongodb-driver-core-5.5.2.jar:na]
	at java.base/java.util.Optional.orElseThrow(Optional.java:403) ~[na:na]
	at com.mongodb.internal.connection.SocketStream.open(SocketStream.java:85) ~[mongodb-driver-core-5.5.2.jar:na]
	at com.mongodb.internal.connection.InternalStreamConnection.open(InternalStreamConnection.java:233) ~[mongodb-driver-core-5.5.2.jar:na]
	at com.mongodb.internal.connection.DefaultServerMonitor$ServerMonitor.lookupServerDescription(DefaultServerMonitor.java:219) ~[mongodb-driver-core-5…
	at com.mongodb.internal.connection.DefaultServerMonitor$ServerMonitor.run(DefaultServerMonitor.java:176) ~[mongodb-driver-core-5.5.2.jar:na]
Caused by: java.net.ConnectException: Connection refused
	at java.base/sun.nio.ch.Net.pollConnect(Native Method) ~[na:na]
	at java.base/sun.nio.ch.Net.pollConnectNow(Net.java:694) ~[na:na]
	at java.base/sun.nio.ch.NioSocketImpl.timedFinishConnect(NioSocketImpl.java:542) ~[na:na]
	at java.base/sun.nio.ch.NioSocketImpl.connect(NioSocketImpl.java:592) ~[na:na]
	at java.base/java.net.SocksSocketImpl.connect(SocksSocketImpl.java:327) ~[na:na]
	at java.base/java.net.Socket.connect(Socket.java:751) ~[na:na]
	at com.mongodb.internal.connection.SocketStreamHelper.initialize(SocketStreamHelper.java:76) ~[mongodb-driver-core-5.5.2.jar:na]
	at com.mongodb.internal.connection.SocketStream.initializeSocket(SocketStream.java:104) ~[mongodb-driver-core-5.5.2.jar:na]
	at com.mongodb.internal.connection.SocketStream.open(SocketStream.java:79) ~[mongodb-driver-core-5.5.2.jar:na]
	... 3 common frames omitted

E5-T07 outbox backlog discarded before PlayoffMigrationIT: 163 PENDING records (more than one dispatch() batch)
E5-T07 outbox backlog discarded before PlayoffMigrationIT: 163 PENDING records (more than one dispatch() batch)
E5-T07 outbox backlog discarded before PlayoffMigrationIT: 163 PENDING records (more than one dispatch() batch)
```

**2. The five repeated runs of step 5**, each **exit 0**, `Tests run: 78, Failures: 0, Errors: 0, Skipped: 0`, `BUILD SUCCESS`, `Integration JVM availableProcessors=2`. There are 78 tests now; round 1 had 69, and E5-T08/T09 added tests to these classes. The command is identical for runs 1–5:

`./mvnw verify -Dtest=NoSuchTest -Dsurefire.failIfNoSpecifiedTests=false -Dit.test='ActivityIT,BookingsIT,WaitlistIT,TrainingIT' -Djacoco.skip=true -DargLine=-XX:ActiveProcessorCount=2 -Dfailsafe.runOrder=random`

| Run | Log | Class order |
|---|---|---|
| 1 | `16-r2-repeat-1.log` | Waitlist, Activity, Training, Bookings |
| 2 | `16-r2-repeat-2.log` | Waitlist, Bookings, Training, Activity |
| 3 | `16-r2-repeat-3.log` | Activity, Training, Bookings, Waitlist |
| 4 | `16-r2-repeat-4.log` | Waitlist, Training, Bookings, Activity |
| 5 | `16-r2-repeat-5.log` | Activity, Training, Bookings, Waitlist |

Run 1, last 40 lines:
```
2026-09-24T12:20:51.237+02:00  INFO 37387 --- [agilityhub-core-api] [           main] c.a.core.clubs.training.api.TrainingIT   : The following 1 prof…
2026-09-24T12:20:51.412+02:00  INFO 37387 --- [agilityhub-core-api] [           main] .s.d.r.c.RepositoryConfigurationDelegate : Bootstrapping Spring…
2026-09-24T12:20:51.430+02:00  INFO 37387 --- [agilityhub-core-api] [           main] .s.d.r.c.RepositoryConfigurationDelegate : Finished Spring Data…
2026-09-24T12:20:51.463+02:00  INFO 37387 --- [agilityhub-core-api] [           main] org.mongodb.driver.client                : MongoClient with met…
2026-09-24T12:20:51.465+02:00  INFO 37387 --- [agilityhub-core-api] [localhost:58546] org.mongodb.driver.cluster               : Monitor thread succe…
2026-09-24T12:20:51.917+02:00  INFO 37387 --- [agilityhub-core-api] [           main] o.s.b.t.m.w.SpringBootMockServletContext : Initializing Spring …
2026-09-24T12:20:51.917+02:00  INFO 37387 --- [agilityhub-core-api] [           main] o.s.t.web.servlet.TestDispatcherServlet  : Initializing Servlet…
2026-09-24T12:20:51.918+02:00  INFO 37387 --- [agilityhub-core-api] [           main] o.s.t.web.servlet.TestDispatcherServlet  : Completed initializa…
2026-09-24T12:20:51.930+02:00  INFO 37387 --- [agilityhub-core-api] [           main] c.a.core.clubs.training.api.TrainingIT   : Started TrainingIT i…
T-09-27 day-grid view=member: {"date":"2026-10-06","dayOfWeek":"TUESDAY","timeZone":"Europe/Madrid","view":"MEMBER","columns":[{"ringId":"s09-r-mun",…
T-09-27 day-grid view=instructor: {"date":"2026-10-06","dayOfWeek":"TUESDAY","timeZone":"Europe/Madrid","view":"INSTRUCTOR","columns":[{"ringId":"s09…
[INFO] Tests run: 22, Failures: 0, Errors: 0, Skipped: 0, Time elapsed: 5.886 s -- in com.agilityhub.core.clubs.training.api.TrainingIT
[INFO] Running com.agilityhub.core.clubs.bookings.api.BookingsIT
2026-09-24T12:20:57.091+02:00  INFO 37387 --- [agilityhub-core-api] [           main] t.c.s.AnnotationConfigContextLoaderUtils : Could not detect def…
2026-09-24T12:20:57.092+02:00  INFO 37387 --- [agilityhub-core-api] [           main] .b.t.c.SpringBootTestContextBootstrapper : Found @SpringBootCon…
2026-09-24T12:20:58.585+02:00  INFO 37387 --- [agilityhub-core-api] [           main] c.a.c.c.b.application.BookingEvents      : Booking blocked oper…
2026-09-24T12:20:58.596+02:00  INFO 37387 --- [agilityhub-core-api] [           main] c.a.c.c.b.application.BookingEvents      : Booking blocked oper…
2026-09-24T12:20:58.603+02:00  INFO 37387 --- [agilityhub-core-api] [           main] c.a.c.c.b.application.BookingEvents      : Booking blocked oper…
2026-09-24T12:20:58.609+02:00  INFO 37387 --- [agilityhub-core-api] [           main] c.a.c.c.b.application.BookingEvents      : Booking blocked oper…
2026-09-24T12:21:01.300+02:00  WARN 37387 --- [agilityhub-core-api] [           main] c.a.c.c.b.application.BookingConsumers   : Class s08-wed cancel…
[INFO] Tests run: 18, Failures: 0, Errors: 0, Skipped: 0, Time elapsed: 5.526 s -- in com.agilityhub.core.clubs.bookings.api.BookingsIT
[INFO] 
[INFO] Results:
[INFO] 
[INFO] Tests run: 78, Failures: 0, Errors: 0, Skipped: 0
[INFO] 
[INFO] 
[INFO] --- failsafe:3.5.6:verify (default) @ agilityhub-core-api ---
[INFO] 
[INFO] --- jacoco:0.8.14:report (coverage) @ agilityhub-core-api ---
[INFO] Skipping JaCoCo execution because property jacoco.skip is set.
[INFO] 
[INFO] --- jacoco:0.8.14:check (coverage) @ agilityhub-core-api ---
[INFO] Skipping JaCoCo execution because property jacoco.skip is set.
[INFO] ------------------------------------------------------------------------
[INFO] BUILD SUCCESS
[INFO] ------------------------------------------------------------------------
[INFO] Total time:  40.667 s
[INFO] Finished at: 2026-09-24T12:21:03+02:00
[INFO] ------------------------------------------------------------------------
```
Run 2, last 40 lines:
```
  .   ____          _            __ _ _
 /\\ / ___'_ __ _ _(_)_ __  __ _ \ \ \ \
( ( )\___ | '_ | '_| | '_ \/ _` | \ \ \ \
 \\/  ___)| |_)| | | | | || (_| |  ) ) ) )
  '  |____| .__|_| |_|_| |_\__, | / / / /
 =========|_|==============|___/=/_/_/_/

 :: Spring Boot ::               (v3.5.16)

2026-09-24T12:21:29.497+02:00  INFO 37900 --- [agilityhub-core-api] [           main] c.a.c.c.a.application.ActivityIT         : Starting ActivityIT …
2026-09-24T12:21:29.497+02:00  INFO 37900 --- [agilityhub-core-api] [           main] c.a.c.c.a.application.ActivityIT         : The following 1 prof…
2026-09-24T12:21:29.686+02:00  INFO 37900 --- [agilityhub-core-api] [           main] .s.d.r.c.RepositoryConfigurationDelegate : Bootstrapping Spring…
2026-09-24T12:21:29.704+02:00  INFO 37900 --- [agilityhub-core-api] [           main] .s.d.r.c.RepositoryConfigurationDelegate : Finished Spring Data…
2026-09-24T12:21:29.733+02:00  INFO 37900 --- [agilityhub-core-api] [           main] org.mongodb.driver.client                : MongoClient with met…
2026-09-24T12:21:29.735+02:00  INFO 37900 --- [agilityhub-core-api] [localhost:58584] org.mongodb.driver.cluster               : Monitor thread succe…
2026-09-24T12:21:30.169+02:00  INFO 37900 --- [agilityhub-core-api] [           main] o.s.b.t.m.w.SpringBootMockServletContext : Initializing Spring …
2026-09-24T12:21:30.170+02:00  INFO 37900 --- [agilityhub-core-api] [           main] o.s.t.web.servlet.TestDispatcherServlet  : Initializing Servlet…
2026-09-24T12:21:30.170+02:00  INFO 37900 --- [agilityhub-core-api] [           main] o.s.t.web.servlet.TestDispatcherServlet  : Completed initializa…
2026-09-24T12:21:30.177+02:00  INFO 37900 --- [agilityhub-core-api] [           main] c.a.c.c.a.application.ActivityIT         : Started ActivityIT i…
2026-09-24T12:21:34.697+02:00  WARN 37900 --- [agilityhub-core-api] [           main] c.a.core.shared.api.ApiExceptionHandler  : Framework request re…
[INFO] Tests run: 25, Failures: 0, Errors: 0, Skipped: 0, Time elapsed: 9.193 s -- in com.agilityhub.core.clubs.activities.application.ActivityIT
[INFO] 
[INFO] Results:
[INFO] 
[INFO] Tests run: 78, Failures: 0, Errors: 0, Skipped: 0
[INFO] 
[INFO] 
[INFO] --- failsafe:3.5.6:verify (default) @ agilityhub-core-api ---
[INFO] 
[INFO] --- jacoco:0.8.14:report (coverage) @ agilityhub-core-api ---
[INFO] Skipping JaCoCo execution because property jacoco.skip is set.
[INFO] 
[INFO] --- jacoco:0.8.14:check (coverage) @ agilityhub-core-api ---
[INFO] Skipping JaCoCo execution because property jacoco.skip is set.
[INFO] ------------------------------------------------------------------------
[INFO] BUILD SUCCESS
[INFO] ------------------------------------------------------------------------
[INFO] Total time:  35.277 s
[INFO] Finished at: 2026-09-24T12:21:39+02:00
[INFO] ------------------------------------------------------------------------
```
Run 3, last 40 lines:
```

2026-09-24T12:22:04.301+02:00  INFO 38064 --- [agilityhub-core-api] [           main] c.a.core.clubs.bookings.api.BookingsIT   : Starting BookingsIT …
2026-09-24T12:22:04.301+02:00  INFO 38064 --- [agilityhub-core-api] [           main] c.a.core.clubs.bookings.api.BookingsIT   : The following 1 prof…
2026-09-24T12:22:04.494+02:00  INFO 38064 --- [agilityhub-core-api] [           main] .s.d.r.c.RepositoryConfigurationDelegate : Bootstrapping Spring…
2026-09-24T12:22:04.514+02:00  INFO 38064 --- [agilityhub-core-api] [           main] .s.d.r.c.RepositoryConfigurationDelegate : Finished Spring Data…
2026-09-24T12:22:04.546+02:00  INFO 38064 --- [agilityhub-core-api] [           main] org.mongodb.driver.client                : MongoClient with met…
2026-09-24T12:22:04.548+02:00  INFO 38064 --- [agilityhub-core-api] [localhost:58628] org.mongodb.driver.cluster               : Monitor thread succe…
2026-09-24T12:22:04.980+02:00  INFO 38064 --- [agilityhub-core-api] [           main] o.s.b.t.m.w.SpringBootMockServletContext : Initializing Spring …
2026-09-24T12:22:04.980+02:00  INFO 38064 --- [agilityhub-core-api] [           main] o.s.t.web.servlet.TestDispatcherServlet  : Initializing Servlet…
2026-09-24T12:22:04.980+02:00  INFO 38064 --- [agilityhub-core-api] [           main] o.s.t.web.servlet.TestDispatcherServlet  : Completed initializa…
2026-09-24T12:22:04.988+02:00  INFO 38064 --- [agilityhub-core-api] [           main] c.a.core.clubs.bookings.api.BookingsIT   : Started BookingsIT i…
2026-09-24T12:22:06.849+02:00  INFO 38064 --- [agilityhub-core-api] [           main] c.a.c.c.b.application.BookingEvents      : Booking blocked oper…
2026-09-24T12:22:06.862+02:00  INFO 38064 --- [agilityhub-core-api] [           main] c.a.c.c.b.application.BookingEvents      : Booking blocked oper…
2026-09-24T12:22:06.870+02:00  INFO 38064 --- [agilityhub-core-api] [           main] c.a.c.c.b.application.BookingEvents      : Booking blocked oper…
2026-09-24T12:22:06.878+02:00  INFO 38064 --- [agilityhub-core-api] [           main] c.a.c.c.b.application.BookingEvents      : Booking blocked oper…
2026-09-24T12:22:09.693+02:00  WARN 38064 --- [agilityhub-core-api] [           main] c.a.c.c.b.application.BookingConsumers   : Class s08-wed cancel…
[INFO] Tests run: 18, Failures: 0, Errors: 0, Skipped: 0, Time elapsed: 6.569 s -- in com.agilityhub.core.clubs.bookings.api.BookingsIT
[INFO] Running com.agilityhub.core.clubs.bookings.api.WaitlistIT
2026-09-24T12:22:10.840+02:00  INFO 38064 --- [agilityhub-core-api] [           main] t.c.s.AnnotationConfigContextLoaderUtils : Could not detect def…
2026-09-24T12:22:10.841+02:00  INFO 38064 --- [agilityhub-core-api] [           main] .b.t.c.SpringBootTestContextBootstrapper : Found @SpringBootCon…
[INFO] Tests run: 13, Failures: 0, Errors: 0, Skipped: 0, Time elapsed: 5.581 s -- in com.agilityhub.core.clubs.bookings.api.WaitlistIT
[INFO] 
[INFO] Results:
[INFO] 
[INFO] Tests run: 78, Failures: 0, Errors: 0, Skipped: 0
[INFO] 
[INFO] 
[INFO] --- failsafe:3.5.6:verify (default) @ agilityhub-core-api ---
[INFO] 
[INFO] --- jacoco:0.8.14:report (coverage) @ agilityhub-core-api ---
[INFO] Skipping JaCoCo execution because property jacoco.skip is set.
[INFO] 
[INFO] --- jacoco:0.8.14:check (coverage) @ agilityhub-core-api ---
[INFO] Skipping JaCoCo execution because property jacoco.skip is set.
[INFO] ------------------------------------------------------------------------
[INFO] BUILD SUCCESS
[INFO] ------------------------------------------------------------------------
[INFO] Total time:  36.961 s
[INFO] Finished at: 2026-09-24T12:22:16+02:00
[INFO] ------------------------------------------------------------------------
```
Run 4, last 40 lines:
```
  .   ____          _            __ _ _
 /\\ / ___'_ __ _ _(_)_ __  __ _ \ \ \ \
( ( )\___ | '_ | '_| | '_ \/ _` | \ \ \ \
 \\/  ___)| |_)| | | | | || (_| |  ) ) ) )
  '  |____| .__|_| |_|_| |_\__, | / / / /
 =========|_|==============|___/=/_/_/_/

 :: Spring Boot ::               (v3.5.16)

2026-09-24T12:22:48.949+02:00  INFO 38244 --- [agilityhub-core-api] [           main] c.a.c.c.a.application.ActivityIT         : Starting ActivityIT …
2026-09-24T12:22:48.950+02:00  INFO 38244 --- [agilityhub-core-api] [           main] c.a.c.c.a.application.ActivityIT         : The following 1 prof…
2026-09-24T12:22:49.131+02:00  INFO 38244 --- [agilityhub-core-api] [           main] .s.d.r.c.RepositoryConfigurationDelegate : Bootstrapping Spring…
2026-09-24T12:22:49.149+02:00  INFO 38244 --- [agilityhub-core-api] [           main] .s.d.r.c.RepositoryConfigurationDelegate : Finished Spring Data…
2026-09-24T12:22:49.180+02:00  INFO 38244 --- [agilityhub-core-api] [           main] org.mongodb.driver.client                : MongoClient with met…
2026-09-24T12:22:49.182+02:00  INFO 38244 --- [agilityhub-core-api] [localhost:58679] org.mongodb.driver.cluster               : Monitor thread succe…
2026-09-24T12:22:49.618+02:00  INFO 38244 --- [agilityhub-core-api] [           main] o.s.b.t.m.w.SpringBootMockServletContext : Initializing Spring …
2026-09-24T12:22:49.618+02:00  INFO 38244 --- [agilityhub-core-api] [           main] o.s.t.web.servlet.TestDispatcherServlet  : Initializing Servlet…
2026-09-24T12:22:49.618+02:00  INFO 38244 --- [agilityhub-core-api] [           main] o.s.t.web.servlet.TestDispatcherServlet  : Completed initializa…
2026-09-24T12:22:49.626+02:00  INFO 38244 --- [agilityhub-core-api] [           main] c.a.c.c.a.application.ActivityIT         : Started ActivityIT i…
2026-09-24T12:22:54.259+02:00  WARN 38244 --- [agilityhub-core-api] [           main] c.a.core.shared.api.ApiExceptionHandler  : Framework request re…
[INFO] Tests run: 25, Failures: 0, Errors: 0, Skipped: 0, Time elapsed: 10.90 s -- in com.agilityhub.core.clubs.activities.application.ActivityIT
[INFO] 
[INFO] Results:
[INFO] 
[INFO] Tests run: 78, Failures: 0, Errors: 0, Skipped: 0
[INFO] 
[INFO] 
[INFO] --- failsafe:3.5.6:verify (default) @ agilityhub-core-api ---
[INFO] 
[INFO] --- jacoco:0.8.14:report (coverage) @ agilityhub-core-api ---
[INFO] Skipping JaCoCo execution because property jacoco.skip is set.
[INFO] 
[INFO] --- jacoco:0.8.14:check (coverage) @ agilityhub-core-api ---
[INFO] Skipping JaCoCo execution because property jacoco.skip is set.
[INFO] ------------------------------------------------------------------------
[INFO] BUILD SUCCESS
[INFO] ------------------------------------------------------------------------
[INFO] Total time:  42.637 s
[INFO] Finished at: 2026-09-24T12:23:00+02:00
[INFO] ------------------------------------------------------------------------
```
Run 5, last 40 lines:
```

2026-09-24T12:23:27.863+02:00  INFO 38463 --- [agilityhub-core-api] [           main] c.a.core.clubs.bookings.api.BookingsIT   : Starting BookingsIT …
2026-09-24T12:23:27.864+02:00  INFO 38463 --- [agilityhub-core-api] [           main] c.a.core.clubs.bookings.api.BookingsIT   : The following 1 prof…
2026-09-24T12:23:28.063+02:00  INFO 38463 --- [agilityhub-core-api] [           main] .s.d.r.c.RepositoryConfigurationDelegate : Bootstrapping Spring…
2026-09-24T12:23:28.081+02:00  INFO 38463 --- [agilityhub-core-api] [           main] .s.d.r.c.RepositoryConfigurationDelegate : Finished Spring Data…
2026-09-24T12:23:28.112+02:00  INFO 38463 --- [agilityhub-core-api] [           main] org.mongodb.driver.client                : MongoClient with met…
2026-09-24T12:23:28.114+02:00  INFO 38463 --- [agilityhub-core-api] [localhost:58733] org.mongodb.driver.cluster               : Monitor thread succe…
2026-09-24T12:23:28.543+02:00  INFO 38463 --- [agilityhub-core-api] [           main] o.s.b.t.m.w.SpringBootMockServletContext : Initializing Spring …
2026-09-24T12:23:28.543+02:00  INFO 38463 --- [agilityhub-core-api] [           main] o.s.t.web.servlet.TestDispatcherServlet  : Initializing Servlet…
2026-09-24T12:23:28.543+02:00  INFO 38463 --- [agilityhub-core-api] [           main] o.s.t.web.servlet.TestDispatcherServlet  : Completed initializa…
2026-09-24T12:23:28.551+02:00  INFO 38463 --- [agilityhub-core-api] [           main] c.a.core.clubs.bookings.api.BookingsIT   : Started BookingsIT i…
2026-09-24T12:23:30.233+02:00  INFO 38463 --- [agilityhub-core-api] [           main] c.a.c.c.b.application.BookingEvents      : Booking blocked oper…
2026-09-24T12:23:30.245+02:00  INFO 38463 --- [agilityhub-core-api] [           main] c.a.c.c.b.application.BookingEvents      : Booking blocked oper…
2026-09-24T12:23:30.250+02:00  INFO 38463 --- [agilityhub-core-api] [           main] c.a.c.c.b.application.BookingEvents      : Booking blocked oper…
2026-09-24T12:23:30.256+02:00  INFO 38463 --- [agilityhub-core-api] [           main] c.a.c.c.b.application.BookingEvents      : Booking blocked oper…
2026-09-24T12:23:33.195+02:00  WARN 38463 --- [agilityhub-core-api] [           main] c.a.c.c.b.application.BookingConsumers   : Class s08-wed cancel…
[INFO] Tests run: 18, Failures: 0, Errors: 0, Skipped: 0, Time elapsed: 6.835 s -- in com.agilityhub.core.clubs.bookings.api.BookingsIT
[INFO] Running com.agilityhub.core.clubs.bookings.api.WaitlistIT
2026-09-24T12:23:34.653+02:00  INFO 38463 --- [agilityhub-core-api] [           main] t.c.s.AnnotationConfigContextLoaderUtils : Could not detect def…
2026-09-24T12:23:34.654+02:00  INFO 38463 --- [agilityhub-core-api] [           main] .b.t.c.SpringBootTestContextBootstrapper : Found @SpringBootCon…
[INFO] Tests run: 13, Failures: 0, Errors: 0, Skipped: 0, Time elapsed: 5.989 s -- in com.agilityhub.core.clubs.bookings.api.WaitlistIT
[INFO] 
[INFO] Results:
[INFO] 
[INFO] Tests run: 78, Failures: 0, Errors: 0, Skipped: 0
[INFO] 
[INFO] 
[INFO] --- failsafe:3.5.6:verify (default) @ agilityhub-core-api ---
[INFO] 
[INFO] --- jacoco:0.8.14:report (coverage) @ agilityhub-core-api ---
[INFO] Skipping JaCoCo execution because property jacoco.skip is set.
[INFO] 
[INFO] --- jacoco:0.8.14:check (coverage) @ agilityhub-core-api ---
[INFO] Skipping JaCoCo execution because property jacoco.skip is set.
[INFO] ------------------------------------------------------------------------
[INFO] BUILD SUCCESS
[INFO] ------------------------------------------------------------------------
[INFO] Total time:  40.079 s
[INFO] Finished at: 2026-09-24T12:23:41+02:00
[INFO] ------------------------------------------------------------------------
```

**3. `grep -rn "ReentrantLock" src/main/java`** → exit 0. Complete output (only `LocalLanes`):
```
src/main/java/com/agilityhub/core/shared/application/LocalLanes.java:7:import java.util.concurrent.locks.ReentrantLock;
src/main/java/com/agilityhub/core/shared/application/LocalLanes.java:25:    private static final class Lane { final ReentrantLock lock = new ReentrantLock(true); int users; }
src/main/java/com/agilityhub/core/shared/application/LocalLanes.java:49:    private ReentrantLock acquire(String key) {
```

**Earlier attempts in this round (one line each):**
- `10-r2-ring-day-hotspot-before.log` (exit 1): the new 20-slot burst on the ring-day design gave 15 × `STALE_VERSION`. I switched to ring-slot documents (point 4).
- `11-r2-targeted-its.log` (exit 1, 107/108 passed): `TrainingLanesOffIT` errored with «namespace `ring_slot_locks` already in use» on the implicit collection creation. I pre-create the collection in `RingSlotLockRepository`.
- `12-r2-training-its.log` (exit 0): `TrainingLanesOffIT`, `TrainingIT`, `HealthIndependenceIT`, 36 tests.
- `13-r2-counterfactual-old-wipe.log` (exit 1, intended) and `14-r2-control-helper.log` (exit 0): point 1.
- Unit tests `TrainingSlotLocksTest` (4) and `SchedulingTransactionsTest` (2) were run on their own first: 6/6 green.

#### Files changed (Round 2)
- **Main.**
  - `clubs/scheduling/persistence/RingSlotLock.java` and `RingSlotLockRepository.java` (new). `RingDayLock.java` and `RingDayLockRepository.java` are deleted.
  - `clubs/scheduling/application/RingScheduleAccess.java` (`lockRingSlots`).
  - `RingBlockService.java` (`lockSlots(ring, from, to)`).
  - `ClassSessionService.java`.
  - `SchedulingTransactions.java` (backoff).
  - `ports/TrainingConflictPort.java` (`lockSlots`) and `ports/SchedulingPortDefaults.java`.
  - `clubs/training/application/TrainingSlotLocks.java` (new).
  - `TrainingConflictService.java`, `RingReservability.java` and `TrainingAutoConfiguration.java`.
  - `TrainingBookingService.java` (ring-slot touch, no `day:` lane) and `TrainingTransactions.java` (Javadoc).
  - `clubs/catalogs/application/RingTrainingBookings.java` (`lockBookableSlots`) and `CatalogService.java`.
  - `clubs/activities/application/ActivityRegistrationService.java` (`cancelMatching` lanes).
  - `shared/application/LocalLanes.java` (Javadoc).
  - `src/main/resources/application.yml` (comment).
- **Tests.**
  - `support/AbstractIntegrationTest.java` (`wipeDatabaseKeepingBootstrap`, `BOOTSTRAP_COLLECTIONS`, the outbox Javadoc).
  - `census/application/DemoScenarioSeedIT.java` and `DemoPlanningSeedIT.java`.
  - `training/api/TrainingLanesOffIT.java`: renames, the 20-slot burst and three T-09-28 tests.
  - `training/api/TrainingFixtures.java` (`ring_slot_locks` cleanup).
  - `training/application/TrainingSlotLocksTest.java` (new).
  - `scheduling/application/SchedulingTransactionsTest.java`.
  - The port doubles in `scheduling/application/CalendarIT.java` and `activities/application/ActivityFixtures.java`.
- **Docs.** `docs/DEPLOY.md` (ring-slot sequences, S05) and `CHANGELOG.md`.
- **Evidence.** `roadmap/evidence/E5-T07/10…16-*.log`.

#### Rules and tests (Round 2)
- **R-09-13 / T-09-28 (S05).** `T_09_28_aRingMadeNotReservableDuringAnUncommittedBookingConflictsInsteadOfSkippingIt`, `T_09_28_aBookingDuringAnUncommittedRingChangeIsRetriedAndSeesTheRingNotReservable` and `T_09_28_lanesOffAConcurrentRingChangeAndBookingNeverLeaveALiveBookingOnANonReservableRing`.
- **R-09-13 (S06/S07, ring slots).**
  - The three `R_09_13_*` renames.
  - `R_09_13_lanesOffTwentyBookingsOnTwentySlotsOfOneRingDayNeverExhaustTheirRetries`.
  - `TrainingSlotLocksTest`: `R_09_13_aRangeTouchesEveryGridSlotItOverlapsAndNoOther`, `R_09_13_aRangeOverSeveralDaysSkipsHolidaysAndClosedDays`, `R_09_13_aRangeWithoutGridSlotsOrWithoutFreeTrainingTouchesNothing` and `R_09_13_aRingThatStopsBeingReservableTouchesEverySlotLeftInTheBookingWindow`.
- **T-06-23 (S06 retries).** `T_06_23_retriesWaitAJitteredBackoffAndStopWhenInterrupted`.
- **CI flake.** `wipeDatabaseKeepingBootstrap`, proven by the counterfactual/control pair (logs 13/14).

#### Assumptions (Round 2)
1. **Grid alignment.** The ring-slot key is the training grid slot start, the same `TrainingGrid.slots` the booking's placement requires (`ON_GRID`). If `club.openingHours` or `training.slotMinutes` change while a booking and a block run concurrently, the two may compute different grids. That is out of scope, as it was for the ring-day design.
2. **The S05 horizon.** It runs from now to the end of `today + training.bookingWindowDays`, which is the booking window check. A ring change touches `windowDays + 1` days of slots: about 120 upserts for 4 days of 07:00–22:00 at 30 min. That is acceptable for a rare admin operation, and it happens only when the ring actually stops being reservable.
3. **The `lockSlots` Javadoc.** A class moved onto a ring and a ring block keep calling it only with `FREE_TRAINING` on (no bookings otherwise). `TrainingSlotLocks` checks the module again for the S05 path.

#### Questions / catalog proposals (Round 2)
1. **Model (replaces the round-1 proposal applied in E31).** `MODEL_DADES_PLATAFORMA.md` line 66 lists `ring_day_locks {_id: clubId:ringId:date, ringId, date, sequence}`. Please replace it with `ring_slot_locks {_id: clubId:ringId:startsAt, clubId, ringId, startsAt, sequence}`: «el toquen (`$inc`) una reserva d'entrenament (el seu slot) i cada escriptura que comprova les reserves de la pista (S05 desactivar / treure `allowsFreeTraining`: tots els slots de la finestra; S06 bloqueig i classe moguda, S07 bloqueig d'activitat: els slots de la graella que el rang solapa) (S09 R-09-13)». Owner `clubs.scheduling`. The code uses it already (point 4 instructed the switch). This is also posted in `MESSAGES.md`.
2. No new parameter, event, notification or error code.

## Organizer verification
Result: changes_requested · 2026-09-24 · organizer (Claude)

Steps 1–4 are done and well proven:
- one `LocalLanes` per aggregate, switchable and documented;
- every Mongo mechanism is tested with the lanes off, and the retry meter proves the retry;
- the dog and ring-day conflicts exist in Mongo.

The outbox root cause (a PENDING backlog that depends on the class order) is reproduced by a counterfactual and fixed in the test base class. `clean verify`: 459 + 739. The five repeated runs on 2 CPUs are green, and CI is green at `55cf2b2`.

**The `impersonating:126` flake is not fixed.** The Objective also names the `BookingFixtures.impersonating:126 NoSuchElement` flake.
- **The explanation:** the independent review `roadmap/reviews/E5-T07-20260924-0926-claude.md` #1 gives it.
- **The confirmation:** the CI log of `1fc4d97`, read by the organizer, shows `JobsApiIT` ×2 and `MemberAggregatesIT` ×1 erroring at `SigningKeys.ring(SigningKeys.java:43)` ← `ImpersonationService.create`.
- **The cause:** `DemoScenarioSeedIT` and `DemoPlanningSeedIT` empty every collection, `signing_keys` included. A cached Spring context then cannot sign.

Round 2 — fix exactly these points:
1. **`signing_keys` (review #1).**
   - Add one shared helper in `AbstractIntegrationTest` for the ITs that empty the database. It keeps the bootstrap-owned collections: at least `signing_keys`; list the others you find.
   - Every wiping IT uses it: `DemoScenarioSeedIT` and `DemoPlanningSeedIT`; also check `HealthIndependenceIT`, `E4ContractIT` and `E5ContractIT`.
   - No production change: outside tests, a missing ring must still fail loudly.
   - Counterfactual: `DemoScenarioSeedIT` then `BookingsIT`/`JobsApiIT` in the same JVM fails with the old wipe and passes with the helper.
2. **Test names (review #2).** The `T_09_28_*` tests of `TrainingLanesOffIT` test ring blocks. Rename them after the ids they assert (`T_09_21_…` or `R_09_13_…`).
3. **The S05 side of R-09-13 (review #3).** E5-T09 adds `409 RING_HAS_BOOKINGS` + `cancelBookings` to the ring update.
   - That path (deactivating a ring, or turning `allowsFreeTraining` off) must conflict in Mongo with a concurrent training booking. For example, it touches the `ring_day_locks` of the booking horizon.
   - Test it with the lanes off.
4. **Ring-day hot spot (review #4).**
   - Add a lanes-off burst of 20 bookings on 20 **different** slots of one ring-day, and report its `STALE_VERSION` count.
   - If any request exhausts its retries, switch to one document per ring slot (a block touches each grid slot of its range) and drop the `day:` lane.
5. **S06 backoff (your Question 3).** `SchedulingTransactions` retries with the same jittered 50–150 ms backoff as the other contexts. The budget is unchanged.
6. **Notes (review #5–#7).**
   - The Javadoc of `discardOutboxBacklog` says that production is unaffected (one real clock; the background dispatcher drains the oldest first).
   - It also says that the discard removes the PENDING rows of every tenant before each test, so no IT may rely on events from an earlier test or from `@BeforeAll`.
   - `RingBlockService.lockDay` gets a comment (or a guard): blocks are single-day.
   - `cancelMatching` locks the lanes of live registrations' activities only.

**Rulings on your questions:**
- (1) Accepted: `ring_day_locks` is added to `MODEL_DADES_PLATAFORMA.md`, next to `seat_locks`.
- (2) R1 keeps one instance, and the retry budgets stay. Your proposal (5 attempts, jittered exponential backoff) is recorded in DECISIONS E26 as the condition for a second instance.
- (3) → point 5.
- (4) → point 1.

### Round 2 verification
Result: verified · 2026-09-24 · organizer (Claude)

The six points are fixed:
1. **The `signing_keys` wipe.** `wipeDatabaseKeepingBootstrap` fixes it. The counterfactual (seeded random order, old wipe) fails the CI way at `SigningKeys.ring:43`, and the control passes. CI has been green on every `main` push since `dfcd02e` (`dfcd02e`, `fe678fe`, `a202870`).
2. **Test names.** The ring-block tests are `R_09_13_*`, and `T_09_28_*` now names the S05 tests.
3. **The S05 side of R-09-13** conflicts in Mongo: `lockBookableSlots` runs before `futureActive`. Three lanes-off tests.
4. **The hot spot.** The ring-day design exhausted 15 of 20 retries, so you moved to one `ring_slot_locks` document per slot, as instructed: 20/20 committed, 0 retries. The pre-created collection avoids the implicit-creation abort you found.
5. **S06 backoff.** It is jittered, 50–150 ms.
6. **Notes.** Applied.

`clean verify`: 483 + 767. Five repeat runs on 2 CPUs are green.

**Rulings on your questions:**
- **Q1 is accepted.** `MODEL_DADES_PLATAFORMA.md` replaces `ring_day_locks` with `ring_slot_locks {_id: clubId:ringId:startsAt, clubId, ringId, startsAt, sequence}` (owner `clubs.scheduling`, S09 R-09-13). E31 in `DECISIONS_PENDENTS.md` points to it.

**Independent review** `roadmap/reviews/E5-T07-20260924-1229-claude.md`:
- #1 (a busy ring always beats the admin's change, and the other order is proven only with a fake S05), #3, #4 and #5 → **E5-T15** (steps 1, 2, 3 and 5).
- «Not checked» (R-09-13 on the week generation, validation and P1 paths) → E5-T15 step 4.
- #2 is applied by the organizer (the model).
- #6 is noted: the round-1 table is labelled as round 1.
