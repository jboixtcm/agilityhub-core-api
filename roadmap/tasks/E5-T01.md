---
id: E5-T01
title: Contract S08 + S09 + S15 (bookings, seat holds, waitlist, training slots and bookings, ring blocks, jobs) + scheduled-process framework (JobCatalog, Job plan/apply, SchedulerTick, job_locks, job_runs, catch-up, metrics, N-42, Clock ArchUnit rule) — OpenAPI forms, Mongo documents + indexes, error status audit, 501 stubs
stage: E5
repo: agilityhub-core-api
thread: A
order: 10
status: awaiting_verification
depends_on: [E4-T03, E4-T04, E4-T05]
branch:
pr:
updated: 2026-09-24
---

# E5-T01 · Contract S08 + S09 + S15 + scheduled-process framework

## Objective
Work packages S08 WP-08-A, S09 WP-09-A and S15 WP-15-A. Every route of S08 §6, S09 §6 and S15 §6 exists in `docs/openapi/openapi.json` with typed schemas, roles, modules, idempotency headers and canonical error statuses, answering `501 NOT_IMPLEMENTED` behind the security/tenant/module guards, so the web builds mocks-first while E5-T02…T05 implement. In the same task the **process framework** lands: `JobCatalog`, the `Job { plan / apply }` contract, `SchedulerTick`, occurrence and catch-up arithmetic in club-local time, `job_locks`, `job_runs`, dry run, metrics, `JobFailed` → N-42 and the ArchUnit rule that forbids `Instant.now()` outside the `Clock` bean. The ten processes themselves are E5-T05 (P1, P2, P6, P7, P9), E6 (P3, P8), E7 (P4) and E8 (P5, P10): here only a **fictitious test job** exercises the framework. Same pattern as E4-T01 (`E4ContractConfiguration`, `E4ContractIT`, `E4ResponseContractTest`, fixtures `e4-*.json`).

## Spec references
S08 §3 (every collection and field), §5 (three state machines), §6 (every row, the four JSON forms, the transaction skeletons, the error-code list), §7, §8, §9, §12 WP-08-A, §13 · S09 §3, §5, §6 (every row + the four JSON forms + the `details` shapes), §7, §9, §12 WP-09-A, §13 · S15 R-15-01 (catalog table: `JobName`, cadence, parameter, module, stage, catch-up window), R-15-02 (occurrences and DST), R-15-03, R-15-04, R-15-05, R-15-06, R-15-07, R-15-08, R-15-09, R-15-10, R-15-22, §3 (`JobRun`, `JobLock`, the fields this vertical adds to other aggregates), §5, §6, §9, §12 WP-15-A, §13 · tests **T-15-01, T-15-02, T-15-03, T-15-04, T-15-05, T-15-06, T-15-07, T-15-10, T-15-29, T-15-31** (framework, with the test job) and the contract halves of **T-08-47** and **T-09-24** · `docs/specs/00-transversal/CATALEG_ERRORS.md` rows S08/S09/S15 + §1 + §3 rule 0 · `CATALEG_ESDEVENIMENTS.md` (booking, waitlist, training, ring-block, scheduler rows + Annex A) · `CATALEG_NOTIFICACIONS.md` N-04, N-05, N-06, N-07, N-15, N-36, N-40, N-42, N-46, N-47 · `CATALEG_PARAMETRES.md` blocks «Classes i reserves», «Llista d'espera», «Entrenaments lliures» and the `jobs.*` family · `MATRIU_PERMISOS.md` rows «Reserves de classe `/bookings`», «Llista d'espera», «Entrenaments (slots i reserves)», «Processos programats (S15)» · `CONVENCIONS_API.md` §2, §4, §5, §6, §7 · S14 R-14-09 (audit actions).

## Context to load
`docs/specs/S08-reserves-de-classes-i-llista-espera.md`, `docs/specs/S09-entrenaments-lliures.md`, `docs/specs/S15-processos-programats.md` (all three complete) · the catalogs above · `docs/MODEL_DADES_PLATAFORMA.md` §0 glossary and Annex B rows `Booking · SeatHold · WaitlistEntry`, `TrainingBooking · Attendance`, `DomainEventRecord · JobRun · job_locks`, plus §7 (mandatory indexes) · `roadmap/tasks/E4-T01.md` and `src/main/java/com/agilityhub/core/configuration/E4ContractConfiguration.java`, `src/test/java/com/agilityhub/core/configuration/E4ContractIT.java`, `E4ResponseContractTest.java`, `OpenApiRequiredContractTest.java`, `ErrorCatalogContractTest.java`, `EventCatalogContractTest.java`, `src/test/resources/fixtures/contracts/e4-*.json` (copy the approach) · `src/main/java/com/agilityhub/core/clubs/bookings/**` and `clubs/training/**` (empty scaffolds: only `package-info.java`) · `clubs/scheduling/application/ports/*` (E4-T03: `ClassBookingsPort`, `TrainingOccupancyPort`, `TrainingConflictPort`, `SchedulingPortDefaults`) and `clubs/dashboard/application/ports/*` (E3-T04: `BookingActivity`, `TrainingBookingsQuery`, `ClassOccupancyQuery`, `RiskEvaluator`) — the interfaces E5-T02/T03/T04 implement · `platform/application/{ClubConfig,ClubConfigService,Module,ModuleGuard,RequiresModule}.java`, `platform/application/audit/*`, `platform/persistence/**` · `shared/application/{ClubClock,DefaultClubClock,EventPublisher,OutboxDispatcher,DomainEventHandler,TenantContext,CoreCommand}.java`, `shared/application/lists/*`, `shared/application/contract/*` · `clubs/scheduling/application/SchedulingTransactions.java` and `identity/application/IdentityTransactions.java` (the retry-on-`WriteConflict` pattern) · `docs/playbooks/new-endpoint.md`, `new-entity.md`, `new-scheduler.md`, `checklist.md` · this file.

## Steps
1. **S08 controllers + DTOs** (`clubs/bookings/api`, springdoc annotations, bodies throw `UnsupportedOperationException` → `501`, guards real): `GET /me/home?dogId=` (MEMBER incl. IMPERSONATED) · `GET /me/bookable-classes?dogId=` (MEMBER) · `POST /seat-holds {classSessionId, dogId, waitlistEntryId?}` (MEMBER, 201) · `DELETE /seat-holds/{id}` (MEMBER owner, 204, also when already gone) · `POST /bookings {seatHoldId, swapBookingId?}` (MEMBER, `Idempotency-Key`, 201) · `GET /me/bookings?dogId=&state=&from=&to=` (MEMBER) · `GET /bookings/{id}` (MEMBER own/group, INSTRUCTOR, ADMIN) · `GET /bookings/{id}/calendar.ics?token=` (signed token, `text/calendar`, no JWT) · `POST /bookings/{id}/cancellation {message?}` (MEMBER own/group, INSTRUCTOR when `bookings.instructorLastMinuteNotice`) · `GET /bookings` (ADMIN, INSTRUCTOR; universal list, `x-filterable: state, dogId, memberId, classSessionId, bookingWeekKey, origin, classStartsAt`, `x-sortable: classStartsAt, bookedAt`; MEMBER → 403) · `POST /waitlist-entries {classSessionId, dogId}` (MEMBER, `WAITLIST`, 201) · `GET /waitlist-entries/{id}` · `POST /waitlist-entries/{id}/cancellation` (MEMBER own, ADMIN) · `POST /waitlist-entries/{id}/claim {seatHoldId, swapBookingId?}` (MEMBER, `Idempotency-Key`, 201) · `GET /class-sessions/{id}/bookings` and `GET /class-sessions/{id}/waitlist-entries` (INSTRUCTOR, ADMIN). Impersonation: every MEMBER route accepts the impersonation token (R-08-19); ADMIN without impersonation on a MEMBER route → `403` (`FORBIDDEN`); the two admin/instructor read routes reject impersonation tokens (`IMPERSONATION_DENIED`), like E4-T01.
2. **S09 controllers + DTOs** (`clubs/training/api`; `@RequiresModule(Module.FREE_TRAINING)` on everything except `/ring-blocks*`): `GET /training-slots?from=&to=&dogId=&ringId=` (MEMBER, INSTRUCTOR, ADMIN) · `GET /me/training-summary?dogId=&date=` (MEMBER) · `GET /me/training-bookings?from=&to=&state=` (MEMBER) · `POST /training-bookings {dogId, startsAt, ringId?, override?{limit, reason}}` (MEMBER incl. IMPERSONATED, `Idempotency-Key`, 201) · `GET /training-bookings/{id}` (MEMBER own, INSTRUCTOR, ADMIN) · `POST /training-bookings/{id}/cancellation {reason?}` (MEMBER own incl. IMPERSONATED) · `GET /training-bookings` + `GET /training-bookings/export?format=xlsx|pdf` (ADMIN RW, INSTRUCTOR R; universal list `x-filterable: date, ringId, memberId, dogId, state, origin`, `x-sortable: startsAt`, `x-exportable` with the `listKey = training-bookings` column labels). `/ring-blocks*` already exists from E4-T01/E4-T03: do **not** redeclare it; only add what S09 needs and E4 did not publish — the `MEMBER` redacted projection is already there; check and state in the report whether `GET /ring-blocks` declares `x-filterable: ringId, kind, reason, state, from, to`.
3. **S15 controllers + DTOs** (`platform/api` for `/platform/jobs*`, `clubs/common/api` for the club routes; the job framework itself is `platform`): `GET /jobs` (ADMIN) · `GET /jobs/{name}/runs` (ADMIN; universal list `x-filterable: status, scheduledFor, trigger, dryRun`, `x-sortable: scheduledFor, startedAt`) · `GET /jobs/{name}/runs/{runId}` (ADMIN) · `POST /jobs/{name}/trigger {dryRun}` (ADMIN, 200) · `PUT /jobs/{name}/switch {enabled}` (ADMIN, 200) · `GET /risk-review?date=` (ADMIN, INSTRUCTOR; form A of S15 §6) · `GET /platform/jobs/overview?clubId=&status=` (AGILITYHUB_ADMIN) · `POST /platform/clubs/{clubId}/jobs/{name}/trigger {dryRun}` (AGILITYHUB_ADMIN). Impersonation tokens → `403` on all of them (R-15-09). `{name}` is the **route id** of R-15-01 (`week-opening`, `risk-review`, `no-show-notices`, `reminders`, `expirations`, `waitlist-fifo`, `payment-timeouts`, `class-finishing`, `cleanup`, `billing-reminder`); an unknown name → `JOB_UNKNOWN`; a process whose module is off → `MODULE_DISABLED` (404) and its row absent from `GET /jobs`.
4. **Schemas** — the names below are the contract and each one's field list is the **complete** JSON example of the owning spec §6 (required by default, nullable marked; transcribe them, do not summarize): `MeHome` (+ `HomeDog`, `ReservationRow` with `type CLASS·CLASS_WAITLIST·TRAINING·ACTIVITY` and `state CONFIRMED·PAYMENT_PENDING·WAITLISTED·REGISTERED`) · `BookableClasses` (+ `BookableClass` with `state BOOKABLE·WAITLIST_OPEN·WAITLIST_FULL·FULL·WEEKLY_LIMIT_DONE·NOT_YET_OPEN·PACK_EMPTY·NOT_BOOKABLE`, `notBookableReason BLOCKED·INACTIVITY·LEAVING`, `week CURRENT·NEXT·LATER`, `PackCard.state ACTIVE·EXPIRING·EXPIRED·EMPTY`) · `SeatHoldResponse` (+ `LimitStatus {reached, unit, week, count, max, swappable[], notSelectable[{…, reason DONE·LATE_WINDOW}]}`, `serverNow`, `holdSeconds`) · `Booking` (`state PAYMENT_PENDING·ACTIVE·CANCELLED·CANCELLED_LATE·CANCELLED_BY_CLUB`, `origin APP·BACKOFFICE·INSTRUCTOR·SYSTEM`, `bookedBy`, `swapFromBookingId?`, `pack?`, `charge?`, `checkoutUrl?`, `calendarLinks{google, outlook, ics}`, `cancellation?`, the derived `displayState`) · `WaitlistEntry` (`state ACTIVE·NOTIFIED·CONSOLIDATED·EXPIRED·CANCELLED`, `position`, `confirmBy?`, `cancelReason?`) · `TrainingSlots` (+ `TrainingRing`, `TrainingDay`, `TrainingSlot`, `SlotCell {state FREE·BOOKED·BLOCKED, reason? CLASS·RING_BLOCK·TRAINING·OWN_TRAINING, occupants[]?, block?, classSession?}`) — document which fields only the INSTRUCTOR/ADMIN projection carries · `TrainingSummary` (`eligibleDogs[].rightSource LEVEL·MANUAL`, `counter`, `cancellableBookings[]`) · `TrainingBooking` (`state ACTIVE·CANCELLED·CANCELLED_BY_CLUB`, `origin APP·BACKOFFICE`, `slotId`, `cancellableUntil`, `counter`; `seatIndex` is **not** exposed) · `JobSummary {name, jobName, module?, enabled, schedule{kind DAILY·WEEKLY·MONTHLY·CONTINUOUS, localTime?, dayOfWeek?, dayOfMonth?}, nextScheduledForLocal?, lastRun?}` · `JobRun {runId, job, scheduledFor, scheduledForLocal, timeZone, trigger SCHEDULE·CATCH_UP·MANUAL, dryRun, status RUNNING·SUCCEEDED·PARTIAL·FAILED·SKIPPED, skipReason? DISABLED·MODULE_OFF·CLUB_INACTIVE·MISSED_WINDOW·LOCKED·NOT_DUE, startedAt, finishedAt?, durationMs?, effects{counters, items[{entityType, entityId, action, detail?}]}, errors[{entityId?, code, message, traceId}], actorAccountId?, parametersSnapshot}` · `JobTriggerRequest {dryRun}`, `JobSwitchRequest/Response` · `RiskReview` = form A (`items[].status AUTO_CANCELLED·AT_RISK·WILL_CANCEL·WILL_REVIEW`, `dayLabel TODAY·TOMORROW·OTHER`) · `PlatformJobsOverview` (`jobs[].health OK·WARN·ALERT`).
5. **Error codes and status audit.** Every S08/S09/S15 code already exists in `ErrorCode` (E0-T04 loaded the catalog): the work is the audit against `CATALEG_ERRORS.md` §1 + §3 rule 0, **the catalog file wins over anything S08/S09/S15 §6 writes**. Explicit §1: `CLASS_FULL`, `SEAT_TAKEN`, `SEAT_HOLD_EXPIRED`, `SLOT_TAKEN`, `BOOKING_LIMIT_REACHED`, `WAITLIST_LIMIT`, `TRAINING_LIMIT_REACHED`, `JOB_ALREADY_RUNNING`, `STALE_VERSION`, `INVALID_STATE`, `IDEMPOTENCY_KEY_REUSED` **409** · `NOT_YET_OPEN`, `CLASS_NOT_BOOKABLE`, `LEVEL_NOT_ALLOWED`, `PACK_EMPTY`, `MEMBER_NOT_ACTIVE`, `BOOKING_BLOCKED`, `INACTIVITY_PERIOD`, `DOG_NOT_ALLOWED`, `SLOT_OUT_OF_WINDOW`, `CLUB_CLOSED`, `RING_NOT_RESERVABLE` **422** · `VALIDATION_ERROR`, `INVALID_FILTER`, `INVALID_TIME_RANGE` **400** · `MODULE_DISABLED`, `NOT_FOUND`, `DOG_NOT_ACCESSIBLE` **404**. By rule 0: `ALREADY_BOOKED`, `ALREADY_ON_WAITLIST` (prefix `ALREADY_`), `RING_BLOCK_CONFLICT`, `CLASS_CONFLICT` (suffix `_CONFLICT`) **409**; **everything else is 422** — `CLASS_NOT_FULL`, `WAITLIST_FULL`, `WAITLIST_NOT_NOTIFIED`, `WAITLIST_OFFER_EXPIRED`, `WAITLIST_ENTRY_NOT_LIVE`, `SWAP_NOT_ALLOWED`, `BOOKING_NOT_CANCELLABLE`, `WEEKLY_LIMIT_DONE`, `SLOT_NOT_ON_GRID` (S09 writes 400), `DOG_ALREADY_BOOKED` (S09 writes 409; rule 0 is literal — only the `ALREADY_` **prefix** counts), `TRAINING_CANCEL_TOO_LATE`, `OVERRIDE_NOT_ALLOWED` (S09 writes 403), `RING_HAS_BOOKINGS`, `RING_BLOCK_MANAGED_BY_ACTIVITY`, `JOB_UNKNOWN` (S15 writes 404), `NOT_DUE`. Fix every enum value that contradicts the catalog, list the changes in the report, and file a catalog **proposal** for the three surprising ones (`OVERRIDE_NOT_ALLOWED` → §1 403, `JOB_UNKNOWN` → §1 404, `SLOT_NOT_ON_GRID` → §1 400); until the catalog is amended the rule-0 status ships. `details` payloads the UI needs: `BOOKING_LIMIT_REACHED {unit, week, limit, current, swappable[], notSelectable[], nextBookableAt}`, `CLASS_FULL {heldOnly}`, `NOT_YET_OPEN {opensAt}`, `WAITLIST_LIMIT {scope: CLASS|DOG_WEEK}`, `INACTIVITY_PERIOD {from, to}`, `TRAINING_LIMIT_REACHED {limit, used, weekStart, weekEnd, cancellableBookings[]}`, `SLOT_TAKEN {ringId, startsAt, reason, freeRings[]}`, `SLOT_OUT_OF_WINDOW {from, to}`, `TRAINING_CANCEL_TOO_LATE {thresholdMinutes, minutesBefore}`, `RING_HAS_BOOKINGS {bookings[]}`. `error.<CODE>` must exist in the three `messages_*.properties`.
6. **Mongo documents + indexes** (`persistence` packages, `TenantEntity`, `clubId` everywhere, `createdAt/By`, `updatedAt/By`): `Booking` · `bookings` (S08 §3 field list) with `{clubId, classSessionId, dogId, state}`, `{clubId, dogId, bookingWeekKey, state}`, `{clubId, memberId, bookingWeekKey, state}`, `{clubId, state, classStartsAt}` (S15 P4/P7) · `SeatHold` · `seat_holds` **TTL on `expiresAt`** + unique `{clubId, classSessionId, dogId}` · `WaitlistEntry` · `waitlist_entries` with `{clubId, classSessionId, state}`, `{clubId, dogId, bookingWeekKey, state}`, `{clubId, state, confirmBy}` (P6) · `SeatLock` · `seat_locks` (`_id = classSessionId`, `clubId`, `version`) · `TrainingBooking` · `training_bookings` with the **partial unique** `{clubId, ringId, startsAt, seatIndex}` on `{state: "ACTIVE"}`, plus `{clubId, startsAt}`, `{clubId, dogId, weekStart, state}`, `{clubId, memberId, weekStart, state}`, `{clubId, state, startsAt}` · `JobRun` · `job_runs` **unique** `{clubId, job, scheduledFor, trigger}` + `{clubId, job, startedAt}` + `{finishedAt}` · `JobLock` · `job_locks` (`_id = "{clubId}:{job}"` or `"tick"`, `holder`, `acquiredAt`, TTL on `expiresAt`). New fields on existing aggregates (S15 §3): `Week.openedAt`, `Week.openingNotifiedAt` (E4-T02 document), `ClassSession.finishedAt` and `risk.lowAlertSentAt` (E4-T01 already persists `risk{exempt, notifiedBookingIds[], adminNotifiedAt, lowAlertSentAt}` — verify and say so), `Booking.reminderSentAt`, `TrainingBooking.reminderSentAt`. `idempotency_records` already exists (E0/E3): reuse it, do not redeclare.
7. **Process framework** (`platform/application/jobs`, `platform/persistence/jobs`; platform **never** imports `clubs.*` — jobs are discovered as `List<Job>` beans): (a) `JobName` enum + `JobCatalog` with one `JobDefinition {JobName name, String routeId, Cadence cadence, String localTimeParameter, Module module?, Duration catchUpWindow (null = unlimited), String switchParameter}` per R-15-01 row, all ten declared now even though eight have no implementation yet — `GET /jobs` lists only those with a registered `Job` bean plus their catalog row, and the report states which ones are missing; (b) `interface Job { JobName name(); List<JobItem> plan(JobContext ctx); Effect apply(JobContext ctx, JobItem item); }` with `JobContext {clubId, ZoneId zone, Instant scheduledFor, LocalDate localDate, boolean dryRun, ClubConfig config, JobRunRecorder recorder}`; (c) `JobOccurrences` (pure, no Spring): `occurrence(date, time, zone)` with Java semantics (gap → shifted forward, overlap → **first** occurrence), `lastDue(now, definition, zone)` for DAILY/WEEKLY/MONTHLY/CONTINUOUS, `triggerFor(now, scheduledFor, window)` → `SCHEDULE` (≤ 2 min), `CATCH_UP` (inside the window), `MISSED_WINDOW` (outside) — R-15-02, R-15-05; (d) `SchedulerTick` `@Scheduled(cron = "0 * * * * *")`, `now = clock.instant()` truncated to the minute, one `TenantContext` scope per club with `status ∈ {ACTIVE, ONBOARDING}` (A18e), fixed catalog order, sequential, guarded by the `"tick"` lock (lease 55 s) with metric `jobs.tick.overrun`; (e) `JobLocks` (`findOneAndUpdate` upsert with the `expiresAt < now` predicate, lease 300 s renewed every 60 s, `DuplicateKey` → `SKIPPED{LOCKED}` or `409 JOB_ALREADY_RUNNING`) — R-15-06; (f) `JobRunner`: guards of R-15-03 (`jobs.<name>.enabled`, module, club status; continuous jobs record a `SKIPPED` at most once per hour), one **Mongo transaction per item** with the outbox (R-15-10), `PARTIAL` on item failure with `errors[]`, `FAILED` on a job-level exception, `effects.items` capped at 500, `parametersSnapshot`, `SchedulerRun` on the outbox for non-dry runs (R-15-07); (g) dry run (R-15-08): only `plan` runs, `action` prefixed `WOULD_`, **no write anywhere** (no collection, no outbox, no S3, no provider) except the `JobRun{dryRun: true}` itself; (h) metrics Micrometer `jobs.run.duration{job,status}`, `jobs.effects{job,key}`, `jobs.last_success_age_seconds{job,club}`, `jobs.tick.overrun`; (i) `JobFailed{job, runId, status, errorCount}` on the outbox on `FAILED`/`MISSED_WINDOW` + the N-42 consumer (ADMINS → APP+EMAIL, `jobs.alertAdminsOnFailure`, **max one per job and club-local day**, deterministic id, keys `notif.N-42.*` in ca/es/en, action `OPEN_JOBS`; pattern of E4-T03's `notifications.N-08a`); (j) **`@Audited(JOB_TRIGGERED)`** on the manual trigger (`entityType = JobRun`, `after {job, dryRun, status, counters}`) — S15 R-15-09 names it but S14 R-14-09 does not list it: add it with its `@AuditCovers` test and file the catalog proposal in the report and `MESSAGES.md`; (k) a **fictitious test job** (`JobName.TEST_NOOP`, registered only under the `test` profile, absent from `GET /jobs`) that plans N items and fails the nth, so T-15-01…07, 10, 31 run without any business vertical; (l) `POST /test/clock` **only** under the `test`/`local` profiles (S15 §12 WP-15-E) advancing a `MutableClock` bean, documented in `README.md`.
8. **ArchUnit rule (R-15-22)**: no production class outside the `Clock`/`ClubClock` beans may call `Instant.now()`, `LocalDate.now()`, `LocalDateTime.now()`, `ZonedDateTime.now()` or `System.currentTimeMillis()`; add it to `ArchitectureTest` as `E5_T01_timeComesFromTheClock` (T-15-07) and fix every existing violation you find (list them in the report).
9. **Events + notification fixtures**: `BookingEvent`, `TrainingEvent` and `SchedulerEvent` records (`domain` packages, `DomainEvent`) with the catalog names and the payloads of S08 §7 / S09 §7 / S15 §7 — `SeatHeld`, `SeatHoldReleased`, `BookingCreated`, `BookingCancelled`, `SeatReleased`, `WaitlistJoined`, `WaitlistLeft`, `WaitlistNotified{…, mode}`, `WaitlistConsolidated`, `WaitlistExpired`, `TrainingBooked`, `TrainingCancelled{…, by, cancelReason, late}`, `SchedulerRun`, `WeekOpened`, `TrainingCounterReset`, `ClassAtRisk`, `ClassAutoCancelled`, `ClassBelowMinimum`, `ReminderDue`, `JobFailed` — registered as samples in `EventCatalogContractTest` with fixtures `e5-events.json`; notification fixtures `e5-notifications.json` for N-04, N-05, N-06, N-07, N-15, N-36, N-40, N-42, N-46, N-47 with the catalog variables (rendering and sending are E5-T02…T05 and E7). The extended payloads of `WeekOpened`, `ClassAtRisk` and `ClassAutoCancelled` are S15 §13 proposals: implement them as S15 writes them and record the proposal in the report.
10. `bin/openapi-snapshot`, a `docs/openapi/CHANGELOG.md` entry (every route + the list/export fields), `E5ContractIT` (501 matrix: every route × ANON/MEMBER/INSTRUCTOR/ADMIN/AGILITYHUB_ADMIN, impersonation rules, cross-tenant 404, `FREE_TRAINING` off → 404 on every S09 route, `WAITLIST` off → 404 on `/waitlist-entries*`, `SINGLE_CLASS` off → no `price`/`checkoutUrl` in the schemas' examples, `MEMBER` on `GET /bookings` and `GET /training-bookings` → 403, no writes on any collection), `E5ResponseContractTest` (fixture round-trip of the S08/S09/S15 forms), `OpenApiRequiredContractTest` updated.

## Fixed conventions
- Code names from `docs/MODEL_DADES_PLATAFORMA.md` §0: `Booking`, `SeatHold`, `WaitlistEntry`, `SeatLock`, `TrainingBooking`, `RingBlock`, `JobRun`. Contexts: `clubs.bookings` (S08), `clubs.training` (S09), `platform` (S15 framework). **Dependency direction, enforced by `ArchitectureTest`** (cross-context only through `application` packages, no cycles): `clubs.bookings` → `clubs.scheduling.application` (+ `platform`, `shared`); `clubs.training` → `clubs.bookings.application` and `clubs.scheduling.application`; `clubs.activities` → `clubs.bookings.application`; **never** the other way — anything `bookings` needs from `training` or `activities` is a port declared in `clubs.bookings.application.ports` whose adapter lives in the providing context. `platform` imports no `clubs` package: jobs arrive as injected `List<Job>` beans.
- Closed catalogs: no new parameter, event, notification or error code. The only additions this task makes are `AuditAction.JOB_TRIGGERED` and the `jobs.*` parameters, both already named by S15 (§9 and R-15-09); everything else that a spec names and a catalog lacks goes to the report and `MESSAGES.md` as a proposal while the closest existing item ships. Catalog rule 0 is literal and the catalog file wins over any status written in S08/S09/S15 or in this file.
- Parameter keys exactly as in `CATALEG_PARAMETRES.md`, never a literal in code: the `bookings.*` block (`maxCurrentWeek` 2, `maxNextWeek` 1, `limitUnit` DOG, `weekOpensAt` {SUNDAY, 20:00}, **`lateCancelThresholdMinutes` 240** — Jordi 05-09, S08 R-08-10 still writes 120 and the catalog wins —, `seatHoldSeconds` 30, `showInstructorHoursBefore` 24, `instructorLastMinuteNotice` true, `paymentPendingMinutes` 30), the `waitlist.*` block (`mode` ALL_AT_ONCE, `fifoConfirmMinutes` 30, `maxPerClass` 3, `maxPerDogPerWeek` 2, `maxPerDogPerWeekIfAttended` 1, `notifyThresholdMinutes` 30), the `training.*` block (`slotMinutes` 30, `bookingWindowDays` 3, `maxPerWeek` 3, `cancelThresholdMinutes` 120, `capacityPerRingSlot` 1 scoped to `ring`, `freeTrainingRequiresLicense` false and informative), `ringBlocks.maxHorizonDays` (60), the `jobs.*` family (`<name>.enabled`, `dailyTime` 06:00, `alertAdminsOnFailure` true, `retention.*`), `classes.finishGraceMinutes` (15), `classes.riskReviewTime` (07:30), `classes.minDogs` (2), `classes.riskLookaheadDays` (2), `classes.riskAutoCancelSameDay` (true), `levels.enabled`, `club.timeZone`, `club.openingHours`, `club.holidays`.
- Everything time-related uses the injected `Clock` and `ClubClock`; instants are UTC on the wire, business dates are club-local; DST semantics identical to S06 R-06-14 and S15 R-15-02.
- Evidence rule: report = command + exit code + last 40 lines; long outputs → `roadmap/evidence/E5-T01/NN-<name>.log`; task file ≤ 120 KB. Fictional data only (`@example.test`, fake ids); no secrets, env vars only. Never edit other tasks, `ROADMAP.md` or the Organizer verification section. English identifiers from the glossary; i18n keys never literals; ca/es/en always together.

## Verification (run all; evidence = command + exit code + last 40 lines; long outputs → `roadmap/evidence/<ID>/*.log`)
- `./mvnw -q verify` green with `E5ContractIT`, `E5ResponseContractTest`, the framework tests named T-15-01…T-15-07, T-15-10, T-15-29, T-15-31 (T-15-01…07 without Spring or Mongo where possible; T-15-31 with two application contexts over the same Mongo), `EventCatalogContractTest`, `ErrorCatalogContractTest`, `AuditContractTest` (with `JOB_TRIGGERED` covered), `ArchitectureTest` including the new `Clock` rule, message parity ca/es/en (`error.*` for every new code, `notif.N-42.*`), coverage gates.
- `bin/openapi-snapshot` run twice → byte-identical (`cmp`); `docs/openapi/CHANGELOG.md` lists the operations added (count them and state the number); `git diff --check` clean.

## Definition of Done
Every S08, S09 and S15 route is published in the snapshot with its schemas, roles, modules, idempotency and canonical error statuses and answers 501 behind the guards; the documents and indexes exist (including the two unique/partial guards the concurrency of E5 depends on); the job framework runs a fictitious job end to end with locks, catch-up, dry run, metrics, `SchedulerRun`, `JobFailed` → N-42 and audit; the `Clock` ArchUnit rule is green; the organizer can stage the snapshot for the web tasks.

## Evidence to return
`verify` tail; snapshot `cmp` result; OpenAPI diff summary (routes + schemas added, existing schemas changed); the list of `ErrorCode` values whose status you changed; the list of `Instant.now()`-style violations the new ArchUnit rule found and how you fixed them; the `JobRun` documents of one real and one dry run of the test job (ids truncated).

---

## Executor report

Done (executor claude-opus-5-5, session 2026-09-23/24). What ships:

- **Contract:** S08/S09/S15 P1 is published as **32 new operations** (16 S08, 8 S09, 8 S15) on **30 new paths**, with **96 new schemas**. Nothing was removed. Every operation runs its tenant, role, ownership, impersonation and module guards and then answers `501 NOT_IMPLEMENTED`.
- **Persistence:** the documents and indexes are in, including the TTL on `seat_holds`, the partial unique active-seat guard on `training_bookings` and the claim index on `job_runs`.
- **Job framework:** it runs the fictitious `TEST_NOOP` job end to end, covering locks, catch-up, dry run, metrics, `SchedulerRun`, `JobFailed` → N-42, the `JOB_TRIGGERED` audit and two application contexts.
- **Verification:** `./mvnw -q verify` exits 0 with **410 unit/contract + 636 integration tests** and zero failures, errors or skips. The coverage and architecture gates hold, including the new `E5_T01_timeComesFromTheClock`.
- **Snapshot:** two consecutive `bin/openapi-snapshot` generations are byte-identical.

The dependencies E4-T04 and E4-T05 were `awaiting_verification` when this task started (allowed by roadmap/README §2). Nothing here changes their code except the `Week` constructor calls (see Assumptions 8).

### Branch / commits

No git write command was run (no branch, add, commit, stash, reset or push). The work is in the working tree on `main` for the publish script. Read-only git only: `git status`, `git diff --check`, and `git show HEAD:docs/openapi/openapi.json` inside `contract-summary.py`.

### Evidence (full command outputs)

Full logs are in `roadmap/evidence/E5-T01/`. Mongo cluster ids and hashes are truncated by `sanitize.py`.

**Earlier failing attempt (one line):** `01-verify.log` — `./mvnw -q verify` exited 1. 628 of 636 ITs passed; all 8 `JobFrameworkIT` tests errored in `@BeforeEach` because another suite already owns the account email `admin@example.test` (unique index `account_email`). I switched the fixture to `e5-jobs-admin@example.test`.

**1. Full verification** — exit **0**; full log `02-verify.log` (4 864 lines). The totals come from the Surefire/Failsafe XML (`test-summary.py` → `08-test-summary.log`): `unit/contract (surefire): tests=410 failures=0 errors=0 skipped=0` · `integration (failsafe): tests=636 failures=0 errors=0 skipped=0`. The per-class lines for every E5 test are in that file. The JaCoCo check goal passed inside `verify`. Coverage of the gated packages this task touches:
- `platform.application.jobs`: 98.0 % lines / 85.9 % branches
- `clubs.bookings.application`: 100 / 96.2
- `clubs.training.application`: 100 / 100
- `clubs.common.application`: 98.2 / 88.3
- `shared.application`: 98.0 / 91.5
- `clubs.bookings.api`: 87.3 / 87.5
- `clubs.training.api`: 87.3 / 85.7
- `clubs.common.api`: 98.9 / 90.0
- `platform.api`: 98.7 / 86.8

```sh
./mvnw -q verify > roadmap/evidence/E5-T01/02-verify.log 2>&1   # exit 0
```
Round 1 tail: see `02-verify.log` (Spring/Mongo shutdown noise only). The final run's tail is in Round 2 below.
After this run I only added two `System.out.println` evidence lines to `JobFrameworkIT.T_15_06`, then re-ran that class alone: 8/8 passed (`06-jobrun-documents.log`). No main code changed after the full verify.

**2. `bin/openapi-snapshot` twice + `cmp`.** Run 1 (`03-snapshot-first.log`, exit 0) was followed by a copy to `/tmp`, which the sandbox refused, so that pair could not be compared. I then used a copy under `target/`:
```sh
bin/openapi-snapshot > roadmap/evidence/E5-T01/04-snapshot-second.log 2>&1   # exit 0
cp docs/openapi/openapi.json target/e5-t01-openapi-previous.json              # exit 0
bin/openapi-snapshot > roadmap/evidence/E5-T01/05-snapshot-third.log 2>&1    # exit 0
cmp target/e5-t01-openapi-previous.json docs/openapi/openapi.json            # exit 0, no output → byte-identical
```
Round 1 tails: `04-snapshot-second.log` and `05-snapshot-third.log` (both end with `Updated docs/openapi/openapi.json`).
`OpenApiSnapshotTest` also passed inside the full verify (Failsafe, 9 tests), so the committed snapshot equals the generated one.

**3. OpenAPI diff summary** — `python3 roadmap/evidence/E5-T01/contract-summary.py > …/07-contract-summary.log`, exit 0. Diffed against `HEAD`:
- **Operations:** 32 added (16 S08, 8 S09, 8 S15; each is listed in the log and in `docs/openapi/CHANGELOG.md`), 0 removed.
- **Paths and schemas:** 30 paths added; 96 schemas added, 0 removed.
- **Existing schemas changed:** only `AuditAction` (+`JOB_TRIGGERED`).
- **Existing operations changed:** none in the final snapshot. An earlier draft renamed `GET /activities/export`'s operationId to `export_1`. I fixed that by giving every new controller method a unique name (all 32 new operationIds are free of `_n` suffixes, asserted in `E5ContractIT`).

**4. `git diff --check`** — exit 0, no output.

**5. `JobRun` documents** of one dry and one real run of the test job (`06-jobrun-documents.log`, ids truncated):
```text
E5-T01 JobRun dry  {"_id": "3f69abb9…[truncated]", "clubId": "e5-jobs-a", "job": "TEST_NOOP", "scheduledFor": {"$date": "2026-10-05T04:00:00Z"}, "scheduledForLocal": "2026-10-05T06:00", "timeZone": "Europe/Madrid", "trigger": "MANUAL", "dryRun": true, "status": "SUCCEEDED", "startedAt": {"$date": "2026-10-05T04:00:00Z"}, "finishedAt": {"$date": "2026-10-05T04:00:00Z"}, "durationMs": 0, "counters": [{"key": "WOULD_NOOP", "value": 3}], "items": [{"entityType": "TestItem", "entityId": "item-1", "action": "WOULD_NOOP", "detail": [{"key": "position", "value": 1}]}, {"entityType": "TestItem", "entityId": "item-2", "action": "WOULD_NOOP", "detail": [{"key": "position", "value": 2}]}, {"entityType": "TestItem", "entityId": "item-3", "action": "WOULD_NOOP", "detail": [{"key": "position", "value": 3}]}], "errors": [], "actorAccountId": "e5-jobs-admin", "parametersSnapshot": [{"key": "jobs.cleanup.enabled", "value": true}, {"key": "jobs.dailyTime", "value": "06:00"}], "exclusive": false, "holder": "92539fb3…[truncated]:5e7810ff…[truncated]", "leaseExpired": false, "_class": "com.agilityhub.core.platform.persistence.jobs.JobRun"}
E5-T01 JobRun real {"_id": "c8afef98…[truncated]", "clubId": "e5-jobs-a", "job": "TEST_NOOP", "scheduledFor": {"$date": "2026-10-05T04:00:00Z"}, "scheduledForLocal": "2026-10-05T06:00", "timeZone": "Europe/Madrid", "trigger": "MANUAL", "dryRun": false, "status": "SUCCEEDED", "startedAt": {"$date": "2026-10-05T04:00:00Z"}, "finishedAt": {"$date": "2026-10-05T04:00:00Z"}, "durationMs": 0, "counters": [{"key": "applied", "value": 3}], "items": [{"entityType": "TestItem", "entityId": "item-1", "action": "NOOP", "detail": [{"key": "position", "value": 1}]}, {"entityType": "TestItem", "entityId": "item-2", "action": "NOOP", "detail": [{"key": "position", "value": 2}]}, {"entityType": "TestItem", "entityId": "item-3", "action": "NOOP", "detail": [{"key": "position", "value": 3}]}], "errors": [], "actorAccountId": "e5-jobs-admin", "parametersSnapshot": [{"key": "jobs.cleanup.enabled", "value": true}, {"key": "jobs.dailyTime", "value": "06:00"}], "exclusive": false, "holder": "92539fb3…[truncated]:351499a9…[truncated]", "leaseExpired": false, "_class": "com.agilityhub.core.platform.persistence.jobs.JobRun"}
```
The test run uses `MockClock`, so `durationMs = 0`. The API view (`JobRun` schema) turns the key/value lists back into maps.

**6. `ErrorCode` statuses changed** (catalog rule 0 wins): `CLASS_NOT_FULL` 409→422, `WAITLIST_FULL` 409→422, `DOG_ALREADY_BOOKED` 409→422 (only the `ALREADY_` *prefix* counts). All other S08/S09/S15 codes already matched. `SLOT_NOT_ON_GRID`, `OVERRIDE_NOT_ALLOWED`, `JOB_UNKNOWN` and `NOT_DUE` were already 422. `error.<CODE>` exists in ca/es/en for all 48 audited codes (asserted).

**7. `Instant.now()`-style violations found by the new ArchUnit rule: none.** The only `*.now(` call in production code is `LocalDate.now(clock.withZone(...))` in `clubs/signup/domain/FirstMonthCalculator.java:18`, which passes the injected Clock and is allowed. `ClockRuleTest` proves the rule rejects `Instant.now()` and `System.currentTimeMillis()` and accepts `LocalDate.now(clock)`/`clock.instant()`. Allowed beans: `ClockConfiguration`, `DefaultClubClock` and the new `OffsetClock`.

### Files changed

**New (main)**
- `clubs/bookings`:
  - `domain/{BookingState, BookingOrigin, BookingCancelReason, ActorRole, WaitlistState, WaitlistCancelReason, ChargeMode, BookingEvent}`
  - `persistence/{Booking, BookingRepository, SeatHold, SeatHoldRepository, WaitlistEntry, WaitlistEntryRepository, SeatLock, SeatLockRepository}`
  - `application/BookingContractAccess`
  - `api/{BookingContracts, BookingRequests, BookingsController}`
- `clubs/training`:
  - `domain/{TrainingBookingState, TrainingOrigin, TrainingCancelledBy, TrainingCancelReason, SlotState, SlotReason, TrainingEvent}`
  - `persistence/{TrainingBooking, TrainingBookingRepository}`
  - `application/TrainingContractAccess`
  - `api/{TrainingContracts, TrainingRequests, TrainingController}`
- `platform/application/jobs/{JobName, JobStatus, JobTrigger, SkipReason, Cadence, CatchUpWindow, JobDefinition, JobCatalog, JobSchedule, JobOccurrences, Job, JobItem, JobEffect, JobContext, JobRunRecorder, JobRunner, SchedulerTick, JobMetrics, JobTriggerService, JobContractAccess, JobViews, TestNoopJob}`
- `platform/persistence/jobs/{JobRun, JobRunRepository, JobLock, JobLockRepository}`
- `platform/api/{PlatformJobsController, TestClockController}`
- `clubs/common/api/{JobsController, RiskReviewContracts}`
- `clubs/common/application/JobFailureNotifications` (N-42)
- `shared/domain/events/SchedulerEvent`
- `shared/application/{MutableClock, OffsetClock}`
- `shared/application/contract/AllowsImpersonation`
- `configuration/E5ContractConfiguration`

**Modified (main)**
- `shared/domain/ErrorCode` (3 statuses)
- `platform/application/audit/AuditAction` and `platform/api/AuditContracts` (`JOB_TRIGGERED`)
- `clubs/scheduling/persistence/Week` (+`openedAt`, `openingNotifiedAt`, plus a legacy constructor)
- `WeekGenerationUseCase` and `WeekValidationUseCase` (keep both marks when they copy a `Week`)
- `platform/persistence/ClubRepository` (+`schedulableClubs`)
- `configuration/ClockConfiguration` (`OffsetClock` under local/test)
- `configuration/SecurityConfiguration` and `shared/api/TenantFilter` (public `.ics` route; `/test/clock` under local/test)
- `clubs/messaging/application/SystemEmailRenderer` (N-42 sendable)
- `messages_{ca,es,en}.properties` (`notif.N-42.*`, `jobs.name.*`)
- `docs/openapi/openapi.json`, `docs/openapi/CHANGELOG.md`, `CHANGELOG.md`, `README.md`
- `docs/specs/S14-…md` R-14-09: `JOB_TRIGGERED` is now backticked, as the row itself instructs for this task

**Tests**
- New:
  - `E5ContractIT`, `E5PersistenceIT`, `E5ResponseContractTest`
  - `platform/application/jobs/{JobOccurrencesTest, JobFrameworkIT, JobFrameworkTwoInstancesIT}`
  - `arch/ClockRuleTest`, `shared/application/OffsetClockTest`
  - fixtures `e5-routes.json`, `e5-bookings-responses.json`, `e5-training-responses.json`, `e5-jobs-responses.json`, `e5-events.json`, `e5-notifications.json`
- Modified:
  - `ArchitectureRules`/`ArchitectureTest` (rule `E5_T01_timeComesFromTheClock`)
  - `ErrorCatalogContractTest` (E5 status audit)
  - `EventCatalogContractTest` (3 new samples)
  - `OpenApiRequiredContractTest` (2 all-optional request bodies, E5 required sets)
  - `support/MockClock` (now `extends MutableClock`)

**Evidence** — `roadmap/evidence/E5-T01/01…08-*.log` plus `contract-summary.py`, `test-summary.py` and `sanitize.py`.

### Rules and tests implemented (R-xx-nn / T-xx-nn)

**Framework (WP-15-A)**
- **R-15-01** — catalog with 10 rows, fixed order; tick every minute under the `tick` lease (55 s); `jobs.tick.overrun`.
- **R-15-02** — `JobOccurrences`: gaps shift forward, overlaps take the first occurrence, weekly/monthly/continuous.
- **R-15-03** — switch, module (+ `Job.activeFor`) and club status; continuous jobs record `SKIPPED` at most once per hour.
- **R-15-04** — exclusive unique index plus leases.
- **R-15-05** — `SCHEDULE` within 2 min, otherwise `CATCH_UP` or `MISSED_WINDOW` by catalog window.
- **R-15-06** — upsert guarded by `expiresAt < now`; `DuplicateKey` → `LOCKED`/`409`; lease renewed every 60 s; expired leases reaped to `FAILED` and retaken as `CATCH_UP`.
- **R-15-07** — `JobRun` for every execution, plus `SchedulerRun` on the outbox for non-dry runs.
- **R-15-08** — dry run: plan only, `WOULD_*`, no writes.
- **R-15-09** — manual run ignores the switch; module off → 404; lock held → 409; audited `JOB_TRIGGERED`.
- **R-15-10** — one transaction per item (retry on `WriteConflict`); `PARTIAL`/`FAILED`; items capped at 500; `JobFailed` → N-42 once per job and local day; Micrometer `jobs.run.duration`, `jobs.effects`, `jobs.last_success_age_seconds`, `jobs.tick.overrun`.
- **R-15-22** — ArchUnit rule.

**Framework tests**
- `JobOccurrencesTest` (no Spring, no Mongo): T-15-01 (`T_15_01_eachClubIsDueAtItsOwnLocalHour`, `…catalogHasTheTenR1Processes…`), T-15-02 (`…daylightSaving…`, `…monthlyContinuousAndNext…`), T-15-04, T-15-10 (value normalisation).
- `JobFrameworkIT`: T-15-03, T-15-05 (×2, incl. missed window), T-15-06, T-15-10, T-15-31 (lease part), T-15-08 (audit half), T-15-01 (tick).
- `JobFrameworkTwoInstancesIT`: T-15-31 (two contexts over the same Mongo).
- `ClockRuleTest` and `ArchitectureTest.E5_T01_timeComesFromTheClock`: T-15-07.

**Contract halves**
- **T-08-47, T-09-24, T-15-29**: `E5ContractIT` (every route × 5 roles; tenant mismatch; missing membership; cross-tenant 404; impersonation; ownership; `MEMBER` → 403 on the lists; `WAITLIST`/`FREE_TRAINING`/process module off → 404; `JOB_UNKNOWN`; no writes; snapshot metadata and fixture ↔ schema conformity), plus `E5ResponseContractTest`, `ErrorCatalogContractTest.T_08_47_T_09_24_T_15_29_…`, `EventCatalogContractTest` and `E5PersistenceIT` (indexes, TTL, partial unique guard, the S15 §3 marks).
- **Roles, matrix and module rows**: T-08-26, T-09-30, T-09-31 (module half) and T-08-27/T-09-23 (impersonation acceptance).
- **Not done here:** the behaviour of T-08-xx/T-09-xx/T-15-08…28 belongs to E5-T02…T05.

### Assumptions
1. **S15 routes are 501 stubs.** E5-T05 step 1 replaces "E5-T01's 501 stubs", so `/jobs*`, `/risk-review` and `/platform/jobs*` run the guards: `JOB_UNKNOWN`, `MODULE_DISABLED` of the process, the tenant-scoped run, and an unknown club → 404. The framework itself is real. `JobTriggerService.trigger(JobName, dryRun)` is the audited service that E5-T05's controllers will call after `JobCatalog.byRoute(name)`. `GET /jobs` row building and the overview `health` are E5-T05.
2. **`RiskReviewForm`.** S15 form A is published under this schema name because `RiskReview` already names the S14 dashboard card inside `GET /dashboard` (E3-T04, a different shape). Renaming the existing schema would break the web client. See Questions 1.
3. **`CatchUpWindow` is an enum** (`HOURS_24`, `END_OF_LOCAL_DAY`, `END_OF_LOCAL_MONTH`, `UNLIMITED`, `CONTINUOUS`) instead of `Duration` (null = unlimited). Two R-15-01 windows end at a club-local boundary that a Duration cannot express.
4. **`job_runs` claim index.** The unique `{clubId, job, scheduledFor, trigger}` index is *partial* on `exclusive: true`, which marks non-dry `SCHEDULE`/`CATCH_UP` rows that execute. With a plain unique index, a `SKIPPED{LOCKED}` row (T-15-03), a dry run followed by a real run in the same minute, or a reaped run's retake would collide.
5. **An occurrence is "taken" once a scheduled row covers it or a later occurrence.** The check is `scheduledFor >=`, so the tick never goes back in time. Moving `jobs.dailyTime` from 06:00 to 07:00 therefore does not resurrect yesterday's 07:00 as a late run (tested in T-15-05). `SKIPPED{LOCKED}` rows and runs reaped after their lease expired do not count.
6. **Club iteration.** The tick iterates `ACTIVE`, `ONBOARDING` and `SUSPENDED` clubs (not templates). A `SUSPENDED` club records `SKIPPED{CLUB_INACTIVE}` (R-15-03 plus A18e).
7. **`JobFailed` on a missed window** carries `status: "SKIPPED"` (the `JobRun` status). It is the only `SKIPPED` that emits `JobFailed`. Counter keys may not contain dots, because they travel as Mongo map keys in the `SchedulerRun` payload; the recorder throws. E5-T05 P9 must name its counters without dots, e.g. `ttlPendingSeatHolds` instead of `ttlPending.seat_holds`. Parameter snapshots and counters are stored in `job_runs` as key/value lists for the same reason.
8. **`Week` marks.** `Week.openedAt`/`openingNotifiedAt` are added as trailing record components with a legacy constructor (the same pattern as `ClassSession.finishedAt`). The two `Week` copy sites preserve them. `ClassSession.finishedAt` and `risk{exempt, notifiedBookingIds[], adminNotifiedAt, lowAlertSentAt}` already existed (E4-T01); I verified them in `E5PersistenceIT`. `Booking`/`TrainingBooking` carry `reminderSentAt`. `idempotency_records` is reused, not redeclared.
9. **N-42.**
   - It lives in `clubs.common.application`: `identity` already depends on `clubs.messaging`, so a messaging consumer that reads admins would create a cycle (ArchitectureTest).
   - Variables follow the catalog (`job_name`, `error_count`; S15 §8 also writes `date`).
   - Delivery is APP + EMAIL; `SystemEmailRenderer` now allows N-42.
   - `job_name` resolves `jobs.name.<JobName>` in the recipient's locale.
   - Once per job and local day, through deterministic notification ids.
10. **No Sentry integration exists in the code base.** Item and job failures are logged at ERROR with `jobRunId`, `job`, `clubId` and `traceId`.
11. **Test clock and test job.** `POST /api/v1/test/clock {instant | advanceSeconds}` exists only under `test`/`local`, needs no tenant and is hidden from OpenAPI. `ClockConfiguration` serves an `OffsetClock` (system time + offset) under those profiles and `Clock.systemUTC()` otherwise. `MockClock` now extends `MutableClock`. `TEST_NOOP` is a `JobName` value (task 7k) but its bean exists only under `test`. It supplies its own definition (not in the catalog), is never listed, and the wire `job` enum lists only the 10 catalog names.
12. **Impersonation rules.**
    - Member routes carry `@AllowsImpersonation`; every other E5 route answers `IMPERSONATION_DENIED` (403). The platform routes may answer `FORBIDDEN` first because their `@PreAuthorize` rejects `imp`.
    - `POST /waitlist-entries/{id}/cancellation` accepts `ADMIN` directly (S08 §6 «MEMBER (pròpia) · ADMIN»).
    - `POST /bookings/{id}/cancellation` accepts `INSTRUCTOR`; the `bookings.instructorLastMinuteNotice` check is E5-T02.
    - `DELETE /seat-holds/{id}` has no resource guard (204 "also when already gone").
13. **Calendar route.** `GET /bookings/{id}/calendar.ics` is a public host-resolved route (`permitAll` plus TenantFilter public) with `security: []` in OpenAPI. The stub checks that `token` is present and the booking exists; signature verification is E5-T02.
14. **Wire choices.**
    - `*Local` fields are strings (`YYYY-MM-DDTHH:mm` or `HH:mm`).
    - The `impersonation` object of `MeHome`/`TrainingBooking` is `{actorName}` (S08/S09 only show `null`).
    - `displayState` uses codes (`CONFIRMED · DONE · NO_SHOW · CANCELLED · CANCELLED_LATE · CANCELLED_BY_CLUB · PAYMENT_PENDING`) for the Catalan labels of S08 §6.
    - `WaitlistEntry` carries `classSession` for screen 07.
    - Fixtures omit null fields because the round trip uses `NON_NULL`; the spec examples' `null`s are therefore represented by absence.
15. **Event payloads.** They follow S08/S09/S15 §7 plus Annex A, as the task says. `WeekOpened`, `ClassAtRisk` and `ClassAutoCancelled` use the extended S15 §13 payloads. `ClassBelowMinimum` uses the catalog's `classId, countedDogs, minDogs` (S15 writes `classSessionId`). `WaitlistExpired` adds `classId` (S15 §7). `SchedulerEvent` lives in `shared.domain.events` so the platform framework and the owning contexts' jobs can emit it (cross-context access only through `application`/shared).
16. **`/ring-blocks` is untouched.** `GET /ring-blocks` already declares `x-filterable: ringId, kind, reason, state, from, to` (asserted again in `E5ContractIT`).
17. **Handoff to E5-T04.** `clubs/catalogs/persistence/MongoUsageCounter` still reads the provisional `status` field on `training_bookings` (E2-T03). The real field is `state` (ACTIVE); E5-T04 should adapt it when training bookings become real.

### Questions / catalog proposals
1. **Schema name for S15 form A** (non-blocking). The task asks for `RiskReview`; that name belongs to the S14 dashboard card. I published `RiskReviewForm`. Proposal: when E5-T05 wires the dashboard to `GET /risk-review`, the organizer either keeps `RiskReviewForm` or renames the dashboard card, coordinated with the web.
2. **Error catalog proposals** (rule 0 ships meanwhile, 422): `OVERRIDE_NOT_ALLOWED` → §1 **403** (S09 writes 403), `JOB_UNKNOWN` → §1 **404** (S15 writes 404), `SLOT_NOT_ON_GRID` → §1 **400** (S09 writes 400).
3. **Audit action** `JOB_TRIGGERED` (S15 R-15-09) was added to `AuditAction`/`AuditActionName` with its `@AuditCovers` test (`JobFrameworkIT.T_15_08_…`) and backticked in S14 R-14-09, as that row instructs. The catalog of S14 should keep it.
4. **Event payload proposals** (implemented as S15/S08 write them): `WeekOpened{openedWeekKey, isoWeekStart, currentWeekKey, opensAt, notified}` (catalog: `weekStart`), `ClassAtRisk{…, newBookingIds[], notifyAdmins}`, `ClassAutoCancelled{…, affected[], waitlistIds[], adminText}`, `ReminderDue{…, dogId, startsAt}`, `SchedulerRun{…, runId, scheduledFor, trigger, status, counters, errorCount}`, `WaitlistExpired{entryId, classId}`, `BookingCreated{…, waitlistEntryId?}`. Open question: `ClassBelowMinimum` `classId` (catalog) vs `classSessionId` (S15 R-15-12b); I kept the catalog's.
5. **N-42 `date` variable** (S15 §8) is not in the catalog row; not implemented. No other new parameter, event, notification or error code was introduced.

### Round 2 (2026-09-24, executor claude-opus-5-5)

This round fixes the five points of the Organizer verification exactly; nothing else changed. E5-T02…T06 landed after round 1, so the job framework already had the E5-T05 processes and `JobAdminService`; they are untouched. No git write command was run.

**Point 1 — evidence sanitized, final run is `clean verify`.**
- `01-verify.log` … `08-test-summary.log` were already tracked (`git ls-files roadmap/evidence/E5-T01`), and `.gitignore:25` un-ignores `roadmap/evidence/E5-*/*.log`.
- A scan of the logs found one unsanitized value: the signup honeypot's `ipHash=` (a base64url hash of the test IP, 25 lines each in `01`/`02-verify.log`). `sanitize.py` now truncates it (`ipHash=XtaS…[truncated]`) and has been re-run on every log, including the new ones.
- Other scans: no `eyJ…` tokens, no hex ids of 32+ chars, no e-mails outside `@example.test`. `password=n…` is the driver's `password=null`.

**Point 2 — conditional final writes (R-15-04, R-15-06).**
- The final write is the new `JobRunRepository.finish(JobRun)`: `findAndReplace` on `{_id, clubId, status: RUNNING, holder}`, with no upsert. It returns `false` when another writer closed the row first.
- `JobRunner.complete()` calls `finish` inside the transaction and publishes `SchedulerRun`/`JobFailed` only when it returned `true`. A dry run calls `finish` only.
- `reap()` is split in two. The loop reads the rows, and `boolean reap(clubId, run)` (package-private, used by the stale-read test) does the conditional close and logs only when it closed the row.
- When `complete()` finds its row already closed (a slow holder whose lease was reaped), `run()` logs a WARN, returns the stored `FAILED` row and records no metrics.

**Point 3 — a reaped run frees its slot.** `JobRun.finished(..., expired = true)` now writes `exclusive = exclusive && !expired`. The reaped row leaves the partial unique index `job_run_occurrence`, so the retake inserts even when it has the same trigger. `T_15_03` no longer asserts the silent empty result. Its last part now turns the first claim into a dead `RUNNING` row: the tick reaps it (`FAILED`, `leaseExpired`, `exclusive = false`), the retake of the same occurrence and trigger (`SCHEDULE`) runs `SUCCEEDED`, and the lease is released. The duplicate insert is still asserted as `DuplicateKeyException`, and a true race loss is still covered by `JobFrameworkTwoInstancesIT`.

**Tests added to `JobFrameworkIT` (T-15-31).** `TestNoopJob` (profile `test` only) gained a one-shot `duringNextApply(Runnable)` hook, which runs while the run is `RUNNING`.
- `T_15_31_aRunThatFinishesBetweenTheReapersReadAndWriteStaysSucceeded`: the hook captures the `RUNNING` row, as the reaper's read would. After the run finishes and releases its lease, `runner.reap(CLUB, staleRow)` returns `false`. The row stays `SUCCEEDED`/`exclusive`/not expired, with no `JobFailed` and a single `SchedulerRun`.
- `T_15_31_aSlowCatchUpHolderReapedMidRunDoesNotOverwriteAndIsRetakenAsCatchUp`:
  - The 06:00 occurrence runs at 04:03Z as `CATCH_UP`, with its lease until 04:08.
  - Inside its `apply`, another thread ticks at 04:09. It reaps the run (`FAILED`, `exclusive = false`) and retakes it as `CATCH_UP`, which ends `SUCCEEDED`.
  - The slow holder then finishes. Its completion changes nothing: the stored row equals the returned `FAILED` row.
  - `applied = 2`, one `JobFailed` (runId = the slow run), and `SchedulerRun` only for the slow run (from the reaper) and the retake.
  - The next tick at 04:10 is `NOT_DUE`, and no lease is left.
- Why each test would fail without its fix (reasoned from the code; I did not run the tests against the old code): without the fix for point 3, the retake's insert hits `DuplicateKey`, the hook's `orElseThrow` throws, and the assertions fail. Without the fix for point 2, the slow holder overwrites the row with `SUCCEEDED`.
- The existing `T_15_31_aRunWhoseLeaseExpiredIsFailedAndTakenAgainAsCatchUp` now also asserts `exclusive = false`.

**Point 4 — ArchUnit (R-15-22).** `ArchitectureRules.TIME_FROM_CLOCK` is now `CompositeArchRule.of(NOW_CALLS).and(SYSTEM_CLOCKS)`, and `ArchitectureTest.E5_T01_timeComesFromTheClock` uses it.
- `NOW_CALLS` is the round-1 rule; the Clock/ClubClock beans are still the only callers allowed.
- `SYSTEM_CLOCKS` forbids `Clock.systemUTC()`, `Clock.systemDefaultZone()`, `Clock.system(ZoneId)` (added for completeness) and the no-arg `new Date()` in every class except `configuration.ClockConfiguration`.
- The new `ClockRuleTest.T_15_07_systemClocksAndNewDateOutsideTheClockConfigurationAreViolations` proves all four are reported and `Date.from(instant)` is not.
- The rule found **no production violation**. The only `Clock.system*` calls are in `ClockConfiguration`; the `java.util.Date` uses are `Date.from(...)`, in `ExportWorkRepository` and `PlayoffPlanner`.

**Point 5.** The two `System.out.println` lines are removed from `JobFrameworkIT.T_15_06`. The round-1 `06-jobrun-documents.log` stays as the historical evidence it printed.

#### Files changed (Round 2)
- Main:
  - `platform/persistence/jobs/JobRunRepository.java` (`finish`)
  - `platform/persistence/jobs/JobRun.java` (`finished` frees the claim when expired)
  - `platform/application/jobs/JobRunner.java` (conditional `complete`/`reap`)
  - `platform/application/jobs/TestNoopJob.java` (test-profile hook)
- Tests: `platform/application/jobs/JobFrameworkIT.java`, `arch/ArchitectureRules.java`, `arch/ArchitectureTest.java`, `arch/ClockRuleTest.java`.
- Evidence: `roadmap/evidence/E5-T01/sanitize.py` (`ipHash`); the re-sanitized `01`/`02-verify.log`; new `09-clean-verify.log`, `10-test-summary.log`, `11-snapshot-r2-first.log`, `12-snapshot-r2-second.log`.
- `CHANGELOG.md` (Unreleased → Changed).
- No API change: `docs/openapi/*` is identical to HEAD.

**Not mine:** while this session's verify was running (07:40–07:41 local), someone else modified `docs/DECISIONS_PENDENTS.md`, `docs/specs/00-transversal/CATALEG_NOTIFICACIONS.md`, `roadmap/tasks/{E4-T05,E5-T09,E7-T02}.md` and `roadmap/tools/check.py`. I did not touch them, and they will be in the same publish commit.

#### Evidence (Round 2)
**1. Full verification.** `./mvnw -q clean verify > roadmap/evidence/E5-T01/09-clean-verify.log 2>&1` exited **0** (5 012 lines, sanitized).
- Totals from the Surefire/Failsafe XML: `python3 roadmap/evidence/E5-T01/test-summary.py > roadmap/evidence/E5-T01/10-test-summary.log`, exit 0.
  - `unit/contract (surefire): tests=454 failures=0 errors=0 skipped=0`
  - `integration (failsafe): tests=716 failures=0 errors=0 skipped=0`
- Per class: `JobFrameworkIT tests=10`, `JobFrameworkTwoInstancesIT tests=1`, `ClockRuleTest tests=3`, `ArchitectureTest tests=6`, `E5ContractIT tests=38`, `E5PersistenceIT tests=4`, `E5ResponseContractTest tests=4`, `ErrorCatalogContractTest tests=3`, `EventCatalogContractTest tests=2`, `AuditContractTest tests=1`, `MessageParityTest tests=1`, `OpenApiRequiredContractTest tests=3`, all with 0 failures/errors/skips.
- The JaCoCo check passed inside `verify`. From `target/site/jacoco/jacoco.csv`: `platform.application.jobs` 95.5 % lines / 84.7 % branches; `platform.application` 99.3 / 93.9; `platform.api` 98.7 / 86.8.

Last 40 lines of `09-clean-verify.log` (a quiet build prints only the last IT's Spring noise and the usual container-shutdown monitor exception):
```text
2026-09-24T07:47:11.605+02:00  INFO 53862 --- [agilityhub-core-api] [           main] t.c.s.AnnotationConfigContextLoaderUtils : Could not detect default configuration classes for test class [com.agilityhub.core.migration.PlayoffMigrationIT]: PlayoffMigrationIT does not declare any static, non-private, non-final, nested classes annotated with @Configuration.
2026-09-24T07:47:11.615+02:00  INFO 53862 --- [agilityhub-core-api] [           main] .b.t.c.SpringBootTestContextBootstrapper : Found @SpringBootConfiguration com.agilityhub.core.CoreApplication for test class com.agilityhub.core.migration.PlayoffMigrationIT

  .   ____          _            __ _ _
 /\\ / ___'_ __ _ _(_)_ __  __ _ \ \ \ \
( ( )\___ | '_ | '_| | '_ \/ _` | \ \ \ \
 \\/  ___)| |_)| | | | | || (_| |  ) ) ) )
  '  |____| .__|_| |_|_| |_\__, | / / / /
 =========|_|==============|___/=/_/_/_/

 :: Spring Boot ::               (v3.5.16)

2026-09-24T07:47:11.627+02:00  INFO 53862 --- [agilityhub-core-api] [           main] c.a.core.migration.PlayoffMigrationIT    : Starting PlayoffMigrationIT using Java 21.0.12.1 with PID 53862 (started by jordib in /Users/jordib/dev/agilityhub/agilityhub-core-api)
2026-09-24T07:47:11.627+02:00  INFO 53862 --- [agilityhub-core-api] [           main] c.a.core.migration.PlayoffMigrationIT    : The following 1 profile is active: "test"
2026-09-24T07:47:11.757+02:00  INFO 53862 --- [agilityhub-core-api] [           main] .s.d.r.c.RepositoryConfigurationDelegate : Bootstrapping Spring Data MongoDB repositories in DEFAULT mode.
2026-09-24T07:47:11.773+02:00  INFO 53862 --- [agilityhub-core-api] [           main] .s.d.r.c.RepositoryConfigurationDelegate : Finished Spring Data repository scanning in 15 ms. Found 0 MongoDB repository interfaces.
2026-09-24T07:47:11.798+02:00  INFO 53862 --- [agilityhub-core-api] [           main] org.mongodb.driver.client                : MongoClient with metadata {"driver": {"name": "mongo-java-driver|sync|spring-boot", "version": "5.5.2"}, "os": {"type": "Darwin", "name": "Mac OS X", "architecture": "aarch64", "version": "26.4.1"}, "platform": "Java/Homebrew/21.0.12.1"} created with settings MongoClientSettings{readPreference=primary, writeConcern=WriteConcern{w=null, wTimeout=null ms, journal=null}, retryWrites=true, retryReads=true, readConcern=ReadConcern{level=null}, credential=null, transportSettings=null, commandListeners=[io.micrometer.core.instrument.binder.mongodb.MongoMetricsCommandListener@4cc32bc5], codecRegistry=ProvidersCodecRegistry{codecProviders=[ValueCodecProvider{}, BsonValueCodecProvider{}, DBRefCodecProvider{}, DBObjectCodecProvider{}, DocumentCodecProvider{}, CollectionCodecProvider{}, IterableCodecProvider{}, MapCodecProvider{}, GeoJsonCodecProvider{}, GridFSFileCodecProvider{}, Jsr310CodecProvider{}, JsonObjectCodecProvider{}, BsonCodecProvider{}, EnumCodecProvider{}, com.mongodb.client.model.mql.ExpressionCodecProvider@57715987, com.mongodb.Jep395RecordCodecProvider@80d9116, com.mongodb.KotlinCodecProvider@431e4e26]}, loggerSettings=LoggerSettings{maxDocumentLength=1000}, clusterSettings={hosts=[localhost:59014], srvServiceName=mongodb, mode=SINGLE, requiredClusterType=UNKNOWN, requiredReplicaSetName='null', serverSelector='null', clusterListeners='[]', serverSelectionTimeout='30000 ms', localThreshold='15 ms'}, socketSettings=SocketSettings{connectTimeoutMS=10000, readTimeoutMS=0, receiveBufferSize=0, proxySettings=ProxySettings{host=null, port=null, username=null, password=null}}, heartbeatSocketSettings=SocketSettings{connectTimeoutMS=10000, readTimeoutMS=10000, receiveBufferSize=0, proxySettings=ProxySettings{host=null, port=null, username=null, password=null}}, connectionPoolSettings=ConnectionPoolSettings{maxSize=100, minSize=0, maxWaitTimeMS=120000, maxConnectionLifeTimeMS=0, maxConnectionIdleTimeMS=0, maintenanceInitialDelayMS=0, maintenanceFrequencyMS=60000, connectionPoolListeners=[io.micrometer.core.instrument.binder.mongodb.MongoMetricsConnectionPoolListener@5a145b60], maxConnecting=2}, serverSettings=ServerSettings{heartbeatFrequencyMS=10000, minHeartbeatFrequencyMS=500, serverMonitoringMode=AUTO, serverListeners='[]', serverMonitorListeners='[]'}, sslSettings=SslSettings{enabled=false, invalidHostNameAllowed=false, context=null}, applicationName='null', compressorList=[], uuidRepresentation=JAVA_LEGACY, serverApi=null, autoEncryptionSettings=null, dnsClient=null, inetAddressResolver=null, contextProvider=null, timeoutMS=null}
2026-09-24T07:47:11.800+02:00  INFO 53862 --- [agilityhub-core-api] [localhost:59014] org.mongodb.driver.cluster               : Monitor thread successfully connected to server with description ServerDescription{address=localhost:59014, type=REPLICA_SET_PRIMARY, cryptd=false, state=CONNECTED, ok=true, minWireVersion=0, maxWireVersion=21, maxDocumentSize=16777216, logicalSessionTimeoutMinutes=30, roundTripTimeNanos=1574334, minRoundTripTimeNanos=0, setName='docker-rs', canonicalAddress=8b15eea40be4:27017, hosts=[8b15eea40be4:27017], passives=[], arbiters=[], primary='8b15eea40be4:27017', tagSet=TagSet{[]}, electionId=7fff...[truncated], setVersion=1, topologyVersion=TopologyVersion{processId=6ab4...[truncated], counter=6}, lastWriteDate=Thu Sep 24 07:47:11 CEST 2026, lastUpdateTimeNanos=91980674411666}
2026-09-24T07:47:12.270+02:00  INFO 53862 --- [agilityhub-core-api] [           main] c.a.core.migration.PlayoffMigrationIT    : Started PlayoffMigrationIT in 0.647 seconds (process running for 172.458)
2026-09-24T07:47:20.856+02:00  INFO 53862 --- [agilityhub-core-api] [localhost:59240] org.mongodb.driver.cluster               : Exception in monitor thread while connecting to server localhost:59240

com.mongodb.MongoSocketOpenException: Exception opening socket
	at com.mongodb.internal.connection.SocketStream.lambda$open$0(SocketStream.java:85) ~[mongodb-driver-core-5.5.2.jar:na]
	at java.base/java.util.Optional.orElseThrow(Optional.java:403) ~[na:na]
	at com.mongodb.internal.connection.SocketStream.open(SocketStream.java:85) ~[mongodb-driver-core-5.5.2.jar:na]
	at com.mongodb.internal.connection.InternalStreamConnection.open(InternalStreamConnection.java:233) ~[mongodb-driver-core-5.5.2.jar:na]
	at com.mongodb.internal.connection.DefaultServerMonitor$ServerMonitor.lookupServerDescription(DefaultServerMonitor.java:219) ~[mongodb-driver-core-5.5.2.jar:na]
	at com.mongodb.internal.connection.DefaultServerMonitor$ServerMonitor.run(DefaultServerMonitor.java:176) ~[mongodb-driver-core-5.5.2.jar:na]
Caused by: java.net.ConnectException: Connection refused
	at java.base/sun.nio.ch.Net.pollConnect(Native Method) ~[na:na]
	at java.base/sun.nio.ch.Net.pollConnectNow(Net.java:694) ~[na:na]
	at java.base/sun.nio.ch.NioSocketImpl.timedFinishConnect(NioSocketImpl.java:542) ~[na:na]
	at java.base/sun.nio.ch.NioSocketImpl.connect(NioSocketImpl.java:592) ~[na:na]
	at java.base/java.net.SocksSocketImpl.connect(SocksSocketImpl.java:327) ~[na:na]
	at java.base/java.net.Socket.connect(Socket.java:751) ~[na:na]
	at com.mongodb.internal.connection.SocketStreamHelper.initialize(SocketStreamHelper.java:76) ~[mongodb-driver-core-5.5.2.jar:na]
	at com.mongodb.internal.connection.SocketStream.initializeSocket(SocketStream.java:104) ~[mongodb-driver-core-5.5.2.jar:na]
	at com.mongodb.internal.connection.SocketStream.open(SocketStream.java:79) ~[mongodb-driver-core-5.5.2.jar:na]
	... 3 common frames omitted
```

**2. `bin/openapi-snapshot` twice + `cmp`**, each exit 0:
```sh
bin/openapi-snapshot > roadmap/evidence/E5-T01/11-snapshot-r2-first.log 2>&1
cp docs/openapi/openapi.json target/e5-t01-r2-openapi-first.json
bin/openapi-snapshot > roadmap/evidence/E5-T01/12-snapshot-r2-second.log 2>&1
cmp target/e5-t01-r2-openapi-first.json docs/openapi/openapi.json   # no output → byte-identical
git diff --stat -- docs/openapi/                                   # no output → unchanged against HEAD
git diff --check                                                   # no output
```
Both logs end with `Updated docs/openapi/openapi.json`. The count of operations added by E5-T01 is unchanged: 32 (round 1, `07-contract-summary.log`).

**Earlier attempt in this round:** a focused run (`-Dtest=ClockRuleTest,ArchitectureTest -Dit.test=JobFrameworkIT,JobFrameworkTwoInstancesIT`, output in `target/`, not kept) passed 3 + 6 + 10 + 1 tests. The first try of that command was refused by the sandbox (it contained `$?`) and did not run.

#### Rules and tests (Round 2)
R-15-04, R-15-06 (conditional completion/reaping, freed claim), R-15-22 (system clocks). T-15-31 (two new `JobFrameworkIT` tests + the updated reaping assertions), T-15-03 (updated `T_15_03`), T-15-07 (new `ClockRuleTest` case + extended `ArchitectureTest` rule).

#### Assumptions (Round 2)
1. A slow holder that finds its row already reaped returns the stored `FAILED` row to its caller. A manual trigger therefore reports `FAILED`, and the audit records `FAILED`. Its item effects were already committed item by item (R-15-10), and the retake runs as `CATCH_UP`. Idempotent `apply` is already a requirement of every process (S15 §5).
2. The metrics of such a run are not recorded, because the reaper's `FAILED` is the outcome of record.

#### Questions / catalog proposals (Round 2)
None. No parameter, event, notification or error code was added.

## Organizer verification
Result: changes_requested · 2026-09-24 · organizer (Claude)

The contract (32 guarded operations, 96 schemas) and the job framework match the task. `JOB_TRIGGERED` with `@AuditCovers` and the S14 row were done as the row instructed; the organizer copied that S14 row to the source documentation so `sync-docs` keeps it. CI of `4534998` red (log not kept); later runs green (`1ebc62d`, `924303e`). The independent review `roadmap/reviews/E5-T01-20260924-0036-claude.md` found two real defects, both confirmed in `JobRunner.reap()`/`complete()` (unconditional `runs.replace`; the reaped row keeps `exclusive = true`).

Round 2 — fix exactly these points:
1. Evidence: `.gitignore` now un-ignores `roadmap/evidence/E5-*/*.log` (organizer, 2026-09-24). Check that `01-verify.log` … `08-test-summary.log` are sanitized, and make the final verification a `./mvnw -q clean verify` (a non-clean run appends to `jacoco.exec`; AGENTS.md).
2. R-15-04/R-15-06: the reaper and `complete()` update only `{_id, status: RUNNING, holder}` (conditional update); `SchedulerRun`/`JobFailed` are published only when exactly one document changed. Tests: a run that finishes between the reaper's read and its write stays `SUCCEEDED` with no `JobFailed`; a slow holder whose lease was reaped does not overwrite the `FAILED` row.
3. A reaped run frees its slot in the partial unique index (`exclusive = false` when reaping). Test (T-15-31): a `CATCH_UP` run that dies is retaken on the next tick as `CATCH_UP`; update `T_15_03`, which currently asserts the silent empty result.
4. `ArchitectureRules` (R-15-22): also forbid `Clock.systemUTC()`, `Clock.systemDefaultZone()` and `new Date()` outside the clock configuration.
5. Remove the `System.out.println` evidence lines from `JobFrameworkIT`.

`CatchUpWindow` as an enum (assumption 3) is accepted. Rulings on your MESSAGES proposals: organizer entry of 2026-09-24. Review #4–#7 go to **E5-T08**: do not do them here.
