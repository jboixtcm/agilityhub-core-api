---
id: E5-T01
title: Contract S08 + S09 + S15 (bookings, seat holds, waitlist, training slots and bookings, ring blocks, jobs) + scheduled-process framework (JobCatalog, Job plan/apply, SchedulerTick, job_locks, job_runs, catch-up, metrics, N-42, Clock ArchUnit rule) — OpenAPI forms, Mongo documents + indexes, error status audit, 501 stubs
stage: E5
repo: agilityhub-core-api
thread: A
order: 10
status: ready
depends_on: [E4-T03, E4-T04, E4-T05]
branch:
pr:
updated: 2026-09-19
---

# E5-T01 · Contract S08 + S09 + S15 + scheduled-process framework

## Objective
Work packages S08 WP-08-A, S09 WP-09-A and S15 WP-15-A. Every route of S08 §6, S09 §6 and S15 §6 exists in `docs/openapi/openapi.json` with typed schemas, roles, modules, idempotency headers and canonical error statuses, answering `501 NOT_IMPLEMENTED` behind the security/tenant/module guards, so the web builds mocks-first while E5-T02…T05 implement. In the same task the **process framework** lands: `JobCatalog`, the `Job { plan / apply }` contract, `SchedulerTick`, occurrence and catch-up arithmetic in club-local time, `job_locks`, `job_runs`, dry run, metrics, `JobFailed` → N-42 and the ArchUnit rule that forbids `Instant.now()` outside the `Clock` bean. The ten processes themselves are E5-T05 (P1, P2, P6, P7, P9), E6 (P3, P8), E7 (P4) and E8 (P5, P10): here only a **fictitious test job** exercises the framework. Same pattern as E4-T01 (`E4ContractConfiguration`, `E4ContractIT`, `E4ResponseContractTest`, fixtures `e4-*.json`).

## Spec references
S08 §3 (every collection and field), §5 (three state machines), §6 (every row, the four JSON forms, the transaction skeletons, the error-code list), §7, §8, §9, §12 WP-08-A, §13 · S09 §3, §5, §6 (every row + the four JSON forms + the `details` shapes), §7, §9, §12 WP-09-A, §13 · S15 R-15-01 (catalog table: `JobName`, cadence, parameter, module, stage, catch-up window), R-15-02 (occurrences and DST), R-15-03, R-15-04, R-15-05, R-15-06, R-15-07, R-15-08, R-15-09, R-15-10, R-15-22, §3 (`JobRun`, `JobLock`, the fields this vertical adds to other aggregates), §5, §6, §9, §12 WP-15-A, §13 · tests **T-15-01, T-15-02, T-15-03, T-15-04, T-15-05, T-15-06, T-15-07, T-15-10, T-15-29, T-15-31** (framework, with the test job) and the contract halves of **T-08-47** and **T-09-24** · `docs/specs/00-transversal/CATALEG_ERRORS.md` rows S08/S09/S15 + §1 + §3 rule 0 · `CATALEG_ESDEVENIMENTS.md` (booking, waitlist, training, ring-block, scheduler rows + Annex A) · `CATALEG_NOTIFICACIONS.md` N-04, N-05, N-06, N-07, N-15, N-36, N-40, N-42, N-46, N-47 · `CATALEG_PARAMETRES.md` blocks «Classes i reserves», «Llista d'espera», «Entrenaments lliures» and the `jobs.*` family · `MATRIU_PERMISOS.md` rows «Reserves de classe `/bookings`», «Llista d'espera», «Entrenaments (slots i reserves)», «Processos programats (S15)» · `CONVENCIONS_API.md` §2, §4, §5, §6, §7 · S14 R-14-09 (audit actions).

## Context to load
`docs/specs/S08-reserves-de-classes-i-llista-espera.md`, `docs/specs/S09-entrenaments-lliures.md`, `docs/specs/S15-processos-programats.md` (all three complete) · the catalogs above · `docs/MODEL_DADES_PLATAFORMA.md` §0 glossary and Annex B rows `Booking · SeatHold · WaitlistEntry`, `TrainingBooking · Attendance`, `DomainEventRecord · JobRun · job_locks`, plus §7 (mandatory indexes) · `roadmap/tasks/E4-T01.md` and `src/main/java/com/agilityhub/core/configuration/E4ContractConfiguration.java`, `src/test/java/com/agilityhub/core/configuration/E4ContractIT.java`, `E4ResponseContractTest.java`, `OpenApiRequiredContractTest.java`, `ErrorCatalogContractTest.java`, `EventCatalogContractTest.java`, `src/test/resources/fixtures/contracts/e4-*.json` (copy the approach) · `src/main/java/com/agilityhub/core/clubs/bookings/**` and `clubs/training/**` (empty scaffolds: only `package-info.java`) · `clubs/scheduling/application/ports/*` (E4-T03: `ClassBookingsPort`, `TrainingOccupancyPort`, `TrainingConflictPort`, `SchedulingPortDefaults`) and `clubs/dashboard/application/ports/*` (E3-T04: `BookingActivity`, `TrainingBookingsQuery`, `ClassOccupancyQuery`, `RiskEvaluator`) — the interfaces E5-T02/T03/T04 implement · `platform/application/{ClubConfig,ClubConfigService,Module,ModuleGuard,RequiresModule}.java`, `platform/application/audit/*`, `platform/persistence/**` · `shared/application/{ClubClock,DefaultClubClock,EventPublisher,OutboxDispatcher,DomainEventHandler,TenantContext,CoreCommand}.java`, `shared/application/lists/*`, `shared/application/contract/*` · `clubs/scheduling/application/SchedulingTransactions.java` and `identity/application/IdentityTransactions.java` (the retry-on-`WriteConflict` pattern) · `docs/playbooks/new-endpoint.md`, `new-entity.md`, `new-scheduler.md`, `checklist.md` · this file.

## Steps
1. **S08 controllers + DTOs** (`clubs/bookings/api`, springdoc annotations, bodies throw `UnsupportedOperationException` → `501`, guards real): `GET /me/home?dogId=` (MEMBER incl. IMPERSONATED) · `GET /me/bookable-classes?dogId=` (MEMBER) · `POST /seat-holds {classSessionId, dogId, waitlistEntryId?}` (MEMBER, 201) · `DELETE /seat-holds/{id}` (MEMBER owner, 204, also when already gone) · `POST /bookings {seatHoldId, swapBookingId?}` (MEMBER, `Idempotency-Key`, 201) · `GET /me/bookings?dogId=&state=&from=&to=` (MEMBER) · `GET /bookings/{id}` (MEMBER own/group, INSTRUCTOR, ADMIN) · `GET /bookings/{id}/calendar.ics?token=` (signed token, `text/calendar`, no JWT) · `POST /bookings/{id}/cancellation {message?}` (MEMBER own/group, INSTRUCTOR when `bookings.instructorLastMinuteNotice`) · `GET /bookings` (ADMIN, INSTRUCTOR; universal list, `x-filterable: state, dogId, memberId, classSessionId, bookingWeekKey, origin, classStartsAt`, `x-sortable: classStartsAt, bookedAt`; MEMBER → 403) · `POST /waitlist-entries {classSessionId, dogId}` (MEMBER, `WAITLIST`, 201) · `GET /waitlist-entries/{id}` · `POST /waitlist-entries/{id}/cancellation` (MEMBER own, ADMIN) · `POST /waitlist-entries/{id}/claim {seatHoldId, swapBookingId?}` (MEMBER, `Idempotency-Key`, 201) · `GET /class-sessions/{id}/bookings` and `GET /class-sessions/{id}/waitlist-entries` (INSTRUCTOR, ADMIN). Impersonation: every MEMBER route accepts the impersonation token (R-08-19); ADMIN without impersonation on a MEMBER route → `403` (`FORBIDDEN`); the two admin/instructor read routes reject impersonation tokens (`IMPERSONATION_DENIED`), like E4-T01.
2. **S09 controllers + DTOs** (`clubs/training/api`; `@RequiresModule(Module.FREE_TRAINING)` on everything except `/ring-blocks*`): `GET /training-slots?from=&to=&dogId=&ringId=` (MEMBER, INSTRUCTOR, ADMIN) · `GET /me/training-summary?dogId=&date=` (MEMBER) · `GET /me/training-bookings?from=&to=&state=` (MEMBER) · `POST /training-bookings {dogId, startsAt, ringId?, override?{limit, reason}}` (MEMBER incl. IMPERSONATED, `Idempotency-Key`, 201) · `GET /training-bookings/{id}` (MEMBER own, INSTRUCTOR, ADMIN) · `POST /training-bookings/{id}/cancellation {reason?}` (MEMBER own incl. IMPERSONATED) · `GET /training-bookings` + `GET /training-bookings/export?format=xlsx|pdf` (ADMIN RW, INSTRUCTOR R; universal list `x-filterable: date, ringId, memberId, dogId, state, origin`, `x-sortable: startsAt`, `x-exportable` with the `listKey = training-bookings` column labels). `/ring-blocks*` already exists from E4-T01/E4-T03: do **not** redeclare it; only add what S09 needs and E4 did not publish — the `MEMBER` redacted projection is already there; check and state in the report whether `GET /ring-blocks` declares `x-filterable: ringId, kind, reason, state, from, to`.
3. **S15 controllers + DTOs** (`platform/api` for `/platform/jobs*`, `clubs/common/api` for the club routes; the job framework itself is `platform`): `GET /jobs` (ADMIN) · `GET /jobs/{name}/runs` (ADMIN; universal list `x-filterable: status, scheduledFor, trigger, dryRun`, `x-sortable: scheduledFor, startedAt`) · `GET /jobs/{name}/runs/{runId}` (ADMIN) · `POST /jobs/{name}/trigger {dryRun}` (ADMIN, 200) · `PUT /jobs/{name}/switch {enabled}` (ADMIN, 200) · `GET /risk-review?date=` (ADMIN, INSTRUCTOR; form A of S15 §6) · `GET /platform/jobs/overview?clubId=&status=` (AGILITYHUB_ADMIN) · `POST /platform/clubs/{clubId}/jobs/{name}/trigger {dryRun}` (AGILITYHUB_ADMIN). Impersonation tokens → `403` on all of them (R-15-09). `{name}` is the **route id** of R-15-01 (`week-opening`, `risk-review`, `no-show-notices`, `reminders`, `expirations`, `waitlist-fifo`, `payment-timeouts`, `class-finishing`, `cleanup`, `billing-reminder`); an unknown name → `JOB_UNKNOWN`; a process whose module is off → `MODULE_DISABLED` (404) and its row absent from `GET /jobs`.
4. **Schemas** — the names below are the contract and each one's field list is the **complete** JSON example of the owning spec §6 (required by default, nullable marked; transcribe them, do not summarize): `MeHome` (+ `HomeDog`, `ReservationRow` with `type CLASS·CLASS_WAITLIST·TRAINING·ACTIVITY` and `state CONFIRMED·PAYMENT_PENDING·WAITLISTED·REGISTERED`) · `BookableClasses` (+ `BookableClass` with `state BOOKABLE·WAITLIST_OPEN·WAITLIST_FULL·FULL·WEEKLY_LIMIT_DONE·NOT_YET_OPEN·PACK_EMPTY·NOT_BOOKABLE`, `notBookableReason BLOCKED·INACTIVITY·LEAVING`, `week CURRENT·NEXT·LATER`, `PackCard.state ACTIVE·EXPIRING·EXPIRED·EMPTY`) · `SeatHoldResponse` (+ `LimitStatus {reached, unit, week, count, max, swappable[], notSelectable[{…, reason DONE·LATE_WINDOW}]}`, `serverNow`, `holdSeconds`) · `Booking` (`state PAYMENT_PENDING·ACTIVE·CANCELLED·CANCELLED_LATE·CANCELLED_BY_CLUB`, `origin APP·BACKOFFICE·INSTRUCTOR·SYSTEM`, `bookedBy`, `swapFromBookingId?`, `pack?`, `charge?`, `checkoutUrl?`, `calendarLinks{google, outlook, ics}`, `cancellation?`, the derived `displayState`) · `WaitlistEntry` (`state ACTIVE·NOTIFIED·CONSOLIDATED·EXPIRED·CANCELLED`, `position`, `confirmBy?`, `cancelReason?`) · `TrainingSlots` (+ `TrainingRing`, `TrainingDay`, `TrainingSlot`, `SlotCell {state FREE·BOOKED·BLOCKED, reason? CLASS·RING_BLOCK·TRAINING·OWN_TRAINING, occupants[]?, block?, classSession?}`) — document which fields only the INSTRUCTOR/ADMIN projection carries · `TrainingSummary` (`eligibleDogs[].rightSource LEVEL·MANUAL`, `counter`, `cancellableBookings[]`) · `TrainingBooking` (`state ACTIVE·CANCELLED·CANCELLED_BY_CLUB`, `origin APP·BACKOFFICE`, `slotId`, `cancellableUntil`, `counter`; `seatIndex` is **not** exposed) · `JobSummary {name, jobName, module?, enabled, schedule{kind DAILY·WEEKLY·MONTHLY·CONTINUOUS, localTime?, dayOfWeek?, dayOfMonth?}, nextScheduledForLocal?, lastRun?}` · `JobRun {runId, job, scheduledFor, scheduledForLocal, timeZone, trigger SCHEDULE·CATCH_UP·MANUAL, dryRun, status RUNNING·SUCCEEDED·PARTIAL·FAILED·SKIPPED, skipReason? DISABLED·MODULE_OFF·CLUB_INACTIVE·MISSED_WINDOW·LOCKED·NOT_DUE, startedAt, finishedAt?, durationMs?, effects{counters, items[{entityType, entityId, action, detail?}]}, errors[{entityId?, code, message, traceId}], actorAccountId?, parametersSnapshot}` · `JobTriggerRequest {dryRun}`, `JobSwitchRequest/Response` · `RiskReview` = form A (`items[].status AUTO_CANCELLED·AT_RISK·WILL_CANCEL·WILL_REVIEW`, `dayLabel TODAY·TOMORROW·OTHER`) · `PlatformJobsOverview` (`jobs[].health OK·WARN·ALERT`).
5. **Error codes and status audit.** Every S08/S09/S15 code already exists in `ErrorCode` (E0-T04 loaded the catalog): the work is the audit against `CATALEG_ERRORS.md` §1 + §3 rule 0, **the catalog file wins over anything S08/S09/S15 §6 writes**. Explicit §1: `CLASS_FULL`, `SEAT_TAKEN`, `SEAT_HOLD_EXPIRED`, `SLOT_TAKEN`, `BOOKING_LIMIT_REACHED`, `WAITLIST_LIMIT`, `TRAINING_LIMIT_REACHED`, `JOB_ALREADY_RUNNING`, `STALE_VERSION`, `INVALID_STATE`, `IDEMPOTENCY_KEY_REUSED` **409** · `NOT_YET_OPEN`, `CLASS_NOT_BOOKABLE`, `LEVEL_NOT_ALLOWED`, `PACK_EMPTY`, `MEMBER_NOT_ACTIVE`, `BOOKING_BLOCKED`, `INACTIVITY_PERIOD`, `DOG_NOT_ALLOWED`, `SLOT_OUT_OF_WINDOW`, `CLUB_CLOSED`, `RING_NOT_RESERVABLE` **422** · `VALIDATION_ERROR`, `INVALID_FILTER`, `INVALID_TIME_RANGE` **400** · `MODULE_DISABLED`, `NOT_FOUND`, `DOG_NOT_ACCESSIBLE` **404**. By rule 0: `ALREADY_BOOKED`, `ALREADY_ON_WAITLIST` (prefix `ALREADY_`), `RING_BLOCK_CONFLICT`, `CLASS_CONFLICT` (suffix `_CONFLICT`) **409**; **everything else is 422** — `CLASS_NOT_FULL`, `WAITLIST_FULL`, `WAITLIST_NOT_NOTIFIED`, `WAITLIST_OFFER_EXPIRED`, `WAITLIST_ENTRY_NOT_LIVE`, `SWAP_NOT_ALLOWED`, `BOOKING_NOT_CANCELLABLE`, `WEEKLY_LIMIT_DONE`, `SLOT_NOT_ON_GRID` (S09 writes 400), `DOG_ALREADY_BOOKED` (S09 writes 409; rule 0 is literal — only the `ALREADY_` **prefix** counts), `TRAINING_CANCEL_TOO_LATE`, `OVERRIDE_NOT_ALLOWED` (S09 writes 403), `RING_HAS_BOOKINGS`, `RING_BLOCK_MANAGED_BY_ACTIVITY`, `JOB_UNKNOWN` (S15 writes 404), `NOT_DUE`. Fix every enum value that contradicts the catalog, list the changes in the report, and file a catalog **proposal** for the three surprising ones (`OVERRIDE_NOT_ALLOWED` → §1 403, `JOB_UNKNOWN` → §1 404, `SLOT_NOT_ON_GRID` → §1 400); until the catalog is amended the rule-0 status ships. `details` payloads the UI needs: `BOOKING_LIMIT_REACHED {unit, week, limit, current, swappable[], notSelectable[], nextBookableAt}`, `CLASS_FULL {heldOnly}`, `NOT_YET_OPEN {opensAt}`, `WAITLIST_LIMIT {scope: CLASS|DOG_WEEK}`, `INACTIVITY_PERIOD {from, to}`, `TRAINING_LIMIT_REACHED {limit, used, weekStart, weekEnd, cancellableBookings[]}`, `SLOT_TAKEN {ringId, startsAt, reason, freeRings[]}`, `SLOT_OUT_OF_WINDOW {from, to}`, `TRAINING_CANCEL_TOO_LATE {thresholdMinutes, minutesBefore}`, `RING_HAS_BOOKINGS {bookings[]}`. `error.<CODE>` must exist in the three `messages_*.properties`.
6. **Mongo documents + indexes** (`persistence` packages, `TenantEntity`, `clubId` everywhere, `createdAt/By`, `updatedAt/By`): `Booking` · `bookings` (S08 §3 field list) with `{clubId, classSessionId, dogId, state}`, `{clubId, dogId, bookingWeekKey, state}`, `{clubId, memberId, bookingWeekKey, state}`, `{clubId, state, classStartsAt}` (S15 P4/P7) · `SeatHold` · `seat_holds` **TTL on `expiresAt`** + unique `{clubId, classSessionId, dogId}` · `WaitlistEntry` · `waitlist_entries` with `{clubId, classSessionId, state}`, `{clubId, dogId, bookingWeekKey, state}`, `{clubId, state, confirmBy}` (P6) · `SeatLock` · `seat_locks` (`_id = classSessionId`, `clubId`, `version`) · `TrainingBooking` · `training_bookings` with the **partial unique** `{clubId, ringId, startsAt, seatIndex}` on `{state: "ACTIVE"}`, plus `{clubId, startsAt}`, `{clubId, dogId, weekStart, state}`, `{clubId, memberId, weekStart, state}`, `{clubId, state, startsAt}` · `JobRun` · `job_runs` **unique** `{clubId, job, scheduledFor, trigger}` + `{clubId, job, startedAt}` + `{finishedAt}` · `JobLock` · `job_locks` (`_id = "{clubId}:{job}"` or `"tick"`, `holder`, `acquiredAt`, TTL on `expiresAt`). New fields on existing aggregates (S15 §3): `Week.openedAt`, `Week.openingNotifiedAt` (E4-T02 document), `ClassSession.finishedAt` and `risk.lowAlertSentAt` (E4-T01 already persists `risk{exempt, notifiedBookingIds[], adminNotifiedAt, lowAlertSentAt}` — verify and say so), `Booking.reminderSentAt`, `TrainingBooking.reminderSentAt`. `idempotency_records` already exists (E0/E3): reuse it, do not redeclare.
7. **Process framework** (`platform/application/jobs`, `platform/persistence/jobs`; platform **never** imports `clubs.*` — jobs are discovered as `List<Job>` beans): (a) `JobName` enum + `JobCatalog` with one `JobDefinition {JobName name, String routeId, Cadence cadence, String localTimeParameter, Module module?, Duration catchUpWindow (null = unlimited), String switchParameter}` per R-15-01 row, all ten declared now even though eight have no implementation yet — `GET /jobs` lists only those with a registered `Job` bean plus their catalog row, and the report states which ones are missing; (b) `interface Job { JobName name(); List<JobItem> plan(JobContext ctx); Effect apply(JobContext ctx, JobItem item); }` with `JobContext {clubId, ZoneId zone, Instant scheduledFor, LocalDate localDate, boolean dryRun, ClubConfig config, JobRunRecorder recorder}`; (c) `JobOccurrences` (pure, no Spring): `occurrence(date, time, zone)` with Java semantics (gap → shifted forward, overlap → **first** occurrence), `lastDue(now, definition, zone)` for DAILY/WEEKLY/MONTHLY/CONTINUOUS, `triggerFor(now, scheduledFor, window)` → `SCHEDULE` (≤ 2 min), `CATCH_UP` (inside the window), `MISSED_WINDOW` (outside) — R-15-02, R-15-05; (d) `SchedulerTick` `@Scheduled(cron = "0 * * * * *")`, `now = clock.instant()` truncated to the minute, one `TenantContext` scope per club with `status ∈ {ACTIVE, ONBOARDING}` (A18e), fixed catalog order, sequential, guarded by the `"tick"` lock (lease 55 s) with metric `jobs.tick.overrun`; (e) `JobLocks` (`findOneAndUpdate` upsert with the `expiresAt < now` predicate, lease 300 s renewed every 60 s, `DuplicateKey` → `SKIPPED{LOCKED}` or `409 JOB_ALREADY_RUNNING`) — R-15-06; (f) `JobRunner`: guards of R-15-03 (`jobs.<name>.enabled`, module, club status; continuous jobs record a `SKIPPED` at most once per hour), one **Mongo transaction per item** with the outbox (R-15-10), `PARTIAL` on item failure with `errors[]`, `FAILED` on a job-level exception, `effects.items` capped at 500, `parametersSnapshot`, `SchedulerRun` on the outbox for non-dry runs (R-15-07); (g) dry run (R-15-08): only `plan` runs, `action` prefixed `WOULD_`, **no write anywhere** (no collection, no outbox, no S3, no provider) except the `JobRun{dryRun: true}` itself; (h) metrics Micrometer `jobs.run.duration{job,status}`, `jobs.effects{job,key}`, `jobs.last_success_age_seconds{job,club}`, `jobs.tick.overrun`; (i) `JobFailed{job, runId, status, errorCount}` on the outbox on `FAILED`/`MISSED_WINDOW` + the N-42 consumer (ADMINS → APP+EMAIL, `jobs.alertAdminsOnFailure`, **max one per job and club-local day**, deterministic id, keys `notif.N-42.*` in ca/es/en, action `OPEN_JOBS`; pattern of E4-T03's `notifications.N-08a`); (j) **`@Audited(JOB_TRIGGERED)`** on the manual trigger (`entityType = JobRun`, `after {job, dryRun, status, counters}`) — S15 R-15-09 names it but S14 R-14-09 does not list it: add it with its `@AuditCovers` test and file the catalog proposal in the report and `MESSAGES.md`; (k) a **fictitious test job** (`JobName.TEST_NOOP`, registered only under the `test` profile, absent from `GET /jobs`) that plans N items and fails the nth, so T-15-01…07, 10, 31 run without any business vertical; (l) `POST /test/clock` **only** under the `test`/`local` profiles (S15 §12 WP-15-E) advancing a `MutableClock` bean, documented in `README.md`.
8. **ArchUnit rule (R-15-22)**: no production class outside the `Clock`/`ClubClock` beans may call `Instant.now()`, `LocalDate.now()`, `LocalDateTime.now()`, `ZonedDateTime.now()` or `System.currentTimeMillis()`; add it to `ArchitectureTest` as `E5_T01_timeComesFromTheClock` (T-15-07) and fix every existing violation you find (list them in the report).
9. **Events + notification fixtures**: `BookingEvent`, `TrainingEvent` and `SchedulerEvent` records (`domain` packages, `DomainEvent`) with the catalog names and the payloads of S08 §7 / S09 §7 / S15 §7 — `SeatHeld`, `SeatHoldReleased`, `BookingCreated`, `BookingCancelled`, `SeatReleased`, `WaitlistJoined`, `WaitlistLeft`, `WaitlistNotified{…, mode}`, `WaitlistConsolidated`, `WaitlistExpired`, `TrainingBooked`, `TrainingCancelled{…, by, cancelReason, late}`, `SchedulerRun`, `WeekOpened`, `TrainingCounterReset`, `ClassAtRisk`, `ClassAutoCancelled`, `ClassBelowMinimum`, `ReminderDue`, `JobFailed` — registered as samples in `EventCatalogContractTest` with fixtures `e5-events.json`; notification fixtures `e5-notifications.json` for N-04, N-05, N-06, N-07, N-15, N-36, N-40, N-42, N-46, N-47 with the catalog variables (rendering and sending are E5-T02…T05 and E7). The extended payloads of `WeekOpened`, `ClassAtRisk` and `ClassAutoCancelled` are S15 §13 proposals: implement them as S15 writes them and record the proposal in the report.
10. `bin/openapi-snapshot`, a `docs/openapi/CHANGELOG.md` entry (every route + the list/export fields), `E5ContractIT` (501 matrix: every route × ANON/MEMBER/INSTRUCTOR/ADMIN/AGILITYHUB_ADMIN, impersonation rules, cross-tenant 404, `FREE_TRAINING` off → 404 on every S09 route, `WAITLIST` off → 404 on `/waitlist-entries*`, `SINGLE_CLASS` off → no `price`/`checkoutUrl` in the schemas' examples, `MEMBER` on `GET /bookings` and `GET /training-bookings` → 403, no writes on any collection), `E5ResponseContractTest` (fixture round-trip of the S08/S09/S15 forms), `OpenApiRequiredContractTest` updated.

## Fixed conventions
- Code names from `docs/MODEL_DADES_PLATAFORMA.md` §0: `Booking`, `SeatHold`, `WaitlistEntry`, `SeatLock`, `TrainingBooking`, `RingBlock`, `JobRun`. Contexts: `clubs.bookings` (S08), `clubs.training` (S09), `platform` (S15 framework). **Dependency direction, enforced by `ArchitectureTest`** (cross-context only through `application` packages, no cycles): `clubs.bookings` → `clubs.scheduling.application` (+ `platform`, `shared`); `clubs.training` → `clubs.bookings.application` and `clubs.scheduling.application`; `clubs.activities` → `clubs.bookings.application`; **never** the other way — anything `bookings` needs from `training` or `activities` is a port declared in `clubs.bookings.application.ports` whose adapter lives in the providing context. `platform` imports no `clubs` package: jobs arrive as injected `List<Job>` beans.
- Closed catalogs: no new parameter, event, notification or error code. The only additions this task makes are `AuditAction.JOB_TRIGGERED` and the `jobs.*` parameters, both already named by S15 (§9 and R-15-09); everything else that a spec names and a catalog lacks goes to the report and `MESSAGES.md` as a proposal while the closest existing item ships. Catalog rule 0 is literal and the catalog file wins over any status written in S08/S09/S15 or in this file.
- Parameter keys exactly as in `CATALEG_PARAMETRES.md`, never a literal in code: the `bookings.*` block (`maxCurrentWeek` 2, `maxNextWeek` 1, `limitUnit` DOG, `weekOpensAt` {SUNDAY, 20:00}, **`lateCancelThresholdMinutes` 240** — Jordi 05-09, S08 R-08-10 still writes 120 and the catalog wins —, `seatHoldSeconds` 30, `showInstructorHoursBefore` 24, `instructorLastMinuteNotice` true, `paymentPendingMinutes` 30), the `waitlist.*` block (`mode` ALL_AT_ONCE, `fifoConfirmMinutes` 30, `maxPerClass` 3, `maxPerDogPerWeek` 2, `maxPerDogPerWeekIfAttended` 1, `notifyThresholdMinutes` 30), the `training.*` block (`slotMinutes` 30, `bookingWindowDays` 3, `maxPerWeek` 3, `cancelThresholdMinutes` 120, `capacityPerRingSlot` 1 scoped to `ring`, `freeTrainingRequiresLicense` false and informative), `ringBlocks.maxHorizonDays` (60), the `jobs.*` family (`<name>.enabled`, `dailyTime` 06:00, `alertAdminsOnFailure` true, `retention.*`), `classes.finishGraceMinutes` (15), `classes.riskReviewTime` (07:30), `classes.minDogs` (2), `classes.riskLookaheadDays` (2), `classes.riskAutoCancelSameDay` (true), `levels.enabled`, `club.timeZone`, `club.openingHours`, `club.holidays`.
- Everything time-related uses the injected `Clock` and `ClubClock`; instants are UTC on the wire, business dates are club-local; DST semantics identical to S06 R-06-14 and S15 R-15-02.
- Evidence rule: report = command + exit code + last 40 lines; long outputs → `roadmap/evidence/E5-T01/NN-<name>.log`; task file ≤ 120 KB. Fictional data only (`@example.test`, fake ids); no secrets, env vars only. Never edit other tasks, `ROADMAP.md` or the Organizer verification section. English identifiers from the glossary; i18n keys never literals; ca/es/en always together.

## Verification (run all; evidence = command + exit code + last 40 lines; long outputs → `roadmap/evidence/<ID>/*.log`)
- `./mvnw -q verify` green with `E5ContractIT`, `E5ResponseContractTest`, the framework tests named T-15-01…T-15-07, T-15-10, T-15-29, T-15-31 (T-15-01…07 without Spring or Mongo where possible; T-15-31 with two application contexts over the same Mongo), `EventCatalogContractTest`, `ErrorCatalogContractTest`, `AuditContractTest` (with `JOB_TRIGGERED` covered), `ArchitectureTest` including the new `Clock` rule, message parity ca/es/en (`error.*` for every new code, `notif.N-42.*`), coverage gates.
- `bin/openapi-snapshot` run twice → byte-identical (`cmp`); `docs/openapi/CHANGELOG.md` lists the operations added (count them and state the number); `git diff --check` clean.

## Definition of Done
Every S08, S09 and S15 route is published in the snapshot with its schemas, roles, modules, idempotency and canonical error statuses and answers 501 behind the guards; the documents and indexes exist (including the two unique/partial guards the concurrency of E5 depends on); the job framework runs a fictitious job end to end with locks, catch-up, dry run, metrics, `SchedulerRun`, `JobFailed` → N-42 and audit; the `Clock` ArchUnit rule is green; the organizer can stage the snapshot for the web tasks.

## Evidence to return
`verify` tail; snapshot `cmp` result; OpenAPI diff summary (routes + schemas added, existing schemas changed); the list of `ErrorCode` values whose status you changed; the list of `Instant.now()`-style violations the new ArchUnit rule found and how you fixed them; the `JobRun` documents of one real and one dry run of the test job (ids truncated).

---

## Executor report
_(not started)_

### Branch / commits

### Evidence (full command outputs)

### Files changed

### Rules and tests implemented (R-xx-nn / T-xx-nn)

### Assumptions

### Questions / catalog proposals

## Organizer verification
_(pending)_
