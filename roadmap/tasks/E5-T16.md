---
id: E5-T16
title: Contract nulls and test names — follow-ups of the E3-T16 round-2 review: GET /club, the dashboard and the training override declare their nulls in the standard form, a normalized tax id, spec-id test names, one shared nullable helper
stage: E5
repo: agilityhub-core-api
thread: A
order: 160
status: ready
depends_on: [E3-T16]
branch:
pr:
updated: 2026-09-25
---

# E5-T16 · Contract nulls and test names

## Objective
The independent review of E3-T16 round 2 (`roadmap/reviews/E3-T16-20260925-2231-claude.md`) found one minor and four nits. None of them blocks gate E3, and the organizer confirmed them. E3-T16 was verified with them routed here, after the gate re-run E3-T11.

**Each fix comes with a test that fails before the fix.**

## Spec references
- `CONVENCIONS_API.md`: optional against nullable (INC-08), and §5 (`fieldErrors[{field, code}]`, amended 25-09).
- S02: R-02-06 (the tax id, checked by the country profile).
- AGENTS rule 5 (test names).

## Context to load
- The review.
- `SettingsContracts.java` (`ClubSettings`), the dashboard contracts (`Dashboard`, `DashboardKpis`), `TrainingBookingRequest`.
- `E3ContractConfiguration.java` (`nullableReference`, `NULLABLE_REFERENCES`) and its E4/E5 copies.
- `ClubSettingsService.java` and `ClubDefinitionWriter.java` (the tax-id check).

## Steps
1. **Nulls in the standard form (review #1).**
   - `GET /club` (`ClubSettings`) sends `legalName`, `taxId`, `address`, `contactEmail`, `contactPhone`, `websiteUrl`, `pwa`, `legal` and `lastChange` as `null` when unset. Declare them nullable.
   - The dashboard (`Dashboard.riskReview`, `pendingSignups`, `dogsByLevel`, the four `DashboardKpis` blocks) and `TrainingBookingRequest.override` still use the house forms (`$ref` with `type: [object, null]`, or `$ref` + `type: "null"`). Write them as E3-T16 did, so that openapi-typescript keeps the `null`.
   - Extend `T_04_29_T_04_33_everyOptionalProperty…` (renamed in step 3) to the `ClubSettings` and `Dashboard` roots.
2. **A normalized tax id (review #3, R-02-06).** Store the tax id normalized: upper case, no spaces or separators. Compare normalized values, so that the same id with other spacing is no change. `/branding.taxId` then publishes the normalized form. Test: `"g-6318 9617"` is stored and published as `G63189617`, and resending it with other spacing is no change.
3. **Test names by spec id (review #2).** Name the tests by the rule they assert, as the `R_04_20_` tests are:
   - `SettingsIT` `T_02_11_R_02_06_…` and `T_02_11_R_02_02_displayCity…` → `R_02_06_…` and `R_02_02_…`;
   - `ClubDefinitionsIT` `T_02_04_T_17_01_…` → by rule;
   - `SignupPaymentMethodsIT` and `E3GateFixesContractTest` `T_04_29_T_04_33_…` → by rule, because T-04-29 and T-04-33 are web UI tests;
   - `SignupPaymentMethodsIT` `R_04_27_T_17_01_clubApplyRefreshes…` → `R_04_10_…` (the organizer's E3-T14 note).
4. **One nullable helper (review #5).** Move `nullableReference` into one shared customizer used by the E3, E4 and E5 configurations. The generated document stays byte-identical, apart from step 1.

## Fixed conventions
- Additive contract change. Regenerate `docs/openapi/openapi.json`, and add one `docs/openapi/CHANGELOG.md` entry.
- Heavy runs go through the host lock.
- Fictional data only.

## Verification (run all; evidence = command + exit code + last 40 lines; long outputs → `roadmap/evidence/E5-T16/*.log`)
- `./mvnw -q clean verify`.
- `bin/openapi-snapshot`, then `git diff --stat docs/openapi/`.

## Definition of Done
- Steps 1–4 are done. Steps 1 and 2 each have a test that fails before the fix.
- The snapshot and its changelog are updated.

## Evidence to return
- The command outputs.
- A step → test table.

---

## Executor report
_(pending)_

## Organizer verification
_(pending)_
