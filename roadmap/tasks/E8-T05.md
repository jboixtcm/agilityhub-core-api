---
id: E8-T05
title: Packs, inactivity and leave (S12 WP-12-E + S13 WP-13-B + WP-13-C): PackBalanceService, InactivityCalendar / InactivityPeriodService / InactivityFeeService, LeaveRequestService / LeaveBillingService / LeaveScheduler, reactivation, booking eligibility, and the replacement of the E5 pack and inactivity doubles
stage: E8
repo: agilityhub-core-api
thread: A
order: 50
status: ready
depends_on: [E8-T01, E8-T02]
branch:
pr:
updated: 2026-09-19
---

# E8-T05 · Packs, inactivity and leave (S12 WP-12-E · S13 WP-13-B · WP-13-C)

## Objective
Work packages S12 WP-12-E, S13 WP-13-B and S13 WP-13-C. Three things that all end up on the same receipt: a **pack** that opens on payment, is consumed and refunded by bookings, warns when it runs low and loses its remaining sessions at expiry; an **inactivity period** a member asks for under the day-25 rule, that the admin approves (cancelling the bookings inside it), that starts and ends by itself and that replaces the monthly fee with the inactivity fee; and a **leave** the member or the admin schedules, that fixes the last invoiced month, cancels later bookings and turns the member into `LEFT` the day after. The same task replaces every double the earlier stages left behind: `PackBalancePort` and `InactivityPort` of E5-T02 (`clubs/bookings/application/ports`) and `InactivityFeePort`, `LeaveBillingPort` and `PackBalanceOpeningPort` of E8-T02 (`payments/application/ports`). The two schedulers become **jobs** in E8-T06; here they are plain services with an explicit `executeDue(clubId, today)` entry point.

## Spec references
S12 **R-12-23** (upfront payment → `PackBalanceService.open`, `expiresOn = openedOn + validityMonths − 1 day`, idempotent per payment, manual registration from D10, expiry loses the remaining sessions), **R-12-24** (consume / refund / adjust, the pack that expires first wins, `PackLowBalance` once, `PACK_NEGATIVE`, reopening an `EXPIRED` pack needs a new `expiresOn`), R-12-02, R-12-04 and R-12-28, §6 pack and upfront-payment rows, §8 N-11a, §9 · tests **T-12-07**, the pack halves of **T-12-22** and **T-12-31** · S13 **R-13-01…R-13-20 in full** — read every row of §4; the load-bearing ones for this task are R-13-01/03 (calendar, `E(avui)`, the generalised day-25 rule), R-13-02/04/05 (request, member changes, admin decision and termination), R-13-06 (cancelling the bookings inside), R-13-07 (the two eligibility contributions), R-13-08 (`InactivityFeeService`), R-13-09/10/12/15 (leave request, decision, later-booking sweep, cancelling a planned leave), R-13-11 (`LeaveBillingService`), R-13-13 (`LeaveScheduler`), R-13-14 (planned leave on pack expiry), R-13-16 (reactivation), R-13-17 (display status, columns, the «Baixes previstes» system view, `overview`), R-13-18/19/20 (permissions and audit, module branches, texts) · §5 (the three state machines), §6 (every row of S13), §7, §8 (N-18a…d, N-14, N-28, N-07), §9, §12 WP-13-B and WP-13-C · tests **T-13-01…T-13-26** (all but the front ones T-13-29…T-13-32 and the scheduler ones T-13-27/T-13-28, which E8-T06 owns) · S08 (`BookingCancellationService.cancelBySystem`, `WaitlistService.cancel`), S09 (`TrainingBookingService.cancelBySystem`), S07 (`ActivityRegistrationService.cancelBySystem`), S03 (`MemberStatusService`, `MemberBookingEligibility`, saved views), S05 R-05-18b (40 % entry discount pack → membership) · S14 R-14-09 (`INACTIVITY_RESOLVED`, `LEAVE_RESOLVED`, `LEAVE_CANCELLED`, `PACK_ADJUSTED`, `MEMBER_STATUS_CHANGED`) · `CATALEG_ERRORS.md` §1 + §3 rule 0 · `CATALEG_PARAMETRES.md` blocks «Quotes i cobraments» and «Inactivitat i baixa».

## Context to load
`docs/specs/S13-inactivitat-i-baixa.md` **complete** · `docs/specs/S12-facturacio-i-pagaments.md` R-12-23, R-12-24, R-12-02, R-12-04, R-12-28 and §3 `PackBalance` · `docs/specs/S03-cens-abonats-gossos.md` (`MemberStatusService`, `MemberBookingEligibility`, `displayStatus` R-03-03, saved views) · `docs/specs/S05-catalegs.md` R-05-18b only · `roadmap/tasks/E8-T01.md` (documents, indexes, schemas, `INACTIVITY_NOT_APPLICABLE`) and `roadmap/tasks/E8-T02.md` (the three ports this task implements) · `roadmap/tasks/E5-T02.md` §11 and `src/main/java/com/agilityhub/core/clubs/bookings/application/ports/{PackBalancePort,InactivityPort}.java` (**the null objects and the in-memory test adapter this task replaces — remove the adapter or keep it only under the `test` profile and say which**) · `clubs/bookings/application/{BookingCancellationService,WaitlistService,BookingEligibility}.java`, `clubs/training/application/TrainingBookingService.java`, `clubs/activities/application/ActivityRegistrationService.java` · `clubs/census/application/{MemberService,MemberStatusService,FamilyGroupService}.java`, `clubs/census/persistence/Member.java`, `clubs/common/application/SavedViewService.java` · `clubs/catalogs/persistence/Plan.java` (`type`, `pack {sessions, validityMonths}`) and `clubs/catalogs/application/PriceResolver.java` · `payments/**` (E8-T01…T04) · `platform/application/**` (config, modules, audit) · `shared/application/{ClubClock,EventPublisher,OutboxDispatcher,DomainEventHandler,TenantContext}.java` · `docs/playbooks/new-entity.md`, `new-event-consumer.md`, `checklist.md` · this file.

## Steps
1. **`PackBalanceService`** (`payments/application/packs`, `@RequiresModule(Module.PACKS)`): `open(memberId, dogId, planId, upfrontPaymentId)` — `sessionsTotal = Plan.pack.sessions`, `openedOn = paidAt` in club-local time, **`expiresOn = openedOn + Plan.pack.validityMonths − 1 day`** (§13-7 assumption: 12-06 + 5 months → 11-11; the mockup's «12-11» is the fictional datum, not the rule), movement `OPEN`, event `PackOpened`; idempotent per `upfrontPaymentId` (the unique sparse index of E8-T01) · `consume(dogId, bookingId)` — called **inside S08's transaction**: an `ACTIVE` pack with `remaining > 0` and `expiresOn ≥ the class date`; when several qualify, **the one that expires first**; movement `CONSUME −1` + `PackConsumed`; `PackLowBalance` → **N-11a** when `remaining ≤ billing.packLowBalanceSessions` (1) after the consumption, **once per pack** · `refund(bookingId)` — only when an unreturned `CONSUME` of that booking exists; `REFUND +1` + `PackRefunded`; **it still works when the pack has expired in the meantime** (§13-8: the session returns to an `EXPIRED` balance and is visibly unusable) · `adjust(packBalanceId, delta, reason, expiresOn?)` — ADMIN, audited **`PACK_ADJUSTED`**, `422 PACK_NEGATIVE` below zero, and reopening an `EXPIRED` pack (`state = ACTIVE`) **requires** a new `expiresOn` · `expire(packBalanceId)` — `state = EXPIRED`, `expiredAt`, **the remaining sessions are lost**: no return, no extension, no automatic refund (Josep 08-09, B11); the scheduler that calls it is E8-T06. `POST /pack-balances` opens one manually (migration, gift) with `422 PLAN_NOT_PACK` for a non-pack plan; `POST /upfront-payments {memberId, dogId?, concept, amountDue, amountPaid, channel, paidAt, reference?, note?}` registers an on-the-spot payment with `amountPaid ≤ amountDue` (`422 AMOUNT_EXCEEDS_DUE`, `PARTIAL` when smaller), audited `UPFRONT_PAYMENT_RECORDED`, and a `PACK` concept opens the balance. `GET /pack-balances` and `GET /me/pack-balances` return `{id, dogId, planName, sessionsTotal, consumed, remaining, expiresOn, state, movements[]}` for D10 and screen 13. T-12-07, T-12-31 pack half.
2. **Implement E8-T02's `PackBalanceOpeningPort`** (so `UpfrontPaymentSucceeded {concept: PACK}` opens the balance) **and E5-T02's `PackBalancePort`** (`consume`, `refund`, `hasUsableBalance`, the `PACK_EMPTY` state of the booking rows), deleting the null objects' behaviour. With `PACKS` off every method is a no-op and S08 treats the plan as `MONTHLY` (R-12-28, T-12-22).
3. **`InactivityCalendar`** (`clubs/census/inactivity/domain`, pure, no Spring): `earliest(today, deadlineDay, zone)` → `E(today)` = next month when `today.day ≤ min(deadlineDay, last day of the current month)`, otherwise the month after; interval helpers (`fromMonth` day 1 00:00 local → last day of `toMonth` 23:59:59 local, open when `toMonth` is null); `mayChange(month, today)` for R-13-03. Everything club-local through `ClubClock`; `2026-09-25T22:30Z` is 2026-11 in `Europe/Madrid` and 2026-10 in `America/Argentina/Buenos_Aires`. T-13-01, T-13-02.
4. **`InactivityPeriodService`** (R-13-02…R-13-06): request (`POST /me/inactivity-periods`) requires `Member.status = ACTIVE` (`422 MEMBER_NOT_ACTIVE`), no planned leave (`422 LEAVE_ALREADY_SCHEDULED`) and a **`MONTHLY` plan** — `PACK` or `SINGLE_CLASS` → **`422 INACTIVITY_NOT_APPLICABLE`** (Josep 08-09, B12); `fromMonth ∈ [E(today), E(today) + inactivity.maxStartMonthsAhead]`, `toMonth` null or `≥ fromMonth` (`422 INACTIVITY_INVALID_RANGE`); **no overlap and no adjacency** with a live period (`409 INACTIVITY_OVERLAP {periodId, hint: EXTEND}`), adjacency with a `FINISHED` one allowed; `Idempotency-Key`; `InactivityRequested` → **N-18a**. Member changes (`PATCH /me/inactivity-periods/{id}`, `version`): all fields on `REQUESTED`; `fromMonth`/`toMonth`/`comments` on `APPROVED` **applied without a new approval** (Josep 08-09) and only `toMonth` on `ACTIVE`, always inside R-13-03 — any month earlier than `E(today)` whose state would flip → `422 INACTIVITY_DEADLINE_PASSED {earliestMonth}`; each change appends to `history[]` and emits `InactivityChanged` → **N-18d** to the admins. Withdrawal: `REQUESTED` always, `APPROVED` only while `fromMonth ≥ E(today)`, `ACTIVE` never (it is shortened instead) → `CANCELLED{MEMBER, WITHDRAWN}` + `InactivityCancelled`. Admin side: `POST /inactivity-periods/{id}/decision {decision, note?}` only on `REQUESTED` (`422 INACTIVITY_INVALID_STATE`) — `APPROVED` freezes `feeSnapshot`, cancels the bookings inside (step 5) and goes straight to `ACTIVE` when today is already inside the interval (`InactivityStarted` in the same transaction); both outcomes emit `InactivityResolved{decision, fee, cancelledBookings[]}` → **N-18b** with `admin_text` and `cancelled_count`; `POST /inactivity-periods` creates one already approved (`origin = BACKOFFICE`, `overrideDeadline` allowed and audited in `decision.deadlineOverridden`); `PATCH /inactivity-periods/{id}` as above with override; `POST …/termination {toMonth}` on `ACTIVE` → `FINISHED` now (`finishReason = ADMIN`, `InactivityEnded` → N-18c) when `toMonth` is already past, otherwise it just fixes `toMonth`; `POST …/cancellation` cancels an `APPROVED` period that has not started. **Already invoiced months are never recomputed** — the admin issues an adjustment invoice in S12 (R-13-05). Audited `INACTIVITY_RESOLVED`. T-13-03, T-13-04, T-13-08, T-13-11…T-13-14, T-13-26.
5. **Cancelling the bookings inside (R-13-06)**, in the approval transaction, for every live booking of **the owner of any of their dogs** whose local session date falls inside the interval: classes → `BookingCancellationService.cancelBySystem(bookingId, reason = INACTIVITY)` (seat released, pack refunded, waitlist notified per the 30-minute threshold), waitlist entries → `WaitlistService.cancel(entryId, reason = INACTIVITY)`, trainings → `TrainingBookingService.cancelBySystem(id, reason = INACTIVITY)` (S09 sends N-07), activity registrations → `ActivityRegistrationService.cancelBySystem(id, reason = INACTIVITY)`. Store `cancelledBookings[]` and pass `cancelled_count` to N-18b. **No per-class notification** (the count travels in N-18b). With `inactivity.cancelBookingsOnApproval = false` nothing is cancelled and the response and N-18b carry `bookingsInside` instead. The same set is cancelled for the **months added** when a period is extended. Bookings of a family-group dog owned by another member are **not touched**. All-or-nothing: if one cancellation fails the whole approval rolls back. T-13-09, T-13-10.
6. **`InactivityFeeService`** (R-13-08): `feeFor(memberId, month)` → `feeSnapshot.firstMonth` when `month = fromMonth` of an `APPROVED`/`ACTIVE`/`FINISHED` period containing it, otherwise `feeSnapshot.followingMonths`, otherwise empty; `feesForMonth(clubId, month)` for D6's KPI. The snapshot is frozen at approval from `billing.inactivityFeeFirstMonth` (20,00 €) and `billing.inactivityFeeFollowingMonths` (10,00 €) — a later parameter change does **not** move an existing period; extending or shortening never creates a new «first month», a new period after a `FINISHED` one does. `BILLING` off → empty, no snapshot. **This implements E8-T02's `InactivityFeePort`.** A non-holder's fee goes on the holder's invoice as a separate line (R-12-04, §13-4: the family fare is not recomputed). T-13-05, T-13-23.
7. **`LeaveRequestService`** (R-13-09, R-13-10, R-13-15): `POST /me/leave-requests {requestedDate, reasonKey, nps?, comment?}` — `ACTIVE` only, one `PENDING` per member (`422 LEAVE_ALREADY_REQUESTED`), none when `leaveDate` is set (`422 LEAVE_ALREADY_SCHEDULED`), `requestedDate ≥ today` local (`422 LEAVE_DATE_INVALID`), `reasonKey` from `leave.reasons` (`422 LEAVE_REASON_UNKNOWN`), `nps` only with `leave.npsEnabled` (otherwise `403 READ_ONLY`), `comment ≤ 2000`; `LeaveRequested` → **N-14** with the localized reason label; withdrawal on `PENDING` only. `POST /leave-requests/{id}/decision {decision, effectiveDate?, note?}` on `PENDING`: `APPROVED` defaults `effectiveDate = requestedDate` and requires `≥ today` (`422 LEAVE_DATE_INVALID`) → `Member.leaveDate`, `leaveRequestId`, the later-booking sweep of step 8, and every live inactivity period closed (`ACTIVE` → `toMonth = month(effectiveDate)`, `finishReason = LEAVE`, **no N-18c**; `APPROVED`/`REQUESTED` → `CANCELLED{SYSTEM, LEAVE}`); `LeaveResolved{decision, source, effectiveDate, cancelledBookings[]}` → **N-28** with `effective_date`, `admin_text`, `cancelled_count`, `decision`, `source`. `POST /members/{id}/leave {effectiveDate, reasonKey?, note?}` creates an already-`APPROVED` request with `source = ADMIN`, cancelling any `PENDING` one (`CANCELLED{ADMIN}`). `DELETE /members/{id}/planned-leave` needs `ACTIVE ∧ leaveDate ≠ null` (`422 NO_PLANNED_LEAVE`) → `leaveDate = null`, request `CANCELLED{ADMIN}`, `LeaveCancelled` → N-28 variant; **cancelled bookings are not restored**. Audited `LEAVE_RESOLVED` / `LEAVE_CANCELLED`. T-13-15…T-13-19.
8. **`LeaveBillingService.lastInvoicedMonth(memberId)`** (R-13-11): empty without `leaveDate`; with `leave.fullMonthIfLater = true` → `month(leaveDate)`; with `false` → `month(leaveDate)` only when `leaveDate` is the last day of its month, otherwise the month before. **This implements E8-T02's `LeaveBillingPort`.** No automatic refund for a cash half-year cut short (§13-5). **Bookings after the leave date (R-13-12)**: on approval and again as a defensive sweep on execution, every live booking, waitlist entry, training and activity registration of the member with a local session date `> leaveDate` is cancelled with `reason = LEAVE` (S09 uses `MEMBER_LEFT`), packs refunded, no per-booking notification. T-13-06, T-13-16.
9. **`LeaveScheduler.executeDue(clubId, today)`** (R-13-13) and the inactivity transitions of R-13-05 exposed as `InactivityScheduler.startDue/finishDue/expireStale(clubId, today)` — plain services here, wired into the `expirations` job in **E8-T06**: `Member.status = ACTIVE ∧ leaveDate < today` → `MemberStatusService.transition(LEFT, leftReason)` with `leftAt`, `LeaveRequest.executedAt`, live inactivity periods closed, the R-13-12 sweep, and `MemberStatusChanged{ACTIVE→LEFT, effectiveDate}` (S01 suspends the `Membership`, S03 deactivates the dogs and clears `lastDogFor*`). Strict `<` comparison so running twice or a day late gives the same result; `effectiveDate = today` executes **tomorrow**, never immediately. `InactivityScheduler`: `APPROVED` with `fromMonth ≤ month(today)` → `ACTIVE` + `InactivityStarted`; `ACTIVE` with `toMonth ≠ null ∧ toMonth < month(today)` → `FINISHED` + `InactivityEnded` → N-18c; an open period never ends by itself; a `REQUESTED` period whose `fromMonth` has passed with no decision → `CANCELLED{SYSTEM, EXPIRED}` silently, audited (§13-12).
10. **Pack expiry → planned leave (R-13-14)**: a `PackExpired {packBalanceId, dogId, memberId}` consumer, idempotent per `packBalanceId`, creates an `APPROVED` `LeaveRequest {source: PACK_EXPIRED, origin: SYSTEM, requestedDate = effectiveDate = expiresOn + leave.packExpiryGraceDays}` (30) and sets `Member.leaveDate`, **only** when the member is `ACTIVE`, on a `PACK` plan, with no other live pack on any of their dogs and no `leaveDate` yet; `LeaveResolved{source: PACK_EXPIRED}` → N-28 variant. A `PackOpened` consumer cancels such a planned leave (`CANCELLED{SYSTEM, PACK_RENEWED}` + `LeaveCancelled`). `PACKS` off → nothing. Moving from a pack to a membership pays the entry fee with **40 % off** when the pack had ≥ `billing.packToMemberMinSessions` (10) sessions (`billing.packToMemberEntryDiscountPercent = 40`, S05 R-05-18b) — verify that S05's `EntryFeeCalculator` already applies it and, if it does not, implement the hook and say so. T-13-20.
11. **`POST /members/{id}/reactivation`** (R-13-16): `LEFT` only (`422 MEMBER_NOT_LEFT`); with `BILLING` on, `planId`, `priceId` and `nextInvoiceDate` are required (`400 VALIDATION_ERROR`); `status = ACTIVE`, `leaveHistory[] += {…, reactivatedAt}`, `leaveDate`/`leftAt`/`leftReason` cleared, `Membership` back to `ACTIVE`, **the dogs stay `INACTIVE`** until the admin reactivates them one by one, `memberNumber` kept, `MemberStatusChanged{LEFT→ACTIVE}`. **No entry fee** on reactivation (Josep 08-09, §13-8); the admin can register an upfront payment if needed. T-13-21.
12. **Eligibility, projections and lists (R-13-07, R-13-17)**: contribute to `MemberBookingEligibility` — (1) with `INACTIVITY`, an `APPROVED`/`ACTIVE` period containing `month(session date)` → `INACTIVITY_PERIOD {from, to}` (**422**, S08 renders `NOT_BOOKABLE{INACTIVITY}`); (2) `leaveDate ≠ null ∧ session date > leaveDate` → `MEMBER_LEAVING {leaveDate}` (**422**) — the leave date itself is still active (BR-07; S08 R-08-04's `≥` becomes `>`, §13-10). **This implements E5-T02's `InactivityPort`.** Extend `displayStatus` with `INACTIVE_PERIOD{date = last day of toMonth, or null when open}` and `LEAVE_SCHEDULED{date = leaveDate}` for any `source`; add `leaveSource`, `inactivityUntil` and `hasPendingRequest` to `GET /members` filters and columns, `inactivity {current?, upcoming?, pendingRequest?}` and `plannedLeave {date, source, requestId, since, cancellable}` to `GET /members/{id}/overview`, and seed the **non-deletable system saved view «Baixes previstes»** (`listKey = members`, `filters = [displayStatus:eq:LEAVE_SCHEDULED]`, `sort = leaveDate,asc`, columns `fullName, dogs, plan, leaveDate, leaveSource`; `DELETE` → `403`). T-13-07, T-13-22.
13. **Contexts for 14 and 15**: `GET /me/inactivity-periods` (`earliestFromMonth`, `proposedFromMonth`, `deadlineDay`, `fee?`, `periods[]` with `editable {fromMonth, toMonth, cancel}`), `GET /me/inactivity-periods/preview?fromMonth=&toMonth=` (`bookingsInside {classes, waitlist, trainings, activities, total}` — counters absent when the module that owns them is off —, `feeSchedule[]`, `earliestMonthViolation?`), `GET /me/leave-requests` (`offerInactivity`, `fee?`, `defaultDate`, `fullMonthIfLater`, `npsEnabled`, `reasons[{key, label}]` resolved in the reader's locale, `plannedLeave?`, `requests[]`). `GET /inactivity-periods` and `GET /leave-requests` as universal lists with the declared filters and sorts. T-13-23.
14. **Permissions and audit (R-13-18)**: a MEMBER only ever reaches their **own** period or request — another member's, **including one from their own family group**, is `404`; impersonation is accepted on `/me/*` with `origin = BACKOFFICE` and an `AuditEntry` carrying both ids, and refused on admin routes (`403 IMPERSONATION_DENIED`); INSTRUCTOR → `403` everywhere. Every decision, creation, change, termination, cancellation, execution and reactivation writes an `AuditEntry` with `before`/`after` of the changed fields, in the same transaction as the change and the outbox. T-13-24, T-13-25.

## Fixed conventions
- Statuses (catalog first, **rule 0 overrides what S13 §6 writes**): `INACTIVITY_OVERLAP` **409** (suffix `_OVERLAP`) · `INACTIVITY_DEADLINE_PASSED`, `INACTIVITY_INVALID_RANGE`, `INACTIVITY_INVALID_STATE`, `INACTIVITY_NOT_APPLICABLE`, `LEAVE_ALREADY_REQUESTED`, `LEAVE_ALREADY_SCHEDULED`, `LEAVE_INVALID_STATE`, `LEAVE_DATE_INVALID`, `LEAVE_REASON_UNKNOWN`, `MEMBER_NOT_LEFT`, `NO_PLANNED_LEAVE`, `MEMBER_NOT_ACTIVE`, `INACTIVITY_PERIOD`, `MEMBER_LEAVING`, `PACK_NEGATIVE`, `PLAN_NOT_PACK`, `AMOUNT_EXCEEDS_DUE` **422** (only the bare `ALREADY_` prefix makes a code 409 — confirmed 09-09) · `STALE_VERSION` **409** · `READ_ONLY`, `IMPERSONATION_DENIED` **403** · `MODULE_DISABLED`, `NOT_FOUND` **404** · `VALIDATION_ERROR` **400**.
- Assumptions in force (do not ask): pack plans cannot request inactivity and the pack simply expires on its date (B12); unused pack sessions are **lost** at expiry, with no return, extension or automatic refund, and an in-time cancellation after expiry still returns the session to the expired balance where it cannot be used (B11, §13-8); member changes to an approved or active period apply by themselves inside the day-25 rule, with N-18d to the admins (§13-2); reactivation charges no entry fee (§13-8); the leave date itself is an active day (§13-10); the family fare is not recomputed when one member goes inactive — the inactivity fee is added to the payer's invoice (§13-4); a `REQUESTED` period that goes stale is cancelled silently with an audit entry (§13-12); «inactiva fins {dd/mm}» shows the **last day of `toMonth`** and an open period shows «inactiva» (§13-1). Never «parella»/«parelles» in any user-facing text.
- One Mongo transaction per business action, outbox inside it; the approval (period + cancellations), the leave decision (leave date + cancellations + closing periods), the leave execution and the two pack consumers are all all-or-nothing. `Clock` and `ClubClock` injected everywhere (E5-T01 ArchUnit rule); every date is club-local.
- Context direction (ArchUnit): `clubs.census` implements the ports declared by `clubs.bookings.application.ports` and `payments.application.ports`; it calls S07/S08/S09 **synchronously** through their `application` services (S13 §7), never by event; `payments` never imports `clubs.census.persistence`.
- Closed catalogs: no new parameter, event, notification, error code or audit action beyond what E8-T01 added; the N-28 and N-18b variants are template **variables** (`decision`, `source`, `admin_text`, `cancelled_count`), not new codes. Proposals to the report and `roadmap/MESSAGES.md`.
- Evidence rule: report = command + exit code + last 40 lines; long outputs → `roadmap/evidence/E8-T05/NN-<name>.log`; task file ≤ 120 KB; fictional data only (`@example.test`); no secrets, env vars only; never edit other tasks, `ROADMAP.md`, `docs/` or the Organizer verification section; English identifiers from the glossary; i18n keys never literals; ca/es/en together.

## Verification (run all; evidence = command + exit code + last 40 lines; long outputs → `roadmap/evidence/<ID>/*.log`)
- `./mvnw -q verify` green with the tests named after **T-13-01…T-13-26** (T-13-01…T-13-07 without Spring or Mongo) and **T-12-07** plus the pack halves of T-12-22 and T-12-31, `E8ContractIT` updated (the S13 routes and the pack routes no longer answer 501), the E5 booking suites still green with the real ports in place of the doubles, `ArchitectureTest`, `AuditContractTest` (`INACTIVITY_RESOLVED`, `LEAVE_RESOLVED`, `LEAVE_CANCELLED`, `PACK_ADJUSTED`, `UPFRONT_PAYMENT_RECORDED` covered), `EventCatalogContractTest`, message parity ca/es/en for `notif.N-11a.*`, `notif.N-14.*`, `notif.N-18a.*`…`N-18d`, `notif.N-28.*` and the new `error.*` keys, coverage gates.
- `bin/e5-smoke` still green (the booking flows now run against the real pack and inactivity implementations).
- OpenAPI snapshot byte-identical to a fresh `bin/openapi-snapshot` (or a `docs/openapi/CHANGELOG.md` entry); `git diff --check` clean.

## Definition of Done
A member on a monthly plan asks for an inactivity period on the 24th and is refused on the 26th with the next allowed month; the admin approves it, four bookings inside the interval are cancelled in one transaction with the pack session returned, and the September receipt carries «Quota inactivitat» instead of the fee; the member shortens the period inside the day-25 rule and the admins get N-18d; a leave requested for 31-10 and approved on 05-10 cancels only the bookings after it, closes the December period, fixes the last invoiced month at October and turns the member `LEFT` on 01-11 (never before); a pack expires, its remaining sessions are lost and a planned leave appears 30 days later, cancelled again when a new pack is bought; and a reactivated member keeps their number, comes back without an entry fee and with their dogs still inactive.

## Evidence to return
`verify` tail; the T-13 and T-12-07 method list; the `curl` sequence `POST /me/inactivity-periods` → `POST /inactivity-periods/{id}/decision` → `GET /members/{id}/overview` → `POST /me/leave-requests` → `POST /leave-requests/{id}/decision` → `GET /members?filter=displayStatus:eq:LEAVE_SCHEDULED`; the `InactivityPeriod` document after approval with `feeSnapshot`, `history[]` and `cancelledBookings[]` (ids truncated); the `PackBalance` movements of an open → consume → refund → expire → adjust cycle; the diff that removed the E5 null objects and the in-memory pack adapter; the `MemberBookingEligibility` output for the three dates of T-13-07.

---

## Executor report
_(not started)_

### Branch / commits

### Evidence (full command outputs)

### Files changed

### Rules and tests implemented (R-xx-nn / T-xx-nn)

### Assumptions

### Questions / catalog proposals

## Organizer verification
_(pending)_
