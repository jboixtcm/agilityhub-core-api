---
id: E2-T04
title: Team and roles: instructors, administrators, RoleAssignmentService (S05 WP-05-B)
stage: E2
repo: agilityhub-core-api
thread: A
order: 40
status: ready
depends_on: [E2-T03, external:api:E1-T02 (Membership services)]
branch: 
pr: 
updated: 2026-09-09
---

# E2-T04 · Team and roles: instructors, administrators, RoleAssignmentService (S05 WP-05-B)

## Objective
Work package WP-05-B: the club's team (instructors, administrators) as profiles on memberships, synchronised with the member record roles.

## Spec references
S05 §3 (`Instructor {memberId?, shortName, color, active}`, `adminProfile {shortName, since}`), R-05-10 (create or reactivate), R-05-02 (delete only when unused), R-05-13…R-05-16 (roles ↔ `Membership.roles`, last admin → `LAST_ADMIN`, member LEFT → profiles deactivated) · tests **T-05-13, 14, 15, 16** · S03 R-03-10 (`PUT /members/{id}/roles` uses this service) · S01 §3 (`Membership.roles`).

## Context to load
S05 §3, §4, §6 · S03 R-03-10 · the identity `MembershipService` · this file.

## Steps
1. `Instructor` entity + `/instructors` endpoints (`INSTRUCTOR_IN_USE` when template classes reference it — port), `/administrators` endpoints over `Membership.adminProfile` (`LAST_ADMIN`, `LAST_PLATFORM_ADMIN` reserved for S17).
2. `RoleAssignmentService.setRoles(memberId, roles)`: keeps `Membership.roles`, creates/reactivates the `Instructor` or `adminProfile` when the role is granted, deactivates when removed; consumer of `MemberStatusChanged{LEFT}` → roles reduced to none, profiles inactive (T-05-16).
3. Audit `ROLE_CHANGED`, `INSTRUCTOR_*`, `ADMIN_PROFILE_*`; event `MembershipChanged` published through the outbox.

## Verification (run all; paste full output in the report)
- `./mvnw -q verify` → green with T-05-13…16.

## Definition of Done
Team endpoints + role service + consumer + audit.

## Evidence to return
`verify` output.

---

## Executor report
_(not started)_

### Branch / commits
### Evidence (full command outputs)
### Files changed
### Rules and tests implemented (R-xx-nn / T-xx-nn)
### Assumptions
### Questions / catalog proposals

## Organizer verification
_(pending)_
