---
id: E4-T03
title: Calendar, classes, cancellation, ring blocks and grids (S06 P3): ClassSession + RingBlock lifecycle, WeekValidationUseCase, ClassCancellationUseCase over the S08/S09 ports, RingBlockService.syncForActivity, RiskEvaluator, DayGridQuery, CalendarQuery, N-08a/N-08b in the dispatcher
stage: E4
repo: agilityhub-core-api
thread: A
order: 30
status: awaiting_verification
depends_on: [E4-T02]
branch:
pr:
updated: 2026-09-19
---

# E4-T03 · Calendar, classes, cancellation, ring blocks and grids (S06 P3): ClassSession + RingBlock lifecycle, WeekValidationUseCase, ClassCancellationUseCase over the S08/S09 ports, RingBlockService.syncForActivity, RiskEvaluator, DayGridQuery, CalendarQuery, N-08a/N-08b in the dispatcher

## Objective
Work package S06 P3: the admin validates a whole week, edits an active class ([Accepta]), cancels or deletes it — never physically: it becomes `CANCELLED` — with the D4c flow that notifies every registrant (N-08a app + mail + SMS intent), blocks rings from D4 (and the instructor from screen 24 through the same endpoint), and everybody sees the day: student grid (10, never counts), instructor «Visió global» (23) and the D4 calendar (forms B and D). Bookings, waitlist, packs and training bookings are E5: this task defines the **ports** S06 §7 names, proves the transaction with in-memory doubles, and exposes the contracts S07 (E4-T04) and S15 (E5) call.

## Spec references
S06 R-06-05 (week scope), R-06-08 (validation), R-06-09 (class lifecycle and edition; filters of D4), R-06-10 (cancellation and deletion with registrants), R-06-11 (ring blocks, shared with S09 R-09-11…13), R-06-12 (who sees what), R-06-13 (risk display), R-06-14, R-06-15 (variants `WAITLIST`, `FREE_TRAINING`, `ACTIVITIES`, `COURSES` off), §5 (both state machines, transition table), §6 rows `POST /weeks/{id}/validation`, `GET /weeks/{id}/calendar`, `/class-sessions*`, `/ring-blocks*`, `GET /day-grid` + forms B and D, §7 (emitted events; consumed `ClassAutoCancelled` = same use case; contracts with S15/S05/S08/S09/S16), §8 (N-08a, N-08b, SMS bodies, dispatcher idempotency), §9, §10 back keys, §13 assumptions 2, 3, 4, 6, 11 · tests **T-06-06, T-06-07, T-06-08, T-06-12, T-06-13, T-06-14, T-06-15, T-06-16, T-06-17, T-06-21, T-06-23, T-06-24, T-06-25, T-06-32, T-06-35, T-06-36** + the N-08a half of **T-06-34** · S07 R-07-05 (the `RingBlockService.syncForActivity/cancelForActivity` contract implemented here) · S09 R-09-11…13 (`TrainingOccupancyService`, `TrainingConflictService`, `ringBlocks.maxHorizonDays`) · S15 R-15-12, R-15-12b (`risk.*` fields), R-15-18 (`finishEnded`, `classes.finishGraceMinutes`) · S14 R-14-09 actions `WEEK_VALIDATED`, `CLASS_CANCELLED`, `CLASS_UPDATED_WITH_BOOKINGS`, `CLASS_RISK_EXEMPTION_CHANGED`, `RING_BLOCK_CREATED`, `RING_BLOCK_CANCELLED` · `CATALEG_NOTIFICACIONS.md` N-08a, N-08b, rules 3, 6, 7 · `CATALEG_PARAMETRES.md`: `classes.minDogs` (2), `classes.riskReviewTime` (07:30), `classes.riskLookaheadDays` (2), `classes.riskAutoCancelSameDay` (true), `classes.slotMinutes` (10), `training.slotMinutes` (30), `bookings.showInstructorHoursBefore` (24), `classes.finishGraceMinutes` (15), `ringBlocks.maxHorizonDays` (60), `club.openingHours`, `club.holidays`, `levels.enabled` · `docs/DECISIONS_PENDENTS.md` A22(e), part C S06 («bloqueig sobre una classe es refusa (409)», «xip exempta de la revisió»).

## Context to load
`docs/specs/S06-planificacio-setmanes-i-calendari.md` §4 (R-06-05, 08…15), §5, §6 (rows + forms B, D, «Altres formes»), §7, §8, §10, §11, §13 · `docs/specs/S07-activitats.md` R-07-05 only · `docs/specs/S09-entrenaments-lliures.md` R-09-11, R-09-12, R-09-13 only · `docs/specs/S15-processos-programats.md` R-15-12, R-15-12b, R-15-18 only · E4-T01 contract + E4-T02 domain (`InconsistencyDetector`, `DescriptionResolver`, `WeekCalendarRules`) · E3-T04 `clubs/dashboard/application/ports` (`RiskEvaluator` port, class repository port, `ClassOccupancyQuery`) · `clubs/messaging/application/SystemNotificationService` (`appOnce`, `sendOnce`, `sendApplicantOnce`), `clubs/messaging/persistence/Notification`, `shared/application/OutboxDispatcher`, `DomainEventHandler`, `NotificationAccounts`, `MemberIdentityAccess` · census reads (`census.application`: members, dogs, phones, emails, account ids) · `platform/application/audit/*` (`@Audited`, `AuditWriter`) · `IdempotencyFilter`, `TransactionTemplate` usage in E3-T03 · `docs/playbooks/new-event-consumer.md`, `new-scheduler.md` · this file.

## Steps
1. **Ports** (`clubs/scheduling/application/ports`, null-object beans `@ConditionalOnMissingBean`, replaced by E5/E4-T04 without touching this code): `ClassBookingsPort` (S08 contract) — `List<BookingRef {bookingId, memberId, dogId, paidWithPack}> activeBookings(classId)`, `List<WaitlistRef {entryId, memberId, dogId}> liveWaitlist(classId)`, `CancellationEffects cancelAllByClub(classId, reason, actorAccountId)` (bookings `ACTIVE` → `CANCELLED_BY_CLUB`, pack refunds → `PackRefunded` only with `PACKS`, waitlist `ACTIVE/NOTIFIED` → `CANCELLED`, live `SeatHold` removed; runs inside the caller's transaction, `Propagation.MANDATORY`), `bookedDogs(classId)` for coverage `booked`; `TrainingOccupancyPort` (S09 `TrainingOccupancyService.occupancy(range, ringIds?, viewerRole)` → intervals `{ringId, from, to, type: TRAINING·RING_BLOCK, reason, memberName?, dogName?, note?, id}`); `TrainingConflictPort` (S09 `findActiveBookings(ringId, from, to)`, `cancelByClub(bookingIds, cancelReason)`); `ActivityTitlePort` (`Map<String, String> titles(Collection<String> activityIds, Locale)`; null-object → empty map, E4-T04 implements it — `scheduling` never imports `clubs.activities`). Tests use in-memory doubles that reproduce the S06 examples.
2. **`ClassSessionRules` + state machines** (pure, T-06-08): `ClassSession` `DRAFT → ACTIVE` (week validation) · `DRAFT → CANCELLED` (`DELETED`) · `ACTIVE → CANCELLED` (`CLUB_MANUAL`, `DELETED`, `RISK_REVIEW`, `ACTIVITY`) · `ACTIVE → FINISHED`; `Week` `PENDING → GENERATED → VALIDATED`, `PENDING → VALIDATED`; every forbidden transition → `INVALID_STATE`; `FINISHED`/`CANCELLED` accept only `notes`.
3. **`WeekValidationUseCase`** (R-06-08): every `DRAFT` class of the week → `ACTIVE` in one transaction (`Week.state = VALIDATED`, `validatedAt/By`, `WeekValidated{weekId, classIds[]}`, `@Audited(WEEK_VALIDATED)`); `NOTHING_TO_VALIDATE` without drafts; `WEEK_INCONSISTENT` when the week-scope `InconsistencyDetector` (drafts + actives + `RingBlock` `ACTIVE` + `TrainingConflictPort` bookings, only with `FREE_TRAINING`) finds anything (§13-4: blocks, like generation); drafts stay invisible to members until then. Bookability (`bookings.weekOpensAt`) is S08's concern: nothing here. T-06-12, T-06-23 (validation concurrent with a `PATCH`: never a `DRAFT` in a `VALIDATED` week).
4. **Class creation and edition** (R-06-09): `POST /class-sessions` → `ACTIVE` if the (get-or-create) week is `VALIDATED`, else `DRAFT`; holiday dates allowed; `ClassSessionCreated{classId}`; a ring with live training bookings → `RING_HAS_BOOKINGS {bookings[]}` unless ADMIN sends `cancelBookings: true` (→ `TrainingConflictPort.cancelByClub`, same transaction). `PATCH /class-sessions/{id}`: editable `ringId`, `levelIds`, `instructorIds`, `capacity` (`null` → back to `AUTO`), `startTime/endTime` (instants re-derived, R-06-14), `description`, `notes`, `riskExempt`; `date` → 400; `version` → `STALE_VERSION`; `capacity < counters.booked` → `CAPACITY_BELOW_BOOKINGS`; `ClassSessionUpdated{classId, diff, bookedCount}`; `@Audited(CLASS_UPDATED_WITH_BOOKINGS)` when `counters.booked > 0`. `POST …/risk-exemption {exempt}` on `ACTIVE` only (`INVALID_STATE`), `risk.exempt` persisted, `ClassRiskExemptionChanged{classId, exempt}`, `@Audited(CLASS_RISK_EXEMPTION_CHANGED)` (§13-6 chip). T-06-13.
5. **`ClassCancellationUseCase`** (R-06-10; public method `cancel(classId, reason: CLUB_MANUAL·DELETED·RISK_REVIEW·ACTIVITY, adminText?, actor)` used by the endpoint, by S15 (`RISK_REVIEW` with `scheduling.autoCancel.text`) and by `RingBlockService.syncForActivity` (`ACTIVITY`)): preconditions `state ∈ {DRAFT, ACTIVE}` (`INVALID_STATE`), `adminText` 1–500 mandatory when `counters.booked > 0 ∨ counters.waiting > 0` (`ADMIN_TEXT_REQUIRED`); **one transaction**: class → `CANCELLED` + `cancellation{reason, adminText, byAccountId, at, affectedBookings, affectedWaitlist}`, `ClassBookingsPort.cancelAllByClub`, `counters {0,0}`, `ClassCancelledByClub{classId, reason, adminText, affected[{bookingId, memberId, dogId}], waitlistIds[]}` to the outbox, `@Audited(CLASS_CANCELLED)`; any failure → full rollback; `Idempotency-Key` on the endpoint (same key → same response; two concurrent → one 200 + one `INVALID_STATE`). `GET …/cancellation-preview` = the port's bookings/waitlist resolved to `memberName`, `dogName`, `levelName` (reader's locale), `channels[]` (APP if the member has an account, EMAIL if it has a contact email, SMS if `SMS` module on and phones > 0 — preferences of S11 refine it in E7) and `phoneCount`, plus `waitlistCount`. Drafts without registrants: `CANCELLED` without notification. `finishEnded(now)` (S15 contract, R-15-18): `ACTIVE` with `endsAt + classes.finishGraceMinutes ≤ now` → `FINISHED` (`finishedAt`), idempotent, no business event; expose it as `ClassSessionService.finishEnded(Instant)` + CLI command `scheduling:finish-ended [--club=]` (E5 schedules it). `ClassSessionRepository.findActiveBetween(clubId, from, to)` (index `{clubId, date, state}`), `countFutureByRing/Instructor` (S05 R-05-07/12) and the real implementations of E3-T04's ports (`RiskEvaluator`, class repository, `ClassOccupancyQuery` = booked/capacity over the week's `ACTIVE`+`FINISHED` classes). T-06-14 (4 bookings, 2 with pack, 2 waiting: class `CANCELLED`, 4 `CANCELLED_BY_CLUB`, 2 `PackRefunded`, 2 waitlist `CANCELLED`, `counters {0,0}`, one event with 4 `affected` + 2 `waitlistIds`; no `adminText` → 422 and nothing changes; `reason = DELETED` identical; `SMS` off → `SKIPPED_MODULE_OFF`), T-06-15, T-06-24, T-06-25.
6. **`RingBlockService`** (R-06-11 + S09 R-09-11…13; `clubs/scheduling/application`): create from D4 (ADMIN) or 24 (INSTRUCTOR) with `kind`/`reason` combinations of §3 (`RESERVATION` → `PRIVATE_CLASS`/`THERAPY`/`PREPARATION`/`OTHER`, needs `FREE_TRAINING` → else `MODULE_DISABLED`; `BLOCK` → `MAINTENANCE`/`OTHER`; `ACTIVITY` rejected on the API with 400 `VALIDATION_ERROR`), `ringId` active, `from/to` aligned to `classes.slotMinutes`, `to − from ≥ training.slotMinutes` (`INVALID_TIME_RANGE`), inside `club.openingHours` (`OUTSIDE_OPENING_HOURS`), `from > now` (400), `date(from) ≤ today + ringBlocks.maxHorizonDays` (S09; apply to both creators, state it in the report), never linked to a member; overlap with `ClassSession` `DRAFT/ACTIVE` or another `RingBlock` `ACTIVE` of the ring → `RING_BLOCK_CONFLICT {conflicts[{ringId, type: CLASS·RING_BLOCK, id, from, to, label, bookedCount?}]}`; live training bookings → `RING_HAS_BOOKINGS {bookings[]}` unless ADMIN `cancelBookings: true` (INSTRUCTOR with it → 403); `PATCH` only future blocks (`INVALID_STATE` once started), `version`; `activityId ≠ null` → `RING_BLOCK_MANAGED_BY_ACTIVITY` on `PATCH` and cancellation; `POST …/cancellation` → `CANCELLED` (`cancelledAt/By`, never deleted); events `RingBlockCreated{blockId, ringId, from, to, reason, activityId?}`, `RingBlockUpdated{blockId, diff}`, `RingBlockCancelled{blockId}`; `@Audited(RING_BLOCK_CREATED)`, `@Audited(RING_BLOCK_CANCELLED)`; reads add `date`, `fromLocal`, `toLocal`, `createdByName`, `activityTitle` (port); MEMBER list/detail redacted (no `note`, no `createdByName`). **S07 contract**: `syncForActivity(ActivityBlockRequest {activityId, ringIds[], from, to, createdByAccountId}, options {cancelBookings, cancelClasses, adminText})` and `cancelForActivity(activityId)` — run inside the caller's transaction (`Propagation.MANDATORY`): create missing blocks (`kind = BLOCK`, `reason = ACTIVITY`), move changed windows (`RingBlockUpdated`), cancel blocks of rings no longer listed; conflicts as above; `cancelClasses: true` → `ClassCancellationUseCase.cancel(classId, ACTIVITY, adminText)` for every conflicting class (`ADMIN_TEXT_REQUIRED` if any has `booked > 0 ∨ waiting > 0`); `cancelBookings: true` → `TrainingConflictPort.cancelByClub`; all or nothing; `conflictsFor(request)` returns the preview for `GET /activities/{id}/ring-conflicts`. The request record lives in `scheduling.application`; no import of `clubs.activities`. T-06-16, T-06-32.
7. **`RiskEvaluator`** (R-06-13, pure, shared with S15/E3-T04): `atRisk = state == ACTIVE ∧ ¬risk.exempt ∧ counters.booked < classes.minDogs ∧ date ≤ today + classes.riskLookaheadDays ∧ now < reviewAt`, `reviewAt = date at classes.riskReviewTime` in `club.timeZone` (E4-T02 helper); tooltip ICU `scheduling.risk.tooltip` (`{count, plural, =0 …, one …}` with `{minDogs}`, `{time}`, `{day}` in the reader's locale) and `scheduling.risk.tooltipNoAutoCancel` when `classes.riskAutoCancelSameDay = false`. T-06-06 (1 booking tomorrow → at risk; 2 → no; exempt → no; +3 days with lookahead 2 → no; today 08:00 with `reviewAt` 07:30 passed → no; texts for 0 and 1 dog in ca/es/en), T-06-07 (DST instants + `reviewAt` in both zones).
8. **`DayGridQuery`** (R-06-12, form D): `date` club-local, rows = distinct start times of every element of the day, columns = active rings in catalog order (+ «Sense» only when a class has no ring; `activeSetupId` only with `COURSES`); cells: `CLASS` (`ACTIVE`/`FINISHED`; `CANCELLED` only in `view=instructor`, dimmed; `DRAFT` never), `TRAINING`/`BLOCK` from `TrainingOccupancyPort` + `RingBlock` (both become `OCCUPIED {reason}` in `view=member`, without names/notes), `ACTIVITY` (blocks with `reason = ACTIVITY`: `activityId` + `title` from `ActivityTitlePort`, one cell per blocked ring, both views); `instructorName` for MEMBER only when `now ≥ startsAt − bookings.showInstructorHoursBefore` h (`0` = always); `occupancy {booked, capacity, waiting?}` only in `view=instructor` (`waiting` absent without `WAITLIST`); `atRisk` + `riskText` per view; empty day → `rows: []`; two cells with the same `ringId` in a row stack. `view=instructor` requires INSTRUCTOR/ADMIN (403); impersonation tokens only on `view=member`. **`CalendarQuery`** (form B): `filter=ACTIVE` → `ACTIVE`+`FINISHED`+`CANCELLED`, `DRAFT` → drafts, `CANCELLED` → cancelled; `week.relative` (`CURRENT`/`NEXT`/`OTHER`, club-local), `rows`, classes with `atRisk`, `displayDescription` (E4-T02 resolver, reader's locale), `inconsistencyIds`, `ringBlocks[]` with `activityTitle`, week-scope `inconsistencies[]`, `draftCount`, `canValidate`. `GET /class-sessions` universal list (`ListEngine`, `x-filterable` of E4-T01; MEMBER 403) and `GET /class-sessions/{id}` projections («Altres formes»: MEMBER only `ACTIVE`/`FINISHED`, `instructorName` per R-06-12, `freeSeats`, `waiting?`, no `notes`; INSTRUCTOR/ADMIN full, `notes` ADMIN only). T-06-17, T-06-35, T-06-36 (`ACTIVITIES` off → no `ACTIVITY` cell and `reason = ACTIVITY` 400; `COURSES` off → no `activeSetupId`/`placementId`; `FREE_TRAINING` off → no `RING_TRAINING_CONFLICT` even with old `training_bookings` documents).
9. **Notifications in the dispatcher** (§8; `DomainEventHandler` beans with durable names `notifications.N-08a`, `notifications.N-08b`; idempotent per (`eventId`, recipient, channel) with deterministic notification ids): N-08a on `ClassCancelledByClub` with `reason ∈ {CLUB_MANUAL, DELETED, ACTIVITY}` and `affected ∪ waitlistIds ≠ ∅` (`RISK_REVIEW` is S15's N-17): one message per dog to each registrant and each waitlist entry → APP (`SystemNotificationService.appOnce`) + EMAIL (`sendOnce` with `notif.N-08a.title/body`, or the applicant path for a member without account) + **SMS intent**: a `Notification` row `channel = SMS`, `status = QUEUED` (no sender until E7/Twilio; the queued body is the rendered `notif.N-08a.sms` ≤ 160 GSM-7, admin text truncated with «…»), or `SKIPPED_MODULE_OFF` when `SMS` is off — add `SKIPPED_BY_PREFERENCE` and `SKIPPED_MODULE_OFF` to `Notification.Status` (catalog rule 7); instructors of the class → APP+EMAIL; ADMINS → APP; action `CHANGE_CLASS`; variables `dog_name`, `class_date`, `class_time`, `class_description` (= `displayDescription` in the **recipient's** locale), `ring_name`, `admin_text`, dates formatted with the club time zone in the recipient's locale. N-08b on `ClassSessionUpdated` with `bookedCount > 0` and `diff ∋ startTime | ringId | instructorIds`: registrants (port) → APP+EMAIL+SMS intent, instructors old and new → APP; action `OPEN_BOOKING`; `changes` = «Hora: 18:50 → 19:00 · Pista: Central → Muntanya» in the recipient's locale; level/capacity/description changes never notify. Default `ca` SMS bodies from §8 in `messages_ca.properties` (`notif.N-08a.sms`, `notif.N-08b.sms`), `es`/`en` equivalents ≤ 160 GSM-7; `scheduling.autoCancel.text` (used by S15). T-06-14 (6 students APP+EMAIL+SMS with `[[admin_text]]`, instructor APP+EMAIL, admins APP), T-06-34 (recipient `en` gets «D and up» and an English `class_date`).
10. **Audit**: add `WEEK_VALIDATED`, `CLASS_CANCELLED`, `CLASS_UPDATED_WITH_BOOKINGS`, `CLASS_RISK_EXEMPTION_CHANGED`, `RING_BLOCK_CREATED`, `RING_BLOCK_CANCELLED` to `AuditAction` (all listed in S14 R-14-09; no new action), each with a `@AuditCovers` test; impersonated calls never reach these ADMIN routes.
11. **Roles and tenant** (T-06-21, every §6 route of P2 and P3): allowed role 2xx, each denied role 403 (INSTRUCTOR on generation/validation/cancellation/class writes, MEMBER on `GET /class-sessions`, `POST /ring-blocks`, `view=instructor`), impersonation token on ADMIN routes → 403, foreign-club template/week/class/block → 404, `day-grid` never leaks another club's classes.

## Fixed conventions
- Statuses (catalog rule 0, literal; the catalog file wins): `INVALID_STATE`, `STALE_VERSION`, `RING_BLOCK_CONFLICT`, `IDEMPOTENCY_KEY_REUSED` 409 · `NOTHING_TO_VALIDATE`, `WEEK_INCONSISTENT`, `CAPACITY_BELOW_BOOKINGS`, `ADMIN_TEXT_REQUIRED`, `RING_HAS_BOOKINGS`, `RING_BLOCK_MANAGED_BY_ACTIVITY`, `OUTSIDE_OPENING_HOURS`, `LEVEL_REQUIRED`, `TOO_MANY_INSTRUCTORS`, `DESCRIPTION_REQUIRED` 422 · `INVALID_TIME_RANGE`, `INVALID_SLOT_GRANULARITY`, `VALIDATION_ERROR` 400 · `MODULE_DISABLED`, `NOT_FOUND` 404 — S06's narrative writes 409 for several 422 codes: ignore it.
- Every transaction of this task is a Mongo transaction (`TransactionTemplate`/`@Transactional` with the replica set); ports run inside it; events through `EventPublisher` in the same transaction; notifications only from the outbox consumers, never from services; `Clock` injected; instants UTC, business dates club-local.
- Assumptions in force: the student never sees counts (A22(e), §13-2); [ELIMINA] = `DELETED` and shows under «Anul·lades» with its reason (§13-3); validation is blocked by inconsistencies (§13-4); the risk-exemption toggle is a chip on the selected class (§13-6); a block over a class is refused interactively, `RING_BLOCKED` only for generated/moved classes (§13-11); R-15-18's grace minutes refine R-06-09's «`endsAt < ara`». S15's 7:30 review, N-16/N-17 and `ClassBelowMinimum` are E5: only the evaluator and the cancellation use case are exposed here.
- Closed catalogs: no new parameter/event/notification/error/audit action; proposals in the report + `MESSAGES.md`. Statuses per `CATALEG_ERRORS.md` (16-09): `BAND_OVERLAP`/`RING_BLOCK_CONFLICT` 409, `WEEK_ALREADY_GENERATED`/`BAND_NOT_EMPTY` 409, `TEMPLATE_INCONSISTENT`/`WEEK_INCONSISTENT`/`NOTHING_TO_VALIDATE`/`RING_HAS_BOOKINGS`/`RING_BLOCK_MANAGED_BY_ACTIVITY`/`RING_BLOCKED` 422. `RING_BLOCKED` is thrown by the manual class creation/edition (`POST /class-sessions`, `PATCH /class-sessions/{id}` moving a class) onto a ring blocked at that time; generation records it as an `Inconsistency` instead. Recipients are resolved through the census/identity application services (reuse), never by reading other contexts' collections.
- Evidence rule: report = command + exit code + last 40 lines; long outputs → `roadmap/evidence/E4-T03/NN-<name>.log`; task file ≤ 120 KB; fictional data only (`@example.test`); no secrets; never edit other tasks, `ROADMAP.md` or the Organizer verification section.

## Verification (run all; evidence = command + exit code + last 40 lines; long outputs → `roadmap/evidence/<ID>/*.log`)
- `./mvnw -q verify` green with T-06-06, 07, 08, 12, 13, 14, 15, 16, 17, 21, 23, 24, 25, 32, 34 (N-08a part), 35, 36 (names carry the ids; T-06-06/07/08 without Spring or Mongo), ArchUnit (context boundaries: `scheduling` imports no `activities`/`bookings`/`training` classes; no cycles), `AuditContractTest` with the six new actions covered, `EventCatalogContractTest`, `E4ContractIT` updated (P3 routes no longer 501), message parity ca/es/en (`notif.N-08a.*`, `notif.N-08b.*`, `scheduling.risk.*`, `scheduling.autoCancel.text`), OpenAPI snapshot byte-identical to a fresh `bin/openapi-snapshot` (or a `docs/openapi/CHANGELOG.md` entry for any field you had to add), coverage gates.

## Definition of Done
A validated week is visible to members; an active class with (doubled) registrants is cancelled in one transaction with `ClassCancelledByClub` in the outbox and N-08a rows (APP, EMAIL, SMS intent) in `notifications`; ring blocks from D4/24 conflict correctly and `syncForActivity` is ready for E4-T04; forms B and D serve 10/23/D4; S15 has `RiskEvaluator`, `cancel(RISK_REVIEW)`, `findActiveBetween` and `finishEnded`.

## Evidence to return
`verify` tail; the T-06 method list; a `curl` sequence on the local stack: validate the E4-T02 week → `GET /day-grid?view=member` (classes visible, no `occupancy`) → `POST /ring-blocks` (201, then a conflicting one → 409) → cancel a class with the in-memory double replaced by seeded doubles (or a draft) → outbox events and `notifications` rows (tokens/ids truncated).

---

## Executor report

Completed the remaining 15 S06 operations and resumed the previously published
`in_progress` implementation. E4-T02 was already `verified` at session start.
This session works only on E4-T03; the S07 activity endpoints remain their
existing stubs. Runtime S08/S09 adapters and the S15 scheduler stay in E5.

### Branch / commits

Working-tree edits on `main`; no git write command, branch, commit or push.
The external publish script owns publication after this session.

### Evidence (full command outputs)

Maven environment: Java 21, Docker replica sets, writable wrapper/cache paths:
`MAVEN_USER_HOME=/private/tmp/e4-t03-maven` and
`MAVEN_OPTS=-Dmaven.repo.local=/private/tmp/e4-t01-maven/repository`.
These exports avoid the sandbox's read-only home Maven wrapper directory.
Commands below ran from the repository root with these exports; no tests or
gates were skipped in the final `verify`.

Earlier attempts (only final successful tails are expanded below):

- `./mvnw -q -Dtest=CalendarRulesTest -Dit.test=CalendarIT,E4ContractIT verify`, exit 1, [01-targeted.log](../evidence/E4-T03/01-targeted.log): fixed CGLIB fixture access, nullable privacy fields, Mongo conflict translation and stale fixture versions.
- Same command, exit 1, [02-targeted.log](../evidence/E4-T03/02-targeted.log): overlapping Maven compilations removed a test class during context startup; subsequent builds ran sequentially.
- `./mvnw -q -Dtest=CalendarRulesTest,AuditContractTest,CoverageQueryTest,ArchitectureTest,EventCatalogContractTest test`, exit 0, [03-contracts.log](../evidence/E4-T03/03-contracts.log).
- First command again, exit 1, [04-targeted.log](../evidence/E4-T03/04-targeted.log): four audit-row assertions exposed missing `@AuditField` metadata; added audited persistence fields.
- `./mvnw -q -Dtest=CalendarRulesTest,SchedulingTransactionsTest -Dit.test=CalendarIT,E4ContractIT verify`, exit 1, [05-targeted.log](../evidence/E4-T03/05-targeted.log): all 75 integration checks passed; this selected suite alone did not meet whole-repository coverage gates.
- `./mvnw -q verify`, exit 1, [06-verify.log](../evidence/E4-T03/06-verify.log): 557 integration tests, one stale OpenAPI snapshot failure; regenerated the 15 changed operation descriptions.
- `python3 roadmap/evidence/E4-T03/smoke.py`, exit 1, [07-smoke.log](../evidence/E4-T03/07-smoke.log): corrected the rehearsal's band ID extraction from the existing `WeekTemplate` response. Disposable services/volumes were removed.

- `python3 roadmap/evidence/E4-T03/smoke.py`, exit 1, [10-smoke.log](../evidence/E4-T03/10-smoke.log): generated/validated week and member privacy passed; added the required `Idempotency-Key` to the ring-block curl calls. The final rehearsal reuses this current-tree image; no main code changed afterward.

**Command:** `./mvnw -q verify`

**Exit code:** `0`. Full output: [09-verify.log](../evidence/E4-T03/09-verify.log). Last 40 lines:

```text
2026-09-19T11:05:00.395+02:00  INFO 95470 --- [agilityhub-core-api] [           main] t.c.s.AnnotationConfigContextLoaderUtils : Could not detect default configuration classes for test class [com.agilityhub.core.migration.PlayoffMigrationIT]: PlayoffMigrationIT does not declare any static, non-private, non-final, nested classes annotated with @Configuration.
2026-09-19T11:05:00.406+02:00  INFO 95470 --- [agilityhub-core-api] [           main] .b.t.c.SpringBootTestContextBootstrapper : Found @SpringBootConfiguration com.agilityhub.core.CoreApplication for test class com.agilityhub.core.migration.PlayoffMigrationIT

  .   ____          _            __ _ _
 /\\ / ___'_ __ _ _(_)_ __  __ _ \ \ \ \
( ( )\___ | '_ | '_| | '_ \/ _` | \ \ \ \
 \\/  ___)| |_)| | | | | || (_| |  ) ) ) )
  '  |____| .__|_| |_|_| |_\__, | / / / /
 =========|_|==============|___/=/_/_/_/

 :: Spring Boot ::               (v3.5.16)

2026-09-19T11:05:00.424+02:00  INFO 95470 --- [agilityhub-core-api] [           main] c.a.core.migration.PlayoffMigrationIT    : Starting PlayoffMigrationIT using Java 21.0.12.1 with PID 95470 (started by jordib in /Users/jordib/dev/agilityhub/agilityhub-core-api)
2026-09-19T11:05:00.424+02:00  INFO 95470 --- [agilityhub-core-api] [           main] c.a.core.migration.PlayoffMigrationIT    : The following 1 profile is active: "test"
2026-09-19T11:05:00.550+02:00  INFO 95470 --- [agilityhub-core-api] [           main] .s.d.r.c.RepositoryConfigurationDelegate : Bootstrapping Spring Data MongoDB repositories in DEFAULT mode.
2026-09-19T11:05:00.565+02:00  INFO 95470 --- [agilityhub-core-api] [           main] .s.d.r.c.RepositoryConfigurationDelegate : Finished Spring Data repository scanning in 14 ms. Found 0 MongoDB repository interfaces.
2026-09-19T11:05:00.593+02:00  INFO 95470 --- [agilityhub-core-api] [           main] org.mongodb.driver.client                : MongoClient with metadata {"driver": {"name": "mongo-java-driver|sync|spring-boot", "version": "5.5.2"}, "os": {"type": "Darwin", "name": "Mac OS X", "architecture": "aarch64", "version": "26.4.1"}, "platform": "Java/Homebrew/21.0.12.1"} created with settings MongoClientSettings{readPreference=primary, writeConcern=WriteConcern{w=null, wTimeout=null ms, journal=null}, retryWrites=true, retryReads=true, readConcern=ReadConcern{level=null}, credential=null, transportSettings=null, commandListeners=[io.micrometer.core.instrument.binder.mongodb.MongoMetricsCommandListener@1a500ad2], codecRegistry=ProvidersCodecRegistry{codecProviders=[ValueCodecProvider{}, BsonValueCodecProvider{}, DBRefCodecProvider{}, DBObjectCodecProvider{}, DocumentCodecProvider{}, CollectionCodecProvider{}, IterableCodecProvider{}, MapCodecProvider{}, GeoJsonCodecProvider{}, GridFSFileCodecProvider{}, Jsr310CodecProvider{}, JsonObjectCodecProvider{}, BsonCodecProvider{}, EnumCodecProvider{}, com.mongodb.client.model.mql.ExpressionCodecProvider@2ae4a15a, com.mongodb.Jep395RecordCodecProvider@31311a5, com.mongodb.KotlinCodecProvider@43e32f01]}, loggerSettings=LoggerSettings{maxDocumentLength=1000}, clusterSettings={hosts=[localhost:54922], srvServiceName=mongodb, mode=SINGLE, requiredClusterType=UNKNOWN, requiredReplicaSetName='null', serverSelector='null', clusterListeners='[]', serverSelectionTimeout='30000 ms', localThreshold='15 ms'}, socketSettings=SocketSettings{connectTimeoutMS=10000, readTimeoutMS=0, receiveBufferSize=0, proxySettings=ProxySettings{host=null, port=null, username=null, password=null}}, heartbeatSocketSettings=SocketSettings{connectTimeoutMS=10000, readTimeoutMS=10000, receiveBufferSize=0, proxySettings=ProxySettings{host=null, port=null, username=null, password=null}}, connectionPoolSettings=ConnectionPoolSettings{maxSize=100, minSize=0, maxWaitTimeMS=120000, maxConnectionLifeTimeMS=0, maxConnectionIdleTimeMS=0, maintenanceInitialDelayMS=0, maintenanceFrequencyMS=60000, connectionPoolListeners=[io.micrometer.core.instrument.binder.mongodb.MongoMetricsConnectionPoolListener@404c3792], maxConnecting=2}, serverSettings=ServerSettings{heartbeatFrequencyMS=10000, minHeartbeatFrequencyMS=500, serverMonitoringMode=AUTO, serverListeners='[]', serverMonitorListeners='[]'}, sslSettings=SslSettings{enabled=false, invalidHostNameAllowed=false, context=null}, applicationName='null', compressorList=[], uuidRepresentation=JAVA_LEGACY, serverApi=null, autoEncryptionSettings=null, dnsClient=null, inetAddressResolver=null, contextProvider=null, timeoutMS=null}
2026-09-19T11:05:00.595+02:00  INFO 95470 --- [agilityhub-core-api] [localhost:54922] org.mongodb.driver.cluster               : Monitor thread successfully connected to server with description ServerDescription{address=localhost:54922, type=REPLICA_SET_PRIMARY, cryptd=false, state=CONNECTED, ok=true, minWireVersion=0, maxWireVersion=21, maxDocumentSize=16777216, logicalSessionTimeoutMinutes=30, roundTripTimeNanos=1420000, minRoundTripTimeNanos=0, setName='docker-rs', canonicalAddress=e3dafa53bf55:27017, hosts=[e3dafa53bf55:27017], passives=[], arbiters=[], primary='e3dafa53bf55:27017', tagSet=TagSet{[]}, electionId=7fffffff0000000000000001, setVersion=1, topologyVersion=TopologyVersion{processId=6aae4fa2d35ca0d83a3cdcb9, counter=6}, lastWriteDate=Sat Sep 19 11:05:00 CEST 2026, lastUpdateTimeNanos=554065187150791}
2026-09-19T11:05:01.085+02:00  INFO 95470 --- [agilityhub-core-api] [           main] c.a.core.migration.PlayoffMigrationIT    : Started PlayoffMigrationIT in 0.668 seconds (process running for 157.101)
2026-09-19T11:05:09.388+02:00  INFO 95470 --- [agilityhub-core-api] [localhost:55146] org.mongodb.driver.cluster               : Exception in monitor thread while connecting to server localhost:55146

com.mongodb.MongoSocketOpenException: Exception opening socket
	at com.mongodb.internal.connection.SocketStream.lambda$open$0(SocketStream.java:85) ~[mongodb-driver-core-5.5.2.jar:na]
	at java.base/java.util.Optional.orElseThrow(Optional.java:403) ~[na:na]
	at com.mongodb.internal.connection.SocketStream.open(SocketStream.java:85) ~[mongodb-driver-core-5.5.2.jar:na]
	at com.mongodb.internal.connection.InternalStreamConnection.open(InternalStreamConnection.java:233) ~[mongodb-driver-core-5.5.2.jar:na]
	at com.mongodb.internal.connection.DefaultServerMonitor$ServerMonitor.lookupServerDescription(DefaultServerMonitor.java:219) ~[mongodb-driver-core-5.5.2.jar:na]
	at com.mongodb.internal.connection.DefaultServerMonitor$ServerMonitor.run(DefaultServerMonitor.java:176) ~[mongodb-driver-core-5.5.2.jar:na]
Caused by: java.net.ConnectException: Connection refused
	at java.base/sun.nio.ch.Net.pollConnect(Native Method) ~[na:na]
	at java.base/sun.nio.ch.Net.pollConnectNow(Net.java:694) ~[na:na]
	at java.base/sun.nio.ch.NioSocketImpl.timedFinishConnect(NioSocketImpl.java:542) ~[na:na]
	at java.base/sun.nio.ch.NioSocketImpl.connect(NioSocketImpl.java:592) ~[na:na]
	at java.base/java.net.SocksSocketImpl.connect(SocksSocketImpl.java:327) ~[na:na]
	at java.base/java.net.Socket.connect(Socket.java:751) ~[na:na]
	at com.mongodb.internal.connection.SocketStreamHelper.initialize(SocketStreamHelper.java:76) ~[mongodb-driver-core-5.5.2.jar:na]
	at com.mongodb.internal.connection.SocketStream.initializeSocket(SocketStream.java:104) ~[mongodb-driver-core-5.5.2.jar:na]
	at com.mongodb.internal.connection.SocketStream.open(SocketStream.java:79) ~[mongodb-driver-core-5.5.2.jar:na]
	... 3 common frames omitted

```


**Command:** `bin/openapi-snapshot`

**Exit code:** `0`. Full output: [08-openapi.log](../evidence/E4-T03/08-openapi.log). Last 40 lines:

```text
 =========|_|==============|___/=/_/_/_/

 :: Spring Boot ::               (v3.5.16)

2026-09-19T11:01:36.806+02:00  INFO 95138 --- [agilityhub-core-api] [           main] c.a.c.configuration.OpenApiSnapshotTest  : Starting OpenApiSnapshotTest using Java 21.0.12.1 with PID 95138 (started by jordib in /Users/jordib/dev/agilityhub/agilityhub-core-api)
2026-09-19T11:01:36.807+02:00  INFO 95138 --- [agilityhub-core-api] [           main] c.a.c.configuration.OpenApiSnapshotTest  : The following 1 profile is active: "test"
2026-09-19T11:01:37.627+02:00  INFO 95138 --- [agilityhub-core-api] [           main] .s.d.r.c.RepositoryConfigurationDelegate : Bootstrapping Spring Data MongoDB repositories in DEFAULT mode.
2026-09-19T11:01:37.664+02:00  INFO 95138 --- [agilityhub-core-api] [           main] .s.d.r.c.RepositoryConfigurationDelegate : Finished Spring Data repository scanning in 33 ms. Found 0 MongoDB repository interfaces.
2026-09-19T11:01:38.108+02:00  INFO 95138 --- [agilityhub-core-api] [           main] o.s.b.w.embedded.tomcat.TomcatWebServer  : Tomcat initialized with port 0 (http)
2026-09-19T11:01:38.118+02:00  INFO 95138 --- [agilityhub-core-api] [           main] o.apache.catalina.core.StandardService   : Starting service [Tomcat]
2026-09-19T11:01:38.119+02:00  INFO 95138 --- [agilityhub-core-api] [           main] o.apache.catalina.core.StandardEngine    : Starting Servlet engine: [Apache Tomcat/10.1.55]
2026-09-19T11:01:38.149+02:00  INFO 95138 --- [agilityhub-core-api] [           main] o.a.c.c.C.[Tomcat].[localhost].[/]       : Initializing Spring embedded WebApplicationContext
2026-09-19T11:01:38.150+02:00  INFO 95138 --- [agilityhub-core-api] [           main] w.s.c.ServletWebServerApplicationContext : Root WebApplicationContext: initialization completed in 1336 ms
2026-09-19T11:01:38.279+02:00  INFO 95138 --- [agilityhub-core-api] [           main] org.mongodb.driver.client                : MongoClient with metadata {"driver": {"name": "mongo-java-driver|sync|spring-boot", "version": "5.5.2"}, "os": {"type": "Darwin", "name": "Mac OS X", "architecture": "aarch64", "version": "26.4.1"}, "platform": "Java/Homebrew/21.0.12.1"} created with settings MongoClientSettings{readPreference=primary, writeConcern=WriteConcern{w=null, wTimeout=null ms, journal=null}, retryWrites=true, retryReads=true, readConcern=ReadConcern{level=null}, credential=null, transportSettings=null, commandListeners=[io.micrometer.core.instrument.binder.mongodb.MongoMetricsCommandListener@3d68b925], codecRegistry=ProvidersCodecRegistry{codecProviders=[ValueCodecProvider{}, BsonValueCodecProvider{}, DBRefCodecProvider{}, DBObjectCodecProvider{}, DocumentCodecProvider{}, CollectionCodecProvider{}, IterableCodecProvider{}, MapCodecProvider{}, GeoJsonCodecProvider{}, GridFSFileCodecProvider{}, Jsr310CodecProvider{}, JsonObjectCodecProvider{}, BsonCodecProvider{}, EnumCodecProvider{}, com.mongodb.client.model.mql.ExpressionCodecProvider@250f839, com.mongodb.Jep395RecordCodecProvider@79a91206, com.mongodb.KotlinCodecProvider@1e85cf67]}, loggerSettings=LoggerSettings{maxDocumentLength=1000}, clusterSettings={hosts=[localhost:54871], srvServiceName=mongodb, mode=SINGLE, requiredClusterType=UNKNOWN, requiredReplicaSetName='null', serverSelector='null', clusterListeners='[]', serverSelectionTimeout='30000 ms', localThreshold='15 ms'}, socketSettings=SocketSettings{connectTimeoutMS=10000, readTimeoutMS=0, receiveBufferSize=0, proxySettings=ProxySettings{host=null, port=null, username=null, password=null}}, heartbeatSocketSettings=SocketSettings{connectTimeoutMS=10000, readTimeoutMS=10000, receiveBufferSize=0, proxySettings=ProxySettings{host=null, port=null, username=null, password=null}}, connectionPoolSettings=ConnectionPoolSettings{maxSize=100, minSize=0, maxWaitTimeMS=120000, maxConnectionLifeTimeMS=0, maxConnectionIdleTimeMS=0, maintenanceInitialDelayMS=0, maintenanceFrequencyMS=60000, connectionPoolListeners=[io.micrometer.core.instrument.binder.mongodb.MongoMetricsConnectionPoolListener@665cfc03], maxConnecting=2}, serverSettings=ServerSettings{heartbeatFrequencyMS=10000, minHeartbeatFrequencyMS=500, serverMonitoringMode=AUTO, serverListeners='[]', serverMonitorListeners='[]'}, sslSettings=SslSettings{enabled=false, invalidHostNameAllowed=false, context=null}, applicationName='null', compressorList=[], uuidRepresentation=JAVA_LEGACY, serverApi=null, autoEncryptionSettings=null, dnsClient=null, inetAddressResolver=null, contextProvider=null, timeoutMS=null}
2026-09-19T11:01:38.287+02:00  INFO 95138 --- [agilityhub-core-api] [localhost:54871] org.mongodb.driver.cluster               : Monitor thread successfully connected to server with description ServerDescription{address=localhost:54871, type=REPLICA_SET_PRIMARY, cryptd=false, state=CONNECTED, ok=true, minWireVersion=0, maxWireVersion=21, maxDocumentSize=16777216, logicalSessionTimeoutMinutes=30, roundTripTimeNanos=16327667, minRoundTripTimeNanos=0, setName='docker-rs', canonicalAddress=2a9629758713:27017, hosts=[2a9629758713:27017], passives=[], arbiters=[], primary='2a9629758713:27017', tagSet=TagSet{[]}, electionId=7fffffff0000000000000001, setVersion=1, topologyVersion=TopologyVersion{processId=6aae4f6f897cdd88895941e0, counter=6}, lastWriteDate=Sat Sep 19 11:01:36 CEST 2026, lastUpdateTimeNanos=553862876762041}
2026-09-19T11:01:38.784+02:00  INFO 95138 --- [agilityhub-core-api] [           main] o.s.boot.web.servlet.RegistrationBean    : Filter rateLimitFilter was not registered (disabled)
2026-09-19T11:01:40.731+02:00  INFO 95138 --- [agilityhub-core-api] [           main] o.a.c.c.C.[Tomcat].[localhost].[/]       : Initializing Spring TestDispatcherServlet ''
2026-09-19T11:01:40.731+02:00  INFO 95138 --- [agilityhub-core-api] [           main] o.s.t.web.servlet.TestDispatcherServlet  : Initializing Servlet ''
2026-09-19T11:01:40.732+02:00  INFO 95138 --- [agilityhub-core-api] [           main] o.s.t.web.servlet.TestDispatcherServlet  : Completed initialization in 1 ms
2026-09-19T11:01:40.797+02:00  INFO 95138 --- [agilityhub-core-api] [           main] o.s.b.w.embedded.tomcat.TomcatWebServer  : Tomcat started on port 54877 (http) with context path '/'
2026-09-19T11:01:40.836+02:00  INFO 95138 --- [agilityhub-core-api] [           main] o.s.b.w.embedded.tomcat.TomcatWebServer  : Tomcat initialized with port 0 (http)
2026-09-19T11:01:40.837+02:00  INFO 95138 --- [agilityhub-core-api] [           main] o.apache.catalina.core.StandardService   : Starting service [Tomcat]
2026-09-19T11:01:40.837+02:00  INFO 95138 --- [agilityhub-core-api] [           main] o.apache.catalina.core.StandardEngine    : Starting Servlet engine: [Apache Tomcat/10.1.55]
2026-09-19T11:01:40.848+02:00  INFO 95138 --- [agilityhub-core-api] [           main] o.a.c.c.C.[Tomcat-1].[localhost].[/]     : Initializing Spring embedded WebApplicationContext
2026-09-19T11:01:40.848+02:00  INFO 95138 --- [agilityhub-core-api] [           main] w.s.c.ServletWebServerApplicationContext : Root WebApplicationContext: initialization completed in 49 ms
2026-09-19T11:01:40.856+02:00  INFO 95138 --- [agilityhub-core-api] [           main] o.s.b.a.e.web.EndpointLinksResolver      : Exposing 2 endpoints beneath base path '/actuator'
2026-09-19T11:01:40.875+02:00  INFO 95138 --- [agilityhub-core-api] [           main] o.s.b.w.embedded.tomcat.TomcatWebServer  : Tomcat started on port 54878 (http) with context path '/'
2026-09-19T11:01:40.885+02:00  INFO 95138 --- [agilityhub-core-api] [           main] c.a.c.configuration.OpenApiSnapshotTest  : Started OpenApiSnapshotTest in 4.291 seconds (process running for 7.236)
Mockito is currently self-attaching to enable the inline-mock-maker. This will no longer work in future releases of the JDK. Please add Mockito as an agent to your build as described in Mockito's documentation: https://javadoc.io/doc/org.mockito/mockito-core/latest/org.mockito/org/mockito/Mockito.html#0.3
WARNING: A Java agent has been loaded dynamically (/private/tmp/e4-t01-maven/repository/net/bytebuddy/byte-buddy-agent/1.17.8/byte-buddy-agent-1.17.8.jar)
WARNING: If a serviceability tool is in use, please run with -XX:+EnableDynamicAgentLoading to hide this warning
WARNING: If a serviceability tool is not in use, please run with -Djdk.instrument.traceUsage for more information
WARNING: Dynamic loading of agents will be disallowed by default in a future release
OpenJDK 64-Bit Server VM warning: Sharing is only supported for boot loader classes because bootstrap classpath has been appended
2026-09-19T11:01:43.036+02:00  INFO 95138 --- [agilityhub-core-api] [           main] o.springdoc.api.AbstractOpenApiResource  : Init duration for springdoc-openapi is: 1393 ms
2026-09-19T11:01:43.501+02:00  INFO 95138 --- [agilityhub-core-api] [omcat-handler-0] o.a.c.c.C.[Tomcat].[localhost].[/]       : Initializing Spring DispatcherServlet 'dispatcherServlet'
2026-09-19T11:01:43.502+02:00  INFO 95138 --- [agilityhub-core-api] [omcat-handler-0] o.s.web.servlet.DispatcherServlet        : Initializing Servlet 'dispatcherServlet'
2026-09-19T11:01:43.502+02:00  INFO 95138 --- [agilityhub-core-api] [omcat-handler-0] o.s.web.servlet.DispatcherServlet        : Completed initialization in 0 ms
2026-09-19T11:01:44.377+02:00  INFO 95138 --- [agilityhub-core-api] [           main] o.springdoc.api.AbstractOpenApiResource  : Init duration for springdoc-openapi is: 817 ms
Updated docs/openapi/openapi.json
```


**Command:** `python3 roadmap/evidence/E4-T03/smoke.py --image agilityhub-e4-t03-smoke:local`

**Exit code:** `0`. Full output: [11-smoke.log](../evidence/E4-T03/11-smoke.log). Last 26 lines:

```text
Starting disposable consumer image Compose stack
PASS club:apply seeds/club-canic.yaml run 1
PASS club:apply seeds/club-canic.yaml run 2: 0 changes
PASS seed:demo --club=canic --seed=42 run 1
PASS seed:demo --club=canic --seed=42 run 2: 0 changes
curl GET /api/v1/health -> 200
PASS private local mailbox and attachment volumes ready; fixture club activated
curl POST /oauth2/token -> 200
curl POST /oauth2/token -> 200
curl POST /api/v1/week-templates -> 201
curl POST /api/v1/week-templates/[id truncated]/bands -> 201
curl POST /api/v1/week-templates/[id truncated]/classes -> 201
curl POST /api/v1/weeks -> 201
curl POST /api/v1/weeks/[id truncated]/generation -> 200
curl GET /api/v1/day-grid?date=2026-09-21&view=member -> 200
curl POST /api/v1/weeks/[id truncated]/validation -> 200
curl GET /api/v1/day-grid?date=2026-09-21&view=member -> 200
PASS generated DRAFT week -> validation -> member CLASS cell, occupancy absent
curl POST /api/v1/ring-blocks -> 201
curl POST /api/v1/ring-blocks -> 409
curl POST /api/v1/class-sessions -> 201
curl POST /api/v1/class-sessions/[id truncated]/cancellation -> 200
curl POST /api/v1/class-sessions/[id truncated]/cancellation -> 200
PASS outbox: WeekValidated=1, ClassCancelledByClub=1, RingBlockCreated=1; N-08a rows=0 for an empty draft
PASS registrant delivery is covered by CalendarIT: 4 bookings + 2 waitlist, 21 N-08a rows, 2 PackRefunded events
Removed disposable Compose services, volumes and private mailbox; existing stacks preserved
```


**Command:** `python3 roadmap/evidence/E4-T03/results.py`

**Exit code:** `0`. Full output: [12-results.log](../evidence/E4-T03/12-results.log). Last 26 lines:

```text
surefire: {'tests': 381, 'failures': 0, 'errors': 0, 'skipped': 0}
failsafe: {'tests': 557, 'failures': 0, 'errors': 0, 'skipped': 0}
T-06 methods in CalendarRulesTest/CalendarIT:
  T_06_14_smsUsesAtMost160BasicGsmSeptetsIncludingTruncation
  T_06_08_exhaustiveStateMachinesAndTerminalNotesOnly
  T_06_06_riskBoundariesAndAllRecipientLocales
  T_06_07_localInstantsAndReviewTimesAcrossBothDstTransitionsAndZones
  T_06_12_validateAtomicallyAndExposeDraftsOnlyAfterValidation
  T_06_25_finishingGraceIsIdempotentAndRiskReviewReusesCancellationWithoutN08a
  T_06_23_T_06_24_validationPatchAndCancellationRacesSerializeWithoutDrafts
  T_06_16_T_06_32_ringBlockLifecycleConflictsTimeRulesRolesAndRedaction
  T_06_13_editPresenceCapacityVersionExemptionAndLocalizedChangeNotifications
  T_06_14_deletedDraftApplicantAndSmsOffAreDistinctDeliveryCases
  T_06_16_activitySyncJoinsCallerTransactionMovesAndCancelsWithFullRollback
  T_06_16_trainingConflictsCancelAtomicallyOnlyForAdmin
  T_06_12_T_06_36_validationSeesTrainingAndActiveBlocksAndHonorsModuleRemoval
  T_06_17_T_06_35_T_06_36_gridVisibilityModulesNoRingAndForeignTenant
  T_06_25_dashboardCoverageAndCliUseRealSessionsAndEnforceTenant
  T_06_14_T_06_15_T_06_34_cancellationRollsBackAndDeliversEveryDogInRecipientLocaleOnce
com.agilityhub.core.clubs.scheduling.application.ports: LINE 16/18 (88.89%)
com.agilityhub.core.clubs.scheduling.api: LINE 176/181 (97.24%)
com.agilityhub.core.clubs.scheduling.application: LINE 783/795 (98.49%)
com.agilityhub.core.clubs.scheduling.application: BRANCH 616/719 (85.67%)
com.agilityhub.core.clubs.scheduling.domain: LINE 181/181 (100.00%)
com.agilityhub.core.clubs.scheduling.domain: BRANCH 220/230 (95.65%)
PASS all coverage gates; generated OpenAPI byte-identical to reviewed snapshot
```


**Command:** `git diff --check`

**Exit code:** `0`. Full output: [13-diff-check.log](../evidence/E4-T03/13-diff-check.log). Last 0 lines (command produced no output).


The successful quiet Maven run ends with a Mongo monitor connection-refused
message during Testcontainers shutdown. Failsafe reports zero failures/errors;
all coverage gates pass. The results summary excludes the duplicate Surefire
copy left by the explicit OpenAPI generation command: 381 unit/contract tests
and 557 integration tests, with no failures, errors or skipped tests.


**Command:** `python3 roadmap/tools/check.py --set E4-T03 awaiting_verification`

**Exit code:** `0`. Full output: [14-status.log](../evidence/E4-T03/14-status.log). Last 2 lines:

```text
E4-T03: status -> awaiting_verification
STATUS.md rendered (51 tasks)
```

### Files changed

- `clubs/scheduling/application`: week validation, cancellation, ring-block lifecycle and mandatory activity synchronization; calendar/day-grid and list projections; durable N-08a/N-08b consumers; dashboard/risk adapters and `FinishEndedCommand`. Continued the existing class service/domain/event/audit implementation and hardened nested Mongo conflict handling.
- `clubs/scheduling/application/ports`: historical waitlist resolution on the replaceable booking port; existing booking/training/activity null objects remain replaceable. `CoverageQuery` now counts current booked dogs through the S08 port.
- `clubs/scheduling/api/{SchedulingController,SchedulingRequests,SchedulingContracts}`: implemented P3 routes, presence-aware nullable patches, role/tenant checks and omission of private optional fields.
- `clubs/scheduling/persistence/{ClassSession,Week,RingBlock}`: audited fields and retained `finishedAt`; compatible constructors preserve earlier fixtures.
- `clubs/census/application/SchedulingRecipients`, `clubs/catalogs/application/PlanningCatalogAccess`, `clubs/catalogs/persistence/Ring`, `identity/application/CensusIdentityService`: tenant-scoped minimal recipient/catalog reads, creator names and optional course setup metadata.
- `clubs/messaging/application/{SystemNotificationService,SystemEmailRenderer}` and `persistence/{Notification,NotificationRepository}`: N-08 email/app variables, deterministic SMS intents and catalog skip statuses.
- `platform/application/ClubConfigService`, `platform/persistence/ClubRepository`: active-club iteration for the CLI.
- `messages/messages_{ca,es,en}.properties`, `SchedulingSms`: localized risk/notification/change text and GSM-7 SMS truncation.
- `CalendarRulesTest`, `CalendarIT`, `SchedulingTransactionsTest`, `CoverageQueryTest`, `E4ContractIT`: domain, transactional port doubles, rollback, holds/refunds, concurrent writes, module/privacy and all route/tenant checks.
- `.gitignore`, `CHANGELOG.md`, `docs/openapi/{openapi.json,CHANGELOG.md}`, this task, generated `roadmap/STATUS.md`, append-only `roadmap/MESSAGES.md`, and `roadmap/evidence/E4-T03/`: reviewable evidence and task handoff. The evidence scripts reuse the existing disposable Compose harness; no E4-T05 smoke command was introduced.

### Rules and tests implemented (R-xx-nn / T-xx-nn)

- R-06-05/08/09/10/14: whole-week transactional validation, class creation/edition/state transitions, cancellation with mandatory text, pack/waitlist/hold effects through the ports, optimistic versions, idempotency and grace-based finishing. T-06-08/12/13/14/15/23/24/25.
- R-06-11, R-07-05, R-09-11/12/13: manual block constraints, force cancellation restricted to ADMIN, activity-managed blocks, preview and all-or-nothing synchronization. T-06-16/32.
- R-06-12/13/15: member/staff grids, locale descriptions, instructor visibility, risk/ICU/DST, no-ring column, course setup metadata and disabled-module variants. T-06-06/07/17/35/36.
- S06 §8/catalog N-08a/N-08b: durable per-dog/channel idempotency, locale-specific dates/description, six student APP+EMAIL+SMS intents plus instructor APP+EMAIL and admin APP (21 rows), RISK_REVIEW excluded, applicant/no-account and SMS-off paths, old/new instructor updates. T-06-13/14/34 (N-08a half).
- S14 R-14-09: real audit rows and `@AuditCovers` for all six specified actions. The full suite includes `AuditContractTest`, `EventCatalogContractTest`, `ArchitectureTest`, locale parity and snapshot comparison.
- T-06-21 in `E4ContractIT`: all S06 route role matrices, foreign-club resources, impersonation restrictions and actual P2/P3 responses.
- `CalendarRulesTest` runs T-06-06/07/08 without Spring/Mongo. Exact new T-06 method names and coverage ratios are in the results evidence below.

### Assumptions

1. Closed catalogs and transition tables take precedence over contradictory task prose. DRAFT cancellation uses `DELETED`; an ACTIVE class displaced by an activity uses `ACTIVITY`. Canonical catalog error statuses are retained.
2. Manual blocks apply `ringBlocks.maxHorizonDays` to ADMIN and INSTRUCTOR. S07 owns activity window granularity and horizon; synchronized blocks still check active rings, duration and opening hours, and all conflicts remain transactional.
3. SMS uses GSM-7-compatible transliteration plus ASCII `...` (U+2026 is outside GSM-7), at most 160 basic septets. SMS rows are intents only, with `QUEUED` or `SKIPPED_MODULE_OFF`; E7 supplies delivery/preferences. N-08a variables follow the catalog, which omits `ring_name`.
4. `waitlistEntries(ids)` must read retained CANCELLED waitlist rows so outbox retries resolve recipients after cancellation. E5 implements the concrete S08/S09 ports, pack refunds only with PACKS, hold deletion and the class-lock/recheck booking protocol. Transactional doubles prove cancellation, effects and rollback here.
5. The dashboard consumes real class/risk/occupancy data now. Historical per-recipient N-16/N-17 notification projections remain empty until E5 owns those processes and persisted notification receipts; this task emits neither N-16/N-17 nor `ClassBelowMinimum`.
6. The HTTP rehearsal uses the permitted empty-DRAFT cancellation path. Registrant delivery/refunds and forced failures are proven against the real Mongo outbox/notification collections by `CalendarIT` with booking/training doubles. No live email/SMS provider or existing user stack was used.

### Questions / catalog proposals

No new catalog item proposed. The two specification reconciliations (GSM-7
ellipsis and DRAFT activity cancellation) and the historical waitlist read
contract are recorded for @organizer in `roadmap/MESSAGES.md`; none blocks the
implementation. E4-T04 can call the mandatory activity sync/preview contracts,
and E5 can replace the conditional booking/training ports.

## Organizer verification
_(pending)_
