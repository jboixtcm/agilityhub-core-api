---
id: E5-T22
title: Last E5 follow-ups (api) — priceLabel and the member's own plan on GET /signup (and a member without a plan chooses one), waitlistRank only on waiting rows and computed once, `fields` consistent on every universal list, `id` filterable on D7's lists, docs
stage: E5
repo: agilityhub-core-api
thread: A
order: 220
status: in_progress
depends_on: [E5-T20]
branch:
pr:
updated: 2026-09-26
---

# E5-T22 · Last E5 follow-ups

## Objective
The web's E4-W12 ran screen 17 against the real core, and two of its steps need data that `GET /signup` does not send. The independent review of E5-T20 (`roadmap/reviews/E5-T20-20260926-0354-claude.md`) found three minors and some nits. The organizer confirmed and decided them all.

This is the last follow-up task of stage E5. The nits of its own review go to `docs/INCIDENCIES_OBERTES.md` for the correction pass, unless they are majors.

**Each fix comes with a test that fails before the fix.**

## Spec references
- S04: §6 `GET /signup` (`plans[].priceLabel`, `plans[].current`; amended 26-09), R-04-09 (the add-dog offer and a member without a plan; amended 26-09), T-04-32; S05 R-05-19 (the price line).
- S07 R-07-08 and §6 (`waitlistRank`).
- `CONVENCIONS_API.md` §4 (`fields`, `x-fields`).

## Context to load
- The E5-T20 review and report (question 2); the web E4-W12 report (questions 1 and 2) and its evidence `signup-plans-core.json` and `signup-add-dog-plans-core.json` (quoted in the organizer's MESSAGES entry of 26-09).
- The `GET /signup` assembly in `SignupService` (plans, `member`, `upfront.planQuotes`), and M8 in `addDog`.
- `ActivityApiService.java` (near `:58`), `ActivityProjection.java` (near `:82`), `ActivityQueryService`.
- `ListFieldsContractIT`, `@ListContract`, and the lists named in step 4.

## Steps
1. **`priceLabel` on `GET /signup` (E4-W12 question 1, S05 R-05-19).** Each plan carries `priceLabel`: `Plan.texts.priceLabel` in the reader's locale, when the plan has one. The Cànic's Teràpia reads «condicions i cost segons cada cas». IT.
2. **The member's own plan in add-dog mode (E4-W12 question 2, R-04-09, M8).**
   - `GET /signup` for a member includes the member's own plan in `plans`, marked `current: true`, even when the public offer hides it (the Cànic's family plan). The other plans keep `current: false` or absent.
   - A member without a plan (instructors migrated without one, B34) gets the offered plans, none `current`. `POST /me/dogs/signup` then requires `planIdRequested`: `400 VALIDATION_ERROR` on `planIdRequested` without it.
   - ITs for both, on the Cànic seed's family member.
3. **`waitlistRank` (E5-T20 reviews #1 and #5).**
   - D7's list sets it only on a row whose `state` is `WAITLISTED`: `null` otherwise, even if a promotion happens between the page read and the rank read.
   - `GET /me/activities` computes the ranks once per activity, not once per waiting row.
   - IT and a unit test.
4. **`fields` on every universal list (E5-T20 review #3 and question 2, `CONVENCIONS_API.md` §4).**
   - Every list that publishes `x-fields` honours `fields`: it leaves out the unrequested keys, always sends the row id, and its item schema requires only the row id. This covers `/weeks`, `/class-sessions`, `/ring-blocks`, `/training-bookings`, `/bookings` and `/jobs/{name}/runs`.
   - A list that cannot honour `fields` stops accepting the parameter and publishes no `x-fields`. This applies to the three contract-only platform lists (`/platform/audit-entries`, `/platform/erasure-requests`, `/platform/security-events`) until they are implemented.
   - Extend `ListFieldsContractIT` to check, on every universal list, that a request with `fields` gets sparse items that conform to the snapshot.
5. **`id` on D7's lists (E5-T20 review #4).** `id` goes into the `x-filterable` of `GET /activities` and `GET /activities/{id}/registrations`, which already accept it. Contract test.
6. **Docs (E5-T20 reviews #2 and #6).**
   - The `ring-conflicts` description, the E5-T20 report and its changelog entry stop saying «like the publication». The preview answers `400 INVALID_TIME_RANGE` when there is no window to block, while the publication refuses those cases earlier, with `422 ACTIVITY_INCOMPLETE`.
   - The `ActivityIT` comment near `:536` names `assertNoEnd` as the proof.

## Fixed conventions
- Contract changes as listed (steps 1, 2 and 4). Regenerate `docs/openapi/openapi.json`, and add one `docs/openapi/CHANGELOG.md` entry that tells the web what changes.
- Heavy runs go through the host lock.
- Fictional data only.

## Verification (run all; evidence = command + exit code + last 40 lines; long outputs → `roadmap/evidence/E5-T22/*.log`)
- `./mvnw -q clean verify`.
- `bin/e3-smoke` twice, on a fresh local stack.
- `bin/openapi-snapshot`, then `git diff --stat docs/openapi/`.

## Definition of Done
- Steps 1–6 are done. Steps 1–5 each have a test that fails before the fix.

## Evidence to return
- The command outputs.
- A step → test table.

---

## Executor report
_(pending)_

## Organizer verification
_(pending)_
