---
id: E2-T06
title: Census domain and endpoints: Member, Dog, FamilyGroup, documents, booking block, roles, /me/* (S03 WP-03-B)
stage: E2
repo: agilityhub-core-api
thread: A
order: 60
status: not_open
depends_on: [E2-T01, E2-T03, E2-T04, E2-T05]
branch: 
pr: 
updated: 2026-09-06
---

# E2-T06 · Census domain and endpoints: Member, Dog, FamilyGroup, documents, booking block, roles, /me/* (S03 WP-03-B)

## Objective
Work package WP-03-B: the heart of the club data — members, dogs and family groups with every rule of S03.

## Spec references
S03 §3 (all fields; `Member.status PENDING|ACTIVE|INACTIVE|LEFT`, `Dog.status PENDING|ACTIVE|INACTIVE`, `bookingBlocked`, `billingMode`, `lastSelectedDog`, documents), §4 rules R-03-01…R-03-31 (eligibility, booking block, access resend, impersonation entry, editing with `version`, payment method, roles, dog level change with future-booking warning, free-training override, transfer, deactivation/reactivation, documents and reminders, instructor note, family groups, saved views, exports policy, instructor scope) · tests **T-03-01…T-03-07, T-03-12…T-03-30** · `docs/specs/00-transversal/MATRIU_PERMISOS.md` · `docs/DECISIONS_PENDENTS.md` B13, B19, Part C S03 · `docs/MODEL_DADES_PLATAFORMA.md` §0/§3.

## Context to load
S03 §3, §4, §6, §7, §11 · MATRIU_PERMISOS · MODEL §3 · this file.

## Steps
0b. **Contract addition (organizer 06-09, from web E2-W03)**: `GET /me/dogs` embeds per dog the R-03-18 task items — `tasks: {open, completed, items[] {id, text, createdAt, instructorName, attachmentsCount, doneAt?}}` (PENDING + DONE of the last 30 days); the E2-T01 snapshot only had the summary. Completion stays S10 (`POST /tasks/{id}/completion`); until S10 exists the items come from a `tasks` collection read-only projection (empty list is fine), and the snapshot is regenerated.
0a. **Compatibility (organizer 06-09)**: E1-T04 reads members through a tenant-scoped projection `MemberIdentityAccess` over the `members` collection (`id`, `clubId`, `accountId`, `status`) and accepts `ACTIVE`/`INACTIVE` for impersonation (R-01-09). Keep those field names and make the projection use the real `Member` document of this task; `LEFT`/erased members stay denied.
0. **Catalog addition (organizer 06-09)**: every mutation of a member with `erasedAt` set (S14 §5 `displayStatus = ERASED`) answers `409 MEMBER_ERASED` (code and messages already in the repo); document it in the OpenAPI responses of the S03 mutations.
1. Entities with tenant repositories and indexes (`memberNumber` unique per club, `idDocument` unique per club, `Dog.chip` unique per club when present), `MemberStatusService`, `MemberBookingEligibility` (port for S08), `FreeTrainingPolicy` (level `grantsFreeTraining` + dog override), `DogTransferService`, `DocumentService` (S3-compatible presigned upload through `POST /attachments` contract of S10 — implement the minimal `Attachment` here if S10 has not landed), `FamilyGroupService` (holder, `billedViaMemberId` derived).
2. Endpoints of S03 §6: `/members*` (overview aggregate for D10, PATCH with `version`, payment method with masked IBAN and Stripe setup-intent placeholder, booking block on/off, access resend (`202`, rate-limited, event `AccessResent` → N-27 through the SYSTEM mail path), impersonation delegates to identity, roles via `RoleAssignmentService`), `/dogs*` (level change with `warnings.futureBookingsOutsideLevel` through a port, free-training override, transfer, deactivation/reactivation with `DOG_HAS_FUTURE_BOOKINGS` port, photo, documents + reminder), `/family-groups*`, `/me/dogs`, `/me/dogs/{id}/instructor-note`, `/me/profile` (28: read-only name fields → `VALIDATION_ERROR (READ_ONLY_FIELD)`), `/me/family-group`.
3. Masking: IBAN and id document masked outside ADMIN views as the spec says; `MemberInstructorView` for INSTRUCTOR; never `passwordHash`.
4. Events (`MemberStatusChanged`, `DogLevelChanged`, `DogTransferred`, `MemberNoteChanged`, `BookingBlockChanged`, `AccessResent`) and audit actions of S03 §7 with `@AuditCovers`.

## Verification (run all; paste full output in the report)
- `./mvnw -q verify` → green with every T-03 test listed (ids in names), tenant isolation tests for members/dogs, role tests (INSTRUCTOR read-only scope), audit coverage.

## Definition of Done
All S03 backend rules with tests; no personal data in fixtures (fictional).

## Evidence to return
`verify` output; `curl` of `GET /members/{id}/overview` for a seeded member.

---

## Executor report
_(not started)_

### Branch / commits
### Evidence (full command outputs)
### Files changed
### Rules and tests implemented (R-xx-nn / T-xx-nn)
### Assumptions
### Questions / catalog proposals

## Organizer verification
_(pending)_
