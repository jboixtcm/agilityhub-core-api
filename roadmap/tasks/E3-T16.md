---
id: E3-T16
title: Gate E3 fixes (api) — the member's document types: GET /me/dogs returns the club's document-type catalog, so screen 13 never needs /parameters (from the web E4-W08 report); the Cànic's legal identity, displayCity and the registered office on /branding for the public footer (Jordi, 25-09); small follow-ups of the E3-T15 review and the list size contract
stage: E3
repo: agilityhub-core-api
thread: A
order: 109
status: changes_requested
depends_on: [E3-T10]
branch:
pr:
updated: 2026-09-25
---

# E3-T16 · The member's document types in `GET /me/dogs`

## Objective
Screen 13 (`/gossos`, MEMBER and impersonation) has «＋ DOC.»: a dialog with the document type, a name and the file (R-03-15). The types are the club's `census.dogDocumentTypes` catalog.
- The web reads them from `GET /parameters/census.dogDocumentTypes`.
- `/parameters/*` is ADMIN only and refused under impersonation (`SettingsController`: `hasRole('ADMIN') and imp != true`; MATRIU_PERMISOS).
- On the real core the read gets 403, and the dialog falls back to the vaccination card. A member cannot upload `INSURANCE` or `OTHER`.

The web executor of E4-W08 reported it, and the organizer confirmed it in the code. S03 was amended on 25-09: `GET /me/dogs` returns `documentTypes: [{key, label, required}]`. The web adopts it in E3-W12.

Jordi also answered question 4 of E3-T10 on 25-09, and chose the public footer: the Cànic's legal identity goes into its seed, and `/branding` gains the town shown and the registered office (step 3). The web shows them in E3-W12.

This must be done before the gate re-run E3-T11, which depends on this task.

**Each fix comes with a test that fails before the fix.**

## Spec references
- S03:
  - R-03-15 (dog documents), R-03-30 (`levels.enabled=false`), R-03-32 (texts: `label` in the reader's locale, fallback `club.defaultLocale`);
  - §6 `GET /me/dogs`, T-03-26;
  - the change log entry of 25-09.
- `MATRIU_PERMISOS.md` (MEMBER has no `/parameters`).
- `CATALEG_PARAMETRES.md` (`census.dogDocumentTypes`).
- S02: §3 (`legalName`, `taxId`, `address`, `displayCity`), R-02-02 (`/branding`: `city`, `legalAddress` and the public footer, LSSI art. 10), and the change log entry of 25-09.
- S17 §3 (the club definition).

## Context to load
- `CensusQuery.myDogs()` and `level()`, `CensusResponses.MeDogs`, and `DocumentService.catalog()`.
- How the other `LocalizedText` values are resolved for the reader (level and plan names).

## Steps
1. **`documentTypes` in `GET /me/dogs` (R-03-15, R-03-32).** Return the club's catalog as `documentTypes: [{key, label, required}]`:
   - in catalog order, with `label` resolved in the reader's locale (fallback `club.defaultLocale`), like the other `LocalizedText` values;
   - for every member, with or without dogs, and under impersonation;
   - `/parameters/*` stays ADMIN only.
   
   Tests:
   - a contract test;
   - extend T-03-26 (or add an IT next to it): a member in `ca` gets the Cànic's types with Catalan labels, and one in `es` gets Spanish labels;
   - after an ADMIN changes the catalog, the next `GET /me/dogs` shows the new type (the configuration cache is evicted as usual);
   - a member upload with a type from `documentTypes` gets `201`, and an unknown type still gets `400 DOCUMENT_TYPE_UNKNOWN`.
2. **`level` follows `levels.enabled` (R-03-30).** The web will show «Nivell {codi}» only when a dog carries `level`. `CensusQuery.level()` already returns nothing when `levels.enabled=false`. Pin that with a test: with levels disabled, a dog with a stored `levelId` has no `level` in `GET /me/dogs`. This test may pass before any change.
3. **The Cànic's legal identity and the public footer (Jordi, 25-09; E3-T10 question 4; S02 amended).** Jordi wants the footer to read «Club Agility Cànic · G63189617 · Cabrera de Mar», with the registered office below: «Carrer Sant Pere, 10 · 08392 Sant Andreu de Llavaneres».
   - **The club gets `displayCity`** (S02 §3): the town shown with the club's name, optional, defaulting to `address.city`. The club definition (S17), the club settings API and the `club:apply` diff carry it.
   - **`GET /branding`** (R-02-02, additive): `club.city` becomes `displayCity ?? address.city`, and a new `club.legalAddress: {street, postalCode, city} | null` gives the registered office for the footer (LSSI art. 10). It is `null` when the street or the postal code is missing.
   - **The Cànic seed** (`seeds/club-canic.yaml`):
     - `legalName: "Club Agility Cànic"`;
     - `taxId: "G63189617"`, which the Spanish country profile must accept;
     - `address: {street: "Carrer Sant Pere, 10", postalCode: "08392", city: "Sant Andreu de Llavaneres", region: "Barcelona", country: "ES"}`, the registered office;
     - `displayCity: "Cabrera de Mar"`.
   
   Tests:
   - the Cànic's `GET /branding` gives `city` «Cabrera de Mar», its `legalName` and `taxId`, and `legalAddress`;
   - a club without `displayCity` shows `address.city`;
   - a second `club:apply` gives no changes;
   - a contract test.
4. **Small follow-ups: the E3-T15 review and the E3-W08 report.**
   - **The «never charge twice» guard, tested through the service (E3-T15 review #1).** Use a repository whose `insert` stores the document and then throws `DuplicateKeyException`. With cap 1, the bucket is still untouched afterwards: a following event for the same address is admitted. The test must fail without the `decided.stored()` check. Add one for the `deciding` lock if it can be made deterministic.
   - **The N-39 side (review #2).** In an existing N-39 IT, assert that `<eventId>:N-39` gets its `expiresAt` = processed + `jobs.retention.domainEventsDays`. Take the capped notification codes from one place.
   - **The lock's javadoc (review #5):** there is one lock per instance, across clubs and recipients.
   - **P9 (S15 R-15-19, amended 25-09):** `signup_notification_admissions` is among the TTL collections it reports (`ttlPending.*`). Extend T-15-27.
   - **The list `size` (CONVENCIONS_API §4, amended 25-09; E3-W08 report).** The api accepts only `size` ∈ {20, 50, 200, 1000} and answers `400 INVALID_FILTER` otherwise, but the OpenAPI says only `minimum: 1`. Document the allowed values in the shared `size` parameter (an `enum`, default 50), with a contract test.

## Fixed conventions
- Additive contract change. Regenerate `docs/openapi/openapi.json`, and add one `docs/openapi/CHANGELOG.md` entry.
- Heavy runs go through the host lock.
- Fictional data only.

## Verification (run all; evidence = command + exit code + last 40 lines; long outputs → `roadmap/evidence/E3-T16/*.log`)
- `./mvnw -q clean verify`.
- `bin/e3-smoke`, twice, on a fresh stack.
- `bin/openapi-snapshot`, then `git diff --stat docs/openapi/`.

## Definition of Done
- Steps 1 and 3 are done, each with a test that fails before the fix.
- Step 2 has its test.
- Step 4 is done; the review-#1 test fails without the guard.
- The snapshot and its changelog are updated.

## Evidence to return
- The command outputs.
- A step → test table.

---

## Executor report

### Summary
Steps 1–4 are done. Every Verification command exits 0:
- `./mvnw -q clean verify`: 542 unit and 950 integration tests.
- `bin/e3-smoke`: exits 0 twice, each run on its own fresh Compose stack.
- `bin/openapi-snapshot`: regenerated. It has a `docs/openapi/CHANGELOG.md` entry.

The new behaviour tests fail on the unfixed code (logs `01`, `02`). Step 2 and parts of step 4 pin behaviour that was already right. Mutation runs show that these tests catch the regression (logs `06a`–`06c`).

What changed:
- **Step 1.** `GET /me/dogs` returns `documentTypes: [{key, label, required}]`. It is the club's `census.dogDocumentTypes`, in catalog order. `label` is in the reader's locale, with fallback to `club.defaultLocale` and then to the key, like `DogDocument.typeLabel`. Every member gets it, with or without dogs, and under impersonation. `/parameters/*` stays ADMIN only.
- **Step 3.**
  - `Club.displayCity` is carried by the club definition (schema, export, merge, `club:apply` diff), by `GET /club` and by `PUT /club`.
  - `/branding`: `club.city` is `displayCity ?? address.city`, and `club.legalAddress {street, postalCode, city}` is new. `legalAddress` is `null` without a street or a postal code; a blank value counts as missing.
  - The Cànic seeds (canonical and consumer) carry the legal identity and the registered office.
  - The club's `taxId` is now checked by its country profile (S02 §3), in `PUT /club` and in `club:apply` (Assumption 1).
- **Step 4.**
  - Two tests go through the service: the «never charge twice» guard and the decision lock.
  - The N-39 decisions' `expiresAt` is asserted.
  - `CAPPED` is the one place for the capped codes.
  - The lock's javadoc now says it is one per instance.
  - P9 reports `ttlPendingSignupNotificationAdmissions`.
  - The list `size` is an OpenAPI `enum [20, 50, 200, 1000]`, default 50. A value above 1000 is now `400 INVALID_FILTER`. Before, it was silently cut to 1000 (Assumption 2).

### Step → test table
| Step | Test (class) | What it asserts | Before the fix |
|---|---|---|---|
| 1 | `T_03_26_myDogsContainOnlyOwnActiveDogsWithSafeTasksAndLicenses` (CensusIT, T-03-26 extended) | Laura-like member in `en`: the three types, in order, with English labels and `required` | failed, log `02` (`documentTypes` missing) |
| 1 | `T_03_26_R_03_32_documentTypesComeInTheReadersLanguageForEveryMemberAndUnderImpersonation` (CensusIT) | `ca` labels «Cartilla de vacunes · Assegurança · Altres»; `es` labels; a member without dogs gets them; `/parameters/census.dogDocumentTypes` → 403 for the member and for the impersonation token, which still gets the types | failed, log `02` |
| 1 | `T_03_23_R_03_15_aTypeTheAdminAddsIsOfferedAtOnceAndAMemberCanUploadIt` (CensusIT) | ADMIN `PUT /parameters/census.dogDocumentTypes` adds `LICENSE` ({ca, es} only) → next `GET /me/dogs` lists it, the `en` reader gets the `ca` label; member upload of `LICENSE` → 201; `UNKNOWN` → 400 `DOCUMENT_TYPE_UNKNOWN` | failed, log `02` |
| 1 (contract) | `T_03_26_meDogsAlwaysCarriesTheClubsDocumentTypes` (E3GateFixesContractTest) | `MeDogs.documentTypes` required, items `DogDocumentType {key, label, required}` all required | failed, log `01` |
| 1 (fixture) | `E2ResponseContractTest` (`e2-responses.json` `MeDogs` gains `documentTypes`) | the mock response round-trips through `MeDogs` | errored, log `01` |
| 2 | `T_03_26_R_03_30_withLevelsDisabledADogWithAStoredLevelHasNoLevel` (CensusIT) | `levels.enabled=false`: no `level` key on either dog; `levelId` still stored | passed (pins the existing behaviour, as the task expected) |
| 3 | `T_02_07_T_02_12_canicSeedCarriesItsLegalIdentityAndRegisteredOfficeForThePublicFooter` (ClubDefinitionsIT) | previous seed → new seed: dry run lists `club.displayCity`, `legalName`, `taxId`, `address` changes, 1 change; `/branding`: `city` «Cabrera de Mar», `legalName`, `taxId` G63189617, `legalAddress` {Carrer Sant Pere, 10 · 08392 · Sant Andreu de Llavaneres}; second apply 0; export round-trips; `G63189618` → `VALIDATION_ERROR`; a blank `displayCity` still exports to a valid definition | failed, log `02` (0 changes instead of 1) |
| 3 | `T_02_12_canicMatchesBrandingAndParametersFixturesAndSecondApplyDoesNotWrite` (ClubDefinitionsIT, fixture `branding-canic.json` updated) | the Cànic `/branding` equals the fixture, then 0 changes | failed, log `02` |
| 3 | `T_02_07_R_02_02_cityFallsBackToTheRegisteredOfficeAndTheLegalAddressNeedsStreetAndPostalCode` (PlatformIT) | no `displayCity` → `address.city`; `displayCity` set → it, `legalAddress.city` stays the office's; blank `displayCity` ignored; blank street or postal code → `legalAddress: null`; no address → `city` and `legalAddress` null | failed, log `02` |
| 3 | `T_02_07_brandingHasOnlyPublicFieldsAndSupportsEtagAndManifest` (PlatformIT) | `club` keys are exactly `slug, name, city, legalName, taxId, legalAddress` | failed, log `02` |
| 3 | `T_02_11_R_02_02_displayCityIsEditedWithTheClubAndTheTaxIdFollowsTheCountryProfile` (SettingsIT) | `PUT /club {displayCity, taxId}` → `GET /club`/`/branding` show it; audit `settings.displayCity`; `B12345675`, `EXAMPLE-ORG`, `displayCity: 5` → 400; `displayCity: null` → `/branding` back to `address.city` | failed, log `02` (400 on `displayCity`) |
| 3 | `T_02_04_spanishTaxIdsAreACifNifOrNieWithTheirCheckCharacter` (CountryProfileTest) | `ES`: G63189617 and other fictional CIFs, DNI, NIE valid; wrong control, letter/digit kind, malformed → invalid; `GENERIC`: 4–40 alphanumerics | written after the fix (new method; see Assumption 1) |
| 3 (contract) | `T_02_07_brandingNamesTheTownAndTheRegisteredOfficeAndTheClubSettingsCarryDisplayCity` (E3GateFixesContractTest), `E1_T11_everySnapshotObjectDeclaresItsRequiredProperties` (updated) | `ClubSummary` requires `city` (`string|null`) and `legalAddress` (`$ref LegalAddress`, `type [object, null]`); `LegalAddress` {street, postalCode, city|null}; `ClubSettings`/`ClubUpdate.displayCity`; `taxId` has no `format: uuid` | failed, log `01` |
| 4 (#1) | `R_04_20_aDecisionAnotherDeliveryStoredFirstIsUsedAndNeverChargedAgain` (SignupFollowUpFixesIT) | the repository stores A's decision then throws `DuplicateKeyException`; A sends; with cap 1, B (same address, same hour) is still admitted; C refused | passed on the guarded code; **fails without `decided.stored()`**, log `06a` («B admitted: A charged nothing») |
| 4 (#1, lock) | `R_04_20_oneRecipientCapDecisionAtATimeSoTwoEventsNeverShareTheLastAllowance` (SignupFollowUpFixesIT) | A's decision is held inside its insert; B (same subject) must wait on `deciding`; then A admitted, B refused | passed with the lock; **fails without it**, log `06b` («B waited for A's decision») |
| 4 (#2) | `R_04_05_R_04_20_theRecipientCapIsTheClubsParameter` (SignupSecurityFixesIT, extended) | the three `SignupRecognitionRequested` events are PUBLISHED and each `<eventId>:N-39` decision (1 admitted, 2 refused) has `expiresAt` = processed + 90 d | passed; **fails when `processed` skips N-39**, log `06c` |
| 4 (P9) | `T_15_27_cleanupDeletesOnlyExpiredTechnicalDataAndKeepsTheLastFiveRuns` (CleanupJobIT, extended) | `ttlPendingSignupNotificationAdmissions` = 1 (expired) in the dry and the real run; the pending (no `expiresAt`), future and other club's ones not counted; nothing deleted | failed, log `02` |
| 4 (size) | `T_03_08_sizeIsOneOfTheFourPageSizes` ×10 (UniversalListsIT) | 20/50/200/1000 → 200 with that `size`; 1, 21, 100, 1001, 5000, -20 → 400 `INVALID_FILTER` | failed for 1001 and 5000, log `02` |
| 4 (size) | `ListQueryTest` (`size=1001`, `size=9000` invalid; the 9000→1000 case replaced) | — | failed, log `01` |
| 4 (size, contract) | `T_03_08_everyListDocumentsTheFourPageSizes` (E3GateFixesContractTest) | all 29 shared `size` parameters: `enum [20, 50, 200, 1000]`, default 50, no `minimum` | failed, log `01` |

### Files changed
- Step 1:
  - `clubs/census/api/CensusResponses.java`: `DogDocumentType`, `MeDogs.documentTypes`.
  - `clubs/census/api/MyCensusController.java`: the description.
  - `clubs/census/application/DocumentService.java`: `types()`, and a shared `label()` that `view()` uses too.
  - `clubs/census/application/CensusQuery.java`: `myDogs()` adds `documentTypes`.
- Step 3:
  - `platform/persistence/Club.java`: `displayCity`.
  - `platform/application/ClubConfig.java`: `ClubView.legalAddress`, record `LegalAddress`.
  - `platform/application/ClubConfigService.java`: computes `city` and `legalAddress`.
  - `platform/api/BrandingResponse.java`: `ClubSummary.city` required, `legalAddress`.
  - `platform/api/SettingsContracts.java`: `displayCity`; `taxId` loses `format: uuid`.
  - `platform/api/SettingsMapper.java`.
  - `platform/application/ClubSettingsService.java`: `displayCity` is editable; the `taxId` check.
  - `platform/application/definition/ClubDefinitionMapper.java`: export and merge `displayCity`.
  - `platform/application/definition/ClubDefinitionWriter.java`: the `taxId` check.
  - `platform/domain/CountryProfile.java`, `GenericCountryProfile.java`, `SpanishCountryProfile.java`: `validateTaxId`.
  - `seeds/club-canic.yaml`, `seeds/club-canic-consumer.yaml`, `seeds/club-definition.schema.json`, `seeds/README.md`.
- Step 4:
  - `clubs/census/application/SignupNotifications.java`: `capped(eventId, event, recipient)` reads `CAPPED`; javadoc.
  - `shared/application/RateLimits.java`: javadoc.
  - `clubs/common/application/CleanupJob.java`: the new counter.
  - `shared/application/lists/ListQuery.java`: `SIZES`, no clamp.
  - `configuration/OpenApiConfiguration.java`: the `size` enum and its description.
- Tests:
  - `CensusIT`, `SettingsIT`, `PlatformIT`, `ClubDefinitionsIT`, `SignupFollowUpFixesIT`, `SignupSecurityFixesIT`, `CleanupJobIT`, `UniversalListsIT`.
  - `CountryProfileTest`, `ListQueryTest`, `OpenApiRequiredContractTest`.
  - New: `configuration/E3GateFixesContractTest.java`.
  - Fixtures: `branding-canic.json`, `contracts/e2-responses.json`.
- `docs/openapi/openapi.json`, `docs/openapi/CHANGELOG.md`, `CHANGELOG.md`, `roadmap/MESSAGES.md` (one entry), `roadmap/evidence/E3-T16/01…18`, this report.
- Paths are under `src/main/java/com/agilityhub/core/` and `src/test/java/com/agilityhub/core/`.

### Rules and tests implemented
- Rules:
  - R-03-15 and R-03-32 (`documentTypes`); R-03-30 (pinned);
  - R-02-02 and S02 §3 (`displayCity`, `city`, `legalAddress`, the `taxId` check), S17 §3 (the definition);
  - R-04-20 (the E3-T15 review follow-ups), R-15-19 (P9 TTL report);
  - CONVENCIONS_API §4 (`size`).
- Tests with spec ids: T-03-26 (extended, plus 2 new), T-03-23, T-02-07 (2), T-02-11, T-02-12, T-02-04, T-15-27 (extended), T-03-08 (2). The step-4 cap tests carry the rule id `R_04_20`, like the E3-T12/T-15 ones.

### Evidence

**Earlier runs (one line each; logs committed):**
- `01-unit-before-fix.log`: the new unit and contract tests on the unfixed code. Command: `./mvnw -q test -Dtest='ListQueryTest,E3GateFixesContractTest,OpenApiRequiredContractTest,E2ResponseContractTest' -Djacoco.skip=true`. Exit 1: `Tests run: 42, Failures: 6, Errors: 1` (all expected, see the table).
- `02-it-before-fix.log`: the targeted ITs on the unfixed code. Command: `./mvnw -q verify -Dtest=NoSuchTest -Dsurefire.failIfNoSpecifiedTests=false -Djacoco.skip=true -Dit.test='CensusIT#T_03_26*+T_03_23_R_03_15*,SettingsIT#T_02_11*,PlatformIT#T_02_07*,ClubDefinitionsIT#T_02_07*+T_02_12*,SignupFollowUpFixesIT#R_04_20_aDecisionAnotherDelivery*+R_04_20_oneRecipientCap*,SignupSecurityFixesIT#R_04_05_R_04_20_theRecipientCapIsTheClubsParameter,CleanupJobIT#T_15_27_cleanupDeletesOnly*,UniversalListsIT#T_03_08_sizeIsOneOfTheFourPageSizes'`. Exit 1: `Tests run: 34, Failures: 11`. The step-2 and step-4 pinning tests passed there.
- `03-unit-after-fix.log`: the same unit tests plus `CountryProfileTest` and `OpenApiNullableEnumContractTest`, after the fix and the snapshot. Exit 0.
- `04-openapi-snapshot-dev.log`: the development regeneration (`bin/openapi-snapshot`, exit 0). My first `legalAddress` annotation (`nullable = true`) made the field optional; I switched to the house form (Assumption 3).
- `05-it-after-fix.log`: the 10 affected IT classes after the fix, plus `OpenApiSnapshotTest` and `DemoSeedsIT`. Exit 0; all reports have failures=0.
- `06a/06b/06c`: the mutation runs, each exit 1 as intended (table). Each mutation was reverted at once; `git diff` of `SignupNotifications.java` afterwards shows only the intended changes.
- `07-clean-verify.log`: first full `clean verify` (heavy.sh). Exit 1: `DemoPlanningSeedIT.T_06_28_seedSourcesContainNoRealStaffOrMockupFirstNames`. My seed comment named the product owner; the comment now says «product owner».
- `08-clean-verify-before-schema-relaxation.log` / `09-test-summary-before-schema-relaxation.log`: green (542 + 950). After it, my self-review found that the definition schema's `minLength: 1` on `displayCity` would make a blank town (accepted by `PUT /club`) fail its own export. I removed it and added the export assertion to the ClubDefinitionsIT test (`14-blank-display-city.log`, targeted, exit 0).
- `10`/`11`: two green `bin/e3-smoke` runs on the code before that schema relaxation. They are superseded by `17`/`18`.

**1. `/Users/jordib/Dropbox/Documents/SOFTWARE_CANIC/05-desenvolupament/roadmap-kit/mac/heavy.sh ./mvnw -q clean verify > roadmap/evidence/E3-T16/15-clean-verify-final.log 2>&1; echo "exit $?"`: exit 0 (final run, final code).**
- The lock waited 2 min 0 s for the web lane (`pnpm e2e:core E3-W08`), log lines 1–2. The full output (5 774 lines) is in `15-clean-verify-final.log`. With `-q`, a green run prints no summary.
- Test summary, read from `target/surefire-reports` and `target/failsafe-reports` of this clean run (`16-test-summary-final.log`, exit 0):
```
surefire: tests=542 failures=0 errors=0 skipped=0
  com.agilityhub.core.configuration.E2ResponseContractTest: tests=2 failures=0 errors=0 skipped=0
  com.agilityhub.core.configuration.E3GateFixesContractTest: tests=3 failures=0 errors=0 skipped=0
    - T_02_07_brandingNamesTheTownAndTheRegisteredOfficeAndTheClubSettingsCarryDisplayCity
    - T_03_08_everyListDocumentsTheFourPageSizes
    - T_03_26_meDogsAlwaysCarriesTheClubsDocumentTypes
  com.agilityhub.core.configuration.OpenApiRequiredContractTest: tests=3 failures=0 errors=0 skipped=0
  com.agilityhub.core.platform.domain.CountryProfileTest: tests=4 failures=0 errors=0 skipped=0
    - T_02_04_spanishTaxIdsAreACifNifOrNieWithTheirCheckCharacter
  com.agilityhub.core.shared.application.lists.ListQueryTest: tests=34 failures=0 errors=0 skipped=0
failsafe: tests=950 failures=0 errors=0 skipped=0
  com.agilityhub.core.clubs.bookings.api.CleanupJobIT: tests=4 failures=0 errors=0 skipped=0
  com.agilityhub.core.clubs.census.api.CensusIT: tests=41 failures=0 errors=0 skipped=0
    - T_03_26_R_03_30_withLevelsDisabledADogWithAStoredLevelHasNoLevel
    - T_03_23_R_03_15_aTypeTheAdminAddsIsOfferedAtOnceAndAMemberCanUploadIt
    - T_03_26_R_03_32_documentTypesComeInTheReadersLanguageForEveryMemberAndUnderImpersonation
  com.agilityhub.core.clubs.census.api.SignupFollowUpFixesIT: tests=14 failures=0 errors=0 skipped=0
    - R_04_20_aDecisionAnotherDeliveryStoredFirstIsUsedAndNeverChargedAgain
    - R_04_20_oneRecipientCapDecisionAtATimeSoTwoEventsNeverShareTheLastAllowance
  com.agilityhub.core.clubs.census.api.SignupSecurityFixesIT: tests=21 failures=0 errors=0 skipped=0
  com.agilityhub.core.clubs.census.application.DemoPlanningSeedIT: tests=7 failures=0 errors=0 skipped=0
  com.agilityhub.core.clubs.census.application.DemoSeedsIT: tests=9 failures=0 errors=0 skipped=0
  com.agilityhub.core.clubs.common.api.UniversalListsIT: tests=33 failures=0 errors=0 skipped=0
    - T_03_08_sizeIsOneOfTheFourPageSizes (10 cases)
  com.agilityhub.core.configuration.OpenApiSnapshotTest: tests=10 failures=0 errors=0 skipped=0
  com.agilityhub.core.platform.api.SettingsIT: tests=15 failures=0 errors=0 skipped=0
    - T_02_11_R_02_02_displayCityIsEditedWithTheClubAndTheTaxIdFollowsTheCountryProfile
  com.agilityhub.core.platform.application.definition.ClubDefinitionsIT: tests=11 failures=0 errors=0 skipped=0
    - T_02_07_T_02_12_canicSeedCarriesItsLegalIdentityAndRegisteredOfficeForThePublicFooter
  com.agilityhub.core.platform.persistence.PlatformIT: tests=10 failures=0 errors=0 skipped=0
    - T_02_07_R_02_02_cityFallsBackToTheRegisteredOfficeAndTheLegalAddressNeedsStreetAndPostalCode
```
- Compared with E3-T14 round 2 (536 unit, 932 integration): +6 unit and +18 integration tests.
- Last 40 lines of `15-clean-verify-final.log`:
```
2026-09-25T21:05:21.694+02:00  INFO 51381 --- [agilityhub-core-api] [           main] org.mongodb.driver.client                : MongoClient with metadata {"driver": {"name": "mongo-java-driver|sync|spring-boot", "version": "5.5.2"}, "os": {"type": "Darwin", "name": "Mac OS X", "architecture": "aarch64", "version": "26.4.1"}, "platform": "Java/Homebrew/21.0.12.1"} created with settings MongoClientSettings{readPreference=primary, writeConcern=WriteConcern{w=null, wTimeout=null ms, journal=null}, retryWrites=true, retryReads=true, readConcern=ReadConcern{level=null}, credential=null, transportSettings=null, commandListeners=[io.micrometer.core.instrument.binder.mongodb.MongoMetricsCommandListener@5d46c86e], codecRegistry=ProvidersCodecRegistry{codecProviders=[ValueCodecProvider{}, BsonValueCodecProvider{}, DBRefCodecProvider{}, DBObjectCodecProvider{}, DocumentCodecProvider{}, CollectionCodecProvider{}, IterableCodecProvider{}, MapCodecProvider{}, GeoJsonCodecProvider{}, GridFSFileCodecProvider{}, Jsr310CodecProvider{}, JsonObjectCodecProvider{}, BsonCodecProvider{}, EnumCodecProvider{}, com.mongodb.client.model.mql.ExpressionCodecProvider@2ad8aeb5, com.mongodb.Jep395RecordCodecProvider@7461712c, com.mongodb.KotlinCodecProvider@4894d5ce]}, loggerSettings=LoggerSettings{maxDocumentLength=1000}, clusterSettings={hosts=[localhost:49399], srvServiceName=mongodb, mode=SINGLE, requiredClusterType=UNKNOWN, requiredReplicaSetName='null', serverSelector='null', clusterListeners='[]', serverSelectionTimeout='30000 ms', localThreshold='15 ms'}, socketSettings=SocketSettings{connectTimeoutMS=10000, readTimeoutMS=0, receiveBufferSize=0, proxySettings=ProxySettings{host=null, port=null, username=null, password=null}}, heartbeatSocketSettings=SocketSettings{connectTimeoutMS=10000, readTimeoutMS=10000, receiveBufferSize=0, proxySettings=ProxySettings{host=null, port=null, username=null, password=null}}, connectionPoolSettings=ConnectionPoolSettings{maxSize=100, minSize=0, maxWaitTimeMS=120000, maxConnectionLifeTimeMS=0, maxConnectionIdleTimeMS=0, maintenanceInitialDelayMS=0, maintenanceFrequencyMS=60000, connectionPoolListeners=[io.micrometer.core.instrument.binder.mongodb.MongoMetricsConnectionPoolListener@533b7ed2], maxConnecting=2}, serverSettings=ServerSettings{heartbeatFrequencyMS=10000, minHeartbeatFrequencyMS=500, serverMonitoringMode=AUTO, serverListeners='[]', serverMonitorListeners='[]'}, sslSettings=SslSettings{enabled=false, invalidHostNameAllowed=false, context=null}, applicationName='null', compressorList=[], uuidRepresentation=JAVA_LEGACY, serverApi=null, autoEncryptionSettings=null, dnsClient=null, inetAddressResolver=null, contextProvider=null, timeoutMS=null}
2026-09-25T21:05:21.695+02:00  INFO 51381 --- [agilityhub-core-api] [localhost:49399] org.mongodb.driver.cluster               : Monitor thread successfully connected to server with description ServerDescription{address=localhost:49399, type=REPLICA_SET_PRIMARY, cryptd=false, state=CONNECTED, ok=true, minWireVersion=0, maxWireVersion=21, maxDocumentSize=16777216, logicalSessionTimeoutMinutes=30, roundTripTimeNanos=1576083, minRoundTripTimeNanos=0, setName='docker-rs', canonicalAddress=74fcb126e7a8:27017, hosts=[74fcb126e7a8:27017], passives=[], arbiters=[], primary='74fcb126e7a8:27017', tagSet=TagSet{[]}, electionId=7fffffff0000000000000001, setVersion=1, topologyVersion=TopologyVersion{processId=6ab6c506783814d3751d02cf, counter=6}, lastWriteDate=Fri Sep 25 21:05:21 CEST 2026, lastUpdateTimeNanos=166927064032875}
2026-09-25T21:05:22.143+02:00  INFO 51381 --- [agilityhub-core-api] [           main] c.a.core.migration.PlayoffMigrationIT    : Started PlayoffMigrationIT in 0.681 seconds (process running for 237.447)
E5-T07 outbox backlog discarded before PlayoffMigrationIT: 158 PENDING records (more than one dispatch() batch)
E5-T07 outbox backlog discarded before PlayoffMigrationIT: 158 PENDING records (more than one dispatch() batch)
E5-T07 outbox backlog discarded before PlayoffMigrationIT: 158 PENDING records (more than one dispatch() batch)
E5-T07 outbox backlog discarded before PlayoffMigrationIT: 162 PENDING records (more than one dispatch() batch)
2026-09-25T21:05:29.120+02:00  INFO 51381 --- [agilityhub-core-api] [localhost:49757] org.mongodb.driver.cluster               : Exception in monitor thread while connecting to server localhost:49757

com.mongodb.MongoSocketOpenException: Exception opening socket
	at com.mongodb.internal.connection.SocketStream.lambda$open$0(SocketStream.java:85) ~[mongodb-driver-core-5.5.2.jar:na]
	at java.base/java.util.Optional.orElseThrow(Optional.java:403) ~[na:na]
	at com.mongodb.internal.connection.SocketStream.open(SocketStream.java:85) ~[mongodb-driver-core-5.5.2.jar:na]
	at com.mongodb.internal.connection.InternalStreamConnection.open(InternalStreamConnection.java:233) ~[mongodb-driver-core-5.5.2.jar:na]
	at com.mongodb.internal.connection.DefaultServerMonitor$ServerMonitor.lookupServerDescription(DefaultServerMonitor.java:219) ~[mongodb-driver-core-5.5.2.jar:na]
	at com.mongodb.internal.connection.DefaultServerMonitor$ServerMonitor.run(DefaultServerMonitor.java:176) ~[mongodb-driver-core-5.5.2.jar:na]
Caused by: java.net.ConnectException: Connection refused
	at java.base/sun.nio.ch.Net.pollConnect(Native Method) ~[na:na]
	at java.base/sun.nio.ch.Net.pollConnectNow(Net.java:694) ~[na:na]
	at java.base/sun.nio.ch.NioSocketImpl.timedFinishConnect(NioSocketImpl.java:542) ~[na:na]
	at java.base/sun.nio.ch.NioSocketImpl.connect(NioSocketImpl.java:592) ~[na:na]
	at java.base/java.net.SocksSocketImpl.connect(SocksSocketImpl.java:327) ~[na:na]
	at java.base/java.net.Socket.connect(Socket.java:751) ~[na:na]
	at com.mongodb.internal.connection.SocketStreamHelper.initialize(SocketStreamHelper.java:76) ~[mongodb-driver-core-5.5.2.jar:na]
	at com.mongodb.internal.connection.SocketStream.initializeSocket(SocketStream.java:104) ~[mongodb-driver-core-5.5.2.jar:na]
	at com.mongodb.internal.connection.SocketStream.open(SocketStream.java:79) ~[mongodb-driver-core-5.5.2.jar:na]
	... 3 common frames omitted

E5-T07 outbox backlog discarded before PlayoffMigrationIT: 158 PENDING records (more than one dispatch() batch)
E5-T07 outbox backlog discarded before PlayoffMigrationIT: 158 PENDING records (more than one dispatch() batch)
E5-T07 outbox backlog discarded before PlayoffMigrationIT: 158 PENDING records (more than one dispatch() batch)
E5-T07 outbox backlog discarded before PlayoffMigrationIT: 158 PENDING records (more than one dispatch() batch)
E5-T07 outbox backlog discarded before PlayoffMigrationIT: 158 PENDING records (more than one dispatch() batch)
E5-T07 outbox backlog discarded before PlayoffMigrationIT: 158 PENDING records (more than one dispatch() batch)
E5-T07 outbox backlog discarded before PlayoffMigrationIT: 159 PENDING records (more than one dispatch() batch)
E5-T07 outbox backlog discarded before PlayoffMigrationIT: 158 PENDING records (more than one dispatch() batch)
E5-T07 outbox backlog discarded before PlayoffMigrationIT: 158 PENDING records (more than one dispatch() batch)
E5-T07 outbox backlog discarded before PlayoffMigrationIT: 158 PENDING records (more than one dispatch() batch)
E5-T07 outbox backlog discarded before PlayoffMigrationIT: 158 PENDING records (more than one dispatch() batch)
E5-T07 outbox backlog discarded before PlayoffMigrationIT: 159 PENDING records (more than one dispatch() batch)
```
- The `MongoSocketOpenException` is logged at INFO by the driver's monitor thread of a Testcontainers Mongo that another IT context had already stopped. It is not a failure: every report shows failures=0 errors=0.

**2a. `/Users/jordib/Dropbox/Documents/SOFTWARE_CANIC/05-desenvolupament/roadmap-kit/mac/heavy.sh bin/e3-smoke > roadmap/evidence/E3-T16/17-e3-smoke-final-run1.log 2>&1; echo "exit $?"`: exit 0.**
- Each run creates its own disposable Compose project, so each starts from a fresh stack and an empty database.
- The lock waited 2 min 30 s (log lines 1–2). The full output is in `17-e3-smoke-final-run1.log` (60 lines). It includes `PASS club:apply seeds/club-canic.yaml run 2: 0 changes`, so the new seed is idempotent on a fresh stack.
- Last 40 lines:
```
PASS D1 demo: 184 ACTIVE members, 242 ACTIVE dogs, three reviewable pending public signups
curl POST /api/v1/signup/family-group-lookups -> 200
curl POST /api/v1/signup/identity-checks -> 200
curl POST /api/v1/signup/upload-urls -> 200
curl PUT /api/v1/signup/uploads -> 204
curl POST /api/v1/signup -> 201
curl POST /api/v1/signup -> 201
PASS D1 dashboard + counters: pending=4, olderThanWarn=1, activeMembers=184
curl GET /api/v1/members/cb1e813f-d78a-4f8b-81ed-a6afbf37c196/signup -> 200
curl GET /api/v1/club -> 200
PASS payment methods: GET /club, GET /signup and D2 offer SEPA_DD and MANUAL
PASS mailbox N-01 correlated by notificationId; locale=ca
curl POST /api/v1/members/cb1e813f-d78a-4f8b-81ed-a6afbf37c196/validation -> 200
curl POST /api/v1/members/cb1e813f-d78a-4f8b-81ed-a6afbf37c196/validation -> 200
PASS D1 dashboard + counters: pending=3, olderThanWarn=1, activeMembers=185
PASS mailbox N-02 correlated by notificationId; locale=ca; token=[truncated]
curl POST /oauth2/token -> 200
curl GET /api/v1/me -> 200
PASS public family signup -> dry run -> validation/number increment -> N-02 -> welcome session
curl POST /api/v1/me/dogs/signup -> 201
PASS N-01 in the member APP inbox, scoped to account and entity
PASS D1 dashboard + counters: pending=4, olderThanWarn=1, activeMembers=185
curl GET /api/v1/members/cb1e813f-d78a-4f8b-81ed-a6afbf37c196/signup -> 200
PASS add-dog D2: only the member's current payment method, not assignable
curl POST /api/v1/members/cb1e813f-d78a-4f8b-81ed-a6afbf37c196/validation -> 200
PASS N-37 in the member APP inbox, scoped to account and entity
PASS D1 dashboard + counters: pending=3, olderThanWarn=1, activeMembers=185
curl POST /api/v1/signup -> 201
PASS D1 dashboard + counters: pending=4, olderThanWarn=1, activeMembers=185
curl GET /api/v1/members/9c802a8b-95b9-4280-b792-db9bf062b287/signup -> 200
curl POST /api/v1/members/9c802a8b-95b9-4280-b792-db9bf062b287/rejection -> 200
PASS mailbox N-03 correlated by notificationId; locale=ca
PASS D1 dashboard + counters: pending=3, olderThanWarn=1, activeMembers=185
PASS signup without family -> rejection -> N-03; D1 counters restored
curl GET /api/v1/parameters/signup.enabled -> 200
curl PUT /api/v1/parameters/signup.enabled -> 200
curl POST /api/v1/signup -> 422 SIGNUP_CLOSED
PASS signup.enabled=false closes the form; canonical catalog status is 422 SIGNUP_CLOSED
PASS T-04-34 / T-14-11: E3 backend gate completed; credentials and capabilities withheld
Removed disposable Compose services, volumes and private mailbox; existing stacks preserved
```

**2b. `/Users/jordib/Dropbox/Documents/SOFTWARE_CANIC/05-desenvolupament/roadmap-kit/mac/heavy.sh bin/e3-smoke > roadmap/evidence/E3-T16/18-e3-smoke-final-run2.log 2>&1; echo "exit $?"`: exit 0.**
- The full output (58 lines) is in `18-e3-smoke-final-run2.log`. Last 40 lines:
```
PASS D1 demo: 184 ACTIVE members, 242 ACTIVE dogs, three reviewable pending public signups
curl POST /api/v1/signup/family-group-lookups -> 200
curl POST /api/v1/signup/identity-checks -> 200
curl POST /api/v1/signup/upload-urls -> 200
curl PUT /api/v1/signup/uploads -> 204
curl POST /api/v1/signup -> 201
curl POST /api/v1/signup -> 201
PASS D1 dashboard + counters: pending=4, olderThanWarn=1, activeMembers=184
curl GET /api/v1/members/6db732b3-971a-44a1-899b-dd921fdcf0df/signup -> 200
curl GET /api/v1/club -> 200
PASS payment methods: GET /club, GET /signup and D2 offer SEPA_DD and MANUAL
PASS mailbox N-01 correlated by notificationId; locale=ca
curl POST /api/v1/members/6db732b3-971a-44a1-899b-dd921fdcf0df/validation -> 200
curl POST /api/v1/members/6db732b3-971a-44a1-899b-dd921fdcf0df/validation -> 200
PASS D1 dashboard + counters: pending=3, olderThanWarn=1, activeMembers=185
PASS mailbox N-02 correlated by notificationId; locale=ca; token=[truncated]
curl POST /oauth2/token -> 200
curl GET /api/v1/me -> 200
PASS public family signup -> dry run -> validation/number increment -> N-02 -> welcome session
curl POST /api/v1/me/dogs/signup -> 201
PASS N-01 in the member APP inbox, scoped to account and entity
PASS D1 dashboard + counters: pending=4, olderThanWarn=1, activeMembers=185
curl GET /api/v1/members/6db732b3-971a-44a1-899b-dd921fdcf0df/signup -> 200
PASS add-dog D2: only the member's current payment method, not assignable
curl POST /api/v1/members/6db732b3-971a-44a1-899b-dd921fdcf0df/validation -> 200
PASS N-37 in the member APP inbox, scoped to account and entity
PASS D1 dashboard + counters: pending=3, olderThanWarn=1, activeMembers=185
curl POST /api/v1/signup -> 201
PASS D1 dashboard + counters: pending=4, olderThanWarn=1, activeMembers=185
curl GET /api/v1/members/4dcf7012-3f9f-43a2-8093-f359ae204622/signup -> 200
curl POST /api/v1/members/4dcf7012-3f9f-43a2-8093-f359ae204622/rejection -> 200
PASS mailbox N-03 correlated by notificationId; locale=ca
PASS D1 dashboard + counters: pending=3, olderThanWarn=1, activeMembers=185
PASS signup without family -> rejection -> N-03; D1 counters restored
curl GET /api/v1/parameters/signup.enabled -> 200
curl PUT /api/v1/parameters/signup.enabled -> 200
curl POST /api/v1/signup -> 422 SIGNUP_CLOSED
PASS signup.enabled=false closes the form; canonical catalog status is 422 SIGNUP_CLOSED
PASS T-04-34 / T-14-11: E3 backend gate completed; credentials and capabilities withheld
Removed disposable Compose services, volumes and private mailbox; existing stacks preserved
```

**3. `/Users/jordib/Dropbox/Documents/SOFTWARE_CANIC/05-desenvolupament/roadmap-kit/mac/heavy.sh bin/openapi-snapshot > roadmap/evidence/E3-T16/12-openapi-snapshot.log 2>&1; echo "exit $?"`: exit 0**, then `git diff --stat docs/openapi/ > roadmap/evidence/E3-T16/13-openapi-diff-stat.log 2>&1; echo "exit $?"`: exit 0.
- This ran before the schema-only change of log `14`, which is not part of the OpenAPI document. The final `clean verify` (run 1) passes `OpenApiSnapshotTest`, so the committed snapshot equals the generated one for the final code.
- Changes against HEAD, computed from the two JSON documents: 30 operations changed (29 with the shared `size` parameter, plus `GET /me/dogs`). 2 schemas added (`DogDocumentType`, `LegalAddress`) and 4 changed (`ClubSettings`, `ClubSummary`, `ClubUpdate`, `MeDogs`). None removed.
- `git diff --stat docs/openapi/`:
```
 docs/openapi/CHANGELOG.md |  19 +++++
 docs/openapi/openapi.json | 180 ++++++++++++++++++++++++++++++----------------
 2 files changed, 136 insertions(+), 63 deletions(-)
```
- Last 40 lines of `12-openapi-snapshot.log`:
```
 =========|_|==============|___/=/_/_/_/

 :: Spring Boot ::               (v3.5.16)

2026-09-25T20:57:44.776+02:00  INFO 50529 --- [agilityhub-core-api] [           main] c.a.c.configuration.OpenApiSnapshotTest  : Starting OpenApiSnapshotTest using Java 21.0.12.1 with PID 50529 (started by jordib in /Users/jordib/dev/agilityhub/agilityhub-core-api)
2026-09-25T20:57:44.776+02:00  INFO 50529 --- [agilityhub-core-api] [           main] c.a.c.configuration.OpenApiSnapshotTest  : The following 1 profile is active: "test"
2026-09-25T20:57:45.805+02:00  INFO 50529 --- [agilityhub-core-api] [           main] .s.d.r.c.RepositoryConfigurationDelegate : Bootstrapping Spring Data MongoDB repositories in DEFAULT mode.
2026-09-25T20:57:45.842+02:00  INFO 50529 --- [agilityhub-core-api] [           main] .s.d.r.c.RepositoryConfigurationDelegate : Finished Spring Data repository scanning in 34 ms. Found 0 MongoDB repository interfaces.
2026-09-25T20:57:46.260+02:00  INFO 50529 --- [agilityhub-core-api] [           main] o.s.b.w.embedded.tomcat.TomcatWebServer  : Tomcat initialized with port 0 (http)
2026-09-25T20:57:46.269+02:00  INFO 50529 --- [agilityhub-core-api] [           main] o.apache.catalina.core.StandardService   : Starting service [Tomcat]
2026-09-25T20:57:46.269+02:00  INFO 50529 --- [agilityhub-core-api] [           main] o.apache.catalina.core.StandardEngine    : Starting Servlet engine: [Apache Tomcat/10.1.55]
2026-09-25T20:57:46.295+02:00  INFO 50529 --- [agilityhub-core-api] [           main] o.a.c.c.C.[Tomcat].[localhost].[/]       : Initializing Spring embedded WebApplicationContext
2026-09-25T20:57:46.295+02:00  INFO 50529 --- [agilityhub-core-api] [           main] w.s.c.ServletWebServerApplicationContext : Root WebApplicationContext: initialization completed in 1513 ms
2026-09-25T20:57:46.413+02:00  INFO 50529 --- [agilityhub-core-api] [           main] org.mongodb.driver.client                : MongoClient with metadata {"driver": {"name": "mongo-java-driver|sync|spring-boot", "version": "5.5.2"}, "os": {"type": "Darwin", "name": "Mac OS X", "architecture": "aarch64", "version": "26.4.1"}, "platform": "Java/Homebrew/21.0.12.1"} created with settings MongoClientSettings{readPreference=primary, writeConcern=WriteConcern{w=null, wTimeout=null ms, journal=null}, retryWrites=true, retryReads=true, readConcern=ReadConcern{level=null}, credential=null, transportSettings=null, commandListeners=[io.micrometer.core.instrument.binder.mongodb.MongoMetricsCommandListener@1575c8cc], codecRegistry=ProvidersCodecRegistry{codecProviders=[ValueCodecProvider{}, BsonValueCodecProvider{}, DBRefCodecProvider{}, DBObjectCodecProvider{}, DocumentCodecProvider{}, CollectionCodecProvider{}, IterableCodecProvider{}, MapCodecProvider{}, GeoJsonCodecProvider{}, GridFSFileCodecProvider{}, Jsr310CodecProvider{}, JsonObjectCodecProvider{}, BsonCodecProvider{}, EnumCodecProvider{}, com.mongodb.client.model.mql.ExpressionCodecProvider@1c71fb74, com.mongodb.Jep395RecordCodecProvider@15999360, com.mongodb.KotlinCodecProvider@3f1bfe98]}, loggerSettings=LoggerSettings{maxDocumentLength=1000}, clusterSettings={hosts=[localhost:49235], srvServiceName=mongodb, mode=SINGLE, requiredClusterType=UNKNOWN, requiredReplicaSetName='null', serverSelector='null', clusterListeners='[]', serverSelectionTimeout='30000 ms', localThreshold='15 ms'}, socketSettings=SocketSettings{connectTimeoutMS=10000, readTimeoutMS=0, receiveBufferSize=0, proxySettings=ProxySettings{host=null, port=null, username=null, password=null}}, heartbeatSocketSettings=SocketSettings{connectTimeoutMS=10000, readTimeoutMS=10000, receiveBufferSize=0, proxySettings=ProxySettings{host=null, port=null, username=null, password=null}}, connectionPoolSettings=ConnectionPoolSettings{maxSize=100, minSize=0, maxWaitTimeMS=120000, maxConnectionLifeTimeMS=0, maxConnectionIdleTimeMS=0, maintenanceInitialDelayMS=0, maintenanceFrequencyMS=60000, connectionPoolListeners=[io.micrometer.core.instrument.binder.mongodb.MongoMetricsConnectionPoolListener@413721f], maxConnecting=2}, serverSettings=ServerSettings{heartbeatFrequencyMS=10000, minHeartbeatFrequencyMS=500, serverMonitoringMode=AUTO, serverListeners='[]', serverMonitorListeners='[]'}, sslSettings=SslSettings{enabled=false, invalidHostNameAllowed=false, context=null}, applicationName='null', compressorList=[], uuidRepresentation=JAVA_LEGACY, serverApi=null, autoEncryptionSettings=null, dnsClient=null, inetAddressResolver=null, contextProvider=null, timeoutMS=null}
2026-09-25T20:57:46.418+02:00  INFO 50529 --- [agilityhub-core-api] [localhost:49235] org.mongodb.driver.cluster               : Monitor thread successfully connected to server with description ServerDescription{address=localhost:49235, type=REPLICA_SET_PRIMARY, cryptd=false, state=CONNECTED, ok=true, minWireVersion=0, maxWireVersion=21, maxDocumentSize=16777216, logicalSessionTimeoutMinutes=30, roundTripTimeNanos=11241209, minRoundTripTimeNanos=0, setName='docker-rs', canonicalAddress=7d3b95b59265:27017, hosts=[7d3b95b59265:27017], passives=[], arbiters=[], primary='7d3b95b59265:27017', tagSet=TagSet{[]}, electionId=7fffffff0000000000000001, setVersion=1, topologyVersion=TopologyVersion{processId=6ab6c42759d23834663f340a, counter=6}, lastWriteDate=Fri Sep 25 20:57:44 CEST 2026, lastUpdateTimeNanos=166471792787250}
2026-09-25T20:57:46.903+02:00  INFO 50529 --- [agilityhub-core-api] [           main] o.s.boot.web.servlet.RegistrationBean    : Filter rateLimitFilter was not registered (disabled)
2026-09-25T20:57:49.111+02:00  INFO 50529 --- [agilityhub-core-api] [           main] o.a.c.c.C.[Tomcat].[localhost].[/]       : Initializing Spring TestDispatcherServlet ''
2026-09-25T20:57:49.111+02:00  INFO 50529 --- [agilityhub-core-api] [           main] o.s.t.web.servlet.TestDispatcherServlet  : Initializing Servlet ''
2026-09-25T20:57:49.112+02:00  INFO 50529 --- [agilityhub-core-api] [           main] o.s.t.web.servlet.TestDispatcherServlet  : Completed initialization in 1 ms
2026-09-25T20:57:49.181+02:00  INFO 50529 --- [agilityhub-core-api] [           main] o.s.b.w.embedded.tomcat.TomcatWebServer  : Tomcat started on port 49239 (http) with context path '/'
2026-09-25T20:57:49.212+02:00  INFO 50529 --- [agilityhub-core-api] [           main] o.s.b.w.embedded.tomcat.TomcatWebServer  : Tomcat initialized with port 0 (http)
2026-09-25T20:57:49.212+02:00  INFO 50529 --- [agilityhub-core-api] [           main] o.apache.catalina.core.StandardService   : Starting service [Tomcat]
2026-09-25T20:57:49.212+02:00  INFO 50529 --- [agilityhub-core-api] [           main] o.apache.catalina.core.StandardEngine    : Starting Servlet engine: [Apache Tomcat/10.1.55]
2026-09-25T20:57:49.222+02:00  INFO 50529 --- [agilityhub-core-api] [           main] o.a.c.c.C.[Tomcat-1].[localhost].[/]     : Initializing Spring embedded WebApplicationContext
2026-09-25T20:57:49.222+02:00  INFO 50529 --- [agilityhub-core-api] [           main] w.s.c.ServletWebServerApplicationContext : Root WebApplicationContext: initialization completed in 40 ms
2026-09-25T20:57:49.227+02:00  INFO 50529 --- [agilityhub-core-api] [           main] o.s.b.a.e.web.EndpointLinksResolver      : Exposing 2 endpoints beneath base path '/actuator'
2026-09-25T20:57:49.243+02:00  INFO 50529 --- [agilityhub-core-api] [           main] o.s.b.w.embedded.tomcat.TomcatWebServer  : Tomcat started on port 49240 (http) with context path '/'
2026-09-25T20:57:49.251+02:00  INFO 50529 --- [agilityhub-core-api] [           main] c.a.c.configuration.OpenApiSnapshotTest  : Started OpenApiSnapshotTest in 4.682 seconds (process running for 7.371)
Mockito is currently self-attaching to enable the inline-mock-maker. This will no longer work in future releases of the JDK. Please add Mockito as an agent to your build as described in Mockito's documentation: https://javadoc.io/doc/org.mockito/mockito-core/latest/org.mockito/org/mockito/Mockito.html#0.3
WARNING: A Java agent has been loaded dynamically (/Users/jordib/.m2/repository/net/bytebuddy/byte-buddy-agent/1.17.8/byte-buddy-agent-1.17.8.jar)
WARNING: If a serviceability tool is in use, please run with -XX:+EnableDynamicAgentLoading to hide this warning
WARNING: If a serviceability tool is not in use, please run with -Djdk.instrument.traceUsage for more information
WARNING: Dynamic loading of agents will be disallowed by default in a future release
OpenJDK 64-Bit Server VM warning: Sharing is only supported for boot loader classes because bootstrap classpath has been appended
2026-09-25T20:57:51.389+02:00  INFO 50529 --- [agilityhub-core-api] [           main] o.springdoc.api.AbstractOpenApiResource  : Init duration for springdoc-openapi is: 1457 ms
2026-09-25T20:57:51.924+02:00  INFO 50529 --- [agilityhub-core-api] [omcat-handler-0] o.a.c.c.C.[Tomcat].[localhost].[/]       : Initializing Spring DispatcherServlet 'dispatcherServlet'
2026-09-25T20:57:51.924+02:00  INFO 50529 --- [agilityhub-core-api] [omcat-handler-0] o.s.web.servlet.DispatcherServlet        : Initializing Servlet 'dispatcherServlet'
2026-09-25T20:57:51.925+02:00  INFO 50529 --- [agilityhub-core-api] [omcat-handler-0] o.s.web.servlet.DispatcherServlet        : Completed initialization in 1 ms
2026-09-25T20:57:52.876+02:00  INFO 50529 --- [agilityhub-core-api] [           main] o.springdoc.api.AbstractOpenApiResource  : Init duration for springdoc-openapi is: 888 ms
Updated docs/openapi/openapi.json
```

### Assumptions
1. **The `taxId` check is new.** S02 §3 says «`taxId` validat pel perfil de país», and the task says that «the Spanish country profile must accept» G63189617. Nothing checked `taxId` before, so the check is added now, as `CountryProfile.validateTaxId`:
   - `ES`: a CIF (entity letter, 7 digits, the control as a digit or a `JABCDEFGHI` letter by entity kind), or a NIF (DNI) or NIE, each with its check character;
   - `GENERIC`: 4–40 letters and digits once separators are removed;
   - a blank value means unset.

   `PUT /club` answers `400 VALIDATION_ERROR`, like its other field checks. `club:apply` answers `VALIDATION_ERROR {field: club.taxId}`. SettingsIT's fixture value `EXAMPLE-ORG` (ES club) became the fictional `B12345674`.
2. **The page-size clamp is removed.** `ListQuery` used to cut any `size` above 1000 to 1000, and a unit test pinned that. CONVENCIONS_API §4 (amended 25-09) says «qualsevol altre valor → 400 INVALID_FILTER», and the task says the same, so a `size` above 1000 is now refused. `GET /tasks` (S10 stub) declares its own `size` and is unchanged.
3. **The OpenAPI form of `legalAddress`.** `ClubSummary.legalAddress` is required and nullable. `RequiredPropertiesModelConverter` treats `nullable = true` as optional, so I used the form D1's `riskReview` already has: a `$ref` with `type: [object, null]`. `ClubSummary.city` is now required too, as `string | null`: the JSON always carried it.
4. **`legalAddress.city`** is `address.city`, and may be `null`. Following R-02-02 to the letter, only a missing street or postal code makes `legalAddress` `null`.
5. **Real data in the seed.** The Cànic's legal name, CIF and registered office are real, public organisation identifiers, placed by the product owner (MESSAGES 25-09). They are not personal data. Every person in the seeds stays fictional.
6. **The capped codes.** `capped()` takes its code from `CAPPED`. An unmapped event type now fails loudly (`requireNonNull`) instead of deciding under a wrong code. The notification codes that are sent stay literal.
7. **The P9 counter key** is `ttlPendingSignupNotificationAdmissions`. It is camelCase like the other `ttlPending*` keys, because counter keys carry no dots. R-15-19 writes these as `ttlPending.*`.
8. **The lock test** calls the private `decide` through reflection from two threads. The first thread's insert is held on a latch, and the test waits until the second thread is either done or queued on `deciding`. This makes it deterministic in both directions (log `06b`).

### Questions / catalog proposals
- No new parameter, event, notification or error code.
- Doc proposals for the organizer:
  - (a) S02 T-02-04 could name the `taxId` check: ES CIF G63189617 valid, a wrong control invalid.
  - (b) S15 R-15-19 could name the counter `ttlPendingSignupNotificationAdmissions`.
  - (c) CONVENCIONS_API §4 could add that `size` is published as an `enum`.
- For the web (E3-W12), regenerate the client:
  - `MeDogs.documentTypes`;
  - `ClubSummary.city` is required and nullable;
  - `ClubSummary.legalAddress` is required and nullable;
  - `ClubSettings.displayCity` and `ClubUpdate.displayCity`;
  - `size` is an enum, and a size above 1000 is `400`;
  - `PUT /club` may answer `400` for a `taxId`.

## Organizer verification
Result: changes_requested · 2026-09-25 · organizer (Claude)

Steps 1, 2 and 4 are in place with their tests, and step 3's seed and `/branding` fields are right:
- `GET /me/dogs` returns `documentTypes` in the reader's locale;
- `level` follows `levels.enabled` (pinned);
- the Cànic seed carries its legal identity, its registered office and `displayCity: "Cabrera de Mar"`, and `/branding` gives `city` and `legalAddress`;
- the E3-T15 follow-ups, P9's TTL list and the list `size` enum.

Evidence:
- `clean verify`: 542 unit and 950 integration tests;
- `bin/e3-smoke`: passes twice;
- the snapshot is regenerated;
- CI green at `5b158ae`.

The independent review (`roadmap/reviews/E3-T16-20260925-2117-claude.md`) found one major, one minor and three nits. Claude reviewed, because Codex could not answer. The organizer confirmed the major against S02: the `GENERIC` profile must not validate the tax id.

Organizer decisions:
- **The list `size` is now strict.** A size above 1000 answers `400 INVALID_FILTER` instead of being cut to 1000. That is what CONVENCIONS_API §4 says since 25-09, and no web caller sends more than 1000. Accepted.
- **Review #4:** S02 R-02-10 is amended. The Cànic seed now carries its real legal identity.

Round 2 — fix exactly these points:
1. **`GENERIC` does not validate the tax id (review #1; S02 §2, R-02-06, T-02-04).** Accept any value. The `GENERIC` half of the test asserts that `"AB1"` and `"ÀÉÍ-ÒÚ"` are accepted.
2. **The tax id is checked only when it changes (review #2, R-02-06).**
   - In `PUT /club`, check it only when the request has `taxId` and it differs from the stored one.
   - In `club:apply`, check it only when the definition's `taxId` differs from the stored one.
   - One IT for each case: a profile switch keeps an old tax id, and an `ES` club whose stored tax id the check refuses can still save another setting.
3. **`displayCity` can be cleared (review #3).** Type it `["string", "null"]` in `ClubSettings` and `ClubUpdate`, and regenerate the snapshot.
4. **`details.field` on a refused tax id in `PUT /club` (review #5)**, as `club:apply` already does.
5. **Nulls the snapshot does not declare (web E3-W08 round-2 report).** The core sends absent optional fields as `null`, but the snapshot marks them optional, not nullable. Examples: `readmission.submitted.paymentMethod.maskedAccount` and `channel`; `member.plan`, `planId` and `maskedAccount` in the D2 view. Declare as nullable every field of `MemberSignupView`, `SignupResult` and `AddDogSignupResult` that the core can send as `null`, as E3-T10 did for the enums. Add a contract test that reads real D2 and submission responses and fails on any `null` the snapshot does not declare.
