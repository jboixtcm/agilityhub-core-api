---
id: E2-T10
title: Playoff migration: MappingConfig, input schema, migration:anonymize and migration:playoff --dry-run (S18 WP-18-A/B)
stage: E2
repo: agilityhub-core-api
thread: A
order: 100
status: not_open
depends_on: [E2-T06, external:api:E1-T02, external:jordi-playoff-column-names]
branch: 
pr: 
updated: 2026-09-06
---

# E2-T10 · Playoff migration: MappingConfig, input schema, migration:anonymize and migration:playoff --dry-run (S18 WP-18-A/B)

## Objective
Work packages WP-18-A and WP-18-B: the census importer runs end to end in dry-run over anonymised fixtures. **Needs Jordi/Josep**: the column names of the Playoff exports (members, dogs, plan types, receipts, mandates) — never values.

## Spec references
`docs/specs/S18-migracio-playoff.md` §3 (mapping), §4 steps 1–6 and 9, §11 tests **T-18-01, 02, 04, 06, 08, 10** · `docs/decisions/ADR-008-*.md` · `docs/DECISIONS_PENDENTS.md` A24, B7, B8, B9 · S01 §14 (onboarding of migrated accounts).

## Context to load
S18 §3, §4, §6 (CLI), §11 · ADR-008 · this file.

## Steps
1. `migration/` context: `MappingConfig` v1 (YAML: Playoff plan types → plans, level names → levels, column mapping per file), input schema (`docs/migration/playoff-input.md` listing expected columns; unknown/missing columns reported, not guessed).
2. `migration:anonymize <dir> <out>`: replaces names, emails, phones, id documents, IBANs and addresses with fictional consistent values (same input → same output), keeps structure; used to build `src/test/resources/fixtures/playoff/` (commit only anonymised fixtures).
3. `migration:playoff <dir> [--dry-run] [--club=canic]`: steps 1–6 (validate, members, dogs, family groups, levels/plans linking, accounts with `onboardingPending`) and 9 (report: created/updated/skipped/errors with row references); idempotent by Playoff ids stored in `externalIds.playoff`; dry-run writes nothing.
4. Members who left more than 5 years ago are skipped (A24a).

## Verification (run all; paste full output in the report)
- `./mvnw -q verify` → green with T-18-01, 02, 04, 06, 08, 10 over the anonymised fixtures.
- `bin/core migration:playoff src/test/resources/fixtures/playoff --dry-run` → report pasted.

## Definition of Done
Importer steps 1–6 + 9 with report; anonymiser; no real data anywhere.

## Evidence to return
Outputs above.

---

## Executor report
_(not started)_

### Branch / commits
### Evidence (full command outputs)
### Files changed
### Rules and tests implemented (R-xx-nn / T-xx-nn)
### Assumptions
### Questions / catalog proposals

## Organizer verification
_(pending)_
