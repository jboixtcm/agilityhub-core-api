---
id: E2-T10
title: Playoff migration: MappingConfig, input schema, migration:anonymize and migration:playoff --dry-run (S18 WP-18-A/B)
stage: E2
repo: agilityhub-core-api
thread: A
order: 100
status: blocked
depends_on: [E2-T06, external:api:E1-T02, external:jordi-playoff-column-names]
branch: 
pr: 
updated: 2026-09-09
---

# E2-T10 · Playoff migration: MappingConfig, input schema, migration:anonymize and migration:playoff --dry-run (S18 WP-18-A/B)

## Objective
Work packages WP-18-A and WP-18-B: the census importer runs end to end in dry-run over anonymised fixtures. **The column names are known (09-09)**: `docs/MAPATGE_CAMPS_PLAYOFF.md` documents the three exports (`Socis` 51 columns, `Associats / Tipologia`, `Associats / Nivell`, joined by `ID associat`), the column → field mapping, the measured incidents and the open questions B29–B33 (answers arrive later from Josep: keep those cases as warnings, never guess). Receipts and mandates are **not** exported by Playoff (all mandates are new, S12) — no receipt file in this task.

## Spec references
`docs/specs/S18-migracio-playoff.md` §3 (mapping), §4 steps 1–6 and 9, §11 tests **T-18-01, 02, 04, 06, 08, 10** · `docs/decisions/ADR-008-*.md` · `docs/DECISIONS_PENDENTS.md` A24, B7, B8, B9 · S01 §14 (onboarding of migrated accounts).

## Context to load
S18 §3, §4, §6 (CLI), §11 · ADR-008 · **`docs/MAPATGE_CAMPS_PLAYOFF.md`** (§2 column table, §4 level marks, §6 incident table with codes `NO_BANK_ACCOUNT`, `DOG_INFERRED`, `CHIP_MISSING`, `EMAIL_SHARED`, `AGE_SUSPECT`, `NUMBER_CONFLICT`) · E2-T06 census entities (`Dog.handlerName`, `licenses[].category/division`) · this file.

## Steps
1. `migration/` context: `MappingConfig` v1 (YAML: Playoff plan types → plans, level names → levels, column mapping per file) **pre-filled from `docs/MAPATGE_CAMPS_PLAYOFF.md`** (the typology → plan and level-name → level tables; unknown values → warning rows, e.g. «Quota reduïda», «Familiar Abonat/curs», level «Pendent» until B30–B32 are answered), input schema (`docs/migration/playoff-input.md` listing the 51 + tipologia + nivell columns; unknown/missing columns reported, not guessed). Fixtures: build the anonymised set with the **same column headers** as the real exports and the incident mix of §6 (26 no IBAN, 9 without dog, 16 without chip, 7 without email, ~20 shared emails, 4 duplicate numbers, 2 age suspects, 1 duplicate id document) so every incident code has a test row.
2. `migration:anonymize <dir> <out>`: replaces names, emails, phones, id documents, IBANs and addresses with fictional consistent values (same input → same output), keeps structure; used to build `src/test/resources/fixtures/playoff/` (commit only anonymised fixtures).
3. `migration:playoff <dir> [--dry-run] [--club=canic]`: steps 1–6 (validate, members, dogs, family groups, levels/plans linking, accounts with `onboardingPending`) and 9 (report: created/updated/skipped/errors with row references); idempotent by Playoff ids stored in `externalIds.playoff`; dry-run writes nothing.
4. Members who left more than 5 years ago are skipped (A24a: of the 1 252 «Baixa» records only those since 2021 are migrated, as `LEFT` members with their leave date); surnames carrying «(dog name)» are cleaned per R-18-04(c); one person with two records (same id document) = one member with two dogs (R-18-04(a)); migrated members skip the signup age validation (`AGE_SUSPECT` warning) and get `onboardingPending` (S01 §14).

## Verification (run all; evidence = command + exit code + last 40 lines; long outputs → `roadmap/evidence/<ID>/*.log`)
- `./mvnw -q verify` → green with T-18-01, 02, 04, 06, 08, 10 over the anonymised fixtures.
- `bin/core migration:playoff src/test/resources/fixtures/playoff --dry-run` → report pasted.

## Definition of Done
Importer steps 1–6 + 9 with report; anonymiser; no real data anywhere.

## Evidence to return
Outputs above.

---

## Executor report

Blocked: concurrent sessions are editing the same migration implementation. This session selected E2-T10 with `--next`, marked it `in_progress`, and began implementation. While it was running, another process committed the partial tree and replaced `PlayoffImportService.java`, added `MigrationApplyService.java` and `PlayoffAnonymizeCommand.java`, and changed `CensusMigrationService.java`. The resulting implementations disagree. Editing stopped to avoid overwriting the other writer. Resume with one writer and reconcile the two implementations before verification.

### Branch / commits
No git write command was run by this session. Read-only `git log` showed another process published `executor: E2-T10 in_progress [skip ci]` at 21:53:37 +02:00. The task's dependency E2-T06 was `awaiting_verification` when selected; an organizer process subsequently verified it during this session.

### Evidence (full command outputs)
Required verification is **not complete**. `./mvnw -q verify` and the required `bin/core migration:playoff src/test/resources/fixtures/playoff --dry-run` are pending reconciliation of the concurrent implementations.

Earlier attempt: `./mvnw -q -DskipTests compile` exited 1 because the default Maven wrapper directory is outside writable roots; switched Maven caches to `/private/tmp/e1-t09-maven`. Output: `roadmap/evidence/E2-T10/01-compile.log`.

Command:
```sh
export MAVEN_USER_HOME=/private/tmp/e1-t09-maven
export MAVEN_OPTS='-Dmaven.repo.local=/private/tmp/e1-t09-maven/repository'
./mvnw -q -DskipTests compile
```
Exit code: 0. Last 40 lines: no output. Full output: `roadmap/evidence/E2-T10/02-compile.log`.

Command:
```sh
export MAVEN_USER_HOME=/private/tmp/e1-t09-maven
export MAVEN_OPTS='-Dmaven.repo.local=/private/tmp/e1-t09-maven/repository'
./mvnw -q -DskipTests package
```
Exit code: 0. Last 40 lines: no output. Full output: `roadmap/evidence/E2-T10/03-package.log`.

Command:
```sh
export MIGRATION_ANONYMIZE_KEY="$(openssl rand -hex 32)"
bin/core migration:anonymize /private/tmp/e2t10-synthetic-source src/test/resources/fixtures/playoff
```
Exit code: 0. Full output and last 40 lines (`roadmap/evidence/E2-T10/04-anonymize.log`):
```text
Anonymized Playoff files written; no input values or key logged.
```
The input was generated from fictional values in `/private/tmp`; no real Playoff export was accessed. The one-use key was not printed or saved. Committed fixture candidates are the anonymizer's output.

Command:
```sh
export MAVEN_USER_HOME=/private/tmp/e1-t09-maven
export MAVEN_OPTS='-Dmaven.repo.local=/private/tmp/e1-t09-maven/repository'
./mvnw -q -Dtest=PlayoffMigrationIT test
```
Exit code: 1. Concurrent implementation mismatch; no tests executed. Full output and last 40 lines (`roadmap/evidence/E2-T10/05-migration-tests.log`):
```text
[ERROR] COMPILATION ERROR : 
[ERROR] /Users/jordib/dev/agilityhub/agilityhub-core-api/src/main/java/com/agilityhub/core/migration/application/MigrationApplyService.java:[55,20] cannot find symbol
  symbol:   method save(com.agilityhub.core.migration.persistence.MigrationRun)
  location: variable runs of type com.agilityhub.core.migration.persistence.MigrationRunRepository
[ERROR] Failed to execute goal org.apache.maven.plugins:maven-compiler-plugin:3.14.1:compile (default-compile) on project agilityhub-core-api: Compilation failure
[ERROR] /Users/jordib/dev/agilityhub/agilityhub-core-api/src/main/java/com/agilityhub/core/migration/application/MigrationApplyService.java:[55,20] cannot find symbol
[ERROR]   symbol:   method save(com.agilityhub.core.migration.persistence.MigrationRun)
[ERROR]   location: variable runs of type com.agilityhub.core.migration.persistence.MigrationRunRepository
[ERROR] 
[ERROR] -> [Help 1]
[ERROR] 
[ERROR] To see the full stack trace of the errors, re-run Maven with the -e switch.
[ERROR] Re-run Maven using the -X switch to enable full debug logging.
[ERROR] 
[ERROR] For more information about the errors and possible solutions, please read the following articles:
[ERROR] [Help 1] http://cwiki.apache.org/confluence/display/MAVEN/MojoFailureException
```

### Files changed
Partial implementation spans `migration/{application,domain,persistence}`, `src/main/resources/migration/playoff-v1.yaml`, migration application/persistence adapters in census/catalogs/platform/identity, census provenance fields and indexes, encrypted-bank masking, `CoreApplication.java`, `application.yml`, `.env.example`, and `src/test/java/com/agilityhub/core/migration/PlayoffMigrationIT.java`. Fixture candidates are in `src/test/resources/fixtures/playoff/`. Evidence is in `roadmap/evidence/E2-T10/`. Some partial files were committed by the other process while this session ran. Input documentation and CHANGELOG are still pending; do not interpret the working tree as a finished implementation.

### Rules and tests implemented (R-xx-nn / T-xx-nn)
Partial, unverified candidates for R-18-02/04/05/06/07/08/11b/12/14/15 and T-18-01/02/04/06/08/10. The fixture includes 184 active source rows, a duplicate identity document, 26 missing IBANs, 9 inferred dogs, 16 missing chips, 7 missing primary emails, 20 shared-email rows, 4 number conflicts, 2 age suspects, recent and excluded leavers, unresolved mappings and explicit fictional payer links. Anonymizer regression tests, complete coverage and final full-suite evidence remain required.

### Assumptions
- Model `sourceIds` is authoritative; `externalIds.playoff` additionally retains all merged member source IDs to meet the task's requested lookup.
- Unconfirmed family relationships must not be inferred from email or a plan. `family_groups.csv` is an explicit auxiliary adapter, not an asserted real Playoff export.
- The documentation does not enumerate the entire Tipologia/Nivell header rows. The current adapter declares known fields (`ID associat`, `Tipologia`; `ID associat`, `Subcategoria`, `Data assignació`) and reports schema mismatches; final input documentation must explain this assumption.
- The task places new mandates in S12. Imported IBANs are an encrypted `ibanEncrypted`/`ibanLast4` handoff, using environment-only `MIGRATION_BANK_KEY`; no mandate is issued. Final tests/docs must substantiate this boundary.
- Reconcile the alternative import entry points, audit approach, transaction strategy, role/member linkage and error handling before choosing final behavior. Current code is not approved for operational imports.

### Questions / catalog proposals
@organizer / @jordi: which executor owns E2-T10? Stop the competing writer before resuming this working tree. Assumption meanwhile: preserve both sets of partial work and stop edits. No catalog additions were made; final warning-code review remains pending. See the concurrent-writer entry in `roadmap/MESSAGES.md`.

## Organizer verification
_(pending)_
