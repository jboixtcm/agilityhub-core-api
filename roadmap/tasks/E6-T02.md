---
id: E6-T02
title: Attendance and instructor aggregates (S10 WP-10-B): Attendance, AttendanceSheetService in one transaction with the S08 cancellation of «ha avisat», attendanceSummary with optimistic locking, claimForNoShowNotice, InstructorDayQuery, WeekAgendaQuery + landscape PDF, InstructorCardQuery metrics, HistoryQuery and GET /attendances
stage: E6
repo: agilityhub-core-api
thread: A
order: 20
status: awaiting_verification
depends_on: [E6-T01]
branch:
pr:
updated: 2026-09-26
---

# E6-T02 · Attendance, instructor day/week and history (S10 WP-10-B)

## Objective
Work package S10 WP-10-B. An instructor opens the day (20), taps one circle per dog on the class sheet (21) and saves: `PRESENT` and `NO_SHOW` are plain marks inside an edit window, and **`NOTIFIED` («ha avisat») cancels the booking through S08 inside the very same Mongo transaction**, so the seat is released and the waitlist is notified without any chance of an overbooked class. The same save feeds `ClassSession.attendanceSummary` with an optimistic-locking `version`, so two instructors on the same class never lose each other's work. On top of that sit the three read aggregates the screens need — the instructor day (20), the week agenda with its landscape PDF (D12) and the student card with the 30-day metrics (22/D13) — plus the member's own history (25) and the universal `/attendances` list. Tasks, attachments, observations and the D14 follow-up are E6-T03; the P3 and P8 scheduled jobs are E6-T04.

## Spec references
S10 R-10-00 (who the «guia» is), **R-10-01** (global vision and the default instructor filter), **R-10-02** (which rows the sheet carries), **R-10-03** (`T0`/`T1` window, roles, `NOTIFIED` is final), **R-10-04** (bulk save, `version`, idempotency, the transaction skeleton), **R-10-05** (the six-row «ha avisat» table), **R-10-06** (no-show mark and `claimForNoShowNotice`), R-10-07 (packs and single class), **R-10-08** (30-day metrics), R-10-09 (`lastClasses` and «fa n mesos»), R-10-14 (history mapping table), **R-10-15** (week agenda and PDF), R-10-16 (club variants), §5 (both transition tables), §6 (the five JSON forms and the error paragraph), §7 (emitted events; the contracts with S06, S08, S09/S07, S12, S15), §8 (N-05, N-15), §9, §12 WP-10-B, §13 (doubts 1–8, 11, 14, 15) · tests **T-10-01, T-10-02, T-10-03, T-10-04, T-10-05, T-10-07, T-10-08, T-10-09, T-10-10, T-10-11, T-10-12, T-10-13, T-10-14, T-10-19, T-10-20, T-10-22** (attendance half), **T-10-23, T-10-24, T-10-33** · S08 **R-08-10** (cancellation preconditions incl. the `Attendance` guard), **R-08-11** (`SeatReleased` and `notifyWaitlist`), R-08-16, R-08-17 (pack refund only on `CANCELLED`), **R-08-19** (`origin = INSTRUCTOR` → N-05, no SMS) · S06 R-06-13, R-06-14, `CalendarQuery` form B · S09 R-09-12 (`TrainingOccupancyService.occupancy`) and the `TrainingHistoryQuery` / `TrainingStatsQuery` contracts of S10 §7 · S07 (`ActivityHistoryQuery`) · S14 R-14-09 (`ATTENDANCE_OVERRIDDEN`), R-14-10 (`@Audited`) · `CATALEG_ERRORS.md` row S10 + rule 0 · `CATALEG_NOTIFICACIONS.md` N-05, N-15 · `CATALEG_PARAMETRES.md` as listed in E6-T01 · `MATRIU_PERMISOS.md` rows «Assistència `/attendances`», «Classes `/class-sessions`» + rule 1.

## Context to load
`roadmap/tasks/E6-T01.md` (the contract you are filling in: schemas, documents, indexes, error statuses) · `docs/specs/S10-assistencia-tasques-i-seguiment.md` complete · `docs/specs/S08-*.md` R-08-02, R-08-10, R-08-11, R-08-16, R-08-17, R-08-19 and §6's transaction skeletons · `docs/specs/S06-*.md` R-06-13, R-06-14 · `docs/specs/S09-*.md` R-09-05, R-09-12 · `docs/specs/S15-*.md` R-15-13, R-15-18 (you expose the repository method they call, you do **not** schedule anything) · `src/main/java/com/agilityhub/core/clubs/bookings/**` from E5 — **read them before writing a line**: `BookingService`, `BookingConfirmationService`, **`BookingCancellationService`** (its `cancel(bookingId, by, origin, reason, message?)` and the record it returns), `SeatHoldService`, **`WaitlistService`** (`offerNext`, `cancelAll`, `sweepStarted`), `BookingTransactions` (the `seat_locks` serialization + retry-on-`WriteConflict` helper), `BookingEligibility`, `BookingLimits`, `InstructorVisibility`, the `ClassBookingsPort` / `BookingActivity` / `ClassOccupancyQuery` adapters and **`AttendanceStatePort`** (E5-T02 declared it with a null-object default — this task provides the real implementation) · `clubs/scheduling/application/{CalendarQuery,ClassSessionService,PlanningViews,SchedulingTransactions}.java` and `clubs/scheduling/persistence/{ClassSession,ClassSessionRepository,RingBlock,RingBlockRepository}.java` · `clubs/training/application/*` from E5-T04 (`TrainingOccupancyService`, `TrainingBookingService`, `TrainingBookingsQuery`) · `clubs/activities/application/ActivityQueryService.java` · `clubs/census/application/{DogService,MemberService}.java`, `clubs/catalogs/**` (`Level`, `Ring`, `Instructor`) · `clubs/common/application/{ListExportRenderer,ExportJobRenderer,ExportValues,ExportPolicy}.java` and `clubs/common/persistence/{S3ExportStorage,LocalExportStorage}.java` (PDFBox 3.0.8 + Thymeleaf are on the classpath) · `shared/application/lists/*` (universal list engine), `shared/application/{EventPublisher,OutboxDispatcher,DomainEventHandler,TenantContext,DogOwnerAccess}.java` · `platform/application/audit/*` · `docs/playbooks/{new-endpoint,new-entity,new-event-consumer,checklist}.md` · mockups `docs/pantalles/mobil/20-*.png`, `21-*.png`, `22-*.png`, `25-*.png` and `docs/pantalles/escriptori/D12-*.png`, `D13-*.png` (the api owes the front every decided value: the front computes nothing).

## Steps
1. **`AttendanceRepository`** (`clubs/bookings/persistence`) over the `attendances` document of E6-T01: `findByClassSession(classSessionId)`, `findByBooking(bookingId)`, `findByDogSince(dogId, from)`, `stateOf(bookingId)` (for `AttendanceStatePort`), `upsert` inside a transaction, and **`claimForNoShowNotice(clubId, today)`** (R-10-06 + R-15-13): a `findAndModify` over `state = NO_SHOW ∧ noShowNotice.queuedAt = null ∧ classDate < today` that writes `queuedAt` and `eventId` and returns the claimed documents, using the index `{clubId, state, "noShowNotice.queuedAt", classDate}`. Running it twice claims nothing new (**T-10-08**). E6-T04 calls it from the job; here it only has to exist, be tested and be reachable from a CLI command `attendance:claim-no-show [--club=]` in the style of `FinishEndedCommand`.
2. **`AttendanceWindow`** (pure domain, `Clock` injected, no Spring) — R-10-03: `T0 = 00:00` of `classDate` and `T1 = 23:59:59` of `classDate + attendance.editDays` (1), both in `club.timeZone`; `canMarkPresence(role, now)` → `INSTRUCTOR` only inside `[T0, T1]` (`422 ATTENDANCE_NOT_OPEN` before, `422 ATTENDANCE_WINDOW_CLOSED` after), **`ADMIN` always** (and every admin mark outside the window writes `AuditEntry{action: ATTENDANCE_OVERRIDDEN, entityType: "Attendance", entityId, details {classSessionId, bookingId, from, to}}`); `canMarkNotice(now)` → booking `ACTIVE`, `now ≤ T1` and `bookings.instructorLastMinuteNotice = true` (`422 INSTRUCTOR_NOTICE_DISABLED`); from `NOTIFIED` no transition at all (`422 ATTENDANCE_NOTIFIED_FINAL`); class `DRAFT` or `CANCELLED` → `422 INVALID_STATE`; class `FINISHED` stays markable until `T1`. **T-10-01** (Madrid and `America/Argentina/Buenos_Aires`), **T-10-02**.
3. **`AttendanceSheetQuery`** — `GET /class-sessions/{id}/attendance` (R-10-02): rows = bookings with `state ∈ {ACTIVE, PAYMENT_PENDING}` ∪ those whose `Attendance.state = NOTIFIED` (cancelled by «ha avisat», still visible and `final: true`); bookings cancelled by the member, by the club or by the system are **out**. Order `bookedAt` ascending (assumption in force, S10 §13-7). Each row: photo URL signed for 5 minutes (`AttachmentService.url`), `{guia} + {gos}` from `Dog.handlerName ?? Member.firstName` (R-10-00; when they differ the backoffice projection also carries `memberFullName`), `levelCode` (`null` when `levels.enabled = false`), `pendingTasksCount` from `DogFollowupPort` (null-object → field absent, which is also the `TASKS`-off shape), `notice`, `noShowNotice {scheduledFor = next occurrence of messaging.noShowNoticeTime, queuedAt, sentAt}`. `sheet {version, canMarkPresence, canMarkNotice, editableUntil, savedAt, savedByName, noShowNoticeTime}` from step 2 and from `attendanceSummary`. `waitlist {mode, fifoConfirmMinutes, entries[]}` from `WaitlistService` when `WAITLIST` is on, absent otherwise. Class `CANCELLED` → `canMarkPresence = false`, `canMarkNotice = false`, rows inert. **T-10-10**.
4. **`AttendanceSheetService.save(classSessionId, version, items, actor, idempotencyKey)`** — the heart of the stage, R-10-04, **one Mongo transaction** built on `BookingTransactions` (same `seat_locks` serialization per class that S08 uses, same retry on `WriteConflict`/`TransientTransactionError`): (1) `seat_locks.incVersion(classSessionId)`; (2) `version ≠ attendanceSummary.version` → `409 STALE_VERSION` with `details.current` = the whole current sheet; (3) for every item whose `state` differs from the stored one: validate step 2, require the booking to be live or already `NOTIFIED` (else `422 ATTENDANCE_BOOKING_NOT_ACTIVE {bookingId}`), apply, append to `history[]`, emit `AttendanceMarked {…, previousState}`; an item equal to the current state is a **no-op with no event** (resending the whole sheet is harmless); (4) recompute `attendanceSummary` (including `notifiedAfterEnd`, the `NOTIFIED` rows whose booking stayed `ACTIVE`, added by E6-T01 round 2) and `version + 1`, set `savedAt`/`savedByName`. **Any error rolls the whole thing back** — `422` on one of three items leaves the other two untouched (**T-10-12**). `Idempotency-Key` through the existing `idempotency_records` machinery: the same key returns the same body and emits each `AttendanceMarked` once. The `PUT` response is the `GET` shape plus `applied[]`. **T-10-11, T-10-12, T-10-23**.
5. **«Ha avisat» (R-10-05)** — inside that same transaction, marking `NOTIFIED` with `now < classEndsAt` calls `BookingCancellationService.cancel(bookingId, actor, origin = INSTRUCTOR, reason = INSTRUCTOR_NOTICE)` and stores its result in `notice {at, late, minutesBefore, seatReleased, waitlistNotified, afterClassEnd: false, bookingState}`. If E5-T02's method returns less than `{state, late, minutesBefore, seatReleased, waitlistNotified}`, widen its result record (internal, no contract impact) and say so in the report. Consequences are S08's, not yours: `late = now > classStartsAt − bookings.lateCancelThresholdMinutes` (**240**, threshold itself still in time) → `CANCELLED` with the pack refunded, otherwise `CANCELLED_LATE` with no refund; `SeatReleased {notifyWaitlist = WAITLIST ∧ minutesBefore > waitlist.notifyThresholdMinutes (30) ∧ there are ACTIVE entries}` → N-15 to the waiting members; **N-05 to the member with no SMS** (the action is theirs, S08 R-08-19). The moment that counts is the **save**, not when the member phoned (§13-5). With `classEndsAt ≤ now ≤ T1` the booking is no longer cancellable: record `NOTIFIED {afterClassEnd: true}`, leave the booking `ACTIVE` (it counts as attended) and notify nobody (§13-4). Implement **T-10-03** as a table-driven test with the six rows of R-10-05 (16:00, 16:50:00, 16:50:01, 18:20:00, 19:05, 20:30 over a class on Thu 15-10 18:50–19:50) asserting `late`, booking state, pack refund, `notifyWaitlist` and `afterClassEnd` exactly.
6. **`AttendanceStatePort` implementation** (`clubs/bookings/application`): `stateOf(bookingId)` for S08's `displayState` «no presentat» and for **R-08-10**'s precondition (a booking with `PRESENT`/`NO_SHOW` is no longer cancellable → `422 BOOKING_NOT_CANCELLABLE`). Until Jordi answers S10 §13-6, S08 keeps refusing and S10 additionally consumes `BookingCancelled {by ∈ MEMBER, ADMIN, SYSTEM}`: if an `Attendance` `PRESENT`/`NO_SHOW` exists it is a contradiction → append it to `history[]` and log a warning, change nothing else (S10 §7 consumer table).
7. **Event consumers** (`DomainEventHandler`, idempotent by `eventId`): `ClassSessionUpdated {startsAt}` → refresh the denormalized `classStartsAt`/`classEndsAt`/`classDate` of the class's attendances; `ClassCancelledByClub` / `ClassAutoCancelled` → nothing to write (the bookings become `CANCELLED_BY_CLUB` and fall out of the sheet and the metrics); `DogDeactivated` → nothing (attendance is kept); `BookingCancelled` → step 6.
8. **`InstructorDayQuery`** — `GET /instructor/day` (R-10-01): the dropdown lists active `Instructor`s and preselects `Membership.instructorId` of the caller, an `ADMIN` with no instructor profile getting the first by `shortName` alphabetical order (assumption in force); **no «Tot el club» entry**; 7 day chips from today with `hasClasses`; classes of the selected instructor only, but **ring blocks of everybody**; `individual = capacity == 1`; `attendance.status` from `AttendanceStatusCalculator` (`NONE`/`PENDING`/`DONE`/`CLOSED`, E6-T01); `waiting` only with `WAITLIST`. Empty day → empty `classes` (the literal «Cap classe aquest dia» is the front's). **T-10-09**.
9. **`WeekAgendaQuery`** — `GET /instructor/week` (R-10-15): ISO week of `date` in `club.timeZone`, Mon–Sat (Sunday only if it has items, assumption in force); classes from `CalendarQuery` form B (`ACTIVE` + `FINISHED` + `CANCELLED`, never `DRAFT`) each with `attendanceStatus` and `waiting`; trainings and ring blocks from `TrainingOccupancyService.occupancy(range, ringIds, INSTRUCTOR)` (S09 R-09-12: guide + dog on the training, reason + note on the block); `rows[]` = the distinct start times; a training occupies `training.slotMinutes` (the front paints it half height). Filters: `instructorId` narrows **classes only** (trainings and blocks always show), `ringId` narrows everything; `instructorId=me` resolves to the caller. `classes.maxInstructorsPerClass > 1` → `instructorName` joined («Marc, Neus») and `me` matches any of them. `FREE_TRAINING` off → no `TRAINING` cell. **T-10-20, T-10-33**.
10. **`GET /instructor/week/export?format=pdf`** — the same query rendered **landscape** with the club logo and theme colour, synchronous (`200 application/pdf`, `Content-Disposition` `{club.slug}_agenda_{yyyyMMdd}.pdf`), built with the existing PDFBox/Thymeleaf helpers of `clubs/common/application` (`ExportJobRenderer` is the closest pattern — reuse its fonts, header and footer «Pàgina {n} de {m}» + club name). Labels in the caller's locale from `messages_{ca,es,en}` (`export.column.instructor-week.*` and the day/hour headers), dates and times with `club.timeZone`. It is **not** an `ExportJob`: no `x-exportable`, no S3, no rate limit — say so in the report, since it is the only synchronous PDF in the product so far.
11. **`InstructorCardQuery`** — `GET /dogs/{id}/instructor-card` (R-10-08, R-10-09): window `W = [now − 30 days, now)` over `Booking.classStartsAt` of the dog; `present`, `noShow`, `notified` (in-time, informative), `cancelledLate` = `CANCELLED_LATE` bookings (by the member or by a late «ha avisat») **plus** `NOTIFIED {afterClassEnd}`; `classesCounted = present + noShow + cancelledLate`; `attendancePct = round(100 · present / classesCounted)`, `null` when the denominator is 0. **Assumption in force (S10 §13-1, still with Josep): `cancelledLate` is inside the denominator** — do not change it without a written answer. `ACTIVE` past bookings with no mark count nowhere. `trainingsCount` = `ACTIVE` `TrainingBooking` with `endsAt ∈ W` via `TrainingStatsQuery.countDone(dogId, window)`, **per dog** (§13-8), `trainingsPerWeek = round1(trainingsCount / (30/7))`. `lastClasses` = the 5 most recent with `classStartsAt ≤ now` and `state ∈ {ACTIVE, CANCELLED_LATE}` or `Attendance.NOTIFIED`, with `displayState` per R-10-09 (`PENDING` → «sense marcar», assumption). `level.assignedAt` feeds the front's «fa {n} mesos»; `levels.enabled = false` → `level` absent. The `instructorNote`, `tasks` and `observations` blocks come from `DogFollowupPort` (null-object → absent, same shape as `TASKS` off). Never expose `Dog.remarks` on any `/me/*`. **T-10-04, T-10-05, T-10-14**.
12. **`HistoryQuery`** — `GET /me/history` (R-10-14): items of the dogs the caller can reach (own + family group when `FAMILY_GROUP` is on) with `startsAt ≥ start of local day − history.monthsVisible months`, excluding live bookings (those live on screen 03): class bookings `ACTIVE` with `classStartsAt ≤ now` plus **every** cancelled one, including future ones (§13-14); trainings from `TrainingHistoryQuery.itemsFor(dogIds, from)`; activities from `ActivityHistoryQuery.itemsFor(memberId, from)`. Order `startsAt` desc, max 500, no pagination (assumption). Map `state`, `counts` and `detail.kind` exactly as the R-10-14 table for every `cancelReason` × `cancelledBy.role` of S08 (`MEMBER` in time / late, `INSTRUCTOR_NOTICE` in time / late, `SWAP`, `BACKOFFICE`, `INACTIVITY`, `LEAVE`, `CLUB_CLASS_CANCELLED`, `AUTO_CANCELLED`, `PAYMENT_TIMEOUT`) — the front picks the sentence, the api picks the `kind`. `showDog` = more than one reachable dog. Modules off remove the `TRAINING`/`ACTIVITY` types. A dog that is not reachable → `404 DOG_NOT_ACCESSIBLE`. **T-10-07, T-10-19**.
13. **`GET /attendances`** — universal list through the shared engine with `x-filterable: dogId, memberId, classSessionId, classDate, state` and `x-sortable: classStartsAt, classDate`; an undeclared field → `400 INVALID_FILTER`; MEMBER → `403`. No export (`x-exportable` is not declared for this list in S14 R-14-12).
14. **Notifications.** Nothing new is rendered here: N-05 and N-15 belong to S08 and E5-T02/T03 already dispatch them from `BookingCancelled` and `SeatReleased`. Verify with a test that an `origin = INSTRUCTOR` cancellation produces **N-05 without SMS** and, above the threshold, **N-15 exactly once** per waiting entry (**T-10-11**); if E5 dispatches N-36 instead (the backoffice variant), fix the dispatcher and report it.
15. **Concurrency.** **T-10-23**: two instructors save the same sheet with the same `version` in parallel → one `200`, one `409 STALE_VERSION` carrying the new sheet; the loser reapplies only its own rows and gets `200`; the same `PUT` repeated with the same key is identical. **T-10-24**: an «ha avisat» save and a waitlist `claim` on the same class at the same time → the freed seat is only visible to the `claim` after the save commits (`seat_locks` serialization), zero overbooking. **T-10-13**: with `SINGLE_CLASS` on and `charge.mode = CHARGE_ON_ATTENDANCE`, a `PRESENT` mark emits one `AttendanceMarked {previousState: PENDING}` that an S12 double consumes once, and going back to `PENDING` emits `AttendanceMarked {previousState: PRESENT}`; with the module off the same events fire and nothing is charged.

## Fixed conventions
- Same as E6-T01 and repeated here because the executor may not read it: closed catalogs (no new parameter, event, notification, error code or audit action — `DOG_UPDATED` already exists and `ATTENDANCE_OVERRIDDEN` is in the wire enum `AuditActionName` but still missing from `AuditAction`: add it there with its `@AuditCovers` test (E6-T01 question 5); anything missing goes to the report and `roadmap/MESSAGES.md` as a proposal while the closest existing item ships); **rule 0 statuses** — every S10 code is `422` except `STALE_VERSION`/`INVALID_STATE` (409), `DOG_NOT_ACCESSIBLE`/`MODULE_DISABLED`/`NOT_FOUND` (404), `VALIDATION_ERROR`/`INVALID_FILTER` (400), `FORBIDDEN`/`IMPERSONATION_DENIED` (403).
- `clubs.bookings` **never** imports `clubs.followup`: `pendingTasksCount` and the three card blocks arrive through `DogFollowupPort` (`clubs.bookings.application.ports`, null-object default, implemented by E6-T03). `clubs.bookings` → `clubs.scheduling.application`, `clubs.training.application` and `clubs.activities.application` for the read queries of steps 9, 11 and 12; `ArchitectureTest` enforces it.
- `attendanceSummary` is **owned by S10**: only `AttendanceSheetService` writes it, and S06's `PATCH /class-sessions/{id}` cannot clear it.
- All instants UTC on the wire; `T0`, `T1`, `classDate`, the ISO week and the 30-day window are computed in `club.timeZone` with the injected `Clock` (the ArchUnit rule of E5-T01 forbids `Instant.now()`); mandatory timezone test with `Europe/Madrid` **and** `America/Argentina/Buenos_Aires` (CONVENCIONS_I18N §5).
- Parameter keys never literals: `attendance.editDays`, `bookings.lateCancelThresholdMinutes` (**240**), `bookings.instructorLastMinuteNotice`, `waitlist.notifyThresholdMinutes`, `waitlist.mode`, `waitlist.fifoConfirmMinutes`, `messaging.noShowNoticeTime`, `history.monthsVisible`, `training.slotMinutes`, `levels.enabled`, `classes.maxInstructorsPerClass`, `club.timeZone`, `club.slug`.
- Vocabulary: never «parella/parelles» anywhere, in code or in messages — the unit is the *pair* in English identifiers and «{guia} + {gos}» in the UI; the red state is «no presentat».
- Evidence rule: report = command + exit code + last 40 lines; long outputs → `roadmap/evidence/E6-T02/NN-<name>.log`; task file ≤ 120 KB. Fictional data only (`@example.test`); no secrets, env vars only. Never edit other tasks, `ROADMAP.md` or the Organizer verification section. English identifiers from the glossary; keys never literals; ca/es/en together.

## Verification (run all; evidence = command + exit code + last 40 lines; long outputs → `roadmap/evidence/<ID>/*.log`)
- `./mvnw -q verify` green with test methods named after **T-10-01…T-10-05, T-10-07…T-10-14, T-10-19, T-10-20, T-10-23, T-10-24, T-10-33** and the attendance half of **T-10-22** (role matrix, impersonation rejected on `/instructor/*` and on both attendance verbs, cross-tenant 404, an instructor marking another instructor's class → 200 by global vision), `ArchitectureTest`, `AuditContractTest` with `ATTENDANCE_OVERRIDDEN` covered, `EventCatalogContractTest`, message parity ca/es/en, coverage gates. T-10-01…05 and T-10-07 run without Spring or Mongo.
- `E6ContractIT` updated: the routes of this task no longer answer 501; `bin/openapi-snapshot` byte-identical to the committed snapshot, or a `docs/openapi/CHANGELOG.md` entry if a shape had to change (say which and why); `git diff --check` clean.
- Manual on the local stack (`docker compose up -d` + `bin/core`): a `curl` sequence against a seeded class — `GET` the sheet, `PUT` with `PRESENT` + `NOTIFIED` four hours before, show the released seat, the `SeatReleased`/`WaitlistNotified` outbox rows and the `notifications` rows for N-05 (no SMS) and N-15; then a second `PUT` with a stale `version` showing the `409` payload; then `GET /instructor/week/export?format=pdf` saved to `roadmap/evidence/E6-T02/agenda.pdf` (state its page count and that it is landscape).

## Definition of Done
An instructor can mark a class from 21 and from D12 with one bulk save that is atomic, versioned and idempotent; «ha avisat» cancels the booking, releases the seat and notifies the waitlist inside the same transaction with the 4 h threshold and the 30 min notice threshold applied by S08; the day, the week (with its PDF), the student card metrics and the member history return every value already decided; `/attendances` filters correctly; `claimForNoShowNotice` is ready for E6-T04; no concurrency scenario can overbook a class or lose a mark.

## Evidence to return
`verify` tail with the test-method list mapped to T-10 ids; the R-10-05 table test output (six rows with their asserted values); the `409 STALE_VERSION` body from the manual run (truncated ids); the outbox and `notifications` rows after the «ha avisat» save; the two timezone runs of T-10-01; the concurrency test counts (T-10-23, T-10-24); the PDF page count and a screenshot or `pdftotext` head of the agenda; the `BookingCancellationService` result-record diff if you widened it; anything you had to assume beyond what this file states.

---

## Executor report
Session 2026-09-26 (claude:claude-opus-5-5). Steps 1–15 are done and every Verification command ran to the end. The dependency E6-T01 is `verified`. A previous session of today only set the task `in_progress`; this one did all the work.

### Branch / commits
`main`, working tree only; the publish script commits it. No git write was run.

### Evidence (full command outputs)

**1. `./mvnw -q clean verify`** (through `heavy.sh`; each run first waited 1–8 min for the web lane's e2e to release the host lock).
- Attempt 1: exit 1 (`02-clean-verify-attempt1.log`). 1 failure of 1056 IT: `ListFieldsContractIT` still expected `/attendances` to answer 501 (its `STUBS`). I moved it out of `STUBS` and seeded one attendance of a demo booking in that IT, so the served list has a row to check.
- Attempt 2: exit 0 (`03-clean-verify.log`, summaries `04`/`05`). After it I added the ADMIN «ha avisat» → N-05 assertion to `AttendanceIT` (self-review), so I ran the evidence run again on the final tree.
- **Final run:** `heavy.sh ./mvnw -q clean verify > roadmap/evidence/E6-T02/13-clean-verify-final.log 2>&1` → **exit 0** (5 995 lines, sanitized by `sanitize.py`). A quiet green build prints only Spring noise. Last 40 lines (lines longer than 300 characters cut here, complete in the log):
```text
2026-09-26T21:09:57.980+02:00  INFO 29511 --- [agilityhub-core-api] [           main] org.mongodb.driver.client                : MongoClient with metadata {"driver": {"name": "mongo-java-driver|sync|spring-boot", "version": "5.5.2"}, "os": {"type": "Darwin", "name": "Mac OS X", "architecture": "aarc …[cut]
2026-09-26T21:09:57.982+02:00  INFO 29511 --- [agilityhub-core-api] [localhost:61380] org.mongodb.driver.cluster               : Monitor thread successfully connected to server with description ServerDescription{address=localhost:61380, type=REPLICA_SET_PRIMARY, cryptd=false, state=CONNECTED, ok=tru …[cut]
2026-09-26T21:09:58.655+02:00  INFO 29511 --- [agilityhub-core-api] [           main] c.a.core.migration.PlayoffMigrationIT    : Started PlayoffMigrationIT in 0.997 seconds (process running for 571.041)
E5-T07 outbox backlog discarded before PlayoffMigrationIT: 158 PENDING records (more than one dispatch() batch)
2026-09-26T21:10:03.937+02:00  INFO 29511 --- [agilityhub-core-api] [localhost:61814] org.mongodb.driver.cluster               : Exception in monitor thread while connecting to server localhost:61814

com.mongodb.MongoSocketOpenException: Exception opening socket
	at com.mongodb.internal.connection.SocketStream.lambda$open$0(SocketStream.java:85) ~[mongodb-driver-core-5.5.2.jar:na]
	at java.base/java.util.Optional.orElseThrow(Optional.java:403) ~[na:na]
	at com.mongodb.internal.connection.SocketStream.open(SocketStream.java:85) ~[mongodb-driver-core-5.5.2.jar:na]
	at com.mongodb.internal.connection.InternalStreamConnection.open(InternalStreamConnection.java:233) ~[mongodb-driver-core-5.5.2.jar:na]
	at com.mongodb.internal.connection.DefaultServerMonitor$ServerMonitor.lookupServerDescription(DefaultServerMonitor.java:219) ~[mongodb-driver-core-5.5.2.jar:na]
	at com.mongodb.internal.connection.DefaultServerMonitor$ServerMonitor.run(DefaultServerMonitor.java:176) ~[mongodb-driver-core-5.5.2.jar:na]
Caused by: java.net.ConnectException: Connection refused
	at java.base/sun.nio.ch.Net.pollConnect(Native Method) ~[na:na]
	at java.base/sun.nio.ch.Net.pollConnectNow(Net.java:694) ~[na:na]
	at java.base/sun.nio.ch.NioSocketImpl.timedFinishConnect(NioSocketImpl.java:542) ~[na:na]
	at java.base/sun.nio.ch.NioSocketImpl.connect(NioSocketImpl.java:592) ~[na:na]
	at java.base/java.net.SocksSocketImpl.connect(SocksSocketImpl.java:327) ~[na:na]
	at java.base/java.net.Socket.connect(Socket.java:751) ~[na:na]
	at com.mongodb.internal.connection.SocketStreamHelper.initialize(SocketStreamHelper.java:76) ~[mongodb-driver-core-5.5.2.jar:na]
	at com.mongodb.internal.connection.SocketStream.initializeSocket(SocketStream.java:104) ~[mongodb-driver-core-5.5.2.jar:na]
	at com.mongodb.internal.connection.SocketStream.open(SocketStream.java:79) ~[mongodb-driver-core-5.5.2.jar:na]
	... 3 common frames omitted

E5-T07 outbox backlog discarded before PlayoffMigrationIT: 158 PENDING records (more than one dispatch() batch)
E5-T07 outbox backlog discarded before PlayoffMigrationIT: 158 PENDING records (more than one dispatch() batch)
E5-T07 outbox backlog discarded before PlayoffMigrationIT: 162 PENDING records (more than one dispatch() batch)
E5-T07 outbox backlog discarded before PlayoffMigrationIT: 158 PENDING records (more than one dispatch() batch)
E5-T07 outbox backlog discarded before PlayoffMigrationIT: 158 PENDING records (more than one dispatch() batch)
E5-T07 outbox backlog discarded before PlayoffMigrationIT: 158 PENDING records (more than one dispatch() batch)
E5-T07 outbox backlog discarded before PlayoffMigrationIT: 158 PENDING records (more than one dispatch() batch)
E5-T07 outbox backlog discarded before PlayoffMigrationIT: 158 PENDING records (more than one dispatch() batch)
E5-T07 outbox backlog discarded before PlayoffMigrationIT: 158 PENDING records (more than one dispatch() batch)
E5-T07 outbox backlog discarded before PlayoffMigrationIT: 159 PENDING records (more than one dispatch() batch)
E5-T07 outbox backlog discarded before PlayoffMigrationIT: 158 PENDING records (more than one dispatch() batch)
E5-T07 outbox backlog discarded before PlayoffMigrationIT: 158 PENDING records (more than one dispatch() batch)
E5-T07 outbox backlog discarded before PlayoffMigrationIT: 158 PENDING records (more than one dispatch() batch)
E5-T07 outbox backlog discarded before PlayoffMigrationIT: 158 PENDING records (more than one dispatch() batch)
E5-T07 outbox backlog discarded before PlayoffMigrationIT: 159 PENDING records (more than one dispatch() batch)
```
(The `MongoSocketOpenException` is the monitor thread of a Testcontainers Mongo that was already stopped, as in the E6-T01 logs.)

**Test summary of the final run** — `python3 roadmap/evidence/E6-T02/test-summary.py > roadmap/evidence/E6-T02/14-test-summary-final.log` → exit 0. It reads the Surefire/Failsafe XML of that run:
```text
  ArchitectureTest                 tests=12  failures=0 errors=0 skipped=0
  AttendanceMetricsTest            tests=5   failures=0 errors=0 skipped=0
  AttendanceTransitionsTest        tests=18  failures=0 errors=0 skipped=0
  AttendanceWindowTest             tests=3   failures=0 errors=0 skipped=0
  HistoryRulesTest                 tests=14  failures=0 errors=0 skipped=0
  NoticeRulesTest                  tests=17  failures=0 errors=0 skipped=0
  E6ResponseContractTest           tests=5   failures=0 errors=0 skipped=0
  OpenApiRequiredContractTest      tests=3   failures=0 errors=0 skipped=0
  AuditContractTest                tests=1   failures=0 errors=0 skipped=0
  MessageParityTest                tests=1   failures=0 errors=0 skipped=0
  EventCatalogContractTest         tests=2   failures=0 errors=0 skipped=0
unit/contract (surefire): tests=642 failures=0 errors=0 skipped=0
  ActivityIT                       tests=30  failures=0 errors=0 skipped=0
  AttendanceIT                     tests=11  failures=0 errors=0 skipped=0
  BookingConcurrencyIT             tests=5   failures=0 errors=0 skipped=0
  BookingsIT                       tests=18  failures=0 errors=0 skipped=0
  InstructorAggregatesIT           tests=7   failures=0 errors=0 skipped=0
  WaitlistIT                       tests=18  failures=0 errors=0 skipped=0
  CalendarIT                       tests=17  failures=0 errors=0 skipped=0
  TrainingIT                       tests=23  failures=0 errors=0 skipped=0
  E6ContractIT                     tests=30  failures=0 errors=0 skipped=0
  ListFieldsContractIT             tests=4   failures=0 errors=0 skipped=0
  OpenApiSnapshotTest              tests=10  failures=0 errors=0 skipped=0
integration (failsafe): tests=1056 failures=0 errors=0 skipped=0
```
`AuditContractTest` prints `Audit coverage: ATTENDANCE_OVERRIDDEN -> AttendanceIT.T_10_01_anAdminMarksOutsideTheWindowAuditedAndAnInstructorIsRefused`. The T-10 map of the same log (every method passed; the T-10-15…18/21/26 lines of E6-T01 and E6-T03 are left out here):
```text
  R-08-10  AttendanceIT.R_08_10_aMarkedBookingIsNoLongerCancellableAndANoShowReadsNoPresentat
  T-10-01  AttendanceIT.T_10_01_anAdminMarksOutsideTheWindowAuditedAndAnInstructorIsRefused
  T-10-01  AttendanceTransitionsTest.T_10_01_instructorWindowInMadridOpensAtLocalMidnightAndClosesAtTheEndOfTheNextDay
  T-10-01  AttendanceTransitionsTest.T_10_01_sheetFlagsPaintTheCircles
  T-10-01  AttendanceTransitionsTest.T_10_01_theSameInstantsInBuenosAiresHitDifferentLimits
  T-10-01  AttendanceWindowTest.T_10_01_windowFollowsTheDstChangeOfTheClubZone · …_windowRunsFromLocalMidnightToTheEndOfClassDatePlusEditDays
  T-10-02  AttendanceTransitionsTest.T_10_02_everyTransitionOfTheTable (13 cases) · …_notifiedBeforeTheClassEndCancelsAfterItOnlyRecordsAndAfterT1IsRefusedForEveryRole · …_notifiedIsFinalAndEveryRefusalIsTheCatalogCode
  T-10-03  NoticeRulesTest.T_10_03_theSixRowsOfTheHaAvisatTable (14 cases) · …_waitlistModuleEntriesAndPackDecideTheSideEffects (3 cases)
  T-10-04  AttendanceMetricsTest.T_10_04_presentSixNoShowOneNotifiedOneIs86PercentOverSeven · …_nothingToCountGivesNull… · …_theWindowIsThirtyClubDaysUpToNowExcluded · …_tenTrainingsInThirtyDaysAre2Point3PerWeek
  T-10-05  AttendanceMetricsTest.T_10_05_lastClassesAreTheFiveNewestStartedWithTheirBadge
  T-10-07  HistoryRulesTest.T_10_07_everyCancelReasonAndRoleMapsToTheScreen25Row (13 cases) · …_activeBookingsAppearOnceStartedAndLiveOnesNever
  T-10-08  AttendanceIT.T_10_08_claimForNoShowNoticeTakesEarlierNoShowsOnceAndLeavesTodaysAndUnmarkedOnes
  T-10-09  InstructorAggregatesIT.T_10_09_theDayPreselectsTheCallerListsOnlyTheirClassesAndEveryBlock (+ E6-T01's CalendarIT/calculator tests)
  T-10-10  AttendanceIT.T_10_10_theSheetIsTheLiveBookingsPlusTheNotifiedOnesByBookedAt · …_consumersRefreshTheClassTimesAndRecordAContradictingCancellationOnce
  T-10-11  AttendanceIT.T_10_11_haAvisatInTimeCancelsReleasesTheSeatAndTellsTheWaitingListOnceThenLateThenAfterTheEnd
  T-10-12  AttendanceIT.T_10_12_anInvalidItemRollsBackTheWholeSaveAStaleVersionGetsTheCurrentSheetAndTheSameKeyReplays
  T-10-13  AttendanceIT.T_10_13_attendanceMarkedReachesTheS12ConsumerOnceWithItsPreviousStateWithTheModuleOnOrOff
  T-10-14  InstructorAggregatesIT.T_10_14_theCardComputesTheThirtyDayMetricsTheLastClassesAndTheModuleBlocks
  T-10-19  InstructorAggregatesIT.T_10_19_theHistoryListsOwnAndGroupDogsWithTheR1014StatesAndTheModuleTypes
  T-10-20  InstructorAggregatesIT.T_10_20_theWeekIsTheIsoWeekWithClassesTrainingsAndBlocksAndItsFiltersAndPdf · …_theIsoWeekIsTheClubsInMadridAndBuenosAires
  T-10-21  InstructorAggregatesIT.T_10_21_attendancesIsAUniversalListWithItsDeclaredFiltersOnly
  T-10-22  AttendanceIT.T_10_22_rolesImpersonationAndGlobalVisionOnBothAttendanceVerbs · E6ContractIT.T_10_22_everyRouteEnforces… · E6ContractIT.T_10_22_impersonationIsAccepted…
  T-10-23  AttendanceIT.T_10_23_twoInstructorsSavingTheSameVersionGiveOne200AndOne409ThatReappliesItsOwnRow
  T-10-24  AttendanceIT.T_10_24_haAvisatAndWaitingListClaimsOnTheSameClassNeverOverbook
  T-10-33  InstructorAggregatesIT.T_10_33_clubVariantsLevelsOffFifoSharedClassesAndTheMinimalClub · E6ContractIT.T_10_33_disabledTasks…
```
T-10-01…05 and T-10-07 are plain JUnit classes in `clubs/bookings/domain` (no Spring, no Mongo).

**Coverage** — `python3 roadmap/evidence/E6-T02/coverage.py > roadmap/evidence/E6-T02/15-coverage-final.log` → exit 0 (from `target/site/jacoco/jacoco.csv`; the JaCoCo gates passed inside the verify):
```text
clubs.bookings.domain                lines  99.6%  branches  95.7%
clubs.bookings.application           lines  98.7%  branches  83.7%
clubs.bookings.application.ports     lines  86.0%  branches 100.0%
clubs.bookings.api                   lines  98.3%  branches  87.5%
clubs.bookings.persistence           lines  98.6%  branches  85.7%
clubs.scheduling.application         lines  98.9%  branches  85.8%
clubs.census.application             lines  98.8%  branches  86.0%
clubs.catalogs.application           lines  98.7%  branches  91.0%
clubs.training.application           lines  99.5%  branches  84.9%
clubs.activities.application         lines  99.2%  branches  85.2%
shared.api                           lines  94.8%  branches  91.7%
```
Per new class, in the same log. The lowest is `InstructorCardQuery` (85.1 % lines): the three TASKS blocks, which only E6-T03's follow-up port reaches.

**Before-fix** — `python3 roadmap/evidence/E6-T02/before-fix.py > roadmap/evidence/E6-T02/11-before-fix.log 2>&1` → the script exits 0 and the Maven run inside it exits 1, as intended. It copies the tree to `target/before-fix` (the working tree is never touched), reverts only the five changes this task made to **pre-existing** code, and runs the tests that pin them. All five fail at their intended assertion:
```text
InstructorAggregatesIT.T_10_20_theWeek…  [R-10-00: the handler when the dog has one] expected: "Pau + Rock" but was: "Laura + Rock"   (TrainingOccupancyService)
InstructorAggregatesIT.T_10_19_theHistory…  GET /me/history expected: 200 but was: 500                                          (ActivityQueryService startsAt)
AttendanceIT.R_08_10_…  PUT … {"code":"BOOKING_NOT_CANCELLABLE"} expected: 200 but was: 422                                        (cancelForNotice guard)
AttendanceIT.T_10_12_…  the same Idempotency-Key re-executed: expected: 200 but was: 409 STALE_VERSION                               (IdempotencyFilter PUT)
AttendanceIT.T_10_01_anAdminMarks…  N-05 rows expected: 1L but was: 0L                                                              (N-05 filter, ADMIN «ha avisat»)
Tests run: 5, Failures: 5, Errors: 0, Skipped: 0
```
Everything else is new code behind routes that answered `501` on `HEAD` (E6-T01's matrix asserted it). During development I saw these new tests fail on real defects before fixing them: T-10-08 (a leftover `NO_SHOW` of another test was claimed: the S08 fixtures never cleaned `attendances`), the `classDate` criteria (`ClassCastException`: the field's value converter also applies to query values), T-10-12's row order (same `bookedAt`), and `ListFieldsContractIT` (attempt 1 above).

**R-10-05 table (T-10-03)** — `NoticeRulesTest.T_10_03_theSixRowsOfTheHaAvisatTable`, 14 cases, 0 failures (`12-notice-rules-table.log`). Class Thu 15-10 18:50–19:50 (Madrid), WAITLIST on, live entries, pack-paid booking. The asserted values (`threshold, saved at → minutesBefore, late, booking, pack refunded, waiting list told, afterClassEnd`):
```text
120, 16:00    → 170, false, CANCELLED,      true,  true,  false      240, 16:00    → 170, true, CANCELLED_LATE, false, true,  false
120, 16:50:00 → 120, false, CANCELLED,      true,  true,  false      240, 16:50:00 → 120, true, CANCELLED_LATE, false, true,  false
120, 16:50:01 → 119, true,  CANCELLED_LATE, false, true,  false      240, 16:50:01 → 119, true, CANCELLED_LATE, false, true,  false
120, 18:20:00 → 30,  true,  CANCELLED_LATE, false, false, false      240, 18:20:00 → 30,  true, CANCELLED_LATE, false, false, false
120, 19:05    → -15, true,  CANCELLED_LATE, false, false, false      240, 19:05    → -15, true, CANCELLED_LATE, false, false, false
120, 20:30    → —,   true,  ACTIVE,         false, false, true       240, 20:30    → —,   true, ACTIVE,         false, false, true
240, 14:50:00 → 240, false, CANCELLED, true, true, false             240, 14:50:01 → 239, true, CANCELLED_LATE, false, true, false
```
The spec table is written with 120 (R-10-05 «Paràmetres»); the catalog value is 240. Both are asserted, plus the 240 limit itself. `seatReleased` is asserted true on every row but 20:30.

**Timezone runs of T-10-01** — `AttendanceTransitionsTest.T_10_01_instructorWindowInMadrid…` (Mon 03-08: 02-08 23:59 → `ATTENDANCE_NOT_OPEN`, 03-08 00:00 and 04-08 23:59:59 → open, 05-08 00:00 → `ATTENDANCE_WINDOW_CLOSED {editableUntil: 2026-08-04T21:59:59Z}`, ADMIN always, override flagged outside) and `…_theSameInstantsInBuenosAiresHitDifferentLimits` (`2026-08-02T22:00Z` opens Madrid but not Buenos Aires; `2026-08-04T23:30Z` is closed in Madrid and open in Buenos Aires; `2026-08-05T03:00Z` closes Buenos Aires). T-10-20 has the same pair for the ISO week (a class at Monday 03:30 Madrid is Sunday 22:30 in Buenos Aires, so it falls in the previous week).

**Concurrency counts** (final run, printed by the tests):
```text
T-10-23 two saves of version 0 in parallel: statuses [200, 409]
T-10-24 round 0: [save 200, hold 409 SEAT_TAKEN, claim 201] → live bookings 1 / capacity 2, «ha avisat» booking CANCELLED
T-10-24 round 1: [save 200, hold 409 SEAT_TAKEN, claim 201] → live bookings 1 / capacity 2, «ha avisat» booking CANCELLED
T-10-24 round 2: [save 200, hold 409 SEAT_TAKEN, claim 201] → live bookings 1 / capacity 2, «ha avisat» booking CANCELLED
```
T-10-23: the 409 carries `details.current` with `sheet.version = 1` and the winner's mark; the loser reapplies only its own row and gets 200; a repeated `PUT` with the same key is byte-identical. T-10-24: the seat «ha avisat» frees was never visible to a hold that ran before the save committed. After the commit and a dispatch, the entry that lost the race is offered it again and claims it: 2 live bookings = capacity, `counters.booked = 2`.

**`BookingCancellationService` result record (widened, internal).** `cancel(bookingId, actor, message)` returned only the `Booking`; the new internal record is:
```diff
+    public record Cancellation(Booking booking, boolean late, int minutesBefore, boolean seatReleased, boolean waitlistNotified, boolean packRefunded) { }
+    @Transactional(propagation = Propagation.MANDATORY)
+    public Cancellation cancelForNotice(String bookingId, BookingActor actor) {
+        return cancel(bookings.require(bookingId), actor, BookingCancelReason.INSTRUCTOR_NOTICE, null, null, false, true);
+    }
-        if (… || !now.isBefore(b.classEndsAt()) || attendance.marked(b.id())) {
+        if (… || !now.isBefore(b.classEndsAt()) || !attendanceNotice && attendance.marked(b.id())) {
-        if (now.isBefore(b.classStartsAt())) { seatReleased(b, now, outcome.minutesBefore(), actor); }
+        boolean waitlistNotified = now.isBefore(b.classStartsAt()) && seatReleased(b, now, outcome.minutesBefore(), actor);
-        return after;
+        return new Cancellation(after, late, outcome.minutesBefore(), true, waitlistNotified, refund != null);
```
The public `cancel`, `cancelBySystem` and the other callers keep returning the `Booking`. No contract impact.

**2. `E6ContractIT`, `bin/openapi-snapshot`, `git diff --check`**
- `E6ContractIT` (30 tests, in the verify above): the 8 routes of this task are marked `"implemented": true` in `e6-routes.json` and answer their success status to the allowed roles (the matrix `PUT` is a no-op on a seeded booking, so 200). The other 14 still answer 501, and «the stubs write nothing» now covers only them. Descriptions of served routes must not say «501».
- `bin/openapi-snapshot > …/06-snapshot-first.log 2>&1 && cp docs/openapi/openapi.json target/openapi-first.json && bin/openapi-snapshot > …/07-snapshot-second.log 2>&1 && cmp target/openapi-first.json docs/openapi/openapi.json && echo "CMP identical"` → **exit 0**, `CMP identical`; both SHA-256 `a7c8c95f…[truncated]`; both logs end with `Updated docs/openapi/openapi.json`. `OpenApiSnapshotTest` (10/10) confirms it inside the final verify.
- The snapshot is **not** byte-identical to `HEAD`: `git diff --stat -- docs/openapi/openapi.json` → 16 insertions, 8 deletions. 0 operations added or removed; 2 optional properties added (`AttendanceRow.memberFullName`, `SheetWaitlistEntry.handlerName`, R-10-00); 8 descriptions changed (the «Contract only; returns 501» sentence is gone). Entry «E6-T02» in `docs/openapi/CHANGELOG.md`.
- `git diff --check` → **exit 0**, no output.

**3. Manual run on the local stack** — `heavy.sh python3 roadmap/evidence/E6-T02/manual-run.py > roadmap/evidence/E6-T02/09-manual-run.log 2>&1` → **exit 0**. The script reuses `bin/e5-smoke`'s `Smoke` helper: a disposable Compose stack built from the working tree, the Cànic with `club:apply` + `seed:demo` twice (second runs 0 changes), the test clock, curl with the tokens on stdin, and `compose down -v` at the end.
- First attempt (`08-manual-run.log`, exit 0): the clock was set to exactly 4 h before the class, and the test clock keeps ticking, so the save landed 1 s past the 240-minute limit (`minutesBefore 239`, `CANCELLED_LATE`, no refund, waiting list still told). I moved the clock 4 h 05 min before to show the in-time path.
- Final run, last 40 lines (three long lines cut at 420 characters here, complete in the log; ids truncated by the script). The class is Tue 06-10 17:40 Madrid; the save is at 13:35 Madrid (the `set_clock` label of `e5-smoke` prints the UTC hour next to «Europe/Madrid»):
```text
  15 changes (applied)
PASS club:apply seeds/club-canic.yaml run 1
  = catalogs: 0 catalog changes
  = pages: 0 page changes
  0 changes (applied)
PASS club:apply seeds/club-canic.yaml run 2: 0 changes
  {activeMembers=184, pendingMembers=3, inactiveMembers=4, leftMembers=3, members=194, dogs=246, familyGroups=12, activeDogs=242, pendingDogs=3, inactiveDogs=1, instructors=3, administrators=2, receivedDocuments=7, pendingDocuments=239}
  452 changes (demo seed)
  {templateClasses=39, templates=3, weeks=3, generatedClasses=114, validatedWeeks=2, looseClasses=1, ringBlocks=1, scenarioRingBlocks=1, riskCancellations=1, classBookings=23, classWaitlist=3, activities=4, publishedActivities=3, activityRegistrations=44, activityWaitlist=2, activityFiles=2, scenarioActivityRegistrations=1, scenarioClassBookings=18, scenarioCancellations=3, scenarioWaitlist=5, scenarioOffers=0, scena …[cut]
  279 changes (demo planning, week start 2026-10-05)
PASS seed:demo --club=canic --seed=42 --week-start=2026-10-05 run 1
  {activeMembers=184, pendingMembers=3, inactiveMembers=4, leftMembers=3, members=194, dogs=246, familyGroups=12, activeDogs=242, pendingDogs=3, inactiveDogs=1, instructors=3, administrators=2, receivedDocuments=7, pendingDocuments=239}
  0 changes (demo seed)
  {templateClasses=39, templates=3, weeks=3, generatedClasses=114, validatedWeeks=2, looseClasses=1, ringBlocks=1, scenarioRingBlocks=1, riskCancellations=1, classBookings=23, classWaitlist=3, activities=4, publishedActivities=3, activityRegistrations=44, activityWaitlist=2, activityFiles=2, scenarioActivityRegistrations=1, scenarioClassBookings=18, scenarioCancellations=3, scenarioWaitlist=5, scenarioOffers=0, scena …[cut]
  0 changes (demo planning, week start 2026-10-05)
PASS seed:demo --club=canic --seed=42 --week-start=2026-10-05 run 2: 0 changes
PASS club:apply + seed:demo --seed=42 --week-start (Cànic; x2): second runs 0 changes · week start 2026-10-05 (8+ days ahead)
curl GET /api/v1/health -> 200
PASS API up (scheduler on); fixture clubs activated: canic
Target class 279dc91a… 2026-10-06 17:40 (Madrid) capacity 4, booked 4, waiting 3
PASS POST /test/clock: 200 · four hours (and 5 minutes) before the class: in time (Tue 06-10 11:35 Europe/Madrid; server now 2026-10-06T11:35Z)
curl GET /api/v1/class-sessions/<id>/attendance -> 200
  sheet: version 0, canMarkPresence True, canMarkNotice True, editableUntil 2026-10-07T21:59:59Z; classSession booked 4/4, waiting 3; 4 rows; waitlist mode ALL_AT_ONCE with 3 entries
curl PUT /api/v1/class-sessions/<id>/attendance -> 200
  PUT applied ['b9b39de5…', '7682a694…']; version 0 → 1; notice {"atLocal": "13:35", "at": "2026-10-06T11:35:01.328Z", "late": false, "minutesBefore": 244, "seatReleased": true, "waitlistNotified": true, "afterClassEnd": false, "bookingState": "CANCELLED"}
curl GET /api/v1/class-sessions/<id>/attendance -> 200
  released seat: classSession booked 4 → 3 of 4; booking 7682a694… is CANCELLED
  outbox rows after the save (dispatched by the scheduler):
    {"type": "AttendanceMarked", "status": "PUBLISHED", "payload": {"bookingId": "7682a694…", "classSessionId": "279dc91a…", "dogId": "a5a7a299…", "memberId": "1d689efd…", "state": "NOTIFIED", "previousState": "PENDING", "by": {"accountId": "00e8b4f4…", "role": "INSTRUCTOR"}, "late": false, "afterClassEnd": false}}
    {"type": "AttendanceMarked", "status": "PUBLISHED", "payload": {"bookingId": "b9b39de5…", "classSessionId": "279dc91a…", "dogId": "b2809e37…", "memberId": "4798b83a…", "state": "PRESENT", "previousState": "PENDING", "by": {"accountId": "00e8b4f4…", "role": "INSTRUCTOR"}}}
    {"type": "BookingCancelled", "status": "PUBLISHED", "payload": {"bookingId": "7682a694…", "by": "INSTRUCTOR", "late": false, "minutesBefore": 244, "origin": "INSTRUCTOR", "reason": "INSTRUCTOR_NOTICE"}}
    {"type": "SeatReleased", "status": "PUBLISHED", "payload": {"classId": "279dc91a…", "freeSeats": 1, "minutesBefore": 244, "notifyWaitlist": true}}
    {"type": "WaitlistNotified", "status": "PUBLISHED", "payload": {"entryIds": ["a3b1029f…", "81779f7d…", "ede39417…"], "classId": "279dc91a…", "mode": "ALL_AT_ONCE"}}
  notifications N-05 (member 18ad0d3e…): [{"code": "N-05", "channel": "APP", "status": "SENT"}]
  notifications N-15 (3 waiting entries now NOTIFIED): [{"code": "N-15", "channel": "PUSH", "status": "QUEUED", "account": "3a5c561a…"}, {"code": "N-15", "channel": "SMS", "status": "QUEUED", "account": "3a5c561a…"}, {"code": "N-15", "channel": "APP", "status": "SENT", "account": "3a5c561a…"}, {"code": "N-15", "channel": "PUSH", "status": "QUEUED", "account": "d3780f06…"}, {"code": "N-15", "channe …[cut]
curl PUT /api/v1/class-sessions/<id>/attendance -> 409 STALE_VERSION
  409 body: {"code": "STALE_VERSION", "message": "Les dades han canviat. Actualitza-les i torna-ho a provar.", "traceId": "6882605d…", "details": {"current": {"sheet": {"version": 1, "canMarkPresence": true, "canMarkNotice": true, "editableUntil": "2026-10-07T21:59:59Z", "savedAt": "2026-10-06T11:35:01.328Z", "savedByName": "Aina", "noShowNoticeTime": "08:00"}, "classSession": {"id": "279dc91a…", "date": "2026-10-06" …[cut]
curl GET /api/v1/instructor/week/export?format=pdf&date=2026-10-06 -> 200 application/pdf; Content-Disposition: attachment; filename="canic_agenda_20261005.pdf"; 13593 bytes saved to roadmap/evidence/E6-T02/agenda.pdf
PASS E6-T02 manual run
Removed disposable Compose services, volumes and private mailbox; existing stacks preserved
```
The N-15 line has 9 rows: APP (SENT) + SMS intent + PUSH intent (QUEUED) for each of the 3 entries, once each. N-05 has only its APP row: no SMS. «Aina» (the seeded `instructor@example.test`) marked a class whose instructor is Ona (global vision). The complete 409 body, with its 4 rows, is in the log.
- **The PDF** — `java -cp "target/pdfbox-lib/*" roadmap/evidence/E6-T02/PdfInfo.java roadmap/evidence/E6-T02/agenda.pdf 30 > roadmap/evidence/E6-T02/10-agenda-pdf.log 2>&1` → exit 0. There is no `pdfinfo`/`pdftotext` on the host, so a single-file PDFBox helper reads it (the jars were copied into `target/pdfbox-lib` with `./mvnw -q dependency:copy-dependencies`). **1 page, 841.9 × 595.3 pt: landscape A4.** Head:
```text
pages=1 width=841,9pt height=595,3pt LANDSCAPE
  | Club Agility Cànic · Agenda de la setmana · del 5 d’oct. 2026 al 10 d’oct. 2026
  | Hora dl 5/10 dt 6/10 dc 7/10 dj 8/10 dv 9/10 ds 10/10
  | 08:30 C+D+E 2/4 Muntanya · Ona
  | A+B 0/5 Central · Laia
  | F i sup. 0/4 Central · Laia A+B 0/5 Central · Laia
  | C+D+E 1/4 Muntanya · Ona
  | C+D+E 0/4 Muntanya · Ona Cadells 0/5 Cadells · Ona A 0/5 Muntanya · Laia
  | B+C 0/5 Central · Ona
  | 09:30 D i sup. 0/4 Central · Laia Cadells 0/5 Cadells · Aina A+B 0/5 Central · Laia Cadells 0/5 Cadells · Ona
  | B+C 0/5 Central · Laia
  | 11:00 11:00–11:30 Reserva Central —
```
The file itself is `roadmap/evidence/E6-T02/agenda.pdf`. T-10-20 also checks the PDF of its own fixture in `ca` and `en` (landscape, «Agenda de la setmana», «dl 5/10», «Reserva Muntanya — Pau + Rock», «Bloqueig Central — manteniment», «Pàgina 1 de 1»; «Weekly agenda», «Mon 5/10»).

### Files changed
**New (main)**
- `clubs/bookings/domain/{AttendanceTransitions, NoticeRules, AttendanceMetrics, HistoryRules}`
- `clubs/bookings/application/{AttendanceSheetQuery, AttendanceSheetService, AttendanceCaller, AttendanceCallers, AttendanceStates, AttendanceConsumers, NoShowNoticeClaims, ClaimNoShowCommand, InstructorDayQuery, WeekAgendaQuery, WeekAgendaPdf, InstructorCardQuery, HistoryQuery, AttendanceListQuery}`
- `clubs/bookings/application/ports/{TrainingHistoryQuery, TrainingStatsQuery, ActivityHistoryQuery}`
- `clubs/scheduling/application/InstructorScheduleAccess`, `clubs/census/application/AttendanceCensusAccess`, `clubs/training/application/TrainingHistory`, `clubs/activities/application/ActivityHistory`

**Modified (main)**
- `clubs/bookings/api/InstructorController` (served), `InstructorContracts` (module-dependent fields absent, `memberFullName`, `handlerName`)
- `clubs/bookings/domain/AttendanceWindow` (`canMarkPresence`, `overrides`, `canMarkNotice`)
- `clubs/bookings/persistence/AttendanceRepository` (reads, versioned upsert, `claimForNoShowNotice`, `noticeEvent`, `refreshClass`, `appendHistory`)
- `clubs/bookings/application/BookingCancellationService` (`Cancellation`, `cancelForNotice`), `BookingNotifications` (N-05 on origin INSTRUCTOR), `BookingsAutoConfiguration` (defaults of the new ports; the `AttendanceStatePort` null object is gone), `ports/DogFollowupPort` (`available()`), `ports/AttendanceStatePort` (Javadoc)
- `clubs/scheduling/persistence/ClassSessionRepository` (`attendanceSummary` write), `clubs/catalogs/application/PlanningCatalogAccess` (`levelRefs`, `instructorRefs`)
- `clubs/census/application/TrainingMemberAccess` (`handlerNames`), `clubs/training/application/TrainingOccupancyService` (the handler in staff cells), `clubs/training/persistence/TrainingBookingRepository` (2 reads)
- `clubs/activities/application/ActivityQueryService` (history rows carry `startsAt`)
- `platform/application/audit/AuditAction` (`ATTENDANCE_OVERRIDDEN`)
- `shared/api/IdempotencyFilter` (the keyed `PUT …/attendance`)
- `messages_{ca,es,en}.properties` (`export.column.instructor-week.*`, 22 keys each)
- `docs/openapi/openapi.json`, `docs/openapi/CHANGELOG.md`, `CHANGELOG.md`

**Tests**
- New: `clubs/bookings/domain/{AttendanceTransitionsTest, NoticeRulesTest, AttendanceMetricsTest, HistoryRulesTest}`, `clubs/bookings/api/{AttendanceIT, InstructorAggregatesIT}`.
- Modified: `BookingFixtures` (also cleans `attendances`, `ring_blocks`, `training_bookings`), `E6ContractIT` + `fixtures/contracts/e6-routes.json` (`implemented`), `ListFieldsContractIT` (`/attendances` served).

**Evidence** — `roadmap/evidence/E6-T02/01…15-*.log`, `agenda.pdf`, and the helpers `test-summary.py`, `coverage.py`, `sanitize.py`, `before-fix.py`, `manual-run.py`, `PdfInfo.java`.

### Rules and tests implemented (R-xx-nn / T-xx-nn)
| Step | Rule | Tests |
|---|---|---|
| 1 `claimForNoShowNotice` + CLI | R-10-06, R-15-13 | T-10-08 (`AttendanceIT`; two runs → one `NoShowNoticeDue`; today's and unmarked rows stay; the CLI `attendance:claim-no-show --club=`) |
| 2 `AttendanceWindow` / `AttendanceTransitions` | R-10-03, §5 | T-10-01 (Madrid, Buenos Aires, DST), T-10-02 (the whole table, `NOTIFIED` final, every refusal code) |
| 3 sheet | R-10-00, R-10-02, R-10-03 | T-10-10 |
| 4 save | R-10-04 | T-10-11, T-10-12, T-10-23 |
| 5 «ha avisat» | R-10-05, R-08-10/11/17/19 | T-10-03 (table), T-10-11 (in time / late / after the end / flag off), `R_08_10_…` |
| 6 `AttendanceStatePort` | R-08-10 | `R_08_10_…` (422 `BOOKING_NOT_CANCELLABLE`, `displayState` NO_SHOW) |
| 7 consumers | S10 §7 | `T_10_10_consumersRefresh…` |
| 8 day | R-10-01 | T-10-09 |
| 9, 10 week + PDF | R-10-15, R-10-16 | T-10-20, T-10-33 |
| 11 card | R-10-08, R-10-09 | T-10-04, T-10-05, T-10-14 |
| 12 history | R-10-14 | T-10-07, T-10-19 |
| 13 `/attendances` | S10 §6, CONVENCIONS_API §4 | `T_10_21_attendancesIsAUniversalList…`, `ListFieldsContractIT` |
| 14 notifications | R-08-19, N-05, N-15 | T-10-11 (N-05 APP only, N-15 once per entry, no N-36), `T_10_01_anAdminMarks…` (an ADMIN's «ha avisat» → N-05) |
| 15 concurrency | R-10-04, R-10-05, R-10-07 | T-10-23, T-10-24, T-10-13 |
| — | S14 R-14-09 | `ATTENDANCE_OVERRIDDEN` with `@AuditCovers` |
| — | T-10-22 (attendance half) | `AttendanceIT.T_10_22_…`, the impersonation check in `T_10_09_…`, `E6ContractIT` |

### Assumptions
1. **Dependency direction.** `clubs.training` and `clubs.activities` already depend on `clubs.bookings`, so importing their application packages from bookings (step 9/11/12 wording) would be a cycle (`E0_T01_contextsHaveNoCycles`). The S09/S07 contracts of S10 §7 are ports in `clubs.bookings.application.ports` (`TrainingHistoryQuery`, `TrainingStatsQuery`, `ActivityHistoryQuery`), served by `TrainingHistory` and `ActivityHistory`, as `MemberTrainingRowsPort` is. The week agenda uses S06's `TrainingOccupancyPort` (S09's service behind it).
2. **The ADMIN's «ha avisat»** (D12) is the same S08 call: `origin = INSTRUCTOR`, `reason = INSTRUCTOR_NOTICE`, `cancelledBy.role = ADMIN`, `BookingCancelled.by = ADMIN`. The N-05 filter now also accepts `origin = INSTRUCTOR`, so the member gets N-05 and never N-36 (R-08-19).
3. **`seatReleased`** in `notice` = the booking no longer takes a seat (true whenever it was cancelled, also after the start: the 19:05 row says «plaça alliberada»). `waitlistNotified` = S08's `SeatReleased.notifyWaitlist`; S08 emits no `SeatReleased` once the class has started (R-08-11).
4. **`NOTIFIED{afterClassEnd}`** stores `late: true`, `minutesBefore: null`, `seatReleased: false`. The history shows it as `CANCELLED_LATE`, `counts: true`, `detail.kind = INSTRUCTOR_NOTICE` with the notice instant (R-10-08 counts it as `cancelledLate`).
5. **Save details:** a save whose items all equal the stored states changes nothing and keeps `version`. A repeated `bookingId` is `400 VALIDATION_ERROR`. A booking that is not of the class is `422 ATTENDANCE_BOOKING_NOT_ACTIVE {bookingId}`. A DRAFT or CANCELLED class is `409 INVALID_STATE` on the `PUT`; the `GET` answers 404 for a DRAFT class and inert rows for a CANCELLED one. `NOTIFIED` has no `T0` bound (the member may phone the day before) and closes at `T1` for every role.
6. **`attendanceSummary` write** bumps the S06 class `version` (`$inc`). An S06 edit read before a save then fails its compare-and-set (409) instead of writing the old summary back, since `SessionEdit` copies the summary it read.
7. **`ATTENDANCE_OVERRIDDEN`** entry: `entityType Attendance`, `entityId` = the attendance, `memberId` = the booking's owner, and `changes` = `state` from → to plus `classSessionId` and `bookingId` (null → value): the audit entry has no `details` field, so the four task values travel as changes.
8. **`scheduledFor`** of a NO_SHOW row: the `messaging.noShowNoticeTime` run of the day it was queued; not yet queued, the first run on a day after the class date that is still ahead. The local time resolves as S06 R-06-14 does.
9. **`NoShowNoticeDue`**: `aggregateType Attendance`, `aggregateId` = the club id (it is a batch). The claimed rows get a batch token first and then, in the same transaction, the published event's id.
10. **Week agenda:** `instructorId=me` for a caller without an instructor profile shows no class and echoes `filters.instructorId: "me"`. Blocks come from S06 (`RingScheduleAccess`, which has `createdByName`); trainings from the occupancy port. A block that starts before Monday is drawn from Monday 00:00.
11. **The PDF has no logo image.** `Theme.logoUrl` is an external URL (the Cànic has none), and a synchronous request should not fetch it. The header is the theme-colour band with the club's name, as the list exports. The labels are the new `export.column.instructor-week.*` keys; day headers are «dl 5/10». See question 1.
12. **The staff training cells** (D12, and also the D4/10/23 day grids through the same port) name the dog's handler when it has one (R-10-00 «{guia} + {gos}»).
13. **History:** activities belong to the member, not to a dog, so they are listed with «Tots» only. PENDING dogs are not chips. Own dogs come first, then by name. A token without a member is `403 NO_MEMBERSHIP`.
14. **`GET /attendances`** lists stored attendances: a booking never marked has no row.
15. **R-10-00 on the sheet:** `memberFullName` is present only when `handlerName` differs from `memberFirstName`. `SheetWaitlistEntry` got `handlerName` too, so the waiting list reads «{guia} + {gos}» like the rows.
16. **`pendingTasksCount` and the three card blocks** stay absent until E6-T03 implements `DogFollowupPort` (`available()` is false on the default), exactly the TASKS-off shape.
17. **PackRefunded:** the in-memory pack port of local/test returns the refund movement (`packRefundMovementId`, asserted); the `PackRefunded` event is emitted by S12's real adapter (E8, per `PackBalancePort`).
18. **The member's «fa {n} mesos»** is the front's (`fmtRelative`, R-10-09): the api sends `level.assignedAt`, and T-10-14 asserts it. T-10-05's «25 dies → dies, 26 mesos → anys» belongs to the front's formatter.

### Questions / catalog proposals
No new parameter, event, notification or error code. `ATTENDANCE_OVERRIDDEN` was already in S14 R-14-09 and the wire enum; it is now in `AuditAction`.
1. **Logo in the D12 PDF** (assumption 11): should the api embed the club's logo? That needs a raster `logoUrl` (or a stored logo file) and an outbound fetch, or a logo stored with the club. Until then the band carries the name.
2. **R-10-05 and S08 R-08-10 still show 120-minute examples** (the table's 16:00 and 16:50:00 rows are in time only with 120). The code follows the catalog (240); T-10-03 asserts both. Should the spec tables be rewritten for 240?
3. **Contract additions** (`AttendanceRow.memberFullName`, `SheetWaitlistEntry.handlerName`): please confirm them in S10 §6, or tell me to drop them.

## Organizer verification
_(pending)_
