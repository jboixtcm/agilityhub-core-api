---
id: E7-T01
title: Contract S11 (communications) — NotificationCatalog N-01…N-54 as code with a document-parity test, Notification/MessageTemplate/PushSubscription/NotificationPreference documents + indexes, OpenAPI of §6 with 501 stubs, error-status audit
stage: E7
repo: agilityhub-core-api
thread: C
order: 10
status: ready
depends_on: [E5-T06, E6-T04]
branch:
pr:
updated: 2026-09-19
---

# E7-T01 · Contract S11 (communications) — NotificationCatalog N-01…N-54 as code with a document-parity test, Notification/MessageTemplate/PushSubscription/NotificationPreference documents + indexes, OpenAPI of §6 with 501 stubs, error-status audit

## Objective
Work package S11 WP-11-A: the single table «event → notification code → category, audiences, channel caps, push, action, variables, `dedupKey`» becomes **code** (`NotificationCatalog`) with a test that proves it equals `docs/specs/00-transversal/CATALEG_NOTIFICACIONS.md`; every route of S11 §6 exists in the OpenAPI snapshot with typed schemas, roles, modules and error codes answering `501 NOT_IMPLEMENTED` behind the security/tenant/module guards; the storage of the vertical (`Notification` in its S11 shape with `deliveries[]`, `MessageTemplate`, `PushSubscription`, `Member.notificationPreferences`, the club SMS counter) exists with its indexes. E7-T02 (engine) and E7-T03 (templates, log, preferences) implement behind this contract; the web builds mocks-first from the published snapshot. Same pattern as E4-T01 (`E4ContractConfiguration`, `E4ContractIT`, `E4ResponseContractTest`, fixtures `e4-*.json`).

## Spec references
S11 §2, §3 (every field table), §4 R-11-01…R-11-17 (contract half only), §5 (both state machines), §6 (every row, the two JSON extracts, the provider interfaces, the consumer pipeline, the own error codes), §7, §8, §9, §11 T-11-16 (contract half), T-11-19, T-11-21, T-11-26, T-11-28, T-11-29, §12 WP-11-A, §13 · `CATALEG_NOTIFICACIONS.md` main table (N-01…N-38) **and Annex A** (N-18d, N-32d, N-39…N-54) + «Variables disponibles» + «Regles» 1–8 · `CATALEG_ESDEVENIMENTS.md` rows `MessageTemplateChanged`, `NotificationQueued/Sent/Failed`, `AnnouncementSent`, `PushSubscribed/Unsubscribed`, `ReminderDue`, and the communications row `EmailBounced{memberId, email}` · `SmsCapReached{month}` · `NotificationPreferencesChanged` · `EmailUnsubscribed` · `CATALEG_ERRORS.md` §1 + §2 row S11 + §3 rule 0 · `CATALEG_MODULS.md` rows `SMS`, `PUSH`, `FAQ`, `LEARN_LINK` · `MATRIU_PERMISOS.md` rows «Paràmetres /parameters, FAQ, plantilles de comunicat», «Notificacions /notifications», «Comunicats massius» · `CONVENCIONS_API.md` §2, §4, §5, §6, §7 · S14 R-14-09 (`CATALOG_CHANGED`, `ANNOUNCEMENT_SENT`, `ACCOUNT_EMAIL_STATUS_CHANGED`), R-14-12 (`notifications` is `x-exportable`), R-14-15 · S15 R-15-14 and R-15-15 step g (SMS counter month key) · `docs/DECISIONS_PENDENTS.md` part C row S11, A12, A21d, B18, B21.

## Context to load
`docs/specs/S11-comunicacions.md` (complete) · the catalogs above · `docs/MODEL_DADES_PLATAFORMA.md` §0 glossary, §2 `LocalizedText`, §3 (`NotificationPreference` embedded, `PushSubscription`), §6 outbox, Annex B rows · `roadmap/tasks/E4-T01.md` (structure to copy) and `src/main/java/com/agilityhub/core/configuration/E4ContractConfiguration.java`, `src/test/java/com/agilityhub/core/configuration/E4ContractIT.java`, `E4ResponseContractTest.java`, `OpenApiRequiredContractTest.java`, `ErrorCatalogContractTest.java`, `EventCatalogContractTest.java`, `src/test/resources/fixtures/contracts/e4-*.json` · **the existing messaging package** `src/main/java/com/agilityhub/core/clubs/messaging/**` — `application/SystemNotificationService.java` (E1-T03 SYSTEM path + `appOnce`/`smsIntentOnce` used by E3…E6), `SystemEmailRenderer.java`, `EmailSender.java` + its three implementations, `SendGridWebhookService.java`, `SendGridSignatureVerifier.java`, `api/SendGridWebhookController.java`, `persistence/Notification.java`, `NotificationRepository.java`, `domain/NotificationEvent.java` · `shared/application/NotificationAccounts.java`, `IcuMessageSource.java`, `LocaleContext.java`, `ClubClock.java`, `DomainEventHandler.java`, `EventPublisher.java`, `contract/ListContract.java`, `lists/*` · `platform/application/ClubEmailSettings.java`, `ParameterCatalog.java`, `RequiresModule.java`, `Module.java`, `audit/AuditAction.java` · `clubs/census/persistence/Member.java` (`contactEmails`, `phones`, `notificationPreferences`) · `clubs/catalogs/**` (`FaqEntry`, `CatalogKind.FAQ`, `FaqReaderView`, the published `/faq-entries` routes) · `clubs/common/api/ExportsController.java` (the already published `GET /notifications/export`) · `docs/playbooks/new-entity.md`, `new-endpoint.md` · `docs/pantalles/mobil/11-notificacions.html`, `12-perfil-i-preferencies.html`, `docs/pantalles/escriptori/D9-comunicats-i-plantilles-d-avis.html`, `D10-fitxa-d-abonat.html` (data shapes only) · this file.

## Steps
1. **`NotificationCatalog`** (`clubs/messaging/domain`, product code, no database): one `NotificationSpec` per row of `CATALEG_NOTIFICACIONS.md` — main table **and** Annex A. Record: `code` (`N-01`…`N-54`, including the lettered `N-08a/b`, `N-11a/b`, `N-18a/b/c/d`, `N-32a/b/c/d`), `eventTypes[]` (the `CATALEG_ESDEVENIMENTS` names), `category` (`OPERATIONAL·PERSONAL·CLUB_CHANGES·CLUB_NEWS·SYSTEM`), `audiences[]` (`MEMBER·INSTRUCTORS·ADMINS·APPLICANT`), `caps` = `Map<Audience, Set<Channel>>` (the channels a template **may** activate, R-11-12: `OPERATIONAL`/`PERSONAL` → `APP+EMAIL` for the three staff-or-member audiences; `CLUB_CHANGES` → `APP+EMAIL+SMS` for `MEMBER`, `APP+EMAIL` for `INSTRUCTORS`/`ADMINS`; `CLUB_NEWS` → `APP+EMAIL`; per-code exception **N-15** = `SMS` on `MEMBER` although its category is `OPERATIONAL`; `APPLICANT` → `EMAIL` only), `push` = `Set<Audience>` (never stored in a template: N-13, N-15, N-24, N-33 per the document's «Públic → canals per defecte» column), `defaultMatrix` (the same column, what the seed activates), `action` (`ActionType` enum `CHANGE_CLASS · CLAIM_SEAT · OPEN_BOOKING · OPEN_DOG · OPEN_TASKS · OPEN_INVOICES · OPEN_ACTIVITY · OPEN_SETUP · OPEN_SIGNUP · OPEN_MEMBER · OPEN_JOBS · OPEN_EXPORT · OPEN_CHALLENGE`, `null` when the row has «—»; per audience where the document distinguishes it, e.g. N-01 «admin: OPEN_SIGNUP»), `variables[]` and `requiredVariables[]` (R-11-12: N-02 `link`; N-08a `admin_text`), `mandatory` (N-02, N-08a, N-15, N-17, N-32c, N-36), `icon` + `color` of the seed (§8 table where it exists, otherwise `bell` · `NEUTRAL` — state the default in the report), `dedupKeyFn` and `stillRelevantFn` (R-11-09/R-11-16; default `{eventId}:{code}:{audience}:{recipientKey}[:{subjectKey}]`, exceptions `N-13` → `N-13:{bookingId|trainingBookingId}` and `N-24` → `{batchId}:{memberId}`), `moduleGuards` (R-11-17: N-15 ↔ `WAITLIST`, N-10/N-11a/N-11b/N-30/N-35/N-38 ↔ `BILLING`…), `stage` (`R1` or `LATER` for N-44, N-45 R2 and N-48 «fora de R1»: registered, never seeded, never emitted). Read-only lookups: `byCode(code)`, `specsFor(eventType)` (0..n, the only event → code table in the product), `codes()`.
2. **`NotificationCatalogContractTest`** (the deliverable of this task, pattern of `ParameterCatalog`'s `T-02-03_catalogMatchesDocument` and `ErrorCatalogContractTest`): parse the two markdown tables of `docs/specs/00-transversal/CATALEG_NOTIFICACIONS.md` and assert, row by row, that code set, `category`, audiences, default channels per audience, variables and action of the document equal the code catalog — both directions (a code in the document and not in the code, or the reverse, fails). Count the rows you register and state the number in the report (today the document has 43 main rows + 18 annex rows; N-12 does not exist — the numbering jumps from `N-11b` to `N-13`, report it as a document observation, do not invent it). Second test: every `eventTypes[]` value exists in `CATALEG_ESDEVENIMENTS.md`, every variable key exists in the document's «Variables disponibles» list, in the additions of its last paragraph (`upfront_total`, `payment_instructions`, `pay_link`, `member_last_names`, `dog_name_article`, `kind`, `class_description`, `cancelled_count`, `auto_cancel`) or in the S11 §5/§8 formatting list (`review_time`, `review_day`, `week_start`, `dogs_count`, `changes`, `masked_account`, `calendar_links`, `expires_minutes`, `month`, `cap`, `email`, `document_type`, `task_excerpt`, `setup_kind`, `retry_link`, `concept`, `count`, `oldest_days`, `state`, `decision`, `fee`, `expires_days`); anything you cannot map goes to the report as a catalog proposal, never into the code silently.
3. **Mongo documents + indexes** (`clubs/messaging/persistence`, `TenantEntity`, `clubId`, `version` where edited, `createdAt/By`, `updatedAt/By`, nothing is deleted):
   - **`MessageTemplate` · `message_templates`** — S11 §3 table: `code?`, `kind` (`CATALOG·CUSTOM`), `category`, `title`/`body`/`smsBody` (`LocalizedText`), `icon` (enum `check·x·unlock·warn·up·heart·bell·doc·flag·mail·cal·clock·info·paw·cone·lock`), `color` (`NEUTRAL·OK·WARNING·ERROR·ACCENT`), `matrix` (`Map<Audience, Map<Channel, Boolean>>`, only `MEMBER·INSTRUCTORS·ADMINS` × `APP·EMAIL·SMS`), `enabled`, `mandatory`, `customized`, `status` (`ACTIVE·DISABLED·ARCHIVED`); unique `{clubId, code}` (partial, `code != null`), index `{clubId, category, status}`.
   - **`Notification` · `notifications`** — **generalise the E1 document to the S11 §3 shape**: `code`, `category`, `templateId?`, `templateVersion?`, `eventId?`, `eventType?`, `dedupKey`, `audience`, `recipient {accountId?, memberId?, instructorId?, email?, displayName}`, `locale`, `subject {dogId?, bookingId?, classSessionId?, waitlistEntryId?, trainingBookingId?, invoiceId?, activityId?, taskId?, memberId?}`, `icon`, `color`, `title`, `body`, `smsBody?`, `action {type, params}`, `deliveries[] {channel, target, status, attempts, nextAttemptAt?, providerRef?, lastError?, sentAt?, deliveredAt?, failedAt?}` (one per channel **and destination**), `readAt?`, `createdAt`. `Status` enum = `QUEUED · SENT · DELIVERED · FAILED · SKIPPED_BY_PREFERENCE · SKIPPED_MODULE_OFF` (catalog rule 7) **+ `SKIPPED_NO_CONTACT`, `SKIPPED_CAP`, `SKIPPED_STALE`** (S11 §13 proposals — implement them and write the proposal in your report). Indexes exactly as §3: `{clubId, dedupKey}` **unique**, `{clubId, recipient.accountId, createdAt desc}`, `{clubId, recipient.accountId, readAt}`, `{clubId, createdAt desc}`, `{deliveries.status, deliveries.nextAttemptAt}`. `SYSTEM` rows keep `clubId = null` (E1-T03 allows it explicitly in `NotificationRepository`).
   - **Compatibility**: adapt `NotificationRepository` and `SystemNotificationService` so the helpers used by E1 and by the E3–E6 dispatchers (`send`, `sendOnce`, `sendOnceLocalized`, `sendApplicantOnce`, `appOnce`, `smsIntentOnce`, `completed`, `finish`, `delivery`) keep **exactly their signatures and semantics** and write one document with a single delivery each (`dedupKey` = the deterministic id they already pass). E7-T02 replaces those call sites and removes the helpers; change no caller in this task. Add the CLI command `messaging:migrate-notifications` (`CoreCommand`, dry-run by default) converting legacy flat rows into the new shape for local/staging databases; there is no production data (R1 is not live) — say so in the report.
   - **`PushSubscription` · `push_subscriptions`** — `accountId`, `endpoint` (stored hashed, unique `{clubId, endpointHash}`), `keys {p256dh, auth}`, `deviceLabel`, `userAgent`, `status` (`ACTIVE·EXPIRED`), `failureCount`, `lastSuccessAt`, `expiredAt`; index `{clubId, accountId, status}`.
   - **`NotificationPreference`** — typed embedded record over the existing `Member.notificationPreferences` map: `emailByCategory {OPERATIONAL:false, PERSONAL:true, CLUB_CHANGES:true, CLUB_NEWS:true}`, `reminderMinutesBefore` (`Integer?`, `null` = «Mai»), `pushClubNews` (`true`), `updatedAt`, `updatedByAccountId`; absence of the block = these defaults (never persisted on read).
   - **SMS counter** — `Club.usage` is a `Map<String, Long>`: keys `smsSentMonth` (count) and `smsMonthKey` (`yyyyMM` as a number, e.g. `202609`), reset when the club-local month changes (S15 R-15-15 g). Do not change the `Club` record shape; document the two keys in the report.
   - **Bounce marks** — `Member.contactEmails[]` gains `bounced` (bool, default false) per R-11-08; `Account.emailStatus` (E1-T03) stays the suppression signal for the login address.
4. **OpenAPI of §6 with 501 stubs** (controllers in `clubs/messaging/api`, springdoc annotations, bodies throw `UnsupportedOperationException` → 501 behind the role/tenant/module guards, roles from `MATRIU_PERMISOS`): `GET /message-templates` (ADMIN; `category?`, `kind?`, `includeArchived?`) · `POST /message-templates` (ADMIN, 201) · `GET /message-templates/{id}` · `PUT /message-templates/{id}` · `POST /message-templates/{id}/preview` · `POST /message-templates/{id}/reset` · `DELETE /message-templates/{id}` (204) · `POST /message-templates/{id}/send` (ADMIN, `Idempotency-Key`, 202 / 200 on `dryRun`) · `GET /notifications`, `GET /notifications/{id}`, `GET /notifications/filter-values` (ADMIN; universal list, `x-filterable: code, category, channel, status, memberId, createdAt`, `x-sortable: createdAt`, `@ListContract` columns `createdAt*, code*, recipient*, channels*, readAt`, default sort `createdAt desc`) and wire the **already published** `GET /notifications/export` (E2-T01, S14 R-14-12) to the same list definition · `GET /me/notifications` (MEMBER, INSTRUCTOR, ADMIN, incl. IMPERSONATED; `page`, `size`, `audience?`) · `POST /me/notifications/{id}/read`, `POST /me/notifications/read-all` · `GET /me/notification-preferences`, `PUT /me/notification-preferences` (MEMBER incl. IMPERSONATED) · `PUT /members/{id}/notification-preferences` (ADMIN) · `POST /push-subscriptions`, `DELETE /push-subscriptions/{id}` (MEMBER, INSTRUCTOR, ADMIN; `@RequiresModule(Module.PUSH)`) · `POST /email-unsubscribes` (ANON, signed token; S11 §13 proposal — publish it and note the proposal). `GET /faq-entries` **already exists** (E2, `clubs.catalogs`, `FaqReaderView`): do not duplicate the entity — E7-T03 completes the reader projection and the `FAQ` module guard (R-11-14). `POST /webhooks/email/sendgrid` already exists (E1-T03): leave it untouched here.
5. **Schemas** (names are the contract, required-by-default, nullable marked; fields = the §6 examples, complete): `MessageTemplateListItem {id, code?, kind, category, name, icon, color, enabled, customized, matrix, caps, push, variables[{key, label}], lastChange?}` + `MessageTemplateList {items[], countsByCategory}` · `MessageTemplateDetail` (adds `titleI18n`, `bodyI18n`, `smsBodyI18n`, `seedDefault`, `mandatory`, `status`, `version`) · `MessageTemplateCreateRequest {category, title, body, smsBody?, icon, color, matrix}` · `MessageTemplateUpdateRequest {title, body, smsBody, icon, color, matrix, enabled, version, category?}` · `TemplatePreviewRequest {locale, draft?{title, body, smsBody}, sendTest?}` (`sendTest` = the backlog's «envia prova», implemented in E7-T03: one delivery of the rendered draft to the acting admin only) / `TemplatePreview {title, body, emailSubject, emailHtml, sms{text, length, segments, truncated}, warnings[]}` · `AnnouncementRequest {recipients{memberIds[]} | {filters[], q}, dryRun}` / `AnnouncementResult {batchId, recipientCount}` · `NotificationListItem {id, createdAt, code, category, audience, recipient{displayName, memberId?, email?}, channels[{channel, status}], readAt?}`, `NotificationDetail` (adds `title`, `body`, `smsBody?`, `locale`, `templateId?`, `templateVersion?`, `eventType?`, `subject`, `deliveries[]` with `providerRef`/`lastError`) · `MeNotification {id, code, category, icon, color, title, body, createdAt, channels[], readAt?, action{type, params, enabled}}` + `MeNotifications {items[], page, size, totalItems, unreadCount}` · `ReadResult {unreadCount}` · `NotificationPreferences {emailByCategory, smsFixed: true, reminderMinutesBefore?, reminderOptionsMinutes[], pushClubNews, locale, availableLocales[], modules{sms, push}}` + `NotificationPreferencesRequest {emailByCategory?, reminderMinutesBefore?, pushClubNews?}` · `PushSubscriptionRequest {endpoint, keys{p256dh, auth}, deviceLabel?}` / `PushSubscriptionCreated {id}` · `EmailUnsubscribeRequest {token}`. Enums published once: `NotificationCategory`, `NotificationAudience`, `NotificationChannel`, `DeliveryStatus`, `NotificationActionType`, `TemplateIcon`, `TemplateColor`, `TemplateKind`, `TemplateStatus`. The two §6 JSON extracts (`GET /me/notifications`, `GET /me/notification-preferences`) become fixtures `e7-me-notifications.json` and `e7-notification-preferences.json` and must round-trip.
6. **Error-code status audit** (no new code; `CATALEG_ERRORS.md` wins over any status written in S11 §6): the enum already carries every S11 code — verify and list them in the report with their canonical status: **400** `TEMPLATE_SYNTAX_ERROR`, `TEMPLATE_UNKNOWN_VARIABLE`, `SMS_BODY_REQUIRED`, `SMS_BODY_TOO_LONG`, `VALIDATION_ERROR`, `INVALID_FILTER`; **401** `WEBHOOK_SIGNATURE_INVALID`; **404** `NOT_FOUND`, `MODULE_DISABLED`; **409** `STALE_VERSION`, `INVALID_STATE`, `IDEMPOTENCY_KEY_REUSED`; **422** (rule 0, although S11 §6 writes 400/409 for several) `CHANNEL_NOT_ALLOWED`, `INVALID_REMINDER_OPTION`, `NO_RECIPIENTS`, `PUSH_SUBSCRIPTION_INVALID`, `TEMPLATE_MANDATORY`, `TEMPLATE_NOT_CATALOG`, `TEMPLATE_NOT_CUSTOM`, `TEMPLATE_NOT_SENDABLE`, `UNSUBSCRIBE_TOKEN_INVALID`. **`TEMPLATE_MISSING_VARIABLE` (S11 §6) does not exist in `CATALEG_ERRORS.md` nor in `ErrorCode`**: use `VALIDATION_ERROR` (400) with `details.missingVariables[]` meanwhile and write the proposal («`TEMPLATE_MISSING_VARIABLE`, 400, S11») in your report and in `roadmap/MESSAGES.md`. Messages `error.<CODE>` must exist in the three `messages_*.properties`.
7. **Events**: records in `clubs/messaging/domain` implementing `DomainEvent` with the catalog names and payloads — `MessageTemplateChanged{templateId, diff}`, `AnnouncementSent{templateId, batchId, recipientCount, filters}`, `PushSubscribed{accountId, endpoint}`, `PushUnsubscribed{accountId, endpoint}`, `EmailBounced{memberId, email, type}`, `SmsCapReached{month, cap}`, `NotificationPreferencesChanged{memberId, diff, byAccountId}`, `EmailUnsubscribed{memberId}`; keep the existing `NotificationEvent` (`NotificationQueued/Sent/Failed{notificationId, channel}`) and add `channel` to its payload. Register samples in `EventCatalogContractTest` with fixtures `e7-events.json`; `ReminderDue` is S15's and is **consumed**, not emitted here.
8. **Audit**: `ANNOUNCEMENT_SENT` is in S14 R-14-09 but missing from the `AuditAction` enum — add it (with its `@AuditCovers` test placeholder for E7-T04). `MessageTemplate` changes audit with the existing `CATALOG_CHANGED` (S14 R-14-09 lists `MessageTemplate` there) and expose `lastChange` on the template detail (R-14-11, `AuditQuery.lastChange`). Preference changes made by an ADMIN at D10 audit with the existing `MEMBER_UPDATED` (`changes[].path = notificationPreferences`) — no dedicated action exists; say so in the report.
9. `bin/openapi-snapshot`, `docs/openapi/CHANGELOG.md` entry (every route + list fields), `E7ContractIT` (501 matrix: every route × ANON/MEMBER/INSTRUCTOR/ADMIN/AGILITYHUB_ADMIN; `/message-templates*` and `/notifications*` with an impersonation token → 403 per T-11-28; MEMBER/INSTRUCTOR on `/message-templates*` → 403; cross-tenant → 404 (T-11-29); `PUSH` off → `/push-subscriptions` 404; `FAQ` off → `/faq-entries` 404; no writes on any collection), `E7ResponseContractTest` (fixtures round-trip of the §6 forms), `OpenApiRequiredContractTest` updated.

## Fixed conventions
- Code names from `docs/MODEL_DADES_PLATAFORMA.md` §0 (`MessageTemplate`, `Notification`, `NotificationPreference`, `PushSubscription`, `FaqEntry`, `Account`, `Member`, `Club`); context `clubs.messaging`; ArchUnit forbids cycles and cross-context access outside `application` packages — `messaging` reads other contexts only through ports (E7-T02 defines them), never the reverse.
- Catalogs are closed: every code, event, parameter, module, audit action and error code named here exists in `docs/specs/00-transversal/` (the only exceptions are named in steps 3, 4 and 6 as explicit proposals — implement the closest existing item and write the proposal in the report **and** in `roadmap/MESSAGES.md`). Rule 0 of `CATALEG_ERRORS.md` is literal and the catalog file wins over S11 §6.
- Where S11 and `MODEL_DADES_PLATAFORMA.md` disagree, the model wins and you say so in the report. Assumptions in force you must not re-decide (`docs/DECISIONS_PENDENTS.md` part C S11): staff texts are non-editable product copy; entering screen 11 marks everything read; templates are deactivable and `CUSTOM` templates may carry SMS; **no Twilio delivery callback at R1** (`SENT` is the terminal SMS state); the notification log is kept forever (pseudonymised on erasure, R-14-15); the 4th row `CLUB_NEWS` exists in the preferences matrix although the mockup does not show it (S11 §13-2, B21).
- Evidence rule: report = command + exit code + last 40 lines; long outputs → `roadmap/evidence/E7-T01/NN-<name>.log`; task file ≤ 120 KB. Fictional data only (`@example.test`, fake ids); no secrets in the repo — every provider key is an environment variable. Never edit other tasks, `roadmap/ROADMAP.md` or the *Organizer verification* section.
- This task implements **no** business behaviour: bodies stay 501, `SystemNotificationService` keeps its current behaviour on the new storage, and no existing green test may change its expectations.

## Verification (run all; evidence = command + exit code + last 40 lines; long outputs → `roadmap/evidence/<ID>/*.log`)
- `./mvnw -q verify` green with `NotificationCatalogContractTest`, `E7ContractIT`, `E7ResponseContractTest`, `EventCatalogContractTest`, `ErrorCatalogContractTest`, `OpenApiRequiredContractTest`, `ArchitectureTest`, `AuditContractTest`, and the E1/E3/E4/E5/E6 suites unchanged (`SystemNotificationServiceIT`, `SendGridWebhookIT` included).
- `bin/openapi-snapshot` twice → byte-identical (`cmp`); `docs/openapi/CHANGELOG.md` lists the operations added (count them and state the number); `git diff --check` clean.
- `bin/core messaging:migrate-notifications --dry-run` on the local stack after `bin/core seed:demo --club=canic --seed=42` → prints the legacy rows it would convert, exit 0.

## Definition of Done
Every S11 §6 route is published with its schemas, roles, modules, idempotency and canonical error statuses and answers 501 behind the guards; `NotificationCatalog` holds every row of `CATALEG_NOTIFICACIONS.md` and a test proves the equality in both directions; the four documents exist with the §3 indexes; the existing E1/E3–E6 notification paths keep working on the new `Notification` shape; the organizer stages the snapshot for the web.

## Evidence to return
`verify` tail; snapshot `cmp` result; OpenAPI diff summary (routes + schemas added); the number of catalog rows registered and the output of the parity test; the list of error codes with the status you asserted; the catalog proposals (`TEMPLATE_MISSING_VARIABLE`, the three `SKIPPED_*` statuses, `POST /email-unsubscribes`) as you wrote them in `roadmap/MESSAGES.md`.

---

## Executor report
_(not started)_

### Branch / commits

### Evidence (full command outputs)

### Files changed

### Rules and tests implemented (R-xx-nn / T-xx-nn)

### Assumptions

### Questions / catalog proposals

## Organizer verification
_(pending)_
