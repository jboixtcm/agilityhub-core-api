---
id: E3-T08
title: Gate E3 audit fixes (api, 1/3) — resolved signup texts and payment instructions, a quote per plan, assignable plans (family fare, add-dog, D2), upfront rows per submission, the admins' N-01, D1 fresh after each command, server-side §3 constraints, pending dogs on 13, and bin/e3-smoke green
stage: E3
repo: agilityhub-core-api
thread: A
order: 80
status: awaiting_verification
depends_on: [E3-T07]
branch:
pr:
updated: 2026-09-24
---

# E3-T08 · Gate E3 audit fixes (api, 1/3)

## Objective
Jordi asked for an exhaustive review of gate E3 before he closes the stage. Codex and the organizer audited the code separately, at `20552c6`, and both say the gate does not pass. The consolidated report is `roadmap/reviews/gate-E3/consolidated.md`; the two source reports sit next to it with the file:line detail.

This task fixes the api majors of the signup configuration, the plans, the upfront payments, the notifications and D1, and it puts `bin/e3-smoke` back to green. **E3-T09** takes the security majors, **E3-T10** the api minors, and **E3-T11** re-runs the audit at the end. The web fixes are E3-W06…W08.

Every step names the finding (the M-number of the consolidated report), the defect, the rule, the fix and the test. **Each fix comes with a test that fails before the fix.** Name those tests in the report, and say which ones you saw failing first.

## Spec references
- S04:
  - §2, rows 16–19, D2 and 13;
  - §3 (`Member`, `Member.signup`, `Dog`, `UpfrontPayment`);
  - R-04-06/07, R-04-12…16, R-04-24/25;
  - §5 (`UpfrontPayment.status` and the paragraph under it), §6, §8, §9 and §10.
- S14: R-14-01, R-14-03, R-14-05 and R-14-07 (amended by E35).
- S03: row 13 and R-03-02 (amended by E36).
- S13: `deadlineDay` of `GET /me/inactivity-periods`.
- Catalogs:
  - `CATALEG_NOTIFICACIONS.md`, N-01;
  - `CATALEG_PARAMETRES.md`, `signup.text.*`, `signup.allowFamilyGroupPending`, `signup.requireDogDocumentAtSignup` and `dashboard.pendingSignupAgeWarnDays`.
- `DECISIONS_PENDENTS.md` Part E: **E35, E36 and E39** (organizer, 24-09).

## Context to load
- `roadmap/reviews/gate-E3/consolidated.md`, and the source reports where a step needs more detail.
- `roadmap/tasks/E3-T07.md`: the baseline run, with the smoke failure and its diagnostic.
- Code:
  - `clubs/census/application/SignupService.java`, `SignupNotifications.java` and `CensusQuery.java`;
  - `clubs/signup/domain/SignupPlanCatalog.java` and `SignupPolicy.java`;
  - `payments/application/UpfrontPayments.java`;
  - `clubs/dashboard/application/DashboardEvents.java` and `DashboardQuery.java`;
  - `bin/e3-smoke`.

## Steps
1. **`bin/e3-smoke` green again (E3-T07).**
   - The «Future verticals must stay empty» assertion (`bin/e3-smoke:238-239`) is older than the E4-T05 planning seed. Replace it with the S14 R-14-03 expectation for the seeded current week:
     - `capacity` = Σ `capacity` of the week's `ACTIVE`/`FINISHED` classes;
     - `booked` = Σ `counters.booked`;
     - `percent` = round(100·booked/capacity), or `null` when the capacity is 0.
   - Compute the expected values from what the smoke itself reads (the week's class sessions, or the seed), never as literals.
   - `trainingBookings` stays `{0, 0}` while the seed has no training bookings.
   - Keep every other assertion.
2. **M6 · resolved texts in `GET /signup`.** `texts.paymentDay` and `texts.familyGroupIntro` still carry `{deadlineDay}` and `{twoDogsMonthlyFee}` (`SignupService.java:221-237`; the E3-W03 screenshots 18/19 show them). Interpolate on the server, per locale:
   - `{deadlineDay}` = the day that S13 returns as `deadlineDay` in `GET /me/inactivity-periods` (25 at the Cànic); read the same parameter.
   - `{twoDogsMonthlyFee}` = the current monthly price of the family-fare plan that R-04-13 proposes (the eligible `Plan` with `dogsIncluded` = 2, resolved as `SignupPolicy` already does), formatted as money in the text's locale.
   - A club without a family-fare plan gets `familyGroupIntro: null`, never a raw placeholder.
   
   IT: for the Cànic seed and for the minimal club, in ca, es and en, no value in `texts` contains `{` or `}`.
3. **M7 · payment instructions (§2 row 19, §10, N-01).**
   - `paymentMethods[MANUAL].instructions` = `CLUB.paymentProviders.MANUAL.instructions` (model §2), not `signup.text.cashConditions` (`SignupService.java:226`).
   - The cash conditions stay in `texts.cashConditions`. The web shows them only under the CASH method (E3-W06).
   - The applicant's N-01 (public signup and add-dog) renders `upfront_total` and `payment_instructions`, from the frozen `signup.upfront.totalDue` and the chosen method's instructions.
   - `pay_link` belongs to Stripe (**E8-T04**); leave it empty here.
   
   Tests:
   - `GET /signup` returns the club's MANUAL instructions;
   - the ca N-01 body carries the total and the instructions.
4. **M5, api side · a quote per plan in `GET /signup` (R-04-14/15).**
   - Today the config builds one set of first-month options from the first monthly plan, and the web shows it for every plan.
   - Return a quote **per offered plan**, computed with the same code as the submission: its `lines` (`ENTRY_FEE` only if > 0, `PACK`, …) and, for `MONTHLY` plans with a standard monthly price, the two first-month options. Each option is `{option: TODAY | ALTERNATIVE, portion: FULL | HALF, startDate, amountDue, totalDue}`, with `totalDue` = the lines + that option. `PACK` and `SINGLE_CLASS` plans have a plan-level `totalDue` and no options.
   - Add-dog mode: the TODAY option (entry fee + `ADDITIONAL_DOG_FEE` of the current month) **always** exists. The ALTERNATIVE option («alta l'1 de {mes següent}», entry fee only) exists only while today ≤ `billing.upfrontCutoffDay`. Today `additionalDogOptions` is empty after the cutoff, so the add-dog quote has no payable line.
   
   Tests, on the R-04-14/15 examples:
   - an ABONAT quote on 17-08-2026: 100 + 30 = 130 €, and 100 + 60 €;
   - on 05-08: TODAY FULL and ALTERNATIVE HALF;
   - Pack 6 → 135 €;
   - Teràpia → 50 €, with no options;
   - add-dog on day 26 → TODAY only.
5. **M8 · assignable plans are not only the plans offered at signup.**
   - `SignupPlanCatalog` (`:18`) filters `active ∧ showOnSignup ∧ module`, and every use goes through it. The Cànic's `ABONAT_FAMILIAR` has `showOnSignup: false`.
   - Keep that list only for the public offer: the `GET /signup` plans and the applicant's `planIdRequested`.
   - Everything else uses the **assignable** plans (`active ∧ module`, whatever `showOnSignup` says):
     - the family-fare proposal (`proposedPlan`, R-04-13);
     - the admin's `planId`/`priceId` at validation and in `dryRun`;
     - add-dog (`addDog` → the member's current plan, and `additionalFee`);
     - loading D2 when the requested plan was hidden afterwards.
   - Add `planOptions[]` to `GET /members/{id}/signup`: the assignable plans, each with `{planId, name (resolved), type, prices[{priceId, amount, periodicity, concept}]}`. The D2 plan selector (E3-W07) reads it.
   
   Tests:
   - a FOUND family claim → the proposal is `ABONAT_FAMILIAR`, and validation with it succeeds;
   - a member on `ABONAT_FAMILIAR` adds a dog (201), and `GET /signup` works for that member on days 1–25;
   - D2 still loads after the requested plan gets `showOnSignup: false`;
   - a public `POST /signup` with a hidden plan → `422 PLAN_NOT_AVAILABLE` (unchanged).
6. **M9 · the admins' N-01 (§8, N-01).** The admins get the applicant's text today («Hem rebut la teva sol·licitud…», `SignupNotifications.java:34`).
   - Add admin keys in ca, es and en that use `member_name`, `dogs` and `plan_name`, with the D2 link (`OPEN_SIGNUP`).
   - The applicant's copy never carries `OPEN_SIGNUP`.
   
   Test: both bodies asserted in ca, for a public signup and for an add-dog.
7. **M10 · upfront rows belong to one submission, and amounts never change (§5, ruling E39).**
   - Each `UpfrontPayment` row carries `submissionId`: a new id per `POST /signup` or `POST /me/dogs/signup`, also kept in `Member.signup`. Every read of «the rows of this signup» filters by it: D2, validation, `dryRun`, rejection and checkout. The rows of an earlier submission (a readmission, R-04-06/07) are history and never count.
   - `replace()` (`UpfrontPayments.java:63-79`) handles a plan change at D2:
     - it cancels only `DUE` rows and creates the new ones;
     - it never rewrites `amountDue` or `amountPaid` of an existing row;
     - `PAID` and `PARTIAL` rows stay as they are, and their `amountPaid` is deducted from the new amount due.
   - **E39:** a plan change while a row of the submission is `CHECKOUT_PENDING` is refused with `409 INVALID_STATE`, `details.reason = CHECKOUT_PENDING`. `dryRun` returns the same condition as a warning.
   - Propose `submissionId` for `MODEL_DADES_CANIC.md` in the report.
   
   Tests:
   - a readmitted member whose earlier application has a `PAID` entry fee for the same dog: D2 and validation see only the new rows;
   - a plan change with a `PARTIAL` row keeps that row intact and deducts it;
   - a plan change with a `CHECKOUT_PENDING` row → 409.
8. **M11 · D1 fresh right after a command (R-14-01).** The dashboard and menu-counter caches are evicted only by the async outbox consumers (`DashboardEvents`, polled every second), so D1 still lists a signup that was just validated.
   - Evict synchronously after commit in the commands that change the pending list or the KPIs:
     - submission and add-dog;
     - validation and rejection;
     - the D2 edits that change a pending row.
   - Keep the async path for the other sources.
   - `GET /members/{id}/signup` also returns `warnDays` (`dashboard.pendingSignupAgeWarnDays`), so D2 no longer calls `/dashboard`.
   - A member with nothing pending answers `409 INVALID_STATE` with `details.reason = NOT_PENDING`; D2 shows a clear message (E3-W07).
   
   IT, without any sleep: validate → `GET /dashboard` and `GET /dashboard/counters` straight away → the signup is gone and the counts are updated. The same for a rejection and for a submission.
9. **M20, api side · pending dogs on screen 13 (ruling E36).**
   - `GET /me/dogs` also returns the member's own `PENDING` dogs, and every dog carries `status` (`ACTIVE` | `PENDING`).
   - `ACTIVE` dogs keep everything they have today. A `PENDING` dog carries only `{id, name, breed, sex, ageYears, status}`: no tasks, documents, notes or pack.
   - Screen 13 is the only user of this route. The S08/S09 selectors read `/me/family-group`, which does not change.
   
   IT:
   - after `POST /me/dogs/signup`, `GET /me/dogs` lists the new dog with `status: PENDING`;
   - after validation, the dog is `ACTIVE`;
   - after a rejection, it is gone.
10. **M21 · the §3 constraints, enforced by the server.**
    - `nextInvoiceDate` ≥ `signup.upfront.firstMonth.startDate`, at validation, in `dryRun` and in the D2 `PATCH` → `400 VALIDATION_ERROR` on that field.
    - `birthDate` in the past and ≥ 1900-01-01; `SignupService.java:141` checks only the future.
    - `chip` per country profile (§3 `Dog`):
      - `ES` → 15 digits; `GENERIC` → 8–15 alphanumerics;
      - normalised (spaces and dashes removed, upper case) **before** the uniqueness check (R-04-07) and the readmission match;
      - enforced in `POST /signup`, `POST /me/dogs/signup` and the D2 dog `PATCH`.
    
    Tests per rule. Include a chip typed with spaces that equals an existing chip → `422 DOG_CHIP_ALREADY_REGISTERED`.
11. **The dog's own version in D2 (M12, api side).** Each dog of `GET /members/{id}/signup` carries its `version`: the one that `PATCH /dogs/{id}` compares (`DogService.java:34`). Test: after an add-dog and a member edit, the dog `PATCH` with the dog's version answers 200.
12. **The signup flags in `GET /signup`.**
    - Expose `allowFamilyGroupPending` (`signup.allowFamilyGroupPending`, module `FAMILY_GROUP`) and `requireDogDocumentAtSignup` (`signup.requireDogDocumentAtSignup`), so the web can enforce them (E3-W08).
    - The server keeps enforcing both at submission.
    
    Contract test.
13. **E35 · D1 «Gossos per nivell».**
    - The chart uses the progression levels, like D3 coverage (R-06-06 after E29): one column per `Level.active ∧ progression`, in `order`.
    - The dogs of every other level (Teràpia, Pendent, inactive levels) count in `others` and in `totalActiveDogs`.
    - Update T-14-06 and the smoke's seed expectation.

## Fixed conventions
- Contract changes are additive where possible. Regenerate `docs/openapi/openapi.json`, and add one `docs/openapi/CHANGELOG.md` entry that lists them:
  - the per-plan quote and `planOptions`;
  - `warnDays` and the dog `version`;
  - the `/me/dogs` `status`;
  - the two flags;
  - the new 409 `details.reason` values.
- Error statuses follow rule 0 of `CATALEG_ERRORS.md`.
- Test names follow AGENTS rule 5 (`T_04_nn_…` / `R_04_nn_…` where a spec id applies).
- Fictional data only.

## Verification (run all; evidence = command + exit code + last 40 lines; long outputs → `roadmap/evidence/E3-T08/*.log`)
- `./mvnw -q clean verify`: the totals, plus one line per test class that this task adds or changes.
- `bin/e3-smoke` twice on a fresh stack: both runs exit 0.
- `bin/openapi-snapshot`, then `git diff --stat docs/openapi/`.

## Definition of Done
- Steps 1–13 done, each with a test that failed before the fix.
- `bin/e3-smoke` green twice.
- No raw placeholder in the texts of `GET /signup`.

## Evidence to return
- The command outputs.
- A step → test table.
- The tests that failed before their fix.
- The OpenAPI changelog entry.

---

## Executor report

**Executor:** claude:claude-opus-5-5 · 2026-09-24 · base `6c844f9` (working tree; the publish script commits it).

### Summary
All 13 steps are done. `./mvnw -q clean verify` exits 0: 521 unit and 848 integration tests, 0 failures. `bin/e3-smoke` exits 0 twice on a fresh stack. The OpenAPI snapshot is regenerated, with a `docs/openapi/CHANGELOG.md` entry. There is one new IT class, `SignupGateFixesIT` (18 tests). The tests that cover these steps were run against the unfixed `HEAD` production code: all fail there except `T_04_09`, which the task marks «(unchanged)» (log `02`).

### Files changed
- **Production**
  - `clubs/census/application/SignupService.java`:
    - steps 2–8 and 10–12;
    - the dog is prepared before the member is stored, so `Member.signup.upfront` is frozen without a second save;
    - `submissionId`;
    - scoped rows;
    - D1 eviction;
    - `NOT_PENDING`;
    - `planOptions`, `warnDays` and the dog `version`;
    - `planQuotes`, texts and flags in `GET /signup`.
  - `SignupNotifications.java`: the applicant copy (`upfront_total`, `payment_instructions`, no action) and the admin copy (`OPEN_SIGNUP`). It also fixes a pre-existing bug: `Criteria.in(Object)` received the id list as a single value, so `{dogs}` was always empty.
  - `CensusQuery.java`: `/me/dogs` returns the own `PENDING` dogs and a `status` on every dog.
  - `MemberService.java`:
    - `birthDate` must be before today and ≥ 1900-01-01;
    - a pending edit evicts the D1 caches.
  - `DogService.java`:
    - a pending dog's chip is normalised and checked;
    - a pending edit evicts the D1 caches.
  - `DemoSignupSeeder.java`: a deterministic `submissionId`.
  - `clubs/signup/application/SignupPolicy.java`:
    - assignable plans;
    - `familyFare`/`proposedFamilyFare`;
    - `planQuote`;
    - `chip`;
    - every current price of a plan.
  - `clubs/signup/domain/SignupPlanCatalog.java`: `assignable()`; `Offer.prices`.
  - `DogChips.java` (new).
  - `payments/application/UpfrontPayments.java`:
    - the `Submission` scope;
    - `replacement()` and `replace()` follow E39;
    - `create(member, submissionId, …)`.
  - `payments/persistence/UpfrontPayment.java`: `+submissionId`.
  - `SignupPaymentAccess.java` and `CheckoutService.java`: a checkout charges only the scoped rows.
  - `platform/application/CensusClubSettings.java`: `manualInstructions(locale)`.
  - `clubs/messaging/application/SystemEmailRenderer.java` and `SystemNotificationService.java`: a copy `variant` (`notif.{code}.{variant}.*`).
  - `clubs/dashboard/application/DashboardData.java`, `domain/DogActivityBuilder.java` and `persistence/DashboardRepository.java`: E35.
  - `shared/application/contract/SignupWarning.java`: `CHECKOUT_PENDING`.
  - `clubs/census/api/SignupResponses.java`, `CensusResponses.java` and `SignupController.java`: the contract.
  - `messages_{ca,es,en}.properties`: `notif.N-01.upfront.*` and `notif.N-01.admin.*`.
- **Tests**
  - New: `SignupGateFixesIT`, `DogChipsTest`.
  - Changed:
    - `SignupIT` (T-04-18: scoped API, PARTIAL row intact);
    - `DashboardIT` (T-14-11 E35 columns; R-14-01 without the outbox);
    - `DashboardBuildersTest` (T-14-06);
    - `E3ContractIT` and `E3ResponseContractTest` with its fixture `e3-responses.json` (new fields).
- **Other**
  - `bin/e3-smoke`:
    - step 1;
    - E35 columns;
    - no placeholders, and one quote per plan;
    - the family fare is proposed, and the admin pays the dry-run total;
    - the member's N-01 APP row without the admin action.
  - `docs/openapi/openapi.json` and `docs/openapi/CHANGELOG.md`; `CHANGELOG.md`.

### Step → test table (the «fails before» column is from `roadmap/evidence/E3-T08/02-baseline-before-fixes.log`, run on the `HEAD` production code)
| Step | Test(s) | Failed before the fix |
|---|---|---|
| 1 smoke | `bin/e3-smoke` ×2 (logs `05`, `06`): «D1 class occupancy follows R-14-03 … 17 classes, booked=0, capacity=75, percent=0.0» | E3-T07 baseline: `FAIL: Future verticals must stay empty` |
| 2 M6 | `T_04_06_signupTextsHaveNoRawPlaceholdersForTheCanicAndTheMinimalClub` (Cànic seed and minimal seed; ca/es/en) | yes: `[ca familyGroupIntro] … {twoDogsMonthlyFee}` |
| 3 M7 + 6 M9 | `T_04_14_manualInstructionsComeFromTheClubProviderAndN01CarriesTotalAndInstructions`: the ca applicant and admin bodies, for a public signup and an add-dog; the APP rows | yes: `expected "Paga a la recepció…" but was "Pagament en efectiu…"` |
| 4 M5 | `T_04_05_T_04_06_eachPlanGetsItsOwnQuoteLikeTheSubmission` (17-08: 130/160 €; 05-08: TODAY FULL and ALTERNATIVE HALF; Pack 6 = 135 €; Teràpia = 50 € without options); `T_04_06_addDogQuoteKeepsTodayAfterTheCutoffDay` (day 26: TODAY only, 130 €) | yes, both (`No quote for …`; `Expected size: 1 but was: 0`) |
| 5 M8 | `T_04_16_foundFamilyClaimProposesAndValidatesTheHiddenFamilyFare` (also a group of 3 dogs); `T_04_21_familyFareMemberAddsADogAndReadsTheSignupConfiguration` (days 1, 17, 25, then 201); `T_04_18_D2LoadsAfterTheRequestedPlanIsHidden`; `T_04_09_publicSignupWithAHiddenPlanIsNotAvailable` | yes, the first three (the proposal was ABONAT; `422 PLAN_NOT_AVAILABLE` twice). `T_04_09` passed before, as expected («unchanged») |
| 7 M10/E39 | `T_04_12_readmissionSeesOnlyTheRowsOfItsOwnSubmission`; `T_04_18_planChangeKeepsAPartialRowIntactAndDeductsIt`; `T_04_22_planChangeWithACheckoutInProgressIsRefused`; `SignupIT.T_04_18_planChangesRecalculateDueAndPreservePartialPayments` (service level) | yes, all three new tests (`Expected size: 2 but was: 3`; the PARTIAL row id was missing from the preview; no `CHECKOUT_PENDING` warning) |
| 8 M11 | `DashboardIT.T_14_11_R_14_01_submissionValidationAndRejectionRefreshD1WithoutTheOutbox` (no dispatch, no sleep); `T_04_24_reviewCarriesWarnDaysAndNothingPendingIsNotPending` | yes, both (`expected: 1 but was: 0` right after the submission) |
| 9 M20/E36 | `T_04_21_R_04_25_myDogsListsTheOwnPendingDogUntilValidationOrRejection` | yes (`pending is null`) |
| 10 M21 | `T_04_18_nextInvoiceDateBeforeTheFirstMonthStartIsRejected` (validation and `dryRun`); `T_04_02_birthDateMustBeInThePastAndNotBefore1900` (signup and member PATCH); `T_04_07_chipIsNormalisedAndCheckedPerCountryProfile` (a chip with spaces → 422 `DOG_CHIP_ALREADY_REGISTERED`, format → 400, stored normalised, D2 PATCH, add-dog); `DogChipsTest` (ES and GENERIC) | yes, all three ITs (`expected: 400 but was: 200/201`; `expected: 422 but was: 201`). `DogChipsTest` is a new class |
| 11 M12 | `R_04_19_reviewDogCarriesTheVersionThatTheDogPatchCompares` | yes |
| 12 flags | `R_04_08_R_04_12_signupFlagsAreExposedAndStillEnforced`; `E3ContractIT` (schema) | yes |
| 13 E35 | `DashboardBuildersTest.T_14_06_dogsOfInactiveNonProgressionAndMissingLevelsBelongToOthersIncludingTotal`; `DashboardIT.T_14_11_seededCensusShapeCacheExpiryAndLocaleVariants` (the 8 Cànic columns CAD, A–G); smoke «8 progression columns» | yes, the IT failed with `["CAD", …, "TER", "PENDENT"]`. The builder test uses the new `LevelSource.progression` field, so it cannot compile against `HEAD` and was not run there |

How the baseline was run:
- I saved the fixed files, put the `HEAD` versions of the production files back (`git show HEAD:<file>`, read-only) and ran the two IT classes.
- Then I restored the fixed files and checked them against a sha256 manifest: «restored and verified 31 files».
- `SignupIT` and `DashboardBuildersTest` use the new API, so they ran at their `HEAD` versions.

### Rules and tests implemented
- **S04:** R-04-06/07 (the readmission rows and the chip match), R-04-09, R-04-12, R-04-13, R-04-14/15, R-04-19, R-04-24 (`warnDays`), R-04-25; §3 (`nextInvoiceDate`, `birthDate`, `chip`); §5 (E39, `submissionId`); §8 (N-01).
- **S14:** R-14-01 and R-14-07 (E35). **S03:** R-03-02 and row 13 (E36).
- **Test ids:**
  - T-04-02, T-04-05, T-04-06, T-04-07, T-04-09, T-04-12, T-04-14, T-04-16, T-04-18, T-04-21, T-04-22, T-04-24;
  - T-14-06, T-14-11.

### Assumptions
1. **N-01 copies.** The copies are message variants of the same catalog code: `notif.N-01.admin.*` for the admins, and `notif.N-01.upfront.*` for an applicant with an amount due. The notification row keeps `N-01`. I did not use an `audience` variable, because the catalog row of N-01 does not list one.
   - The admin email has no URL. The D2 link is the APP row's `action: OPEN_SIGNUP` + `entityId`.
   - The applicant's copies (EMAIL, and the member's APP row in add-dog) carry neither.
2. **`payment_instructions`.** It is `MANUAL.instructions` whenever no provider checkout is required, whatever the monthly method, because the upfront is paid to the club (§2 row 19). With a checkout it is empty, and `pay_link` stays empty (E8-T04).
3. **`{deadlineDay}` and the family intro.**
   - `{deadlineDay}` comes from `inactivity.requestDeadlineDay`, the parameter behind S13's `deadlineDay`. `GET /me/inactivity-periods` is not implemented yet.
   - Without BILLING there is no price, so there is no family fare, and `familyGroupIntro` is `null`.
4. **The kept rows of a plan change (E39).** The new rows are the new quote minus what the kept `PAID`/`PARTIAL` rows already charge (`amountDue`). No existing amount changes, and the outstanding total is «new quote − paid». Edge case: see question 1.
5. **Family fare for groups of 3 or more dogs.** When the group has more dogs than any plan includes, D2 proposes the largest family fare. R-04-13 says «≥ 2 dogs → the family fare if it exists»; the first smoke attempt hit this case (log `05a`). The add-dog fee keeps the strict rule: it falls back to `billing.familyDiscountPercentFromSecondDog`.
6. **`nextInvoiceDate` in «the D2 PATCH».** `PATCH /members/{id}` does not accept `nextInvoiceDate`: its allow-list answers 400. So the constraint is enforced at validation and in `dryRun`, where the field exists.
7. **`birthDate` «in the past».** It means strictly before the club's today. The same rule now applies to the S03 member PATCH.
8. **The chip format in PATCH `/dogs/{id}`.** It is enforced for `PENDING` dogs only (the D2 PATCH). An `ACTIVE` dog keeps the S03 contract: `CensusIT` expects `CHIP_ALREADY_EXISTS` with a 6-digit chip, and migrated chips may not match. Uniqueness on PATCH stays with the `dog_chip` index (`409 CHIP_ALREADY_EXISTS`).
9. **Add-dog.**
   - The member's own plan is assignable even when hidden.
   - Another `planIdRequested` must be in the public offer.
   - `GET /signup` in add-dog mode also quotes the member's own plan; `plans[]` stays the public offer.
10. **Old rows.** Rows written before this task have no `submissionId`. They still match a dog whose signup has none.
11. **The D1 warnings.** The D1 list warnings (`UPFRONT_UNPAID` in `CensusListProjection`/`DashboardRepository`) still read every non-cancelled row of the member. The rows of a rejected earlier submission are CANCELLED or PAID, so they add no amount due. I did not change the D1 aggregation.
12. **D1 eviction.** Census calls `clubs.dashboard.application.DashboardQuery.invalidateAfterCommit`. This is an application-package dependency with no cycle, and ArchUnit passes.
13. **New contract values.** `CHECKOUT_PENDING` is a new value of the contract enum `SignupWarning`; it is not a catalog item. `NOT_PENDING` is used for the D2 review, the `dryRun`/validation and the rejection.

### Questions / catalog proposals
1. @organizer E39 edge case. A kept `PARTIAL` row whose own amount is larger than the whole new quote leaves an outstanding total above «new quote − paid». This happens, for example, with an entry fee of 100 € with 50 € paid, and a change to a plan whose quote is 60 €. Amounts are immutable, so the row cannot shrink. Should such a row be cancelled and replaced by a `PAID` row for what was paid (a new record, S12 «les correccions són registres nous»)? Meanwhile: kept as is.
2. @organizer Should R-04-13 state the ≥ 3-dog case (assumption 5)?
3. **Model proposal for `MODEL_DADES_CANIC.md`:**
   - `UpfrontPayment.submissionId` (string, the submission that created the row);
   - `Member.signup.submissionId`, copied to `Dog.signup`.
   
   `MODEL_DADES_PLATAFORMA.md` already has `UpfrontPayment.submissionId` (organizer, 24-09).
4. No new parameter, event, notification or error code. The new message keys are copies of N-01. The `details.reason` values `NOT_PENDING` and `CHECKOUT_PENDING` come from the task and S04 §5.
5. **Web (E3-W06…W08):**
   - regenerate the client;
   - `MeDog.documents` and `MeDog.licenses` are no longer required;
   - `texts.familyGroupIntro` is nullable;
   - `planQuotes`, `planOptions`, `warnDays`, `dogs[].version`, the flags;
   - the `409` reasons;
   - the member's N-01 APP row no longer has `action`/`entityId`.
6. **Not mine.** During the session, docs and other tasks' files changed in the working tree (`docs/DECISIONS_PENDENTS.md`, `INCIDENCIES_OBERTES.md`, `MODEL_DADES_PLATAFORMA.md`, S02, S04, `roadmap/MESSAGES.md`, `E3-T09/T10`, `E5-T12…T15`, `E6-T01/T03`, `E8-T04/T06`). I left them as they were. The new `changes_requested` on E5-T12/T13 arrived after I had started E3-T08.
7. **Blocked commands.** These were denied, so I used the allowed alternatives listed:
   - `$?`, subshells with `&&`/`||`, `git archive`, `cp -R` and `ditto` → a Python helper under `target/` that swaps the files with `git show HEAD:<file>`, plus the exit codes the tool reports;
   - Python heredocs with `{"…` content → the Edit tool.

### Evidence
Logs in `roadmap/evidence/E3-T08/`:
- `01-clean-verify.log`: final run, exit 0.
- `01-clean-verify-attempt1.log`: `ArchitectureTest` failed (`SignupPolicy.chip` called `platform.domain.CountryProfile.code()`) and `E3ResponseContractTest` failed (fixture without `version`/`warnDays`). Fix: use `CountryContactRules.code()` and update the fixture.
- `01-clean-verify-attempt2.log`: `E3ContractIT` failed (the exact `SignupWarning` enum and property lists). Fix: update the assertions.
- `01-clean-verify-attempt3-green-before-smoke-fix.log`: exit 0, but superseded by the step-5 fallback below.
- `02-baseline-before-fixes.log`: the new tests on the `HEAD` production code.
- `03-openapi-snapshot.log`.
- `04-test-summary.log`.
- `05a-e3-smoke-attempt1-family-fare.log`: `FAIL: The family fare was not proposed`. Fix: `proposedFamilyFare` plus the 3-dog case in `T_04_16`.
- `05-e3-smoke-first.log`, `06-e3-smoke-second.log`.
- `dev-*.log`: development runs of single test classes, not verification runs.

**1. `./mvnw -q clean verify`: exit 0** (full output `01-clean-verify.log`, 5 621 lines). Last 40 lines:
```
2026-09-24T17:47:15.837+02:00  INFO 47174 --- [agilityhub-core-api] [           main] .s.d.r.c.RepositoryConfigurationDelegate : Finished Spring Data repository scanning in 37 ms. Found 0 MongoDB repository interfaces.
2026-09-24T17:47:15.945+02:00  INFO 47174 --- [agilityhub-core-api] [           main] org.mongodb.driver.client                : MongoClient with metadata {"driver": {"name": "mongo-java-driver|sync|spring-boot", "versio…[cut at 220 chars]
2026-09-24T17:47:16.053+02:00  INFO 47174 --- [agilityhub-core-api] [localhost:63087] org.mongodb.driver.cluster               : Monitor thread successfully connected to server with description ServerDescription{address=…[cut]
2026-09-24T17:47:16.852+02:00  INFO 47174 --- [agilityhub-core-api] [           main] c.a.core.migration.PlayoffMigrationIT    : Started PlayoffMigrationIT in 1.239 seconds (process running for 269.336)
2026-09-24T17:47:16.894+02:00  INFO 47174 --- [agilityhub-core-api] [           main] o.s.b.w.e.tomcat.GracefulShutdown        : Commencing graceful shutdown. Waiting for active requests to complete
2026-09-24T17:47:16.895+02:00  INFO 47174 --- [agilityhub-core-api] [tomcat-shutdown] o.s.b.w.e.tomcat.GracefulShutdown        : Graceful shutdown complete
2026-09-24T17:47:16.895+02:00  INFO 47174 --- [agilityhub-core-api] [           main] o.s.b.w.e.tomcat.GracefulShutdown        : Commencing graceful shutdown. Waiting for active requests to complete
2026-09-24T17:47:16.896+02:00  INFO 47174 --- [agilityhub-core-api] [tomcat-shutdown] o.s.b.w.e.tomcat.GracefulShutdown        : Graceful shutdown complete
2026-09-24T17:47:16.897+02:00  INFO 47174 --- [agilityhub-core-api] [           main] o.apache.catalina.core.StandardService   : Stopping service [Tomcat]
E5-T07 outbox backlog discarded before PlayoffMigrationIT: 158 PENDING records (more than one dispatch() batch)
2026-09-24T17:47:23.631+02:00  INFO 47174 --- [agilityhub-core-api] [localhost:63493] org.mongodb.driver.cluster               : Exception in monitor thread while connecting to server localhost:63493

com.mongodb.MongoSocketOpenException: Exception opening socket
	at com.mongodb.internal.connection.SocketStream.lambda$open$0(SocketStream.java:85) ~[mongodb-driver-core-5.5.2.jar:na]
	at java.base/java.util.Optional.orElseThrow(Optional.java:403) ~[na:na]
	at com.mongodb.internal.connection.SocketStream.open(SocketStream.java:85) ~[mongodb-driver-core-5.5.2.jar:na]
	at com.mongodb.internal.connection.InternalStreamConnection.open(InternalStreamConnection.java:233) ~[mongodb-driver-core-5.5.2.jar:na]
	at com.mongodb.internal.connection.DefaultServerMonitor$ServerMonitor.lookupServerDescription(DefaultServerMonitor.java:219) ~[mongodb-driver-core-5.5.2.jar:na]
	at com.mongodb.internal.connection.DefaultServerMonitor$ServerMonitor.run(DefaultServerMonitor.java:176) ~[mongodb-driver-core-5.5.2.jar:na]
Caused by: java.net.ConnectException: Connection refused
	at java.base/sun.nio.ch.Net.pollConnect(Native Method) ~[na:na]
	at java.base/sun.nio.ch.Net.pollConnectNow(Net.java:694) ~[na:na]
	at java.base/sun.nio.ch.NioSocketImpl.timedFinishConnect(NioSocketImpl.java:542) ~[na:na]
	at java.base/sun.nio.ch.NioSocketImpl.connect(NioSocketImpl.java:592) ~[na:na]
	at java.base/java.net.SocksSocketImpl.connect(SocksSocketImpl.java:327) ~[na:na]
	at java.base/java.net.Socket.connect(Socket.java:751) ~[na:na]
	at com.mongodb.internal.connection.SocketStreamHelper.initialize(SocketStreamHelper.java:76) ~[mongodb-driver-core-5.5.2.jar:na]
	at com.mongodb.internal.connection.SocketStream.initializeSocket(SocketStream.java:104) ~[mongodb-driver-core-5.5.2.jar:na]
	at com.mongodb.internal.connection.SocketStream.open(SocketStream.java:79) ~[mongodb-driver-core-5.5.2.jar:na]
	... 3 common frames omitted

E5-T07 outbox backlog discarded before PlayoffMigrationIT: 162 PENDING records (more than one dispatch() batch)
E5-T07 outbox backlog discarded before PlayoffMigrationIT: 158 PENDING records (more than one dispatch() batch)
E5-T07 outbox backlog discarded before PlayoffMigrationIT: 158 PENDING records (more than one dispatch() batch)
E5-T07 outbox backlog discarded before PlayoffMigrationIT: 160 PENDING records (more than one dispatch() batch)
E5-T07 outbox backlog discarded before PlayoffMigrationIT: 160 PENDING records (more than one dispatch() batch)
E5-T07 outbox backlog discarded before PlayoffMigrationIT: 158 PENDING records (more than one dispatch() batch)
E5-T07 outbox backlog discarded before PlayoffMigrationIT: 158 PENDING records (more than one dispatch() batch)
E5-T07 outbox backlog discarded before PlayoffMigrationIT: 158 PENDING records (more than one dispatch() batch)
E5-T07 outbox backlog discarded before PlayoffMigrationIT: 158 PENDING records (more than one dispatch() batch)
```
(With `-q` a green run prints no summary. The Mongo socket trace is the monitor of an already stopped test context. Three long lines are cut at 220 characters.)

Test summary (`04-test-summary.log`, read from `target/surefire-reports` and `target/failsafe-reports` after the final run):
```
surefire: tests=521 failures=0 errors=0 skipped=0 (115 classes)
  DashboardBuildersTest: tests=6 failures=0 errors=0 skipped=0
  DogChipsTest: tests=3 failures=0 errors=0 skipped=0
  E3ResponseContractTest: tests=4 failures=0 errors=0 skipped=0
failsafe: tests=848 failures=0 errors=0 skipped=0 (77 classes)
  SignupGateFixesIT: tests=18 failures=0 errors=0 skipped=0
  SignupIT: tests=31 failures=0 errors=0 skipped=0
  DashboardIT: tests=9 failures=0 errors=0 skipped=0
  E3ContractIT: tests=17 failures=0 errors=0 skipped=0
```

**2. `bin/e3-smoke`, first run: exit 0** (`05-e3-smoke-first.log`, 55 lines). First 15 lines, which contain the new step-1/E35/M6 checks:
```
Starting disposable development build Compose stack
Building the current working tree image
PASS working tree image built
PASS club:apply seeds/club-canic.yaml run 1
PASS club:apply seeds/club-canic.yaml run 2: 0 changes
PASS seed:demo --club=canic --seed=42 run 1
PASS seed:demo --club=canic --seed=42 run 2: 0 changes
curl GET /api/v1/health -> 200
PASS private local mailbox and attachment volumes ready; fixture club activated
curl GET /api/v1/signup -> 200
PASS GET /signup: texts resolved, one quote per offered plan
curl POST /oauth2/token -> 200
PASS D1 dashboard + counters: pending=3, olderThanWarn=1, activeMembers=184
PASS D1 dogs by level: 8 progression columns, others=0
PASS D1 class occupancy follows R-14-03 for the seeded week: 17 classes, booked=0, capacity=75, percent=0.0
```
The last 40 lines have the same shape as the second run below (other generated ids); they end with `PASS T-04-34 / T-14-11: E3 backend gate completed; credentials and capabilities withheld` and `Removed disposable Compose services, volumes and private mailbox; existing stacks preserved`.

**3. `bin/e3-smoke`, second run: exit 0** (`06-e3-smoke-second.log`, 55 lines). Last 40 lines:
```
curl GET /api/v1/members/db1fce2e-8366-324b-b252-f0cc1963adad/signup -> 200
curl GET /api/v1/members/1ea02916-0954-39c2-ac83-e18f7139be10/signup -> 200
curl GET /api/v1/members/5dbcd99a-8d85-3ea3-b0aa-eb1e5a33c74b/signup -> 200
PASS D1 demo: 184 ACTIVE members, 242 ACTIVE dogs, three reviewable pending public signups
curl POST /api/v1/signup/family-group-lookups -> 200
curl POST /api/v1/signup/identity-checks -> 200
curl POST /api/v1/signup/upload-urls -> 200
curl PUT /api/v1/signup/uploads -> 204
curl POST /api/v1/signup -> 201
curl POST /api/v1/signup -> 201
PASS D1 dashboard + counters: pending=4, olderThanWarn=1, activeMembers=184
curl GET /api/v1/members/af32f3fc-c4b2-407f-bb92-224b2a23fa31/signup -> 200
PASS mailbox N-01 correlated by notificationId; locale=ca
curl POST /api/v1/members/af32f3fc-c4b2-407f-bb92-224b2a23fa31/validation -> 200
curl POST /api/v1/members/af32f3fc-c4b2-407f-bb92-224b2a23fa31/validation -> 200
PASS D1 dashboard + counters: pending=3, olderThanWarn=1, activeMembers=185
PASS mailbox N-02 correlated by notificationId; locale=ca; token=[truncated]
curl POST /oauth2/token -> 200
curl GET /api/v1/me -> 200
PASS public family signup -> dry run -> validation/number increment -> N-02 -> welcome session
curl POST /api/v1/me/dogs/signup -> 201
PASS N-01 in the member APP inbox, scoped to account and entity
PASS D1 dashboard + counters: pending=4, olderThanWarn=1, activeMembers=185
curl GET /api/v1/members/af32f3fc-c4b2-407f-bb92-224b2a23fa31/signup -> 200
curl POST /api/v1/members/af32f3fc-c4b2-407f-bb92-224b2a23fa31/validation -> 200
PASS N-37 in the member APP inbox, scoped to account and entity
PASS D1 dashboard + counters: pending=3, olderThanWarn=1, activeMembers=185
curl POST /api/v1/signup -> 201
PASS D1 dashboard + counters: pending=4, olderThanWarn=1, activeMembers=185
curl GET /api/v1/members/dd27ea39-3052-46eb-a1c0-03b48656cfe9/signup -> 200
curl POST /api/v1/members/dd27ea39-3052-46eb-a1c0-03b48656cfe9/rejection -> 200
PASS mailbox N-03 correlated by notificationId; locale=ca
PASS D1 dashboard + counters: pending=3, olderThanWarn=1, activeMembers=185
PASS signup without family -> rejection -> N-03; D1 counters restored
curl GET /api/v1/parameters/signup.enabled -> 200
curl PUT /api/v1/parameters/signup.enabled -> 200
curl POST /api/v1/signup -> 422 SIGNUP_CLOSED
PASS signup.enabled=false closes the form; canonical catalog status is 422 SIGNUP_CLOSED
PASS T-04-34 / T-14-11: E3 backend gate completed; credentials and capabilities withheld
Removed disposable Compose services, volumes and private mailbox; existing stacks preserved
```
(For N-01, the «scoped to account and entity» line is the helper's fixed text. Since this task, that check requires the member's row to have *no* `action`; see `bin/e3-smoke` `app_notification`.)

**4. `bin/openapi-snapshot`: exit 0** (`03-openapi-snapshot.log`, Spring test output). Last line: `Updated docs/openapi/openapi.json`. Then `git diff --stat docs/openapi/`: exit 0.
```
 docs/openapi/CHANGELOG.md |  23 ++++++
 docs/openapi/openapi.json | 175 +++++++++++++++++++++++++++++++++++++++++++---
 2 files changed, 187 insertions(+), 11 deletions(-)
```

### OpenAPI changelog entry
It is at the top of `docs/openapi/CHANGELOG.md` («2026-09-24 · E3-T08 · gate E3 fixes (api 1/3) …»). 0 operations added, 3 changed.
- `GET /signup`: `upfront.planQuotes[]`; `additionalDogOptions` (TODAY always); the flags `allowFamilyGroupPending` and `requireDogDocumentAtSignup`; resolved `texts`, with `familyGroupIntro` nullable; the MANUAL `instructions`.
- `GET /members/{id}/signup`: `planOptions[]`, `warnDays`, `dogs[].version`, and `409 NOT_PENDING`.
- `POST /members/{id}/validation`: the `409` reasons `NOT_PENDING` and `CHECKOUT_PENDING`, and `SignupWarning.CHECKOUT_PENDING`.
- `GET /me/dogs` (`MeDog`): `status`. **Not additive:** `documents` and `licenses` are no longer required.

## Organizer verification
_(pending)_
