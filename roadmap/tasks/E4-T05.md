---
id: E4-T05
title: E4 integration (back): demo seed with templates A/B + Saturdays, validated week, ring block, inconsistency and the 4 D7 activities; bin/e4-smoke on the local stack (generate → validate → cancel with registrants → outbox → activity blocks a ring → public API); README/DEPLOY; gate E4 evidence
stage: E4
repo: agilityhub-core-api
thread: A
order: 50
status: verified
depends_on: [E4-T02, E4-T03, E4-T04]
branch:
pr:
updated: 2026-09-24
---

# E4-T05 · E4 integration (back): demo seed with templates A/B + Saturdays, validated week, ring block, inconsistency and the 4 D7 activities; bin/e4-smoke on the local stack (generate → validate → cancel with registrants → outbox → activity blocks a ring → public API); README/DEPLOY; gate E4 evidence

## Objective
Work packages S06 P7 and S07 P6 (back halves): the demo seed of E2-T11 grows with the planning and activities data every front task and screenshot needs, and the gate E4 scenario runs unattended against the local stack with one command, so the web integration tasks (E4-W…) reuse the same seed and the organizer can tick the gate from evidence.

## Spec references
S06 §12 P7 («`demo-seed` amb plantilles A/B + dissabtes, setmana validada amb inscrits, bloqueig i incoherència») and T-06-28 (E2E, back half: the states the D4/D4b/D4c scenario needs) · S07 §12 P6 («`demo-seed` amb les 4 activitats del mockup (una amb conflicte de classe), inscrits i espera») and T-07-32 (E2E, back half) · S06 R-06-01 (band example), R-06-03 (description examples), R-06-05 (inconsistency examples), R-06-07, R-06-08, R-06-10 (cancellation example: 4 registrants, 2 with pack, 2 waiting), R-06-11 (block example: Carretera Wednesday 16:00–18:00 «manteniment») · S07 R-07-05 (publication example with a class conflict on Central and a training booking), R-07-08 · `docs/pantalles/escriptori/D3-*.html`, `D4-*.html`, `D4b-*.html`, `D4c-*.html`, `D7-*.html`, `docs/pantalles/mobil/10-*.html`, `23-*.html` (data shapes) · backlog gate E4: «l'admin genera i valida una setmana des de les plantilles (A/B + dissabtes); alumne i instructor veuen 10/23; D4c anul·la una classe amb inscrits ficticis (transacció + esdeveniments a l'outbox); una activitat publicada bloqueja la pista i apareix a 04» · E2-T11 (`seed:demo`, seed 42), E3-T05 (`bin/e3-smoke` pattern), E1-T10 (`bin/e1-smoke`, local mailbox), E1-T14 (`docker-compose.consumer.yml`, image).

## Context to load
`seeds/demo-canic.yaml`, `seeds/club-canic.yaml`, `seeds/README.md` · `src/main/java/com/agilityhub/core/clubs/census/application/DemoSeedCommand.java`, `DemoSeedService.java` · `bin/e1-smoke`, `bin/e3-smoke` (if merged), `bin/core`, `compose.yaml`, `docker-compose.consumer.yml` · the application services of E4-T02/E4-T03/E4-T04 (`WeekTemplate*`, `WeekGenerationUseCase`, `WeekValidationUseCase`, `ClassCancellationUseCase`, `RingBlockService`, `ActivityService`, `ActivityLifecycleService`, `ActivityRegistrationService`) and the ports `ClassBookingsPort`, `TrainingConflictPort` of E4-T03 · `docs/DEPLOY.md`, `README.md`, `roadmap/ROADMAP.md` (read-only: gate E4 checklist) · the mockups above · this file.

## Steps
1. **Seed data through the application services** (never raw inserts, so every invariant, derived instant, block and outbox event is real): extend `seed:demo` (`seeds/demo-canic.yaml` + `DemoSeedService`, deterministic with `--seed=42`, idempotent: a second run reports 0 changes) with a new option `--week-start=YYYY-MM-DD` (default: the Monday of the club-local current week — planning data must be relative to the run date, unlike the census `referenceDate`): (a) templates «Setmana A» and «Setmana B» (`WEEKDAYS`) and «Dissabtes» (`SATURDAY`) with the R-06-01 bands 08:30–09:30 · 09:30–10:30 · 16:30–17:30 · 17:40–18:40 · 18:50–19:50 · 20:00–21:00 (Saturdays: 08:30, 09:30, 16:30, 17:40, 18:30 slot for the Torneig conflict) and the D3 mockup classes (descriptions «A», «A+B», «B+C», «C+D+E», «F+G», «D i sup.», «Cadells», one manual «Obed. urbana» at Wednesday 20:00; rings by colour as in the mockup; the three demo instructors of E2-T11 rotated — never the mockup's first names, never real staff); «Setmana B» carries the D3 inconsistency (Wednesday 20:00, two classes on Central → `RING_DOUBLE_BOOKED`, `canGenerate = false`); «Setmana A» and «Dissabtes» are clean; (b) the current week generated from A + Dissabtes and **validated** (`VALIDATED`, classes `ACTIVE`, the SETMANES table of D3 shows it generated/validated) plus one loose class added after validation that duplicates an instructor on Thursday 18:50 → the D4 week warning «… assignat a dues pistes alhora» (`INSTRUCTOR_DOUBLE_BOOKED`, a warning on a validated week); (c) the next week generated as a **draft** (`GENERATED`, D4b «Esborrany» filter, validation card with `draftCount`); (d) one `RingBlock` `BLOCK`/`MAINTENANCE` on Carretera, Wednesday of the current week 16:00–18:00, note «manteniment», created by an admin (D4 «Carretera bloquejada»); (e) demo registrants for the cancellation flow — bookings are E5: add `DemoClassBookingsAdapter` (`ClassBookingsPort` implementation active only in the `local`/`test` profiles and `@ConditionalOnMissingBean` so E5's real adapter replaces it) backed by a `demo_class_bookings` collection the seed fills with fictional member + dog pairs from the census (the D4 mockup counts: `4/5`, `3/5`, `5/5 +1`, `2/5`, `1/1` on the «Teràpia» and «Particular» classes with manual descriptions and capacity 1, `0/5` on a class cancelled by `RISK_REVIEW` with the seed's `scheduling.autoCancel.text`) including the R-06-10 case (Wednesday 18:50 «B+C»: 4 registrants, 2 `paidWithPack`, 2 waiting) — `counters` on the `ClassSession` come from this adapter through the same code path S08 will use, never patched by hand; document that E5 replaces the collection with real `bookings`; (f) the 4 D7 activities dated relative to `--week-start`: «Torneig d'Estiu 2026» (`COMPETITION`, Saturday of week +1, 18:30–20:30, all five rings, 40 places, registration open until the day before, 22 `ACTIVE` registrants, `waitlistEnabled = true`) **published with the class conflict** of R-07-05 resolved by the real lifecycle call (`cancelClasses: true` + admin text → the Saturday 18:30 class on Central becomes `CANCELLED{ACTIVITY}` with its N-08a rows and the 5 `ACTIVITY` blocks exist); «Seminari de handling» (`SEMINAR`, Saturday of week +5, 9:00–13:00, Central, registration open, **full: 12 places, 12 `ACTIVE` + 2 `WAITLISTED`** — the mockup shows «6/12»; the waitlist needs a full activity, state the deviation in `seeds/README.md`); «Lliga social — 3a jornada» (`SOCIAL_LEAGUE`, Saturday of week +6, 9:00, all rings, no maximum → «obertes», registration open, 10 registrants, image + one document uploaded through the signed-upload path with tiny fictional files); «Demostració Festa Major» (`DEMONSTRATION`, Sunday of week +8, no hours, `atClub = false`, `location.name` fictional, `DRAFT`). Registrants are eligible `ACTIVE` demo members (levels respected), registered through `ActivityRegistrationService` with `origin = APP`.
2. **`bin/e4-smoke [--image]`** (Python, pattern of `bin/e1-smoke`/`bin/e3-smoke`): starts the stack (`compose.yaml`, or reuses `--image` with `docker-compose.consumer.yml`), applies `seeds/club-canic.yaml` and `seed:demo --seed=42`, logs in with the fictional seed accounts (`SEED_PASSWORD`, never printed), then asserts with `curl`-equivalent calls, exact statuses and codes: `GET /week-templates` (3 templates; «Setmana B» `inconsistencyCount = 1`) → `POST /weeks/{B-week}/generation` with «Setmana B» → `422 TEMPLATE_INCONSISTENT` → `GET /weeks/generation-candidates` (`proposed` = first week after the seeded draft) → `POST /weeks/{id}/generation {weekdayTemplateId: A, saturdayTemplateId: Dissabtes}` → 200 `classCount`, `skipped` → `GET /weeks/{id}/calendar?filter=DRAFT` (`draftCount > 0`, `canValidate = true`) → member `GET /day-grid?date=<Monday>&view=member` shows **no** class of that week → `POST /weeks/{id}/validation` → 200 `validatedClassIds` → member grid shows the classes without `occupancy`/`instructorName` beyond `bookings.showInstructorHoursBefore`, instructor grid shows `occupancy` → `POST /class-sessions/{Wed 18:50 B+C}/cancellation {reason: CLUB_MANUAL}` without text → `422 ADMIN_TEXT_REQUIRED`; with `adminText` → 200 `CANCELLED`, `affectedBookings = 4`, `affectedWaitlist = 2` → outbox `ClassCancelledByClub` (4 `affected`, 2 `waitlistIds`) and `notifications` rows APP + EMAIL (captured in the local mailbox log) + SMS `QUEUED` for the 6 recipients (Mongo assertions through `docker compose exec mongo mongosh --quiet --eval`, ids truncated) → `POST /ring-blocks` over an active class → `409 RING_BLOCK_CONFLICT`; on a free slot → 201 and visible as `OCCUPIED` in the member grid, `BLOCK` in the instructor grid → admin `POST /activities {title, type}` → `PATCH` with the next Saturday 18:30–20:30 on Central → `GET …/ring-conflicts` lists the class → `POST …/publication` → `409 RING_BLOCK_CONFLICT`; with `{cancelClasses: true, adminText}` → 200 `PUBLISHED`, `ringBlockIds` of size 1, the class `CANCELLED{ACTIVITY}` → `GET /weeks/{id}/calendar` shows the block with `activityTitle` → member `GET /me/activities` lists it (`rowState = OPEN`) → `POST /activity-registrations` → 201 `ACTIVE` → `GET /public/canic/activities` without key → 403 `INVALID_API_KEY`; with the seeded key (masked in output) → the activity with `places.free` and no `member*`/name/phone key anywhere in the JSON → `POST …/cancellation` of the activity with text → 200, registration `CANCELLED{ACTIVITY_CANCELLED}`, N-32c rows → `bin/core activities:finish-ended` and `scheduling:finish-ended` exit 0 → prints a summary table (step · status · key values) and exits non-zero on the first failed assertion; the second run of the script is idempotent (seed 0 changes; the script creates its own activity/blocks with a run suffix and cancels them at the end).
3. **Docs**: `seeds/README.md` (planning/activities data, `--week-start`, the demo bookings adapter and its E5 replacement, the Seminari deviation), `README.md` (E4 commands: `seed:demo` options, `scheduling:finish-ended`, `activities:finish-ended`, `bin/e4-smoke`), `docs/DEPLOY.md` (nothing new expected in environment variables — say so; the gate E4 checklist section with the smoke as its evidence, organizer-run steps for the two front checks «alumne i instructor veuen 10/23» and «apareix a 04»).
4. **Gate E4 (back) evidence**: `roadmap/ROADMAP.md` is organizer-owned — do not edit it; paste in your report, per gate line, the smoke output lines that prove it (week generated from A/B + Saturdays and validated; class with fictional registrants cancelled in one transaction with `ClassCancelledByClub` in the outbox and N-08a rows; published activity blocking a ring and listed for the member). E2E T-06-28 and T-07-32 (web) rely on this seed: the «Esborrany» filter opens the seeded draft week; the D4c modal reads «ANUL·LA I AVISA ELS 4 ALUMNES» on the Wednesday 18:50 class; a member account of the seed can register to «Lliga social — 3a jornada»; the front selects rows by state, never by the mockup's first names.
5. **Regression**: `./mvnw -q verify` stays green (add the seed tests: the generator output is deterministic for seed 42, the second application changes nothing, every seeded document belongs to the Cànic tenant, no real name from `staff.canic.yaml` or the mockups appears in seed sources).

## Fixed conventions
- Fictional data only: names from the E2-T11 generator, `@example.test` accounts, tiny generated image/PDF fixtures («Fictional demo document») — never the club's real staff, never the mockup first names as real people; no secrets in output (`SEED_PASSWORD`, public API key, tokens truncated).
- The seed uses the E4 application services and the shared `Clock`; it never writes `class_sessions`, `ring_blocks`, `activities` or `activity_registrations` directly; `counters` are produced by the demo bookings adapter, not set by hand.
- If Docker is not available in the session, block per the protocol (the organizer verifies the smoke in the executor evidence + CI); the smoke never prints tokens or keys.
- Closed catalogs: nothing new; any doubt on seed values (dates, counts) is an assumption in the report. Evidence rule: report = command + exit code + last 40 lines; long outputs → `roadmap/evidence/E4-T05/NN-<name>.log`; task file ≤ 120 KB; never edit other tasks, `ROADMAP.md` or the Organizer verification section.

## Verification (run all; evidence = command + exit code + last 40 lines; long outputs → `roadmap/evidence/<ID>/*.log`)
- `bin/core club:apply seeds/club-canic.yaml` then `bin/core seed:demo --club=canic --seed=42` twice → second run 0 changes; `bin/e4-smoke` exit 0 **twice** (full tail of the second run in the report); `./mvnw -q verify` green (seed tests included); `git diff --check` clean.

## Definition of Done
Gate E4 (back) is reproducible with one command on the local stack; the demo seed holds every state D3/D4/D4b/D4c/D7/10/23/04 need; web E4 integration tasks can run T-06-28 and T-07-32 against the same seed.

## Evidence to return
Seed tails (both runs), smoke tails (both runs), `verify` tail, README/DEPLOY diffs, the gate E4 lines with their proving output.

---

## Executor report

### Session history
- **2026-09-19 (blocked)**: the default current-week timeline and the tournament's ACTIVE-class cancellation clashed with the
  past-date guards and the DRAFT→DELETED rule (details in MESSAGES 2026-09-19). The organizer answered the same day: W+1 GENERATED
  (draft), W+2 generated **and** validated (R-06-10 class, maintenance block, tournament conflict), W+3 proposed by the candidates.
- **2026-09-23, session 1**: implemented the seed, the demo bookings adapter, `bin/e4-smoke` and the docs; smoke passed once
  (`04-e4-smoke-run1.log`); the session ended as `in_progress` without a report.
- **2026-09-23, session 2 (this report)**: fixed the remaining coverage gate, re-ran every Verification command, wrote the report.

### Branch / commits
Working tree on `main`; no git write command. Dependencies: E4-T02 and E4-T03 `verified`, E4-T04 `awaiting_verification`
(round 2 published in `a578515`; if it gets `changes_requested`, this seed calls only its public services and should need no change).

### Final arrangement (for E4-W05; also in `seeds/README.md` → «E4 planning and activities»)
W = the club-local week of the run (`--week-start` overrides it, tests only). W: no planning data. **W+1** `GENERATED`, 38 DRAFT
classes (A + Dissabtes), D4b «Esborrany». **W+2** `VALIDATED`: D4 counters (`4/5`, `3/4`, `5/5 +1`, `2/5`, «Teràpia»/«Particular» `1/1`),
**Wednesday 18:50 Central B+C = 4 registrants (2 `paidWithPack`) + 2 waiting** (the D4c case, ACTIVE because only ACTIVE classes
hold bookings), Wednesday 09:30 Cadells `CANCELLED{RISK_REVIEW}`, Carretera `BLOCK/MAINTENANCE` Wednesday 16:00–18:00 «manteniment»,
the loose Thursday 18:50 class → `INSTRUCTOR_DOUBLE_BOOKED`, and the Saturday 18:30 Central class `CANCELLED{ACTIVITY}` by
«Torneig d'Estiu 2026» (5 ACTIVITY blocks). **W+3** not generated → `generation-candidates` `proposed = true`. Activities: Torneig
(W+2 Sat, 22 ACTIVE), Seminari (W+5 Sat, full 12 + 2 waitlisted), Lliga social (W+6 Sat, no maximum, 10 registrants, image +
PDF), Demostració (W+8 Sun, `atClub = false`, DRAFT). The 15 members linked to seed login accounts get no registrations.

### Evidence (full command outputs)

Earlier failing attempts (one line each, full logs kept):
- `bin/e4-smoke` attempt 1 → exit 1, `KeyError 'instructorName'` (a null-omitted key read with `[]`) → read it with `.get()`. `01-e4-smoke-attempt1.log`
- `bin/e4-smoke` attempt 2 → exit 1, the privacy check rejected the contract's document/location `name` keys → it now checks person keys and the registrant's real name/phone values. `02-e4-smoke-attempt2.log`
- `bin/e4-smoke` attempt 3 → exit 1, `KeyError 'cancelReason'` → the script now reads the contract's `cancellation.reason`. `03-e4-smoke-attempt3.log` (a summary written in session 1 + the raw tail); first green run `04-e4-smoke-run1.log` (session 1, before the verify fixes).
- `./mvnw -q verify` attempt 1 → exit 1: `ArchitectureTest` (census called `platform.domain`) + `DemoDatasetTest` (`planning` field unknown) → `CountryContacts` from `platform.application`; `DemoFixtures` strips the E4 sections. `05-verify-attempt1.log`
- `./mvnw -q verify` attempt 2 (this session) → exit 1 at `jacoco:check`; all tests passed, but `clubs.bookings.application` branch coverage was 0.719 (< 0.80) → added `DemoBookingsTest` (3 unit tests: adapter FIFO/duplicate dog/waitlist order/cancel-all, seeder guards). `06-verify.log`

**1. `bin/core club:apply seeds/club-canic.yaml` then `bin/core seed:demo --club=canic --seed=42` twice.** Run by
`python3 roadmap/evidence/E4-T05/cli-seed.py` (exit 0). It starts a disposable `mongo:7` replica set on a random loopback port
(local profile, database `e4_cli`, generated `SEED_PASSWORD`/`OIDC_MASTER_KEY` never printed), runs the three `bin/core` commands on
the jar built by verify, compares the full Mongo document snapshot before and after the second seed run, then removes the container.
Transcript: `11-cli-seed-run.log`; per-command logs: `08-club-apply.log`, `09-seed-demo-first.log`, `10-seed-demo-second.log`.

`bin/core club:apply seeds/club-canic.yaml`, exit 0 (last lines; 62-line log):
```text
+ catalogs: 31 catalog changes
+ messageTemplates: schema accepted; application deferred to E7
+ pages: 3 page changes
15 changes (applied)
```
`bin/core seed:demo --club=canic --seed=42` (first), exit 0, complete output:
```text
{activeMembers=184, pendingMembers=3, inactiveMembers=4, leftMembers=3, members=194, dogs=245, familyGroups=12, activeDogs=242, pendingDogs=3, instructors=3, administrators=2, receivedDocuments=6, pendingDocuments=239}
451 changes (demo seed)
{templateClasses=39, templates=3, weeks=2, generatedClasses=76, validatedWeeks=1, looseClasses=1, ringBlocks=1, riskCancellations=1, classBookings=23, classWaitlist=3, activities=4, publishedActivities=3, activityRegistrations=44, activityWaitlist=2, activityFiles=2}
205 changes (demo planning, week start 2026-09-21)
```
`bin/core seed:demo --club=canic --seed=42` (second), exit 0, complete output, followed by the helper's verdict:
```text
{activeMembers=184, pendingMembers=3, inactiveMembers=4, leftMembers=3, members=194, dogs=245, familyGroups=12, activeDogs=242, pendingDogs=3, instructors=3, administrators=2, receivedDocuments=6, pendingDocuments=239}
0 changes (demo seed)
{templateClasses=39, templates=3, weeks=2, generatedClasses=76, validatedWeeks=1, looseClasses=1, ringBlocks=1, riskCancellations=1, classBookings=23, classWaitlist=3, activities=4, publishedActivities=3, activityRegistrations=44, activityWaitlist=2, activityFiles=2}
0 changes (demo planning, week start 2026-09-21)
PASS second seed:demo run: 0 changes and complete Mongo document snapshot unchanged
Removed the disposable Mongo container and runtime directory
```
(The counts line lists the planning totals; the change count is on the next line. Club-local run date: Wednesday 2026-09-23 → W = 2026-09-21.)

**2. `bin/e4-smoke` run 1**: exit 0, full output `12-e4-smoke-run1.log` (same summary as run 2; outbox event `d5d18a4c…`).

**3. `bin/e4-smoke` run 2**: exit 0, full output `13-e4-smoke-run2.log` (110 lines). Last 40 lines:
```text
curl GET /api/v1/public/canic/activities -> 403 INVALID_API_KEY
curl GET /api/v1/public/canic/activities -> 200
PASS GET /public/canic/activities: 403 without key; 200 with key (masked) · places.free=9; no member*/phone/person keys; registrant name and phone absent
curl POST /api/v1/activities/<id>/cancellation -> 200
curl GET /api/v1/activity-registrations/<id> -> 200
PASS POST /activities/{id}/cancellation: 200 · registration CANCELLED{ACTIVITY_CANCELLED}; N-32c rows
curl POST /api/v1/ring-blocks/<id>/cancellation -> 200
PASS POST /ring-blocks/{smoke}/cancellation: 200 · smoke block released
PASS bin/core activities:finish-ended --club=canic: exit 0
PASS bin/core scheduling:finish-ended --club=canic: exit 0

SUMMARY (step · status · key values)
  seed club:apply + seed:demo --seed=42 (x2)                     · second runs 0 changes · week start 2026-09-21
  GET /week-templates                                            · 200 · 3 templates; Setmana B inconsistencyCount=1
  POST /weeks/{week+3}/generation (Setmana B)                    · 422 TEMPLATE_INCONSISTENT
  GET /weeks/generation-candidates                               · 200 · proposed=2026-10-12 (week +3)
  POST /weeks/{week+3}/generation (A + Dissabtes)                · 200 · classCount=38, skipped=[]
  GET /weeks/{week+3}/calendar?filter=DRAFT                      · 200 · draftCount=38, canValidate=true
  member GET /day-grid (draft week)                              · 200 · 0 classes
  POST /weeks/{week+3}/validation                                · 200 · validatedClassIds=38
  day-grid member / instructor                                   · 200 · member 7 classes without occupancy/instructor; instructor sees occupancy
  POST /class-sessions/{Wed 18:50 B+C}/cancellation without text · 422 ADMIN_TEXT_REQUIRED
  POST /class-sessions/{Wed 18:50 B+C}/cancellation              · 200 CANCELLED · affectedBookings=4, affectedWaitlist=2
  outbox ClassCancelledByClub                                    · committed · event 249dcb47…: affected=4, waitlistIds=2
  notifications N-08a                                            · 6 APP + 6 EMAIL (mailbox) + 6 SMS QUEUED · 4 bookings + 2 waitlist
  POST /ring-blocks over an ACTIVE class                         · 409 RING_BLOCK_CONFLICT
  POST /ring-blocks on a free slot                               · 201 · member grid OCCUPIED, instructor grid BLOCK
  GET /activities/{id}/ring-conflicts                            · 200 · 1 CLASS conflict (Sat 18:30 Central)
  POST /activities/{id}/publication                              · 409 RING_BLOCK_CONFLICT
  POST /activities/{id}/publication {cancelClasses}              · 200 PUBLISHED · ringBlockIds=1; class CANCELLED{ACTIVITY}
  GET /weeks/{week+3}/calendar                                   · 200 · activity block with activityTitle
  member GET /me/activities                                      · 200 · rowState=OPEN
  member POST /activity-registrations                            · 201 ACTIVE
  GET /public/canic/activities                                   · 403 without key; 200 with key (masked) · places.free=9; no member*/phone/person keys; registrant name and phone absent
  POST /activities/{id}/cancellation                             · 200 · registration CANCELLED{ACTIVITY_CANCELLED}; N-32c rows
  POST /ring-blocks/{smoke}/cancellation                         · 200 · smoke block released
  bin/core activities:finish-ended --club=canic                  · exit 0
  bin/core scheduling:finish-ended --club=canic                  · exit 0
PASS E4 backend gate: week generated from A/B + Saturdays and validated; class with fictional registrants cancelled with ClassCancelledByClub + N-08a; published activity blocks a ring and is listed for the member
Removed disposable Compose services, volumes and private mailbox; existing stacks preserved
```
Each smoke run uses a new disposable Compose project and database. Its own idempotency check is inside the run (`club:apply` and
`seed:demo` applied twice, second runs 0 changes). Its activity and blocks carry a run suffix and are cancelled at the end.

**4. `./mvnw -q verify`**: exit 0, full output `07-verify.log` (4127 lines of Spring/test logging). With `-q`, Maven prints no summary.
Counts from the reports this run produced (`target/surefire-reports`, `target/failsafe-reports`) and coverage from `target/site/jacoco/jacoco.csv`:
```text
target/surefire-reports run=404 failures=0 errors=0 skipped=0
target/failsafe-reports run=585 failures=0 errors=0 skipped=0
clubs.bookings.application lines 1.000 branches 0.984
```
Last 40 lines of the log, each cut at 150 characters. They are Testcontainers shutdown noise from the last IT (a Mongo monitor thread
reconnecting to a stopped container), not a failure:
```text
2026-09-23T23:14:32.114+02:00  INFO 74969 --- [agilityhub-core-api] [           main] t.c.s.AnnotationConfigContextLoaderUtils : Could not detect defa
2026-09-23T23:14:32.123+02:00  INFO 74969 --- [agilityhub-core-api] [           main] .b.t.c.SpringBootTestContextBootstrapper : Found @SpringBootConf

  .   ____          _            __ _ _
 /\\ / ___'_ __ _ _(_)_ __  __ _ \ \ \ \
( ( )\___ | '_ | '_| | '_ \/ _` | \ \ \ \
 \\/  ___)| |_)| | | | | || (_| |  ) ) ) )
  '  |____| .__|_| |_|_| |_\__, | / / / /
 =========|_|==============|___/=/_/_/_/

 :: Spring Boot ::               (v3.5.16)

2026-09-23T23:14:32.136+02:00  INFO 74969 --- [agilityhub-core-api] [           main] c.a.core.migration.PlayoffMigrationIT    : Starting PlayoffMigra
2026-09-23T23:14:32.136+02:00  INFO 74969 --- [agilityhub-core-api] [           main] c.a.core.migration.PlayoffMigrationIT    : The following 1 profi
2026-09-23T23:14:32.258+02:00  INFO 74969 --- [agilityhub-core-api] [           main] .s.d.r.c.RepositoryConfigurationDelegate : Bootstrapping Spring
2026-09-23T23:14:32.272+02:00  INFO 74969 --- [agilityhub-core-api] [           main] .s.d.r.c.RepositoryConfigurationDelegate : Finished Spring Data
2026-09-23T23:14:32.300+02:00  INFO 74969 --- [agilityhub-core-api] [           main] org.mongodb.driver.client                : MongoClient with meta
2026-09-23T23:14:32.302+02:00  INFO 74969 --- [agilityhub-core-api] [localhost:59009] org.mongodb.driver.cluster               : Monitor thread succes
2026-09-23T23:14:32.679+02:00  INFO 74969 --- [agilityhub-core-api] [           main] c.a.core.migration.PlayoffMigrationIT    : Started PlayoffMigrat
2026-09-23T23:14:41.436+02:00  INFO 74969 --- [agilityhub-core-api] [localhost:59180] org.mongodb.driver.cluster               : Exception in monitor

com.mongodb.MongoSocketOpenException: Exception opening socket
	at com.mongodb.internal.connection.SocketStream.lambda$open$0(SocketStream.java:85) ~[mongodb-driver-core-5.5.2.jar:na]
	at java.base/java.util.Optional.orElseThrow(Optional.java:403) ~[na:na]
	at com.mongodb.internal.connection.SocketStream.open(SocketStream.java:85) ~[mongodb-driver-core-5.5.2.jar:na]
	at com.mongodb.internal.connection.InternalStreamConnection.open(InternalStreamConnection.java:233) ~[mongodb-driver-core-5.5.2.jar:na]
	at com.mongodb.internal.connection.DefaultServerMonitor$ServerMonitor.lookupServerDescription(DefaultServerMonitor.java:219) ~[mongodb-driver-core-5.
	at com.mongodb.internal.connection.DefaultServerMonitor$ServerMonitor.run(DefaultServerMonitor.java:176) ~[mongodb-driver-core-5.5.2.jar:na]
Caused by: java.net.ConnectException: Connection refused
	at java.base/sun.nio.ch.Net.pollConnect(Native Method) ~[na:na]
	at java.base/sun.nio.ch.Net.pollConnectNow(Net.java:694) ~[na:na]
	at java.base/sun.nio.ch.NioSocketImpl.timedFinishConnect(NioSocketImpl.java:542) ~[na:na]
	at java.base/sun.nio.ch.NioSocketImpl.connect(NioSocketImpl.java:592) ~[na:na]
	at java.base/java.net.SocksSocketImpl.connect(SocksSocketImpl.java:327) ~[na:na]
	at java.base/java.net.Socket.connect(Socket.java:751) ~[na:na]
	at com.mongodb.internal.connection.SocketStreamHelper.initialize(SocketStreamHelper.java:76) ~[mongodb-driver-core-5.5.2.jar:na]
	at com.mongodb.internal.connection.SocketStream.initializeSocket(SocketStream.java:104) ~[mongodb-driver-core-5.5.2.jar:na]
	at com.mongodb.internal.connection.SocketStream.open(SocketStream.java:79) ~[mongodb-driver-core-5.5.2.jar:na]
	... 3 common frames omitted
```

**5. `git diff --check`**: exit 0, no output (run after the last code change).

**Extra (not in Verification) `bin/openapi-snapshot`**: exit 0, `Updated docs/openapi/openapi.json`. `git status --short docs/openapi/`
shows nothing, so the snapshot is byte-identical and there is no API change. Log: `14-openapi-snapshot.log`.

### Gate E4 (back): gate lines and the smoke output that proves them (run 2)
- «l'admin genera i valida una setmana des de les plantilles (A/B + dissabtes)»: `POST /weeks/{week+3}/generation (Setmana B) · 422
  TEMPLATE_INCONSISTENT`; `POST /weeks/{week+3}/generation (A + Dissabtes) · 200 · classCount=38`; `calendar?filter=DRAFT · draftCount=38,
  canValidate=true`; `member GET /day-grid (draft week) · 0 classes`; `POST /weeks/{week+3}/validation · 200 · validatedClassIds=38`.
- «D4c anul·la una classe amb inscrits ficticis (transacció + esdeveniments a l'outbox)»: `cancellation without text · 422
  ADMIN_TEXT_REQUIRED`; `cancellation · 200 CANCELLED · affectedBookings=4, affectedWaitlist=2`; `outbox ClassCancelledByClub ·
  committed · event 249dcb47…: affected=4, waitlistIds=2`; `notifications N-08a · 6 APP + 6 EMAIL (mailbox) + 6 SMS QUEUED`.
- «una activitat publicada bloqueja la pista i apareix a 04» (back half): `ring-conflicts · 1 CLASS conflict (Sat 18:30 Central)`;
  `publication · 409 RING_BLOCK_CONFLICT`; `publication {cancelClasses} · 200 PUBLISHED · ringBlockIds=1; class CANCELLED{ACTIVITY}`;
  `calendar · activity block with activityTitle`; `member GET /me/activities · rowState=OPEN`. Screen 04 itself is a front check.
- «alumne i instructor veuen 10/23» (back half): `day-grid member / instructor · member 7 classes without occupancy/instructor;
  instructor sees occupancy`. The screens themselves are organizer-run front checks (`docs/DEPLOY.md` checklist).

### Files changed
Session 1 (published in `919ca3c`): `bin/e4-smoke` (new); `seeds/demo-canic.yaml` (`planning`, `bookings`, `activities`
sections), `seeds/README.md`, `README.md`, `docs/DEPLOY.md`, `CHANGELOG.md`, `.gitignore` (E4-T05 logs);
`shared/application/DemoSeedStep.java`, `DemoSeedActor.java`; `clubs/census/application/DemoPlanningService.java` (new),
`DemoSeedCommand.java` (`--week-start`), `DemoSeedService.java` (fictional phones via `CountryContacts`),
`census/domain/DemoDataset.java`, `census/persistence/DemoSeedRun.java`; `clubs/scheduling/application/DemoPlanningSeeder.java` (new),
`ClassSessionService.java` (`slot`, `bookingCounters`), `scheduling/persistence/ClassSessionRepository.java` (`findLive`);
`clubs/bookings/application/DemoBookingSeeder.java`, `DemoClassBookings.java`, `DemoBookingsConfiguration.java`,
`clubs/bookings/persistence/DemoClassBooking.java`, `DemoClassBookingRepository.java` (all new); `clubs/activities/application/DemoActivitySeeder.java`
(new); `clubs/catalogs/application/PlanningCatalogAccess.java`; `META-INF/spring/…AutoConfiguration.imports`; tests
`DemoPlanningSeedIT.java` (new), `DemoSeedsIT.java`, `DemoDatasetTest.java`, `census/support/DemoFixtures.java`.
Session 2 (this one): `src/test/java/com/agilityhub/core/clubs/bookings/application/DemoBookingsTest.java` (new),
`roadmap/evidence/E4-T05/cli-seed.py` (new) and logs `06`–`14`, `CHANGELOG.md`, this report, `roadmap/STATUS.md` (generated).

### Rules and tests implemented (R-xx-nn / T-xx-nn)
- T-06-28 / T-07-32 (back half): `DemoPlanningSeedIT.T_06_28_T_07_32_demoSeedHoldsThePlanningAndActivityStatesOfTheGate` (every
  seeded state above, tenant-only documents, levels respected, no seed-login registrations),
  `T_06_28_demoPlanningIsDeterministicIdempotentAndGuarded` (seed 42 deterministic, second run and another `--week-start` change
  nothing, invalid `--week-start` rejected, prod/staging forbidden), `T_06_28_planningNeedsTheCensusDemoFirst`,
  `T_06_28_D4cCancellationOfTheSeededClassNotifiesTheDemoRegistrants`, `T_06_28_seedSourcesContainNoRealStaffOrMockupFirstNames`;
  `DemoBookingsTest.T_06_28_*` (3 unit tests for the adapter and seeder guards).
- Demo data exercised through the real rules: R-06-01 (bands), R-06-03 (automatic/manual descriptions), R-06-05 (`RING_DOUBLE_BOOKED` in «Setmana B»,
  `INSTRUCTOR_DOUBLE_BOOKED` on W+2), R-06-07/R-06-08 (generation and validation), R-06-10 (4 + 2 cancellation), R-06-11 (maintenance
  block), R-07-05 (publication with `cancelClasses`), R-07-08 (registrations/waitlist).

### Assumptions
- Timeline follows the organizer's 2026-09-19 answer. The D4c class is on validated W+2 because only ACTIVE classes can hold bookings
  (`bookingCounters` rechecks ACTIVE). The tournament is on the W+2 Saturday, not W+1, because publication maps DRAFT conflicts to `DELETED`.
- Deviations (documented in `seeds/README.md`): template bands may not overlap, so the Saturday template omits R-06-01's 17:40 band and
  uses 18:30 for the tournament conflict. «Seminari de handling» is full (12 + 2 waitlisted), not the mockup's «6/12», because a waitlist
  needs a full activity. The automatic «D i sup.» would need TER too, so that description is manual. «Lliga social» runs 09:00–14:00
  (the task gives only 09:00).
- Idempotency is recorded per club (`demo_seed_runs` `<clubId>:planning`). A later run with any `--week-start` reports 0 changes.
  A changed planning section is rejected with `CLUB_NOT_EMPTY` (use a fresh database).
- The smoke provisions the public API key by writing its SHA-256 into the disposable club's `publicApiKeyHash`, and activates that
  club directly in Mongo. No key-provisioning API exists yet. This is fixture setup on the disposable database only.
- The CLI verification runs on a disposable Mongo container, not on a long-lived `docker compose up` stack. It is the same jar and
  profile, and nothing on the machine changes. Env-prefixed shell commands are denied in this sandbox, so the Python helper sets the env.
- Dependency note: E4-T04 is `awaiting_verification`.

### Questions / catalog proposals
No catalog proposals. No parameter, event, notification, error code or audit action was added.
Commands denied by the sandbox (worked around; nothing depends on them): `git check-ignore -v roadmap/evidence/E4-T05/cli-seed.py …`
(meant to confirm the helper is not git-ignored; `git status` lists it as untracked, so it will be committed), and shell loops with
`$VAR` expansion (replaced by Python one-liners).

## Organizer verification
Result: verified · 2026-09-24 · organizer (Claude)

Seed timeline as agreed on 2026-09-19 (W+1 GENERATED draft; W+2 VALIDATED with the D4c class, the maintenance block and the tournament conflict; candidates from W+3), documented in `seeds/README.md`; second `seed:demo` → 0 changes; `bin/e4-smoke` exit 0 twice; `DemoBookingsTest` added for the branch gate. CI of `77658d3` not kept; this task's code is included unchanged in `924303e` (green).

Independent review `roadmap/reviews/E4-T05-20260923-2326-claude.md`:
- #1 (real-format demo phone numbers receive N-08a SMS intents) → **E7-T01** gets a non-production SMS allow-list; **E5-T08** notes it in `seeds/README.md`.
- #2 (`DemoSeedActor` is callable from any code path), #6 (the demo planning ages on a long-lived stack — staging demos need a refresh), #8 (`bookingCounters` callers) → **E5-T08**.
- #4 (`3/4` instead of `4/5` for the Monday 08:30 class: capacity comes from the levels) accepted; #5, #7, #9, #10 accepted as described.
