---
id: E4-T05
title: E4 integration (back): demo seed with templates A/B + Saturdays, validated week, ring block, inconsistency and the 4 D7 activities; bin/e4-smoke on the local stack (generate → validate → cancel with registrants → outbox → activity blocks a ring → public API); README/DEPLOY; gate E4 evidence
stage: E4
repo: agilityhub-core-api
thread: A
order: 50
status: ready
depends_on: [E4-T02, E4-T03, E4-T04]
branch:
pr:
updated: 2026-09-19
---

# E4-T05 · E4 integration (back): demo seed with templates A/B + Saturdays, validated week, ring block, inconsistency and the 4 D7 activities; bin/e4-smoke on the local stack (generate → validate → cancel with registrants → outbox → activity blocks a ring → public API); README/DEPLOY; gate E4 evidence

## Objective
Work packages S06 P7 and S07 P6 (back halves): the demo seed of E2-T11 grows with the planning and activities data every front task and screenshot needs, and the gate E4 scenario runs unattended against the local stack with one command, so the web integration tasks (E4-W…) reuse the same seed and the organizer can tick the gate from evidence.

## Spec references
S06 §12 P7 («`demo-seed` amb plantilles A/B + dissabtes, setmana validada amb inscrits, bloqueig i incoherència») and T-06-28 (E2E, back half: the states the D4/D4b/D4c scenario needs) · S07 §12 P6 («`demo-seed` amb les 4 activitats del mockup (una amb conflicte de classe), inscrits i espera») and T-07-32 (E2E, back half) · S06 R-06-01 (band example), R-06-03 (description examples), R-06-05 (inconsistency examples), R-06-07, R-06-08, R-06-10 (cancellation example: 4 registrants, 2 with pack, 2 waiting), R-06-11 (block example: Carretera Wednesday 16:00–18:00 «manteniment») · S07 R-07-05 (publication example with a class conflict on Central and a training booking), R-07-08 · `docs/pantalles/escriptori/D3-*.html`, `D4-*.html`, `D4b-*.html`, `D4c-*.html`, `D7-*.html`, `docs/pantalles/mobil/10-*.html`, `23-*.html` (data shapes) · backlog gate E4: «l'admin genera i valida una setmana des de les plantilles (A/B + dissabtes); alumne i instructor veuen 10/23; D4c anul·la una classe amb inscrits ficticis (transacció + esdeveniments a l'outbox); una activitat publicada bloqueja la pista i apareix a 04» · E2-T11 (`seed:demo`, seed 42), E3-T05 (`bin/e3-smoke` pattern), E1-T10 (`bin/e1-smoke`, local mailbox), E1-T14 (`docker-compose.consumer.yml`, image).

## Context to load
`seeds/demo-canic.yaml`, `seeds/club-canic.yaml`, `seeds/README.md` · `src/main/java/com/agilityhub/core/clubs/census/application/DemoSeedCommand.java`, `DemoSeedService.java` · `bin/e1-smoke`, `bin/e3-smoke` (if merged), `bin/core`, `compose.yaml`, `docker-compose.consumer.yml` · the application services of E4-T02/E4-T03/E4-T04 (`WeekTemplate*`, `WeekGenerationUseCase`, `WeekValidationUseCase`, `ClassCancellationUseCase`, `RingBlockService`, `ActivityService`, `ActivityLifecycleService`, `ActivityRegistrationService`) and the ports `ClassBookingsPort`, `TrainingConflictPort` of E4-T03 · `docs/DEPLOY.md`, `README.md`, `roadmap/ROADMAP.md` (read-only: gate E4 checklist) · the mockups above · this file.

## Steps
1. **Seed data through the application services** (never raw inserts, so every invariant, derived instant, block and outbox event is real): extend `seed:demo` (`seeds/demo-canic.yaml` + `DemoSeedService`, deterministic with `--seed=42`, idempotent: a second run reports 0 changes) with a new option `--week-start=YYYY-MM-DD` (default: the Monday of the club-local current week — planning data must be relative to the run date, unlike the census `referenceDate`): (a) templates «Setmana A» and «Setmana B» (`WEEKDAYS`) and «Dissabtes» (`SATURDAY`) with the R-06-01 bands 08:30–09:30 · 09:30–10:30 · 16:30–17:30 · 17:40–18:40 · 18:50–19:50 · 20:00–21:00 (Saturdays: 08:30, 09:30, 16:30, 17:40, 18:30 slot for the Torneig conflict) and the D3 mockup classes (descriptions «A», «A+B», «B+C», «C+D+E», «F+G», «D i sup.», «Cadells», one manual «Obed. urbana» at Wednesday 20:00; rings by colour as in the mockup; the three demo instructors of E2-T11 rotated — never the mockup's first names, never real staff); «Setmana B» carries the D3 inconsistency (Wednesday 20:00, two classes on Central → `RING_DOUBLE_BOOKED`, `canGenerate = false`); «Setmana A» and «Dissabtes» are clean; (b) the current week generated from A + Dissabtes and **validated** (`VALIDATED`, classes `ACTIVE`, the SETMANES table of D3 shows it generated/validated) plus one loose class added after validation that duplicates an instructor on Thursday 18:50 → the D4 week warning «… assignat a dues pistes alhora» (`INSTRUCTOR_DOUBLE_BOOKED`, a warning on a validated week); (c) the next week generated as a **draft** (`GENERATED`, D4b «Esborrany» filter, validation card with `draftCount`); (d) one `RingBlock` `BLOCK`/`MAINTENANCE` on Carretera, Wednesday of the current week 16:00–18:00, note «manteniment», created by an admin (D4 «Carretera bloquejada»); (e) demo registrants for the cancellation flow — bookings are E5: add `DemoClassBookingsAdapter` (`ClassBookingsPort` implementation active only in the `local`/`test` profiles and `@ConditionalOnMissingBean` so E5's real adapter replaces it) backed by a `demo_class_bookings` collection the seed fills with fictional member + dog pairs from the census (the D4 mockup counts: `4/5`, `3/5`, `5/5 +1`, `2/5`, `1/1` on the «Teràpia» and «Particular» classes with manual descriptions and capacity 1, `0/5` on a class cancelled by `RISK_REVIEW` with the seed's `scheduling.autoCancel.text`) including the R-06-10 case (Wednesday 18:50 «B+C»: 4 registrants, 2 `paidWithPack`, 2 waiting) — `counters` on the `ClassSession` come from this adapter through the same code path S08 will use, never patched by hand; document that E5 replaces the collection with real `bookings`; (f) the 4 D7 activities dated relative to `--week-start`: «Torneig d'Estiu 2026» (`COMPETITION`, Saturday of week +1, 18:30–20:30, all five rings, 40 places, registration open until the day before, 22 `ACTIVE` registrants, `waitlistEnabled = true`) **published with the class conflict** of R-07-05 resolved by the real lifecycle call (`cancelClasses: true` + admin text → the Saturday 18:30 class on Central becomes `CANCELLED{ACTIVITY}` with its N-08a rows and the 5 `ACTIVITY` blocks exist); «Seminari de handling» (`SEMINAR`, Saturday of week +5, 9:00–13:00, Central, registration open, **full: 12 places, 12 `ACTIVE` + 2 `WAITLISTED`** — the mockup shows «6/12»; the waitlist needs a full activity, state the deviation in `seeds/README.md`); «Lliga social — 3a jornada» (`SOCIAL_LEAGUE`, Saturday of week +6, 9:00, all rings, no maximum → «obertes», registration open, 10 registrants, image + one document uploaded through the signed-upload path with tiny fictional files); «Demostració Festa Major» (`DEMONSTRATION`, Sunday of week +8, no hours, `atClub = false`, `location.name` fictional, `DRAFT`). Registrants are eligible `ACTIVE` demo members (levels respected), registered through `ActivityRegistrationService` with `origin = APP`.
2. **`bin/e4-smoke [--image]`** (Python, pattern of `bin/e1-smoke`/`bin/e3-smoke`): starts the stack (`compose.yaml`, or reuses `--image` with `docker-compose.consumer.yml`), applies `seeds/club-canic.yaml` and `seed:demo --seed=42`, logs in with the fictional seed accounts (`SEED_PASSWORD`, never printed), then asserts with `curl`-equivalent calls, exact statuses and codes: `GET /week-templates` (3 templates; «Setmana B» `inconsistencyCount = 1`) → `POST /weeks/{B-week}/generation` with «Setmana B» → `422 TEMPLATE_INCONSISTENT` → `GET /weeks/generation-candidates` (`proposed` = first week after the seeded draft) → `POST /weeks/{id}/generation {weekdayTemplateId: A, saturdayTemplateId: Dissabtes}` → 200 `classCount`, `skipped` → `GET /weeks/{id}/calendar?filter=DRAFT` (`draftCount > 0`, `canValidate = true`) → member `GET /day-grid?date=<Monday>&view=member` shows **no** class of that week → `POST /weeks/{id}/validation` → 200 `validatedClassIds` → member grid shows the classes without `occupancy`/`instructorName` beyond `bookings.showInstructorHoursBefore`, instructor grid shows `occupancy` → `POST /class-sessions/{Wed 18:50 B+C}/cancellation {reason: CLUB_MANUAL}` without text → `422 ADMIN_TEXT_REQUIRED`; with `adminText` → 200 `CANCELLED`, `affectedBookings = 4`, `affectedWaitlist = 2` → outbox `ClassCancelledByClub` (4 `affected`, 2 `waitlistIds`) and `notifications` rows APP + EMAIL (captured in the local mailbox log) + SMS `QUEUED` for the 6 recipients (Mongo assertions through `docker compose exec mongo mongosh --quiet --eval`, ids truncated) → `POST /ring-blocks` over an active class → `409 RING_BLOCK_CONFLICT`; on a free slot → 201 and visible as `OCCUPIED` in the member grid, `BLOCK` in the instructor grid → admin `POST /activities {title, type}` → `PATCH` with the next Saturday 18:30–20:30 on Central → `GET …/ring-conflicts` lists the class → `POST …/publication` → `409 RING_BLOCK_CONFLICT`; with `{cancelClasses: true, adminText}` → 200 `PUBLISHED`, `ringBlockIds` of size 1, the class `CANCELLED{ACTIVITY}` → `GET /weeks/{id}/calendar` shows the block with `activityTitle` → member `GET /me/activities` lists it (`rowState = OPEN`) → `POST /activity-registrations` → 201 `ACTIVE` → `GET /public/canic/activities` without key → 403 `INVALID_API_KEY`; with the seeded key (masked in output) → the activity with `places.free` and no `member*`/name/phone key anywhere in the JSON → `POST …/cancellation` of the activity with text → 200, registration `CANCELLED{ACTIVITY_CANCELLED}`, N-32c rows → `bin/core activities:finish-ended` and `scheduling:finish-ended` exit 0 → prints a summary table (step · status · key values) and exits non-zero on the first failed assertion; the second run of the script is idempotent (seed 0 changes; the script creates its own activity/blocks with a run suffix and cancels them at the end).
3. **Docs**: `seeds/README.md` (planning/activities data, `--week-start`, the demo bookings adapter and its E5 replacement, the Seminari deviation), `README.md` (E4 commands: `seed:demo` options, `scheduling:finish-ended`, `activities:finish-ended`, `bin/e4-smoke`), `docs/DEPLOY.md` (nothing new expected in environment variables — say so; the gate E4 checklist section with the smoke as its evidence, organizer-run steps for the two front checks «alumne i instructor veuen 10/23» and «apareix a 04»).
4. **Gate E4 (back) evidence**: `roadmap/ROADMAP.md` is organizer-owned — do not edit it; paste in your report, per gate line, the smoke output lines that prove it (week generated from A/B + Saturdays and validated; class with fictional registrants cancelled in one transaction with `ClassCancelledByClub` in the outbox and N-08a rows; published activity blocking a ring and listed for the member). E2E T-06-28 and T-07-32 (web) rely on this seed: the «Esborrany» filter opens the seeded draft week; the D4c modal reads «ANUL·LA I AVISA ELS 4 ALUMNES» on the Wednesday 18:50 class; a member account of the seed can register to «Lliga social — 3a jornada»; the front selects rows by state, never by the mockup's first names.
5. **Regression**: `./mvnw -q verify` stays green (add the seed tests: the generator output is deterministic for seed 42, the second application changes nothing, every seeded document belongs to the Cànic tenant, no real name from `staff.canic.yaml` or the mockups appears in seed sources).

## Fixed conventions
- Fictional data only: names from the E2-T11 generator, `@example.test` accounts, tiny generated image/PDF fixtures («Fictional demo document») — never the club's real staff, never the mockup first names as real people; no secrets in output (`SEED_PASSWORD`, public API key, tokens truncated).
- The seed uses the E4 application services and the shared `Clock`; it never writes `class_sessions`, `ring_blocks`, `activities` or `activity_registrations` directly; `counters` are produced by the demo bookings adapter, not set by hand.
- If Docker is not available in the session, block per the protocol (the organizer verifies the smoke in the executor evidence + CI); the smoke never prints tokens or keys.
- Closed catalogs: nothing new; any doubt on seed values (dates, counts) is an assumption in the report. Evidence rule: report = command + exit code + last 40 lines; long outputs → `roadmap/evidence/E4-T05/NN-<name>.log`; task file ≤ 120 KB; never edit other tasks, `ROADMAP.md` or the Organizer verification section.

## Verification (run all; evidence = command + exit code + last 40 lines; long outputs → `roadmap/evidence/<ID>/*.log`)
- `bin/core club:apply seeds/club-canic.yaml` then `bin/core seed:demo --club=canic --seed=42` twice → second run 0 changes; `bin/e4-smoke` exit 0 **twice** (full tail of the second run in the report); `./mvnw -q verify` green (seed tests included); `git diff --check` clean.

## Definition of Done
Gate E4 (back) is reproducible with one command on the local stack; the demo seed holds every state D3/D4/D4b/D4c/D7/10/23/04 need; web E4 integration tasks can run T-06-28 and T-07-32 against the same seed.

## Evidence to return
Seed tails (both runs), smoke tails (both runs), `verify` tail, README/DEPLOY diffs, the gate E4 lines with their proving output.

---

## Executor report
Blocked on 2026-09-19 before implementation: steps 1(b–f) require incompatible
date/state fixtures through the existing application services. Docker is available.
This is source-level diagnosis, not a passing seed or smoke rehearsal.

### Branch / commits
Working tree only; no git write command. Initial `git status --short` was empty.
E4-T03 and E4-T04 were `awaiting_verification` at task start; E4-T02 was `verified`.

### Evidence (full command outputs)

All outputs below are shorter than 40 lines and reproduced in full.

Command: `python3 roadmap/tools/check.py --next` — exit 0.
```text
E4-T05
```

Command: `python3 roadmap/tools/check.py --set E4-T05 in_progress` — exit 0.
```text
E4-T05: status -> in_progress
STATUS.md rendered (51 tasks)
```

Command: `docker info --format '{{.ServerVersion}}'` — exit 0.
```text
29.7.2
```

Exact date diagnostic command — exit 0. Uses the timezone declared in
`seeds/club-canic.yaml`; no database writes.
```sh
python3 - <<'PY'
from datetime import datetime, timedelta
from zoneinfo import ZoneInfo
now = datetime.now(ZoneInfo('Europe/Madrid'))
monday = now.date() - timedelta(days=now.weekday())
wednesday = monday + timedelta(days=2)
block = datetime.combine(wednesday, datetime.min.time(), tzinfo=now.tzinfo).replace(hour=16)
print('Club-local date:', now.date(), now.strftime('%A'))
print('Required default week-start:', monday)
print('Required Wednesday class date:', wednesday)
print('Generation skips Wednesday as PAST:', wednesday < now.date())
print('Required maintenance start:', block.isoformat())
print('Maintenance start satisfies from > Clock.instant():', block > now)
print('Candidate future demo week-start:', monday + timedelta(weeks=1))
PY
```
```text
Club-local date: 2026-09-19 Saturday
Required default week-start: 2026-09-14
Required Wednesday class date: 2026-09-16
Generation skips Wednesday as PAST: True
Required maintenance start: 2026-09-16T16:00:00+02:00
Maintenance start satisfies from > Clock.instant(): False
Candidate future demo week-start: 2026-09-21
```

Source-level findings (not runtime test claims), under
`src/main/java/com/agilityhub/core/clubs/scheduling/`:

- `application/WeekGenerationUseCase.java:98–105`: past dates are skipped;
  generated classes start DRAFT.
- `application/RingBlockService.java:93`: manual blocks require a future start.
- `application/RingBlockService.java:113`: publication cancels DRAFT conflicts
  with DELETED, ACTIVE conflicts with ACTIVITY.
- `domain/ClassSessionRules.java:11–15`: a DRAFT cannot cancel with ACTIVITY.
- `application/WeekValidationUseCase.java:29–33`: validation activates every
  draft and makes the entire week VALIDATED.

Required implementation verification deferred under the blocked-task protocol:

- `bin/core club:apply seeds/club-canic.yaml`: not run; no demo database created.
- `bin/core seed:demo --club=canic --seed=42` twice: not run; E4 extension absent.
- `bin/e4-smoke` twice: not run; E4 smoke absent.
- `./mvnw -q verify`: not run; application and test code unchanged.
- `git diff --check`: exit 0, no output.

Command: `python3 roadmap/tools/check.py --set E4-T05 blocked` — exit 0.
```text
E4-T05: status -> blocked
STATUS.md rendered (51 tasks)
```

Gate E4: no gate line claimed complete. Week/cancellation/activity rehearsal,
day-grid checks and regression evidence remain pending; no organizer checkbox edited.

### Files changed

- `roadmap/tasks/E4-T05.md`: diagnosis/report and task status.
- `roadmap/STATUS.md`: generated by `check.py`.
- `roadmap/MESSAGES.md`: organizer question and proposed fixture arrangement.
- `CHANGELOG.md`: Unreleased entry explicitly describing blocked work.

### Rules and tests implemented (R-xx-nn / T-xx-nn)

None. R-06-07/08/10/11 and R-07-05 demo scenarios, T-06-28/T-07-32 back-half
support, adapter and seed tests remain unimplemented. Existing lifecycle guards
were inspected and preserved.

### Assumptions

- Use real application services and the injected clock as required; no direct
  scheduling inserts or silent lifecycle exceptions.
- Moving the explicit default week or tournament changes frontend fixture
  acceptance criteria. No such change has been implemented.
- A future explicit `--week-start` could avoid past dates for one run, but does
  not satisfy the default verification command or resolve the draft/state conflict.
- Existing census data and running Docker services were not changed.

### Questions / catalog proposals

Blocking question to @organizer: reconcile the seed timeline and states.

1. Step 1 requires the current Monday by default, Wednesday's generated class
   and maintenance block, and service-only creation. On this Saturday generation
   skips Wednesday as PAST and the manual block rejects its past start. Moving
   the fixture changes the explicit default; backdating the clock would bypass
   the guard and is not authorized as a fixture convention.
2. Step 1(c) requires week +1 GENERATED with DRAFT classes, but 1(f) requires its
   Saturday tournament to cancel a class as ACTIVITY with N-08a rows. Publication
   maps DRAFT conflicts to DELETED. ACTIVITY requires ACTIVE; the available
   validation use case activates all drafts and makes the whole week VALIDATED,
   losing the requested draft-week state. This matches the E4-T03 handoff in
   MESSAGES.md and is independent of today's date.

Proposed assumption on resume: preserve lifecycle rules, anchor the full demo in
a future week, retain week +1 GENERATED for D4b, and move the tournament conflict
to a separate generated-and-validated week +2. Expect the next generation
candidate after the latest generated week. These are proposed task/fixture
amendments, not implemented changes. No catalog addition proposed.

## Organizer verification
_(pending)_
