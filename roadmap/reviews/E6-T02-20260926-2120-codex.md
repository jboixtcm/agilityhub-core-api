<!-- independent review · task E6-T02 · reviewer codex:gpt-6-astra · executor claude · commit 7aab2ac · 2026-09-26 21:20 -->

# Review E6-T02 — needs changes

## Findings

1. **major** `src/main/java/com/agilityhub/core/shared/api/IdempotencyFilter.java:61` — Attendance PUTs now enter the cached-response path, which returns before MVC’s impersonation guard and `@PreAuthorize` execute. After a successful staff save, a MEMBER-only token with the same subject and club can replay the same request/key and receive `200` with the entire sheet. The cache scope contains no authorization context. This violates S10 §6 / T-10-22. Check current staff authorization and reject impersonation before replay; add a test that reuses a successful key with a MEMBER-only token.

2. **major** `src/main/java/com/agilityhub/core/clubs/bookings/application/AttendanceSheetQuery.java:79` — With `levels.enabled=false`, `levelCodes()` returns `Map.of()`, but this lookup passes nullable `pair.levelId()`. `Map.of().get(null)` throws `NullPointerException`; the waitlist lookup at line 124 has the same defect. Dogs without levels are explicitly valid under `MODEL_DADES_PLATAFORMA.md` §3 and R-10-16. Consequently, GET fails and PUT rolls back while constructing its response. Guard null level IDs and add T-10-33 cases with unassigned levels in both bookings and waitlist entries.

3. **minor** `src/test/java/com/agilityhub/core/clubs/bookings/domain/NoticeRulesTest.java:40` — The required six-row T-10-03 test exercises `NoticeRules`, which has no production callers. Its booking-state and refund assertions therefore validate a parallel implementation rather than the actual save/cancellation flow. This leaves the claimed R-10-05 boundary evidence incomplete under AGENTS rule 5. Exercise the table through the real save path, or make the tested decision logic authoritative in production.

4. **minor** `src/main/java/com/agilityhub/core/clubs/bookings/application/WeekAgendaPdf.java:35` — The renderer accepts no logo and only draws the club name and colour band. Step 10 and R-10-15 explicitly require the configured club logo. The report acknowledges this omission but still declares all steps complete. Implement logo rendering from an appropriate stored/cached asset, or record an approved scope amendment.

5. **nit** `roadmap/tasks/E6-T02.md:248` — The reported clean whitespace check does not cover the published change: `git diff --check 7aab2ac^ 7aab2ac` exits **2**, reporting 27 trailing-whitespace occurrences in added evidence logs. Normalize those lines and repeat a check that includes newly added files.

## Not checked

No fresh Maven/Docker tests, manual API scenario, or OpenAPI regeneration were run because they write files or change service state. Runtime findings above are based on source inspection; existing test reports and evidence were examined.