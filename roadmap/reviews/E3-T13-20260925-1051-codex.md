<!-- independent review · task E3-T13 · reviewer codex:gpt-6-astra · executor claude · commit 8132135 · 2026-09-25 10:51 -->

# Review E3-T13 — needs changes

## Findings

1. **major** `src/main/java/com/agilityhub/core/clubs/census/application/SignupService.java:78` — Recovering the public-signup cutoff only through `payments.owed()` misses readmissions that created no payment rows. Reproduce with unpaid signup R1, a validated readmission R2 using a `SINGLE_CLASS` plan with no entry fee, then a legacy add-dog C that overwrites `Member.signup`. R2 has no entry in `owed`; even when its dog is loaded through R1’s rows, `blocks()` rejects R2’s block because its `submissionId` differs. The cutoff therefore disappears or predates R2, and checkout charges superseded R1 rows alongside C. This violates step 1, R-04-06/R-04-26 and S04 §5. Recover the member’s public-signup boundary independently of whether that submission generated payments, preserving debtor identity across transfers. Add a regression covering this zero-payment readmission followed by a legacy overwrite.

## Not checked

Maven verification, Docker smoke runs and before-fix executions were not rerun because they write files or application state. Review used static inspection, committed logs and existing Surefire/Failsafe XML reports. The finding above was traced through the code, not executed end to end.