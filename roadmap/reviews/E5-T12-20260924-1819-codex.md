<!-- independent review · task E5-T12 · reviewer codex:gpt-6-astra · executor claude · commit b5f0e07 · 2026-09-24 18:19 -->

# Review E5-T12 — needs changes

## Findings

1. **major** `src/main/java/com/agilityhub/core/migration/application/PlayoffPlanner.java:149` — Removed joins are detected only when both records participate in candidate resolution. After loading fixture pair 62←82, remove `persones.csv` and omit record 82 from the input files: the planner reports **zero unsupported transitions** and schedules member 62 with its original NIF, overwriting the club-confirmed NIF while retaining alias 82. The same bypass occurs when an already-inactive joined record is filtered out as an old leaver. Reproduced both cases with the planner. This violates R-18-14 and round-3 requirement 1: removing an existing join must protect the destination member. Detect removed joins against stored relationships, including absent/skipped records, and add dry-run/apply tests asserting preservation of the member and its dependants.

2. **major** `src/main/java/com/agilityhub/core/migration/application/PlayoffPlanner.java:380` — Family-group protection checks only incoming rows, ignoring the stored group’s holder and members. After loading the fixture, change holder 61 to `LEFT`, then replace that group’s incoming membership with 81 and 83, holder 81. The planner protects member 61 but still schedules a replacement of the same group, removing 61 while its preserved `familyGroupId` still references that group. Reproduced with `blocking=false`, one unsupported member and one group change. This violates round-3 requirement 2 and leaves inconsistent membership links. Check `storedGroups.get(groupId)` against `protectedMembers` as well as incoming membership; reject the group with `REEXECUTION_UNSUPPORTED field=familyGroup`. Add a regression covering changed membership.

## Not checked

- Did not rerun `clean verify`, Docker/CLI stack checks, or Mongo-backed apply tests because this review is read-only.
- Reproductions exercised the existing compiled planner with synthetic fixtures and in-memory service doubles; database effects were traced through the apply/repository code.