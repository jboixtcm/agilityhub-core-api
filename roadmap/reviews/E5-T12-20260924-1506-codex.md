<!-- independent review · task E5-T12 · reviewer codex:gpt-6-astra · executor claude · commit f162cd3 · 2026-09-24 15:06 -->

# Review E5-T12 — needs changes

## Findings

1. **major** `src/main/java/com/agilityhub/core/migration/application/PlayoffPlanner.java:195` — Re-importing an ACTIVE member as LEFT skips identity processing but preserves their existing `accountId` and ACTIVE membership. If another ACTIVE person now shares that email, the existing membership also prevents them from receiving the account. This violates R-18-12/E32 on re-execution (R-18-14). Reconcile migration-owned account links and tenant memberships transactionally, preserving unrelated global-account access. Add an ACTIVE → LEFT re-import test with an ACTIVE shared-email counterpart.

2. **major** `src/main/java/com/agilityhub/core/migration/application/PlayoffPlanner.java:132` — Adding `persones.csv` after an initial import does not reconcile the previously separate member. With the supplied 62/82 fixture, the planner adds record 82’s alias to member 62 while its original member still owns that alias. Dry-run can report success, but apply conflicts with the unique `member_playoff_ids` index. If the confirmed NIF belongs to the joined member, planning instead rejects it with `ID_DOCUMENT_ALREADY_EXISTS`. R-18-04(f)/R-18-14 require a consistent merge and re-execution path. Implement reconciliation of imported duplicates, or explicitly reject this unsupported transition during planning; test import without the file followed by import with it.

3. **major** `src/main/java/com/agilityhub/core/migration/application/PlayoffPlanner.java:229` — B29 is only partially implemented: `Foto` is stored in `sourceIds.playoffPhoto`, while `photoFileKey` remains unset. No consumer implements the promised download/upload, and the new test explicitly asserts that the required destination is null. Task step 1 and S18 §3 require `Foto → Dog.photoFileKey`; the census API reads that field to display photos. Complete the photo-import path with synthetic storage tests, or obtain an explicit organizer scope amendment identifying the deferred work.

## Not checked

- Did not rerun Maven, Docker, seed application, or migration commands because this review is read-only. Inspected committed evidence and existing Surefire/Failsafe XML reports.
- Did not run web e2e tests or access real Playoff exports.
- Unrelated tasks and organizer changes bundled into the commits were not independently reviewed.