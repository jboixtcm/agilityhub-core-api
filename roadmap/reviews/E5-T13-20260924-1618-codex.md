<!-- independent review · task E5-T13 · reviewer codex:gpt-6-astra · executor claude · commit c920963 · 2026-09-24 16:18 -->

# Review E5-T13 — needs changes

## Findings

1. **major** `src/main/java/com/agilityhub/core/payments/application/CheckoutService.java:74` — Late completion remains unrecorded when the club cancels a `PAYMENT_PENDING` booking and the completion arrives after `expiresAt`. Club cancellation leaves the checkout `PENDING`, so line 76 throws `INVALID_STATE` before storing the provider payment ID or reconciliation mark. P7 cannot recover this: it selects only `PAYMENT_PENDING` bookings, excluding the cancelled booking. This violates step 2 / E34; report assumption 5 incorrectly relies on a later P7 run for this case. Handle cancelled-booking completions before the deadline rejection. Extend T-15-25 with club cancellation followed by completion after 31 minutes, asserting the mark, WARN, and idempotent retry.

2. **minor** `src/main/java/com/agilityhub/core/platform/application/jobs/JobViews.java:75` — Platform work still contributes to the club’s `WOULD_DELETE` counter when the trace exceeds 500 items. `JobRunner` counts the entire plan but stores only its first 500 items; this filter subtracts only platform items present in that truncated trace. For 500 tenant items followed by one platform item, the club receives `WOULD_DELETE=501`. This leaves step 6 / AGENTS rule 4 incomplete, despite the limitation being acknowledged in the report. Record separate tenant/platform action totals before truncation and construct club counters from those totals. Add an ADMIN-view test exceeding 500 items.

3. **minor** `src/main/java/com/agilityhub/core/platform/application/jobs/JobRunner.java:229` — Releasing the platform claim is not durable with the `FAILED` transition. The run is committed as failed before the hook executes; a crash between these operations, or an exception swallowed at lines 235–237, leaves the claim held until midnight. Subsequent reaping only examines `RUNNING` rows, so it never retries the release. The same-day recovery required by step 5 can therefore still fail. Release the matching claim atomically with completion, or make claim acquisition recover claims belonging to failed runs. Test interruption or release failure after the failed row commits.

4. **nit** `roadmap/tasks/E5-T13.md:114` — The report substitutes abbreviated excerpts for the required last 40 output lines of `clean verify` and `bin/openapi-snapshot`. It also states that earlier failing attempts were unlogged or overwritten. AGENTS session protocol §4 requires literal final tails and retained logs for earlier failures. Paste the complete final tails from the committed logs and retain separate attempt logs in subsequent verification.

## Not checked

- Did not rerun Maven tests, Docker/Testcontainers, or `bin/openapi-snapshot`: these write files or change runtime state, contrary to the read-only review.
- The identified edge cases were established by code inspection, not executable reproduction.
- Actual provider webhook handling and refunds belong to E8-T04 and were not validated here.