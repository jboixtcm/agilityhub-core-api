<!-- independent review · task E6-T01 · reviewer codex:gpt-6-astra · executor claude · commit eca0da3 · 2026-09-24 15:25 -->

# Review E6-T01 — minor issues

## Findings

1. **minor** `src/main/java/com/agilityhub/core/clubs/followup/application/FollowupContractAccess.java:76` — The `deletedAt` check only applies to non-staff callers. Consequently, an instructor can reach the 501 stub for a deleted task, including its completion and attachment routes. S10 §3 hides deleted tasks except through ADMIN’s `includeDeleted`, and R-10-10 requires completion of a deleted task to return 404. Reject deleted tasks independently of ownership in this guard; keep any administrative archive access explicit. Add staff regression cases—the current unit test explicitly permits this case.

2. **minor** `src/main/java/com/agilityhub/core/clubs/followup/api/TasksController.java:42` — `includeDone` defaults to `false`, also published in OpenAPI, whereas S10 §6 specifies `includeDone=true` for the task history contract. This establishes a different default for clients built against the snapshot. Align the default and operation description with the spec, regenerate the snapshot, and assert the default in the contract test.

## Not checked

I did not rerun Maven, Docker integration tests, snapshot generation or `club:apply`, because they modify files or runtime state. Evidence assessment used committed logs and existing Surefire/Failsafe and coverage artifacts, not a fresh execution.