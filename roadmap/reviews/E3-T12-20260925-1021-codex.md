<!-- independent review · task E3-T12 · reviewer codex:gpt-6-astra · executor claude · commit 026b6d8 · 2026-09-25 10:21 -->

# Review E3-T12 — needs changes

## Findings

1. **major** `src/main/java/com/agilityhub/core/shared/application/RateLimits.java:86` — Admission survives only in the instance’s evictable cache. If A is admitted but delivery fails, the application restarts during backoff, and B consumes the recipient’s allowance before A retries, A is refused and its consumer completes without sending. A read-only probe reproduced `A=true`, then on a fresh instance `B=true`, `A=false` with cap 1. Step 2 and the event-consumer playbook require admitted retries to retain their admission. Persist the decision per event/notification through delivery completion; add a restart regression test.

2. **minor** `src/main/java/com/agilityhub/core/clubs/signup/application/SignupPolicy.java:156` — Older first-month snapshots derive `portion` using today’s split-day parameter. A submission on January 17 with split day 16 freezes a €30 half month; changing the split day to 20 makes the fallback return `FULL` while retaining €30. This was reproduced against the compiled calculator. It violates step 5’s frozen-value contract and R-04-15. Recover the historical portion rather than using current configuration, and test a parameter change after removing the stored portion.

3. **minor** `src/main/java/com/agilityhub/core/clubs/census/application/SignupService.java:643` — The dry run can return `firstMonth` without a `FIRST_MONTH` line. Submit a monthly plan while BILLING is disabled, enable BILLING, then dry-run the unchanged plan: `firstMonth()` falls back to the newly calculated quote, but the unchanged-plan branch retains the original empty payment lines and zero total. Step 5 explicitly requires the field to be absent otherwise. Derive its presence from the effective payment lines and add this module-toggle regression test.

4. **minor** `roadmap/tasks/E3-T12.md:295` — Snapshot evidence includes only seven output lines, although the log contains 72; the clean-verification tail also truncates lines at 200 characters. AGENTS.md and the task require the literal last 40 lines, with only necessary secret redaction. Paste those tails from the committed logs.

## Not checked

Full Maven verification, Docker smoke runs, and snapshot generation were not rerun because the review is read-only. Existing XML reports and committed evidence were inspected. The restart defect was reproduced at the rate-limit component level, not through a complete outbox integration run. CI, deployment, and the web’s `pending.json` were not independently verified.