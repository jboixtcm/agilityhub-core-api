<!-- independent review · task E5-T06 · reviewer codex:gpt-6-astra · executor claude · commit 32c2d8c · 2026-09-24 14:52 -->

# Review E5-T06 — needs changes

## Findings

1. **major** `src/main/java/com/agilityhub/core/shared/application/CacheLoads.java:33` — The invalidation fix still exposes stale values. An old load can resume after invalidation completes, publish its result with `put`, and pause before the generation check. A new request then returns that stale value through lines 28–29. Removing it afterwards cannot undo that response. This affects configuration and host resolution and violates the organizer’s round-2 requirement that overlapping loads never store stale values. Keep loading outside locks, but make generation checking and publication atomic with invalidation. Add a test that reads between publication and cleanup; existing tests check only after the loader finishes.

2. **major** `src/main/java/com/agilityhub/core/clubs/bookings/application/BookableClassesQuery.java:89` — Row eligibility ignores both the booker’s and owner’s `status`, unlike `BookingEligibility.check`. For example, after a member transitions to `LEFT` effective today, their dogs remain active until the outbox consumer deactivates them. During that interval, today’s classes can appear `BOOKABLE` because `leaving()` only excludes dates after the leave day, while requesting a hold returns `422 MEMBER_NOT_ACTIVE`. This violates R-08-04 and Step 2’s requirement to reuse eligibility rules. Share the active-member validation and add an aggregate test covering this transition.

3. **minor** `bin/e5-perf:177` — The claimed T-15-30 evidence does not establish load during notification fan-out. `run_after()` waits for P1 to succeed before starting k6; overlap is neither synchronized nor asserted. Furthermore, lines 182–196 accept any positive N-33 count across all channels, so partial delivery can pass the timing check. Start load concurrently with opening, record overlap, and require an APP notification for every intended recipient within the deadline. Alternatively, mark this optional scenario unproven in the report and documentation.

4. **minor** `src/test/java/com/agilityhub/core/platform/application/CacheInvalidationRaceIT.java:95` — Three tests cite incorrect specification IDs: module toggling is T-02-09, not T-02-01; the two host-resolution tests are T-02-06, not T-02-11. AGENTS rule 5 requires meaningful specification traceability. Rename these tests and correct the report’s references.

5. **nit** `roadmap/tasks/E5-T06.md:511` — The assertion that new files contain no trailing whitespace is contradicted by the committed evidence. `git diff-tree --check 28d35f8^ 28d35f8` exits 2 with 126 whitespace diagnostics, all in evidence logs; the first implementation commit also fails. Trim whitespace when preparing logs or accurately qualify the verification claim.

## Not checked

- Maven tests, coverage, Docker smokes, k6, and fresh OpenAPI generation were not rerun under the read-only restriction. Runtime conclusions rely on static inspection and committed evidence.
- Staging behavior and web T-08-40 were not verified.
- Findings target the supplied commit and its implementation ancestors; unrelated working-tree changes were excluded.