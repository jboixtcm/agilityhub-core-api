<!-- independent review · task E5-T05 · reviewer claude:claude-opus-5-5 · executor claude · commit aa55ae8 · 2026-09-24 08:40 -->

I've finished checking. Here is the review.

# Review E5-T05 (round 2, commit `aa55ae8`): minor issues

All six organizer points are fixed. The findings below are minor or nits.

## Findings
1. **minor** `src/main/java/com/agilityhub/core/clubs/scheduling/application/RiskNotifications.java:112`, `messages_{ca,es,en}.properties` `notif.N-16.body`: the new variable `audience` (`STAFF|MEMBER`) is already applied, but the N-16 row of `CATALEG_NOTIFICACIONS.md:31` still lists only `dog_name, class_date, class_time, review_time, review_day`.
   - The executor proposed it openly (report R2-1 and `MESSAGES.md`), and organizer point 4 suggested this approach. Still, until the catalog row is amended the code carries a variable the closed catalog does not list (AGENTS rule 2).
   - **Fix:** the organizer either accepts it and adds `audience` to the N-16 row, or names a different discriminator.
2. **minor** `E5ResponseContractTest.java:117` with `fixtures/contracts/e5-notifications.json`: the notification contract fixture covers N-04…N-47 but not the four notifications this task adds (N-16, N-17, N-33, N-54).
   - So nothing checks their stored variables against the catalog rows. That check is exactly what would have caught the `audience` gap (#1) and the N-17 `class_description`/`ring_name` discrepancy (proposal 1).
   - The executor says round 1 "fixed per-code catalog variables", but only ITs assert that.
   - **Fix:** add N-16/N-17/N-33/N-54 entries to the fixture (in E5-T08 if the organizer prefers).
3. **minor** `src/main/java/com/agilityhub/core/clubs/bookings/application/WaitlistFifoJob.java:53`: P6 passes `recount(classId, true, …)`, reusing the `inTimeCancellation` flag for an expiry.
   - An expiry changes no booking, so the alert fires for any class that is below the minimum and has no standing alert. That includes a class that never reached the minimum: the IT fixture «last» has capacity 1 and one booking, and a single booking never alerts.
   - This matches the literal text of R-15-12b ("`WaitlistExpired` que deixi la classe igual … `countedDogs < minDogs` → emet"), but it is the only path where N-54 fires without a drop.
   - **Fix:** rename the parameter (e.g. `alertIfBelowMinimum`) so the call site is not misleading, and state the "no drop required" reading as an assumption.
4. **minor** `src/main/java/com/agilityhub/core/clubs/scheduling/application/SchedulingDashboard.java:37-52` → `RiskReviewQuery.rows()`: D1 now runs `bookings.activeBookings(classId)` per class, plus `census.person`/`census.dog` per notified booking, on every dashboard rebuild.
   - The deleted `ClassSessionsQuery` javadoc promised "Bulk projection … no per-row repository calls".
   - The 60 s dashboard cache and the 3-day window keep the load small, so this is not a blocker.
   - **Fix:** mention the change in the report, or batch the recount (one `$in` query over the window's class ids) in a follow-up.
5. **minor** Evidence: the local-stack curl sequence ("Evidence to return") was not re-run in round 2. The executor says so openly.
   - D1 is now produced by a different path (`RiskReviewSource`), and the N-16 admin rows have a new shape. Only the ITs show this, not the running jar with `club:apply` + `seed:demo`.
   - **Fix:** a short re-run of `curl-sequence.py` with `GET /dashboard` added would close the "`GET /risk-review` feeds D1" item of the Definition of Done on the real stack.
6. **nit** `DashboardBuildersTest.T_14_05_…`: S14 T-14-05 names the `CLUB_MANUAL`, exempt and +3-days exclusions as assertions of `RiskCardBuilder`.
   - After the refactor they are asserted only in `RiskReviewJobIT.T_15_15_T_14_05_…`, which is fine because the builder no longer filters.
   - The T-14-05 domain test still carries the spec id, though it now only checks mapping and localization.
   - **Fix:** note in the report that the S14 T-14-05 exclusions live in the parity IT.
7. **nit** S14 R-14-06 vs S15 §6: S14 maps `riskAutoCancelSameDay = false` → `PENDING_DECISION` for every not-yet-warned row. Form A keeps `AT_RISK` for warned rows with registrants whatever that parameter says, and D1 inherits this.
   - This follows the organizer's "S15 wins" ruling (assumption R2-1), but the two specs still disagree.
   - **Fix:** a one-line amendment to S14 R-14-06 so later readers do not re-open it.
8. **nit** `src/main/java/com/agilityhub/core/clubs/bookings/application/WaitlistConsumers.java:7`: `java.util.Objects`, and probably part of the `persistence.*` wildcard, are unused now that `alerts.WaitlistExpired` is gone.
9. **nit** `src/main/java/com/agilityhub/core/clubs/scheduling/application/SchedulingDashboard.java:51`: the ring name is wrapped as `new LocalizedText(Map.of("ca", …), "ca")`, a hard-coded locale. It already existed and `RiskCardBuilder` overrides the default locale, so the output is correct. Still, the `ringName == null → "—"` placeholder and the `"ca"` key belong in the projection, not in the adapter.

## Not checked
- I did not run `./mvnw -q clean verify`, `bin/openapi-snapshot` or the local stack, because they need Docker/Testcontainers.
  - The totals (456 unit/contract + 722 IT, 0 failures), the per-method PASS list and the unchanged OpenAPI come from the committed `r2-01`/`r2-02`/`r2-03` logs and the report.
  - With `-q`, the `r2-01` tail has no Maven summary, so "green" rests on the stated exit code plus the XML-derived totals.
- `ArchitectureTest` for the new `clubs.scheduling` → `clubs.dashboard.application.ports` dependency (`RiskReviewSource` bean in `SchedulingDashboard`). It was accepted before for `ClassSessionsQuery`, so it is presumably allowed.
- ICU rendering of the new N-16 `select` by the front-end feed or the E7 push templates. I only read the three backend property files and `RiskNotificationTextsTest`.
- Round-1 review items #6, #10 and #11, which the organizer deferred to E5-T08.
