<!-- independent review · task E3-T10 · reviewer codex:gpt-6-astra · executor claude · commit 9625bda · 2026-09-25 09:35 -->

# Review E3-T10 — needs changes

## Findings

1. **major** `src/main/java/com/agilityhub/core/clubs/census/application/SignupService.java:72` — The checkout cutoff incorrectly excludes payable submissions on existing records. Before this task, `addDog()` overwrote `Member.signup` with an `APP_ADD_DOG` block. Create pending submissions A and then B under that implementation: after upgrading, this filter excludes A’s earlier payment rows because it treats B’s timestamp as a public-signup/readmission boundary. No readmission occurred. This violates step 15.1’s requirement to exclude **only superseded readmission rows** and regresses checkout for previously persisted data. Derive the cutoff from an actual public/readmission submission, handle or backfill existing `APP_ADD_DOG` blocks, and add an upgrade regression test with distinct submission timestamps.

2. **minor** `src/main/java/com/agilityhub/core/clubs/census/application/SignupService.java:88` — Checkout still obtains its locale from `Member.signup`, although step 5 now preserves that block as the original public signup. A public signup in `ca` followed by an add-dog submission in `es` produces Catalan checkout descriptions: `CheckoutService.java:43` formats every payment item using this value. S04 R-04-26 requires descriptions in the submission’s locale. Carry the relevant submission locale into payment-item rendering and test differing public-signup/add-dog languages.

## Not checked

Maven verification, Docker smoke runs, seed application, and OpenAPI regeneration were not rerun because they write files or application state. Review used static code inspection, committed evidence, and existing Surefire/Failsafe XML reports.