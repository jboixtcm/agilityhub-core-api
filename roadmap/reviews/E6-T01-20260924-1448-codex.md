<!-- independent review · task E6-T01 · reviewer codex:gpt-6-astra · executor claude · commit a202870 · 2026-09-24 14:48 -->

# Review E6-T01 — needs changes

## Findings

1. **major** `src/main/java/com/agilityhub/core/clubs/scheduling/application/SessionProjection.java:28` — `booked + notified` double-counts `NOTIFIED{afterClassEnd}` bookings, which remain `ACTIVE` under R-10-05. For one such booking, `marked=1`, `booked=1`, `notified=1` produces `PENDING` although every row is marked. This violates step 7 and R-10-02’s union of live bookings and notified rows. Calculate the distinct row total and add a calendar/day-grid regression test. Recording this known defect for E6-T02 does not complete step 7.

2. **major** `src/main/java/com/agilityhub/core/clubs/followup/api/AttachmentsController.java:77` — `POST /attachments` still declares no `Idempotency-Key`; the published operation consequently has no header parameter. S10 §6 explicitly requires it, and this task’s objective includes idempotency contracts for every S10 route. Add the header contract and missing-header validation, regenerate the snapshot, and include this existing operation in the contract assertions.

3. **minor** `src/main/java/com/agilityhub/core/clubs/bookings/api/InstructorContracts.java:126` — `WeekCell` makes every variant-specific field optional. A `CLASS` cell containing only `date`, `time`, `endTime`, and `kind` satisfies the published schema without `classId`, occupancy, or attendance status. Step 3 requires the three complete variants with required fields by default. Publish constrained variants, such as `oneOf` with a `kind` discriminator, and test that incomplete variants fail validation.

4. **minor** `roadmap/evidence/E6-T01/test-summary.py:19` — The summary prints per-class results only for the `NEW` allowlist. Most classes contributing to the reported 507 unit tests and 811 integration tests are omitted. AGENTS.md’s evidence protocol requires per-class test/failure/error/skipped counts. Commit the complete summary from the clean verification run, with its exit code.

5. **nit** `roadmap/evidence/E6-T01/02-clean-verify.log:771` — The committed change does not satisfy the reported clean whitespace check: `git diff --check a202870^ a202870` exits **2**, identifying trailing whitespace in four evidence logs. Strip trailing whitespace during log sanitization and verify the complete patch, including newly added files.

## Not checked

- Maven/Testcontainers tests, snapshot regeneration, and `club:apply` were not rerun because they write files or databases. Runtime and coverage evidence was assessed from the committed logs and test sources.
- Unrelated E0-T13/E5-T12 work bundled into the commit, and subsequent working-tree edits, were outside this E6-T01 review.