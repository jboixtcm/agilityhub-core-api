<!-- independent review · task E3-T11 · reviewer claude:claude-opus-5-5 · executor claude · commit 3a8f10c · 2026-09-26 00:29 -->

# Review E3-T11 — minor issues

Round 2 addresses all five organizer points correctly:
- Item 7 now reads «PASS for the member record only», and item 3 is described accurately.
- L66, L68 and L69 are updated as asked.
- «(IdempotencyIT)» is dropped from item 1.
- The four partial items (1, 3, 6, 7) are listed together.

The E3-T17 step references (1–2 the reused dog, 3 the holder's group, 4 the add-dog replay) match `roadmap/tasks/E3-T17.md:41-61`. The cited test lines are correct: `SignupGateFixesIT.java:238-257` and `:251`, `SignupSecurityFixesIT.java:145` and `:187-207`. The *Organizer verification* section is untouched, no product file changed, no `__pycache__` was left behind, and the task file is 77 KB.

## Findings
1. **minor** `roadmap/tasks/E3-T11.md:178,205` — `main` has moved past the audited commit, and the report does not say so. Round 2 was written on 26-09, after `22a8ea9` put E5-T16 (still `awaiting_verification`) on `main`. That commit changes E3 contract items the gate lines rely on:
   - **Snapshot:** `docs/openapi/openapi.json` changed by 134 lines between `d791361` and HEAD. The `Dashboard`/`DashboardKpis` null blocks became `anyOf [$ref, null]` unions (`E3ContractIT.java:252-259`). So the proposed L68 text «OpenAPI snapshot unchanged by the E3-T11 re-run» is true for `d791361`, but not for the `main` the organizer will tick.
   - **Traceability:** E5-T16 renamed `SignupPaymentMethodsIT.T_04_29_T_04_33_…` and `E3GateFixesContractTest.T_04_29_T_04_33_…` to `R_04_19_R_04_25_R_04_26_…`. At HEAD, T-04-29 and T-04-33 each have **1** method (`E3ResponseContractTest` only), not the 3 stated at `:205` and in `trace-summary.md`.
   - **Tests:** `DashboardIT` gained snapshot-conformance asserts.
   - Why it matters: this is the same concern as review #3 (the gate text must describe what is being ticked), now for a task that lands *after* the run. The executor was rightly told not to re-run.
   - Fix: add one line to the report or MESSAGES: «After `d791361`, `main` also holds E5-T16 (awaiting_verification): snapshot changes to the D1 and `GET /club` nulls, and T-04-29/T-04-33 go from 3 methods to 1». Alternatively, the organizer ticks L67/L68 on E3-T17's run (clean verify + snapshot + smoke ×2), which will include E5-T16.
2. **nit** `roadmap/tasks/E3-T11.md:86` — «The rest is E3-T17's» covers all four partial items. For item 6, the missing part (MinIO) is an accepted E3-T09 deviation, not E3-T17 work. L69 (`:179`) has it right: «E3-T17 completes items 1, 3 and 7». Fix: change `:86` to «the rest of items 1, 3 and 7 is E3-T17's».

## Not checked
- **The consistency check was not re-run.** My request to run Python (the import of `trace.py` and the embedded-copy comparison) was denied. Instead I compared the round-2 `AUDIT` strings and intro in `trace.py` with the `trace-summary.md` rows and the copy embedded in the report, by reading the diff. All five changed texts (items 1, 3, 6, 7 and the intro) match word for word.
- **Nothing that needs Docker was re-run** (verify, smoke, seeds, snapshot). Round 2 re-ran nothing either, by design: all results refer to `d791361`, and the first review already checked those logs.
- **CI run `36191167664` was not queried.**
- **E5-T16 was not reviewed for correctness.** I only looked at what it changes in E3 tests and in the snapshot since `d791361`.
