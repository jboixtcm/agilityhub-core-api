<!-- independent review · task E5-T18 · reviewer claude:claude-opus-5-5 · executor claude · commit f0d3a30 · 2026-09-26 02:46 -->

# Review E5-T18 — minor issues

## Findings
1. **minor** `src/test/java/com/agilityhub/core/shared/application/TransactionRetriesTest.java:35` (`R_07_08_R_08_07_theSharedBackoffDrawsFiftyToOneHundredFiftyMilliseconds`): the test is named after two rules whose code it never runs.
   - **What the test covers:** it only checks `TransactionRetries.jitter()`. Only the S05 ring change (`CatalogService.java:48`) and the S06 writers (`SchedulingTransactions.java:32`) call that method.
   - **What the cited rules use:** the S07 writer (`ActivityTransactions.java:37`, R-07-08) and the S08 writer (`BookingTransactions.java:47`, R-08-07) each draw their own `ThreadLocalRandom.current().nextLong(50, 151)`. So does `TrainingTransactions.java:42`.
   - **Consequence:** a regression in the S07 or S08 backoff would not fail a test that carries their rule ids. This is the kind of wrong citation step 2 (review E5-T17 #1, AGENTS rule 5) asked to remove.
   - **Javadoc:** the class Javadoc this task edited (`:12-15`) still says `TrainingTransactions` shares "one backoff range". It does not.
   - **Fix, either way:**
     - make the S07, S08 and S09 writers call `TransactionRetries.jitter()`, so the name becomes true; or
     - name the test after the S05/S06 retry it really covers, and correct the Javadoc.
2. **nit** Other test names that cite rules only loosely:
   - `CatalogServiceRetryTest.java:66` (`R_05_08_…`): the retried ring change covers both deactivating a ring (R-05-07) and turning `allowsFreeTraining` off (R-05-08). See `CatalogService.java:109-110`. The test's patch (`{version: 0}`) is neither, so `R_05_07_R_05_08_` would fit better.
   - `TransactionRetriesTest.java:23` (`R_09_06_R_09_13_…`): R-09-13 is about cross-conflicts via `findActiveBookings`, not the conflict classifier this test checks. R-09-06 alone fits.
   - `SettingsIT` / `ClubDefinitionsIT` (`R_02_06_…`): R-02-06 is «Perfil de país». The blank-clears rule is S02 §3 text, which has no R-id. This is consistent with E5-T17's names, so it is only a note.
3. **nit** Step 5, `club:apply` with an explicit `null`:
   - The step asks to pin "like `""` and `null` … in `club:apply`". The executor pinned `""`, `"   "` and `" \t "`. For `null`, they argued from reading the code, without running anything (assumption 5).
   - The code supports that argument: `ClubDefinitionCodec.read` → `validate` (`:42`, `:62`) checks the definition against the schema, and the schema types `club.taxId` as `"string"` (`seeds/club-definition.schema.json:24`). So a YAML `taxId: null` is refused with `VALIDATION_ERROR`.
   - **Fix:** one test that pins this refusal. It would turn the open question to the organizer into tested behaviour. S02 §3 («… o `null` l'esborren») still needs the organizer to decide what applies to definitions.
4. **nit** `docs/openapi/openapi.json:3621-3623` (`ClubUpdate.taxId`, `SettingsContracts.java:148`): the contract does not match S02 §3.
   - S02 §3 (26-09) says «un valor en blanc (buit o només espais) o `null` l'esborren», and `SettingsIT` pins it.
   - The property is still typed `"string"` (not nullable), and its description never says that blank or `null` clears it. A generated web client therefore cannot send `null`.
   - This task could not fix it (description-only, "no other change" convention). **Follow-up:** add `types = {"string","null"}` and «blank or null clears it», like `displayCity` in the same record (`:149`).
5. **nit** `src/test/java/com/agilityhub/core/configuration/E5PersistenceIT.java:403-417` (`R_15_19_theDeployBackfill…`): the test does not really check the `DEPLOY.md` command.
   - The test re-types the filter and pipeline in Java. If `docs/DEPLOY.md:589` drifts, no test fails, yet the Javadoc says «exactly as written there» and the report says «the IT runs exactly that filter and pipeline».
   - `getModifiedCount()).isPositive()` could be `isEqualTo(1)`.
   - **Fix:** read the `--eval` expression from `DEPLOY.md`, or soften the claim.
6. **nit (for the organizer, not the executor)** Step 1 text vs the mockup: the executor used the task's wording, which is not the mockup's.
   - The approved PNG and HTML (`19-alta-pagament-consentiments-i-enviament.html:197`) read «Autoritzo **a** Club d'Agility Cànic **l'emissió de** rebuts … (**llei** 16/2009 …)».
   - The implemented `ca` text is «Autoritzo [[club_legal_name]] **a emetre** rebuts … (**Llei** 16/2009 …)».
   - The executor flagged this (assumption 1). If the mockup is the source, `ca` could be «Autoritzo a [[club_legal_name]] l'emissió de rebuts…»; the placeholder works in that phrasing too. The organizer should confirm which wording is approved before the web adopts it.

## Not checked
- I ran nothing (read-only session): no `./mvnw clean verify`, no `bin/openapi-snapshot`, no Testcontainers ITs.
  - The evidence looks consistent: before-fix logs `02`/`03` fail on exactly the step 1 and 4 tests, the mutation log `09` fails as claimed, and the totals in the summary `11` match the report (566 + 989).
  - I did not independently confirm that the 5950-line `10-clean-verify.log` matches `target/` output.
- I did not check Mongo 7's error code for a same-name, different-key `createIndexes`. The IT failed before the fix and passes after it, which suggests the path works. I also did not check an index with the right name, key and TTL but extra options (for example a `partialFilterExpression`), which the new check would still accept.
- I did not run the `DEPLOY.md` `mongosh` command against the consumer Compose stack. Its form matches the existing `DEPLOY.md:384` command.
- The organizer's edits bundled in this commit (`docs/specs/S04`, `S05`, `S07`, `CATALEG_PARAMETRES.md`, `CONVENCIONS_API.md`, `roadmap/ROADMAP.md`, E3-T17 and E5-T19/T20/E6-T03 task files) are not reviewed.
