<!-- independent review · task E5-T14 · reviewer codex:gpt-6-astra · executor claude · commit 30823f8 · 2026-09-24 16:57 -->

# Review E5-T14 — minor issues

## Findings

1. **nit** `src/test/java/com/agilityhub/core/clubs/bookings/api/WaitlistIT.java:426` — The two new regression tests at lines 426 and 446 use `R_08_…` rule IDs instead of `T_08_…` test IDs. AGENTS.md rule 5 and the task’s Definition of Done require test-ID naming where applicable. Rename them using `T_08_20` (ALL_AT_ONCE/N-15) and `T_08_21` (FIFO expiry), retaining the rule references in their comments and updating the report’s mappings.

2. **nit** `roadmap/tasks/E5-T14.md:108` — The report links the complete clean-verify and OpenAPI logs but omits their required last 40 output lines; line 138 also omits the exit code for `git diff --stat docs/openapi/`. This falls short of the explicit evidence format in AGENTS.md session protocol §4 and the task’s Verification section. Add the existing log tails and the diff command’s exit code.

## Not checked

- Did not rerun Maven, Docker integration tests, snapshot generation, or the CLI re-anchor scenario because this review is read-only. Recomputed the test summary from existing XML; this checks the saved evidence, not a fresh execution.
- Did not review the unrelated organizer changes to E3 specifications, roadmap tasks, and gate reviews bundled into the same commit.