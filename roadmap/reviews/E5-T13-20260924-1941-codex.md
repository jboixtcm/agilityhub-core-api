<!-- independent review · task E5-T13 · reviewer codex:gpt-6-astra · executor claude · commit c5f2d13 · 2026-09-24 19:41 -->

# Review E5-T13 — minor issues

## Findings

1. **minor** `src/main/java/com/agilityhub/core/platform/application/jobs/JobViews.java:75` — Club views still include platform work in `WOULD_DELETE` when the plan exceeds the 500-item trace limit. The filter subtracts only platform items retained in the trace: 500 tenant items followed by one platform item yields `WOULD_DELETE=501`. This leaves step 6 / AGENTS rule 4 partially implemented. Record tenant/platform totals before truncation and test the ADMIN view with more than 500 items. **Already accepted and deferred to E5-T15 step 12; not a new round-2 blocker.**

2. **minor** `src/main/java/com/agilityhub/core/platform/application/jobs/JobRunner.java:229` — Platform-claim release remains separate from committing `FAILED`. A crash between these operations, or a release-hook exception, leaves the claim held until midnight; reaping does not revisit failed rows. Step 5’s same-day recovery is therefore incomplete. Release atomically with completion, or recover claims whose owning run is failed, with a failure-injection test. **Already accepted and deferred to E5-T15 step 13; not a new round-2 blocker.**

## Not checked

- Did not rerun Maven, Docker/Testcontainers, or `bin/openapi-snapshot`, because they write files or change runtime state. Verification was limited to code, tests, committed logs, summaries, and report-tail comparison.
- Actual provider webhook handling and refund execution remain E8-T04 work and were not validated.