<!-- independent review · task E3-T11 · reviewer claude:claude-opus-5-5 · executor claude · commit ee932bc · 2026-09-25 23:44 -->

# Review E3-T11 — needs changes

## Findings
1. **major** `roadmap/tasks/E3-T11.md:103,117` + `trace.py` (AUDIT item 7) — The report calls audit item 7, «a readmission that is rejected, with the original data intact», **PASS** with no caveat. The test only proves this for the **member** document.
   - `SignupSecurityFixesIT.java:187-207` compares the LEFT member before and after the rejection.
   - The readmission body is `original.deepCopy()` (`:145`), so the dog is resubmitted with the same values. The dog is never read after `reject(id)`.
   - In the product code, `SignupService.prepareDog` (`SignupService.java:402-407`) reuses the LEFT member's INACTIVE dog. At submission it overwrites `name`, `sex`, `breed`, `birthDate` and `instructorNote`, and clears `deactivatedAt`/`deactivationReason`.
   - `reject()` (`:797-802`) restores only the member (`restoreLeft`, `:302-309`). The dog keeps the rejected submission's values, gets a new `deactivatedAt`, and its `signup` block is stamped.
   - `SignupFollowUpFixesIT.java:245` shows it: readmission A renames the dog to «Alpha Dog», A is rejected, and nothing restores the name.
   - E38 (`DECISIONS_PENDENTS.md:300`) says: «Si es rebutja, la fitxa queda exactament com era» and «qualsevol que sabés el DNI … podia reescriure la fitxa». Here, anyone who knows the DNI and the chip can still rewrite the dog record, and a rejection does not undo it.
   - Part of this is intended: `INACTIVE{SIGNUP_REJECTED}` on the dog comes from T-04-19. The descriptive fields and the original `deactivatedAt` are not covered by any rule.
   - The executor's assumption 2 promises to flag partial proofs, as it did for items 1 and 6. Item 7 was missed.
   - **Fix (report only, no product change in this task):**
     - mark item 7 «PASS (member record only)» in the report, `trace-summary.md` and MESSAGES;
     - add a question to @organizer: does E38 also cover the dog record of a readmission? If yes, open a fix task: keep the submitted dog values in the request until validation, or restore them on rejection, plus a test that renames the dog.
   - Until then, L69's «9 of 9 green» should not be ticked as stated.

2. **minor** `SignupGateFixesIT.java:251` (audit item 3) — The report says «the validation stores that plan and the family group» (`E3-T11.md:112`). The test only asserts `familyGroupId` is not null. It does not check that the validated member joined the **holder's** group, or that the holder belongs to it.
   - Fix: soften the wording, or propose a follow-up assertion (`member(id).familyGroupId == member(holder).familyGroupId`), for example in E5-T16.

3. **minor** `E3-T11.md:78,165-171` — The audited commit `d791361` includes E5-T15, which is still `awaiting_verification`. E5-T15 changes product code (ring-change retries, bookings). The executor says this and cites the organizer's request, but the proposed L68/L69 texts («CI green at `d791361`», «re-run green at `d791361`») do not.
   - If E5-T15 comes back `changes_requested` with product edits, those lines no longer describe `main`.
   - Fix: make the L68/L69 text conditional on E5-T15's verification, or have the organizer record that E5-T15 was accepted when the gate is ticked.

4. **nit** `E3-T11.md:149` (proposed L66 text) — «signed upload claimed» suggests S3 on the stack. In fact the smoke uses the local-storage signed route (`bin/e3-smoke:297` requires `/api/v1/signup/uploads?`). S3 is proven only by `SignupS3UploadIT`, on LocalStack.
   - Fix: say «local signed upload claimed (S3: `SignupS3UploadIT` on LocalStack)».

5. **nit** `roadmap/evidence/E3-T11/trace.py` (AUDIT item 1 proof text, reproduced in `trace-summary.md`) — «that route relies on the shared IdempotencyFilter (IdempotencyIT)» suggests `IdempotencyIT` covers the add-dog replay. It does not.
   - `IdempotencyIT` has only generic `E0_T04_*`/`E0_T08_*`/`E3_T06_*` tests on its own test routes.
   - It does not exercise the signup branch (`IdempotencyFilter.java:142`), which is what `POST /me/dogs/signup` uses.
   - The report's own text (question 2) is correct. Only the trace text overstates.
   - Fix: drop «(IdempotencyIT)».

## Not checked
- I did not re-run anything: no `./mvnw -q clean verify`, no `bin/e3-smoke`, no seeds, no `bin/openapi-snapshot` (they need Docker, and my shell access was restricted mid-review).
  - The logs look consistent: `01-clean-verify.log` has no `BUILD FAILURE`, no `Tests run … Failures/Errors ≥1`, and no JaCoCo failure; its one `FAILED` hit is an expected `JobRunner` lease test.
  - The smoke logs have 58 lines each and end with `PASS T-04-34 / T-14-11`.
  - In the seeds log, run 2 shows `0 changes (applied)` and run 4 shows `0 changes` for both the demo seed and the demo planning.
  - `trace-tests.csv` has 236 lines (a header and 235 rows).
- CI run `36191167664` (success at `d791361`): I could not query GitHub.
- I did not check every row of `trace-tests.csv`, and I did not check that each T-xx-nn method asserts its rule. I read only the nine audit-item tests and the product code behind items 4 and 7.
  - Item 4: `review()` lists only PENDING dogs (`SignupService.java:163`), so `/dogs/0` is the added dog, and the test is valid.
- I did not review E5-T15's content. I did not check the 120 KB limit on the task file (`check.py` accepted `awaiting_verification`, so it should be under the limit).
- No secrets found in the evidence: I grepped for `eyJ…`, `Bearer`, `SEED_PASSWORD=` and `password=`. The scripts only pass the password in memory.
