package com.agilityhub.core.platform.application.jobs;

/** S15 R-15-01 cadence; CONTINUOUS jobs run every tick with `scheduledFor = now`. */
public enum Cadence { DAILY, WEEKLY, MONTHLY, CONTINUOUS }
