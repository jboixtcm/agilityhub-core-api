package com.agilityhub.core.platform.application.audit;

/** E0 subset of the closed action list in S14 R-14-09. */
public enum AuditAction {
    PARAMETER_CHANGED,
    CLUB_UPDATED,
    CLUB_MODULES_CHANGED,
    CLUB_STATUS_CHANGED
}
